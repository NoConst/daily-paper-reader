---
title: "HAWK: Head Importance-Aware Visual Token Pruning in Multimodal Models"
title_zh: HAWK：多模态模型中感知注意力头重要性的视觉 Token 剪枝
authors: "Qihui Zhu, Tao Zhang, Yuchen Wang, Zijian Wen, Mengjie Zhang, Shuangwu Chen, Xiaobin Tan, Jian Yang, Yang Liu, Zhenhua Dong, Xianzhi Yu, Yinfei Pan"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07812v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大语言模型中的视觉标记剪枝
tldr: 针对多模态大模型推理中视觉Token过多导致的计算开销问题，本文提出HAWK，一种无须训练的视觉Token剪枝方法。HAWK基于“不同注意力头在视觉处理中重要性不同”的发现，结合头重要性权重与文本引导注意力来识别并保留关键Token。该方法可无缝集成至多种模型，在大幅降低计算延迟和显存占用的同时，保持了极高的任务精度，为实时多模态应用提供了高效方案。
selection_source: fresh_fetch
motivation: 现有剪枝方法忽略了不同注意力头在处理视觉语义时的差异性，通常错误地假设所有头对视觉解释的贡献是均等的。
method: 通过引入注意力头重要性权重并结合文本引导的注意力机制，动态评估并移除冗余的视觉Token。
result: "在Qwen2.5-VL上剪除80.2%的Token后仍能保持96%的精度，且端到端延迟缩减至74.4%。"
conclusion: HAWK证明了感知注意力头重要性对视觉Token剪枝的关键作用，是一种高效且通用的多模态模型加速方案。
---

## 摘要
在多模态大语言模型（MLLMs）中，视觉 Token 的激增显著增加了推理时间和计算开销，使其在实时或资源受限的应用场景中变得不切实际。视觉 Token 剪枝是通过移除冗余视觉 Token 来降低 MLLM 推理成本的一种极具前景的策略。现有研究通常假设所有注意力头对视觉解释的贡献是均等的。然而，我们的研究表明，不同的注意力头可能会捕捉到不同的视觉语义，并在视觉处理中固有地发挥着截然不同的作用。基于这一观察，我们提出了 HAWK，一种感知注意力头重要性的视觉 Token 剪枝方法，该方法通过感知注意力头在视觉任务中的不同重要性，以最大限度地保留关键 Token。通过利用注意力头重要性权重和文本引导的注意力来评估视觉 Token 的显著性，HAWK 有效地保留了与任务相关的视觉 Token，同时移除了冗余 Token。所提出的 HAWK 完全无需训练，并可无缝应用于各种 MLLM。在多个主流视觉语言基准测试上的广泛实验表明，HAWK 达到了最先进的准确率。当应用于 Qwen2.5-VL 时，HAWK 在剪枝了 80.2% 的视觉 Token 后，仍保留了原始准确率的 96.0%。此外，它将端到端延迟降低至原始的 74.4%，并进一步减少了所测试模型中的 GPU 显存占用。代码可在 https://github.com/peppery77/HAWK.git 获取。

## Abstract
In multimodal large language models (MLLMs), the surge of visual tokens significantly increases the inference time and computational overhead, making them impractical for real-time or resource-constrained applications. Visual token pruning is a promising strategy for reducing the cost of MLLM inference by removing redundant visual tokens. Existing research usually assumes that all attention heads contribute equally to the visual interpretation. However, our study reveals that different heads may capture distinct visual semantics and inherently play distinct roles in visual processing. In light of this observation, we propose HAWK, a head importance-aware visual token pruning method that perceives the varying importance of attention heads in visual tasks to maximize the retention of crucial tokens. By leveraging head importance weights and text-guided attention to assess visual token significance, HAWK effectively retains task-relevant visual tokens while removing redundant ones. The proposed HAWK is entirely training-free and can be seamlessly applied to various MLLMs. Extensive experiments on multiple mainstream vision-language benchmarks demonstrate that HAWK achieves state-of-the-art accuracy. When applied to Qwen2.5-VL, HAWK retains 96.0% of the original accuracy after pruning 80.2% of the visual tokens. Additionally, it reduces end-to-end latency to 74.4% of the original and further decreases GPU memory usage across the tested models. The code is available at https://github.com/peppery77/HAWK.git.

---

## 论文详细总结（自动生成）

### HAWK：多模态模型中感知注意力头重要性的视觉 Token 剪枝总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：多模态大语言模型（MLLM）在处理高分辨率图像或长视频时，会产生大量的视觉 Token（常达数百甚至数千个）。由于 Transformer 的计算复杂度随序列长度呈平方增长，过多的视觉 Token 导致推理延迟高、显存占用大，限制了实时应用。
*   **核心问题**：现有的视觉 Token 剪枝方法（如基于相似度或注意力的方法）通常假设所有注意力头对视觉信息的贡献是均等的。
*   **研究动机**：作者通过实验发现，MLLM 中不同的注意力头在视觉处理中扮演的角色截然不同，某些头对视觉理解至关重要，而另一些则可能编码冗余信息或噪声。因此，忽略头重要性的差异会导致误删关键信息。

#### 2. 方法论
HAWK 是一种**无需训练（Training-free）**的剪枝方法，其核心思想是结合“静态头重要性”与“动态文本引导注意力”来评估 Token 的显著性。
*   **静态视觉头重要性权重（Static Head Importance）**：
    *   通过一次性的离线分析，系统性地屏蔽（Ablation）单个注意力头对视觉 Token 的访问，测量模型在基准测试上的性能下降程度。
    *   性能下降越明显的头，其权重 $w_i$ 越高。
*   **动态文本引导注意力分数（Dynamic Text-Guided Attention）**：
    *   利用 LLM 第一层注意力机制中的 Query（来自文本）和 Key（来自视觉）计算相关性矩阵。
    *   **去位置偏置**：在计算时故意省略旋转位置编码（RoPE），以确保注意力仅取决于文本与视觉之间的语义对应关系。
*   **头重要性感知剪枝**：
    *   将每个头的动态注意力分数与其静态重要性权重加权求和，得到每个视觉 Token 的最终重要性得分。
    *   根据预设的剪枝比例，保留得分最高的 Top-K 个 Token。

#### 3. 实验设计
*   **数据集/场景**：
    *   **图像基准（10个）**：包括通用推理（POPE, MME, HallBench, ScienceQA-IMG, RealWorldQA, CCBench）和文本/图表导向（TextVQA, OCRVQA, ChartQA, AI2D）任务。
    *   **视频基准（2个）**：Video-MME 和 WorldSense。
*   **对比方法（Baselines）**：
    *   注意力基础方法：FastV。
    *   相似度基础方法：DivPrune。
    *   混合方法：CDPruner。
    *   其他：VisionZip, DART（基于微调）。
*   **测试模型**：Qwen2.5-VL-7B, InternVL3-8B。

#### 4. 资源与算力
*   **硬件环境**：使用 8 张 GPU（每张 96GB 显存）进行基准测试评估；效率分析在单张 GPU 上完成。
*   **计算开销**：
    *   **推理阶段**：完全无需额外训练或微调。
    *   **离线阶段**：计算头重要性权重的成本极低。在 8 张 GPU 上，单个数据集的分析耗时仅需 0.39 至 0.83 小时。相比之下，基于微调的方法通常需要数百小时。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 12 个主流基准测试，并在两种最先进的开源模型架构上进行了验证。
*   **维度丰富**：实验不仅对比了固定分辨率输入，还测试了原生分辨率（Native-Resolution）和视频流输入下的表现。
*   **消融实验**：针对头重要性权重、文本引导策略、RoPE 处理等核心组件进行了详细的消融实验，验证了各模块的必要性。
*   **客观性**：通过在多个不同类型的任务（如 OCR、空间推理、幻觉检测）上取得一致的 SOTA 结果，证明了该方法的鲁棒性和公平性。

#### 6. 主要结论与发现
*   **性能卓越**：在 Qwen2.5-VL 上，剪掉 80.2% 的视觉 Token 后，HAWK 仍能保持 96.0% 的原始精度，显著优于现有方法。
*   **效率提升**：在 80% 剪枝率下，端到端推理延迟缩短至原始的 74.4%（加速 1.34x），KV Cache 占用大幅减少，显存占用也有所下降。
*   **通用性**：不同模型（LLaVA, Qwen）的注意力头重要性分布具有内在一致性，证明了该发现具有普适性。

#### 7. 优点
*   **即插即用**：无需任何参数微调，能够直接应用于现有的预训练 MLLM。
*   **感知差异性**：首次量化并利用了注意力头在视觉处理中的重要性差异，填补了现有剪枝研究的空白。
*   **鲁棒性强**：在极端剪枝比例（如 90%）下，性能下降远小于同类方法，尤其在处理复杂图表和文本识别任务时优势明显。

#### 8. 不足与局限
*   **离线依赖**：虽然开销很小，但仍需针对新模型进行一次性的离线头重要性评估。
*   **任务偏置风险**：头重要性的计算依赖于选定的离线数据集，如果离线数据集与实际应用场景偏差过大，可能会影响权重分配的准确性。
*   **层级限制**：目前主要在 LLM 的早期层进行剪枝决策，尚未深入探讨跨层动态调整剪枝比例的可能性。

（完）
