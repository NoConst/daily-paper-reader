---
title: "Visual Inception: Compromising Long-term Planning in Agentic Recommenders via Multimodal Memory Poisoning"
title_zh: 视觉植入：通过多模态记忆投毒破坏智能体推荐系统中的长期规划
authors: Jiachen Qian
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16966v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 智能体推荐系统中的多模态记忆投毒
tldr: 本研究揭示了智能体推荐系统在长期记忆中的安全隐患，提出了一种名为“视觉植入”的攻击方法，通过在图像中注入触发器来劫持系统的长期规划。为应对此威胁，研究者设计了CognitiveGuard防御框架，利用扩散模型净化输入并结合反事实推理验证记忆一致性。实验证明该攻击具有极高成功率，而所提防御方案能在保持系统性能的同时显著降低风险。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.16966v1/fig-001.webp\", \"caption\": \"Figure 5: Comparison of defense effectiveness (including the No Defense baseline and defense methods, aligned with Table 1). COGNITIVEGUARD reduces Goal-Hit Rate (GHR) from 85.1% to 9.7%, outperforming baseline defenses including Input Filtering, Output Moderation, DiffPure-Only, Retrieval Adversarial Training, TrustRAG-Hybrid, and Provenance-Track.\", \"page\": 6, \"index\": 1, \"width\": 458, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2604.16966v1/fig-002.webp\", \"caption\": \"Table 1: Main results on ShopBench-Agent (mean ± std over 5 runs). GHR: Goal-Hit Rate (%), the broad outputlevel target-hit proxy; ASR-M: Memory-mediated Attack Success Rate (%), the stricter attributed subset of goal-hit cases; HD: Hijack Depth; RCS: Reasoning Consistency Score. Upload-time reports one-time per-image preprocessing overhead; Query-time reports per-request overhead and excludes the one-time upload-time cost. †With k=5 memories. ‡Adapted from text-only settings. CG-Parallel: parallel queries in the parallel setting. CGLite: LLaMA-8B with k=3.\", \"page\": 6, \"index\": 2, \"width\": 434, \"height\": 602}, {\"url\": \"assets/figures/arxiv/2604.16966v1/fig-003.webp\", \"caption\": \"Figure 4: System 1: Diffusion-based perceptual sanitization. (Left) Forward diffusion adds calibrated noise to the input image. (Right) Reverse diffusion reconstructs a purified version, removing adversarial perturbations while preserving semantic content. Stopping uses a benign reference-anchored dual criterion: purified embedding distance to benign centroid and purification-run stability.\", \"page\": 4, \"index\": 3, \"width\": 458, \"height\": 460}]"
motivation: 智能体推荐系统对长期记忆的深度依赖使其容易受到多模态记忆污染攻击，从而导致自主规划被恶意操控。
method: 提出“视觉植入”攻击及CognitiveGuard防御框架，后者通过感知净化和推理验证的双重机制拦截恶意触发器。
result: "攻击在模拟电商环境中达到了85%的成功率，而CognitiveGuard将其降至10%左右，且仅增加可控的延迟。"
conclusion: 针对智能体长期记忆的攻击具有隐蔽性和高效性，必须构建结合感知与逻辑校验的防御体系以确保系统安全。
---

## 摘要
从静态排序模型向智能体推荐系统（Agentic RecSys）的演进，使 AI 智能体能够维护长期用户画像并自主规划服务任务。虽然这种范式转变增强了个性化，但也引入了一个漏洞：对长期记忆（LTM）的依赖。在本文中，我们揭示了一种被称为“视觉植入”（Visual Inception）的威胁。与寻求即时误分类的传统对抗性攻击不同，“视觉植入”在用户上传的图像（如生活照）中注入触发器，这些触发器在系统记忆中充当“潜伏特工”。当在未来的规划过程中被检索时，这些被投毒的记忆会劫持智能体的推理链，在无需提示词注入的情况下将其引导至攻击者定义的目标（例如推广高利润产品）。为了缓解这一问题，我们提出了 CognitiveGuard，这是一个受人类认知启发的双过程防御框架。它由用于清洗感官输入的系统 1 感知净化器（基于扩散模型的净化）和用于检测记忆驱动规划中异常情况的系统 2 推理验证器（反事实一致性检查）组成。在模拟电子商务智能体环境中的广泛实验表明，“视觉植入”达到了约 85% 的目标达成率（GHR），而 CognitiveGuard 在不降低质量的情况下，通过可配置的延迟权衡（轻量模式约 1.5 秒，完整顺序验证约 6.5 秒），将该风险降低至约 10%。

## Abstract
The evolution from static ranking models to Agentic Recommender Systems (Agentic RecSys) empowers AI agents to maintain long-term user profiles and autonomously plan service tasks. While this paradigm shift enhances personalization, it introduces a vulnerability: reliance on Long-term Memory (LTM). In this paper, we uncover a threat termed "Visual Inception." Unlike traditional adversarial attacks that seek immediate misclassification, Visual Inception injects triggers into user-uploaded images (e.g., lifestyle photos) that act as "sleeper agents" within the system's memory. When retrieved during future planning, these poisoned memories hijack the agent's reasoning chain, steering it toward adversary-defined goals (e.g., promoting high-margin products) without prompt injection. To mitigate this, we propose CognitiveGuard, a dual-process defense framework inspired by human cognition. It consists of a System 1 Perceptual Sanitizer (diffusion-based purification) to cleanse sensory inputs and a System 2 Reasoning Verifier (counterfactual consistency checks) to detect anomalies in memory-driven planning. Extensive experiments on a mock e-commerce agent environment demonstrate that Visual Inception achieves about 85% Goal-Hit Rate (GHR), while CognitiveGuard reduces this risk to around 10% with configurable latency trade-offs (about 1.5s in lite mode to about 6.5s for full sequential verification), without quality degradation under our setup.

---

## 论文详细总结（自动生成）

这是一份关于论文《Visual Inception: Compromising Long-term Planning in Agentic Recommenders via Multimodal Memory Poisoning》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
随着推荐系统从静态排序转向基于大语言模型（LLM）的**智能体推荐系统（Agentic RecSys）**，系统开始依赖**长期记忆（LTM）**来维护用户画像并进行自主任务规划。
*   **核心问题**：论文揭示了一个名为“**视觉植入（Visual Inception）**”的新型安全威胁。
*   **研究背景**：传统的对抗性攻击通常追求即时的误分类，而该攻击利用了 RAG（检索增强生成）内存库的漏洞。攻击者通过在用户上传的看似无害的图像中注入“语义触发器”，使其作为“潜伏特工”存在于记忆中。当智能体在未来进行长期规划并检索到这些记忆时，其推理链会被劫持，从而在没有直接提示词注入的情况下，诱导智能体执行攻击者预设的目标（如定向推广特定品牌）。

### 2. 方法论：核心思想与技术细节
论文提出了攻击模型和相应的防御框架：

#### A. 视觉植入攻击（Visual Inception）
*   **核心思想**：通过单层多目标优化生成对抗图像 $I_{adv}$，使其在视觉上与原图无异，但在嵌入空间中靠近目标概念。
*   **技术细节**：
    *   **优化目标**：最小化组合损失函数 $L = L_{ret} + \lambda_1 L_{semantic} + \lambda_2 L_{perceptual}$。
    *   **检索损失 ($L_{ret}$)**：使用 Softmax 代理函数提高图像在未来特定查询下的检索概率。
    *   **语义损失 ($L_{semantic}$)**：强制图像嵌入向量与目标概念（如“高利润家具”）对齐。
    *   **感知损失 ($L_{perceptual}$)**：利用 LPIPS 保持图像的视觉质量，确保隐蔽性。
    *   **查询采样**：通过 GPT-4 生成多样化的未来查询样本进行离线优化，增强攻击的泛化性。

#### B. 防御框架：CognitiveGuard
受人类认知双过程理论启发，该框架分为两个系统：
*   **系统 1：感知净化器（Perceptual Sanitizer）**：在图像存入记忆前，利用**扩散模型（Diffusion-based purification）**进行去噪和重构。其关键在于“良性参考锚定准则”，确保净化后的图像向用户已有的良性记忆分布靠拢。
*   **系统 2：推理验证器（Reasoning Verifier）**：在检索阶段进行**反事实一致性检查**。
    *   **推理发散得分 (RDS)**：计算“包含该记忆”与“移除该记忆”两种情况下智能体规划输出的语义差异。
    *   **逻辑校验**：若 RDS 超过阈值且 LLM 判定该记忆与当前上下文逻辑不符，则将其标记为攻击点。

### 3. 实验设计
*   **数据集/场景**：构建了 **ShopBench-Agent** 基准环境，涵盖三个领域：电子商务（500 会话）、室内设计（300 会话）和旅游规划（200 会话）。
*   **智能体配置**：使用 LLaMA-3.2-Vision-90B 和 GPT-4V 作为后端，配合 FAISS 向量数据库。
*   **对比方法**：
    *   基础防御：输入过滤（Input Filter）、输出审核（Output Mod）。
    *   先进防御：DiffPure（仅净化）、检索对抗训练（Ret. Adv. Train）、TrustRAG（文本 RAG 防御迁移）、来源追踪（Provenance-Track）。
*   **评估指标**：目标达成率 (GHR)、记忆介导攻击成功率 (ASR-M)、推理一致性得分 (RCS) 和隐蔽性评分。

### 4. 资源与算力
*   **模型使用**：实验涉及 LLaMA-3.2-Vision-90B、GPT-4V、SigLIP-SO400M 和 OpenCLIP-ViT-G 等大规模模型。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **延迟数据**：提供了详细的推理延迟分析。系统 1 净化每张图约 0.3s；系统 2 完整验证在顺序模式下约 6.5s，并行模式下可优化至 1.5s-2.1s。

### 5. 实验数量与充分性
*   **实验规模**：在三个领域共进行了 1000 个测试会话，每组实验运行 5 次取平均值，提供了标准差。
*   **充分性分析**：
    *   **消融实验**：分别验证了系统 1 和系统 2 的独立贡献。
    *   **迁移性测试**：测试了跨不同视觉编码器（CLIP vs SigLIP）和不同 LLM 后端的攻击效果。
    *   **鲁棒性测试**：分析了查询分布偏移（Query Distribution Shift）对攻击的影响。
    *   **对抗性评估**：针对防御系统设计了“白盒自适应攻击”。
*   **客观性**：实验设计较为全面，不仅关注攻击成功率，还通过反事实控制和人工校验来验证攻击的因果归因，确保 ASR-M 指标的客观性。

### 6. 主要结论与发现
*   **攻击极具威胁**：在无防御情况下，视觉植入攻击的 GHR 达到 85.1%，且具有极强的隐蔽性（潜伏期长）。
*   **传统防御失效**：输入过滤和来源追踪对语义层面的投毒几乎无效，因为攻击图像在格式和来源上是合法的。
*   **CognitiveGuard 有效性**：该框架将 ASR-M 从 82.3% 降低至 8.3%，同时保持了 0.89 的推理一致性，证明了双过程防御的必要性。
*   **迁移性规律**：攻击在相似架构的编码器间迁移效果好，但在自监督模型（如 DINOv2）上存在显著衰减。

### 7. 优点（亮点）
*   **视角新颖**：首次将攻击目标从“即时分类错误”转向“劫持长期自主规划”，符合智能体系统的特性。
*   **防御机制科学**：借鉴认知科学（系统 1 与系统 2），结合了底层的感知净化和高层的逻辑校验，形成了深度防御体系。
*   **因果归因严谨**：引入反事实推理来定义和检测攻击，解决了 RAG 系统中“相关性不等于因果性”的评估难题。

### 8. 不足与局限
*   **延迟挑战**：系统 2 的推理验证带来了显著的计算开销（6.5s），在实时推荐场景中可能面临性能瓶颈。
*   **环境模拟性**：ShopBench-Agent 仍是模拟环境，真实生产环境中的用户行为多样性和经济博弈（如攻击成本）未被完全模拟。
*   **模态局限**：目前仅关注视觉模态，未探讨音频或视频记忆投毒的可能性。
*   **冷启动依赖**：系统 1 的净化依赖于用户的良性记忆历史，对于新用户（缺乏历史数据）的防御效果可能打折扣。

（完）
