---
title: "ReDiPrune: Relevance-Diversity Pre-Projection Token Pruning for Efficient Multimodal LLMs"
title_zh: ReDiPrune：面向高效多模态大语言模型的相关性-多样性投影前 Token 剪枝
authors: "An Yu, Ting Yu Tsai, Zhenfei Zhang, Weiheng Lu, Felix X. -F. Ye, Ming-Ching Chang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24680v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 用于高效多模态大语言模型的令牌剪枝
tldr: 针对多模态大模型视觉Token过多导致的计算负担，本文提出ReDiPrune。这是一种无需训练的剪枝方法，在视觉编码器与投影层之间运行。它通过结合文本相关性和多样性评分，在保留细粒度语义的同时剔除冗余Token。该方法即插即用，在多个视频和图像基准上显著提升了推理效率并保持甚至提高了模型精度。
selection_source: fresh_fetch
motivation: 旨在降低多模态大模型处理海量视觉Token时的计算成本，同时保留关键的细粒度空间和语义信息。
method: 提出一种在投影层前执行的剪枝策略，利用轻量化规则综合评估Token的文本相关性与最大最小多样性。
result: "在EgoSchema等多个基准上，仅保留15%的Token即可实现2%的精度提升，并减少超过6倍的计算量。"
conclusion: ReDiPrune证明了在投影前进行Token筛选能有效平衡多模态模型的性能与效率，且具备极强的通用性。
---

## 摘要
近期的多模态大语言模型计算开销巨大，因为 Transformer 必须处理大量的视觉 Token。我们提出了 ReDiPrune，这是一种在视觉-语言投影器之前应用的无需训练的 Token 剪枝方法，此时视觉特征仍保持着丰富的判别性。与在压缩表示上运行的投影后剪枝方法不同，ReDiPrune 直接从视觉编码器输出中选择信息丰富的 Token，从而保留了细粒度的空间和语义线索。每个 Token 都由一个轻量级规则进行评分，该规则共同考虑了文本条件相关性和最大最小多样性，确保所选 Token 既与查询相关又不冗余。ReDiPrune 完全是即插即用的，不需要重新训练或修改架构，并且可以无缝插入到编码器和投影器之间。在四个视频和五个图像基准测试中，它一致地改善了准确率与效率之间的权衡。例如，在 EgoSchema 基准测试中，使用 LLaVA-NeXT-Video-7B 模型，仅保留 15% 的视觉 Token 即可获得 +2.0% 的绝对准确率提升，同时将 TFLOPs 计算量减少 6 倍以上。代码可在 https://github.com/UA-CVML/ReDiPrune 获取。

## Abstract
Recent multimodal large language models are computationally expensive because Transformers must process a large number of visual tokens. We present \textbf{ReDiPrune}, a training-free token pruning method applied before the vision-language projector, where visual features remain rich and discriminative. Unlike post-projection pruning methods that operate on compressed representations, ReDiPrune selects informative tokens directly from vision encoder outputs, preserving fine-grained spatial and semantic cues. Each token is scored by a lightweight rule that jointly consider text-conditioned relevance and max-min diversity, ensuring the selected tokens are both query-relevant and non-redundant. ReDiPrune is fully plug-and-play, requiring no retraining or architectural modifications, and can be seamlessly inserted between the encoder and projector. Across four video and five image benchmarks, it consistently improves the accuracy-efficiency trade-off. For example, on EgoSchema with LLaVA-NeXT-Video-7B, retaining only 15\% of visual tokens yields a +2.0\% absolute accuracy gain while reducing computation by more than $6\times$ in TFLOPs. Code is available at https://github.com/UA-CVML/ReDiPrune.

---

## 论文详细总结（自动生成）

### 论文总结：ReDiPrune —— 面向高效多模态大语言模型的相关性-多样性投影前 Token 剪枝

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLMs）在处理高分辨率图像或长视频时，视觉编码器产生的 Token 数量巨大，导致 Transformer 注意力机制的计算开销呈平方级增长，严重限制了推理速度并增加了显存负担。
*   **现有挑战**：目前的 Token 剪枝方法大多在“投影后”（Post-projection）进行，此时视觉特征已被压缩到 LLM 的嵌入空间，容易丢失细粒度的空间和语义细节。
*   **研究动机**：提出一种在“投影前”（Pre-projection）运行的剪枝策略，在视觉特征最丰富、最具判别性的阶段进行筛选，以实现更高的效率与精度的平衡。

#### 2. 方法论：核心思想与关键技术
ReDiPrune 是一种**无需训练（Training-free）**、**即插即用**的模块，位于视觉编码器和投影层（Projector）之间。其核心思想是通过一个统一的目标函数，同时优化 Token 的**查询相关性**和**视觉多样性**。

*   **核心步骤**：
    1.  **查询嵌入构建**：从用户输入的文本 Prompt 中提取 Token 嵌入，通过加权聚合（如指数加权）生成一个归一化的查询向量 $q$。
    2.  **文本相关性评分**：计算每个视觉 Token 与查询向量 $q$ 之间的余弦相似度，识别出对回答问题最有潜力的区域。
    3.  **视觉多样性计算**：计算 Token 之间的余弦不相似度，确保选出的 Token 集合能够覆盖图像/视频帧的不同视觉内容，减少冗余。
    4.  **候选预过滤**：设置阈值 $\tau$，初步筛选出相关性较高的 Token 候选池。
    5.  **统一目标函数与贪婪求解**：采用贪婪算法迭代选择 Token，每次选择都能最大化“当前 Token 与已选集合的最小距离（多样性）”与“文本相关性”的加权和。
*   **算法流程**：输入原始视觉特征 $\rightarrow$ 构建文本查询 $\rightarrow$ 逐帧计算相关性与多样性 $\rightarrow$ 贪婪筛选子集 $\rightarrow$ 仅将保留的 Token 送入投影层和 LLM。

#### 3. 实验设计
*   **数据集/场景**：
    *   **视频理解**：NextQA, EgoSchema, ActivityNet-QA, Video-ChatGPT。
    *   **图像理解**：GQA, MMBench (MMB), MME, POPE, ScienceQA-IMG (SQA)。
*   **基准模型（Backbones）**：Video-LLaVA-7B, LLaVA-NeXT-Video-7B, LLaVA-1.5-7B。
*   **对比方法**：
    *   **训练无关方法**：DivPrune（仅考虑多样性）、CDPruner（投影后条件多样性）、FastV、VTW、DART。
    *   **需训练/微调方法**：TRIM、PruMerge、PruMerge+。

#### 4. 资源与算力
*   **硬件环境**：实验在 **4 × NVIDIA A100 (80GB) GPUs** 上进行。
*   **训练时长**：由于该方法是 **Training-free（无需训练）** 的，因此不存在训练时长。
*   **推理设置**：Batch size 为 1，使用 `lmmsevals` 框架进行评估，LLM 评测指标通过调用 `gpt-3.5-turbo` API 获取。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 9 个主流多模态基准测试，跨越视频和图像两个领域。
*   **消融实验**：详细探讨了不同的查询加权策略（如均匀加权、自注意力加权、指数加权等）对性能的影响。
*   **定性分析**：通过注意力图可视化展示了剪枝后模型如何更聚焦于关键帧和关键区域。
*   **客观性与公平性**：在相同的计算预算（TFLOPs）和 Token 保留率（如 10% 或 15%）下与最新方法进行对比，实验设计较为严谨、客观。

#### 6. 主要结论与发现
*   **效率显著提升**：在 ActivityNet-QA 上，将 Token 从 2056 减至 206，计算量（TFLOPs）从 11.61 降至 1.17，延迟缩短约 3 倍。
*   **精度不降反升**：在 EgoSchema 任务中，使用 LLaVA-NeXT-Video-7B 仅保留 15% Token，准确率反而提升了 **2.0%**。这表明剪枝起到了“去噪”作用，帮助模型排除了无关视觉信息的干扰。
*   **投影前剪枝的优越性**：相比于投影后剪枝，ReDiPrune 能更好地保留细粒度语义，在极低保留率（如 5%-10%）下表现更稳健。

#### 7. 优点
*   **即插即用**：无需对原始模型进行任何重训练或架构修改，适配性强。
*   **计算前置**：在投影层之前剪枝，不仅减少了 LLM 的负担，也减少了投影层本身的计算开销。
*   **双重引导**：结合了文本导向（相关性）和视觉导向（多样性），避免了盲目剪枝导致的语义缺失。

#### 8. 不足与局限
*   **架构局限**：目前主要在 LLaVA 系列架构上验证，对于其他非 Transformer 架构或不同模态融合方式的模型（如基于 Q-Former 的模型）适配性有待验证。
*   **时空优化不足**：目前的剪枝主要基于“逐帧”进行，尚未充分利用视频序列中的全局时空冗余（例如跨帧的 Token 合并）。
*   **超参数敏感性**：虽然提供了默认配置，但针对不同任务，相关性与多样性的平衡系数 $\alpha$ 以及保留率 $r$ 可能仍需手动微调以达到最优效果。

（完）
