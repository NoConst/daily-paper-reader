---
title: Unified and Efficient Approach for Multi-Vector Similarity Search
title_zh: 多向量相似性搜索的统一高效方法
authors: "Binhan Yang, Yuxiang Zeng, Hengxin Zhang, Zhuanglin Zheng, Yunzhen Chi, Yongxin Tong, Ke Xu"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02815v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 用于细粒度语义检索的多向量相似度搜索
tldr: 针对多向量相似性搜索中现有方法依赖单向量索引导致的效率与召回率权衡难题，本文提出了首个原生多向量分层图索引MV-HNSW。该方法通过引入满足对称性与鲁棒性的新型边权重函数、加速相似度计算算法及增强搜索策略，克服了传统过滤-细化框架的局限性。实验证明，MV-HNSW在保持高召回率的同时，显著提升了搜索速度，最高可达现有方法的14倍。
selection_source: fresh_fetch
motivation: 现有方法将多向量对象拆分为独立向量并依赖单向量索引，导致在处理向量间关联时面临召回率与计算成本的权衡困境。
method: 设计了首个原生多向量分层图索引MV-HNSW，引入了新型边权重函数、加速计算算法及可动态发现相关候选的增强搜索策略。
result: "在七个真实数据集上的实验显示，MV-HNSW在维持90%以上召回率的同时，搜索延迟比现有最先进方法降低了高达14倍。"
conclusion: MV-HNSW通过原生图索引架构有效解决了多向量检索的效率瓶颈，为细粒度语义检索提供了高效统一的解决方案。
---

## 摘要
多向量相似性搜索在许多实际应用中的细粒度语义检索中至关重要，它提供了比传统单向量范式更丰富的表示。由于缺乏原生的多向量索引，现有方法依赖于构建在单向量索引之上的“过滤-细化”框架。通过孤立地处理每个多向量对象中的标记向量并忽略它们之间的相关性，这些方法面临着一个固有的困境：激进的过滤会牺牲召回率，而保守的过滤则在细化阶段产生极高的计算成本。为了解决这一局限性，我们提出了 MV-HNSW，这是首个专为多向量数据设计的原生分层图索引。MV-HNSW 引入了一种新型边权重函数，该函数满足基于图索引的基本属性（对称性、基数鲁棒性和查询一致性），以及一种加速的多向量相似性计算算法和一种能够动态发现拓扑上不连通但相关的候选对象的增强搜索策略。在七个真实世界数据集上的广泛实验表明，MV-HNSW 实现了最先进的搜索性能，在保持 90% 以上召回率的同时，与现有方法相比，搜索延迟降低了高达 14.0 倍。

## Abstract
Multi-Vector Similarity Search is essential for fine-grained semantic retrieval in many real-world applications, offering richer representations than traditional single-vector paradigms. Due to the lack of native multi-vector index, existing methods rely on a filter-and-refine framework built upon single-vector indexes. By treating token vectors within each multi-vector object in isolation and ignoring their correlations, these methods face an inherent dilemma: aggressive filtering sacrifices recall, while conservative filtering incurs prohibitive computational cost during refinement. To address this limitation, we propose MV-HNSW, the first native hierarchical graph index designed for multi-vector data. MV-HNSW introduces a novel edge-weight function that satisfies essential properties (symmetry, cardinality robustness, and query consistency) for graph-based indexing, an accelerated multi-vector similarity computation algorithm, and an augmented search strategy that dynamically discovers topologically disconnected yet relevant candidates. Extensive experiments on seven real-world datasets show that MV-HNSW achieves state-of-the-art search performance, maintaining over 90% recall while reducing search latency by up to 14.0$\times$ compared to existing methods.

---

## 论文详细总结（自动生成）

### 论文总结：多向量相似性搜索的统一高效方法 (MV-HNSW)

#### 1. 核心问题与整体含义（研究动机和背景）
随着检索增强生成（RAG）和大型语言模型（LLM）的发展，传统的**单向量检索**（将整个文档压缩为一个向量）因语义稀释难以捕捉细粒度信息。**多向量表示**（如 ColBERT，将文档分解为多个 token 向量）能提供更精确的语义匹配，但面临计算瓶颈。
*   **现有局限**：目前主流方法采用“过滤-细化”（filter-and-refine）框架，依赖单向量索引。这种做法孤立处理 token 向量，忽略了对象内部的相关性，导致在“激进过滤（损失召回率）”与“保守过滤（计算成本极高）”之间存在固有矛盾。
*   **研究目标**：开发一种原生的多向量图索引，直接在多向量层面进行导航和检索，以打破效率与召回率的权衡困境。

#### 2. 方法论：核心思想与关键技术
论文提出了 **MV-HNSW**，这是首个专为多向量数据设计的原生分层图索引。
*   **新型边权重函数**：为使 HNSW 适用于多向量，定义了必须满足的三个属性：**对称性**、**基数鲁棒性**（不受向量数量影响）和**查询一致性**。提出公式：$f(u, v) = \frac{1}{2} \left( \frac{USim(u, v)}{|u|} + \frac{USim(v, u)}{|v|} \right)$，其中 $USim$ 是统一多向量相似度函数。
*   **加速相似度计算**：引入基于聚类的过滤-细化策略。在计算两个多向量相似度时，先对查询向量进行 k-means 聚类，通过质心进行初步筛选，再在缩小的候选集中进行精细计算，显著降低了索引构建和搜索时的计算开销。
*   **增强搜索策略（Augmented Search）**：针对多向量结构复杂导致的“拓扑断裂”问题（即相关节点在图中不连通），引入了**辅助导航表（ANT）**。在搜索过程中，除了遍历邻居节点，还会根据 ANT 动态扩展候选列表，发现拓扑上遥远但语义相关的节点。

#### 3. 实验设计
*   **数据集**：使用了 7 个基于 LoTTE 和 MS MARCO 的真实世界数据集（包括 Recreation, Lifestyle, Science, Technology 等），规模从 2.6 亿到 88 亿个 token 向量不等。
*   **Benchmark 与对比方法**：
    *   **ColBERT / ColBERTv2**：多向量检索的基准方法。
    *   **XTR**：Google 提出的基于 token 检索的框架。
    *   **WARP**：目前最先进的基于 IVF 索引的多向量检索方法。
    *   **Qdrant**：知名的向量数据库（在完整论文中有所对比）。
*   **评价指标**：召回率（Recall）、搜索延迟（Latency）、索引构建时间及索引大小。

#### 4. 资源与算力
*   **硬件环境**：服务器配备 32 核 AMD Ryzen 9 5950X (3.40GHz) 处理器，128 GB RAM。
*   **算力细节**：实验主要在 **CPU** 环境下运行。索引构建利用了多线程并行，而搜索性能测试在单线程环境下进行。
*   **GPU 使用**：文中未提及使用 GPU 进行加速，强调了该方法在通用 CPU 硬件上的高效性。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从千万级到十亿级不同维度的 7 个数据集，进行了包括不同 $k$ 值（Top-k）、不同向量维度 $d$、不同对象长度 $c$ 的全方位测试。
*   **消融实验**：专门验证了“加速相似度计算”和“增强搜索策略”两个模块的有效性。
*   **充分性评价**：实验设计非常充分且客观。通过 Recall-Time 曲线直观展示了性能增益，并对比了多种不同技术路线的 SOTA 方法，证明了其在各种参数变化下的稳健性。

#### 6. 主要结论与发现
*   **性能飞跃**：在保持 90% 以上高召回率的前提下，MV-HNSW 的搜索延迟比现有最快的方法降低了 **5.6 至 14.0 倍**。
*   **稳定性**：随着数据集规模和查询参数 $k$ 的增加，MV-HNSW 的召回率保持稳定，延迟增长呈亚线性，表现出极佳的可扩展性。
*   **原生优势**：证明了原生多向量索引在处理复杂语义关联时，远优于基于单向量索引修改的方案。

#### 7. 优点与亮点
*   **理论严谨**：首次形式化定义了多向量图索引边权重函数所需满足的三大核心属性，并给出了数学证明。
*   **架构创新**：打破了传统的“过滤-细化”两阶段范式，实现了端到端的多向量图导航。
*   **实用性强**：在不依赖昂贵 GPU 的情况下，在 CPU 上实现了极高性能，适合大规模工业化部署。

#### 8. 不足与局限
*   **索引构建成本**：相比于 ColBERTv2 或 WARP 等采用残差压缩或简单倒排索引的方法，MV-HNSW 的索引构建时间较长，索引占用的存储空间也相对较大（约为对比方法的 2-5 倍）。
*   **内存依赖**：作为基于 HNSW 的变体，该方法对内存容量有较高要求，在处理超大规模数据时可能面临内存瓶颈。
*   **动态更新**：论文主要关注静态数据集的检索性能，未详细讨论频繁插入/删除节点时的索引维护开销。

（完）
