---
title: "Storing Less, Finding More: How Novelty Filtering Improves Cross-Modal Retrieval on Edge Cameras"
title_zh: 存得更少，找得更多：新颖性过滤如何提升边缘摄像头的跨模态检索
authors: Sherif Abdelwahab
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29631v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 利用新颖性过滤和VLP模型改进跨模态检索
tldr: 针对边缘摄像头持续视频流产生的冗余数据降低跨模态检索效率的问题，本文提出一种流式检索架构。通过在设备端使用 epsilon-net 过滤器仅保留语义新颖的帧，构建去噪嵌入索引，并结合云端重排序机制补偿轻量级编码器的对齐不足。该方法在多个视觉语言模型和数据集上均优于传统离线采样方案，在极低功耗下显著提升了检索精度。
selection_source: fresh_fetch
motivation: 边缘摄像头产生的冗余视频帧会挤占检索结果中的正确项，导致跨模态检索的 Top-k 性能下降。
method: 采用设备端 epsilon-net 过滤器筛选新颖帧，并结合跨模态适配器与云端重排序器优化检索流程。
result: "在两个自我中心数据集上，该架构使用 8M 参数编码器实现了 45.6% 的 Hit@5，且功耗仅为 2.7 mW。"
conclusion: 通过在边缘端进行语义新颖性过滤，可以在减少存储开销的同时显著提升跨模态检索的准确性与效率。
---

## 摘要
常开式边缘摄像头产生连续的视频流，其中的冗余帧会将正确结果挤出前 k 个搜索结果，从而降低跨模态检索的性能。本文提出了一种流式检索架构：设备端 epsilon-net 过滤器仅保留语义新颖的帧，从而构建去噪的嵌入索引；跨模态适配器和云端重排序器则弥补了紧凑型编码器对齐能力弱的问题。在两个第一视角数据集（AEA、EPIC-KITCHENS）上，针对八个视觉语言模型（8M-632M）的测试表明，单次流式过滤器的表现优于离线替代方案（k-means、最远点采样、均匀采样、随机采样）。结合这些技术，该架构在使用 8M 设备端编码器的情况下，在留出数据上达到了 45.6% 的 Hit@5，且估计功耗仅为 2.7 mW。

## Abstract
Always-on edge cameras generate continuous video streams where redundant frames degrade cross-modal retrieval by crowding correct results out of top-k search. This paper presents a streaming retrieval architecture: an on-device epsilon-net filter retains only semantically novel frames, building a denoised embedding index; a cross-modal adapter and cloud re-ranker compensate for the compact encoder's weak alignment. A single-pass streaming filter outperforms offline alternatives (k-means, farthest-point, uniform, random) across eight vision-language models (8M-632M) on two egocentric datasets (AEA, EPIC-KITCHENS). Combined, the architecture reaches 45.6% Hit@5 on held-out data using an 8M on-device encoder at an estimated 2.7 mW.

---

## 论文详细总结（自动生成）

以下是对论文《Storing Less, Finding More: How Novelty Filtering Improves Cross-Modal Retrieval on Edge Cameras》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：常开式边缘设备（如AR眼镜、执法记录仪）产生海量视频流，存储和传输成本极高。
*   **核心问题**：冗余帧不仅浪费空间，还会导致跨模态检索（如用文字搜视频）性能下降。在 Top-k 搜索中，大量相似的冗余帧会挤占正确结果的名额。
*   **整体含义**：本文证明了在边缘端进行“新颖性过滤”（Novelty Filtering）不仅能减少数据量，还能通过消除几何偏差来显著提升检索准确率，使低功耗的小型模型也能实现高效检索。

### 2. 论文提出的方法论
该架构将检索分为边缘端处理和云端搜索四个阶段：
*   **核心思想**：利用 $\epsilon$-net 理论进行流式过滤，仅保留语义上新颖的帧。
*   **关键技术细节**：
    1.  **设备端编码器**：将每一帧映射为紧凑的嵌入向量（如 8M 参数模型）。
    2.  **流式 $\epsilon$-net 过滤器**：计算当前帧嵌入向量 $v_i$ 与已保留集合 $M$ 中向量的最大余弦相似度。若相似度小于阈值 $\tau$，则保留该帧。这是一种单次扫描算法，无需回溯。
    3.  **跨模态适配器（LiT Adapter）**：在云端或配对手机上运行，通过一个轻量级 MLP 将文本查询投影到图像嵌入空间，增强图文对齐。
    4.  **云端重排序器（Re-ranker）**：使用大型模型对初步筛选出的候选帧进行二次打分，补偿边缘端小模型的精度损失。

### 3. 实验设计
*   **数据集**：
    *   **AEA (Aria Everyday Activities)**：包含 19 个记录，553 个事件，涵盖 5 个地点。
    *   **EPIC-KITCHENS**：用于测试短时动作（中位数 1.7s）的泛化性。
*   **Benchmark 与对比方法**：
    *   **对比策略**：全量存储（Full）、K-Means 聚类、最远点采样（FP）、均匀采样（Uniform）、随机采样（Random）。
    *   **评估模型**：涵盖 8 个视觉语言模型，参数量从 8M（TinyCLIP）到 632M（ImageBind）。
    *   **评价指标**：Hit@5（检索出的前 5 个结果中是否包含正确时间戳前后的帧）。

### 4. 资源与算力
*   **边缘端算力**：在 Hexagon v69 NPU（约 2.5 TOPS）上进行了分析。TinyCLIP 模型推理耗时 2.16 ms/帧，仅占用约 1% 的 NPU 周期。
*   **功耗估计**：设备端（编码器+过滤器）估计功耗仅为 **2.7 mW**。
*   **训练细节**：LiT 适配器仅 525K 参数，在 10,911 个图文对上进行微调。文中未详细列出具体的 GPU 型号和总训练时长，但强调了适配器和微调过程的轻量性。

### 5. 实验数量与充分性
*   **实验规模**：针对 8 种不同架构和训练范式的模型进行了交叉验证，确保了结论的普适性。
*   **消融实验**：分别验证了过滤、适配器、领域微调以及云端重排序对性能的贡献。
*   **客观性**：通过自助法（Bootstrap）重采样（1000 次试验）确认了新颖性过滤增益的稳定性。实验对比了流式算法与多种离线算法，证明了流式过滤在受限条件下的优越性。

### 6. 论文的主要结论与发现
*   **去噪提升检索**：移除语义冗余帧能显著提升 Top-k 检索精度，对对齐能力较弱的小模型提升尤为明显（如 MobileCLIP 提升 8.9%）。
*   **流式处理足够高效**：单次扫描的新颖性过滤器在 7/8 的模型中优于离线 K-Means。
*   **端到端性能**：结合过滤、适配器和云端重排序，8M 模型在留出数据上达到了 45.6% 的 Hit@5，且数据传输量减少了 14 倍。
*   **隐私保护**：仅传输嵌入向量而非原始图像，从设计上保护了视觉隐私。

### 7. 优点
*   **实用性强**：针对电池供电的边缘设备设计，极大地降低了带宽需求（从 45 MB/min 降至 43 KB/min），使蓝牙同步成为可能。
*   **模块化设计**：四个阶段相互独立，编码器或重排序器可以随技术进步独立升级。
*   **理论支撑**：将冗余问题转化为几何偏差问题，并利用 $\epsilon$-net 理论提供覆盖保证。

### 8. 不足与局限
*   **阈值敏感性**：过滤阈值 $\tau$ 需根据数据集事件的平均时长进行调整，缺乏自适应机制。
*   **场景局限**：实验数据集主要集中在厨房场景，在视觉多样性更高的户外或工业环境中的表现尚待验证。
*   **时间重入问题**：目前的过滤器无法识别“回到之前访问过的场景”，可能会导致重复存储。
*   **训练数据量**：微调实验中存在一定的过拟合风险（训练集与测试集表现存在 23% 的差距），需要更多样化的数据来弥合。

（完）
