---
title: "MM-CondChain: A Programmatically Verified Benchmark for Visually Grounded Deep Compositional Reasoning"
title_zh: MM-CondChain：一个用于视觉定位深度组合推理的程序化验证基准测试
authors: "Haozhan Shen, Shilin Yan, Hongwei Xue, Shuaiqi Lu, Xiaojun Tang, Guannan Zhang, Tiancheng Zhao, Jianwei Yin"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12266v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大模型中视觉接地深度组合推理的基准
tldr: 针对多模态大模型在处理复杂视觉工作流时缺乏深层组合推理能力的问题，本文提出了MM-CondChain基准测试。该基准通过多层推理链构建，每层包含基于视觉证据的复杂组合条件。研究者采用一种包含规划器、可验证程序中间表示（VPIR）和合成器的代理合成流水线，确保了数据的可验证性。实验涵盖自然图像、图表和GUI领域，结果表明现有模型在处理深层逻辑分支时仍面临巨大挑战。
selection_source: fresh_fetch
motivation: 现有基准测试主要关注浅层组合，缺乏对需要多步逻辑判断和深层嵌套条件的复杂视觉推理任务的评估。
method: 开发了一套代理合成流水线，利用VPIR确保每层视觉条件的机械可验证性，并自动组装成多层推理链。
result: 最强模型在Path F1指标上仅获得53.33分，且表现随推理深度和逻辑复杂度的增加而大幅下降。
conclusion: 深层组合推理是当前多模态大模型的一个核心瓶颈，MM-CondChain为该领域的研究提供了高难度的评测基准。
---

## 摘要
多模态大语言模型 (MLLMs) 越来越多地被用于执行视觉工作流，例如导航图形用户界面 (GUI)，其中下一步取决于经过验证的视觉组合条件（例如，“如果出现权限对话框且界面颜色为绿色，则点击允许”），且该过程可能会产生分支或提前终止。然而，这种能力仍未得到充分评估：现有的基准测试侧重于浅层组合或独立约束，而非深度链式的组合条件。在本文中，我们介绍了 MM-CondChain，这是一个用于视觉定位深度组合推理的基准测试。每个基准实例都被组织为一个多层推理链，其中每一层都包含一个基于视觉证据、由多个对象、属性或关系构建的非平凡组合条件。为了正确回答，MLLM 必须详细感知图像，在每一步对多个视觉元素进行推理，并遵循生成的执行路径到达最终结果。为了可扩展地构建此类工作流式数据，我们提出了一种智能体合成流水线：规划器 (Planner) 编排组合条件的逐层生成，而可验证程序化中间表示 (VPIR) 则确保每一层的条件都是可机械验证的。随后，合成器 (Composer) 将这些经过验证的层组装成完整的指令。利用该流水线，我们构建了涵盖三个视觉领域的基准测试：自然图像、数据图表和 GUI 轨迹。对一系列 MLLM 的实验表明，即使是最强的模型也仅获得了 53.33 的 Path F1 分数，且在面对困难负样本以及随着深度或谓词复杂度的增加时，性能会急剧下降，这证实了深度组合推理仍然是一个根本性的挑战。

## Abstract
Multimodal Large Language Models (MLLMs) are increasingly used to carry out visual workflows such as navigating GUIs, where the next step depends on verified visual compositional conditions (e.g., "if a permission dialog appears and the color of the interface is green, click Allow") and the process may branch or terminate early. Yet this capability remains under-evaluated: existing benchmarks focus on shallow-compositions or independent-constraints rather than deeply chained compositional conditionals. In this paper, we introduce MM-CondChain, a benchmark for visually grounded deep compositional reasoning. Each benchmark instance is organized as a multi-layer reasoning chain, where every layer contains a non-trivial compositional condition grounded in visual evidence and built from multiple objects, attributes, or relations. To answer correctly, an MLLM must perceive the image in detail, reason over multiple visual elements at each step, and follow the resulting execution path to the final outcome. To scalably construct such workflow-style data, we propose an agentic synthesis pipeline: a Planner orchestrates layer-by-layer generation of compositional conditions, while a Verifiable Programmatic Intermediate Representation (VPIR) ensures each layer's condition is mechanically verifiable. A Composer then assembles these verified layers into complete instructions. Using this pipeline, we construct benchmarks across three visual domains: natural images, data charts, and GUI trajectories. Experiments on a range of MLLMs show that even the strongest model attains only 53.33 Path F1, with sharp drops on hard negatives and as depth or predicate complexity grows, confirming that deep compositional reasoning remains a fundamental challenge.

---

## 论文详细总结（自动生成）

以下是对论文《MM-CondChain: A Programmatically Verified Benchmark for Visually Grounded Deep Compositional Reasoning》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态大模型（MLLM）在处理复杂的视觉工作流（如GUI导航、自动驾驶决策）时，是否具备**深度组合推理能力**？
*   **研究动机**：
    *   **现有基准局限性**：目前的评测主要集中在浅层组合（如“红色的球”）或独立指令约束，缺乏对多层嵌套、逻辑分支且每一步都需视觉验证的复杂链式推理的评估。
    *   **现实需求**：实际应用中，下一步操作往往取决于前一步视觉条件的验证结果（例如：“如果对话框弹出且按钮是绿色的，则点击；否则...”），这要求模型能处理深度的逻辑链条。

### 2. 核心方法论
论文提出了一套基于**智能体合成流水线**的构建方法，核心思想是**逻辑构建与语言表达解耦**：
*   **VPIR（可验证程序化中间表示）**：这是该方法的核心。它不直接生成自然语言指令，而是先生成类似 Python 的谓词逻辑（如 `color == 'red' and position == 'left'`），通过在提取的视觉事实（JSON格式）上运行代码来机械地验证真值。
*   **智能体合成流水线流程**：
    1.  **规划器 (Planner)**：决定推理链的走向（扩展、结束或回滚）。
    2.  **事实提取器 (Fact Extractor)**：从图像中提取结构化的视觉事实。
    3.  **VPIR 生成器**：生成一对逻辑谓词（一个为真，一个为经过微小扰动的假）。
    4.  **翻译器 (Translator)**：将验证过的逻辑谓词转化为自然语言。
    5.  **专用验证器 (Verifier)**：进行两阶段质检（事实准确性与语言表达的一致性）。
    6.  **合成器 (Composer)**：组装成“真路径”（所有条件均满足）和“假路径”（其中一层条件不满足，需提前终止）的配对样本。

### 3. 实验设计
*   **数据集与场景**：涵盖三个视觉领域：
    *   **自然图像**：基于 SAM 和 GQA 数据集。
    *   **数据图表**：基于 ChartQA，涉及数值统计和趋势推理。
    *   **GUI 轨迹**：基于 AITZ/AITW，涉及多帧截图和动作序列。
*   **Benchmark 规模**：共 975 个原始样本，每个样本包含一对“真/假路径”实例，总计 1950 个测试用例。
*   **对比方法**：评估了 10 余种主流 MLLM，包括：
    *   **开源模型**：Qwen3/3.5 系列、InternVL3/3.5、GLM-4.6V、Kimi-K2.5 等。
    *   **闭源模型**：GPT-4o、GPT-5 (预览版)、Gemini-2.5/3 系列。
*   **评测指标**：True-path Accuracy、False-path Accuracy 以及综合两者的 **Path F1**。

### 4. 资源与算力
*   **模型训练**：论文未涉及模型训练，主要侧重于基准测试的构建与推理评估。
*   **数据生成算力**：论文明确提到使用 **Gemini-3-Pro** 作为合成流水线中的核心智能体（规划器、验证器等）来生成数据。
*   **推理算力**：未详细列出具体的 GPU 型号和数量，但由于是零样本（Zero-shot）推理评估，主要消耗在于调用各模型的 API 或本地推理显存。

### 5. 实验数量与充分性
*   **实验规模**：测试了从 0.8B 到近 400B 参数规模的多种模型，涵盖了目前最顶尖的闭源和开源模型。
*   **消融实验**：
    *   **链条深度 (Depth)**：对比了 2、4、6 层深度的表现。
    *   **谓词复杂度 (Complexity)**：对比了简单谓词与复杂嵌套谓词的影响。
*   **客观性与公平性**：采用程序化生成的客观选择题（MCQ），避免了“LLM-as-a-judge”带来的主观偏差；通过配对的“硬负样本”（Hard Negatives）设计，确保模型必须真正理解视觉细节而非靠猜测。

### 6. 主要结论与发现
*   **性能瓶颈**：即使是最强的模型（Gemini-3-Pro），在 Path F1 指标上也仅达到 **53.33**，远未达到饱和。
*   **“真路径”偏见**：多数模型在“真路径”上表现良好，但在“假路径”（需要识别出某个条件不满足并提前退出）上表现极差，说明模型倾向于盲目遵循指令而忽略了视觉验证。
*   **复杂度敏感**：随着推理深度增加或单层逻辑复杂度提升，模型性能呈线性或更剧烈的下降。
*   **领域差异**：GUI 领域最为困难，因为其涉及多帧时序信息和复杂的界面状态。

### 7. 优点与亮点
*   **机械可验证性**：通过 VPIR 确保了 Ground Truth 的绝对准确，解决了复杂指令遵循任务中答案难以自动判定的痛点。
*   **硬负样本设计**：通过微小扰动生成的假路径样本，极大地提升了基准测试的区分度。
*   **通用框架**：VPIR 框架具有良好的领域通用性，可以轻松扩展到医疗、工业等其他视觉领域。

### 8. 不足与局限
*   **样本规模**：总样本量（975对）相对较小，可能不足以支撑大规模的微调训练。
*   **生成模型依赖**：基准测试的质量高度依赖于用于生成的智能体（如 Gemini-3-Pro），可能存在特定模型的逻辑偏好或潜在的系统性错误。
*   **静态评估**：目前仅限于静态图像或预录轨迹的离线评估，尚未扩展到实时的交互式环境。

（完）
