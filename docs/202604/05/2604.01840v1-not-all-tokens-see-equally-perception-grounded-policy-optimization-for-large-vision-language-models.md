---
title: "Not All Tokens See Equally: Perception-Grounded Policy Optimization for Large Vision-Language Models"
title_zh: 并非所有 Token 都能同等程度地“看见”：大语言-视觉模型的感知驱动策略优化
authors: "Zekai Ye, Qiming Li, Xiaocheng Feng, Ruihan Chen, Ziming Li, Haoyu Ren, Kun Chen, Dandan Tu, Bing Qin"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01840v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 大型视觉语言模型的感知落地策略优化
tldr: 针对大视觉语言模型在强化学习中对所有Token分配相同奖励导致视觉感知信号稀释的问题，本文提出了感知导向策略优化（PGPO）。该方法通过量化Token对视觉输入的依赖程度，动态调整Token级别的优势函数，强化关键视觉推理步骤的学习。实验表明，PGPO在多个多模态推理基准上显著提升了模型性能，有效减少了梯度方差并增强了推理的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的强化学习框架对所有Token平均分配奖励，忽略了不同Token对视觉信息依赖程度的差异，导致视觉感知信号被语言先验噪声稀释。
method: 提出PGPO框架，利用KL散度衡量Token的视觉依赖度，并通过阈值门控机制动态重塑Token级别的优势函数，放大视觉相关Token的学习信号。
result: "在Qwen2.5-VL系列模型上的实验显示，PGPO在七个多模态推理基准测试中平均性能提升了18.7%。"
conclusion: PGPO通过精细化的信用分配，有效解决了多模态强化学习中的梯度方差和训练崩溃问题，是实现鲁棒感知推理的有效手段。
---

## 摘要
尽管基于可验证奖励的强化学习（RLVR）提升了大语言-视觉模型（LVLMs）的推理能力，但现有框架存在一个根本性的方法论缺陷：通过将相同的优势（advantages）分配给所有生成的 token，这些方法本质上稀释了优化多模态推理中关键视觉驱动步骤所需的学习信号。为了弥补这一差距，我们提出了“Token 视觉依赖度”（Token Visual Dependency），通过计算视觉条件预测分布与纯文本预测分布之间的 KL 散度，来量化视觉输入的因果信息增益。研究发现这种依赖度具有高度稀疏性且在语义上至关重要，据此我们引入了感知驱动策略优化（PGPO），这是一种新型的细粒度信用分配框架，能够在 token 级别动态重塑优势。通过一种阈值门控且质量守恒的机制，PGPO 主动放大视觉依赖型 token 的学习信号，同时抑制来自语言先验的梯度噪声。基于 Qwen2.5-VL 系列在七个具有挑战性的多模态推理基准测试上的广泛实验表明，PGPO 平均将模型性能提升了 18.7%。理论和实证分析均证实，PGPO 有效降低了梯度方差，防止了训练崩溃，并作为一种强有力的正则化手段，促进了鲁棒的、基于感知的多模态推理。

## Abstract
While Reinforcement Learning from Verifiable Rewards (RLVR) has advanced reasoning in Large Vision-Language Models (LVLMs), prevailing frameworks suffer from a foundational methodological flaw: by distributing identical advantages across all generated tokens, these methods inherently dilute the learning signals essential for optimizing the critical, visually-grounded steps of multimodal reasoning. To bridge this gap, we formulate \textit{Token Visual Dependency}, quantifying the causal information gain of visual inputs via the Kullback-Leibler (KL) divergence between visual-conditioned and text-only predictive distributions. Revealing that this dependency is highly sparse and semantically pivotal, we introduce Perception-Grounded Policy Optimization (PGPO), which is a novel fine-grained credit assignment framework that dynamically reshapes advantages at the token level. Through a threshold-gated, mass-conserving mechanism, PGPO actively amplifies learning signals for visually-dependent tokens while suppressing gradient noise from linguistic priors. Extensive experiments based on the Qwen2.5-VL series across seven challenging multimodal reasoning benchmarks demonstrate that PGPO boosts models by 18.7% on average. Both theoretical and empirical analyses confirm that PGPO effectively reduces gradient variance, prevents training collapse, and acts as a potent regularizer for robust, perception-grounded multimodal reasoning. Code will be published on https://github.com/Yzk1114/PGPO.

---

## 论文详细总结（自动生成）

以下是对论文《Not All Tokens See Equally: Perception-Grounded Policy Optimization for Large Vision-Language Models》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态强化学习（RLVR）框架（如 GRPO）在优化大视觉语言模型（LVLM）时存在**信用分配（Credit Assignment）不均**的问题。
*   **研究背景**：在多模态推理任务中，并非所有生成的 token 都依赖视觉信息。例如，推导过程中的逻辑连接词主要依赖语言先验，而关键的数值或几何属性则高度依赖图像。
*   **动机**：传统的 RL 方法将相同的奖励/优势（Advantage）均匀分配给序列中的所有 token，这会稀释关键视觉推理步骤的学习信号，并引入语言先验带来的梯度噪声，导致训练不稳定或“训练崩溃”（Training Collapse）。

### 2. 方法论：感知驱动策略优化 (PGPO)
PGPO 的核心思想是根据每个 token 对视觉信息的依赖程度，动态调整其在强化学习中的权重。
*   **Token 视觉依赖度量化**：利用 **KL 散度**衡量“有图像输入”和“无图像输入”两种情况下模型预测分布的差异。差异越大，说明该 token 越依赖视觉感知（即“因果信息增益”）。
*   **优势重塑机制**：
    1.  **对数压缩与归一化**：对原始 KL 散度进行对数处理并进行序列内的 Min-Max 归一化，得到平滑的依赖得分 $I_t$。
    2.  **阈值门控函数**：设置阈值 $\tau$，将 token 分为“视觉依赖型”和“噪声型”。对于依赖型 token 放大其学习信号（乘上增强系数 $\beta$），对于噪声型 token 则抑制其梯度。
    3.  **质量守恒归一化**：为了保证训练稳定性，PGPO 采用**总和保持（Sum-preserving）**的归一化方法，确保重塑后的总优势量与原始序列一致，避免梯度爆炸。
*   **算法流程**：在每次策略迭代中，进行两次前向传播（一次带图像，一次通过注意力掩码屏蔽图像），计算 token 级别的视觉依赖度，随后重塑优势函数并更新策略。

### 3. 实验设计
*   **基础模型**：Qwen2.5-VL-3B 和 Qwen2.5-VL-7B。
*   **数据集**：训练使用 ViRL39K（包含 3.9 万个多模态推理问题）。
*   **评估基准（7个）**：DynaMath（动态数学）、Geo3k（几何）、MathVerse（视觉数学）、MathVision（竞赛数学）、MMK12（基础教育）、LogicVista（逻辑推理）、MMMU-Pro（大学水平多学科推理）。
*   **对比方法**：GRPO（基准）、DAPO（开源 RL 系统）、PAPO（感知感知优化）、VPPO（Token 感知优化）以及 MM-Eureka、VL-Rethinker 等 SOTA 模型。

### 4. 资源与算力
*   **硬件环境**：使用了 **NVIDIA H100 80GB GPU** 集群。
*   **训练时长**：以 Qwen2.5-VL-3B 为例，总训练时长约为 **28.1 小时**。
*   **计算开销**：由于需要额外的一次不带图像的前向传播，PGPO 相比基准方法（DAPO）引入了约 **10.6% 的额外计算开销**，但换取了显著的性能提升。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个主流推理基准上进行了全面测试，并额外在 MMBench、MMStar 等通用 VQA 任务上验证了泛化性。
*   **消融实验**：详细探讨了“仅抑制”、“仅增强”、“无归一化”等变体，验证了各组件的必要性。
*   **超参数分析**：对阈值 $\tau$ 和增强系数 $\beta$ 进行了敏感性分析。
*   **客观性**：所有评估均采用**精确匹配（Exact Match）**而非 LLM 打分，确保了结果的客观性和可重复性。

### 6. 主要结论与发现
*   **性能飞跃**：PGPO 在多个基准上刷新了记录，平均性能提升达 **18.7%**，尤其在几何和视觉依赖型数学题上表现突出。
*   **训练稳定性**：理论证明并实验观察到，PGPO 有效降低了梯度方差，解决了 DAPO 等方法在训练后期容易出现的熵爆炸和性能崩溃问题。
*   **感知增强**：训练过程中，模型生成的 token 视觉依赖度稳步上升，证明模型确实学会了更有效地利用视觉证据进行推理。

### 7. 优点与亮点
*   **理论扎实**：基于信息论（贝叶斯惊喜）定义视觉依赖度，并提供了关于梯度方差和质量守恒的数学证明。
*   **细粒度控制**：打破了传统 RL 序列级奖励的局限，实现了 token 级别的精准信用分配。
*   **即插即用**：该方法可以轻松集成到现有的 GRPO 或 PPO 框架中，具有较强的通用性。

### 8. 不足与局限
*   **计算成本**：虽然开销可控（10%），但仍需两次前向传播，在大规模预训练阶段可能会增加成本。
*   **超参数依赖**：阈值 $\tau$ 和增强系数 $\beta$ 可能需要针对不同的模型架构或任务分布进行微调。
*   **模型规模限制**：实验目前最高验证到 7B 参数规模，在超大规模模型（如 70B+）上的表现尚待验证。

（完）
