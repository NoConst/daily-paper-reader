---
title: "Perceptio: Perception Enhanced Vision Language Models via Spatial Token Generation"
title_zh: Perceptio：通过空间 Token 生成增强感知的视觉语言模型
authors: "Yuchen Li, Amanmeet Garg, Shalini Chaudhuri, Rui Zhao, Garin Kessler"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18795v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 通过空间标记增强感知能力的视觉语言模型
tldr: Perceptio 是一种增强感知的多模态大模型，旨在解决 LVLM 在细粒度空间定位方面的不足。它通过在自回归序列中生成显式的 2D 语义分割和 3D 深度标记，实现了空间思维链。该模型利用 VQVAE 蒸馏深度信息并集成 SAM2 分割能力，在 RefCOCO、HardBLINK 等多个基准测试中取得领先性能，显著提升了模型的空间推理和理解能力。
selection_source: fresh_fetch
motivation: 现有的多模态大模型在处理细粒度空间定位时，由于缺乏显式的几何解释能力，难以进行精确的 2D 和 3D 空间推理。
method: 通过引入 VQVAE 深度标记和 SAM2 语义分割标记，并结合复合损失函数与软合并技术，使模型能够在回答前先生成显式的空间感知标记。
result: "在 RefCOCO 系列任务中 cIoU 提升显著，HardBLINK 空间理解准确率提高 10.3%，MMBench 准确率提升 1.0%，均达到 SOTA 水平。"
conclusion: 显式的空间思维链（Spatial Chain-of-Thought）能有效增强多模态大模型的空间定位与感知能力。
---

## 摘要
大型视觉语言模型 (LVLMs) 在语义理解方面表现出色，但在细粒度空间定位方面面临挑战，因为模型必须在不产生空间解释的情况下隐式推断复杂的几何结构。我们提出了 Perceptio，这是一种具有 2D 和 3D 空间推理能力的感知增强型 LVLM，通过在自回归序列中直接生成的显式语义分割 Token 和深度 Token 来实现。具体而言，我们 (i) 从强大的单目教师模型中蒸馏出 VQVAE 深度码本，将密集深度标记化为紧凑序列；(ii) 在 LLM 中集成基于 SAM2 的语义分割 Token 和 VQ-VAE 深度 Token，使模型先输出空间 Token 再进行回答。为了稳定深度 Token 的生成，我们引入了新型复合深度 Token 目标函数（标记、Token 和计数损失）以及一种用于可微重建的软合并技术。我们在不同数据集上采用了多任务协同训练策略，让模型学习感知 Token 以应对多个下游任务。基于 InternVL，Perceptio 在各项基准测试中均达到了最先进的性能：在 RefCOCO/+/g 上将指代词分割提高了 +0.8/+1.4/+1.1 cIoU，在 HardBLINK 空间理解准确率上提高了 10.3%，在 MMBench 准确率上提高了 1.0%，证明了显式的空间思维链 (spatial chain-of-thought) 能够实质性地增强 LVLM 的空间定位能力。

## Abstract
Large Vision Language Models (LVLMs) excel at semantic understanding but struggle with fine grained spatial grounding, as the model must implicitly infer complex geometry without ever producing a spatial interpretation. We present Perceptio, a perception enhanced LVLM with 2D and 3D spatial reasoning abilities, enabled via explicit semantic segmentation tokens and depth tokens generated directly within the autoregressive sequence. Concretely, we (i) distill a VQVAE depth codebook from a strong monocular teacher to tokenize dense depth into compact sequences, and (ii) integrate SAM2 based semantic segmentation tokens and VQ-VAE depth tokens inside the LLM so the model first emits spatial tokens and then answers. To stabilize depth token generation, we introduce novel composite depth-token objectives (marker, token, and count losses) and a soft-merging technique for differentiable reconstruction. We adopt a multi-task co-training strategy across diverse datasets, letting the model learn perception tokens to tackle multiple downstream tasks. Building on InternVL, Perceptio achieves state-of-the-art performance across benchmarks: improving referring expression segmentation by +0.8/+1.4/+1.1 cIoU on RefCOCO/+/g HardBLINK spatial understanding accuracy by 10.3%, and MMBench accuracy by 1.0%, demonstrating that explicit spatial chain-of-thought materially strengthens spatial grounding in LVLMs.

---

## 论文详细总结（自动生成）

### Perceptio: 通过空间 Token 生成增强感知的视觉语言模型 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
现代大型视觉语言模型（LVLM，如 InternVL、Qwen-VL）虽然在语义理解和对话方面表现出色，但在**细粒度空间定位和 3D 几何推理**（如深度、距离、比例）方面仍然吃力。
*   **痛点**：现有模型通常将图像特征池化后直接生成文本，缺乏显式的空间解释过程。即使是强大的模型，在简单的空间关系判断（如“哪个点离摄像头更近”）上往往表现得像随机猜测。
*   **核心思想**：论文提出 **Perceptio**，通过在自回归序列中引入显式的 **2D 语义分割 Token** 和 **3D 离散深度 Token**，构建一种“空间思维链（Spatial Chain-of-Thought）”。模型在回答问题前，必须先“感知”并输出物体的形状和深度，从而增强后续文本生成的准确性。

#### 2. 方法论：核心思想与关键技术
Perceptio 基于 InternVL-2.5 架构，集成了 SAM2（分割）和 Depth Anything V2（深度）的能力。
*   **感知增强生成序列**：输出格式被强制设定为：`[分割 Token] -> [深度 Token 序列] -> [文本答案]`。这种顺序让模型在生成答案前先锚定空间坐标。
*   **深度标记化（Depth Tokenization）**：
    *   使用 **VQ-VAE** 将连续深度图蒸馏并离散化为码本（Codebook）。
    *   深度图被划分为 $10 \times 10$ 的网格，生成 100 个深度 Token。
*   **复合深度损失函数**：为了稳定 Token 生成，引入了三种损失：
    1.  **Marker Loss**：确保深度序列的起始和结束标记位置正确。
    2.  **Token Loss**：确保生成的深度索引值准确。
    3.  **Count Loss**：惩罚生成长度不符的序列。
*   **软合并技术（Soft-merging）**：在训练中，通过对码本嵌入进行概率加权求和（Softmax），实现从离散 Token 到连续深度图的可微重建，从而允许端到端训练。
*   **2D 分割集成**：利用特殊的 `[seg]` Token 激活 SAM2 解码器，生成与查询相关的掩码。

#### 3. 实验设计
*   **数据集**：
    *   **训练集**：约 110 万个图像-文本对，包括 LLaVA-1.5 指令微调数据、RefCOCO 系列（分割）、以及作者自创的 5.6 万个包含深度 Token 的联合数据集。
    *   **评估基准**：
        *   **指代词分割**：RefCOCO、RefCOCO+、RefCOCOg。
        *   **空间推理**：HardBLINK（评估相对深度准确率）。
        *   **综合能力**：MME、MMBench、SEED-Bench。
        *   **科学/图表 VQA**：AI2D、MMStar、ScienceQA。
*   **对比方法**：包括 LLaVA-1.5、InternVL2、PixelLM、GLaMM、Sa2VA 以及专门针对深度的 LLaVA-Aurora。

#### 4. 资源与算力
*   **硬件**：使用了 **64 台 NVIDIA A100 GPU**。
*   **时长**：训练耗时约 **24 小时**。
*   **配置**：采用 LoRA 微调（Rank=256），最大序列长度 8,192，有效 Batch Size 为 512。

#### 5. 实验数量与充分性
*   **实验规模**：论文在超过 10 个主流基准测试上进行了评估，涵盖了从像素级分割到高层逻辑推理的多个维度。
*   **消融实验**：
    *   **感知源消融**：分别移除深度 Token 或分割 Token，验证了深度 Token 对 3D 推理（HardBLINK 准确率从 71% 掉至 45%）和分割 Token 对通用 VQA 的必要性。
    *   **损失函数消融**：验证了复合深度损失和重建损失对性能的增益。
*   **评价**：实验设计全面且对比公平，通过消融实验清晰地展示了各组件的贡献，具有较高的客观性。

#### 6. 主要结论与发现
*   **SOTA 性能**：Perceptio-8B 在 RefCOCO/+/g 上刷新了纪录，并在 HardBLINK 空间推理任务上比之前的最佳模型（LLaVA-Aurora）高出 **10.3%**。
*   **空间思维链的有效性**：显式生成感知 Token 确实能帮助 LLM “理解”物理世界，而不仅仅是拟合文本概率。
*   **多任务协同**：2D 分割和 3D 深度是互补的，同时优化两者比单一任务效果更好。

#### 7. 优点
*   **统一框架**：首次在单个自回归序列中同时优化 2D 语义和 3D 几何感知。
*   **可微重建**：通过软合并技术解决了离散 Token 训练难的问题，使深度学习更稳定。
*   **推理成本低**：虽然生成了额外 Token，但相比专门的感知插件，其推理开销增加极小（FLOPs 增加不明显）。

#### 8. 不足与局限
*   **优化张力（Optimization Tension）**：消融实验显示，加入深度 Token 后，通用 VQA（如 MMBench）的性能有轻微下降（约 0.4%），说明感知任务与纯文本任务之间存在一定的竞争关系。
*   **静态图像限制**：目前仅限于静态图像，尚未扩展到视频领域（视频需要考虑时序一致性的深度 Token）。
*   **教师模型依赖**：模型性能受限于教师模型（SAM2 和 Depth Anything V2）的质量，教师模型的噪声会传播给学生模型。

（完）
