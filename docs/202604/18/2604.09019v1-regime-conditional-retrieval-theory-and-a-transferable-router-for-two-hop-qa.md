---
title: "Regime-Conditional Retrieval: Theory and a Transferable Router for Two-Hop QA"
title_zh: 状态条件检索：双跳问答的理论与可迁移路由
authors: Andre Bacellar
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09019v1"
tags: ["query:cir-ml"]
score: 6.0
evidence: 多跳密集检索的理论与路由
tldr: 本研究针对两步问答（Two-hop QA）检索，提出了“政体条件检索”理论，将查询分为问题主导和桥接段落主导两种模式。通过三个定理形式化了这种划分，并开发了轻量级分类器RegimeRouter。该工具利用五个文本特征动态选择检索策略，在多个数据集上显著提升了检索召回率，且具备良好的跨领域迁移能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09019v1/fig-001.webp\", \"caption\": \"Figure 1: AUCi vs. Φ(Si/σ) for all four query types (comparison, bridge-comp, compositional, inference). Points are per-query; dashed line is y = x (theoretical prediction). The scatter follows the diagonal closely across types and encoders; Kendall τ is significant for all eight type×encoder pairs (p < 0.01).\", \"page\": 4, \"index\": 1, \"width\": 861, \"height\": 445}, {\"url\": \"assets/figures/arxiv/2604.09019v1/fig-002.webp\", \"caption\": \"Figure 4: Regime AUC pattern for NV-Embed-v2 and BGE-large-en-v1.5 (plus e5-mistral-7b, not shown; results consistent). AUC(Q) dominates for comparison and bridge-comp (Q-dominant); AUC(B) dominates for compositional and inference (B-dominant). The ordering is encoder-agnostic; Kendall τ is significant across all type×encoder pairs (p < 0.01).\", \"page\": 7, \"index\": 2, \"width\": 865, \"height\": 295}]"
motivation: 现有的两步问答检索方法未区分查询模式，导致在处理不同类型的复杂问题时检索效率和准确性受限。
method: 提出RegimeRouter，通过五个文本特征识别查询模式，动态决定是仅使用问题还是结合关系句进行检索。
result: "在2WikiMultiHopQA上训练并零样本迁移至MuSiQue和HotpotQA，分别实现了5.6、5.3和1.1个百分点的R@5提升。"
conclusion: 理论证明和实验结果表明，根据查询模式条件化选择检索策略能有效优化多跳问答的检索性能。
---

## 摘要
双跳问答检索将查询分为两种状态，取决于第二跳实体是显式出现在问题中（Q主导型）还是仅出现在桥接段落中（B主导型）。我们通过三个定理对这种划分进行了形式化：（T1）单查询 AUC 是余弦分离间隔的单调函数，在八个“类型-编码器”对中的六个上 R^2 ≥ 0.90；（T2）状态由两个表层文本谓词表征，其中 P1 对路由起决定性作用，P2 限定了 B 主导型情况，该结论在三种编码器和三个数据集上均成立；（T3）桥接优势需要包含关系的句子，而不仅仅是实体名称，移除该句子会导致性能下降 8.6-14.1 个百分点（p < 0.001）。基于该理论，我们提出了 RegimeRouter，这是一种轻量级二元路由，它利用直接源自谓词定义的五个文本特征，在“仅问题”检索和“问题加关系句”检索之间进行选择。RegimeRouter 在 2WikiMultiHopQA（n = 881，五折交叉拟合）上进行训练，并零样本应用于 MuSiQue 和 HotpotQA，分别实现了 +5.6 个百分点（p < 0.001）、+5.3 个百分点（p = 0.002）和 +1.1 个百分点（不显著，无负面影响）的 R@5 提升，且具有伪影驱动特性。

## Abstract
Two-hop QA retrieval splits queries into two regimes determined by whether the hop-2 entity is explicitly named in the question (Q-dominant) or only in the bridge passage (B-dominant). We formalize this split with three theorems: (T1) per-query AUC is a monotone function of the cosine separation margin, with R^2 >= 0.90 for six of eight type-encoder pairs; (T2) regime is characterized by two surface-text predicates, with P1 decisive for routing and P2 qualifying the B-dominant case, holding across three encoders and three datasets; and (T3) bridge advantage requires the relation-bearing sentence, not entity name alone, with removal causing an 8.6-14.1 pp performance drop (p < 0.001). Building on this theory, we propose RegimeRouter, a lightweight binary router that selects between question-only and question-plus-relation-sentence retrieval using five text features derived directly from the predicate definitions. Trained on 2WikiMultiHopQA (n = 881, 5-fold cross-fitted) and applied zero-shot to MuSiQue and HotpotQA, RegimeRouter achieves +5.6 pp (p < 0.001), +5.3 pp (p = 0.002), and +1.1 pp (non-significant, no-regret) R@5 improvement, respectively, with artifact-driven.