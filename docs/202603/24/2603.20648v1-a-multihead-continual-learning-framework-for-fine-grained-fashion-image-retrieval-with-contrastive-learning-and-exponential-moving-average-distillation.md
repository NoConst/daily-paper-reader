---
title: A Multihead Continual Learning Framework for Fine-Grained Fashion Image Retrieval with Contrastive Learning and Exponential Moving Average Distillation
title_zh: 一种结合对比学习与指数移动平均蒸馏的多头持续学习细粒度时尚图像检索框架
authors: "Ling Xiao, Toshihiko Yamasaki"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20648v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 细粒度时尚图像检索的持续学习
tldr: 针对细粒度时尚图像检索（FIR）在动态场景下全量重训成本高的问题，本文提出了一种名为MCL-FIR的多头持续学习框架。该框架结合对比学习与指数移动平均（EMA）蒸馏，通过多头设计应对类别增长，利用InfoNCE简化训练并提升效果。实验证明，该方法在保持高精度的同时显著降低了训练成本，为增量式细粒度检索提供了高效解决方案。
selection_source: fresh_fetch
motivation: 传统的细粒度时尚图像检索方法在面对新属性时需要昂贵的全量重训，且缺乏针对该领域的类增量学习研究。
method: 提出MCL-FIR框架，采用多头设计适配新增类别，并结合InfoNCE对比学习与EMA蒸馏技术实现高效的知识迁移。
result: "在四个数据集上的实验显示，该方法在性能显著优于现有增量学习基准的同时，仅需静态方法约30%的训练成本。"
conclusion: MCL-FIR在效率与准确性之间取得了良好平衡，是解决动态环境下细粒度图像检索问题的有效方案。
---

## 摘要
大多数细粒度时尚图像检索（FIR）方法假设处于静态环境，当出现新属性时需要进行完全重新训练，这在动态场景中成本高昂且不切实际。尽管预训练模型支持零样本推理，但在缺乏监督的情况下其准确率会下降，且此前尚无研究探索针对细粒度 FIR 的类增量学习（CIL）。我们提出了一种结合对比学习与指数移动平均（EMA）蒸馏的多头持续学习细粒度时尚图像检索框架（MCL-FIR）。MCL-FIR 采用多头设计以适应增量过程中不断演化的类别，利用 InfoNCE 将三元组输入重新表述为二元组，从而实现更简单且更有效的训练，并采用 EMA 蒸馏进行高效的知识迁移。在四个数据集上的实验表明，除了可扩展性外，MCL-FIR 在效率和准确性之间实现了良好的平衡。在相似的训练成本下，它显著优于 CIL 基准方法；与静态方法相比，它在仅使用约 30% 训练成本的情况下实现了相当的性能。源代码已在 https://github.com/Dr-LingXiao/MCL-FIR 公开。

## Abstract
Most fine-grained fashion image retrieval (FIR) methods assume a static setting, requiring full retraining when new attributes appear, which is costly and impractical for dynamic scenarios. Although pretrained models support zero-shot inference, their accuracy drops without supervision, and no prior work explores class-incremental learning (CIL) for fine-grained FIR. We propose a multihead continual learning framework for fine-grained fashion image retrieval with contrastive learning and exponential moving average (EMA) distillation (MCL-FIR). MCL-FIR adopts a multi-head design to accommodate evolving classes across increments, reformulates triplet inputs into doublets with InfoNCE for simpler and more effective training, and employs EMA distillation for efficient knowledge transfer. Experiments across four datasets demonstrate that, beyond its scalability, MCL-FIR achieves a strong balance between efficiency and accuracy. It significantly outperforms CIL baselines under similar training cost, and compared with static methods, it delivers comparable performance while using only about 30% of the training cost. The source code is publicly available in https://github.com/Dr-LingXiao/MCL-FIR.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Multihead Continual Learning Framework for Fine-Grained Fashion Image Retrieval with Contrastive Learning and Exponential Moving Average Distillation》（一种结合对比学习与指数移动平均蒸馏的多头持续学习细粒度时尚图像检索框架）的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的细粒度时尚图像检索（FIR）方法大多基于“静态学习”假设。当系统中出现新的服装属性（如新的领型、袖长）时，模型通常需要结合所有旧数据和新数据进行全量重新训练，这在实际应用中成本极高且不可扩展。
*   **研究背景**：
    *   **静态方法局限**：如 RPF 等先进方法在单张 A100 GPU 上训练 FashionAI 数据集需耗时 121 小时以上。
    *   **预训练模型局限**：虽然 CLIP 等模型具备零样本能力，但在缺乏特定属性监督的情况下，捕捉细粒度视觉差异的准确率会大幅下降。
    *   **持续学习空白**：此前尚无研究专门针对细粒度 FIR 领域探索类增量学习（CIL）。

### 2. 论文提出的方法论：MCL-FIR 框架
该论文提出了一种名为 **MCL-FIR** 的多头持续学习框架，其核心思想是通过模块化设计和高效蒸馏实现属性的增量学习。
*   **多头架构（Multi-head Design）**：
    *   采用共享的图像编码器（ResNet-50 的前三个 block）提取通用特征。
    *   为每个新任务（新属性）分配一个轻量级的“文本引导注意力模块”（Attn head），参数量仅 0.246M。这种设计允许模型在学习新属性时，不干扰已学过的属性头。
*   **文本引导注意力机制**：
    *   利用预训练 CLIP 文本编码器提取属性标签的语义向量。
    *   结合**空间注意力**（定位属性相关区域）和**通道注意力**（重权衡特征通道），生成属性感知的图像表示。
*   **InfoNCE 损失函数**：
    *   将传统 FIR 中复杂且不稳定的“三元组采样（Triplet）”简化为基于“二元组（Doublet）”的 InfoNCE 对比损失。这减少了 1/3 的计算量，同时提供了更强的对比信号。
*   **EMA 蒸馏（Exponential Moving Average Distillation）**：
    *   维护一个通过指数移动平均更新的“教师模型”。
    *   通过计算当前模型与教师模型之间的蒸馏损失（MSE），提供稳定的时间监督，有效缓解共享编码器在增量更新过程中的灾难性遗忘问题。

### 3. 实验设计
*   **数据集**：
    *   **FashionAI**：8 个属性，约 18 万张图像。
    *   **DeepFashion**：6 个属性，约 29 万张图像。
    *   **DARN**：9 个属性。
    *   **Zappos50K**：用于跨领域（鞋类）验证的 4 个属性。
*   **Benchmark（基准）**：
    *   **静态 SOTA 方法**：CSN, ASENet V2, ASEN, RPF, GeoDCL, MKD 等。
    *   **CIL 基准**：作者自行实现了基于经验回放（ER-based）和标准多头（Multi-head-based）的增量学习基线。
*   **对比设置**：模拟真实场景，将 22 个属性按顺序（FashionAI → DeepFashion → DARN）进行增量训练。

### 4. 资源与算力
*   **硬件环境**：实验主要在 **NVIDIA V100 GPU** 上进行（PyTorch 2.2.0）。
*   **训练时长**：
    *   静态方法总训练时间约为 75~470 小时（取决于具体方法）。
    *   **MCL-FIR 总训练时间仅为 65.32 小时**，约为最先进静态方法（RPF）训练成本的 **14%**，约为普通静态方法（ASENet V2）的 **30%**。
*   **参数量**：共享主干约 20M，每个新增属性头仅增加约 0.246M 参数。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流数据集上进行了详尽测试，涵盖了服装和鞋类。
*   **消融实验**：针对 InfoNCE 损失、EMA 蒸馏、注意力模块等核心组件均做了消融对比，验证了各部分的有效性。
*   **鲁棒性测试**：
    *   **任务顺序**：测试了 6 种不同的数据集学习顺序，证明模型对顺序不敏感。
    *   **超参数敏感性**：对蒸馏权重 $\lambda$、Batch Size、训练样本量（2k 到 100k）以及训练轮数（Epochs）进行了全面分析。
*   **评价**：实验设计非常充分且客观，通过 t-SNE 可视化和注意力图可视化直观展示了模型的判别能力。

### 6. 主要结论与发现
*   **效率与精度的平衡**：MCL-FIR 在显著降低训练成本（仅需静态方法的 30% 左右）的前提下，达到了与静态 SOTA 方法相当的检索精度（mAP）。
*   **抗遗忘能力**：在 Zappos50K 的扩展实验中，模型在学习全新的鞋类属性后，对之前学习的服装属性几乎没有性能下降，甚至因特征共享有轻微提升。
*   **采样优势**：证明了在增量学习场景下，二元组 InfoNCE 损失比传统三元组损失更稳定且高效。

### 7. 优点（亮点）
*   **实用性强**：直接解决了 FIR 在工业界落地时面临的“新属性上线需全量重训”的痛点。
*   **模块化设计**：多头架构使得模型具有极高的可扩展性，新增属性只需增加极少量的参数。
*   **训练高效**：通过简化采样和引入 EMA 蒸馏，大幅缩短了收敛时间和计算开销。
*   **跨领域潜力**：实验证明该框架不仅适用于服装，也能很好地迁移到鞋类等其他细粒度检索任务。

### 8. 不足与局限
*   **峰值精度差距**：虽然 MCL-FIR 达到了“相当”的水平，但在某些特定属性上，其绝对精度仍略低于最先进的静态全量重训方法（如 RPF）。
*   **任务路由假设**：在推理时，模型假设已知查询属于哪个属性（以选择对应的 Head），虽然这符合 FIR 的交互逻辑（用户通常会选择属性标签进行搜索），但在完全盲测的场景下可能受限。
*   **主干网络更新**：虽然使用了 EMA 蒸馏，但随着任务数量极度增加（如成百上千个属性），共享主干网络是否会遇到表征瓶颈仍有待观察。

（完）
