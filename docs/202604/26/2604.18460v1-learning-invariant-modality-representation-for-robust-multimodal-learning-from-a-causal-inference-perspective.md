---
title: Learning Invariant Modality Representation for Robust Multimodal Learning from a Causal Inference Perspective
title_zh: 从因果推理视角学习不变模态表示以实现鲁棒多模态学习
authors: "Sijie Mai, Shiqin Han"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18460v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 因果模态不变表示，旨在解决伪相关和模态噪声问题
tldr: 针对多模态情感计算中模型易学习虚假相关性导致泛化性差的问题，本文提出基于因果推理的因果模态不变表示学习框架（CmIR）。该框架通过解耦学习将模态分解为因果不变表示和环境特有虚假表示，并利用多种约束确保表示的稳定性。实验证明CmIR在多个基准测试中达到SOTA，尤其在处理分布偏移和噪声数据时表现出极强的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18460v1/fig-001.webp\", \"caption\": \"Table 1: Comparisons on the CMU-MOSI and CMU-MOSEI datasets. The results labeled with † are obtained from original papers, and other results are obtained from our experiments. The best results are highlighted.\", \"page\": 8, \"index\": 1, \"width\": 908, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2604.18460v1/fig-002.webp\", \"caption\": \"Figure 7: The structures of encoder, decoder, and predictor. d ′ represents the hidden dimensionality.\", \"page\": 19, \"index\": 2, \"width\": 458, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2604.18460v1/fig-003.webp\", \"caption\": \"Table 4: Discussion on noisy modalities on CMU-MOSI and CMU-MOSEI. MuBo denotes Multimodal Boosting.\", \"page\": 9, \"index\": 3, \"width\": 867, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2604.18460v1/fig-004.webp\", \"caption\": \"Figure 3: The overall framework of CmIR and the visualization of the proposed constraints.\", \"page\": 5, \"index\": 4, \"width\": 902, \"height\": 329}]"
motivation: 现有多模态模型容易捕捉到数据中的虚假相关性，导致在面对分布偏移或噪声时泛化性能下降。
method: 提出CmIR框架，通过因果推理将各模态解耦为因果不变表示和环境特有虚假表示，并引入不变性、互信息和重构约束进行优化。
result: 在多个多模态基准测试中取得了最先进的性能，并在分布外数据和噪声数据上展现出显著的鲁棒性。
conclusion: 证明了通过因果解耦学习不变特征是提升多模态学习模型泛化能力和鲁棒性的有效途径。
---

## 摘要
多模态情感计算旨在利用语言、音频和视觉模态来预测人类的情感、情绪、意图和观点。然而，现有模型通常会学习到虚假相关性，这在分布偏移或存在噪声模态的情况下会损害泛化能力。为了解决这一问题，我们提出了一种因果模态不变表示（CmIR）学习框架，用于鲁棒多模态学习。其核心是，我们从因果推理的视角引入了一种具有理论依据的解耦方法，将每个模态分解为“因果不变表示”和“环境特定虚假表示”。CmIR 通过不变性约束、互信息约束和重构约束，确保所学习的不变表示在不同环境下与标签保持稳定的预测关系，同时保留来自原始输入的充分信息。在多个多模态基准测试上的实验表明，CmIR 达到了最先进的性能。CmIR 在分布外数据和噪声数据上表现尤为出色，证实了其鲁棒性和泛化能力。

## Abstract
Multimodal affective computing aims to predict humans' sentiment, emotion, intention, and opinion using language, acoustic, and visual modalities. However, current models often learn spurious correlations that harm generalization under distribution shifts or noisy modalities. To address this, we propose a causal modality-invariant representation (CmIR) learning framework for robust multimodal learning. At its core, we introduce a theoretically grounded disentanglement method that separates each modality into `causal invariant representation' and `environment-specific spurious representation' from a causal inference perspective. CmIR ensures that the learned invariant representations retain stable predictive relationships with labels across different environments while preserving sufficient information from the raw inputs via invariance constraint, mutual information constraint, and reconstruction constraint. Experiments across multiple multimodal benchmarks demonstrate that CmIR achieves state-of-the-art performance. CmIR particularly excels on out-of-distribution data and noisy data, confirming its robustness and generalizability.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Invariant Modality Representation for Robust Multimodal Learning from a Causal Inference Perspective》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
多模态情感计算（MAC）旨在整合语言、视觉和音频信息来识别情感。然而，现有的深度学习模型存在一个致命缺陷：**容易捕捉数据中的“虚假相关性”（Spurious Correlations）**。
*   **研究动机**：例如，如果训练集中某个说话人总是微笑（视觉特征）且总是表达正面情绪，模型会错误地认为“微笑 = 正面”，当测试集中出现“皮笑肉不笑”的讽刺场景时，模型就会失效。
*   **核心问题**：如何在存在分布偏移（OOD）或模态噪声的现实环境中，提取出真正与标签相关的“因果不变特征”，从而提升模型的鲁棒性和泛化能力。

### 2. 方法论：CmIR 框架
论文提出了 **CmIR（Causal modality-Invariant Representation）** 框架，其核心思想是基于结构因果模型（SCM）将每个模态的特征解耦。

*   **核心思想**：将模态特征 $X_m$ 分解为两个独立部分：
    1.  **因果不变表示 ($Z_{inv}$)**：在不同环境下与标签保持稳定因果关系的特征。
    2.  **环境特定虚假表示 ($Z_{spu}$)**：捕捉环境噪声、说话人风格等与标签无因果关系的特征。
*   **关键技术细节（三重约束）**：
    *   **不变性约束 (Invariance Constraint)**：通过对原始特征注入不同强度的噪声来模拟“虚拟环境”，强制模型在这些不同环境下提取出一致的 $Z_{inv}$。
    *   **互信息约束 (Mutual Information Constraint)**：通过特征正交化手段，最小化 $Z_{inv}$ 与 $Z_{spu}$ 之间的互信息，确保两者彻底解耦。
    *   **重构约束 (Reconstruction Constraint)**：使用解码器尝试从解耦后的特征还原原始输入，确保在分解过程中没有丢失关键信息。
*   **算法流程**：输入原始模态 -> 编码器解耦 -> 应用三重损失函数优化 -> 仅将各模态的 $Z_{inv}$ 进行融合并输入预测头。

### 3. 实验设计
*   **数据集**：
    *   **情感分析**：CMU-MOSI（标准及 OOD 版本）、CMU-MOSEI、CH-SIMS-v2（中文）。
    *   **幽默/讽刺检测**：UR-FUNNY、MUStARD。
*   **Benchmark 与对比方法**：
    *   对比了超过 20 种基准方法，包括经典的 Self-MM、MISA，以及最新的 ITHP (2024)、GSCon (2025) 等。
    *   专门对比了基于因果推理的最新方法，如 CLUE、GEAR、MulDeF 和 AtCAF。
*   **场景设置**：涵盖了标准测试、分布外（OOD）测试以及不同噪声比例（10%-70%）的鲁棒性测试。

### 4. 资源与算力
*   **硬件设备**：论文明确提到使用了一块 **NVIDIA RTX 3090 GPU**。
*   **软件环境**：PyTorch 1.13.1, CUDA 11.6。
*   **训练细节**：使用了 AdamW 优化器；通过 50 次随机网格搜索（Random Grid Search）来确定最优超参数；每组实验重复运行 5 次并取平均值以确保结果可靠。

### 5. 实验数量与充分性
*   **实验规模**：实验设计非常详尽，包含了：
    *   6 个主流数据集的性能对比。
    *   **OOD 专项实验**：验证模型在分布偏移下的生存能力。
    *   **噪声鲁棒性实验**：模拟了 7 种不同强度的 Gaussian 噪声，以及 Laplace 和随机擦除等 OOD 噪声。
    *   **消融实验**：逐一验证了不变性、互信息和重构三个约束项的必要性。
    *   **敏感性分析**：探讨了环境数量 $K$、噪声强度 $\alpha$ 等超参数的影响。
*   **充分性评价**：实验覆盖了从情感分析到幽默识别的多种任务，且对比了 2025 年的最前沿方法，实验设计客观、公平，且通过了显著性检验（t-test）。

### 6. 主要结论与发现
*   **SOTA 性能**：CmIR 在所有数据集上均刷新了纪录，尤其在 CMU-MOSI (OOD) 上，Acc2 提升了约 3 个百分点。
*   **因果特征的优越性**：实验证明，只使用因果不变特征 $Z_{inv}$ 进行预测，比使用原始全量特征在面对噪声时更稳定。
*   **解耦有效性**：探测实验（Probing）显示，$Z_{spu}$ 几乎无法预测情感标签，而 $Z_{inv}$ 预测准确率极高，证明了特征解耦的成功。

### 7. 优点：亮点与创新
*   **理论支撑强**：论文不仅提出了模型，还提供了三个完整的数学定理证明，从理论上保证了不变表示的存在性、可提取性以及在 OOD 风险下的优势。
*   **无需先验偏见知识**：与以往需要预定义“说话人偏见”或“性别偏见”的方法不同，CmIR 通过模拟环境自动发现不变性，具有更强的通用性。
*   **极强的抗噪性**：在 70% 的极端噪声干扰下，CmIR 的性能下降幅度远小于其他 SOTA 模型。

### 8. 不足与局限
*   **环境模拟的局限性**：虽然通过加噪模拟了虚拟环境，但现实世界的分布偏移（如文化背景差异、语调突变）可能比单纯的加噪更复杂。
*   **互信息近似**：论文使用正交化作为互信息的代理指标，这在处理高度非线性的复杂相关性时，可能不如专门的互信息估计器（如 MINE）精确。
*   **计算开销**：为了实现不变性约束，训练过程中需要为每个样本生成多个环境副本，这会增加训练时的显存占用和计算耗时。

（完）
