---
title: "AgentForge: Execution-Grounded Multi-Agent LLM Framework for Autonomous Software Engineering"
title_zh: AgentForge：面向自主软件工程的基于执行验证的多智能体大语言模型框架
authors: "Rajesh Kumar, Waqar Ali, Junaid Ahmed, Najma Imtiaz Ali, Shaban Usman"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.13120v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 通过共享记忆协调的多智能体 LLM 框架
tldr: "AgentForge是一个针对自主软件工程的多智能体LLM框架，旨在解决LLM生成代码难以验证正确性的问题。该框架将“基于执行的验证”作为核心原则，要求所有代码更改必须在Docker沙箱中通过执行验证。通过Planner、Coder、Tester、Debugger和Critic等角色的协作，AgentForge在SWE-BENCH Lite上取得了40.0%的解决率，显著优于单智能体基准，证明了执行反馈和角色分解在提升软件工程任务性能方面的关键作用。"
selection_source: fresh_fetch
motivation: 现有的LLM生成代码虽然看似合理但缺乏验证机制，且现有多智能体系统往往忽略或仅将执行验证作为可选项。
method: 提出一种以执行为基础的验证机制，通过在Docker沙箱中运行Planner、Coder、Tester等多个专业化智能体，实现代码变更的强制性验证与迭代优化。
result: "在SWE-BENCH Lite基准测试中实现了40.0%的任务解决率，比单智能体基准提升了26至28个百分点。"
conclusion: 强制性的执行反馈和多智能体角色分解是提升自主软件工程系统性能的核心驱动力。
---

## 摘要
大语言模型能够生成看似合理的代码，但无法验证其正确性。现有的多智能体系统通常只是模拟执行，或者将验证作为可选环节。我们引入了“基于执行的验证”作为核心原则：每一次代码变更在传播之前都必须通过沙箱执行。我们在 AGENTFORGE 中实现了这一原则，该多智能体框架包含规划者（Planner）、编码者（Coder）、测试者（Tester）、调试者（Debugger）和评论者（Critic）等智能体，它们通过共享内存和强制性的 Docker 沙箱进行协作。我们将基于大语言模型的软件工程形式化为代码库状态上的迭代决策过程，其中执行反馈提供了比“下一个 Token 概率”更强的监督信号。AGENTFORGE 在 SWE-BENCH Lite 上实现了 40.0% 的解决率，比单智能体基准线高出 26-28 个百分点。消融实验证实，执行反馈和角色分解各自独立地推动了性能提升。该框架已在 https://github.com/raja21068/AutoCodeAI 开源。

## Abstract
Large language models generate plausible code but cannot verify correctness. Existing multi-agent systems simulate execution or leave verification optional. We introduce execution-grounded verification as a first-class principle: every code change must survive sandboxed execution before propagation. We instantiate this principle in AGENTFORGE, a multi-agent framework where Planner, Coder, Tester, Debugger, and Critic agents coordinate through shared memory and a mandatory Docker sandbox. We formalize software engineering with LLMs as an iterative decision process over repository states, where execution feedback provides a stronger supervision signal than next-token likelihood. AGENTFORGE achieves 40.0\% resolution on SWE-BENCH Lite, outperforming single-agent baselines by 26--28 points. Ablations confirm that execution feedback and role decomposition each independently drive performance. The framework is open-source at https://github.com/raja21068/AutoCodeAI.

---

## 论文详细总结（自动生成）

以下是对论文《AgentForge: Execution-Grounded Multi-Agent LLM Framework for Autonomous Software Engineering》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）虽然能生成看似合理的代码，但在处理复杂的真实软件工程任务时，往往无法保证正确性，且容易产生幻觉或引入回归错误。
*   **研究动机**：现有的多智能体系统（MAS）要么仅依靠模拟执行，要么将代码验证视为可选步骤。作者认为，缺乏真实的执行反馈是限制自主软件工程发展的关键瓶颈。
*   **整体含义**：论文提出了 **AgentForge** 框架，将“基于执行的验证”作为核心原则，要求所有代码变更必须在隔离的沙箱中通过真实测试验证后才能生效。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将软件工程任务建模为一个在代码库状态上的**迭代决策过程（MDP）**。通过五个专业化智能体的协作，利用真实的执行信号（而非模型内部的概率信号）来驱动代码修复。
*   **关键技术细节**：
    *   **五大智能体角色**：
        1.  **Planner（规划者）**：生成结构化执行计划。
        2.  **Coder（编码者）**：生成最小化的统一差异补丁（Unified Diffs），降低错误面。
        3.  **Tester（测试者）**：合成可执行的测试用例（pytest）。
        4.  **Debugger（调试者）**：根据沙箱返回的错误日志进行迭代修复。
        5.  **Critic（评论者）**：对最终结果进行合规性审查。
    *   **双重检索机制**：结合了**情景记忆**（以往成功任务的经验）和**代码库索引**（当前代码的语义检索），均存储在 ChromaDB 向量数据库中。
    *   **强制性 Docker 沙箱**：所有代码在资源受限（512MB RAM, 0.5 CPU）、网络隔离的容器中运行，确保安全性和反馈的真实性。
    *   **迭代修复循环**：Debugger 允许最多 3 次重试，利用执行反馈形成闭环。

### 3. 实验设计
*   **数据集/场景**：使用 **SWE-bench Lite** 基准测试，包含 300 个真实的 GitHub Issue，涉及 Django、scikit-learn、NumPy 等 11 个流行 Python 项目。
*   **对比方法**：
    *   **Single-agent GPT-4o**：单次调用，无工具和反馈。
    *   **ReAct (GPT-4o)**：典型的推理-行动循环智能体。
    *   **SWE-agent**：现有的开源自主软件工程基准。
*   **评估指标**：解决率（Resolve Rate），即生成的补丁必须通过所有预设的测试用例且不引入新错误。

### 4. 资源与算力
*   **模型**：主要使用 **GPT-4o** (gpt-4o-2024-08-06)。
*   **硬件**：16 核 CPU、64GB RAM，配备专用 GPU 用于处理嵌入向量和加速计算。
*   **成本**：平均每项任务的 API 成本约为 **0.1445 美元**，完整运行 300 个任务约需 43.35 美元。

### 5. 实验数量与充分性
*   **实验规模**：在完整的 300 个任务上进行了主实验，并在前 100 个任务上进行了详细的消融实验。
*   **消融实验**：分别移除了 Critic、Debugger、Tester 和 Planner，验证了每个组件的必要性。
*   **充分性与客观性**：实验设计较为充分，包含了重试曲线分析、成本-性能权衡分析以及对 30 个失败案例的深度错误分类分析。使用了官方评估工具，确保了结果的公平性。

### 6. 主要结论与发现
*   **性能卓越**：AgentForge 实现了 **40.0%** 的解决率，比单智能体基准（14.0%）提升了 26 个百分点，也优于 SWE-agent（18.1%）。
*   **执行反馈至关重要**：消融实验显示，移除 Tester 和 Debugger（即移除执行反馈循环）会导致性能大幅下降。
*   **角色分解的优势**：专业化分工降低了单个步骤的复杂度，减少了错误累积。
*   **Diff 补丁效率高**：相比全文件生成，基于 Diff 的编辑显著降低了 Token 消耗并提高了成功率。

### 7. 优点
*   **真实性强**：强制性的沙箱执行消除了模拟环境带来的幻觉问题。
*   **理论支撑**：将多智能体协作形式化为 MDP，并提出了关于执行信号优势和错误传播的理论命题。
*   **双重记忆系统**：有效地结合了跨任务的长期经验和任务内的即时上下文。
*   **开源贡献**：提供了完整的开源框架，便于社区复现和扩展。

### 8. 不足与局限
*   **故障定位（Localization）瓶颈**：40% 的失败源于定位错误（找错了文件或行），说明系统在理解复杂跨文件依赖方面仍有欠缺。
*   **认知僵局（Cognitive Deadlock）**：在 20% 的失败案例中，Debugger 会陷入局部搜索，重复尝试无效的修改。
*   **环境依赖**：对复杂环境（如特定版本的系统库）的配置仍存在 13.3% 的失败率。
*   **模型依赖**：目前高度依赖 GPT-4o 的推理能力，尚未在较小规模的开源模型上验证其泛化性。

（完）
