---
title: "MemRerank: Preference Memory for Personalized Product Reranking"
title_zh: MemRerank：面向个性化商品重排序的偏好记忆
authors: "Zhiyuan Peng, Xuyang Wu, Huaixiao Tou, Yi Fang, Yu Gong"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29247v2"
tags: ["query:cir-ml"]
score: 9.0
evidence: 使用大语言模型进行个性化产品重排序的偏好记忆
tldr: 针对大模型购物助手在处理冗长且含噪声的购买历史时效率低下的问题，本文提出MemRerank框架。该框架通过强化学习训练的存储提取器，将用户历史蒸馏为简洁、与查询无关的偏好信号，用于个性化商品重排序。通过构建1-in-5选择任务基准进行评估，实验证明该方法显著提升了重排序准确率，为电商智能体的个性化提供了实用方案。
selection_source: fresh_fetch
motivation: 直接将原始购买历史输入大模型存在噪声多、长度长且相关性差的问题，限制了个性化推荐的效果。
method: 提出MemRerank框架，利用强化学习以重排序性能为监督信号，将用户历史蒸馏为简洁的偏好记忆。
result: 实验显示MemRerank在1-in-5选择任务中显著优于基准模型，准确率最高提升了10.61个百分点。
conclusion: 显式的偏好记忆是实现电商智能体个性化重排序的一种实用且高效的解决方案。
---

## 摘要
基于大语言模型（LLM）的购物智能体日益依赖长期的购买历史和多轮交互来实现个性化，但由于噪声、长度和相关性不匹配，直接将原始历史记录附加到提示词（prompts）中往往效果不佳。我们提出了 MemRerank，这是一个偏好记忆框架，它将用户的购买历史提炼为简洁且独立于查询的信号，用于个性化商品重排序。为了研究这一问题，我们构建了一个端到端的基准测试和评估框架，其核心是一个基于 LLM 的“五选一”（1-in-5）选择任务，该任务同时衡量记忆质量和下游重排序效用。我们进一步利用强化学习（RL）训练记忆提取器，并以下游重排序性能作为监督信号。在两个基于 LLM 的重排序器上进行的实验表明，MemRerank 始终优于无记忆、原始历史记录和现成的记忆基准，在“五选一”准确率上最高提升了 10.61 个绝对百分点。这些结果表明，显式偏好记忆是代理式电子商务系统中实现个性化的实用且有效的构建模块。

## Abstract
LLM-based shopping agents increasingly rely on long purchase histories and multi-turn interactions for personalization, yet naively appending raw history to prompts is often ineffective due to noise, length, and relevance mismatch. We propose MemRerank, a preference memory framework that distills user purchase history into concise, query-independent signals for personalized product reranking. To study this problem, we build an end-to-end benchmark and evaluation framework centered on an LLM-based \textbf{1-in-5} selection task, which measures both memory quality and downstream reranking utility. We further train the memory extractor with reinforcement learning (RL), using downstream reranking performance as supervision. Experiments with two LLM-based rerankers show that MemRerank consistently outperforms no-memory, raw-history, and off-the-shelf memory baselines, yielding up to \textbf{+10.61} absolute points in 1-in-5 accuracy. These results suggest that explicit preference memory is a practical and effective building block for personalization in agentic e-commerce systems.

---

## 论文详细总结（自动生成）

以下是对论文《MemRerank: Preference Memory for Personalized Product Reranking》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着大语言模型（LLM）在电商领域作为智能体（Agent）的应用增加，如何利用用户长期的购买历史实现个性化推荐成为关键。
*   **核心问题**：直接将原始购买历史（Raw History）放入 Prompt 存在三大痛点：**噪声多**（无关购买干扰）、**长度长**（超出上下文窗口）以及**相关性失配**（历史与当前查询不直接相关）。
*   **论文意义**：提出了一种名为 **MemRerank** 的框架，通过将冗长的历史记录“蒸馏”为简洁、结构化的偏好记忆，显著提升了 LLM 在商品重排序任务中的个性化表现。

### 2. 方法论
*   **核心思想**：将个性化重排序分为“偏好记忆提取”和“下游重排序”两个阶段，并利用强化学习（RL）使提取器生成的记忆直接服务于重排序的准确性。
*   **关键技术细节**：
    *   **双维度记忆结构**：区分“类内偏好（Within-category）”和“跨类偏好（Cross-category）”。前者关注用户在特定品类下的细粒度需求，后者捕捉用户的通用购物风格（如预算、品牌倾向）。
    *   **结构化提取**：提取器输出包含“维度（Aspect）- 偏好（Preference）- 证据（Evidence）”的结构化文本，确保记忆的可解释性和真实性。
    *   **RL 训练（GRPO 算法）**：使用组相对策略优化（Group Relative Policy Optimization）训练提取器。
        *   **奖励函数（Reward Function）**：由“格式奖励”（确保输出符合 XML 标签规范）和“重排序奖励”（如果下游重排序器根据该记忆选对了正确商品，则给予正奖励）组成。
*   **算法流程**：输入用户历史 $\rightarrow$ 提取器生成记忆 $\rightarrow$ 记忆 + 当前查询 + 候选商品输入重排序器 $\rightarrow$ 输出最终排名。

### 3. 实验设计
*   **数据集**：基于 **Amazon-Review-2023** 和 **Amazon-C4** 构建。主要测试场域为“电子产品（Electronics）”类别。
*   **Benchmark**：构建了一个 **1-in-5 选择任务**（从1个正样本和4个同类硬负样本中选出最相关商品），作为衡量重排序性能的标准。
*   **对比方法**：
    *   **无记忆基准**：仅输入查询和商品。
    *   **原始历史注入**：直接在 Prompt 中添加最近购买的商品列表。
    *   **现成记忆框架**：如 Mem0、MR.Rec。
    *   **强模型提取**：使用未经 RL 训练的 Qwen2.5-7B 或 GPT-5.2 提取记忆。
    *   **重排序器变量**：分别测试了 GPT-4.1-mini 和 o4-mini 作为下游重排序器的表现。

### 4. 资源与算力
*   **模型规格**：记忆提取器采用 **Qwen2.5-7B-Instruct**。
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及训练总时长。但提到了使用 GRPO 算法进行后训练（Post-training），这通常需要中等规模的计算集群支持。

### 5. 实验数量与充分性
*   **实验规模**：
    *   主实验对比了 10 余种不同的配置组合。
    *   **消融实验**：测试了三种不同的提取 Prompt 模板（v1 固定维度、v2 自由发现、v3 半结构化带证据），验证了 Prompt 设计对 RL 效果的影响。
    *   **长度实验**：分析了不同长度的原始历史对重排序的影响，证明了简单增加历史长度并不等同于提升性能。
*   **充分性评价**：实验设计较为全面，涵盖了从模型能力、Prompt 工程到历史数据分布的多个维度。通过在两个主流 LLM 重排序器上取得一致性提升，证明了方法的稳健性。

### 6. 主要结论与发现
*   **性能提升**：MemRerank 相比无记忆基准，在 o4-mini 上提升了 **10.61%** 的准确率。
*   **RL 的必要性**：经过 RL 训练的 7B 模型提取的记忆，效果优于未经训练的强闭源模型（如 GPT-5.2）。
*   **Prompt 的关键性**：半结构化且要求提供“证据片段”的 Prompt（v3）效果最好，因为它能抑制模型幻觉并提供更具行动力的偏好描述。
*   **思考链（CoT）的作用**：在重排序阶段加入 `<think>` 标签能进一步放大高质量记忆的优势。

### 7. 优点（亮点）
*   **任务导向**：不只是做摘要，而是通过 RL 将记忆提取与下游任务的最终指标（准确率）直接挂钩。
*   **实用性强**：生成的文本记忆体积小、可读性强，有效节省了 LLM 的上下文窗口成本。
*   **细粒度建模**：通过区分“类内”和“跨类”记忆，解决了推荐系统中长短期兴趣建模的经典问题。

### 8. 不足与局限
*   **品类覆盖**：实验主要集中在“电子产品”类别，对于服装、生鲜等偏好更主观或变化更快的品类，其泛化能力有待验证。
*   **规模限制**：目前仅验证了 1-in-5 的重排序场景，在处理更大规模候选集（如 1-in-100）时的端到端性能尚未探讨。
*   **冷启动问题**：对于购买历史极短的用户，该框架提取有效偏好记忆的能力受限。

（完）
