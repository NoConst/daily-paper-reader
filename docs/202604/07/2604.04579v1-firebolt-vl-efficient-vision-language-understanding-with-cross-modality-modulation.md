---
title: "Firebolt-VL: Efficient Vision-Language Understanding with Cross-Modality Modulation"
title_zh: Firebolt-VL：基于跨模态调制的高效视觉语言理解
authors: "Quoc-Huy Trinh, Mustapha Abdullahi, Bo Zhao, Debesh Jha"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04579v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大模型的高效视觉语言理解
tldr: 针对多模态大模型计算成本高且在细粒度任务中表现不足的问题，本文提出Firebolt-VL。该模型采用Liquid Foundation Model (LFM) 替代传统的Transformer解码器以实现线性推理效率。同时，引入Token-Grid相关性模块，通过轻量级文本-图像关联计算和FiLM调节的状态空间模型，增强了模型对任务相关视觉区域的捕捉能力。Firebolt-VL在保持高效性的同时，显著提升了细粒度推理性能。
selection_source: fresh_fetch
motivation: 现有多模态模型受限于Transformer的二次复杂度及对细粒度视觉特征捕捉的不足，难以在资源受限场景下高效部署。
method: 采用LFM解码器并结合Token-Grid相关性模块，利用FiLM调节的状态空间模型实现跨模态特征调制。
result: 在多个基准测试中，Firebolt-VL在显著提高推理效率的同时，实现了更精准的细粒度视觉理解。
conclusion: Firebolt-VL证明了通过线性复杂度架构与高效跨模态调制，可以在资源受限环境下实现高性能的视觉语言理解。
---

## 摘要
多模态大语言模型（MLLMs）的最新进展在视觉语言理解方面取得了显著进步，但其高昂的计算成本限制了在个人助手、文档理解和智能摄像头等资源受限场景中的部署。大多数现有方法依赖于基于 Transformer 的交叉注意力机制，其二次复杂度阻碍了效率。此外，小型视觉语言模型往往难以精确捕捉细粒度的、与任务相关的视觉区域，导致在细粒度推理任务上的性能下降，限制了其在现实世界中的有效性。为了解决这些问题，我们推出了 Firebolt-VL，这是一种高效的视觉语言模型，它将基于 Transformer 的解码器替换为液体基础模型（Liquid Foundation Model, LFM）解码器。为了进一步增强视觉定位，我们提出了一个 Token-Grid 相关性模块，该模块计算文本 token 与图像块之间的轻量级相关性，并通过带有 FiLM 条件的状态空间模型进行调制。这使得模型能够在保持线性时间推理的同时，选择性地强调与文本提示相关的视觉区域。在多个基准测试上的实验结果表明，Firebolt-VL 在显著提高效率的同时，实现了准确的细粒度理解。我们的模型和代码可在以下网址获取：https://fireboltvl.github.io

## Abstract
Recent advances in multimodal large language models (MLLMs) have enabled impressive progress in vision-language understanding, yet their high computational cost limits deployment in resource-constrained scenarios such as personal assistants, document understanding, and smart cameras. Most existing methods rely on Transformer-based cross-attention, whose quadratic complexity hinders efficiency. Moreover, small vision-language models often struggle to precisely capture fine-grained, task-relevant visual regions, leading to degraded performance on fine-grained reasoning tasks that limit their effectiveness in the real world. To address these issues, we introduce Firebolt-VL, an efficient vision-language model that replaces the Transformer-based decoder with a Liquid Foundation Model (LFM) decoder. To further enhance visual grounding, we propose a Token-Grid Correlation Module, which computes lightweight correlations between text tokens and image patches and modulates via the state-space model with FiLM conditioning. This enables the model to selectively emphasize visual regions relevant to the textual prompt while maintaining linear-time inference. Experimental results across multiple benchmarks demonstrate that Firebolt-VL achieves accurate, fine-grained understanding with significantly improved efficiency. Our model and code are available at: https://fireboltvl.github.io

---

## 论文详细总结（自动生成）

### Firebolt-VL：基于跨模态调制的高效视觉语言理解论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
现有的多模态大语言模型（MLLM）主要面临两大挑战：
*   **计算效率瓶颈**：大多数模型依赖 Transformer 架构，其注意力机制具有二次方计算复杂度，导致在长序列或资源受限设备（如手机、智能摄像头）上推理缓慢。
*   **细粒度感知不足**：小型视觉语言模型在处理需要精确视觉定位的任务（如 OCR、图表分析）时，往往无法准确捕捉与任务相关的局部视觉区域，导致性能下降。

**Firebolt-VL** 的研究动机在于开发一种既能保持线性推理效率，又能实现精确细粒度视觉理解的轻量级多模态模型。

#### 2. 方法论：核心思想与关键技术
Firebolt-VL 由三个核心组件构成：
*   **视觉编码器**：采用 **SigLIP** 提取图像的网格级（grid-level）嵌入，保留原始长宽比。
*   **液体基础模型（LFM）解码器**：取代传统的 Transformer，利用 **Liquid Foundation Model (LFM2)** 作为语言骨干。LFM 具有线性时间复杂度，显著提升了推理速度和扩展性。
*   **跨模态调制器（CMM）**：这是本文的核心创新，用于替代高成本的交叉注意力机制。其流程如下：
    1.  **Token-Grid 相关性计算**：计算文本 Token 与图像网格之间的点积相关性矩阵。
    2.  **Top-k 筛选**：为每个文本 Token 仅保留最相关的 $k$ 个视觉网格，减少噪声。
    3.  **FiLM 调制**：利用特征线性调制（FiLM）根据视觉上下文对文本表示进行缩放和偏移。
    4.  **状态空间模型（SSM）处理**：使用 S4 等结构化 SSM 对调制后的特征进行序列建模，捕捉长距离依赖。
    5.  **二次调制与融合**：再次应用 FiLM 并通过前馈网络（FFN）输出最终的跨模态表示。

#### 3. 实验设计
*   **数据集**：
    *   **预训练阶段**：使用 CC3M 数据集初始化 CMM 模块。
    *   **指令微调阶段**：使用 LLaVA-CoT 和处理后的 MMPR-v1.2 数据集进行端到端训练。
*   **基准测试（Benchmarks）**：涵盖了 VQAv2（视觉问答）、POPE（物体幻觉）、AI2D（图表理解）、MMMU（多学科推理）、MME（综合感知）、SQA-Image（科学问答）和 MMB（综合能力）。
*   **对比方法**：
    *   **大型模型（>7B）**：IDEFICS, OpenFlamingo v2, MiniGPT-4, Chameleon, FastV 等。
    *   **小型模型（<3B）**：Kosmos-2, MobileVLM (V1/V2), MoE-LLaVA, LLaVA-Phi, SmolVLM2 等。

#### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA H100 80GB GPU**。
*   **训练细节**：
    *   第一阶段（CMM 预训练）：Batch size 128，学习率 $5 \times 10^{-4}$。
    *   第二阶段（全模型微调）：Batch size 8，学习率 $1 \times 10^{-4}$。
    *   两个阶段各训练 5 个 Epoch，通常在第 2 个 Epoch 选出最佳模型。

#### 5. 实验数量与充分性
论文进行了较为充分的实验验证：
*   **基准对比**：在 7 个主流多模态榜单上进行了定量评估。
*   **效率评估**：专门对比了吞吐量（Tokens/s）和延迟（Latency）。
*   **消融实验**：
    1.  **SSM 变体对比**：对比了 Mamba、S4D 和 S4，发现 S4 效果最佳。
    2.  **融合策略对比**：对比了 Prepend（直接拼接）、Cross-attention（交叉注意力）和 CMM，证明了 CMM 的优越性。
    3.  **Top-k 参数分析**：测试了 $k=1$ 到 $5$ 的影响，发现 $k=4$ 时性能最优。
*   **客观性**：实验涵盖了感知、推理、效率等多个维度，对比对象包含了最新的同类轻量级模型（如 2025 年的 SmolVLM2），具有较强的时效性和公平性。

#### 6. 主要结论与发现
*   **高效性**：Firebolt-VL 的吞吐量达到 **46.67 tok/s**，优于 MobileVLM V2 和 SmolVLM2 等基准模型。
*   **高性能**：尽管参数量仅为 **0.8B**，但在 MME 感知评分（1376.2）和 VQAv2 上达到了同类模型中的领先水平，甚至在某些指标上超越了 7B 以上的大模型。
*   **细粒度能力**：定性分析显示，CMM 模块使模型能更准确地定位图像中的关键信息（如搜索栏文字、微小标签）。

#### 7. 优点
*   **架构创新**：成功将线性复杂度的 LFM 和 SSM 应用于多模态领域，打破了 Transformer 的计算瓶颈。
*   **轻量且精准**：通过 CMM 模块实现了低成本的“注意力”效果，解决了小模型容易忽略视觉细节的问题。
*   **落地潜力**：极高的推理效率使其非常适合端侧部署。

#### 8. 不足与局限
*   **物体幻觉风险**：在 POPE 基准测试中表现略逊于部分模型，论文推测这可能是由于 SSM 的隐式状态传播在抑制幻觉方面不如显式注意力机制有效。
*   **输入限制**：目前仅支持单图输入，尚未扩展到多图或视频序列。
*   **任务覆盖**：虽然在感知任务上表现优异，但在某些复杂的科学推理（SQA-I）上仍有提升空间。

（完）
