---
title: "PLUME: Latent Reasoning Based Universal Multimodal Embedding"
title_zh: PLUME：基于隐式推理的通用多模态嵌入
authors: "Chenwei He, Xiangzhao Hao, Tianyu Yang, Yuxiang Ma, Yuheng Jia, Lingxiang Wu, Chaoyang Zhao, Haiyun Guo, Jinqiao Wang"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02073v2"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大模型中复杂查询意图的通用多模态嵌入
tldr: 本研究针对通用多模态嵌入（UME）中显式思维链（CoT）推理开销大且存在文本瓶颈的问题，提出了PLUME框架。PLUME利用连续潜状态的短程自动回归演化取代了冗长的文本生成，并引入语义锚点引导适配器以支持多样化查询。通过渐进式训练策略，PLUME在推理时无需显式CoT，在MMEB-v2基准上实现了超越显式CoT模型的性能，且推理速度提升逾30倍，尤其擅长处理视频和视觉文档等复杂检索任务。
selection_source: fresh_fetch
motivation: 显式思维链推理在提升多模态嵌入性能的同时，带来了巨大的计算开销和文本表达瓶颈。
method: 提出一种潜空间推理框架，通过连续状态的自动回归演化替代文本生成，并采用语义锚点引导和渐进式课程学习进行优化。
result: 在包含78个任务的MMEB-v2基准上，PLUME在性能超过显式CoT基准的同时，实现了30倍以上的推理加速。
conclusion: 结构化潜空间计算能有效保留中间推理优势并消除生成开销，为构建高效实用的多模态检索系统提供了更优范式。
---

## 摘要
通用多模态嵌入（UME）通过单一模型将异构输入映射到共享的检索空间中。最近的方法通过在提取嵌入之前生成显式思维链（CoT）原理来改进 UME，使多模态大语言模型能够更好地推断复杂的查询意图。然而，显式 CoT 会产生巨大的推理开销，并可能将丰富的多模态证据压缩进狭窄的文本瓶颈中。我们提出了 PLUME，这是一个隐式推理框架，它通过将口语化 CoT 替换为连续隐状态的短自回归展开（rollout）来改进 UME。为了支持多样化的多模态查询，PLUME 进一步引入了一个语义锚点引导的转换适配器，在相同的固定计算预算下，引导隐式展开沿不同的推理轨迹进行。为了稳定训练，PLUME 采用了一种渐进式的“显式到隐式”课程学习，仅将口语化推理作为临时的训练支架，并逐渐将这种行为转移到隐状态计算中，从而在推理阶段消除显式 CoT。在包含 78 个任务的 MMEB-v2 基准测试中，PLUME 的性能优于强大的显式 CoT UME 基线模型，同时将推理过程从数百个生成的 token 减少到不到 10 个隐式步骤，实现了超过 30 倍的推理加速。PLUME 特别适用于相关证据密集、结构复杂且难以通过口语化中间原理进行组织的检索场景，例如视频和视觉文档检索。这些结果表明，结构化隐式计算可以保留中间推理的优势，而无需显式原理生成的开销，为实际检索系统提供了一种更强大、更高效的范式。

## Abstract
Universal multimodal embedding (UME) maps heterogeneous inputs into a shared retrieval space with a single model. Recent approaches improve UME by generating explicit chain-of-thought (CoT) rationales before extracting embeddings, enabling multimodal large language models to better infer complex query intent. However, explicit CoT incurs substantial inference overhead and can compress rich multimodal evidence into a narrow textual bottleneck. We propose PLUME, a latent reasoning framework that advances UME by replacing verbalized CoT with a short autoregressive rollout of continuous latent states. To support diverse multimodal queries, PLUME further introduces a semantic-anchor-guided transition adapter that steers latent rollout along different reasoning trajectories under the same fixed computation budget. To stabilize training, PLUME adopts a progressive explicit-to-latent curriculum that uses verbalized reasoning only as a temporary training scaffold and gradually transfers this behavior into hidden-state computation, eliminating explicit CoT at inference. On the 78-task MMEB-v2 benchmark, PLUME outperforms strong explicit-CoT UME baselines while reducing reasoning from hundreds of generated tokens to fewer than 10 latent steps, delivering over 30x faster inference. PLUME is especially well suited to retrieval settings where relevant evidence is dense, structurally complex, and difficult to organize through verbalized intermediate rationales, such as video and visual document retrieval. These results show that structured latent computation can preserve the benefits of intermediate reasoning without the overhead of explicit rationale generation, providing a stronger and more efficient paradigm for practical retrieval systems.

---

## 论文详细总结（自动生成）

以下是对论文《PLUME: Latent Reasoning Based Universal Multimodal Embedding》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不牺牲检索效率的前提下，利用多模态大语言模型（MLLM）的推理能力来提升通用多模态嵌入（UME）的质量？
*   **研究动机**：
    *   **现有方法的局限**：传统的单次前向传播方法难以处理复杂的查询意图（如空间理解、时序聚合）；而近期提出的“显式思维链（CoT）”方法虽然提升了精度，但面临**推理开销巨大**（生成数百个 token 导致高延迟）和**文本瓶颈**（将丰富的多模态信息压缩为离散文本会丢失细粒度特征）的双重挑战。
    *   **核心思路**：检索需要的是“中间计算”而非“中间文本”。因此，论文提出将推理过程从显式的文本生成转向隐式的连续向量空间演化。

### 2. 方法论
PLUME 提出了一个渐进式隐式推理框架，核心技术包括：
*   **隐式展开（Latent Rollout）**：在模型内部进行 $K$ 步自回归的隐状态转换，而非生成离散 token。每一轮迭代中，模型在隐藏层进行计算并更新 KV Cache，保留了推理的序列依赖结构。
*   **语义锚点引导的转换适配器（Transition Adapter）**：
    *   为了让有限的隐式计算步骤能适应不同模态（视频、文档、图像），引入了轻量级的**混合专家（MoE）适配器**。
    *   **语义锚点**：从输入前缀中提取一个固定的语义摘要向量，用于引导路由器的专家选择，确保推理路径与输入意图对齐。
*   **渐进式“显式到隐式”课程学习**：
    *   直接训练隐式推理不稳定。PLUME 先让模型学习生成显式 CoT，然后通过多个阶段逐渐将文本片段替换为隐式块（Latent Blocks），最终在推理阶段完全消除文本生成。
*   **损失函数**：结合了对比学习损失（InfoNCE）、因果语言建模损失（LCE）、专家平衡损失以及辅助的锚点嵌入损失。

### 3. 实验设计
*   **基准测试**：在 **MMEB-v2** 上进行评估，该基准包含 9 个元任务、共 **78 个子任务**，涵盖图像、视频、视觉文档（VisDoc）等多种模态。
*   **对比方法**：
    *   **早期 UME 基线**：LamRA, VLM2Vec, GME, VLM2Vec-V2, DUME。
    *   **推理增强型 UME**：UME-R1（采用显式 CoT 的强基线）。
*   **实验控制**：所有对比方法均统一使用 **Qwen2-VL-2B** 作为骨干网络，确保对比的公平性。

### 4. 资源与算力
*   **硬件**：实验主要在 **NVIDIA H20 GPU** 上完成。
*   **训练开销**：
    *   训练 4 步隐式推理约需 2562 H20 GPU 小时。
    *   训练 8 步隐式推理（默认配置）约需 **3119 H20 GPU 小时**。
*   **推理效率**：在单张 H20 上，PLUME 的吞吐量约为 3.3 samples/s，比显式 CoT 方法（UME-R1）快 **30.3 倍**。

### 5. 实验数量与充分性
*   **实验规模**：在 78 个任务上进行了大规模测试，结果具有统计学意义。
*   **消融实验**：非常充分，涵盖了隐式转换模块、MoE 结构、语义锚点、课程学习策略、隐式步数 $K$ 等关键变量。
*   **诊断分析**：通过可视化专家激活模式（证明了不同任务会调用不同专家）和隐式轨迹分析（证明了隐式推理比显式 CoT 更稳定），深入探讨了模型内部机制。
*   **评价**：实验设计严谨，通过控制变量（相同骨干和数据）确保了客观性。

### 6. 主要结论与发现
*   **性能超越**：PLUME 在 MMEB-v2 总分上达到 61.6，超过了显式 CoT 模型 UME-R1（60.1）和最强的单次传播模型 VLM2Vec-V2（58.0）。
*   **模态优势**：在**视频（+1.9）**和**视觉文档（+3.6）**任务上提升尤为显著，证明了连续隐式空间比离散文本更能保留复杂的时空和结构信息。
*   **效率飞跃**：仅需 8 步隐式计算即可达到甚至超过生成 400+ token 的推理效果，推理速度提升 30 倍以上。

### 7. 优点
*   **高效性**：成功打破了推理能力与检索延迟之间的矛盾，使推理增强型嵌入具备了实际部署的可能。
*   **架构创新**：引入语义锚点引导的 MoE 适配器，实现了输入自适应的推理路径分配。
*   **训练策略**：渐进式课程学习有效地解决了隐式推理难以收敛的问题。

### 8. 不足与局限
*   **特定任务劣势**：在图像问答（Image QA）子集上，PLUME 略逊于显式 CoT 方法，尤其是在需要精确文本读取或外部知识的任务（如 ChartQA, InfographicsVQA）中，这表明极度压缩的隐式表示在处理细粒度文本细节时仍有挑战。
*   **可解释性**：尽管进行了可视化分析，但连续隐式状态的推理逻辑相比文本 CoT 仍然缺乏直观的可解释性。
*   **训练成本**：虽然推理极快，但训练过程涉及多个课程阶段，且需要高质量的 CoT 数据作为“支架”，训练复杂度较高。

（完）
