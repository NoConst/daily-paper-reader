---
title: "Rocks, Pebbles and Sand: Modality-aware Scheduling for Multimodal Large Language Model Inference"
title_zh: 岩石、卵石与沙子：面向多模态大语言模型推理的模态感知调度
authors: "Konstantinos Papaioannou, Thaleia Dimitra Doudali"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26498v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大语言模型推理的模态感知调度
tldr: 针对多模态大模型（MLLM）推理中视频、图像和文本负载差异巨大导致的队头阻塞问题，本文提出了 RPS-Serve 调度系统。该系统将不同模态请求抽象为岩石（视频）、卵石（图像）和沙子（文本），通过模态感知的动态优先级调度和老化机制，让轻量级请求快速通过，同时避免重负载请求导致系统饥饿，显著提升了多模态推理的响应速度。
selection_source: fresh_fetch
motivation: 现有的 LLM 推理系统无法有效处理多模态负载的异构性，导致视频等大资源请求长时间占用资源并阻塞其他交互式请求。
method: 设计了 RPS-Serve 调度器，利用模态感知分类、动态优先级排序和老化算法来优化资源分配并防止请求饥饿。
result: "与现有系统相比，RPS-Serve 将整体平均首字延迟（TTFT）降低了 54%，对延迟敏感请求的 TTFT 降低了 78.5%。"
conclusion: 模态感知调度是提升多模态大模型推理效率的关键，RPS-Serve 成功在异构负载下实现了高效且具交互性的资源管理。
---

## 摘要
多模态大语言模型（MLLM）为 ChatGPT、Gemini 和 Copilot 等平台提供支持，实现了与文本、图像和视频更丰富的交互。这些异构工作负载引入了额外的推理阶段，如视觉预处理和编码，从而增加了延迟和内存需求。现有的 LLM 服务系统针对纯文本工作负载进行了优化，在多模态场景下表现不佳：大型请求（如视频）垄断了资源，导致严重的队头阻塞（head-of-line blocking）和性能下降。我们的核心见解是，多模态请求在资源需求上存在数量级的差异，我们通过一个简单的抽象来捕捉这一点：视频表现得像岩石，图像像卵石，而文本像沙子。我们设计了 RPS-Serve，这是一种模态感知调度器，它让“沙子”能够快速穿过“卵石”和“岩石”，在确保交互响应性的同时避免饥饿现象。RPS-Serve 对请求进行分类，动态调整其优先级，并应用老化（aging）机制以防止饥饿。对最先进 MLLM 的评估表明，与现有系统相比，RPS-Serve 将总体平均首字延迟（TTFT）降低了 54%，对于延迟敏感型请求则降低了 78.5%。通过模态感知调度并最有效地利用可用资源，RPS-Serve 为 MLLM 提供了类似于 LLM 的响应速度。

## Abstract
Multimodal Large Language Models (MLLMs) power platforms like ChatGPT, Gemini, and Copilot, enabling richer interactions with text, images, and videos. These heterogeneous workloads introduce additional inference stages, such as vision preprocessing and encoding, that inflate latency and memory demand. Existing LLM serving systems, optimized for text-only workloads, fail under multimodality: large requests (e.g., videos) monopolize resources, causing severe head-of-line blocking and performance degradation. Our key insight is that multimodal requests differ by orders of magnitude in resource demands, which we capture through a simple abstraction: videos behave like rocks, images like pebbles, and text like sand. We design RPS-Serve, a modality-aware scheduler that lets sand flow quickly through pebbles and rocks, ensuring interactive responsiveness while avoiding starvation. RPS-Serve classifies requests, prioritizes them dynamically, and applies aging to avoid starvation. Evaluation across state-of-the-art MLLMs shows that RPS-Serve reduces, on average, time-to-first-token (TTFT) by 54% overall, and by 78.5% for latency-critical requests, compared to current systems. RPS-Serve delivers LLM-like responsiveness for MLLMs, with modality-aware scheduling and by making the most efficient use of the available resources.

---

## 论文详细总结（自动生成）

### 论文总结：Rocks, Pebbles and Sand (RPS-Serve)

#### 1. 核心问题与研究动机
随着多模态大语言模型（MLLM）的普及，推理负载变得高度异构。与纯文本相比，图像和视频请求在显存占用和预填充（Prefill）延迟上高出 1 到 3 个数量级。现有的 LLM 推理系统（如 vLLM）多采用先来先服务（FCFS）调度，这导致大型视频请求（“岩石”）会长时间垄断资源，造成严重的**队头阻塞（Head-of-Line Blocking）**，使延迟敏感的文本请求（“沙子”）等待数秒甚至数十秒，严重破坏了交互式体验。

#### 2. 方法论
论文提出了 **RPS-Serve**，其核心思想是将请求抽象为**岩石（视频）、卵石（图像）和沙子（文本）**，并让“沙子”能够快速穿过“岩石”间的缝隙。
*   **工作负载分析器（Workload Profiler）**：离线分析不同模态在特定模型下的预处理、编码和预填充时间和内存特征。
*   **影响估计器（Impact Estimator）**：利用分位数回归（Quantile Regression）实时预测新请求的预填充延迟和 KV Cache 内存占用。
*   **智能分类器（Request Classifier）**：不简单按模态分类，而是基于资源特征（延迟和内存）通过聚类算法将请求归入三类队列。
*   **优先级调节器（Priority Regulator）**：
    *   **静态优先级**：沙子 > 卵石 > 岩石。
    *   **动态老化机制（Aging）**：引入公式 $Priority_c = StaticPriority_c + (1 - e^{-k_c \cdot waiting\_time})$。随着等待时间增加，重型请求的优先级会缓慢提升，以防止饥饿。
    *   **抢占与调度**：在每个迭代步动态调整批处理组合，确保高优先级请求优先执行。

#### 3. 实验设计
*   **数据集/场景**：
    *   **ShareGPT**（纯文本对话）。
    *   **LLaVA-Instruct**（图像推理）。
    *   **LLaVA-Video**（视频描述）。
*   **测试模型**：涵盖 LLaVA-7B、Gemma-4B/12B、Qwen-3B/7B、Pixtral-12B 等主流 MLLM。
*   **Benchmark 与对比方法**：
    *   **vLLM**：使用分块预填充（Chunked-prefill）的最先进推理系统。
    *   **EDF（Earliest Deadline First）**：基于截止日期的优先级调度算法。
*   **负载组合**：TO（纯文本）、ML（轻度多模态混合）、MH（重度多模态混合）。

#### 4. 资源与算力
*   **硬件环境**：单台服务器，配备 1 张 **NVIDIA A100 GPU (40GB)**，2 颗 AMD EPYC 7313 处理器，256GB 内存。
*   **训练/分析耗时**：离线分析器针对每个“模型-模态”对的分析平均耗时约 **20 分钟**。文中未涉及大规模模型训练，重点在于推理调度优化。

#### 5. 实验数量与充分性
论文进行了多维度的详尽实验：
*   **端到端性能对比**：在 4 种不同架构的模型上验证了 RPS-Serve 的普适性。
*   **消融实验**：分别验证了智能分类器、静态优先级和老化机制对性能的贡献。
*   **压力测试**：测试了在不同请求到达率（1.0 - 3.0 req/s）下的系统稳定性。
*   **敏感性分析**：探讨了 KV Cache 内存受限（100% 到 25%）以及不同 SLO（服务等级目标）标准对结果的影响。
实验设计客观、公平，覆盖了从轻量到极度拥塞的多种真实生产环境模拟。

#### 6. 主要结论与发现
*   **延迟大幅降低**：相比 vLLM，RPS-Serve 将整体平均首字延迟（TTFT）降低了 **54%**，对文本请求（沙子）的 TTFT 降低了 **78.5%**。
*   **SLO 达标率提升**：在重负载下，RPS-Serve 显著减少了违约率和违约严重程度，确保文本请求在 1 秒内响应。
*   **内存鲁棒性**：在显存极度紧张（仅剩 25%）时，RPS-Serve 依然能通过优先级管理维持文本请求的交互性，而传统系统会陷入瘫痪。

#### 7. 优点
*   **直观且有效的抽象**：将复杂的异构调度问题简化为“岩石、卵石、沙子”模型，易于理解和实现。
*   **模态感知而非模态绑定**：分类器基于资源需求而非简单的文件后缀，能处理“长文本比短视频更重”的特殊情况。
*   **即插即用**：作为调度层优化，不修改模型权重或注意力机制，兼容性强。

#### 8. 不足与局限
*   **模态覆盖有限**：目前仅验证了文本、图像、视频，尚未涵盖音频、3D 等其他模态。
*   **单节点限制**：实验主要在单 GPU 节点上完成，未深入探讨多 GPU 或分布式集群中的模型并行/流水线并行下的调度表现。
*   **离线依赖**：需要针对新模型进行预先的离线 Profile，对于频繁更新的模型部署可能存在一定的运维开销。

（完）
