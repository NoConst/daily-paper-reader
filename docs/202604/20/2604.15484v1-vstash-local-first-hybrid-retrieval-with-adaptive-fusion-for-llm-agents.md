---
title: "vstash: Local-First Hybrid Retrieval with Adaptive Fusion for LLM Agents"
title_zh: vstash：面向大语言模型智能体的自适应融合本地优先混合检索系统
authors: Jayson Steffens
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15484v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 面向大模型智能体的本地优先混合检索文档记忆系统
tldr: 本研究提出 vstash，一个面向 LLM 智能体的本地优先混合检索系统。它结合向量搜索与全文检索，利用 SQLite 实现高效存储。核心贡献包括：通过检索结果不一致性进行自监督微调，显著提升小模型性能；引入自适应 IDF 加权的 RRF 融合算法；并提供了一个低延迟、生产级的检索框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15484v1/fig-001.webp\", \"caption\": \"Table 3: Adaptive vs fixed RRF weights on 5 BEIR datasets (NDCG@10)\", \"page\": 6, \"index\": 1, \"width\": 981, \"height\": 297}, {\"url\": \"assets/figures/arxiv/2604.15484v1/fig-002.webp\", \"caption\": \"Table 7: Hybrid RRF at scale – 1,000 ML papers, 35 topic-based queries\", \"page\": 11, \"index\": 2, \"width\": 980, \"height\": 272}, {\"url\": \"assets/figures/arxiv/2604.15484v1/fig-003.webp\", \"caption\": \"Figure 1: vstash architecture.\", \"page\": 3, \"index\": 3, \"width\": 730, \"height\": 614}, {\"url\": \"assets/figures/arxiv/2604.15484v1/fig-004.webp\", \"caption\": \"Table 8: Search latency across corpus sizes\", \"page\": 12, \"index\": 4, \"width\": 979, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2604.15484v1/fig-005.webp\", \"caption\": \"Table 2: Distance-based relevance signal on BEIR\", \"page\": 5, \"index\": 5, \"width\": 640, \"height\": 196}]"
motivation: 旨在解决 LLM 智能体在本地环境下高效、准确检索文档记忆的需求，并降低对大规模模型的依赖。
method: 采用基于 SQLite 的向量与全文检索混合架构，利用检索差异进行自监督微调，并结合自适应 IDF 权重的 RRF 算法。
result: 经过微调的 33M 模型在多个数据集上性能提升显著，部分指标超越了 110M 的 ColBERTv2，延迟仅 20.9ms。
conclusion: vstash 证明了通过自监督微调和自适应融合，轻量级本地检索系统也能在保持高性能的同时达到生产级可靠性。
---

## 摘要
我们介绍了 vstash，这是一个本地优先（local-first）的文档记忆系统，它通过倒数排名融合（Reciprocal Rank Fusion, RRF）和自适应单查询 IDF 加权，将向量相似度搜索与全文关键词匹配相结合。所有数据均存储在单个 SQLite 文件中，利用 sqlite-vec 进行近似最近邻搜索，并使用 FTS5 进行关键词匹配。我们做出了四个主要贡献：(1) 通过混合检索不一致性进行自监督嵌入微调：在 SciFact、NFCorpus 和 FiQA 的 753 个 BEIR 查询中，74.5% 的查询在重向量搜索（vec=0.95, fts=0.05）与重全文搜索（vec=0.05, fts=0.95）之间产生了前 10 名结果的不一致（各数据集比例分别为 63.4% / 73.4% / 86.7%，见 5.2 节），这在无需人工标注的情况下提供了免费的训练信号。使用 MultipleNegativesRankingLoss 在 7.6 万个不一致三元组上对 BGE-small（3300 万参数）进行微调，提升了所有 5 个 BEIR 数据集的 NDCG@10（在 NFCorpus 上相比 BGE-small 基础 RRF 提升高达 +19.5%，见表 6）。在 3 个数据集中，经过不同预处理的微调 33M 参数流水线达到或超过了已发表的 ColBERTv2（1.1 亿参数）和未经训练的 BGE-base（1.1 亿参数）的结果；在 FiQA 和 ArguAna 上表现逊于 ColBERTv2（见 5.5 节）。(2) 带有单查询 IDF 加权的自适应 RRF：与固定权重相比，该方法在所有 5 个 BEIR 数据集上均提升了 NDCG@10（在 ArguAna 上提升高达 +21.4%），在 SciFact 上配合 BGE-small 达到了 0.7263。(3) 关于 RRF 后评分的负面结果：频率+衰减、历史增强召回以及交叉编码器（cross-encoder）重排序均未能提升 NDCG。(4) 生产级底层架构：具备完整性检查、模式版本控制、排名诊断以及基于距离的相关性信号，并在 5 个 BEIR 数据集的 50,425 个经过相关性判定的查询中得到了验证。在 5 万个分块（chunks）下，搜索中值延迟保持在 20.9 毫秒，且 NDCG 表现稳定。微调后的模型已在 HuggingFace 发布为 Stffens/bge-small-rrf-v2。所有代码、数据和实验均已开源。

## Abstract
We present **vstash**, a local-first document memory system that combines vector similarity search with full-text keyword matching via Reciprocal Rank Fusion (RRF) and adaptive per-query IDF weighting. All data resides in a single SQLite file using sqlite-vec for approximate nearest neighbor search and FTS5 for keyword matching.   We make four primary contributions. **(1)** Self-supervised embedding refinement via hybrid retrieval disagreement: across 753 BEIR queries on SciFact, NFCorpus, and FiQA, 74.5% produce top-10 disagreement between vector-heavy (vec=0.95, fts=0.05) and FTS-heavy (vec=0.05, fts=0.95) search (per-dataset rates 63.4% / 73.4% / 86.7%, Section 5.2), providing a free training signal without human labels. Fine-tuning BGE-small (33M params) with MultipleNegativesRankingLoss on 76K disagreement triples improves NDCG@10 on all 5 BEIR datasets (up to +19.5% on NFCorpus vs. BGE-small base RRF, Table 6). On 3 of 5 datasets, under different preprocessing, the tuned 33M-parameter pipeline matches or exceeds published ColBERTv2 results (110M params) and an untrained BGE-base (110M); on FiQA and ArguAna it underperforms ColBERTv2 (Section 5.5). **(2)** Adaptive RRF with per-query IDF weighting improves NDCG@10 on all 5 BEIR datasets versus fixed weights (up to +21.4% on ArguAna), achieving 0.7263 on SciFact with BGE-small. **(3)** A negative result on post-RRF scoring: frequency+decay, history-augmented recall, and cross-encoder reranking all failed to improve NDCG. **(4)** A production-grade substrate with integrity checking, schema versioning, ranking diagnostics, and a distance-based relevance signal validated on 50,425 relevance-judged queries across the 5 BEIR datasets.   Search latency remains 20.9 ms median at 50K chunks with stable NDCG. The fine-tuned model is published as `Stffens/bge-small-rrf-v2` on HuggingFace. All code, data, and experiments are open-source.

---

## 论文详细总结（自动生成）

以下是对论文《vstash: Local-First Hybrid Retrieval with Adaptive Fusion for LLM Agents》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）智能体需要持久化记忆，但现有的云端向量数据库对于开发者工具、个人知识管理及隐私敏感等“本地优先（Local-First）”场景过于沉重。
*   **研究动机**：
    *   **检索质量瓶颈**：纯向量搜索易丢失精确关键词（如错误代码），纯关键词搜索则缺乏语义理解。
    *   **缺乏置信度信号**：现有系统难以区分“找到了相关内容”与“返回了最不无关的内容”。
    *   **时间感知缺失**：大多数 RAG 系统无法有效处理文档的访问频率和衰减。
    *   **资源效率**：如何在资源受限的本地环境下，利用小参数模型达到大模型的检索效果。

### 2. 方法论
*   **核心架构**：基于单文件 SQLite 数据库，集成 `sqlite-vec`（向量搜索）和 `FTS5`（全文检索）。
*   **自适应 RRF 融合**：
    *   使用倒数排名融合（RRF）合并向量和全文检索结果。
    *   **自适应 IDF 加权**：根据查询词的逆文档频率（IDF）动态调整权重。罕见词（高 IDF）增加全文检索权重，常见词（低 IDF）增加向量搜索权重。
*   **自监督嵌入微调（核心创新）**：
    *   **不一致性信号**：利用向量搜索与全文检索结果之间的“不一致”作为训练信号。如果两者对前 10 名结果产生分歧，则认为该样本具有学习价值。
    *   **三元组生成**：自动生成（查询、正样本、硬负样本）三元组，无需人工标注。
    *   **损失函数**：采用 `MultipleNegativesRankingLoss` (MNRL) 对 33M 参数的 BGE-small 模型进行微调。
*   **后处理技术**：
    *   **MMR 去重**：文档内最大边际相关性（MMR）算法，提升结果多样性。
    *   **上下文扩展**：自动获取检索分块的前后相邻分块，丰富 LLM 的上下文。
    *   **距离置信度**：利用向量余弦距离作为相关性信号（而非复杂的评分模型）。

### 3. 实验设计
*   **数据集**：使用了 5 个 BEIR 标准基准数据集（SciFact, NFCorpus, FiQA, SciDocs, ArguAna），以及自定义的 arXiv 论文库和维基百科语料。
*   **Benchmark 与对比方法**：
    *   **基准**：BM25 (Elasticsearch)、ColBERTv2 (110M 参数)、未经训练的 BGE-base (110M 参数)。
    *   **对比**：对比了固定权重 RRF 与自适应 IDF RRF，以及微调前后的模型性能。
*   **评估指标**：NDCG@10、Precision@k、MRR、搜索延迟（Latency）。

### 4. 资源与算力
*   **推理算力**：使用 FastEmbed (ONNX Runtime)，在 CPU 上可达到约 700 chunks/sec 的嵌入速度。
*   **训练算力**：文中提到对 BGE-small (33M) 进行了 2 个 epoch 的微调，学习率为 3e-6，batch size 为 64。**未明确说明具体的 GPU 型号和总训练时长**，但鉴于模型仅 33M 参数且数据量为 76K 三元组，该训练过程在消费级 GPU 上应非常迅速。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 753 个 BEIR 查询上分析了检索不一致性。
    *   在 50,425 个经过判定的查询上验证了相关性信号。
    *   进行了 5 万个分块规模的合成扩展性测试。
*   **充分性与客观性**：实验覆盖了消融实验（RRF vs. 向量 vs. 全文）、不同数据集的横向对比、以及针对失败策略（如交叉编码器重排序）的负面结果记录。实验设计较为客观，承认了在某些特定领域（如 FiQA）的局限性。

### 6. 主要结论与发现
*   **微调效果显著**：仅 33M 参数的微调模型在 3/5 的数据集上达到或超过了 110M 参数的 ColBERTv2 性能。在 NFCorpus 上 NDCG@10 提升了 19.5%。
*   **自适应融合有效**：IDF 加权在 ArguAna 数据集上带来了 21.4% 的性能提升。
*   **负面结果**：发现 RRF 之后的复杂评分（如频率衰减、交叉编码器重排序）在标准基准上**未能**提升检索质量，提升性能的关键在于嵌入模型本身的微调。
*   **性能稳定**：在 5 万个分块的规模下，中值延迟仅 20.9ms，且检索质量（NDCG）保持稳定。

### 7. 优点
*   **自监督闭环**：提出了一种无需人工标注、利用系统内部检索差异进行自我进化的方法。
*   **生产级设计**：不仅是算法研究，还包含了完整性检查、模式版本控制、MCP 服务器集成等工程化实现。
*   **轻量高效**：证明了通过精巧的训练数据选择，小模型可以挑战参数量大其 3 倍的模型。
*   **开源透明**：代码、数据、微调模型及实验脚本全部开源。

### 8. 不足与局限
*   **领域依赖性**：基于距离的相关性信号在跨领域（如 ArguAna）表现极佳（F1=0.996），但在域内（如 NFCorpus）表现较差（F1=0.472）。
*   **训练分布偏差**：微调数据主要来自科学声明和问答，导致在长篇反驳论点（ArguAna）等特殊分布上提升有限。
*   **规模上限**：实验最高仅测试至 5 万个分块，对于百万级以上超大规模本地数据的表现尚不明确。
*   **LLM 评估样本量**：端到端答案相关性评估仅使用了每组 30 个查询，样本量较小，不具统计学显著性。

（完）
