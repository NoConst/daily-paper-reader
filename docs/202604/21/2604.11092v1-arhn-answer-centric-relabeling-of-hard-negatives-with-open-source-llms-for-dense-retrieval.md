---
title: "ARHN: Answer-Centric Relabeling of Hard Negatives with Open-Source LLMs for Dense Retrieval"
title_zh: ARHN：利用开源大语言模型进行以答案为中心的稠密检索难负样本重标注
authors: "Hyewon Choi, Jooyoung Choi, Hansol Jang, Hyun Kim, Chulmin Yun, ChangWook Jun, Stanley Jungkyu Choi"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11092v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 利用大语言模型对密集检索中的硬负样本进行重新标注
tldr: 针对神经检索模型训练中硬负样本挖掘引入的标签噪声问题，本文提出ARHN框架。该框架利用开源大模型，通过生成答案片段和列表排序两阶段，对候选样本进行以答案为中心的重新标注。ARHN能有效识别假负样本并过滤模糊样本，为检索模型提供更精准的监督信号，在BEIR基准测试中显著提升了检索效果，且具有成本效益和可扩展性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11092v1/fig-001.webp\", \"caption\": \"Figure 1: Intuition of ARHN. (Left) Conventional hardnegative mining can include answer-bearing documents as negatives, introducing false negatives. (Right) Using extracted answer snippets, ARHN relabels answer-bearing negatives as positives and filters ambiguous negatives to refine supervision.\", \"page\": 1, \"index\": 1, \"width\": 494, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.11092v1/fig-002.webp\", \"caption\": \"Figure 4: Effect of LLM scale used in ARHN labeling (Stage 1– 2) on retrieval performance. The plot reports nDCG@10 on BEIR (Avg. 16) for an E5-base retriever fine-tuned on ARHN(R+F)-refined data, comparing No Refinement with Qwen3-8B/14B/32B. Performance improves with larger LLMs, and Qwen3-32B achieves the best nDCG@10.\", \"page\": 6, \"index\": 2, \"width\": 419, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2604.11092v1/fig-003.webp\", \"caption\": \"Table 1: nDCG@10 on 16 BEIR datasets for E5-base and LG-ANNA-Embedding (Mistral-7B), comparing No Refinement, baselines from prior work, and ARHN variants (Filter/Relabel/R+F). Datasets marked with *correspond to the 7 out-of-domain (OOD) datasets not seen during training; we report the average over all 16 datasets (Avg. 16) and over the 7 OOD datasets (Avg. 7) at the bottom. Best results for each encoder are in bold.\", \"page\": 6, \"index\": 3, \"width\": 1056, \"height\": 390}, {\"url\": \"assets/figures/arxiv/2604.11092v1/fig-004.webp\", \"caption\": \"Table 2: nDCG@10 comparison between PRHN and ARHN on 16 BEIR datasets, using an E5-base retriever fine-tuned on the corresponding refined training data. PRHN performs the Stage 2 listwise ranking directly on the original passages, whereas ARHN first generates an answer snippet for each passage in Stage 1 and then conducts Stage 2 ranking conditioned on the extracted snippets.\", \"page\": 7, \"index\": 4, \"width\": 506, \"height\": 533}, {\"url\": \"assets/figures/arxiv/2604.11092v1/fig-005.webp\", \"caption\": \"Figure 2: Overview of the two-stage ARHN pipeline. Given a query 𝑞, the original training instance consists of a positive document 𝐴 and hard negative documents {𝐵,𝐶, 𝐷}. In Stage 1, an open-source LLM extracts an answer snippet for the query from the document text; if no supporting evidence is found, it outputs NO_ANSWER (e.g., 𝐷). In Stage 2, the extracted snippets are compared in a listwise manner to produce an ordering (e.g., 𝐵 > 𝐴 > 𝐶 > 𝐷), which is then used to reconstruct labels.\", \"page\": 3, \"index\": 5, \"width\": 998, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2604.11092v1/fig-006.webp\", \"caption\": \"Table 5: Examples of label noise in retrieval training (FiQA-2018, NQ, MS MARCO). Highlighted text denotes an answer snippet. We contrast labeled positives with answer-bearing negatives, illustrating complementary evidence, more specific answers, true-answer false negatives, and contaminated negatives with overlapping answer snippets.\", \"page\": 8, \"index\": 6, \"width\": 1056, \"height\": 798}, {\"url\": \"assets/figures/arxiv/2604.11092v1/fig-007.webp\", \"caption\": \"Figure 3: Stage 1–2 prompts of ARHN. In Stage 1, the LLM extracts an answer snippet from each query–document pair as a verbatim contiguous span (or outputs NO_ANSWERwhen no relevant evidence exists) to verify whether a candidate contains answer-supporting evidence. In Stage 2, the LLM performs answer-centric listwise reranking over the extracted snippets by producing a total order based on how well each snippet answers or supports the query; snippet[1] corresponds to the positive document’s an answer snippet.\", \"page\": 4, \"index\": 7, \"width\": 487, \"height\": 790}]"
motivation: 传统的硬负样本挖掘常引入包含答案的假负样本，导致训练监督信号不一致并损害检索性能。
method: 采用开源大模型先生成基于段落的答案片段，再通过列表排序将高相关段落重标为正样本并过滤含答案的模糊负样本。
result: 在BEIR基准上的实验表明，结合重标注与过滤的策略在多个数据集上均优于单一策略。
conclusion: ARHN通过开源大模型实现了高效且低成本的负样本精炼，为大规模神经检索模型训练提供了更清洁的监督数据。
---

## 摘要
神经检索器通常在由查询、正向段落和一组难负样本组成的大规模三元组数据上进行训练。在实践中，难负样本挖掘可能会引入假负样本和其他模糊负样本，包括与查询相关或包含部分答案的段落。这种标签噪声会导致监督信号不一致，并可能降低检索效果。我们提出了 ARHN（以答案为中心的难负样本重标注），这是一个两阶段框架，利用开源大语言模型（LLM）通过以答案为中心的相关性信号来优化难负样本。在第一阶段，对于每个查询-段落对，ARHN 提示 LLM 生成基于段落的答案片段，或指出该段落不支持回答。在第二阶段，ARHN 对候选集应用基于 LLM 的列表排序（listwise ranking），根据对查询的直接可回答性对段落进行排序。排名高于原始正样本的段落被重新标注为额外的正样本。在排名低于正样本的段落中，ARHN 从负样本集中排除任何包含答案片段的段落，以避免模糊监督。我们在 BEIR 基准测试上评估了 ARHN 的三种配置：仅重标注、仅过滤以及两者的结合。在各个数据集上，结合策略的效果始终优于任何单一步骤，这表明联合重标注假负样本和过滤模糊负样本能为训练神经检索模型提供更清晰的监督。通过严格依赖开源模型，ARHN 建立了一个具有成本效益且可扩展的优化流水线，适用于大规模训练。

## Abstract
Neural retrievers are often trained on large-scale triplet data comprising a query, a positive passage, and a set of hard negatives. In practice, hard-negative mining can introduce false negatives and other ambiguous negatives, including passages that are relevant or contain partial answers to the query. Such label noise yields inconsistent supervision and can degrade retrieval effectiveness.   We propose ARHN (Answer-centric Relabeling of Hard Negatives), a two-stage framework that leverages open-source LLMs to refine hard negative samples using answer-centric relevance signals. In the first stage, for each query-passage pair, ARHN prompts the LLM to generate a passage-grounded answer snippet or to indicate that the passage does not support an answer. In the second stage, ARHN applies an LLM-based listwise ranking over the candidate set to order passages by direct answerability to the query. Passages ranked above the original positive are relabeled to additional positives. Among passages ranked below the positive, ARHN excludes any that contain an answer snippet from the negative set to avoid ambiguous supervision.   We evaluated ARHN on the BEIR benchmark under three configurations: relabeling only, filtering only, and their combination. Across datasets, the combined strategy consistently improves over either step in isolation, indicating that jointly relabeling false negatives and filtering ambiguous negatives yields cleaner supervision for training neural retrieval models. By relying strictly on open-source models, ARHN establishes a cost-effective and scalable refinement pipeline suitable for large-scale training.

---

## 论文详细总结（自动生成）

这是一份关于论文《ARHN: Answer-Centric Relabeling of Hard Negatives with Open-Source LLMs for Dense Retrieval》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
稠密检索模型（Dense Retrievers）通常采用对比学习训练，依赖“查询-正样本-难负样本”三元组。然而，传统的难负样本挖掘（Hard Negative Mining）常会引入**假负样本（False Negatives）**——即那些实际上与查询相关或包含答案，却被错误标注为负样本的文档。
*   **研究动机**：这种标签噪声会导致监督信号不一致，扭曲嵌入空间，降低检索性能。
*   **核心目标**：利用开源大语言模型（LLM）的语义理解能力，通过识别文档中的“答案证据”来重新校准难负样本的标签，从而提供更纯净的训练数据。

### 2. 方法论：ARHN 框架
ARHN 采用两阶段流程，核心思想是**以答案为中心（Answer-Centric）**进行评估：

*   **第一阶段：答案片段生成（Answer Snippet Generation）**
    *   对于每个查询-文档对，提示 LLM 从文档中**原样提取**（Verbatim）能够回答查询的片段。
    *   如果文档不含答案，LLM 必须输出 `NO_ANSWER`。这一步通过约束提取而非生成，减少了幻觉风险。
*   **第二阶段：以答案为中心的列表排序与重标注（Reranking & Relabeling）**
    *   **列表排序（Listwise Reranking）**：将原始正样本的片段与所有负样本的片段放在一起，让 LLM 根据“回答的直接性”进行排序。
    *   **重标注逻辑**：
        1.  **正向重标（Relabeling）**：若某个负样本的片段排名高于原始正样本，则将其提升为正样本。
        2.  **过滤（Filtering）**：若负样本包含答案片段但排名低于正样本，视为“模糊样本”，从负样本集中剔除。
        3.  **保留（Retention）**：只有那些被 LLM 判定为 `NO_ANSWER` 的文档才作为真正的难负样本保留。

### 3. 实验设计
*   **数据集**：在 **BEIR 基准测试**（包含 16 个数据集，如 NQ, HotpotQA, MS MARCO 等）上进行评估。
*   **检索模型（Backbones）**：
    *   **E5-base**（基于 BERT，110M 参数）。
    *   **LG-ANNA-Embedding**（基于 Mistral-7B 的指令微调嵌入模型）。
*   **对比方法**：
    *   **No Refinement**：原始 BGE 训练集。
    *   **TopK-PercPos**：基于重排序器分数的启发式过滤。
    *   **RLHN**：使用 GPT-4o-mini/GPT-4o 进行重标注的现有先进方法。
*   **消融实验**：对比了“仅过滤”、“仅重标注”以及“两者结合（R+F）”的效果。

### 4. 资源与算力
*   **算力设备**：使用了 **16 台 NVIDIA A100 GPU** 进行 LLM 推理（用于数据标注）。
*   **模型微调**：E5-base 和基于 Mistral-7B 的模型微调同样在 A100 GPU 上完成。
*   **LLM 规模**：实验对比了 Qwen3-8B、14B 和 32B 三种规模的开源模型。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 16 个不同领域的检索数据集，并区分了训练内（In-domain）和域外（OOD）表现。
*   **充分性**：
    *   进行了 **LLM 规模实验**，验证了模型能力对标注质量的影响。
    *   进行了 **人工验证**，计算了 LLM 标注与人类判断的 Cohen’s Kappa 系数（一致性随模型规模增加）。
    *   对比了**段落中心（PRHN）**与**答案中心（ARHN）**的差异，证明了提取片段的优越性。
*   **客观性**：通过多随机种子运行并取平均值，确保了结果的可靠性。

### 6. 主要结论与发现
*   **协同效应**：同时进行“重标注”和“过滤”的效果最好，在 BEIR 平均分上显著超过原始基准。
*   **域外泛化（OOD）**：ARHN 在未见过的域外数据集上提升更明显（E5-base 提升 2.1 个点），说明减少标签噪声能显著增强模型的鲁棒性。
*   **开源胜闭源**：使用 **Qwen3-32B** 的 ARHN 效果优于使用 GPT-4 接口的 RLHN，证明了高质量开源模型在数据清洗任务中的潜力。
*   **规模效应**：较小的 LLM（如 8B）可能会因为误判引入新噪声，导致性能下降；32B 模型则表现稳定。

### 7. 优点
*   **精度高**：通过“答案片段提取”而非“全文对比”，消除了文档背景信息的干扰，使排序更聚焦。
*   **成本效益**：完全依赖开源模型，避免了昂贵的商业 API 调用，适合大规模工业级应用。
*   **逻辑严密**：将假负样本细分为“更优正样本”和“模糊样本”，处理方式更细腻。

### 8. 不足与局限
*   **模型依赖性**：该方法高度依赖 LLM 的推理能力，若 LLM 规模不足，可能会适得其反。
*   **计算开销**：虽然模型开源，但对数百万条数据进行两阶段 LLM 推理仍需要大量的 GPU 算力支持。
*   **语言限制**：实验主要集中在英文数据集，尚未验证在多语言或跨语言检索场景下的有效性。
*   **最大长度限制**：由于 LLM 上下文限制，若答案出现在文档极靠后的位置，可能会被截断导致漏判。

（完）
