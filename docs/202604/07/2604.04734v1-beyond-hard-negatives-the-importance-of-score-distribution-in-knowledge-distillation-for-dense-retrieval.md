---
title: "Beyond Hard Negatives: The Importance of Score Distribution in Knowledge Distillation for Dense Retrieval"
title_zh: 超越难负样本：稠密检索知识蒸馏中分数分布的重要性
authors: "Youngjoon Jang, Seongtae Hong, Hyeonseok Moon, Heuiseok Lim"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04734v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 密集检索中的知识蒸馏
tldr: 本研究探讨了稠密检索知识蒸馏中训练数据组成对模型性能的影响。传统方法侧重于挖掘难负样本，但忽略了教师模型评分分布的完整性。作者提出分层采样策略，通过均匀覆盖整个评分谱来模拟教师的偏好结构。实验证明，该方法在领域内和跨领域基准测试中均优于Top-K和随机采样，强调了保持教师评分多样性的重要性。
selection_source: fresh_fetch
motivation: 现有的检索模型蒸馏过度关注难负样本，导致学生模型无法学习到教师模型全面的偏好结构，限制了泛化能力。
method: 提出一种分层采样策略，通过在整个评分范围内均匀采样，以保留教师评分的方差和熵。
result: 在多个领域内和跨领域基准测试中，分层采样显著优于传统的Top-K采样和随机采样。
conclusion: 知识蒸馏的核心在于保留教师模型感知的多样化相对评分范围，而非仅仅关注极少数的难负样本。
---

## 摘要
通过知识蒸馏（KD）从交叉编码器（cross-encoder）教师模型迁移知识，已成为训练检索模型的标准范式。虽然现有研究主要集中在挖掘难负样本以提高区分度，但训练数据的系统构成以及由此产生的教师分数分布受到的关注相对较少。在这项工作中，我们强调仅关注难负样本会阻碍学生模型学习教师模型全面的偏好结构，从而可能损害泛化能力。为了有效模拟教师分数分布，我们提出了一种分层采样（Stratified Sampling）策略，该策略均匀地覆盖了整个分数谱。在域内和跨域基准测试上的实验证实，保留了教师分数方差和熵的分层采样是一种鲁棒的基准方法，在各种设置下均显著优于 top-K 采样和随机采样。这些发现表明，蒸馏的本质在于保留教师模型所感知的多样化相对分数范围。

## Abstract
Transferring knowledge from a cross-encoder teacher via Knowledge Distillation (KD) has become a standard paradigm for training retrieval models. While existing studies have largely focused on mining hard negatives to improve discrimination, the systematic composition of training data and the resulting teacher score distribution have received relatively less attention. In this work, we highlight that focusing solely on hard negatives prevents the student from learning the comprehensive preference structure of the teacher, potentially hampering generalization. To effectively emulate the teacher score distribution, we propose a Stratified Sampling strategy that uniformly covers the entire score spectrum. Experiments on in-domain and out-of-domain benchmarks confirm that Stratified Sampling, which preserves the variance and entropy of teacher scores, serves as a robust baseline, significantly outperforming top-K and random sampling in diverse settings. These findings suggest that the essence of distillation lies in preserving the diverse range of relative scores perceived by the teacher.

---

## 论文详细总结（自动生成）

以下是对论文《Beyond Hard Negatives: The Importance of Score Distribution in Knowledge Distillation for Dense Retrieval》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在稠密检索（Dense Retrieval）领域，通过知识蒸馏（KD）将交叉编码器（Cross-Encoder）教师模型的知识迁移给双编码器（Bi-Encoder）学生模型已成为标准做法。
*   **核心问题**：现有的蒸馏方法过度关注“难负样本挖掘”（Hard Negative Mining），即优先选择教师评分最高的负样本。作者认为，这种启发式采样仅让学生模型观察到教师评分分布的一个极小且有偏的片段，忽略了教师模型完整的“偏好结构”，从而限制了模型的泛化能力（尤其是跨域表现）。
*   **研究动机**：探索训练数据中教师评分的分布多样性对蒸馏效果的影响，并重新定义蒸馏数据组合的标准。

### 2. 方法论
作者提出了一种**分层采样（Stratified Sampling）**策略，旨在均匀覆盖教师的分数谱：
*   **候选池构建**：为每个查询构建一个包含200个负样本的固定池（100个由检索器挖掘的难负样本 + 100个从语料库随机抽取的样本），以扩大难度范围。
*   **分数归一化**：使用教师模型（Qwen3-Reranker-8B）对候选池评分，并进行查询级的 Min-Max 归一化，将分数映射到 $[0, 1]$ 区间。
*   **分层采样逻辑**：
    1.  根据预设的样本数 $K$，在 $[0, 1]$ 区间内均匀设置 $K$ 个分位数锚点（Quantile Anchors）。
    2.  在候选池中寻找与每个锚点分数最接近的文档。
    3.  这种方法确保了选中的 $K$ 个样本在教师评分分布中具有代表性，保留了分数的方差和熵。
*   **训练流程**：采用两阶段训练。第一阶段通过对比学习（CL）进行基础适配；第二阶段使用 KL 散度（KL-Divergence）或 MarginMSE 损失函数进行知识蒸馏。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：MS MARCO-Train。
    *   **域内评估**：MS MARCO Dev、TREC DL 19。
    *   **跨域评估**：BEIR 基准测试（包含 13 个多样化的检索数据集）。
*   **基准模型（Student Backbones）**：BERT-base、DistilBERT-base、Co-condenser。
*   **对比方法（采样策略）**：
    1.  **retriever-top**：检索器排名的前 K 个。
    2.  **reranker-top**：教师评分最高的前 K 个（传统难负样本）。
    3.  **low/mid**：教师评分最低或居中的样本。
    4.  **random**：随机采样。
    5.  **stratified**：本文提出的分层采样。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **超参数细节**：提到了对比学习阶段 Batch Size 为 1024，蒸馏阶段为 16；最大长度 512；学习率 2e-5；各阶段训练 1 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：作者在 3 种不同的学生模型架构和 2 种主流蒸馏损失函数上进行了交叉验证，并在 15 个测试集（2 域内 + 13 跨域）上评估。
*   **充分性评价**：实验设计较为充分且客观。通过固定候选池的方法，排除了动态挖掘器带来的干扰变量，严格孤立了“采样策略”这一变量。此外，还进行了关于样本数量 $K$ 的敏感性分析（$K \in \{4, 8, 16\}$）以及评分分布多样性的统计分析。

### 6. 主要结论与发现
*   **分布多样性至关重要**：分层采样在所有模型和损失函数下均表现最稳健，显著优于传统的 Top-K 采样。
*   **难负样本的局限性**：仅使用 `reranker-top`（极难样本）会导致模型在跨域任务上表现不稳定，甚至在 MarginMSE 损失下出现训练崩溃（性能接近于零）。
*   **统计相关性**：实验证明，采样数据的分数覆盖范围（Coverage）、熵（Entropy）和标准差（Std）与最终检索性能呈正相关。
*   **鲁棒性**：随着样本数 $K$ 的增加，分层采样能系统地填补分数空白，提供互补的学习信号，而其他方法可能引入冗余。

### 7. 优点
*   **简单高效**：分层采样是一种确定性的、无参数的策略，不需要复杂的课程学习调度或辅助损失函数。
*   **理论深刻**：挑战了“负样本越难越好”的传统直觉，强调了模拟教师完整偏好结构的重要性。
*   **泛化性强**：在 BEIR 跨域测试中的优异表现证明了该方法能有效提升模型的通用检索能力。

### 8. 不足与局限
*   **静态池限制**：实验基于固定的候选池，未探讨在动态挖掘（Dynamic Mining）场景下该策略的实时表现。
*   **教师模型依赖**：该方法高度依赖教师模型评分的准确性，若教师模型本身对某些域存在偏见，分层采样可能会放大这种偏见。
*   **计算开销**：虽然采样本身简单，但为了进行分层，需要教师模型对较多候选文档（如文中 200 个）进行预评分，这在超大规模数据集上会增加预处理成本。

（完）
