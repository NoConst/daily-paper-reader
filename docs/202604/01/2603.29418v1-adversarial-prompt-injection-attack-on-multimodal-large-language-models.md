---
title: Adversarial Prompt Injection Attack on Multimodal Large Language Models
title_zh: 多模态大语言模型上的对抗性提示注入攻击
authors: "Meiwen Ding, Song Xia, Chenqi Kong, Xudong Jiang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29418v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 专门针对多模态大语言模型（MLLM）的对抗性提示注入攻击研究
tldr: 本研究针对多模态大模型（MLLM）提出了一种不可见的视觉提示注入攻击方法。针对现有攻击易被察觉的问题，该方法通过在输入图像中嵌入受限的文本叠加层提供语义引导，并结合迭代优化的不可见视觉扰动，使图像特征在多个粒度上与恶意目标对齐。实验证明，该方法能有效攻击包括闭源模型在内的多种MLLM，在保持视觉不可见性的同时，展现出比现有方法更强的攻击性能和迁移性。
selection_source: fresh_fetch
motivation: 现有的多模态提示注入攻击通常依赖于人类可察觉的视觉或文本提示，缺乏对隐蔽性更强的不可见攻击手段的探索。
method: 结合受限文本叠加层的语义引导与迭代优化的不可见视觉扰动，通过粗细粒度特征对齐将恶意指令嵌入图像。
result: 在多项任务和多个闭源多模态大模型上的实验验证了该方法具有卓越的攻击成功率和跨模型迁移能力。
conclusion: 该研究证明了多模态大模型极易受到隐蔽视觉攻击的影响，强调了开发更具鲁棒性的防御机制的紧迫性。
---

## 摘要
尽管多模态大语言模型（MLLMs）在现实应用中的部署日益广泛，但其指令遵循行为使其容易受到提示注入攻击。现有的提示注入方法主要依赖于文本提示或人类用户可察觉的视觉提示。在这项工作中，我们研究了针对强大的闭源 MLLMs 的不可感知视觉提示注入，其中对抗性指令被嵌入在视觉模态中。我们的方法通过有界文本叠加将恶意提示自适应地嵌入到输入图像中，以提供语义引导。同时，通过迭代优化不可感知的视觉扰动，使受攻击图像的特征表示在粗粒度和细粒度水平上与恶意视觉及文本目标的特征表示对齐。具体而言，视觉目标被实例化为文本渲染图像，并在优化过程中逐步细化，以更忠实地表达目标语义并提高迁移性。在多个闭源 MLLMs 上针对两个多模态理解任务进行的广泛实验表明，与现有方法相比，我们的方法具有优越的性能。

## Abstract
Although multimodal large language models (MLLMs) are increasingly deployed in real-world applications, their instruction-following behavior leaves them vulnerable to prompt injection attacks. Existing prompt injection methods predominantly rely on textual prompts or perceptible visual prompts that are observable by human users. In this work, we study imperceptible visual prompt injection against powerful closed-source MLLMs, where adversarial instructions are embedded in the visual modality. Our method adaptively embeds the malicious prompt into the input image via a bounded text overlay to provide semantic guidance. Meanwhile, the imperceptible visual perturbation is iteratively optimized to align the feature representation of the attacked image with those of the malicious visual and textual targets at both coarse- and fine-grained levels. Specifically, the visual target is instantiated as a text-rendered image and progressively refined during optimization to more faithfully represent the desired semantics and improve transferability. Extensive experiments on two multimodal understanding tasks across multiple closed-source MLLMs demonstrate the superior performance of our approach compared to existing methods.

---

## 论文详细总结（自动生成）

以下是对论文《Adversarial Prompt Injection Attack on Multimodal Large Language Models》的结构化深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：多模态大语言模型（MLLM）在图像描述、视觉问答等任务中表现卓越，但其“指令遵循”特性使其面临提示注入（Prompt Injection）风险。
*   **核心问题**：现有的提示注入攻击通常依赖于人类肉眼可见的文本或视觉提示，容易被察觉或过滤；而传统的对抗性攻击虽然隐蔽，但在诱导模型生成特定、精确的恶意指令（如执行特定命令）方面能力有限。
*   **研究目标**：探索一种**不可见（Imperceptible）**的视觉提示注入攻击，既能保持对抗扰动的隐蔽性，又能实现对闭源模型输出内容的精确控制。

### 2. 方法论：CoTTA 框架
论文提出了一种名为 **CoTTA（Covert Triggered dual-Target Attack）** 的隐蔽触发双目标攻击方法，核心思想包括：
*   **隐蔽文本触发器（Covert Text Trigger）**：在图像中心嵌入一个有界的、可学习的文本叠加层。通过仿射变换（缩放 $s$ 和旋转 $\theta$）自适应调整文本形态，使其作为轻量级语义线索引导模型。
*   **双目标对齐（Dual-Target Alignment）**：
    *   **图像-文本对齐**：将受攻击图像的特征拉向目标恶意文本的嵌入向量。
    *   **图像-图像对齐**：将受攻击图像特征拉向一个“动态目标图像”。
*   **动态目标图像（Dynamic Target Image）**：为了解决跨模态对齐的鸿沟，算法初始化一个渲染了目标文本的图像，并在优化过程中迭代更新。更新策略包括“拉近”目标图像与目标文本的距离，同时“推开”目标图像与当前受攻击图像的距离，以增强迁移性。
*   **粗细粒度优化**：同时优化全局特征（Global features）和令牌级特征（Token-level features/Patch tokens），确保模型能捕捉到细微的语义指令。
*   **算法流程**：使用 I-FGSM 迭代更新对抗扰动 $\delta$ 和触发器参数，约束总扰动在 $\ell_\infty$ 范数限制内（如 $\epsilon=16$）。

### 3. 实验设计
*   **任务场景**：
    1.  **图像描述（Image Captioning）**：诱导模型生成特定指令（如“Search ICML website”）。
    2.  **视觉问答（VQA）**：诱导模型忽略问题并给出错误答案（如“None of the above”）。
*   **数据集**：NIPS 2017 对抗攻击竞赛数据集（100张/1000张）、ScienceQA 数据集（100个样本）。
*   **受试模型（Victim Models）**：均为闭源商业模型，包括 GPT-4o、GPT-5、Gemini-2.5 和 Claude-4.5。
*   **对比方法**：AttackVLM、AnyAttack、M-Attack、FOA-Attack、Agent-Attack。
*   **评估指标**：攻击成功率（ASR）和平均语义相似度（AvgSim），并引入 LLM-as-a-judge 进行评分。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和数量。
*   **训练细节**：提到了使用 CLIP-B/16、CLIP-B/32 和 LAION 作为替代（Surrogate）模型来提取特征并计算梯度。优化周期（Epochs）设置为 300 次迭代。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个顶级闭源模型上进行了跨任务测试。
    *   针对核心对比（FOA-Attack），将样本量扩大到了 1000 张图像以验证统计显著性。
    *   进行了详细的消融实验，分别验证了隐蔽触发器、图像-文本对齐、图像-图像对齐及目标图像更新机制的有效性。
*   **充分性评价**：实验设计较为充分，涵盖了当前主流的闭源模型，且对比了多种最新的对抗攻击基准，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **攻击有效性**：CoTTA 在 GPT-4o 和 Gemini-2.5 上表现极佳，ASR 普遍超过 70%-80%，显著优于现有方法。
*   **模型鲁棒性差异**：Claude-4.5 表现出最强的防御能力，所有攻击方法的成功率均较低，但 CoTTA 仍保持领先。
*   **关键组件**：消融实验证明“图像-图像特征对齐”对攻击成功贡献最大，其次是隐蔽文本触发器。
*   **隐蔽性**：生成的对抗样本在视觉上与原图几乎无异，恶意指令被成功隐藏在不可见的扰动中。

### 7. 优点
*   **创新性对齐策略**：通过动态更新目标图像来弥合模态鸿沟，解决了直接进行图像-文本对齐效果差的问题。
*   **隐蔽与精确并存**：成功将提示注入的“高可控性”与对抗攻击的“高隐蔽性”结合。
*   **强迁移性**：在完全黑盒的情况下，通过替代模型生成的扰动能有效迁移至多个顶级商业闭源模型。

### 8. 不足与局限
*   **防御模型挑战**：尽管在多数模型上成功，但在面对如 Claude-4.5 这样鲁棒性极高的模型时，攻击效率仍有待提升。
*   **计算开销**：300 次迭代的优化过程对于实时攻击场景可能较慢。
*   **应用限制**：攻击效果依赖于替代模型与目标模型在特征空间上的相似度，若目标模型采用完全不同的编码器，攻击效果可能会打折扣。
*   **防御对策缺失**：论文主要侧重于攻击实现，对于如何有效防御此类隐蔽注入攻击的探讨相对较少。

（完）
