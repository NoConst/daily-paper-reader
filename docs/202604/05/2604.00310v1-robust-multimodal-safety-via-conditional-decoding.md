---
title: Robust Multimodal Safety via Conditional Decoding
title_zh: 通过条件解码实现鲁棒的多模态安全性
authors: "Anurag Kumar, Raghuveer Peri, Jon Burnsky, Alexandru Nelus, Rohit Paturi, Srikanth Vishnubhotla, Yanjun Qi"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00310v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大语言模型（MLLM）的安全对齐
tldr: "多模态大语言模型在处理跨模态有害查询时常面临安全对齐失效的问题。本文提出CASA（基于安全注意力增强的分类）解码策略，通过模型内部表示在生成响应前预测安全标识位。该方法引入了创新的安全注意力模块，无需外部分类器或特定模态微调，在多个基准测试中将攻击成功率降低了97%以上，同时通过自动化和人工评估证实其在良性输入下仍保持极高的实用性。"
selection_source: fresh_fetch
motivation: 针对多模态大模型在跨模态交互中容易受到有害查询攻击且现有安全对齐鲁棒性不足的问题。
method: 提出CASA框架，通过安全注意力模块利用模型内部表示在响应生成前进行安全预测和条件解码。
result: "在多个主流基准测试中将攻击成功率降低了97%以上，并有效兼顾了模型在正常任务中的实用性。"
conclusion: CASA证明了无需外部辅助工具或特定微调即可通过条件解码显著增强多模态大模型的安全性。
---

## 摘要
多模态大语言模型 (MLLM) 在有害查询利用跨模态交互时，往往会出现安全对齐性能下降的情况。仅在文本上进行对齐的模型在扩展到两种或更多模态时，表现出更高的攻击成功率。在这项工作中，我们提出了一种简单的条件解码策略 CASA（利用安全注意力增强的分类），该策略利用 MLLM 的内部表示在生成响应之前预测一个二元安全标记。我们引入了一种新颖的安全注意力模块，旨在增强模型检测恶意查询的能力。我们的设计确保了鲁棒的安全对齐，且无需依赖任何外部分类器或辅助头，也不需要进行特定模态的安全微调。在 MM-SafetyBench、JailbreakV-28k 和对抗性音频测试等多种基准测试中，CASA 在不同模态和攻击类型下的平均攻击成功率降低了 97% 以上。我们的实证评估还表明，CASA 在良性输入中保持了强大的实用性，这一结果通过自动化评估和人工评估（由 13 名经过培训的标注员完成）得到了验证。总之，这些结果凸显了 CASA 作为一个简单且具有泛化能力的框架，能够提升多模态大语言模型的安全性。

## Abstract
Multimodal large-language models (MLLMs) often experience degraded safety alignment when harmful queries exploit cross-modal interactions. Models aligned on text alone show a higher rate of successful attacks when extended to two or more modalities. In this work, we propose a simple conditional decoding strategy, CASA (Classification Augmented with Safety Attention) that utilizes internal representations of MLLMs to predict a binary safety token before response generation. We introduce a novel safety attention module designed to enhance the model's ability to detect malicious queries. Our design ensures robust safety alignment without relying on any external classifier or auxiliary head, and without the need for modality-specific safety fine-tuning. On diverse benchmarks such as MM-SafetyBench, JailbreakV-28k, and adversarial audio tests, CASA lowers the average attack success rate by more than 97% across modalities and across attack types. Our empirical evaluations also show that CASA maintains strong utility in benign inputs, a result validated through both automated and human evaluations (via 13 trained annotators). Together, these results highlight CASA as a simple and generalizable framework to improve multimodal LLM safety.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CASA**（Classification Augmented with Safety Attention，基于安全注意力增强的分类）的框架，旨在解决多模态大语言模型（MLLM）在面对跨模态攻击时安全对齐失效的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的 MLLM 虽然在纯文本上做了安全对齐，但在引入图像、音频等模态后，跨模态交互产生的复杂性使得模型容易受到“越狱攻击”（Jailbreak Attacks）。
*   **核心挑战**：传统的监督式安全微调（SSFT）往往会导致“过度阻断”（Overblocking），即为了安全而牺牲了模型在正常任务中的实用性（Utility）。此外，为每种新模态进行特定的安全微调成本极高。
*   **整体含义**：论文提出了一种“先分类后生成”的条件解码策略，通过模型内部表示预测安全标识位，从而在不牺牲实用性的前提下实现跨模态的鲁棒安全防御。

### 2. 方法论
*   **核心思想**：将安全判断与内容生成解耦。在生成任何实质性响应之前，强制模型先预测一个二元安全标记（`Safe` 或 `Unsafe`），后续的响应生成将基于该标记进行条件解码。
*   **关键技术细节**：
    *   **条件解码（Conditional Decoding）**：修改生成概率分布，使响应 $y$ 的生成不仅取决于输入 $x$，还取决于预先生成的安全标记 $C$。
    *   **安全注意力模块（Safety Attention Module）**：引入一个交叉注意力机制，将输入的隐藏状态与预定义的“安全嵌入”（从“UNSAFE”提示语中提取）进行对比。该模块会计算一个权重，用于缩放安全标记的逻辑值（Logits），从而放大恶意信号。
    *   **内部化设计**：CASA 完全利用模型自身的隐藏层表示，不需要外部分类器或额外的预测头。
*   **算法流程**：输入查询 -> 安全注意力模块处理隐藏状态 -> 预测安全标记 -> 根据标记生成正常响应或拒绝语。

### 3. 实验设计
*   **模型对象**：Qwen_2.5_Omni（3B 和 7B 版本）。
*   **数据集与场景**：
    *   **安全评估**：文本越狱（JB-Prompt）、Prefill 攻击（预填充攻击）、视觉越狱（JailbreakV-28k, MM-SafetyBench）、音频越狱（AIAH 拼写攻击）。
    *   **实用性评估**：Utility-text（通用指令）、MME Benchmark（感知与认知能力）。
*   **对比方法**：
    *   **Pretrained**：未对齐的原始模型。
    *   **SSFT**：标准的监督式安全微调。
    *   **Circuit Breaker (CB)**：一种通过修改内部表示来阻断有害生成的强力基准方法。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA A100 GPU**。
*   **训练细节**：采用 RS-LoRA（秩稳定低秩自适应）进行微调，秩 $r=8$。
*   **参数量**：3B 模型微调参数约 27M，7B 模型约 58M。
*   **时长**：文中未明确给出具体的训练总时长，但提到使用了 16k 条数据进行微调。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了文本、图像、音频三大模态的多种攻击类型，并进行了消融实验（验证安全注意力模块和安全提示语的作用）以及 Prefill 长度的敏感性分析。
*   **充分性与客观性**：实验设计较为全面，不仅使用了 Claude 3.7 作为自动评测官（LLMaJ），还聘请了 13 名专业语言学家进行人工验证，并计算了 Krippendorff’s $\alpha$ 等指标证明了评估的一致性，实验结果具有较高的可信度。

### 6. 主要结论与发现
*   **极高的防御成功率**：CASA 在各种模态下的平均攻击成功率（ASR）降低了 **97% 以上**。
*   **完美防御 Prefill 攻击**：在其他方法（如 SSFT 和 CB）随 Prefill 长度增加而失效时，CASA 保持了 0% 的 ASR。
*   **实用性不降反升**：在 MME 视觉基准测试中，CASA 的得分甚至超过了原始模型和 SSFT 模型，有效解决了安全与实用性的权衡问题。
*   **内部知识激活**：实验证明模型内部其实具备区分安全与否的能力，CASA 的作用是有效地激活并利用了这些内部表示。

### 7. 优点
*   **架构简洁**：无需外挂分类器，不改变模型原有推理逻辑，易于部署。
*   **跨模态通用性**：一套机制同时解决文本、图像、音频的安全问题，无需针对特定模态准备大量安全数据。
*   **防御鲁棒性**：对复杂的拼写攻击和长前缀攻击表现出极强的韧性。

### 8. 不足与局限
*   **计算开销**：安全注意力模块需要对整个 Prompt 进行交叉注意力计算，虽然只在生成第一个标记时执行一次，但对于超长文本输入可能会产生一定的计算瓶颈。
*   **防御范围**：目前主要针对显式的恶意查询，对于更隐晦的“情境安全”（Situational Safety，即看似无害但在特定语境下有害）的覆盖可能不足。
*   **静态防御风险**：随着攻击手段的进化（如针对安全标记预测过程的对抗性攻击），该机制可能需要持续更新。

（完）
