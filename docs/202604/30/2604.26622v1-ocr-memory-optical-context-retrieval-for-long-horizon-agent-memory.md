---
title: "OCR-Memory: Optical Context Retrieval for Long-Horizon Agent Memory"
title_zh: OCR-Memory：面向长时程智能体记忆的光学上下文检索
authors: "Jinze Li, Yang Zhang, Xin Yang, Jiayi Qu, Jinfeng Xu, Shuo Yang, Junhua Ding, Edith Cheuk-Han Ngai"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26622v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 用于长程智能体记忆的光学上下文检索
tldr: 针对自主LLM智能体在长程交互中面临的文本上下文限制问题，本文提出OCR-Memory框架。该方法将历史轨迹渲染为带有视觉标识符的高密度图像，通过“定位并转录”范式检索经验，避免了传统文本摘要的信息损失和幻觉。实验证明，该方法在严格上下文限制下显著提升了智能体的记忆容量和证据恢复的准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26622v1/fig-001.webp\", \"caption\": \"Table 1: Main Results on Long-Horizon Agent Tasks. We report fine-grained metrics for Mind2Web, including Element Accuracy, Action F1 Score, Step Success Rate, and Task Success Rate. For AppWorld, we report Success Rates across three difficulty levels and the overall Average. OCR-Memory consistently outperforms baselines, achieving significant gains in metrics requiring precise structural grounding.\", \"page\": 6, \"index\": 1, \"width\": 808, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2604.26622v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of the OCR-Memory. The system enables long-horizon agent memory by storing interaction histories as compressed multi-resolution images (left). To retrieve information, we employ a Locate-and-Transcribe paradigm: the model scans the visual history annotated with Set-of-Mark (SoM) visual anchors (center) to predict the index of relevant segments. Finally, the verbatim text corresponding to the selected index is deterministically fetched (right), avoiding generation-based hallucinations and minimizing token usage.\", \"page\": 3, \"index\": 2, \"width\": 824, \"height\": 464}]"
motivation: 现有的智能体记忆系统受限于文本上下文预算，存储原始轨迹成本高，而文本摘要则会导致信息丢失和碎片化。
method: 提出OCR-Memory框架，将历史轨迹转化为带视觉锚点的图像，并采用“定位并转录”模式检索精确的原始文本。
result: 在长程智能体基准测试中，OCR-Memory在严格的上下文限制下实现了性能的持续提升，并有效减少了幻觉。
conclusion: 通过将视觉模态作为高密度经验表示，OCR-Memory在保持证据忠实度的同时，极大地扩展了智能体的有效记忆容量。
---

## 摘要
自主大语言模型（LLM）智能体越来越多地在长时程、交互式环境中运行，其成功取决于对长期历史中积累的经验的重用。然而，现有的智能体记忆系统从根本上受到文本上下文预算的限制：存储或回顾原始轨迹的 Token 开销极高，而摘要和纯文本检索则以信息丢失和证据碎片化为代价来节省 Token。为了解决这一局限性，我们提出了光学上下文检索记忆（OCR-Memory），这是一种利用视觉模态作为智能体经验高密度表示的记忆框架，能够在检索时以极低的提示词开销保留任意长度的历史记录。具体而言，OCR-Memory 将历史轨迹渲染为带有唯一视觉标识符标注的图像。OCR-Memory 通过“定位并转录”（locate-and-transcribe）范式检索存储的经验，该范式通过视觉锚点选择相关区域并检索相应的逐字文本，从而避免自由生成并减少幻觉。在长时程智能体基准测试上的实验表明，在严格的上下文限制下，该方法取得了持续的性能提升，证明了光学编码在增加有效记忆容量的同时，能够保持忠实的证据恢复。

## Abstract
Autonomous LLM agents increasingly operate in long-horizon, interactive settings where success depends on reusing experience accumulated over extended histories. However, existing agent memory systems are fundamentally constrained by text-context budgets: storing or revisiting raw trajectories is prohibitively token-expensive, while summarization and text-only retrieval trade token savings for information loss and fragmented evidence. To address this limitation, we propose Optical Context Retrieval Memory (OCR-Memory), a memory framework that leverages the visual modality as a high-density representation of agent experience, enabling retention of arbitrarily long histories with minimal prompt overhead at retrieval time. Specifically, OCR-Memory renders historical trajectories into images annotated with unique visual identifiers. OCR-Memory retrieves stored experience via a \emph{locate-and-transcribe} paradigm that selects relevant regions through visual anchors and retrieves the corresponding verbatim text, avoiding free-form generation and reducing hallucination. Experiments on long-horizon agent benchmarks show consistent gains under strict context limits, demonstrating that optical encoding increases effective memory capacity while preserving faithful evidence recovery.

---

## 论文详细总结（自动生成）

### OCR-Memory：面向长时程智能体记忆的光学上下文检索

#### 1. 核心问题与整体含义（研究动机和背景）
自主 LLM 智能体在处理长时程任务（如网页自动化、移动应用操作）时，需要依赖历史经验来完成当前决策。然而，现有的智能体记忆系统面临**文本上下文预算（Text-context budget）**的严峻挑战：
*   **存储原始轨迹：** Token 开销极大，容易超出 LLM 的上下文窗口。
*   **摘要或文本检索：** 摘要会丢失关键的结构化、时间或程序细节；纯文本检索（RAG）在处理因果关系或长程依赖时容易出现证据碎片化。
*   **研究动机：** 论文受到视觉模态高密度信息编码能力的启发，提出将智能体经验存储为图像，利用光学编码在极低 Token 开销下保留完整、无损的历史信息。

#### 2. 方法论
OCR-Memory 框架将记忆的存储和检索从文本域转移到图像域，核心包含以下三个关键技术：

*   **光学经验存储（Optical Encoding）：**
    将智能体的交互轨迹（推理链、工具调用、环境反馈）渲染成图像。利用 DeepSeek-OCR 等模型将文档图像映射为紧凑的视觉 Token 序列，实现比原始文本高出约 10 倍的压缩率。
*   **“定位并转录”检索范式（Locate-and-Transcribe）：**
    *   **视觉锚点（SoM）：** 在渲染图像时，为每个文本段落添加红色边界框和唯一数字 ID（Set-of-Mark）。
    *   **索引选择：** 检索模型不再生成自由文本，而是针对查询（Query）输出相关段落的二进制相关性向量（0 或 1）。
    *   **确定性提取：** 根据选中的索引，直接从外部原始日志中提取逐字文本。这种设计解耦了“理解”与“生成”，彻底消除了检索阶段的幻觉。
*   **多分辨率与主动召回（Multi-Resolution & Active Recall）：**
    *   模拟人类记忆“由清晰到模糊”的特性：近期记忆存为高分辨率图像，远期记忆自动降采样为低分辨率缩略图以节省 Token。
    *   一旦检索模型识别出低分辨率图像中的相关片段，系统会触发“主动召回”，将其重新渲染为高分辨率，确保后续推理获得完整细节。

#### 3. 实验设计
*   **数据集/场景：**
    *   **Mind2Web：** 跨域网页导航任务，评估智能体在复杂 UI 环境下的操作能力。
    *   **AppWorld：** API 交互任务，重点测试“Hard”子集，评估智能体处理长历史回溯的能力。
*   **Benchmark 与对比方法：**
    *   **对比基准：** Zero-Shot（无记忆）、标准文本 RAG（基于向量相似度）。
    *   **先进方法：** MemoryBank（长期记忆增强）、AWM（智能体工作流记忆）、ACON（上下文压缩优化）。
*   **评估指标：** 元素准确率（Ele Acc）、动作 F1 分数、步骤成功率（Step SR）、任务成功率（Task SR）。

#### 4. 资源与算力
*   **模型架构：** 检索器基于 **DeepSeek-OCR (3B)**，推理智能体主要使用 **GPT-4**（也测试了 Qwen3-32B 以验证泛化性）。
*   **训练细节：** 
    *   使用 LoRA 对 DeepSeek-OCR 的语言解码器进行微调，冻结视觉编码器。
    *   在 HotpotQA 数据集上训练 3 个 Epoch。
    *   设置学习率为 1e-5，全局 Batch Size 为 128。
*   **算力说明：** 论文提到了具体的超参数设置（如 AdamW 优化器、余弦学习率调度），但**未明确说明具体的 GPU 型号、数量及总训练时长**。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了两个主流的长程智能体基准测试，并进行了多维度的消融实验。
*   **消融与鲁棒性测试：**
    *   验证了 SoM 机制对减少幻觉和降低延迟的作用。
    *   测试了不同 Token 限制（1k 到 8k）下的性能表现，证明了在极端资源受限下的优越性。
    *   通过 Needle-in-a-Haystack（大海捞针）测试验证了在 32k 超长上下文下的检索准确性。
*   **客观性：** 实验对比了多种不同范式的记忆系统，并更换了底层推理模型（GPT-4 vs Qwen3），结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能领先：** OCR-Memory 在 Mind2Web 和 AppWorld 上均刷新了 SOTA，尤其在复杂任务（Hard）中提升显著（如 AppWorld Hard 成功率从 21.4% 提升至 30.8%）。
*   **高压缩比：** 光学编码实现了超过 10 倍的上下文压缩，且在 32k 长度下仍保持 94.1% 的检索准确率。
*   **零幻觉检索：** 通过“定位并转录”机制，实现了 100% 的内容检索忠实度。
*   **资源权衡：** 该方法通过增加适度的磁盘存储和检索延迟，换取了极其宝贵的推理上下文 Token 节省（减少约 6.7 倍）。

#### 7. 优点
*   **创新的模态转换：** 首次提出将智能体轨迹以图像形式存储，解决了文本上下文溢出的根本矛盾。
*   **确定性检索：** SoM 锚点与索引选择的设计巧妙地规避了多模态模型常见的文本生成幻觉。
*   **动态记忆管理：** 多分辨率主动召回机制在存储效率与信息保真度之间取得了极佳的平衡。

#### 8. 不足与局限
*   **训练成本：** 与无需训练的 RAG 相比，该方法需要专门微调光学检索模型。
*   **计算开销：** 将日志渲染为图像的过程比存储纯文本更耗费计算资源。
*   **存储空间：** 图像记忆占用的磁盘空间远大于文本日志（实验显示每集从 18KB 增加到 1.47MB）。
*   **部署复杂性：** 需要在内存中同时维护视觉编码器和主语言模型，增加了系统的显存占用。

（完）
