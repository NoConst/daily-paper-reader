---
title: "GAM: Hierarchical Graph-based Agentic Memory for LLM Agents"
title_zh: GAM：面向大语言模型智能体的分层图结构智能体记忆
authors: "Zhaofen Wu, Hanrong Zhang, Fulin Lin, Wujiang Xu, Xinran Xu, Yankai Chen, Henry Peng Zou, Shaowen Chen, Weizhi Zhang, Xue Liu, Philip S. Yu, Hongwei Wang"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12285v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 基于分层图的智能体记忆机制
tldr: 针对LLM智能体在长期交互中难以平衡新旧信息的问题，本文提出GAM框架。该框架采用分层图结构，将内存编码与巩固解耦，通过事件推进图处理即时对话，并在语义切换时将其整合进主题关联网络。结合多因子检索策略，GAM在保持长期一致性的同时减少了噪声干扰，在LoCoMo和LongDialQA基准测试中表现优于现有SOTA方法。
selection_source: fresh_fetch
motivation: 现有的流式内存易受噪声干扰，而结构化内存难以适应动态演变的叙事，亟需一种能平衡快速感知与稳定保留的内存机制。
method: 提出一种分层图内存框架，通过事件推进图隔离当前对话，并在语义偏移时将其整合至主题关联网络，辅以多因子图引导检索。
result: 在LoCoMo和LongDialQA实验中，GAM在推理准确性和效率方面均显著超越了当前最先进的基准模型。
conclusion: GAM通过解耦内存处理流程，有效解决了长期交互中的知识冲突与干扰问题，为构建更稳健的智能体内存系统提供了新方案。
---

## 摘要
为了维持连贯的长期交互，大语言模型（LLM）智能体必须在获取新信息与保留先验知识之间寻求平衡。目前的统一流式记忆系统虽然便于上下文更新，但仍易受瞬时噪声的干扰。相反，离散的结构化记忆架构提供了稳健的知识保留能力，但往往难以适应不断演变的叙事。针对这一问题，我们提出了 GAM，一种基于分层图的智能体记忆框架。该框架通过显式地将记忆编码与巩固解耦，有效解决了快速上下文感知与稳定知识保留之间的冲突。通过将正在进行的对话隔离在事件推进图中，并仅在发生语义偏移时将其整合进主题关联网络，我们的方法在保持长期一致性的同时最大限度地减少了干扰。此外，我们引入了一种图引导的多因子检索策略，以增强上下文的精准度。在 LoCoMo 和 LongDialQA 上的实验表明，我们的方法在推理准确性和效率方面均持续优于现有的基准模型。

## Abstract
To sustain coherent long-term interactions, Large Language Model (LLM) agents must navigate the tension between acquiring new information and retaining prior knowledge. Current unified stream-based memory systems facilitate context updates but remain vulnerable to interference from transient noise. Conversely, discrete structured memory architectures provide robust knowledge retention but often struggle to adapt to evolving narratives. To address this, we propose GAM, a hierarchical Graph-based Agentic Memory framework that explicitly decouples memory encoding from consolidation to effectively resolve the conflict between rapid context perception and stable knowledge retention. By isolating ongoing dialogue in an event progression graph and integrating it into a topic associative network only upon semantic shifts, our approach minimizes interference while preserving long-term consistency. Additionally, we introduce a graph-guided, multi-factor retrieval strategy to enhance context precision. Experiments on LoCoMo and LongDialQA indicate that our method consistently outperforms state-of-the-art baselines in both reasoning accuracy and efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《GAM: Hierarchical Graph-based Agentic Memory for LLM Agents》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了LLM智能体在长期交互中面临的**记忆冲突**问题：智能体既需要快速捕捉实时对话（感知），又需要保护已有的知识不被瞬时噪声干扰（保留）。
*   **现有方案缺陷**：
    *   **统一流式记忆**（如MemGPT）：直接更新活跃流，容易导致“记忆丢失”和“语义漂移”（不同话题混淆）。
    *   **离散结构化记忆**（如GraphRAG）：结构过于僵硬，难以适应动态演变的对话叙事。
*   **研究动机**：受人类睡眠依赖的记忆巩固机制启发，提出将记忆的**编码（感知）**与**巩固（保留）**显式解耦，构建一种既灵活又稳健的分层图记忆系统。

### 2. 方法论：核心思想与关键技术
GAM框架通过**语义事件触发机制**来管理记忆生命周期，核心包含三个部分：
*   **分层图记忆架构 (Hierarchical Graph Memory)**：
    *   **局部层（事件推进图 $G_{event}$）**：存储原子交互单元，负责快速追加实时对话，实现写入隔离。
    *   **全局层（主题关联网络 $G_{topic}$）**：存储抽象语义主题及其深层关联，仅在语义切换时更新。
    *   **跨层连接**：通过索引将全局主题指向具体的历史事件快照。
*   **基于状态的记忆巩固 (State-Based Consolidation)**：
    *   系统在“情节缓冲状态”和“语义巩固状态”之间切换。
    *   **触发机制**：利用LLM作为判别器，检测语义分歧（$b_t$）。当检测到话题结束或缓冲区满时，将缓冲区的事件总结并整合进全局图。
*   **图引导的多因子检索 (Graph-Guided Multi-Factor Retrieval)**：
    *   采用“自顶向下”的遍历策略：先定位核心主题，再扩展相关主题，最后深入挖掘具体的事件细节。
    *   **多因子重排序**：在语义匹配基础上，引入**时间（Temporal）**、**置信度（Confidence）**和**角色（Role）**三个调节因子，提升检索精度。

### 3. 实验设计
*   **数据集**：
    *   **LoCoMo**：长文本开放域对话，包含多跳推理、时间推理等五类任务。
    *   **LongDialQA**：基于美剧剧本（如《生活大爆炸》、《老友记》）的多人对话，测试复杂场景下的上下文保留。
*   **基准方法 (Baselines)**：
    *   启发式方法：ReadAgent, MemoryBank。
    *   流式/OS启发方法：MemGPT, MemoryOS, Mem0（工业标准）。
    *   自反思方法：A-Mem。
*   **骨干模型**：Llama-3.2-3B, Qwen2.5 (7B/14B), GPT-4o-mini。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA RTX 4090 GPU** 上运行。
*   **训练/推理情况**：GAM 是一个**无需训练 (Training-free)** 的内存框架，直接应用于现有的指令微调模型。
*   **效率指标**：论文详细记录了推理延迟和 Token 消耗。结果显示 GAM 的 Token 消耗比 Mem0 降低了约 11%，且推理速度保持在亚秒级（约 0.8s）。

### 5. 实验数量与充分性
*   **实验规模**：在两个大型基准测试上进行了跨模型（3B到GPT-4o级别）的对比实验。
*   **消融实验**：分别验证了主题网络（TAN）、事件图（EPG）、状态切换机制（SSM）和多因子检索（MFR）的必要性。
*   **鲁棒性测试**：针对“话题分割噪声”做了专门分析，证明即使在 40% 的分割错误率下，GAM 依然稳健。
*   **参数敏感性**：对检索数量 $k$ 和三个调节因子（$\beta$）进行了详尽的网格搜索分析。
*   **评价**：实验设计全面，涵盖了准确性、效率、鲁棒性和超参数影响，对比基准包含了最新的 SOTA 方法，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：GAM 在所有测试的 LLM 骨干模型上均显著优于基准方法，尤其在时间推理和多人对话场景下提升巨大（如在 Qwen2.5-7B 上 Temporal F1 提升超过 18%）。
*   **结构优势**：解耦感知与巩固能有效防止“记忆污染”。
*   **效率提升**：通过分层检索和粗到精的候选筛选，GAM 在处理极长对话时比传统流式记忆更节省 Token。

### 7. 优点与亮点
*   **架构创新**：引入了类似人类记忆巩固的“双状态”切换机制，从结构上解决了感知与保留的冲突。
*   **多因子融合**：检索时不只看语义相似度，还考虑了角色和时间，这对于解决多人对话中的叙事交织非常有效。
*   **即插即用**：作为推理侧框架，无需对 LLM 进行昂贵的微调。

### 8. 不足与局限
*   **模态限制**：目前仅支持文本模态，无法处理视觉或音频等非语言信号（尽管论文在附录中做了代理多模态实验）。
*   **依赖 LLM 判别**：话题边界的检测依赖于 LLM 的判断，虽然是稀疏触发，但在极端高频交互下仍可能产生额外的推理开销。
*   **总结损耗**：在巩固阶段的语义总结过程理论上仍存在丢失极细微信息的风险（尽管通过保留原始文本 $c_{raw}$ 进行了缓解）。

（完）
