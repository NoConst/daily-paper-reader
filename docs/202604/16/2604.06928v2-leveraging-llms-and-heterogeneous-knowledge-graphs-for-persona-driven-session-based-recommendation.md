---
title: Leveraging LLMs and Heterogeneous Knowledge Graphs for Persona-Driven Session-Based Recommendation
title_zh: 利用大语言模型与异构知识图谱实现画像驱动的基于会话的推荐
authors: "Muskan Gupta, Suraj Thapa, Jyotsana Khatri"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06928v2"
tags: ["query:cir-ml"]
score: 9.0
evidence: 画像驱动的推荐以及使用大模型建模用户画像
tldr: 针对会话式推荐系统在匿名环境下缺乏个性化的问题，本文提出一种结合大语言模型（LLM）和异构知识图谱（KG）的画像驱动框架。该框架通过构建包含外部元数据的异构图，利用LLM生成的项嵌入和无监督图学习提取潜在用户画像，并将其融入数据驱动的推荐模型中。实验证明，该方法在处理冷启动和稀疏数据时显著提升了推荐性能。
selection_source: fresh_fetch
motivation: 传统的会话式推荐因会话匿名性难以捕捉长期个性化偏好，且LLM在缺乏显式用户画像时难以进行精准建模。
method: 采用两阶段架构，先利用LLM和异构图学习提取潜在用户画像，再将画像与项嵌入集成到推荐模型中进行候选生成与重排序。
result: "在Amazon Books和Movies & TV数据集上的实验表明，该方法在各项指标上均优于仅依赖会话历史的序列模型。"
conclusion: 结合结构化知识图谱的关联信号与LLM的语义能力，能有效建模匿名会话中的用户画像，从而增强推荐系统的个性化能力。
---

## 摘要
基于会话的推荐系统 (SBRS) 旨在从交互序列中捕捉用户的短期意图。然而，匿名会话的常见假设限制了其个性化能力，特别是在数据稀疏或冷启动的情况下。大语言模型 (LLM) 增强推荐的最新进展表明，LLM 可以生成丰富的物品表示，但由于会话的匿名性，利用 LLM 建模用户画像仍然具有挑战性。在这项工作中，我们提出了一个画像驱动的 SBRS 框架，该框架显式建模从异构知识图谱 (KG) 中推断出的潜在用户画像，并将其集成到数据驱动的 SBRS 中。我们的框架采用两阶段架构，包括个性化信息提取和个性化信息利用。在个性化信息提取阶段，我们构建了一个异构知识图谱，整合了与时间无关的用户-物品交互、物品-物品关系、物品-特征关联以及来自 DBpedia 的外部元数据。随后，我们在使用 LLM 衍生的物品嵌入初始化的知识图谱上，通过异构深度图互信息 (HDGI) 目标以无监督方式学习潜在用户画像。在个性化信息利用阶段，学习到的画像表示与 LLM 衍生的物品嵌入被共同整合到改进的数据驱动 SBRS 架构中，以生成相关物品的候选集，随后利用基础序列模型进行重排序，以强调短期会话意图。与以往仅依赖序列建模或基于文本的用户表示的方法不同，我们的方法将用户画像建模建立在源自异构知识图谱的结构化关系信号之上。在 Amazon Books 和 Amazon Movies & TV 数据集上的实验表明，我们的方法始终优于使用会话历史衍生用户嵌入的序列模型。

## Abstract
Session-based recommendation systems (SBRS) aim to capture user's short-term intent from interaction sequences. However, the common assumption of anonymous sessions limits personalization, particularly under sparse or cold-start conditions. Recent advances in LLM augmented recommendation have shown that LLMs can generate rich item representations, but modeling user personas with LLMs remains challenging due to anonymous sessions. In this work, we propose a persona driven SBRS framework that explicitly models latent user personas inferred from a heterogeneous knowledge graph (KG) and integrates them into a data-driven SBRS. Our framework adopts a two-stage architecture consisting of personalized information extraction and personalized information utilization. In the personalized information extraction stage, we construct a heterogeneous KG that integrates time-independent user-item interactions, item-item relations, item-feature associations, and external metadata from DBpedia. We then learn latent user personas in an unsupervised manner using a Heterogeneous Deep Graph Infomax (HDGI) objective over a KG initialized with LLM-derived item embeddings. In the personalized information utilization stage, the learned persona representations together with LLM-derived item embeddings are incorporated into a modified architecture of data-driven SBRS to generate a candidate set of relevant items, followed by reranking using the base sequential model to emphasize short-term session intent. Unlike prior approaches that rely solely on sequence modeling or text-based user representations, our method grounds user persona modeling in structured relational signals derived from a heterogeneous KG. Experiments on Amazon Books and Amazon Movies & TV demonstrate that our approach consistently improves over sequential models with user embeddings derived using session history.

---

## 论文详细总结（自动生成）

这是一份关于论文《Leveraging LLMs and Heterogeneous Knowledge Graphs for Persona-Driven Session-Based Recommendation》的结构化分析报告：

### 1. 核心问题与整体含义
*   **研究动机**：传统的会话式推荐系统（SBRS）主要关注用户在短时间内的点击序列，通常假设会话是匿名的。这种做法忽略了用户跨会话的稳定偏好（即“用户画像”，Persona），导致在数据稀疏或冷启动场景下推荐效果不佳。
*   **核心挑战**：虽然大语言模型（LLM）在理解物品语义方面表现出色，但直接利用 LLM 建模用户画像存在计算成本高、缺乏结构化约束以及在匿名环境下难以推断长期偏好的问题。
*   **研究目标**：通过结合**异构知识图谱（HKG）**的结构化关联信号与 **LLM** 的丰富语义表示，显式地建模潜在用户画像，以增强匿名会话中的个性化推荐能力。

### 2. 方法论
该框架采用两阶段架构，灵感来源于思维链（CoT）推荐方法：

*   **阶段一：个性化信息提取（Personalized Information Extraction）**
    1.  **异构图构建**：整合了用户-物品交互、物品-物品关系（如“常一起购买”）、物品-特征关联，并引入了来自 **DBpedia** 的外部元数据（如导演、流派、奖项等）。
    2.  **LLM 初始化**：利用 LLM（如 Qwen-3-8B）对物品标题、描述及属性进行编码，生成初始语义嵌入。用户节点则采用随机初始化。
    3.  **无监督画像学习**：采用**异构深度图互信息（HDGI）**目标。通过最大化局部节点表示与全局图摘要之间的互信息，在不依赖特定任务标签的情况下，学习捕捉了多跳关系和语义信息的潜在用户画像嵌入。

*   **阶段二：个性化信息利用（Personalized Information Utilization）**
    1.  **候选生成（检索）**：将学习到的用户画像嵌入与 LLM 衍生的物品嵌入注入到数据驱动的推荐模型中，生成初步的候选物品集。
    2.  **重排序（Reranking）**：使用基础序列模型（如 SASRec）对候选集进行重排序，以重新强调当前会话的短期意图，平衡长期偏好与即时需求。

### 3. 实验设计
*   **数据集**：使用了 Amazon 平台的两个公开基准数据集：**Amazon Books** 和 **Amazon Movies & TV**。
*   **评估指标**：Hit Rate (HR@K), NDCG@K, Mean Reciprocal Rank (MRR)。
*   **对比策略（Benchmark）**：
    *   **None**：不使用用户画像。
    *   **Random**：用户画像随机初始化并在训练中学习。
    *   **LLM**：直接利用 LLM 根据历史交互的文本信息生成关键词形式的画像。
    *   **KG-LLM（本文方法）**：基于异构图和 HDGI 学习的画像。
*   **基础模型**：以 SASRec 作为序列建模的骨干网络。

### 4. 资源与算力
*   **模型使用**：文中明确提到使用了 **Qwen-3-8B** 进行文本生成和描述提取，使用 **Qwen3-Embedding-8B** 生成文本嵌入。
*   **算力细节**：**论文未明确说明**具体的硬件配置（如 GPU 型号、数量）以及具体的训练时长。仅提到在超参数搜索中对 Transformer 深度、隐藏层维度、学习率等进行了调优。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同领域（图书、影视）的数据集上进行了实验。
*   **消融与对比**：对比了四种不同的用户表示策略，并验证了重排序（Reranking）阶段的有效性。
*   **充分性评价**：实验设计较为合理，涵盖了从“无画像”到“纯 LLM 画像”再到“KG 驱动画像”的对比。然而，实验主要集中在 Amazon 生态系统内，且数据集规模（用户数约 1k-10k 级别）相对较小，对于超大规模工业级场景的泛化性验证略显不足。

### 6. 主要结论与发现
*   **画像的价值**：引入基于 KG 的用户画像显著提升了推荐性能，尤其是在 **HR@100** 指标上表现突出，说明该方法能有效增强候选集的召回质量。
*   **结构化知识的优势**：相比于纯 LLM 生成的文本画像，基于异构图学习的画像在处理稀疏数据时更具鲁棒性，因为它能通过图结构捕捉到长尾物品之间的关联。
*   **长短期结合**：通过“画像驱动检索 + 序列模型重排”的组合，系统能够同时兼顾用户的长期风格偏好和当前的即时购买意图。

### 7. 优点与亮点
*   **多模态融合**：巧妙地结合了 LLM 的语义理解能力和异构知识图谱的结构化推理能力。
*   **无监督学习**：利用 HDGI 进行画像提取，不需要昂贵的个性化标注数据，具有较好的通用性。
*   **冷启动友好**：通过引入 DBpedia 外部知识，缓解了匿名会话中因交互信息不足导致的冷启动问题。

### 8. 不足与局限
*   **计算复杂度**：虽然避免了在线调用 LLM 推断画像，但构建大规模异构图及进行 HDGI 预训练仍具有较高的离线计算开销。
*   **数据集局限**：实验仅限于 Amazon 评论数据集，缺乏在其他类型（如新闻、短视频）会话场景下的验证。
*   **实时性挑战**：论文未详细讨论当用户产生新交互时，异构图和画像嵌入的实时更新机制。
*   **硬件信息缺失**：缺乏算力消耗的量化数据，难以评估其在资源受限环境下的部署可行性。

（完）
