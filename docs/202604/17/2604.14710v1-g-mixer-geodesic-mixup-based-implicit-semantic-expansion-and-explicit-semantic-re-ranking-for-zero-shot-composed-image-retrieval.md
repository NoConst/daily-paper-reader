---
title: "G-MIXER: Geodesic Mixup-based Implicit Semantic Expansion and Explicit Semantic Re-ranking for Zero-Shot Composed Image Retrieval"
title_zh: G-MIXER：基于测地线混合的隐式语义扩展与显式语义重排序，用于零样本组合图像检索
authors: "Jiyoung Lim, Heejae Yang, Jee-Hyong Lee"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14710v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 基于多模态大模型的零样本组合图像检索
tldr: 针对零样本组合图像检索（ZS-CIR）中隐式语义捕捉不足和检索多样性低的问题，本文提出了一种无需训练的框架 G-MIXER。该方法通过测地线混合（Geodesic Mixup）在多种比例下扩展隐式语义特征以构建多样化候选集，并结合多模态大模型生成的显式语义进行重排序。实验证明，G-MIXER 在多个基准测试中达到了 SOTA 性能，有效平衡了组合查询中的隐式与显式语义。
selection_source: fresh_fetch
motivation: 现有方法过度依赖多模态大模型生成的文本描述，忽略了图文组合中的隐式语义及检索过程中的模糊性。
method: 提出 G-MIXER 框架，通过测地线混合技术扩展隐式语义特征并构建候选集，随后利用显式语义进行重排序。
result: 在多个 ZS-CIR 基准测试中取得了最先进的性能，有效提升了检索结果的准确性和多样性。
conclusion: G-MIXER 证明了结合隐式语义扩展与显式语义重排序是提升无需训练的组合图像检索性能的有效途径。
---

## 摘要
组合图像检索 (CIR) 旨在通过整合参考图像与相应的修改文本来检索目标图像。CIR 需要共同考虑查询中指定的显式语义以及嵌入在其双模态组合中的隐式语义。最近的免训练零样本 CIR (ZS-CIR) 方法利用多模态大语言模型 (MLLM) 生成详细的目标描述，将隐式信息转化为显式文本表达。然而，这些方法过度依赖文本模态，且未能捕捉到需要考虑候选结果多样化组合的模糊检索特性。这导致检索结果的多样性和准确性下降。为了解决这一局限性，我们提出了一种名为 G-MIXER 的新型免训练方法，即基于测地线混合的隐式语义扩展与显式语义重排序。G-MIXER 通过在一系列混合比例上进行测地线混合，构建反映参考图像-文本对隐式语义的组合查询特征，并建立一个多样化的候选集。随后，利用从 MLLM 导出的显式语义对生成的候选结果进行重排序，从而提高了检索的多样性和准确性。我们提出的 G-MIXER 在多个 ZS-CIR 基准测试中达到了最先进的性能，在无需额外训练的情况下有效处理了隐式和显式语义。我们的代码将在 https://github.com/maya0395/gmixer 上发布。

## Abstract
Composed Image Retrieval (CIR) aims to retrieve target images by integrating a reference image with a corresponding modification text. CIR requires jointly considering the explicit semantics specified in the query and the implicit semantics embedded within its bi-modal composition. Recent training-free Zero-Shot CIR (ZS-CIR) methods leverage Multimodal Large Language Models (MLLMs) to generate detailed target descriptions, converting the implicit information into explicit textual expressions. However, these methods rely heavily on the textual modality and fail to capture the fuzzy retrieval nature that requires considering diverse combinations of candidates. This leads to reduced diversity and accuracy in retrieval results. To address this limitation, we propose a novel training-free method, Geodesic Mixup-based Implicit semantic eXpansion and Explicit semantic Re-ranking for ZS-CIR (G-MIXER). G-MIXER constructs composed query features that reflect the implicit semantics of reference image-text pairs through geodesic mixup over a range of mixup ratios, and builds a diverse candidate set. The generated candidates are then re-ranked using explicit semantics derived from MLLMs, improving both retrieval diversity and accuracy. Our proposed G-MIXER achieves state-of-the-art performance across multiple ZS-CIR benchmarks, effectively handling both implicit and explicit semantics without additional training. Our code will be available at https://github.com/maya0395/gmixer.

---

## 论文详细总结（自动生成）

以下是对论文《G-MIXER: Geodesic Mixup-based Implicit Semantic Expansion and Explicit Semantic Re-ranking for Zero-Shot Composed Image Retrieval》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：零样本组合图像检索（ZS-CIR）要求根据“参考图像”和“修改文本”检索目标图像。现有的免训练方法（如利用 MLLM 生成目标描述）过度依赖文本模态，将图像中的隐式语义（如未被文本提及的背景、物体细节）强行转化为显式文本，导致检索范围过窄、多样性不足，且难以处理图文组合中的模糊性。
*   **研究动机**：作者认为有效的 ZS-CIR 需要进行“模糊检索”，既要通过隐式语义扩展检索范围以包含多样化的候选结果，又要利用显式语义过滤掉不符合修改要求的噪声。

### 2. 方法论
G-MIXER 是一个免训练框架，包含两个核心模块：
*   **基于测地线混合的隐式语义扩展（G-MIX）**：
    *   **核心思想**：在 CLIP 的超球面嵌入空间中，沿着图像特征和文本特征之间的最短路径（测地线）进行插值，而非简单的线性混合，以保持几何结构。
    *   **技术细节**：通过设置一系列混合比例 $\lambda$（如 0.7 到 1.0），生成多个组合查询特征。较小的 $\lambda$ 保留更多图像结构，较大的 $\lambda$ 突出文本修改。
    *   **流程**：对每个比例生成的查询进行检索，取 Top-K 结果并聚合，构建一个具有语义多样性的初始候选集。
*   **显式语义重排序（ER）**：
    *   **核心思想**：利用 MLLM 提取修改文本中明确要求的属性（Include）和需排除的属性（Exclude）。
    *   **技术细节**：计算候选图像与“包含属性”和“排除属性”文本的相似度差异（$\Delta$）。如果图像更接近“包含属性”且远离“排除属性”，则获得奖励分；反之则受罚。
    *   **最终评分**：结合原始修改文本相似度、组合特征相似度以及显式语义差异分进行最终排名。

### 3. 实验设计
*   **数据集**：在四个主流 ZS-CIR 基准上进行评估：
    *   **CIRCO & CIRR**：真实场景图像检索。
    *   **FashionIQ**：细粒度服装检索（Dress, Shirt, Toptee）。
    *   **GeneCIS**：基于对象-属性推理的条件图像相似性检索。
*   **对比方法**：
    *   **基于训练的方法**：SEARLE, PrediCIR, LinCIR 等。
    *   **免训练方法**：CIReVL, LDRE, OSrCIR（当前的 SOTA）。
*   **Backbone**：使用了 CLIP 的 ViT-B/32, ViT-L/14 和 ViT-G/14 三种模型。

### 4. 资源与算力
*   **硬件**：所有计算在单张 **NVIDIA RTX 4090 GPU** 上完成。
*   **效率**：
    *   MLLM 步骤（使用 GPT-4o）每条查询耗时约 0.6 秒（占总耗时 97%）。
    *   推理步骤耗时 0.34 秒，与现有方法相当。
    *   由于图像和文本嵌入可预计算，多比例混合带来的额外计算开销极低（< 0.02s）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个数据集、3 种模型架构，并进行了详尽的消融实验。
*   **消融实验**：验证了 G-MIX 比例范围的影响、ER 模块中各项评分组件（奖励与惩罚项）的有效性，以及不同 MLLM（GPT-4o, GPT-4o-mini, GPT-4o-turbo）对结果的影响。
*   **客观性**：实验遵循了 ZS-CIR 领域的标准评估协议，对比了最新的 SOTA 方法，实验设计全面且公平。

### 6. 主要结论与发现
*   **性能领先**：G-MIXER 在所有基准测试中均取得了 SOTA 性能。例如在 CIRCO (ViT-L/14) 上，mAP@50 达到 32.39%，比之前的最佳方法 OSrCIR 提升了 3.42%。
*   **隐式语义的重要性**：通过 G-MIX 扩展检索范围能有效捕捉到 MLLM 文本推理容易忽略的视觉细节。
*   **重排序的必要性**：显式语义重排序能显著过滤掉因特征混合引入的噪声，确保检索结果在保持多样性的同时具备高精度。

### 7. 优点（亮点）
*   **免训练（Training-free）**：无需昂贵的标注三元组数据，具有极强的通用性和可扩展性。
*   **处理模糊性**：通过测地线混合模拟了用户查询的模糊意图，比单一文本描述更符合实际检索场景。
*   **即插即用**：可以灵活结合不同的 VLP 编码器和 MLLM。

### 8. 不足与局限
*   **MLLM 依赖性**：虽然消融实验显示受模型版本影响较小，但整体性能仍受限于 MLLM 提取显式属性的准确性。
*   **检索延迟**：尽管 GPU 计算很快，但依赖在线 LLM API（如 GPT-4o）可能会在实际大规模应用中产生延迟和成本问题。
*   **超参数敏感性**：虽然作者提出了范围搜索，但混合比例 $\lambda$ 的起始值在不同领域（如时尚 vs 真实场景）仍存在细微的最优差异。

（完）
