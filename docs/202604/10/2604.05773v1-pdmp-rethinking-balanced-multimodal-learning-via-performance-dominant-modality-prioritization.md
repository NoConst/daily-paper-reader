---
title: "PDMP: Rethinking Balanced Multimodal Learning via Performance-Dominant Modality Prioritization"
title_zh: PDMP：通过性能主导模态优先化重新思考平衡多模态学习
authors: "Shicai Wei, Chunbo Luo, Qiang Zhu, Yang Luo"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05773v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态机器学习的基础与进展
tldr: 针对多模态学习中模型表现往往不如单模态的欠优化问题，本文挑战了传统的“平衡学习”观念，提出性能领先模态优先（PDMP）策略。该方法通过识别单模态表现最优的模态，并利用非对称梯度调节系数使其主导优化过程，从而解决主导模态学习不足的问题。PDMP 具有结构无关性且易于部署，在多个数据集上显著提升了多模态学习性能，为多模态优化提供了新视角。
selection_source: fresh_fetch
motivation: 研究发现多模态欠优化并非源于模态不平衡，而是因为性能领先的模态在平衡策略下未能得到充分学习。
method: 提出PDMP策略，通过单模态性能排序确定主导模态，并引入非对称梯度调节系数使该模态主导模型优化。
result: 在多个基准数据集上的实验结果表明，PDMP在性能上优于现有的平衡学习方法，且具有良好的通用性。
conclusion: 研究证明让性能更强的模态主导学习过程比追求模态间的绝对平衡更能有效提升多模态模型的整体表现。
---

## 摘要
多模态学习因其广泛的实用性而受到越来越多的关注。然而，它经常面临优化不足的问题，即多模态模型的表现甚至不如其对应的单模态模型。现有方法将这一问题归因于模态之间的不平衡学习，并通过梯度调节（gradient modulation）来解决。本文认为，平衡学习并非多模态学习的最佳设置。相反，由具有优异单模态性能的性能主导模态驱动的不平衡学习，有助于获得更好的多模态性能。而优化不足的问题实际上是由性能主导模态的学习不充分导致的。为此，我们提出了性能主导模态优先化（PDMP）策略来辅助多模态学习。具体而言，PDMP 首先通过独立训练的单模态模型的性能排名来挖掘性能主导模态。然后，PDMP 引入非对称系数来调节各模态的梯度，使性能主导模态在优化中占据主导地位。由于 PDMP 仅依赖于单模态性能排名，它独立于多模态模型的结构和融合方法，在实际应用场景中具有巨大潜力。最后，在各种数据集上的广泛实验验证了 PDMP 的优越性。

## Abstract
Multimodal learning has attracted increasing attention due to its practicality. However, it often suffers from insufficient optimization, where the multimodal model underperforms even compared to its unimodal counterparts. Existing methods attribute this problem to the imbalanced learning between modalities and solve it by gradient modulation. This paper argues that balanced learning is not the optimal setting for multimodal learning. On the contrary, imbalanced learning driven by the performance-dominant modality that has superior unimodal performance can contribute to better multimodal performance. And the under-optimization problem is caused by insufficient learning of the performance-dominant modality. To this end, we propose the Performance-Dominant Modality Prioritization (PDMP) strategy to assist multimodal learning. Specifically, PDMP firstly mines the performance-dominant modality via the performance ranking of the independently trained unimodal model. Then PDMP introduces asymmetric coefficients to modulate the gradients of each modality, enabling the performance-dominant modality to dominate the optimization. Since PDMP only relies on the unimodal performance ranking, it is independent of the structures and fusion methods of the multimodal model and has great potential for practical scenarios. Finally, extensive experiments on various datasets validate the superiority of PDMP.

---

## 论文详细总结（自动生成）

这是一份关于论文《PDMP: Rethinking Balanced Multimodal Learning via Performance-Dominant Modality Prioritization》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态学习中普遍存在“优化不足”现象，即多模态模型的表现有时甚至不如单模态模型。
*   **研究背景**：现有研究通常将其归因于“模态不平衡”，认为某些模态在训练中占据主导并抑制了其他模态，因此主流方案是采用“梯度调节”来追求各模态间的**平衡学习**。
*   **核心挑战**：本文挑战了“平衡学习即最优”的假设。作者发现，在某些数据集（如AVE）上，强制平衡反而会降低性能。
*   **研究动机**：基于信息论证明，多模态模型应当更多地关注那些“性能主导”（单模态表现更好）的模态，而非盲目追求平衡。

### 2. 方法论
论文提出了**性能主导模态优先化（PDMP）**策略，包含两个关键阶段：
*   **模态分析（Modality Analysis）**：
    *   通过独立训练单模态模型并进行性能排序，识别出**性能主导模态**（Performance-Dominant, $m_p$）。
    *   识别**优化主导模态**（Optimization-Dominant, $m_o$），即在训练初期收敛最快、贡献梯度最大的模态。
*   **梯度调节（Gradient Modulation）**：
    *   引入非对称系数 $\gamma_p$ 和 $\gamma_r$。
    *   对于性能主导模态 $m_p$，放大其梯度（$g_p = \gamma_p \cdot g'_p$，其中 $\gamma_p > 1$），使其主导优化过程。
    *   对于剩余模态 $m_r$，根据其是否为优化主导模态来调整系数 $\gamma_r$：如果 $m_r$ 是优化主导但非性能主导，则抑制其梯度（$1/\gamma_p$）；否则也适当加速。
*   **理论支撑**：从信息论角度证明，为了最大化模型输出与标签之间的互信息，各模态的贡献权重应与其判别能力（判别强度）成正比。

### 3. 实验设计
*   **数据集**：涵盖了多种任务和规模，包括 **CREMA-D**（情感识别）、**AVE**（视听事件定位）、**Kinetics-Sounds**（动作识别）、**CEFA**（人脸防欺骗，含3个模态）、**UCF-101**（动作识别）以及大规模数据集 **VGGSound**。
*   **Benchmark 与对比方法**：
    *   **基础融合**：Concatenation, Summation, Film, Gated。
    *   **梯度调节类**：OGM-GE, AGM, PMR, Grad-Blending。
    *   **正则化/协作优化类**：MLA, MMPareto, D&R。
*   **网络架构**：主要使用 ResNet-18 作为骨干网络，并验证了在 SE-Fusion 和 mmFormer（Transformer 架构）上的通用性。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练参数**：提到了使用 SGD 优化器，动量 0.9，学习率 1e-3，权重衰减 1e-4，Batch Size 为 64，训练 100 个 Epoch。
*   **额外开销**：PDMP 需要预先训练单模态模型，但作者提出可以通过在数据集的子集（如 40% 数据）上进行分析来降低算力成本。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同类型的多模态数据集上进行了测试，实验覆盖面广。
*   **消融实验**：
    *   验证了不同融合方法（4种）的提升效果。
    *   验证了不同架构（CNN 与 Transformer）的兼容性。
    *   分析了超参数 $\gamma_p$ 的敏感性。
    *   探讨了数据采样比例对模态分析准确性的影响。
*   **充分性评价**：实验设计非常充分，不仅对比了最新的 SOTA 方法，还通过可视化“优化依赖系数 $w$”深入解释了方法生效的机制，逻辑闭环完整。

### 6. 主要结论与发现
*   **平衡并非最优**：强制使各模态梯度平衡并不总是能带来最佳性能，有时甚至有害。
*   **性能主导至上**：让单模态性能更强的模态在多模态训练中占据主导地位，能显著提升最终的融合效果。
*   **解决欠优化**：多模态性能差的根本原因在于“性能主导模态”在联合训练中未得到充分学习。
*   **通用性**：PDMP 作为一个“即插即用”策略，在不同任务、不同模态数量（支持2个以上）和不同架构下均表现稳健。

### 7. 优点
*   **视角独特**：挑战了多模态学习领域长期存在的“追求平衡”的直觉，提出了更符合信息论的优化方向。
*   **简单高效**：不需要复杂的辅助网络或复杂的损失函数，仅通过梯度缩放即可实现，易于部署。
*   **扩展性强**：原生支持多于两个模态的场景（如 RGB+深度+红外），而许多现有梯度调节方法仅限于双模态。
*   **理论与实践结合**：既有严谨的信息论推导，又有详实的实验数据支撑。

### 8. 不足与局限
*   **预训练依赖**：虽然可以使用子集，但仍需额外的单模态训练步骤来确定模态属性，增加了训练流水线的复杂度。
*   **超参数敏感性**：系数 $\gamma_p$ 的选择对结果有一定影响，不同数据集的最优值可能不同（实验中在 10 到 15 之间波动）。
*   **动态性不足**：目前的模态分析是静态的（训练前确定），如果模态的主导地位在训练过程中发生动态变化，该方法可能无法实时捕捉。

（完）
