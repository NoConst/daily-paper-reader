---
title: "G-MIXER: Geodesic Mixup-based Implicit Semantic Expansion and Explicit Semantic Re-ranking for Zero-Shot Composed Image Retrieval"
title_zh: G-MIXER：基于测地线 Mixup 的隐式语义扩展与显式语义重排序，用于零样本组合图像检索
authors: "Jiyoung Lim, Heejae Yang, Jee-Hyong Lee"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14710v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 使用多模态大模型进行零样本组合图像检索
tldr: 本研究针对零样本组合图像检索（ZS-CIR）中现有方法过度依赖文本描述而忽视隐式语义的问题，提出了无需训练的G-MIXER框架。该方法通过测地线混合（Geodesic Mixup）扩展隐式语义以构建多样化候选集，并结合多模态大模型的显式语义进行重排序。实验证明，G-MIXER在多个基准测试中达到了SOTA性能，有效提升了检索的准确性与多样性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14710v1/fig-001.webp\", \"caption\": \"Table 2. Comparison on FashionIQ Validation set. G-MIXER is able to significantly outperform existing training-free ZS-CIR methods across all sub-benchmarks. Gray rows indicate training-free ZS-CIR methods. Bold and underlined values denote the best and second-best results, respectively.\", \"page\": 6, \"index\": 1, \"width\": 729, \"height\": 357}, {\"url\": \"assets/figures/arxiv/2604.14710v1/fig-002.webp\", \"caption\": \"Figure 1. Illustration of our motivation. The query contains implicit information cat, basket present in the image but not mentioned in the text, while the modification text includes explicit attributes such as bench and vegetables. The MLLM based caption generation approach narrows the retrieval scope by converting implicit information from reference image into explicit descriptions. In contrast, the composed query that incorporates information expands the retrieval scope and produces more diverse combinations of results.\", \"page\": 2, \"index\": 2, \"width\": 1036, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2604.14710v1/fig-003.webp\", \"caption\": \"Figure 4. Qualitative results on Fashion-IQ dress.\", \"page\": 7, \"index\": 3, \"width\": 496, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2604.14710v1/fig-004.webp\", \"caption\": \"Figure 3. Qualitative results on CIRCO.\", \"page\": 7, \"index\": 4, \"width\": 496, \"height\": 245}, {\"url\": \"assets/figures/arxiv/2604.14710v1/fig-005.webp\", \"caption\": \"Figure 2. Overview of G-MIXER. (1) Generate the Target Image Description and Re-ranking Attributes using MLLM; (2) Perform extended Geodesic Mixup on the features obtained by encoding the Target Image Description and Reference Image with the pretrained CLIP encoder; (3) Filter noisy samples through Explicit Semantic Re-ranking.\", \"page\": 3, \"index\": 5, \"width\": 1036, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2604.14710v1/fig-006.webp\", \"caption\": \"Figure 5. Comparison between the proposed range-based geodesic mixup (orange) and the fixed-ratio mixup (gray). The range-based approach expands the retrieval space by applying a range of mixup ratios, while the fixed-ratio method uses a single static value.\", \"page\": 8, \"index\": 6, \"width\": 482, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2604.14710v1/fig-007.webp\", \"caption\": \"Table 1. Comparison on CIRCO and CIRR Test Data. G-MIXER outperforms existing ZS-CIR methods on both datasets CIRCO and CIRR. Gray rows indicate training-free ZS-CIR methods. Bold and underlined values denote the best and second-best results, respectively.\", \"page\": 5, \"index\": 7, \"width\": 850, \"height\": 407}]"
motivation: 现有的零样本组合图像检索方法过度依赖文本模态，难以捕捉复杂的隐式语义，导致检索结果的多样性和准确性不足。
method: 提出G-MIXER方法，利用测地线混合技术扩展隐式语义特征并构建候选集，随后利用多模态大模型生成的显式语义进行重排序。
result: G-MIXER在多个ZS-CIR基准测试中均取得了当前最先进的性能表现，证明了其在处理隐式和显式语义方面的有效性。
conclusion: 该研究通过结合隐式语义扩展与显式语义重排序，为无需训练的零样本组合图像检索提供了一种高效且准确的新方案。
---

## 摘要
组合图像检索（CIR）旨在通过整合参考图像与相应的修改文本来检索目标图像。CIR 需要共同考虑查询中指定的显式语义以及其双模态组合中嵌入的隐式语义。最近的免训练零样本 CIR（ZS-CIR）方法利用多模态大语言模型（MLLMs）生成详细的目标描述，将隐式信息转换为显式文本表达。然而，这些方法过度依赖文本模态，且未能捕捉到需要考虑候选对象多样化组合的模糊检索本质。这导致检索结果的多样性和准确性降低。为了解决这一局限性，我们提出了一种名为 G-MIXER 的新型免训练方法，即基于测地线 Mixup 的隐式语义扩展与显式语义重排序。G-MIXER 通过在一系列 Mixup 比例上进行测地线 Mixup，构建反映参考图像-文本对隐式语义的组合查询特征，并建立一个多样化的候选集。随后，利用从 MLLMs 导出的显式语义对生成的候选对象进行重排序，从而提高检索的多样性和准确性。我们提出的 G-MIXER 在多个 ZS-CIR 基准测试中实现了最先进的性能，在无需额外训练的情况下有效处理了隐式和显式语义。我们的代码将在 https://github.com/maya0395/gmixer 发布。

## Abstract
Composed Image Retrieval (CIR) aims to retrieve target images by integrating a reference image with a corresponding modification text. CIR requires jointly considering the explicit semantics specified in the query and the implicit semantics embedded within its bi-modal composition. Recent training-free Zero-Shot CIR (ZS-CIR) methods leverage Multimodal Large Language Models (MLLMs) to generate detailed target descriptions, converting the implicit information into explicit textual expressions. However, these methods rely heavily on the textual modality and fail to capture the fuzzy retrieval nature that requires considering diverse combinations of candidates. This leads to reduced diversity and accuracy in retrieval results. To address this limitation, we propose a novel training-free method, Geodesic Mixup-based Implicit semantic eXpansion and Explicit semantic Re-ranking for ZS-CIR (G-MIXER). G-MIXER constructs composed query features that reflect the implicit semantics of reference image-text pairs through geodesic mixup over a range of mixup ratios, and builds a diverse candidate set. The generated candidates are then re-ranked using explicit semantics derived from MLLMs, improving both retrieval diversity and accuracy. Our proposed G-MIXER achieves state-of-the-art performance across multiple ZS-CIR benchmarks, effectively handling both implicit and explicit semantics without additional training. Our code will be available at https://github.com/maya0395/gmixer.

---

## 论文详细总结（自动生成）

以下是对论文《G-MIXER: Geodesic Mixup-based Implicit Semantic Expansion and Explicit Semantic Re-ranking for Zero-Shot Composed Image Retrieval》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：零样本组合图像检索（ZS-CIR）要求根据“参考图像”和“修改文本”检索目标。现有方法（如基于 MLLM 的方法）倾向于将所有信息转化为显式文本描述，这会导致：
    1.  **忽视隐式语义**：图像中未被文本提及但重要的视觉细节（如背景、未说明的物体）在转化过程中可能丢失。
    2.  **检索范围过窄**：过度精确的文本描述限制了检索的多样性，无法处理用户意图中的模糊性。
*   **研究动机**：提出一种“模糊检索”机制，既能通过隐式语义扩展搜索范围，又能利用显式语义过滤噪声，实现准确度与多样性的平衡。

### 2. 方法论
G-MIXER 是一个**无需训练（Training-free）**的框架，包含两个核心模块：
*   **G-MIX（基于测地线混合的隐式语义扩展）**：
    *   **核心思想**：在 CLIP 的超球面特征空间中，沿着图像特征和文本特征之间的最短路径（测地线）进行插值。
    *   **技术细节**：不同于固定比例的混合，G-MIX 在一个范围（如 $\lambda \in [0.7, 1.0]$）内取多个混合比例，生成多个组合查询特征。
    *   **作用**：通过改变混合比例，捕捉不同程度的图像/文本侧重，从而构建一个包含多样化隐式语义组合的候选集。
*   **ER（显式语义重排序）**：
    *   **核心思想**：利用 MLLM（如 GPT-4o）提取修改文本中明确要求的“包含（Include）”和“排除（Exclude）”属性。
    *   **技术细节**：计算候选图像与这些显式属性的相似度差异（$\Delta$）。如果图像包含应有的属性且排除了不应有的属性，则获得奖励分；反之则受罚。
    *   **流程**：最终得分 = 文本相似度 + 组合特征相似度 + 显式语义修正分。

### 3. 实验设计
*   **数据集**：
    *   **CIRCO & CIRR**：真实场景图像检索，包含复杂的语义组合。
    *   **FashionIQ**：细粒度服装检索（Dress, Shirt, Toptee）。
    *   **GeneCIS**：侧重于对象-属性推理的条件图像相似性检索。
*   **对比方法**：
    *   **有训练方法**：SEARLE, PrediCIR, LinCIR 等。
    *   **免训练方法（SOTA）**：CIReVL, LDRE, OSrCIR 等。
*   **Backbone**：采用了 CLIP 的 ViT-B/32, ViT-L/14 和 ViT-G/14 多种规模的视觉骨干网络。

### 4. 资源与算力
*   **硬件**：实验在单张 **NVIDIA RTX 4090 GPU** 上完成。
*   **效率**：
    *   MLLM 处理阶段（生成描述和属性）：约 0.6 秒/查询（占总耗时 97%）。
    *   推理检索阶段：约 0.34 秒/查询。
    *   由于特征可预计算，多比例 Mixup 的计算开销极低（< 0.02s），适合大规模并行。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流基准数据集上进行了全面测试，涵盖了从通用场景到细粒度时尚领域的多种任务。
*   **消融实验**：
    *   验证了 G-MIX 范围搜索 vs. 固定比例的效果（证明了范围搜索的鲁棒性）。
    *   验证了重排序中各得分组件（$S_m, S_\lambda, \Delta$）的贡献。
    *   测试了不同 MLLM（GPT-4o, mini, turbo）对结果的影响。
*   **评价**：实验设计客观、公平，通过跨模型（不同 Backbone）和跨数据集的对比，充分证明了方法的泛化能力。

### 6. 主要结论与发现
*   **性能领先**：G-MIXER 在所有基准测试中均达到了 SOTA 性能。例如在 CIRCO (ViT-L/14) 上，mAP@50 达到 32.39%，超过之前最好的 OSrCIR 约 3.42%。
*   **隐式语义的重要性**：通过测地线混合保留图像的隐式特征，能有效弥补纯文本推理在处理模糊查询时的不足。
*   **重排序的必要性**：显式语义过滤能显著减少因扩展搜索范围而引入的噪声样本。

### 7. 优点
*   **无需训练**：直接利用预训练的 CLIP 和 MLLM，部署成本低，泛化性强。
*   **几何感知**：采用测地线插值而非线性插值，更符合 CLIP 嵌入空间的数学特性。
*   **平衡性好**：通过“扩展+精炼”的策略，同时提升了检索的召回率（多样性）和精确度。

### 8. 不足与局限
*   **MLLM 依赖性**：性能高度依赖于 MLLM（如 GPT-4o）生成属性的质量，且调用 API 可能产生额外的成本和延迟。
*   **超参数敏感性**：虽然范围搜索降低了敏感性，但 Mixup 的起始比例 $\lambda$ 仍需根据经验设定（文中建议 0.6-0.7）。
*   **应用限制**：对于极度缺乏显式修改信息的查询，重排序模块的效果可能会打折扣。

（完）
