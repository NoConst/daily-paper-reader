---
title: "Bridging Perception and Reasoning: Token Reweighting for RLVR in Multimodal LLMs"
title_zh: 连接感知与推理：多模态大语言模型中 RLVR 的 Token 重加权
authors: "Jinda Lu, Junkang Wu, Jinghan Li, Kexin Huang, Shuo Yang, Guoyin Wang, Jiancan Wu, Xiang Wang, Xiangnan He"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25077v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大模型中感知与推理标记的优化
tldr: 本研究针对多模态大语言模型在可验证奖励强化学习（RLVR）中感知与推理令牌相互交织的挑战，提出了Token-Reweighting (ToR) 策略。研究发现感知与推理能力具有内在耦合性，仅优化单一类型令牌效果有限。ToR 通过动态识别并重新加权这两类关键令牌，实现了感知与推理的协同优化，在多个基准测试中显著提升了模型的视觉定位与逻辑推理性能，达到了当前领先水平。
selection_source: fresh_fetch
motivation: 多模态模型中感知与推理令牌高度耦合，导致传统的单一优化策略无法有效平衡视觉定位与逻辑推理能力。
method: 提出一种即插即用的 Token-Reweighting (ToR) 策略，在 RLVR 训练中动态识别并重新加权感知和推理两类关键令牌。
result: 在 GRPO 和 DAPO 等方法上应用 ToR 后，模型在多个多模态推理基准测试中均取得了显著的性能增益并达到 SOTA。
conclusion: 显式建模感知与推理令牌间的相互依赖性对于提升多模态大语言模型的综合推理能力至关重要。
---

## 摘要
将具有可验证奖励的强化学习（RLVR）扩展到多模态大语言模型（MLLMs）面临一个根本性挑战：它们的响应本质上交织着感知相关 token（用于定位视觉内容）和推理相关 token（用于构建推理链）。这些 token 类型实例化了不同但又相互依赖的能力——视觉定位和符号推理——这使得孤立的优化变得不足。通过 token 级别的实证分析，我们证明了仅优化感知或仅优化推理 token 的表现始终不如全面优化，这强调了它们内在的耦合性。为了解决这个问题，我们提出了一种即插即用的 Token 重加权（ToR）策略，该策略通过识别这两类关键 token 并在 RLVR 训练期间动态重新加权，显式地对这种相互依赖性进行建模。在现有方法（如 GRPO 和 DAPO）的基础上应用 ToR，在多个多模态推理基准测试中实现了持续的性能提升，在准确的视觉定位和连贯的推理方面均达到了最先进的性能。

## Abstract
Extending Reinforcement Learning with Verifiable Rewards (RLVR) to multimodal large language models (MLLMs) faces a fundamental challenge: their responses inherently interleave perception-related tokens, which ground visual content, with reasoning-related tokens, which construct reasoning chains. These token types instantiate distinct yet interdependent capacities -- visual grounding and symbolic reasoning -- making isolated optimization insufficient. Through token-level empirical analysis, we demonstrate that optimizing either perception- or reasoning-only tokens consistently underperforms full optimization, underscoring their inherent coupling. To address this, we propose a plug-and-play Token-Reweighting (ToR) strategy that explicitly models this interdependence by identifying critical tokens of both types and dynamically reweighting them during RLVR training. Applied on top of existing methods (e.g., GRPO and DAPO), ToR delivers consistent performance gains across multiple multi-modal reasoning benchmarks, achieving state-of-the-art performance with both accurate visual grounding and coherent reasoning.

---

## 论文详细总结（自动生成）

### 论文总结：连接感知与推理——多模态大语言模型中的 Token 重加权 (ToR)

#### 1. 核心问题与整体含义
*   **研究背景**：可验证奖励强化学习（RLVR，如 DeepSeek-R1）在提升大语言模型（LLM）推理能力上表现卓越。然而，将其扩展到多模态大语言模型（MLLM）时，模型生成的响应会交织着**感知相关 Token**（用于定位视觉内容）和**推理相关 Token**（用于构建逻辑链）。
*   **核心问题**：现有的 MLLM 优化方法通常将感知和推理能力孤立对待。作者通过实证分析发现，这两者在 Token 级别是高度耦合且相互依赖的。如果仅优化其中一类 Token，会导致模型在另一类能力上出现退化（例如：推理强但视觉定位错，或感知强但逻辑不连贯）。

#### 2. 方法论：核心思想与关键技术
作者提出了一种名为 **ToR (Token-Reweighting)** 的即插即用策略，旨在 RLVR 训练期间显式建模感知与推理的相互依赖性。
*   **推理 Token 识别**：基于**预测熵 (Entropy)**。高熵 Token 通常对应推理链中的关键决策点（Forking points）。
*   **感知 Token 识别**：基于**视觉敏感度 (Visual Sensitivity)**。通过计算在“有图像”和“无图像（空占位符）”两种条件下，Token 预测对数概率的差异（logp-diff）来识别。
*   **Token 重加权机制**：
    *   在计算策略梯度（如 GRPO 或 DAPO 损失函数）时，不再对所有 Token 一视同仁。
    *   为识别出的推理 Token 分配权重 $\gamma_r$，为感知 Token 分配权重 $\gamma_p$。
    *   非关键 Token 的权重设为零（即不参与优势函数计算），从而使梯度更新集中在对感知和推理最关键的 Token 上。

#### 3. 实验设计
*   **数据集**：
    *   **训练**：Geometry3K（2.1K 样本，主要用于消融和基础对比）和 ViRL-39K（用于规模化验证）。
    *   **评测基准**：涵盖视觉推理（MathVerse, MathVision, MathVista, WeMath）和视觉感知（HalluBench）五大 Benchmark。
*   **对比方法**：
    *   **开源指令微调模型**：InternVL-2.5/3, LLaVA-OneVision, Qwen-2.5-VL。
    *   **最新 RLVR 方法**：R1-VL, Vision-R1, R1-OneVision, OpenVLThinker, MM-Eureka, NoisyRollout 等。
    *   **基础算法**：GRPO 和 DAPO（作为 ToR 的搭载基座）。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H800 GPU (80GB)**。
*   **训练配置**：学习率 1e-6，全局 Batch Size 为 128，Rollout Batch Size 为 512，每个样本生成 12 个响应（Rollout n=12）。
*   **时长**：文中未明确标注具体的训练总时长，但给出了详细的超参数设置。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括在不同模型规模（3B 和 7B）和不同数据规模（2.1K 和 39K）下的验证。
*   **消融实验**：非常充分。作者对比了不同 Token 选择比例（20% 到 80%）、不同权重分配（$\gamma_p$ 从 0.1 到 1.5）、以及多种感知 Token 识别准则（概率差、对数概率差、熵差、注意力分数）。
*   **客观性**：实验在统一的框架（EasyR1）下进行，对比了 SOTA 模型在相同训练集下的表现，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **耦合性验证**：实验证明，仅优化推理 Token 会导致视觉定位能力下降；仅优化感知 Token 则无法解决推理不确定性。
*   **性能提升**：ToR 显著提升了 GRPO 和 DAPO 的表现。在 Qwen-2.5-VL-7B 基础上，ToR 在多个推理榜单上刷新了 SOTA 记录。
*   **Token 特性**：感知能力依赖于少数视觉敏感 Token 的稳定性，而推理能力依赖于高熵 Token 的鲁棒性。

#### 7. 优点
*   **即插即用**：ToR 作为一个轻量级模块，无需修改模型架构，可直接应用于现有的 RL 算法。
*   **自监督识别**：无需额外的外部标注或视觉模型，仅利用模型自身的预测信号即可识别关键 Token。
*   **协同效应**：成功解决了多模态强化学习中感知与推理“顾此失彼”的失衡问题。

#### 8. 不足与局限
*   **识别精度限制**：目前的感知 Token 识别基于全局概率变化，可能无法像专门的视觉模型（如 SAM）那样实现像素级的精确对应。
*   **上下文依赖**：Token 级别的重加权可能忽略了 Token 之间更深层的语义上下文联系。
*   **任务覆盖**：虽然在数学和几何推理上表现优异，但在更广泛的开放域多模态对话或视频理解任务上的表现仍需进一步验证。

（完）
