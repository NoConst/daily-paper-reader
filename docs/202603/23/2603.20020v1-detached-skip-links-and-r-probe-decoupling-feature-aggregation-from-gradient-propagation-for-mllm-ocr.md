---
title: "Detached Skip-Links and $R$-Probe: Decoupling Feature Aggregation from Gradient Propagation for MLLM OCR"
title_zh: 分离式跳跃连接与 $R$-Probe：为多模态大语言模型 OCR 任务解耦特征聚合与梯度传播
authors: "Ziye Yuan, Ruchang Yao, Chengxin Zheng, Yusheng Zhao, Daxiang Dong, Ming Zhang"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20020v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大模型在OCR任务中的特征融合优化
tldr: 针对多模态大模型在OCR任务中细粒度视觉细节丢失的问题，本文揭示了多层特征融合中跳跃连接导致的梯度干扰现象。作者提出了“分离式跳跃连接”（Detached Skip-Links），在保留浅层特征的同时阻断其梯度回传，并引入R-Probe评估视觉特征的可重建性。该方法在不增加参数的情况下，显著提升了模型在OCR及通用多模态任务上的性能与训练稳定性。
selection_source: fresh_fetch
motivation: 传统的跳跃连接会将高层语义梯度直接传导至早期视觉层，导致细粒度信号被覆盖并干扰模型训练。
method: 提出在特征融合时阻断跳跃路径的梯度回传，并利用浅层解码器R-Probe诊断视觉Token的像素级信息保留程度。
result: 在多个ViT骨干网络和多模态基准测试中，该方法一致性地提高了OCR性能，并增强了通用多模态任务的表现。
conclusion: 通过解耦特征聚合与梯度传播，可以有效缓解梯度干扰，在不增加计算开销的前提下提升MLLM处理细粒度视觉任务的能力。
---

## 摘要
多模态大语言模型 (MLLM) 在高层推理方面表现出色，但在细粒度视觉细节受损或失配的 OCR 任务中表现不佳。我们发现了多层特征融合中一个被忽视的优化问题：跳跃路径引入了从高层语义目标到早期视觉层的直接反向传播路径。这种机制会覆盖低层信号并导致训练不稳定。为了减轻这种梯度干扰，我们提出了分离式跳跃连接 (Detached Skip-Links)，这是一种极简的修改，在前向传播中重用浅层特征，同时在联合训练期间阻断跳跃分支的梯度。这种非对称设计减少了梯度干扰，在不增加可学习参数的情况下提高了稳定性和收敛性。为了诊断细粒度信息是否被保留并可供 LLM 使用，我们引入了 $R$-Probe，它利用从 LLM 前四分之一层初始化的浅层解码器来衡量投影视觉 token 的像素级可重建性。在多个 ViT 骨干网络和多模态基准测试中，以及在高达 700 万个训练样本的规模下，我们的方法持续改进了以 OCR 为中心的基准测试，并在通用多模态任务上取得了显著收益。

## Abstract
Multimodal large language models (MLLMs) excel at high-level reasoning yet fail on OCR tasks where fine-grained visual details are compromised or misaligned. We identify an overlooked optimization issue in multi-layer feature fusion. Skip pathways introduce direct back-propagation paths from high-level semantic objectives to early visual layers. This mechanism overwrites low-level signals and destabilizes training. To mitigate this gradient interference, we propose Detached Skip-Links, a minimal modification that reuses shallow features in the forward pass while stopping gradients through the skip branch during joint training. This asymmetric design reduces gradient interference, improving stability and convergence without adding learnable parameters. To diagnose whether fine-grained information is preserved and usable by an LLM, we introduce $R$-Probe, which measures pixel-level reconstructability of projected visual tokens using a shallow decoder initialized from the first quarter of the LLM layers. Across multiple ViT backbones and multimodal benchmarks, and at scales up to 7M training samples, our approach consistently improves OCR-centric benchmarks and delivers clear gains on general multimodal tasks.

---

## 论文详细总结（自动生成）

### 论文总结：Detached Skip-Links and R-Probe

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）虽然在高级语义推理上表现优异，但在需要细粒度视觉感知的任务（如 OCR、文档解析）中经常出现“幻觉”或识别错误。
*   **研究动机**：现有的多层特征融合方法（如 Skip-Links）旨在将 ViT 的浅层几何信息引入 LLM，但作者发现这会引入**优化干扰**。
*   **核心问题**：直接的跳跃连接建立了一条从 LLM 语义目标到 ViT 早期视觉层的直接反向传播路径。这些原本负责捕获像素级、几何特征的浅层参数会被强烈的、不一致的高层语义梯度“冲刷”，导致训练不稳定和细粒度信号的丢失。

#### 2. 方法论：核心思想与关键技术
论文提出了两个核心组件，旨在解耦特征聚合与梯度传播：
*   **分离式跳跃连接 (Detached Skip-Links)**：
    *   **核心思想**：在前向传播中保留浅层特征的利用，但在反向传播中切断其梯度流。
    *   **技术细节**：将 ViT 的中间层特征分为浅层组（$h_{shallow}$）和深层组（$h_{deep}$）。在融合时，对浅层特征应用 `stop-gradient` ($sg$) 操作。
    *   **公式表达**：融合后的特征 $z = MLP([h_{main}, h_{deep}, sg(h_{shallow})])$。这种非对称设计确保了 LLM 能“看到”细节，但不会“干扰”产生这些细节的早期视觉层。
*   **R-Probe (重建探针)**：
    *   **核心思想**：一种诊断工具，用于衡量视觉 Token 是否保留了足够的像素级信息，并且这些信息是否能被 LLM 架构有效“消化”。
    *   **技术细节**：使用一个浅层 Transformer 解码器（初始化自目标 LLM 的前 1/4 层参数），尝试从投影后的视觉 Token 中重建原始像素。如果重建损失低，说明特征保留度高且对齐良好。

#### 3. 实验设计
*   **数据集与规模**：
    *   **预训练**：500 万（5M）多模态样本（包含图文对、通用 VQA）。
    *   **微调 (SFT)**：200 万（2M）任务特定样本（OCR、文档理解、数学、图表等）。
*   **Benchmark**：涵盖 22 个基准测试，分为四大类：STEM Puzzle (MMMU, MathVista)、General (MMVet, SEEDBench)、Alignment (POPE, HallusionBench) 和 OCR (DocVQA, OCRBench, TextVQA)。
*   **对比方法**：对比了主流的多层融合策略，包括 DenseConnector (DC)、DeepStack 和 Multi-Layer Fusion (ML)。
*   **骨干网络**：验证了多种 ViT 骨干，包括 InternViT-300M、AimV2-L 和 SigLip2-So400M。

#### 4. 资源与算力
*   **算力说明**：论文提到了使用 **DeepSpeed ZeRO-3**、**FlashAttention-2** 和 **bf16** 精度进行大规模训练。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）及具体数量，但从 7M 样本的训练规模和 16k 的序列长度来看，该研究依赖于大规模计算集群。

#### 5. 实验数量与充分性
*   **实验规模**：作者进行了大规模的消融实验，涵盖了不同的采样步长（Stride $S$）和分离层数（Detach $D$）的组合。
*   **充分性与公平性**：
    *   **梯度统计分析**：通过实验测量了主路径与跳跃路径的梯度范数、余弦相似度和方差，从优化理论角度验证了假设。
    *   **跨模型验证**：在四种不同的 ViT 骨干上重复实验，证明了方法的普适性。
    *   **基准对比**：在相同的训练数据和预算下对比了三种 SOTA 融合方法，实验设计客观且具有说服力。

#### 6. 主要结论与发现
*   **性能提升**：Detached Skip-Links 在 OCR 相关任务上提升显著（在 PE 骨干上 OCR 平均分提升 3.1%），同时在通用任务上也保持了稳健增长。
*   **优化稳定性**：切断浅层梯度能有效保持浅层注意力的结构一致性，防止预训练的视觉先验被破坏。
*   **R-Probe 的价值**：证明了视觉 Token 的“可重建性”与下游 OCR 性能之间存在正相关，为 MLLM 的架构设计提供了新的诊断维度。

#### 7. 优点
*   **极简且高效**：不需要增加任何额外的可学习参数或推理开销，仅通过训练时的梯度控制即可获益。
*   **理论与实证结合**：不仅有显著的实验提升，还通过贝叶斯风险分解和梯度方差分析提供了坚实的理论支撑。
*   **诊断工具创新**：R-Probe 解决了 MLLM 领域长期缺乏直接衡量视觉特征质量指标的问题。

#### 8. 不足与局限
*   **任务权衡**：实验显示，当将 R-Probe 作为辅助 Loss 联合训练时，虽然 OCR 性能提升，但在某些复杂推理任务（如 CharXiv RQ）上出现了轻微下降，暗示了感知与推理之间可能存在分布偏移。
*   **算力细节缺失**：未公开具体的训练时长和硬件资源消耗，不便于其他研究者评估复现成本。
*   **超高分辨率限制**：虽然提升了细粒度感知，但对于极高分辨率（如 4k 以上文档）的动态处理能力尚未在本文中深入探讨。

（完）
