---
title: "DEO: Training-Free Direct Embedding Optimization for Negation-Aware Retrieval"
title_zh: DEO：用于否定感知检索的无需训练的直接嵌入优化
authors: "Taegyeong Lee, Jiwon Park, Seunghyun Hwang, JooYoung Jang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09185v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 无需训练的否定感知多模态检索方法
tldr: 针对现有检索模型在处理否定和排除查询时表现不佳且微调成本高的问题，本文提出了一种无需训练的直接嵌入优化方法（DEO）。DEO通过将查询分解为正面和负面组件，并利用对比目标在推理阶段优化查询嵌入。该方法在不增加训练负担的情况下，显著提升了文本和多模态检索的准确性，为现实场景中的否定感知检索提供了高效实用的解决方案。
selection_source: fresh_fetch
motivation: 现有检索方法在处理包含否定或排除语义的查询时效果较差，且现有的改进方案往往需要额外的微调成本和部署复杂度。
method: 提出DEO方法，通过将查询分解为正向和负向组件，并使用对比目标函数直接优化查询嵌入，无需任何模型训练或数据更新。
result: "在NegConstraint测试中显著优于基准模型，并在多模态检索中使CLIP的Recall@5提升了6%，同时大幅提高了nDCG和MAP指标。"
conclusion: DEO证明了在无需重新训练的情况下，通过直接优化嵌入即可有效解决检索中的否定识别问题，具有极强的实用价值。
---

## 摘要
大语言模型（LLM）和检索增强生成（RAG）的最新进展催生了多样化的检索方法。然而，现有的检索方法在处理否定和排除查询时，往往无法准确检索到结果。为了解决这一局限性，先前的方法依赖于嵌入适配或微调，这引入了额外的计算成本和部署复杂性。我们提出了直接嵌入优化（DEO），这是一种用于否定感知文本和多模态检索的无需训练的方法。DEO 将查询分解为正向和负向组件，并利用对比目标优化查询嵌入。在没有额外训练数据或模型更新的情况下，DEO 在 NegConstraint 上的表现优于基准模型，nDCG@10 提升了 0.0738，MAP@100 提升了 0.1028，同时在多模态检索中相比 OpenAI CLIP 将 Recall@5 提高了 6%。这些结果证明了 DEO 在现实场景中进行否定和排除感知检索的实用性。

## Abstract
Recent advances in Large Language Models (LLMs) and Retrieval-Augmented Generation (RAG) have enabled diverse retrieval methods. However, existing retrieval methods often fail to accurately retrieve results for negation and exclusion queries. To address this limitation, prior approaches rely on embedding adaptation or fine-tuning, which introduce additional computational cost and deployment complexity. We propose Direct Embedding Optimization (DEO), a training-free method for negation-aware text and multimodal retrieval. DEO decomposes queries into positive and negative components and optimizes the query embedding with a contrastive objective. Without additional training data or model updates, DEO outperforms baselines on NegConstraint, with gains of +0.0738 nDCG@10 and +0.1028 MAP@100, while improving Recall@5 by +6\% over OpenAI CLIP in multimodal retrieval. These results demonstrate the practicality of DEO for negation- and exclusion-aware retrieval in real-world settings.

---

## 论文详细总结（自动生成）

以下是对论文《DEO: Training-Free Direct Embedding Optimization for Negation-Aware Retrieval》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的密集检索模型（Dense Retrieval）在处理包含**否定（Negation）**或**排除（Exclusion）**语义的查询（例如：“给我看最新的收益预测，但排除2024年的结果”）时表现较差。
*   **研究背景**：虽然现有的研究尝试通过微调（Fine-tuning）或引入适配器（Adaptor）来解决此问题，但这些方法通常需要大量的计算资源（GPU）、大规模标注数据集，且部署复杂，甚至可能导致模型在常规查询上的性能退化。
*   **研究目标**：开发一种**无需训练（Training-free）**、轻量级且通用的方法，使现有检索模型能够准确理解并执行否定指令。

### 2. 核心方法论
DEO（直接嵌入优化）的核心思想是在**推理阶段**通过优化查询嵌入（Query Embedding）来实时调整语义空间，主要分为三个步骤：

1.  **查询分解（Query Decomposition）**：利用大语言模型（LLM）将原始查询分解为：
    *   **正向子查询（Positive Sub-queries）**：强化用户想要检索的核心语义。
    *   **负向子查询（Negative Sub-queries）**：明确用户想要排除的概念。
2.  **直接嵌入优化（Direct Embedding Optimization）**：
    *   将原始查询嵌入作为初始值 $e_u$。
    *   定义一个**对比损失函数（Contrastive Loss）**，包含三部分：
        *   **吸引项**：拉近 $e_u$ 与正向子查询嵌入的距离。
        *   **排斥项**：推开 $e_u$ 与负向子查询嵌入的距离。
        *   **一致性正则项**：确保优化后的嵌入不会偏离原始查询语义太远。
    *   使用 Adam 优化器在推理时对 $e_u$ 进行少量步数（如20步）的梯度下降更新，而保持编码器（Encoder）参数冻结。
3.  **执行检索**：使用优化后的嵌入 $e_u$ 在向量数据库中进行相似度搜索。

### 3. 实验设计
*   **数据集与场景**：
    *   **文本检索**：使用 NegConstraint 和 NevIR 数据集，评估指标为 nDCG@10、MAP@100 和 Pairwise 准确率。
    *   **多模态检索（文到图）**：使用 COCO-Neg 数据集，评估指标为 Recall@5。
*   **对比基准（Baselines）**：
    *   **文本模型**：BGE 系列（small, large, M3）。
    *   **多模态模型**：OpenAI CLIP、CLIP-laion400m、CLIP-datacomp 以及专门针对否定优化的 NegCLIP。
*   **消融实验**：测试了不同 LLM 后端（GPT-4 vs. Qwen）、不同损失权重（$\lambda$）、仅分解不优化、以及不同优化步数的影响。

### 4. 资源与算力
*   **硬件环境**：论文明确提到了测试环境。
    *   **CPU**：AMD Ryzen 7 5800X (8-Core)。
    *   **GPU**：NVIDIA GeForce RTX 3060 (12GB)。
*   **计算效率**：
    *   在 CPU 上，20步优化仅需 **0.016秒**。
    *   在 GPU 上，20步优化仅需 **0.033秒**。
*   **结论**：该方法计算开销极低，非常适合实时生产环境。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从单模态到多模态的多个主流否定感知基准测试。
*   **充分性**：
    *   **模型覆盖广**：验证了 DEO 在不同规模和架构（BGE, CLIP）上的通用性。
    *   **消融实验详尽**：通过对比“仅分解”和“分解+优化”，证明了性能提升主要源于嵌入空间的直接优化，而非仅仅是 LLM 的改写。
    *   **可视化分析**：通过 PCA 降维展示了查询嵌入在优化过程中的轨迹，直观证明了方法有效性。
*   **客观性**：实验对比了专门微调过的模型（如 NegCLIP），证明 DEO 即使在强基准上仍有增益。

### 6. 主要结论与发现
*   **性能显著提升**：在 NegConstraint 上，BGE-large 结合 DEO 后 nDCG@10 提升了 **0.0738**，MAP 提升了 **16.32%**。
*   **多模态通用性**：在 COCO-Neg 任务中，OpenAI CLIP 的 Recall@5 提升了 **6%**。
*   **无需微调的优越性**：DEO 在不改变模型权重的前提下，达到了甚至超过了某些经过专门否定训练的模型的效果。
*   **鲁棒性**：即使使用参数量较小的 LLM（如 Qwen-1.5B）进行分解，DEO 依然能带来明显的性能增益。

### 7. 优点与亮点
*   **即插即用（Plug-and-Play）**：无需重新训练模型，可直接应用于任何现有的密集检索系统。
*   **极高效率**：推理时的优化耗时在毫秒级，几乎不影响系统响应速度。
*   **可解释性与控制力**：通过调整正负向子查询的权重（$\lambda$），开发者可以精确控制“排除”语义的强度。
*   **跨模态支持**：不仅适用于文本，也完美适配视觉-语言模型。

### 8. 不足与局限
*   **对 LLM 的依赖**：DEO 的效果高度依赖于第一步 LLM 分解的质量。如果 LLM 无法正确识别否定逻辑，优化方向可能会产生偏差。
*   **超参数敏感性**：虽然实验证明了其鲁棒性，但在不同领域（如医疗、法律）可能需要微调损失函数的权重（$\lambda$）以达到最优效果。
*   **多模态覆盖有限**：目前仅验证了图像检索，尚未在音频、视频等更复杂的多模态场景中进行大规模测试。

（完）
