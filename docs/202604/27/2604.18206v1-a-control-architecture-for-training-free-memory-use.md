---
title: A Control Architecture for Training-Free Memory Use
title_zh: 一种免训练内存使用的控制架构
authors: "Yanzhen Lu, Muchen Jiang, Zhicheng Qian, Xingyu Zhou"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18206v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 随时间推移对记忆库进行治理
tldr: 本研究针对提示注入记忆在推理中的应用控制问题，提出了一种无需训练的控制架构。该架构通过不确定性路由、置信度选择性接受及记忆库动态管理，解决了何时触发记忆辅助及如何维护记忆库的难题。在算术基准测试中，该方法显著提升了模型性能，证明了控制机制在提升记忆利用效率中的核心作用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18206v1/fig-001.webp\", \"caption\": \"Figure 1: TAG decision flow. At each step, the baseline action is decoded and its confidence assessed. Low-confidence steps are routed to memory retrieval and a second-pass decode. The second-pass output is accepted only if it passes both a confidence-margin check and structural guards; otherwise the system rolls back to the baseline action. Over time, persistently harmful memory entries are retired.\", \"page\": 2, \"index\": 1, \"width\": 943, \"height\": 509}, {\"url\": \"assets/figures/arxiv/2604.18206v1/fig-002.webp\", \"caption\": \"Table 15: Agent fixed-budget retrieval comparison. Fixed-budget can improve raw score on WebShop, but TAG yields lower harmful-acceptance rate (HAR), better left-tail risk (LTR), and higher trajectory recovery (TR).\", \"page\": 14, \"index\": 2, \"width\": 596, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2604.18206v1/fig-003.webp\", \"caption\": \"Figure 2: Confidence-binned accuracy for baseline (blue) and memory-augmented (orange). Gains concentrate in low-confidence bins, where routing has the most headroom.\", \"page\": 8, \"index\": 3, \"width\": 943, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2604.18206v1/fig-004.webp\", \"caption\": \"Table 1: Execution summary of TAG. The training-free constraint applies throughout: the model weights never change, and only prompts, routing decisions, and fit-time memory-bank membership are updated.\", \"page\": 4, \"index\": 4, \"width\": 931, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2604.18206v1/fig-005.webp\", \"caption\": \"Table 10: Confidence signal sweep summary. Sequence-level signals yield useful sparse-routing points; ASDiv first-token confidence degrades toward near always-inject behavior.\", \"page\": 12, \"index\": 5, \"width\": 583, \"height\": 208}]"
motivation: 旨在解决提示注入记忆在推理过程中因应用时机不当而导致的控制难题。
method: 结合不确定性路由、置信度选择性接受和记忆库治理，构建了一套免训练的适用性控制架构。
result: 在SVAMP和ASDiv算术任务上分别实现了7.0和7.67个百分点的性能提升，并成功迁移至QA和智能体任务。
conclusion: 研究表明，有效的控制架构而非单纯的记忆检索，是实现免训练记忆利用和性能提升的关键驱动力。
---

## 摘要
提示注入式内存（Prompt-injected memory）可以在不更新模型权重的情况下提高推理能力，但它也带来了一个控制问题：检索到的内容只有在正确的状态下应用时才有帮助。我们在严格的免训练设置下研究这一问题，并将其表述为适用性控制（applicability control）：何时触发内存辅助的第二次传递，何时信任它，以及如何随时间维护内存库。我们的方法结合了基于不确定性的路由、基于置信度的选择性接受、跨规则和示例内存的库选择，以及随时间推移对内存库进行的基于证据的治理。在具有计算匹配对照的锁定免训练协议下，该方法在两个核心算术基准测试中表现优异，SVAMP 提升了 7.0 分，ASDiv 提升了 7.67 分。相同的架构也可以迁移到问答（QA）和智能体（agent）基准测试中，并产生较小的正面效果，且在主要算术任务的第二个检查点上显示出相同的正面趋势。在算术任务上，主要的实证模式是控制架构而非原始内存暴露驱动了 SVAMP 和 ASDiv 的改进。从机制上看，置信度区分了有益和有害的规则库干预，并且在固定检索下，修复与损坏之间的差异定位于那些检索集确实包含已编辑条目的行。

## Abstract
Prompt-injected memory can improve reasoning without updating model weights, but it also creates a control problem: retrieved content helps only when it is applied in the right state. We study this problem in a strict training-free setting and formulate it as applicability control: when to trigger a memory-assisted second pass, when to trust it, and how to maintain the memory bank over time. Our method combines uncertainty-based routing, confidence-based selective acceptance, bank selection across rule and exemplar memory, and evidence-based governance of the memory bank over time. Under a locked training-free protocol with compute-matched controls, it improves two core arithmetic benchmarks by +7.0 points on SVAMP and +7.67 points on ASDiv over baseline. The same architecture also transfers to QA and agent benchmarks with smaller positive effects and shows the same positive direction on a second checkpoint for the main arithmetic tasks. On arithmetic, the main empirical pattern is that the control architecture, rather than raw memory exposure, drives the improvements on SVAMP and ASDiv. Mechanistically, confidence separates helpful from harmful rule-bank interventions, and under fixed retrieval the repair-versus-corrupt difference localizes to rows whose retrieved set actually contains the edited entries.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Control Architecture for Training-Free Memory Use》（一种免训练内存使用的控制架构）的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在不更新模型权重的情况下，通过提示注入（Prompt-injected）记忆（如规则或示例）可以提升大模型的推理能力。
*   **核心问题**：检索到的记忆内容并非总是有效。如果应用时机不当，错误的记忆干预反而会误导模型或造成计算资源浪费。
*   **研究动机**：现有的检索增强生成（RAG）研究多关注“检索什么”，而忽略了“是否应该应用检索内容”。本论文旨在解决**适用性控制（Applicability Control）**问题，即在免训练的前提下，决定何时触发记忆、何时信任记忆以及如何维护记忆库。

### 2. 方法论：TAG 架构
论文提出了 **TAG (Training-free Applicability Governance)** 架构，其核心思想是将记忆使用视为一个多级的干预控制过程：
*   **不确定性路由（Uncertainty-Based Routing）**：首先进行基座模型解码，计算输出的置信度（如 Token 平均对数概率）。只有当置信度低于设定阈值 $\tau$ 时，才触发记忆检索和第二次解码。
*   **带回滚的选择性接受（Guarded Acceptance with Rollback）**：对比第二次（带记忆）和第一次（基座）生成的候选答案。只有当第二次生成的置信度显著高于第一次（超过边际 $m$），且通过了结构化守卫（如格式检查、逻辑一致性）时，才接受新答案；否则回滚至基座答案。
*   **证据驱动的记忆治理（Evidence-Based Governance）**：在开发阶段，记录每个记忆条目对结果的贡献。利用 UCB（置信上限）算法剔除那些长期导致负面效果的记忆条目，实现记忆库的动态优化。
*   **多库选择**：支持在“规则库”（抽象原理）和“示例库”（具体案例）之间进行级联或并行选择。

### 3. 实验设计
*   **数据集与场景**：
    *   **算术推理**：SVAMP、ASDiv、MultiArith。
    *   **常识问答（QA）**：OpenBookQA、ARC-Easy、ARC-Challenge。
    *   **智能体（Agent）任务**：WebShop、ScienceWorld。
*   **基座模型**：主要使用 Qwen3-0.6B（推理）和 Qwen3-8B（智能体），并使用 Qwen2.5-1.5B 进行鲁棒性验证。
*   **对比方法（Benchmarks）**：
    *   **Baseline**：单次解码。
    *   **Retry**：计算量对齐的二次解码（不加记忆），用于证明增益非来自“多试一次”。
    *   **Always-retrieve**：无控制的强制检索应用。
    *   **Verifier/Self-consistency**：其他常见的推理增强手段。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。
*   **计算特性**：由于是“免训练”方法，主要的计算开销集中在推理阶段的二次解码。通过路由机制，TAG 实际上比“全量检索”方法节省了大量计算资源。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 大类任务、7 个主流数据集，并进行了跨模型检查点的验证。
*   **充分性**：
    *   采用了严格的“锁定协议”（Locked Protocol），在开发集确定超参后在测试集冻结，确保了结果的客观性。
    *   进行了详细的消融实验，拆解了路由、接受、治理等各个组件的贡献。
    *   引入了 McNemar 配对检验和 95% 置信区间，统计学论证较为充分。

### 6. 主要结论与发现
*   **控制架构是核心**：在 SVAMP 和 ASDiv 上，TAG 分别比基座提升了 **7.0** 和 **7.67** 个百分点。实验证明，性能提升主要来自“控制层”拦截了有害干预，而非单纯的记忆暴露。
*   **置信度的区分力**：置信度信号能有效区分规则库（Rule Bank）的有益和有害干预（AUC 达 0.79-0.85），但在某些示例库上效果较弱。
*   **反事实验证**：通过固定检索标识符的实验证明，增益确实源于记忆内容的修复作用，而非检索漂移或随机性。
*   **通用性**：该架构在 QA 和智能体任务中同样表现出正向收益，或在保持性能的同时显著降低了调用成本。

### 7. 优点与亮点
*   **完全免训练**：无需微调模型，易于在现有生产系统中直接部署。
*   **干预视角**：将 RAG 问题转化为决策控制问题，提供了清晰的数学框架（基于干预价值 $V_t$）。
*   **鲁棒性强**：通过回滚机制和结构化守卫，极大地降低了引入记忆带来的“副作用”。

### 8. 不足与局限
*   **任务敏感性**：在 MultiArith 等某些任务上增益不明显，说明控制逻辑对任务类型有一定依赖。
*   **置信度依赖**：该方法高度依赖模型输出的置信度信号，若模型本身校准极差，控制效果会打折扣。
*   **记忆库构建**：论文未详细讨论记忆库本身的自动化构建成本，目前仍依赖于从训练集或开发集提取的资源。

（完）
