---
title: "ABMAMBA: Multimodal Large Language Model with Aligned Hierarchical Bidirectional Scan for Efficient Video Captioning"
title_zh: ABMAMBA：基于对齐分层双向扫描的高效视频字幕生成多模态大语言模型
authors: "Daichi Yashima, Shuhei Kurita, Yusuke Oda, Shuntaro Suzuki, Seitaro Otsuki, Komei Sugiura"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08050v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 用于视频字幕生成的多模态大语言模型 (MLLM)
tldr: 针对视频字幕生成中Transformer架构计算复杂度随序列长度呈平方增长的问题，本文提出ABMamba。这是一种基于深度状态空间模型（SSM）的全开源多模态大语言模型，通过引入对齐分层双向扫描模块，实现了对视频多尺度时间分辨率的高效处理。该模型在保持竞争力的同时，显著提升了推理吞吐量，为长视频理解提供了高效的线性复杂度方案。
selection_source: fresh_fetch
motivation: 现有的基于Transformer的多模态大模型在处理长视频序列时，因注意力机制的平方级计算复杂度而面临巨大的计算瓶颈。
method: 提出ABMamba模型，采用Mamba作为语言骨干网络，并设计了对齐分层双向扫描模块来捕捉视频的多尺度时间依赖关系。
result: 在VATEX和MSR-VTT等基准测试中，ABMamba在性能与主流模型相当的前提下，实现了约三倍的推理吞吐量提升。
conclusion: ABMamba证明了基于SSM的架构在视频理解任务中具有高效处理长序列的能力，是替代传统Transformer架构的有力竞争者。
---

## 摘要
在本研究中，我们专注于利用全开源多模态大语言模型（MLLM）进行视频字幕生成。由于视觉序列具有复杂的时空依赖性和较长的序列长度，对其进行理解具有挑战性。现有基于 Transformer 的方法的核心注意力机制随序列长度呈二次方增长，导致计算成本极高。为了解决这些局限性，我们提出了对齐分层双向扫描 Mamba（ABMamba），这是一种具有线性计算复杂度的全开源 MLLM，能够实现视频序列的可扩展处理。ABMamba 采用深度状态空间模型（Deep State Space Models）作为其语言主干，取代了昂贵的二次方注意力机制，并引入了一种新型的对齐分层双向扫描模块，可在多个时间分辨率下处理视频。在 VATEX 和 MSR-VTT 等标准视频字幕生成基准测试中，ABMamba 与典型的 MLLM 相比展现出具有竞争力的性能，同时实现了约三倍的吞吐量。

## Abstract
In this study, we focus on video captioning by fully open multimodal large language models (MLLMs). The comprehension of visual sequences is challenging because of their intricate temporal dependencies and substantial sequence length. The core attention mechanisms of existing Transformer-based approaches scale quadratically with the sequence length, making them computationally prohibitive. To address these limitations, we propose Aligned Hierarchical Bidirectional Scan Mamba (ABMamba), a fully open MLLM with linear computational complexity that enables the scalable processing of video sequences. ABMamba extends Deep State Space Models as its language backbone, replacing the costly quadratic attention mechanisms, and employs a novel Aligned Hierarchical Bidirectional Scan module that processes videos across multiple temporal resolutions. On standard video captioning benchmarks such as VATEX and MSR-VTT, ABMamba demonstrates competitive performance compared to typical MLLMs while achieving approximately three times higher throughput.

---

## 论文详细总结（自动生成）

以下是对论文《ABMAMBA: Multimodal Large Language Model with Aligned Hierarchical Bidirectional Scan for Efficient Video Captioning》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：视频理解任务（如视频字幕生成）面临两大挑战：一是视频序列具有复杂的时空依赖性；二是序列长度巨大。
*   **核心问题**：现有的基于 Transformer 的多模态大语言模型（MLLM）核心注意力机制的计算复杂度随序列长度呈**二次方增长**（$O(L^2)$），导致处理长视频时计算成本极高，难以实现实时处理或扩展到超长序列。
*   **整体含义**：本文旨在开发一种**全开源**（数据集、代码、权重均公开）的高效视频 MLLM，通过引入线性复杂度的状态空间模型（SSM）替代 Transformer，在保持理解能力的同时大幅提升推理效率。

### 2. 论文提出的方法论
ABMamba 模型由三个核心组件构成：
*   **双视觉编码器（Vision Encoder）**：同时使用 **SigLIP**（捕捉鲁棒的语义对齐）和 **DINOv2**（捕捉细粒度的视觉特征），将视频帧转换为特征向量并进行拼接。
*   **对齐分层双向扫描模块（AHBS）**：这是本文的核心创新。
    *   **多分辨率处理**：通过多个并行的时间路径，以不同的采样率（Stride）处理视频，从而同时捕捉细粒度和粗粒度的时间动态。
    *   **双向扫描**：在每个路径中，利用 Mamba 操作进行前向和后向双向扫描，克服了传统 SSM 的单向性，有效建模非因果关系的视觉数据。
    *   **特征聚合**：将不同分辨率的扫描结果进行聚合（如相加或拼接），生成最终的视觉表示。
*   **Mamba-LLM 主干**：采用 **Mamba-2.8b-zephyr** 作为语言模型。相比 Transformer，Mamba 具有线性计算复杂度，能够更高效地处理融合了视频和文本的长序列。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：LLaVA 1.5 的 665K 图像-文本指令数据集 + Video-ChatGPT 的 100K 视频-文本指令数据集。
    *   **评估集**：**VATEX**（包含 4.1 万个视频剪辑，涵盖 600 种人类活动）和 **MSR-VTT**（包含 1 万个 Web 视频剪辑）。
*   **对比方法**：
    *   **全开源 MLLM**：Video-ChatGPT (7B), Video-LLaVA (7B), LLaVA-OneVision (7B)。
    *   **小型 MLLM**（<4B 参数）：InternVL2.5 (2.2B), VideoLLaMA3 (2B)。
    *   **闭源模型参考**：Gemini-1.5-Pro。
*   **评估指标**：BLEU, ROUGE-L, METEOR, CIDEr, PAC-S 以及推理吞吐量（tokens/s）。

### 4. 资源与算力
*   **硬件环境**：训练使用了 **16 台 NVIDIA H200 SXM GPU**（每张显卡 141GB 显存）。
*   **训练时长**：总训练时间约为 **8 小时**。
*   **推理测试**：在单张 **NVIDIA A100 (40GB)** 上进行评估。
*   **模型规模**：可训练参数约为 **3.6B**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流视频字幕基准（VATEX, MSR-VTT）上进行了完整的定量对比。
    *   进行了**消融实验**，验证了双向扫描、多分辨率分支数量（M）和步长（Stride）对性能的影响。
    *   增加了 **Video QA**（VideoMME 基准）的泛化能力测试。
    *   进行了**显存效率分析**和**错误案例分析**（Error Analysis）。
*   **充分性评价**：实验设计较为全面，不仅关注精度指标，还深入探讨了推理速度和显存占用。消融实验清晰地证明了 AHBS 模块的必要性，而非简单地套用 Mamba 架构。

### 6. 论文的主要结论与发现
*   **性能领先**：ABMamba 在 VATEX 和 MSR-VTT 上的 BLEU4 等指标优于或等同于参数量更大的 7B 级 Transformer 模型。
*   **推理极快**：ABMamba 的推理吞吐量（约 95 tokens/s）是基线模型（如 Video-ChatGPT）的 **3 倍左右**。
*   **显存高效**：在推理过程中，ABMamba 的显存增长仅为 482MB，远低于 Transformer 模型（通常为 2GB-22GB），证明了其在资源受限环境下的潜力。
*   **时间建模有效性**：实验证明，简单的线性投影无法处理视频，而 AHBS 模块的多尺度双向扫描是捕捉视频动态的关键。

### 7. 优点
*   **高效率与低延迟**：成功将 SSM 的线性复杂度引入视频 MLLM，解决了长视频处理的计算瓶颈。
*   **创新的 AHBS 模块**：通过分层和双向扫描，弥补了原生 Mamba 在处理非因果视觉序列时的短板。
*   **全开源贡献**：提供了完整的数据、代码和权重，对学术界和工业界的透明度和可复现性具有重要意义。

### 8. 不足与局限
*   **幻觉问题**：错误分析显示，**物体幻觉**（Object Hallucination）仍是主要瓶颈（占错误类型的 68%），模型有时会识别出视频中不存在的物体。
*   **任务覆盖有限**：主要集中在视频字幕生成，对于更复杂的长视频推理、多轮对话和细粒度时间定位（Temporal Grounding）的探索尚不充分。
*   **融合策略**：目前采用的是后期融合（Late Fusion），视觉和语言特征在进入 LLM 前的交互可能不够深入，未来可能需要类似交叉注意力的机制来增强对齐。

（完）
