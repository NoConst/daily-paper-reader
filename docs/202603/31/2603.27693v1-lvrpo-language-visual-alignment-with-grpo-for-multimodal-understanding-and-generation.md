---
title: "LVRPO: Language-Visual Alignment with GRPO for Multimodal Understanding and Generation"
title_zh: LVRPO：基于 GRPO 的语言-视觉对齐，用于多模态理解与生成
authors: "Shentong Mo, Sukmin Yun"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27693v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态预训练中的语言-视觉对齐
tldr: 本研究针对统一多模态预训练中隐式对齐导致的细粒度推理和生成能力不足问题，提出了LVRPO框架。该框架利用组相对策略优化（GRPO），通过偏好驱动的强化学习信号直接优化模型行为，实现语言与视觉表示的显式对齐。LVRPO无需辅助编码器或手工设计的对齐目标，在多模态理解、生成和推理任务中均表现出色，为构建高性能统一多模态基础模型提供了新路径。
selection_source: fresh_fetch
motivation: 现有的统一多模态预训练方法多依赖隐式对齐信号，难以在细粒度推理和可控生成任务中达到最优效果。
method: 提出LVRPO框架，采用组相对策略优化（GRPO）通过偏好驱动的强化学习信号直接优化多模态模型行为。
result: 实验表明，LVRPO在涵盖多模态理解、生成和推理的广泛基准测试中一致优于现有的强基线模型。
conclusion: LVRPO证明了无需辅助编码器，通过强化学习显式对齐语言与视觉表示能有效提升多模态模型的综合性能。
---

## 摘要
统一多模态预训练已成为在单个基础模型中联合建模语言和视觉的一种极具前景的范式。然而，现有方法在很大程度上依赖于隐式或间接的对齐信号，在同时支持多模态理解与生成方面仍表现欠佳，特别是在需要细粒度语言-视觉推理和可控生成的场景中。在这项工作中，我们提出了 LVRPO，这是一种基于语言-视觉强化学习的偏好优化框架，利用群体相对策略优化（GRPO）显式地对齐语言和视觉表示。LVRPO 并非在表示层引入额外的对齐损失，而是通过偏好驱动的强化信号直接优化多模态模型的行为，从而在理解和生成任务中促进语言与视觉之间一致且具有语义基础的交互。这种形式化方法无需辅助编码器或手工设计的跨模态目标即可实现有效对齐，并能自然地扩展到多种多模态能力。实验结果表明，在涵盖多模态理解、生成和推理的广泛基准测试中，LVRPO 始终优于强大的统一预训练基线模型。

## Abstract
Unified multimodal pretraining has emerged as a promising paradigm for jointly modeling language and vision within a single foundation model. However, existing approaches largely rely on implicit or indirect alignment signals and remain suboptimal for simultaneously supporting multimodal understanding and generation, particularly in settings that require fine-grained language-visual reasoning and controllable generation. In this work, we propose LVRPO, a language-visual reinforcement-based preference optimization framework that explicitly aligns language and visual representations using Group Relative Policy Optimization (GRPO). Instead of introducing additional alignment losses at the representation level, LVRPO directly optimizes multimodal model behaviors through preference-driven reinforcement signals, encouraging consistent and semantically grounded interactions between language and vision across both understanding and generation tasks. This formulation enables effective alignment without requiring auxiliary encoders or handcrafted cross-modal objectives, and naturally extends to diverse multimodal capabilities. Empirically, LVRPO consistently outperforms strong unified-pretraining baselines on a broad suite of benchmarks spanning multimodal understanding, generation, and reasoning.

---

## 论文详细总结（自动生成）

这是一份关于论文《LVRPO: Language-Visual Alignment with GRPO for Multimodal Understanding and Generation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的统一多模态基础模型（如 BAGEL, Emu3）主要依赖隐式或间接的对齐信号（如预测下一个 Token 或流匹配），导致模型在处理需要细粒度语言-视觉推理和精确可控生成的任务时表现不佳。
*   **研究背景**：
    *   **表示对齐的局限性**：传统方法（如 REPA）试图将生成特征与固定的视觉编码器（如 DINOv2）对齐，但这往往过于僵化，无法捕捉生成任务所需的复杂语义。
    *   **理解与生成的冲突**：在统一模型中，理解任务（交叉熵损失）与生成任务（扩散或自回归损失）之间存在目标冲突，导致模型可能擅长描述图像却无法按指令精确编辑图像。
    *   **缺乏惩罚机制**：现有模型缺乏对“统计上合理但语义上错误”输出（如要求红车却生成蓝车）的显式惩罚。

### 2. 方法论
LVRPO 提出了一种基于强化学习的偏好优化框架，核心思想是**从“表示对齐”转向“行为对齐”**。
*   **核心技术：GRPO（组相对策略优化）**：
    *   借鉴了 DeepSeek 的 GRPO 算法，通过对同一提示词（Prompt）采样一组（$G$ 个）输出，利用组内相对奖励来计算优势（Advantage），从而**无需额外的 Critic（评论员）网络**，显著降低了计算开销。
*   **多维奖励函数**：
    *   **语义接地奖励 ($r_{sem}$)**：利用冻结的 **SigLIP 2** 编码器计算生成内容与文本提示之间的密集语义相似度。
    *   **指令遵循奖励 ($r_{ins}$)**：通过可验证的规则（如分辨率、物体数量、长宽比）评估输出是否符合指令。
    *   **知识一致性奖励 ($r_{kn}$)**：利用 VQA 代理（如 PaLI-3）验证生成内容是否符合事实知识。
*   **架构细节**：
    *   采用 **MoT（Mixture-of-Transformers）** 架构，将模型分为“推理专家”和“生成专家”，通过路由机制减少模态间的干扰，实现多模态思维链（M-CoT）。

### 3. 实验设计
*   **数据集**：
    *   **训练**：使用 50 万个“指令-行为”样本，涵盖科学问答（ScienceQA）、人类偏好数据（Pick-a-Pic v2）和合成约束指令。
    *   **预训练基座**：基于 BAGEL 模型（在万亿级 Token 上预训练）。
*   **Benchmark（基准测试）**：
    *   **图像生成**：GenEval（评估组合属性）。
    *   **世界知识**：WISE（评估知识与图像的对齐）。
    *   **图像编辑**：GEdit-Bench, IntelligentBench。
    *   **多模态理解**：MMMU, MathVista, MME, MMBench 等。
*   **对比方法**：包括仅生成模型（SDXL, DALL-E 3, Flux.1）、统一模型（Chameleon, Janus, Emu3, BAGEL）以及闭源模型（GPT-4o, Gemini 2.0）。

### 4. 资源与算力
*   **模型规模**：主要实验基于 **7B** 参数版本的模型，同时也测试了 1.3B 和 13B 版本。
*   **训练配置**：
    *   使用了 **DeepSpeed ZeRO-3** 优化策略。
    *   训练步数为 **5000 步**，全局 Batch Size 为 256。
    *   组采样大小 $G=8$。
*   **算力细节**：文中**未明确说明具体的 GPU 型号和数量**，但提到 7B 模型在 $G=8$ 时每步训练耗时约 2.1 秒。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从生成、编辑到理解的四大类任务，共计十余个主流基准测试。
*   **消融实验**：
    *   测试了不同组大小 $G$（2 到 16）对性能的影响。
    *   分析了奖励模型（SigLIP 2）冻结与否对训练稳定性的影响（证明了冻结的必要性，防止“奖励作弊”）。
    *   探讨了理解与生成任务之间的“跷跷板效应”。
*   **充分性评价**：实验设计非常全面，不仅对比了开源模型，还直接挑战了 GPT-4o 等顶尖闭源模型，且通过理论证明和定性分析（如“绿色消防车”案例）解释了模型行为，具有较高的说服力。

### 6. 主要结论与发现
*   **性能飞跃**：LVRPO 在 GenEval 上达到 0.91 分，显著优于基座模型 BAGEL（0.82），并在 WISE 知识推理任务上超越了 GPT-4o。
*   **正向干预**：强化生成路径的行为对齐不仅没有损害理解能力，反而提升了判别式任务（如视觉感知 MMVP）的性能，证明了两种模态在共享表示下的协同效应。
*   **克服先验偏差**：通过强化学习，模型能够克服训练数据中的强统计先验（例如能根据指令生成“绿色的消防车”，而非默认的红色）。

### 7. 优点
*   **高效对齐**：引入 GRPO 实现了无 Critic 网络的强化学习，解决了统一模型在大规模参数下进行 RLHF 的算力瓶颈。
*   **行为驱动**：避开了僵化的特征蒸馏，通过最终输出的质量来引导模型学习，更符合实际应用需求。
*   **架构解耦**：MoT 架构有效地平衡了数值精度（生成所需）和离散推理（语言所需）之间的矛盾。

### 8. 不足与局限
*   **奖励模型依赖**：模型性能高度依赖于 SigLIP 2 等奖励模型的质量，如果奖励模型存在视觉盲点（如极细粒度的空间模拟），LVRPO 也会受到限制。
*   **计算开销**：虽然省去了 Critic 网络，但组采样（Group Sampling）过程（如 $G=8$）相比标准监督微调（SFT）仍增加了数倍的推理计算量。
*   **潜在偏见**：强化学习可能导致模型过度拟合于奖励函数的偏好，存在放大奖励模型本身偏见的风险。

（完）
