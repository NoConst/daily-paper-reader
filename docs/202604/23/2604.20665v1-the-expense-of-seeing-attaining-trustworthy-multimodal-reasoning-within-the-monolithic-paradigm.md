---
title: "The Expense of Seeing: Attaining Trustworthy Multimodal Reasoning Within the Monolithic Paradigm"
title_zh: 视觉的代价：在单体范式下实现可信的多模态推理
authors: "Karan Goyal, Dikshant Kukreja"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20665v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 利用强语言先验绕过视觉表示
tldr: 本研究揭示了当前视觉语言模型（VLM）在多模态推理中的不可信问题，即模型倾向于利用语言先验而非视觉输入（功能性盲视）。作者提出了一种基于信息论的“模态转换协议”，并定义了ToS、CoS和FoS三项新指标及语义充分性准则（SSC），旨在量化视觉表征瓶颈。研究还提出了多模态缩放的发散定律，为构建真正具备视觉理解能力的下一代AI系统提供了理论基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20665v1/fig-001.webp\", \"caption\": \"Figure 1: The Divergence Law of Multimodal Scaling. As Model Scale in Parameters (x-axis) increases, the LLM’s true reasoning capability (𝑆𝑆𝑦𝑚𝑇 , logarithmic curve) rises sharply, while the macroscopic benchmark performance (𝑆𝐹𝑢𝑙𝑙 ) scales more shallowly. The expanding shaded region between the two curves represents the compounding Toll of Seeing (𝑇𝑜𝑆).\", \"page\": 4, \"index\": 1, \"width\": 506, \"height\": 284}]"
motivation: 现有视觉语言模型常依赖语言先验而非视觉数据进行推理，且传统评估方法难以区分数据集偏差与架构缺陷。
method: 提出模态转换协议，通过翻译语义负载而非消融数据，定义了衡量视觉代价的三项新指标及语义充分性准则。
result: 发现了多模态缩放的发散定律，即随着语言引擎能力的提升，视觉知识瓶颈带来的数学惩罚反而会增加。
conclusion: 必须将语义充分性准则从诊断约束提升为架构蓝图，以强制模型实现真正的多模态推理而非虚假增益。
---

## 摘要
视觉语言模型（VLM）的迅速普及被广泛誉为统一多模态知识发现的曙光，但其基础建立在一个危险且未受质疑的公理之上：即当前的 VLM 能够忠实地合成多模态数据。我们认为事实并非如此。相反，在主流的“视觉编码器-投影器-大语言模型”（Vision Encoder-Projector-LLM）范式下，潜藏着深刻的可信度危机。最先进的模型并非从视觉输入中提取有根据的知识，而是经常表现出“功能性盲视”，即利用强大的语言先验来绕过严重的视觉表示瓶颈。在这项工作中，我们挑战了传统的多模态评估方法，这些方法依赖于数据消融或创建新数据集，因此致命地将数据集偏差与架构能力的不足混为一谈。我们提出了一种激进的、基于信息论的新方法：模态转换协议（Modality Translation Protocol），旨在量化揭示“视觉的代价”（Expense of Seeing）。通过转换语义负载而非将其消融，我们制定了三个新颖的指标——视觉的通行费（ToS）、视觉的诅咒（CoS）和视觉的谬误（FoS）——并最终形成了语义充分性准则（SSC）。此外，我们提出了一个具有启发性的“多模态缩放发散定律”（Divergence Law of Multimodal Scaling），假设随着底层语言引擎缩放到前所未有的推理能力，视觉知识瓶颈的数学惩罚反而会矛盾地增加。我们呼吁 KDD 社区放弃对“多模态增益”的虚幻追求。通过将 SSC 从被动的诊断约束提升为主动的架构蓝图，我们为迫使下一代 AI 系统真正“看见”数据并实现真正的多模态推理提供了严谨、可信的基础。

## Abstract
The rapid proliferation of Vision-Language Models (VLMs) is widely celebrated as the dawn of unified multimodal knowledge discovery but its foundation operates on a dangerous, unquestioned axiom: that current VLMs faithfully synthesise multimodal data. We argue they do not. Instead, a profound crisis of trustworthiness underlies the dominant Vision Encoder-Projector-LLM paradigm. Rather than extracting grounded knowledge from visual inputs, state-of-the-art models frequently exhibit functional blindness, i.e., exploiting strong language priors to bypass severe visual representation bottlenecks. In this work, we challenge the conventional methodology of multimodal evaluation, which relies on data ablation or new dataset creation and therefore fatally conflates dataset biases with architectural incapacity. We propose a radical, information-theoretic departure: the Modality Translation Protocol, designed to quantifiably unmask the Expense of Seeing. By translating semantic payloads rather than ablating them, we formulate three novel metrics -- the Toll (ToS), Curse (CoS), and Fallacy (FoS) of Seeing -- culminating in the Semantic Sufficiency Criterion (SSC). Furthermore, we posit a provocative Divergence Law of Multimodal Scaling, hypothesising that as the underlying language engines scale to unprecedented reasoning capabilities, the mathematical penalty of the visual knowledge bottleneck paradoxically increases. We challenge the KDD community to abandon the illusory pursuit of "multimodal gain". By elevating the SSC from a passive diagnostic constraint to an active architectural blueprint, we provide the rigorous, trustworthy foundation required to force the next generation of AI systems to truly see the data, achieving true multimodal reasoning.

---

## 论文详细总结（自动生成）

这是一份关于论文《The Expense of Seeing: Attaining Trustworthy Multimodal Reasoning Within the Monolithic Paradigm》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：论文挑战了当前视觉语言模型（VLM）能够“忠实合成多模态数据”的假设。作者指出，现有的 VLM 在处理复杂任务时存在**“功能性盲视”**，即模型往往依赖其强大的大语言模型（LLM）背后的语言先验知识进行“统计学猜测”，而非真正理解视觉输入。
*   **研究背景**：当前的评估方法（如“多模态增益” $MG$）通常采用数据消融（删除图像看性能损失）来衡量视觉贡献。作者认为这种方法存在逻辑缺陷：它混淆了数据集本身的偏差与模型架构的表征瓶颈。如果视觉输入在当前架构下是一种“负担”而非“资产”，那么单纯增加模型规模可能只会掩盖问题，而非解决问题。

### 2. 论文提出的方法论
作者提出了一种基于信息论的**“模态转换协议”（Modality Translation Protocol）**，其核心是不删除信息，而是通过改变信息的呈现模态来隔离架构瓶颈。

*   **核心思想**：定义三种评估状态，确保语义负载（Semantic Payload）在不同模态间等价：
    1.  $S_{Full}$：标准 VLM 输入（图像 $V$ + 文本 $T$）。
    2.  $S_{SymT}$（符号化文本上限）：将图像 $V$ 转换为完美的文本描述 $V_{label}$，这是 LLM 推理能力的绝对天花板。
    3.  $S_{SymV}$（符号化视觉）：将文本问题 $T$ 渲染进图像中，强制模型仅通过视觉编码管道读取信息。
*   **关键指标**：
    *   **视觉通行费（ToS, Toll of Seeing）**：$S_{SymT} - S_{Full}$。衡量模型处理视觉模态相对于等价文本时付出的性能代价。
    *   **视觉诅咒（CoS, Curse of Seeing）**：$S_{SymT} - S_{SymV}$。衡量模型在处理不同模态呈现的相同信息时是否存在不对称的惩罚。
    *   **视觉谬误（FoS, Fallacy of Seeing）**：$S_{Full} - S_{SymV}$。用于定位瓶颈：正值表示视觉编码器（如 ViT）分辨率不足；负值表示跨模态投影器（Projector）融合失败。
*   **语义充分性准则（SSC）**：要求上述指标的极大值为 0，作为实现可信多模态推理的数学约束。

### 3. 实验设计
*   **场景与案例研究**：论文并未在传统的大规模 Benchmark 上刷分，而是通过三个高风险领域的**案例研究（Case Studies）**来展示该协议的诊断能力：
    1.  **金融时间序列挖掘**：分析 K 线图预测走势。
    2.  **医疗诊断**：结合胸部 X 光片与临床笔记诊断癌症。
    3.  **药物研发的分子图挖掘**：基于视觉拓扑结构筛选毒性。
*   **对比基准**：论文对比了现有的评估指标（如 $MG$ 多模态增益和 $ML$ 多模态泄漏），并指出了它们的数学缺陷（如无法处理性能退化场景）。

### 4. 资源与算力
*   **算力说明**：文中心**未明确说明**具体的 GPU 型号、数量、训练时长或推理开销。
*   **原因分析**：该论文更偏向于**方法论和理论框架的构建**，旨在为 KDD（知识发现与数据挖掘）社区提供一套新的诊断标准，而非报告特定模型的训练细节。

### 5. 实验数量与充分性
*   **实验规模**：论文主要通过逻辑推导、数学公式定义以及针对性的案例分析来论证。它引用了 BabyVision、MMVP、MMStar 等多个前沿数据集的研究结果作为背景支撑。
*   **充分性与客观性**：
    *   **客观性**：作者提出的 SSC 准则具有数据集无关性（Dataset Agnostic），能够区分“数据集偏差”和“架构缺陷”，这比传统的消融实验更公平。
    *   **局限性**：由于缺乏在主流开源模型（如 LLaVA, Qwen-VL）上的大规模量化实验数据，该框架的普适性仍需进一步的实证支持。

### 6. 主要结论与发现
*   **多模态缩放发散定律（Divergence Law）**：这是一个挑衅性的发现。作者假设，随着 LLM 规模的扩大，其推理上限（$S_{SymT}$）提升速度远快于视觉处理能力（$S_{Full}$），导致“视觉通行费”（ToS）反而随规模增加而扩大。
*   **视觉是架构负债**：在当前“编码器-投影器-LLM”范式下，视觉输入往往成为推理的瓶颈，模型通过走“语言先验”的捷径来掩盖视觉能力的不足。
*   **架构蓝图**：SSC 不应仅作为诊断工具，而应作为损失函数的一部分，强制模型实现模态间的对称性。

### 7. 优点
*   **视角独特**：深刻揭示了 VLM 领域“看似在看，实则在猜”的现状，挑战了盲目扩大模型规模的行业惯性。
*   **数学严谨性**：通过信息论视角重新定义了多模态评估指标，解决了消融实验带来的信息缺失问题。
*   **诊断精准**：FoS 指标能够具体定位故障发生在视觉编码阶段还是模态融合阶段。

### 8. 不足与局限
*   **实操难度**：实施“模态转换协议”需要获取图像的完美符号化文本（$V_{label}$），在现实复杂场景中，这种“Oracle”级别的标注获取成本极高。
*   **缺乏大规模实证**：论文主要停留在理论框架和定律假设层面，缺乏在多个 SOTA 模型上的横向对比数值。
*   **应用限制**：该协议目前更适用于具备明确语义逻辑的任务（如数学、物理、图表分析），对于高度抽象或感性的视觉理解任务（如艺术鉴赏）可能难以定义“等价文本”。

（完）
