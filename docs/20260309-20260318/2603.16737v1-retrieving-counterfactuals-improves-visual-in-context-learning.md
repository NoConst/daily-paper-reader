---
title: Retrieving Counterfactuals Improves Visual In-Context Learning
title_zh: 检索反事实样本提升视觉上下文学习
authors: "Guangzhi Xiong, Sanchit Sinha, Zhenghao He, Aidong Zhang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16737v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 使用组合图像检索（CIR）进行因果学习样本选择
tldr: 针对视觉语言模型在上下文学习中易受虚假相关性干扰的问题，本文提出CIRCLES框架。该方法利用属性引导的组合图像检索技术，主动选取反事实风格的示例作为演示，旨在引导模型理解属性与结果间的因果关系。实验表明，CIRCLES在多个数据集上显著提升了模型（尤其是小规模模型）的推理鲁棒性，有效克服了传统相似度检索带来的局限性。
selection_source: fresh_fetch
motivation: 现有的上下文学习检索方法主要依赖相似度，容易选取具有虚假相关性而非因果关系的示例，限制了模型的推理能力。
method: 提出CIRCLES框架，通过针对性的属性引导组合图像检索技术，主动构建包含反事实示例的演示集以促进因果推理。
result: 在四个数据集上的实验证明，CIRCLES在多种架构上均优于现有方法，尤其在小模型和数据稀缺场景下提升显著。
conclusion: 引入反事实示例能帮助视觉语言模型超越表面相关性，实现更稳健、更具因果性的视觉上下文推理。
---

## 摘要
视觉语言模型 (VLMs) 在广泛的多模态推理任务中取得了显著的性能，但它们往往难以解耦细粒度的视觉属性并推理潜在的因果关系。上下文学习 (ICL) 为 VLMs 适应新任务提供了一条充满前景的途径，但其有效性关键取决于演示示例的选择。现有的检索增强方法通常依赖于基于相似性的被动检索，这往往会选择相关但非因果的示例，从而放大伪相关性并限制模型的鲁棒性。我们引入了 CIRCLES (Composed Image Retrieval for Causal Learning Example Selection)，这是一个通过有针对性的、属性引导的组合图像检索来检索反事实风格示例，从而主动构建演示集的新型框架。通过引入反事实风格的示例，CIRCLES 使 VLMs 能够隐式地推理属性与结果之间的因果关系，超越表面的相关性，并促进更稳健、更有根据的推理。在四个不同数据集上的全面实验表明，CIRCLES 在多种架构上始终优于现有方法，特别是在小规模模型上，在信息稀缺的情况下增益尤为显著。此外，CIRCLES 检索到了更多样化且具有因果信息量的示例，为模型如何利用上下文演示来改进推理提供了定性见解。我们的代码可在 https://github.com/gzxiong/CIRCLES 获取。

## Abstract
Vision-language models (VLMs) have achieved impressive performance across a wide range of multimodal reasoning tasks, but they often struggle to disentangle fine-grained visual attributes and reason about underlying causal relationships. In-context learning (ICL) offers a promising avenue for VLMs to adapt to new tasks, but its effectiveness critically depends on the selection of demonstration examples. Existing retrieval-augmented approaches typically rely on passive similarity-based retrieval, which tends to select correlated but non-causal examples, amplifying spurious associations and limiting model robustness. We introduce CIRCLES (Composed Image Retrieval for Causal Learning Example Selection), a novel framework that actively constructs demonstration sets by retrieving counterfactual-style examples through targeted, attribute-guided composed image retrieval. By incorporating counterfactual-style examples, CIRCLES enables VLMs to implicitly reason about the causal relations between attributes and outcomes, moving beyond superficial correlations and fostering more robust and grounded reasoning. Comprehensive experiments on four diverse datasets demonstrate that CIRCLES consistently outperforms existing methods across multiple architectures, especially on small-scale models, with pronounced gains under information scarcity. Furthermore, CIRCLES retrieves more diverse and causally informative examples, providing qualitative insights into how models leverage in-context demonstrations for improved reasoning. Our code is available at https://github.com/gzxiong/CIRCLES.

---

## 论文详细总结（自动生成）

以下是对论文《Retrieving Counterfactuals Improves Visual In-Context Learning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉上下文学习（Visual ICL）主要依赖基于相似性的检索（如 RICES），即选择与查询图像视觉上最接近的样本。然而，这种“被动检索”容易引入与答案相关但非因果的虚假关联（Spurious Correlations），导致模型在面对细粒度属性敏感的任务或分布偏移时鲁棒性不足。
*   **研究动机**：作者认为，有效的推理需要理解属性与结果之间的因果关系。通过引入“反事实”示例（即改变某个关键属性但保持其他特征相似的对比样本），可以引导视觉语言模型（VLM）隐式地进行因果推理，从而实现更稳健的泛化。

### 2. 方法论（CIRCLES 框架）
CIRCLES（组合图像检索用于因果学习样本选择）的核心思想是主动构建包含“相关性”和“因果性”两部分的演示集。
*   **关键技术流程**：
    1.  **关键属性识别**：利用 VLM 提取查询图像中对回答问题至关重要的属性-值对（如“腹部花纹：条纹”）。
    2.  **反事实描述生成**：提示 VLM 生成反事实标题（Counterfactual Caption），即通过干预（do-operation）改变一个属性值，同时保持其他描述不变。
    3.  **组合图像检索（CIR）**：使用生成的反事实标题和查询图像，在训练库中检索视觉上符合修改后描述的图像。检索评分函数结合了视觉忠实度（图像-标题相似度）和语义相关性（问题-问题相似度）。
    4.  **上下文构建**：将检索到的反事实示例（$R_{causal}$）与标准相似度检索到的示例（$R_{corr}$）合并，作为 VLM 推理的上下文。
*   **公式表达**：检索评分 $S_j = \text{sim}_{img}(I_j, c_{do}) + \text{sim}_{txt}(Q_q, Q_j)$，旨在确保检索到的样本既符合反事实干预，又与原始任务语义相关。

### 3. 实验设计
*   **数据集**：
    *   **细粒度分类**：CUB（鸟类）、Flowers（花卉）。
    *   **视觉问答（VQA）**：OK-VQA（基于知识）、VizWiz（真实世界/盲人辅助）。
*   **基准模型（Backbones）**：Gemma3 (4B/12B)、Qwen2.5-VL (3B/7B)。
*   **对比方法**：
    *   **None**：零样本（Zero-shot）。
    *   **Random**：随机选择示例。
    *   **RICES**：基于 CLIP 图像相似度的标准检索。
    *   **MUIER / MMICES**：现有的多模态相似度检索改进方法。

### 4. 资源与算力
*   **算力说明**：论文提到使用 **vLLM 库**进行推理，并采用 **BFloat16 精度**。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）及数量，也未提及具体的训练/推理总时长。由于该方法是 **Training-free（无需训练）** 的，其主要开销在于推理时的多次 VLM 调用（属性提取和标题生成）。

### 5. 实验数量与充分性
*   **实验规模**：作者在 4 个主流数据集上测试了 4 种不同规模和架构的 VLM，并对比了 5 种基准方法，实验覆盖面较广。
*   **消融与分析**：
    *   **信息稀缺性分析**：模拟训练集样本缺失（0%-75%）的情况，验证了 CIRCLES 在数据稀缺时的鲁棒性。
    *   **预算分配实验**：研究了标准检索样本与反事实样本的最佳比例。
    *   **组件消融**：对比了仅提供文本属性与提供反事实图像的效果。
*   **评价**：实验设计较为充分且客观，通过跨模型、跨任务的对比证明了方法的普适性。

### 6. 主要结论与发现
*   **性能提升**：CIRCLES 在所有数据集和模型上均优于现有检索方法，平均准确率提升显著，尤其是在小规模模型（如 3B/4B）上增益更大。
*   **因果推理**：定性分析显示，反事实示例能清晰展示属性变化如何导致标签变化，帮助模型识别决定性特征。
*   **鲁棒性**：在训练数据稀缺的情况下，CIRCLES 的领先优势扩大，说明反事实样本比单纯的相似样本包含更高质量的信息。

### 7. 优点
*   **即插即用**：无需对 VLM 进行微调，适用于任何现成的视觉语言模型。
*   **因果启发**：将因果干预的思想引入 ICL 检索阶段，从根本上缓解了视觉任务中的虚假相关性问题。
*   **可解释性**：通过检索到的反事实样本，用户可以直观看到模型推理所依据的属性对比。

### 8. 不足与局限
*   **推理延迟**：相比 RICES，CIRCLES 需要额外的 VLM 调用来提取属性和生成标题，增加了推理阶段的计算开销（约 10% 的 Token 开销）。
*   **依赖属性提取质量**：如果 VLM 在第一阶段无法准确识别关键属性，或者 CIR 检索到的图像质量较差，可能会引入噪声。
*   **应用限制**：在问题极其复杂或图像属性极度模糊的场景下（如 VizWiz 部分样本），反事实干预的构建难度较大，提升效果相对有限。

（完）
