---
title: "SPARROW: Learning Spatial Precision and Temporal Referential Consistency in Pixel-Grounded Video MLLMs"
title_zh: SPARROW：在像素级定位视频多模态大语言模型中学习空间精度与时间指代一致性
authors: "Mohamad Alansari, Naufal Suryanto, Divya Velayudhan, Sajid Javed, Naoufel Werghi, Muzammal Naseer"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12382v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 像素级视频多模态大模型架构与时间一致性
tldr: "SPARROW 针对视频多模态大模型在像素级定位中存在的空间漂移和时间不一致问题，提出了结合目标特定追踪特征（TSF）与双提示设计（[BOX] 和 [SEG]）的新框架。通过引入 3 万余条视频参考数据集和端到端 SAM2 提议器，SPARROW 在多个基准测试中显著提升了定位精度和时间稳定性，为视频理解提供了更强的时空一致性。"
selection_source: fresh_fetch
motivation: 现有视频 MLLM 依赖静态分割标记，在处理物体移动或重现时容易出现空间漂移和身份切换等不稳定性问题。
method: "引入目标特定追踪特征（TSF）以增强时间对齐，并采用 [BOX] 与 [SEG] 双提示设计来融合几何先验与语义分割。"
result: "在 RVOS、视觉定位和 GCG 等六个基准测试中取得显著提升，最高改善了 8.9 J&F 和 5.4 CLAIR 分数。"
conclusion: SPARROW 证明了通过结合时空追踪特征与双重几何语义提示，可以大幅提升像素级视频理解的参考稳定性和空间精度。
---

## 摘要
多模态大语言模型（MLLMs）已从图像级推理演进到像素级定位（grounding），但将这些能力扩展到视频领域仍具挑战性，因为模型必须实现空间精度和时间一致的指代追踪。现有的视频 MLLMs 通常依赖静态分割标记（[SEG]）进行逐帧定位，这虽然提供了语义信息，但缺乏时间上下文，导致在物体移动或重新出现时出现空间漂移、身份切换和不稳定的初始化。我们提出了 SPARROW，这是一种像素级定位视频 MLLM，通过两个关键组件统一了空间准确性和时间稳定性：(i) 特定目标追踪特征（TSF），在训练期间注入时间对齐的指代线索；(ii) 双提示设计，通过解码框（[BOX]）和分割（[SEG]）标记，将几何先验与语义定位相融合。SPARROW 得到了一个包含 30,646 个视频和 45,231 个问答对的精选指代视频数据集的支持，并通过基于 SAM2 的类别无关提议器（proposer）实现端到端运行，无需外部检测器。通过集成到三个近期的开源视频 MLLMs（UniPixel、GLUS 和 VideoGLaMM）中，SPARROW 在六个基准测试中均取得了持续提升，在 RVOS 上提升高达 +8.9 J&F，在视觉定位上提升 +5 mIoU，在 GCG 上提升 +5.4 CLAIR。这些结果表明，SPARROW 显著提高了像素级定位视频理解中的指代稳定性、空间精度和时间连贯性。项目主页：https://risys-lab.github.io/SPARROW

## Abstract
Multimodal large language models (MLLMs) have advanced from image-level reasoning to pixel-level grounding, but extending these capabilities to videos remains challenging as models must achieve spatial precision and temporally consistent reference tracking. Existing video MLLMs often rely on a static segmentation token ([SEG]) for frame-wise grounding, which provides semantics but lacks temporal context, causing spatial drift, identity switches, and unstable initialization when objects move or reappear. We introduce SPARROW, a pixel-grounded video MLLM that unifies spatial accuracy and temporal stability through two key components: (i) Target-Specific Tracked Features (TSF), which inject temporally aligned referent cues during training, and (ii) a dual-prompt design that decodes box ([BOX]) and segmentation ([SEG]) tokens to fuse geometric priors with semantic grounding. SPARROW is supported by a curated referential video dataset of 30,646 videos and 45,231 Q&A pairs and operates end-to-end without external detectors via a class-agnostic SAM2-based proposer. Integrated into three recent open-source video MLLMs (UniPixel, GLUS, and VideoGLaMM), SPARROW delivers consistent gains across six benchmarks, improving up to +8.9 J&F on RVOS, +5 mIoU on visual grounding, and +5.4 CLAIR on GCG. These results demonstrate that SPARROW substantially improves referential stability, spatial precision, and temporal coherence in pixel-grounded video understanding. Project page: https://risys-lab.github.io/SPARROW

---

## 论文详细总结（自动生成）

### SPARROW：在像素级定位视频多模态大语言模型中学习空间精度与时间指代一致性

#### 1. 核心问题与整体含义（研究动机和背景）
现有的像素级定位视频多模态大语言模型（Video MLLMs）在处理视频时面临两大挑战：**空间漂移（Spatial Drift）**和**时间指代不一致（Temporal Incoherence）**。
*   **动机：** 现有模型多依赖静态的 `[SEG]` 标记进行逐帧分割，缺乏时间上下文，导致在物体移动、遮挡或重现时容易丢失目标身份（身份切换）或初始化失败。
*   **核心目标：** 提出 SPARROW 框架，旨在通过增强时间稳定性和空间初始化精度，实现更稳健的视频目标分割与指代理解。

#### 2. 方法论：核心思想与技术细节
SPARROW 通过两个互补组件增强了基准视频 MLLM：
*   **目标特定追踪特征（Target-Specific Tracked Features, TSF）：**
    *   **核心思想：** 在训练阶段注入与时间对齐的指代线索，使模型学习跨帧的身份持久性。
    *   **流程：** 利用 GroundingDINO 检测和 CLDTracker 追踪生成候选框，通过 K-means 聚类筛选出代表性外观特征，将其投影为 TSF 标记输入 LLM。
*   **双提示定位设计（Dual-Prompt Grounding）：**
    *   **核心思想：** 结合 `[BOX]`（几何先验）和 `[SEG]`（语义分割）标记，实现从粗到精的推理。
    *   **技术细节：** 
        *   基于 SAM2 的 Hiera 特征构建一个类别无关的提议器（Proposer），使用 Deformable-DETR 解码器生成候选框。
        *   `[BOX]` 标记用于过滤和精细化候选框，提供空间约束。
        *   `[SEG]` 标记引导 SAM2 解码器生成最终的精确掩码。
*   **端到端运行：** 推理时默认无需外部检测器或追踪器，保持了模型的简洁性。

#### 3. 实验设计
*   **数据集：** 涵盖了 6 个主流基准测试，包括：
    *   **RVOS（指代视频对象分割）：** MeViS (val/val u), Ref-YouTube-VOS, Ref-DAVIS17。
    *   **VG（视频视觉定位）：** VidSTG。
    *   **GCG（视频定位对话生成）：** VideoGCG。
*   **基准模型（Baselines）：** 集成到了三个最先进的开源模型中：**UniPixel** (NeurIPS'25), **GLUS** (CVPR'25), 和 **VideoGLaMM** (CVPR'24)。
*   **对比方法：** 与上述基准模型的原始版本进行直接对比，验证 SPARROW 插件的增益。

#### 4. 资源与算力
论文详细列出了训练成本：
*   **硬件：** 使用 NVIDIA **A100** GPU。
*   **训练时长：**
    *   **阶段 1（TSF 注入）：** 8 张 A100 运行 12 小时（约 96 GPU 小时）。
    *   **阶段 2（双提示学习）：** 8 张 A100 运行 2 小时（约 16 GPU 小时）。
    *   **总计：** 约 112 GPU 小时。
*   **离线预处理：** 离线生成 TSF 数据集耗时约 51.9 小时（单张 A100）。

#### 5. 实验数量与充分性
*   **实验规模：** 论文构建了一个包含 **30,646 个视频**和 **45,231 个问答对**的大规模精选数据集进行训练。
*   **消融实验：** 进行了详尽的消融研究，包括 TSF 的使用模式（仅训练 vs 训练+推理）、提示词组合（仅 Box, 仅 Seg, Box+Seg）、提议器架构对比、以及对噪声监督的鲁棒性测试。
*   **充分性评价：** 实验覆盖了从短视频到长视频、从单一目标到多目标、从简单动作到复杂运动描述的多种场景，对比实验和消融实验设计严谨，结果具有统计意义上的显著性。

#### 6 主要结论与发现
*   **性能提升：** SPARROW 在所有基准上均实现显著增长。在 MeViS 上提升高达 **+8.9 J&F**，在 VidSTG 上提升 **+5 mIoU**，在 GCG 任务中 CLAIR 分数提升 **+5.4**。
*   **稳定性：** TSF 机制显著减少了身份切换，而双提示设计有效缓解了第一帧初始化不准导致的误差累积。
*   **通用性：** 作为一个模块化插件，SPARROW 能无缝集成到不同的视频 MLLM 架构中并提供稳定的性能增益。

#### 7. 优点
*   **模块化设计：** 插件式架构，无需修改 LLM 或视觉编码器的主干网络。
*   **时空结合：** 巧妙地通过 TSF 解决时间一致性，通过双提示解决空间精度，双管齐下。
*   **推理高效：** 默认推理模式下不依赖外部工具，且引入的额外参数量（约 17M）和计算延迟极低。
*   **高质量数据：** 贡献了一个大规模、高质量的指代视频数据集，对社区具有参考价值。

#### 8. 不足与局限
*   **提议器依赖：** 性能受限于类别无关提议器的召回率。如果提议器未能捕捉到微小或严重遮挡的物体，后续的 `[BOX]` 和 `[SEG]` 修正也无法奏效。
*   **长序列挑战：** 尽管缓解了漂移，但在极长视频序列中，几何误差仍可能缓慢累积。
*   **伪标签噪声：** TSF 依赖离线生成的伪追踪轨迹，虽然实验证明了其鲁棒性，但极端的追踪错误仍可能对训练产生负面影响。

（完）
