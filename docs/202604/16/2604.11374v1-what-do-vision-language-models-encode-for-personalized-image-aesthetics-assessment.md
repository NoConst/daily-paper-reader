---
title: What Do Vision-Language Models Encode for Personalized Image Aesthetics Assessment?
title_zh: 视觉语言模型在个性化图像美学评估中编码了什么？
authors: "Koki Ryu, Hitomi Yanaka"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11374v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 视觉语言模型的个人级个性化
tldr: 本研究探讨了视觉语言模型（VLM）在个性化图像美学评估（PIAA）中的内部表征。通过分析发现，VLM内部层编码了丰富的多层级美学属性，并能传播至语言解码层。基于此，作者提出了一种无需微调的轻量化方法，利用简单的线性模型即可实现有效的个性化美学预测。该研究揭示了VLM在处理主观审美偏好方面的潜力，并深入分析了不同架构和领域下的信息传递机制。
selection_source: fresh_fetch
motivation: 旨在探究视觉语言模型内部是否编码了足以支持个性化图像美学评估的多层级审美属性。
method: 通过分析VLM内部表示的审美属性分布，并基于这些特征构建无需微调的轻量级线性模型进行个性化预测。
result: 研究发现VLM内部层蕴含多样化的美学信息，且仅需简单的线性模型即可在PIAA任务中取得良好效果。
conclusion: VLM内部丰富的审美表征使其成为建模个体主观审美偏好的有效工具，为实现轻量化个性化评估提供了新见解。
---

## 摘要
个性化图像美学评估（PIAA）是一个具有实际应用价值的重要研究问题。虽然基于视觉语言模型（VLM）的方法是 PIAA 的有力候选方案，但目前尚不清楚这些模型内部是否编码了有效实现个性化所需的丰富、多层级的美学属性。在本文中，我们首先分析了 VLM 的内部表示，以检查这些美学属性的存在和分布，然后利用这些表示在无需模型微调的情况下实现轻量级的个体级个性化。我们的分析表明，VLM 编码了多种美学属性，并传播到了语言解码器层。基于这些表示，我们证明了简单的线性模型即可有效地执行 PIAA。我们进一步分析了美学信息如何在不同 VLM 架构的各层之间以及不同图像领域之间进行传递。我们的研究结果为如何利用 VLM 建模主观的、个体的美学偏好提供了见解。我们的代码可在 https://github.com/ynklab/vlm-latent-piaa 获取。

## Abstract
Personalized image aesthetics assessment (PIAA) is an important research problem with practical real-world applications. While methods based on vision-language models (VLMs) are promising candidates for PIAA, it remains unclear whether they internally encode rich, multi-level aesthetic attributes required for effective personalization. In this paper, we first analyze the internal representations of VLMs to examine the presence and distribution of such aesthetic attributes, and then leverage them for lightweight, individual-level personalization without model fine-tuning. Our analysis reveals that VLMs encode diverse aesthetic attributes that propagate into the language decoder layers. Building on these representations, we demonstrate that simple linear models can perform PIAA effectively. We further analyze how aesthetic information is transferred across layers in different VLM architectures and across image domains. Our findings provide insights into how VLMs can be utilized for modeling subjective, individual aesthetic preferences. Our code is available at https://github.com/ynklab/vlm-latent-piaa.

---

## 论文详细总结（自动生成）

这篇论文对视觉语言模型（VLM）在个性化图像美学评估（PIAA）中的内部机制进行了深入探讨。以下是该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：图像美学评估具有高度的主观性，不同用户对同一张照片的审美偏好差异巨大。虽然视觉语言模型（VLM）在通用美学评估（GIAA）中表现出色，但它们是否内部编码了足以支持“个体级”个性化偏好的多层级美学属性（如光影、色彩、构图等）尚不明确。
*   **核心问题**：VLM 的内部表征中是否蕴含丰富的美学特征？能否利用这些特征在不进行大规模模型微调的情况下，实现轻量化且精准的个性化美学预测？

### 2. 方法论
*   **核心思想**：通过“线性探测（Linear Probing）”分析 VLM 各层表征对美学属性的编码能力，并基于此提出一种基于隐藏表征的线性回归方法（Linear-Hidden）来实现 PIAA。
*   **关键技术细节**：
    *   **特征提取**：从 VLM 的视觉编码器（V）、语言解码器的文本标记（LT）和视觉标记（LV）中提取各层的隐藏向量。
    *   **线性探测**：使用岭回归（Ridge Regression）将隐藏向量映射到 11 种预定义的美学属性（如色彩和谐、景深、光影等），验证特征的存在性。
    *   **个性化预测**：针对每个目标用户，利用其少量的标注数据（如 100 张图），训练一个用户特有的线性回归器 $M_u$，将 VLM 的隐藏表征直接映射为该用户的个性化评分。
    *   **无需微调**：整个过程保持 VLM 参数冻结，仅训练极轻量级的线性层。

### 3. 实验设计
*   **数据集**：
    *   **AADB**：用于美学属性探测（包含 11 种属性标注）。
    *   **PARA**：摄影作品数据集，用于照片领域的 PIAA 评估。
    *   **LAPIS**：艺术作品数据集，用于跨领域的艺术美学 PIAA 评估。
*   **对比方法（Baselines）**：
    *   **文本输出类**：Raw Text（直接询问评分）、Few-shot（上下文学习）、LoRA（参数高效微调）。
    *   **领域专用模型**：PIAA-ICI（专门针对 PIAA 设计的 SOTA 模型）。
    *   **变体分析**：Linear-Hidden (GIAA)（仅预测通用分）、Linear-Hidden (Reduce)（仅使用探测到的 11 维属性空间）。
*   **评估指标**：用户平均的 Spearman 相关系数（$\rho$）和决定系数（$R^2$）。

### 4. 资源与算力
*   **硬件环境**：单节点，配备 1 张 **NVIDIA H100 GPU**。
*   **训练时长**：
    *   Few-shot 和 LoRA 基准测试约需 **24 GPU 小时**。
    *   线性探测和 Linear-Hidden 预测任务约需 **8 GPU 小时**。
*   **模型规模**：测试了 Qwen3-VL (2B, 4B, 8B) 和 Gemma 3 (4B, 12B)。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 种主流 VLM 架构、5 种模型尺寸、3 个核心数据集。
*   **充分性分析**：
    *   **多维度验证**：不仅做了性能对比，还进行了层级分析（Layer-wise）、消融实验（不同表征组件对比）。
    *   **鲁棒性测试**：通过图像增强（灰度化、几何畸变）验证属性编码的独立性；测试了不同 Prompt 对结果的影响。
    *   **统计显著性**：使用了 2000 次自助法（Bootstrap）重采样进行统计验证，确保结论客观公平。

### 6. 主要结论与发现
*   **美学编码的存在性**：VLM 的隐藏层（尤其是语言解码器层）确实编码了丰富的、可线性提取的多层级美学属性。
*   **线性预测的优越性**：简单的线性回归（Linear-Hidden）在 PIAA 任务上显著优于基于文本输出的方法（如 Few-shot 或 LoRA），尤其是在数据稀缺的情况下。
*   **架构差异**：不同架构处理美学信息的方式不同。例如，Gemma 3 更多在解码器中整合信息，而 Qwen3-VL 由于其 DeepStack 架构，在视觉端保留了更多特征。
*   **领域鲁棒性**：相比于在特定数据集上训练的 PIAA 模型，VLM 的表征在跨领域（从照片到艺术品）迁移时表现出更强的鲁棒性。

### 7. 优点
*   **轻量高效**：提出了一种无需微调大模型即可实现高性能个性化的方案，极大地降低了部署成本。
*   **深入的解释性**：通过层级探测揭示了美学信息在 VLM 内部的流动和分布规律。
*   **跨领域洞察**：揭示了照片和艺术品在审美特征编码上的差异（艺术品更依赖视觉标记中的潜在信息）。

### 8. 不足与局限
*   **属性覆盖有限**：探测所用的 11 种属性无法涵盖人类审美的所有维度。
*   **因果关系不明确**：虽然证明了信息“存在”且“可提取”，但尚未证明模型在生成文本回复时是否真的“使用”了这些线性特征。
*   **风格迁移限制**：实验发现，当在截然不同的艺术风格（如抽象派 vs. 具象派）之间迁移时，性能仍会出现明显下降。
*   **应用风险**：个性化可能加剧算法偏见，论文指出在实际部署前需进行更广泛的公平性分析。

（完）
