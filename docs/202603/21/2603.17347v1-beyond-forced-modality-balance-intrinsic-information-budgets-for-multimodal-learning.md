---
title: "Beyond Forced Modality Balance: Intrinsic Information Budgets for Multimodal Learning"
title_zh: 超越强制模态平衡：多模态学习的内在信息预算
authors: "Zechang Xiong, Da Li, Kexin Tang, Pengyuan Li, Wenkang Kong, Yulan Hu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17347v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态学习基础与模态不平衡
tldr: 针对多模态学习中主导模态抑制弱势模态导致的性能不佳问题，本文提出IIBalance框架。该框架不再盲目强制模态平衡，而是引入“内在信息预算（IIB）”概念，通过评估各模态的信息容量来指导贡献分配。利用原型对齐机制纠正语义漂移，并结合概率门控模块实现校准融合，有效提升了多模态信息的利用率。
selection_source: fresh_fetch
motivation: 现有方法通过重权平衡模态，却忽略了各模态本身具有不同的有限信息容量，导致强制平衡效果不佳。
method: 提出IIBalance框架，利用任务相关的内在信息预算作为先验，结合原型相对对齐机制和概率门控模块来优化模态贡献。
result: 在三个代表性基准测试上的实验表明，IIBalance在性能上持续优于现有的最先进平衡方法。
conclusion: 通过对齐模态贡献与其实际信息预算，可以更有效地利用互补的多模态线索，解决模态失衡问题。
---

## 摘要
多模态模型通常会收敛到主导模态解，其中更强、收敛更快的模态会掩盖较弱的模态。这种模态不平衡导致了次优的性能。现有方法试图通过重新加权梯度或损失来平衡不同的模态，然而，它们忽略了每个模态都具有有限信息容量这一事实。在这项工作中，我们提出了 IIBalance，这是一个将模态贡献与内在信息预算（Intrinsic Information Budgets, IIB）对齐的多模态学习框架。我们为每个模态提出了一种基于任务的 IIB 估计器，将其容量转化为模态贡献的全局先验。以预算最高的模态为锚点，我们设计了一种基于原型的相对对齐机制，仅在较弱模态偏离其预算潜力时纠正语义漂移，而不是强制模仿。在推理过程中，我们提出了一个概率门控模块，该模块结合了全局预算与样本级不确定性，以生成校准后的融合权重。在三个代表性基准测试上的实验表明，IIBalance 始终优于最先进的平衡方法，并实现了对互补模态线索的更好利用。我们的代码可在以下网址获取：https://github.com/XiongZechang/IIBalance。

## Abstract
Multimodal models often converge to a dominant-modality solution, in which a stronger, faster-converging modality overshadows weaker ones. This modality imbalance causes suboptimal performance. Existing methods attempt to balance different modalities by reweighting gradients or losses. However, they overlook the fact that each modality has finite information capacity. In this work, we propose IIBalance, a multimodal learning framework that aligns the modality contributions with Intrinsic Information Budgets (IIB). We propose a task-grounded estimator of each modality's IIB, transforming its capacity into a global prior over modality contributions. Anchored by the highest-budget modality, we design a prototype-based relative alignment mechanism that corrects semantic drift only when weaker modalities deviate from their budgeted potential, rather than forcing imitation. During inference, we propose a probabilistic gating module that integrates the global budgets with sample-level uncertainty to generate calibrated fusion weights. Experiments on three representative benchmarks demonstrate that IIBalance consistently outperforms state-of-the-art balancing methods and achieves better utilization of complementary modality cues. Our code is available at: https://github.com/XiongZechang/IIBalance.

---

## 论文详细总结（自动生成）

以下是对论文《Beyond Forced Modality Balance: Intrinsic Information Budgets for Multimodal Learning》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态学习中普遍存在“模态不平衡”现象，即性能强、收敛快的模态（主导模态）会抑制弱势模态的学习，导致模型过度依赖单一信号，缺乏对互补信息的利用。
*   **研究动机**：
    *   **现有方法的缺陷**：目前的平衡策略（如梯度调节、损失重权）大多追求“绝对平衡”，即强制各模态贡献相等。
    *   **内在差异性**：作者指出，不同模态携带的任务相关信息量本质上是不平等的（例如视觉通常比音频包含更多细节）。强制弱势模态去匹配强势模态的贡献，会导致弱势模态去拟合残差噪声，产生过拟合。
    *   **核心主张**：应从“绝对平衡”转向“相对平衡”，即根据模态各自的**内在信息预算（Intrinsic Information Budget, IIB）**来分配其贡献权重。

### 2. 方法论
该论文提出了 **IIBalance** 框架，包含三个关键步骤：
*   **内在信息预算（IIB）估计**：
    *   首先独立训练各模态的单模态分类器。
    *   利用预测分布的**负熵**作为信号置信度的代理指标，计算全数据集的平均负熵，以此量化该模态的内在任务相关容量。
    *   通过 Softmax 归一化得到全局先验权重 $\beta$，作为各模态的“预算”。
*   **阶段 I：原型引导的相对对齐（PRA）**：
    *   以预算最高的模态作为“锚点模态”，通过指数移动平均（EMA）维护其类原型。
    *   设计原型对比损失，将弱势模态的特征拉向锚点模态的类原型。
    *   **对齐强度控制**：对齐的力度由预算差距（锚点预算 - 当前模态预算）决定，预算越低的模态受到的对齐压力越大，从而纠正语义漂移而非盲目模仿。
*   **阶段 II：不确定性感知的贝叶斯融合**：
    *   在推理/融合阶段，结合全局先验（IIB）和样本级信号。
    *   **融合权重计算**：权重 = 全局预算先验 $\times$ 样本预测置信度（似然代理） $\times$ 学习到的校准因子。
    *   这种设计确保了融合权重既遵循全局容量限制，又能根据样本的具体质量（如视觉遮挡或音频噪声）动态调整。

### 3. 实验设计
*   **数据集**：
    *   **Kinetics-Sounds**：大规模音视频动作识别。
    *   **CREMA-D**：音视频情感识别。
    *   **AVE**：音视频事件定位。
*   **Benchmark 与对比方法**：
    *   对比了 9 种主流方法，包括：
        *   梯度/优化调节类：OGM-GE, MSLR, G-Blending, AGM, GGDM。
        *   原型/重平衡类：PMR。
        *   多目标/辅助学习类：MMPareto, Greedy Learning。
*   **评价指标**：多模态准确率（Acc m）以及单模态分支的准确率（Acc a, Acc v）。

### 4. 资源与算力
*   **模型架构**：视觉骨干网络采用 ResNet-18 或 MobileNetV2，音频采用 1D CNN 或 RNN。
*   **训练细节**：使用 Adam 优化器，学习率 $1 \times 10^{-4}$，Batch Size 为 32，训练 50 个 Epoch。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量及总训练时长。但从模型规模（ResNet-18）和数据集大小来看，属于中等算力需求。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同性质的基准数据集上进行了完整对比。
*   **消融实验**：针对 IIB 先验、阶段 I（PRA）和阶段 II（融合机制）分别进行了消融，验证了各模块的必要性。
*   **敏感性分析**：对超参数（温度系数 $\tau$、损失权重 $\gamma$）进行了敏感性测试。
*   **客观性评价**：实验设计较为充分，通过对比单模态准确率的变化，客观证明了该方法在提升弱势模态的同时没有损害强势模态，验证了“相对平衡”的有效性。

### 6. 主要结论与发现
*   **性能领先**：IIBalance 在所有三个数据集上均达到了 SOTA 性能，尤其在弱势模态（通常是视频或音频中的某一方）的准确率上有显著提升。
*   **预算的指导意义**：实验证明，学习到的平均融合权重与预先估计的 IIB 先验高度一致，说明 IIB 确实捕捉到了模态的内在容量。
*   **避免过拟合**：相比于强制平衡方法，IIBalance 通过限制弱势模态的“任务量”，有效防止了其对噪声的过度拟合。

### 7. 优点
*   **理论视角新颖**：打破了“模态必须对等”的思维定式，提出了基于容量预算的相对平衡理念。
*   **两阶段解耦**：将特征层的对齐（Stage I）与决策层的融合（Stage II）解耦，既保证了特征的语义一致性，又保留了融合的灵活性。
*   **贝叶斯启发**：融合机制结合了全局先验与局部似然，具有较好的统计学解释性。

### 8. 不足与局限
*   **离线依赖**：IIB 的估计依赖于预先训练的单模态模型，增加了训练流程的复杂度和前期开销。
*   **模态局限性**：实验主要集中在音视频（Audio-Visual）领域，尚未在文本-图像或传感器融合等更多元的多模态场景下验证。
*   **静态预算**：目前的 IIB 是在数据集层面静态估计的，虽然融合阶段有动态调整，但预算先验本身若能随训练过程在线更新可能会更具鲁棒性。

（完）
