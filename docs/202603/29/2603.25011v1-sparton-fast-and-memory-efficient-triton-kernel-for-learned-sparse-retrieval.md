---
title: "Sparton: Fast and Memory-Efficient Triton Kernel for Learned Sparse Retrieval"
title_zh: Sparton：用于学习型稀疏检索的高效、内存节省型 Triton 内核
authors: "Thong Nguyen, Cosimo Rulli, Franco Maria Nardini, Rossano Venturini, Andrew Yates"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25011v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 快速且内存高效的稀疏检索内核
tldr: 针对学习型稀疏检索（LSR）模型中语言模型（LM）头带来的巨大内存瓶颈和I/O开销，本文提出了Sparton。这是一个基于Triton的高效GPU算子，通过将矩阵乘法、激活函数和最大池化等操作融合，并采用在线归约技术，避免了中间大矩阵的显式存储。实验表明，Sparton显著降低了内存占用并提升了训练速度，特别是在大词表场景下表现优异。
selection_source: fresh_fetch
motivation: LSR模型中庞大的词表导致LM头产生巨大的中间矩阵，造成严重的内存瓶颈和计算效率低下。
method: 提出Sparton算子，利用Triton将矩阵乘法、ReLU、Log1P和最大池化融合，通过对原始逻辑值分块进行早期在线归约来减少内存占用。
result: 相比PyTorch基准，Sparton实现高达4.8倍的算子加速，并在大词表模型中将训练速度提升2.5倍，支持的批次大小增加26倍。
conclusion: Sparton通过算子融合和内存优化，有效解决了LSR模型扩展的性能瓶颈，是提升大规模稀疏检索模型效率的有力工具。
---

## 摘要
最先进的学习型稀疏检索（LSR）模型（如 Splade）通常采用语言建模（LM）头，将潜在隐状态投影到以词表为锚点的 logit 矩阵中。该中间矩阵随后通过逐元素操作（ReLU、Log1P）和在序列维度上的最大池化（max-pooling）转换为稀疏词汇表示。尽管其效果显著，但由于词表（V）的巨大规模（在近期模型中可达 30,000 到超过 250,000 个 token），LM 头造成了巨大的内存瓶颈。实例化该矩阵会产生显著的内存瓶颈，限制了模型的扩展。算子之间产生的 I/O 开销进一步限制了吞吐量和运行时性能。在本文中，我们提出了 Sparton，这是一种专为 LSR 模型中的 LM 头量身定制的高效、内存节省型 Triton 内核。Sparton 采用融合方法，将分块矩阵乘法、ReLU、Log1P 和最大值归约（max-reduction）集成到单个 GPU 内核中。通过直接在原始 logit 分块上执行早期在线归约，Sparton 避免了在内存中实例化完整的 logit 矩阵。我们的实验表明，与 PyTorch 基准相比，Sparton 内核在独立运行时可实现高达 4.8 倍的加速，并将峰值内存占用降低一个数量级。集成到 Splade（|V| ~ 30k）后，Sparton 在不损失效果的情况下，使 batch size 增加了 33%，训练速度提升了 14%。在多语言骨干网络（|V| ~ 250k）上，这些增益跃升至 26 倍的 batch size 和 2.5 倍的训练加速。

## Abstract
State-of-the-art Learned Sparse Retrieval (LSR) models, such as Splade, typically employ a Language Modeling (LM) head to project latent hidden states into a lexically-anchored logit matrix. This intermediate matrix is subsequently transformed into a sparse lexical representation through element-wise operations (ReLU, Log1P) and max-pooling over the sequence dimension. Despite its effectiveness, the LM head creates a massive memory bottleneck due to the sheer size of the vocabulary (V), which can range from 30,000 to over 250,000 tokens in recent models. Materializing this matrix creates a significant memory bottleneck, limiting model scaling. The resulting I/O overhead between operators further throttles throughput and runtime performance. In this paper, we propose Sparton, a fast memory-efficient Triton kernel tailored for the LM head in LSR models. Sparton utilizes a fused approach that integrates the tiled matrix multiplication, ReLU, Log1P, and max-reduction into a single GPU kernel. By performing an early online reduction directly on raw logit tiles, Sparton avoids materializing the full logit matrix in memory. Our experiments demonstrate that the Sparton kernel, in isolation, achieves up to a 4.8x speedup and an order-of-magnitude reduction in peak memory usage compared to PyTorch baselines. Integrated into Splade (|V| ~ 30k), Sparton enables a 33% larger batch size and 14% faster training with no effectiveness loss. On a multilingual backbone (|V| ~ 250k), these gains jump to a 26x larger batch size and 2.5x faster training.

---

## 论文详细总结（自动生成）

以下是对论文《Sparton: Fast and Memory-Efficient Triton Kernel for Learned Sparse Retrieval》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：学习型稀疏检索（LSR）模型（如 SPLADE）在处理大规模词表时表现优异，但其**语言模型头（LM Head）**存在严重的性能瓶颈。
*   **核心问题**：LM Head 需要将隐状态投影到巨大的词表空间（$V$），生成一个维度为 $Batch \times Sequence \times |V|$ 的中间 logit 矩阵。在标准实现中，这个矩阵必须完整存储在显存（HBM）中，导致极大的内存压力（显存瓶颈）和频繁的 I/O 数据交换（计算瓶颈），限制了模型在大 batch size、长序列或大词表场景下的扩展。

### 2. 方法论
*   **核心思想**：利用 **Triton** 编写自定义 GPU 内核，实现**算子融合（Operator Fusion）**和**在线归约（Online Reduction）**，避免在显存中实例化完整的中间 logit 矩阵。
*   **关键技术细节**：
    *   **单调性重排**：利用激活函数（ReLU, Log1p）的单调递增特性，将“最大池化（Max-pooling）”操作提前到激活函数之前。公式逻辑从 `Log1p(ReLU(max(L)))` 变为在原始 logit 上直接进行归约。
    *   **前向传播（Forward Pass）**：采用分块（Tiling）策略。利用高效的矩阵乘法库（cuBLAS/rocBLAS）计算 logit 分块，随后立即在 Triton 内核中进行掩码最大值归约。显存中仅存储归约后的结果（$Batch \times |V|$）和对应的索引（Argmax），数据量减少了 $S$（序列长度）倍。
    *   **反向传播（Backward Pass）**：利用前向传播存储的最大值索引，仅对贡献了最大值的隐状态和嵌入向量计算梯度，消除了自动微分（Autograd）对中间大矩阵的依赖。

### 3. 实验设计
*   **实验场景**：
    1.  **算子级测试**：对比 Sparton 内核与 PyTorch 原生（Eager/Compiled）实现在不同参数下的性能。
    2.  **端到端训练**：在 SPLADE-V3 模型上进行完整训练测试。
*   **Benchmark 与对比方法**：
    *   **基准**：PyTorch Eager 模式、PyTorch 2.0+ 编译模式（`torch.compile`）。
    *   **数据集**：使用 Mistral-Splade 数据进行训练，在 BEIR 检索基准测试集（Arguana, FiQA, NFCorpus, SciDocs, SciFact）上评估 NDCG@10。
    *   **模型架构**：BERT-based（词表约 30k）和多语言 XLM-RoBERTa（词表约 250k）。

### 4. 资源与算力
*   **硬件平台**：使用了 NVIDIA **A100 (40GB)**、**H100** 和 **H200** GPU。
*   **训练时长**：在 H200 上，Sparton 训练 SPLADE-V3 约需 **12.24 小时**，而编译后的 PyTorch 基准需 14.24 小时。在多语言场景下，Sparton 将训练时间从 67 小时缩短至 25 小时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从微观算子性能（随 Batch Size、序列长度、词表大小的变化曲线）到宏观端到端训练效果的全面评估。
*   **充分性**：实验设计较为充分。作者不仅验证了速度和内存的提升，还通过检索准确度（NDCG@10）实验证明了 Sparton 在数学上与原生实现等价，确保了优化的无损性。对比了不同 GPU 架构（A100/H100/H200），增强了结论的普适性。

### 6. 主要结论与发现
*   **性能提升**：Sparton 内核相比 PyTorch 编译模式实现高达 **4.8 倍**的加速，峰值内存占用降低了一个数量级。
*   **扩展性**：在多语言模型（大词表）中，Sparton 支持的 batch size 提升了 **26 倍**，训练速度提升 **2.5 倍**。
*   **长序列支持**：Sparton 能够处理长度达 **8192** 的序列，而原生实现在序列长度达到 4096 时即发生显存溢出（OOM）。
*   **训练增益**：由于支持更大的 batch size，Sparton 训练出的模型在检索效果上略优于或等同于基准模型。

### 7. 优点
*   **针对性强**：精准捕捉到了 LSR 模型中 LM Head 这一特定瓶颈。
*   **数学巧妙**：利用算子单调性进行计算顺序重排，极大地减少了需要存储的激活值。
*   **工程落地**：基于 Triton 实现，既保证了高性能，又比纯 CUDA 具有更好的可维护性，且已开源。

### 8. 不足与局限
*   **硬件依赖**：虽然 Triton 具有一定的跨平台性，但目前的优化主要针对 NVIDIA GPU，在其他加速器（如华为昇腾、寒武纪等）上的表现未经验证。
*   **算子局限**：该优化高度依赖于“最大池化”这一特定操作，如果 LSR 模型未来转向平均池化或其他非单调聚合方式，该内核将不再适用。
*   **精度探索不足**：论文主要讨论了半精度（Half-precision），未深入探讨 FP8 或更低精度格式对该融合内核的影响。

（完）
