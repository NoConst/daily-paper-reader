---
title: "KARMA: Knowledge-Action Regularized Multimodal Alignment for Personalized Search at Taobao"
title_zh: KARMA：面向淘宝个性化搜索的知识-行为正则化多模态对齐
authors: "Zhi Sun, Wenming Zhang, Yi Wei, Liren Yu, Zhixuan Zhang, Dan Ou, Haihong Tang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22779v2"
tags: ["query:cir-ml"]
score: 9.0
evidence: 针对个性化搜索和大语言模型多模态对齐进行微调
tldr: "针对淘宝个性化搜索中大语言模型（LLM）微调导致的“知识-行动差距”和语义坍缩问题，本文提出KARMA框架。该框架通过引入语义重构作为训练正则项，在优化检索嵌入的同时，利用历史条件生成和嵌入条件重构来保持LLM的语义能力。实验证明，KARMA有效缓解了语义坍缩，显著提升了淘宝搜索的召回、粗排和精排性能，并带来了0.9%的GMV增长。"
selection_source: fresh_fetch
motivation: 直接在个性化任务上微调LLM会导致预训练语义知识与特定行动目标冲突，引发语义坍缩并限制泛化能力。
method: 提出KARMA框架，将语义重构作为正则化项，通过历史条件生成和嵌入条件重构确保兴趣嵌入具备语义可恢复性。
result: "在淘宝搜索系统中，KARMA显著提升了各阶段的点击率和命中率指标，并实现了0.9%的在线GMV增长。"
conclusion: 通过在训练中平衡语义保持与行动对齐，可以有效解决LLM在工业级个性化推荐任务中的性能瓶颈。
---

## 摘要
大语言模型（LLMs）具备深厚的语义知识，使其成为在个性化搜索系统中注入语义泛化能力的自然选择。然而，在实践中我们发现，在工业级个性化任务（如下一商品预测）上直接微调 LLM 通常效果不佳。我们将这一瓶颈归因于关键的“知识-行为鸿沟”（Knowledge-Action Gap）：即保留预训练语义知识与通过判别性目标对齐特定个性化行为之间的内在冲突。经验表明，仅针对行为的训练目标会导致“语义崩溃”（Semantic Collapse），例如出现注意力“汇聚”（attention sinks）。这种退化严重削弱了 LLM 的泛化能力，导致其无法为个性化搜索系统带来提升。我们提出了 KARMA（知识-行为正则化多模态对齐），这是一个将语义重构视为仅训练期正则化项的统一框架。KARMA 在优化用于检索的下一兴趣嵌入（行为）的同时，通过两个互补目标强制实现语义可解码性（知识）：(i) 基于历史条件的语义生成，将优化锚定在 LLM 原生的下一词元（next-token）分布上；(ii) 基于嵌入条件的语义重构，约束兴趣嵌入保持语义可恢复性。在淘宝搜索系统中，KARMA 缓解了语义崩溃（注意力汇聚分析），并提升了行为指标和语义保真度。在消融实验中，语义可解码性带来了高达 +22.5 的 HR@200 提升。凭借 KARMA，我们在精排阶段实现了 +0.25 的 CTR AUC 提升，在粗排阶段实现了 +1.86 的 HR 提升，在召回阶段实现了 +2.51 的 HR 提升。KARMA 已在线部署，在精排和粗排阶段的推理开销较低，带动了 GMV 增长 +0.9%。

## Abstract
Large Language Models (LLMs) are equipped with profound semantic knowledge, making them a natural choice for injecting semantic generalization into personalized search systems. However, in practice we find that directly fine-tuning LLMs on industrial personalized tasks (e.g. next item prediction) often yields suboptimal results. We attribute this bottleneck to a critical Knowledge--Action Gap: the inherent conflict between preserving pre-trained semantic knowledge and aligning with specific personalized actions by discriminative objectives. Empirically, action-only training objectives induce Semantic Collapse, such as attention "sinks". This degradation severely cripples the LLM's generalization, failing to bring improvements to personalized search systems.   We propose KARMA (Knowledge--Action Regularized Multimodal Alignment), a unified framework that treats semantic reconstruction as a train-only regularizer. KARMA optimizes a next-interest embedding for retrieval (Action) while enforcing semantic decodability (Knowledge) through two complementary objectives: (i) history-conditioned semantic generation, which anchors optimization to the LLM's native next-token distribution, and (ii) embedding-conditioned semantic reconstruction, which constrains the interest embedding to remain semantically recoverable.   On Taobao search system, KARMA mitigates semantic collapse (attention-sink analysis) and improves both action metrics and semantic fidelity. In ablations, semantic decodability yields up to +22.5 HR@200. With KARMA, we achieve +0.25 CTR AUC in ranking, +1.86 HR in pre-ranking and +2.51 HR in recalling. Deployed online with low inference overhead at ranking & pre-ranking stage, KARMA drives +0.9% increase in GMV.

---

## 论文详细总结（自动生成）

这篇论文由阿里巴巴淘宝技术团队提出，旨在解决大语言模型（LLM）在工业级个性化搜索应用中的“语义坍缩”问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：直接在工业级个性化任务（如“下一商品预测”）上微调 LLM 往往效果不佳。
*   **研究动机**：作者发现存在**“知识-行为鸿沟”（Knowledge-Action Gap）**。当 LLM 仅使用判别性目标（如点击率预测）进行训练时，会发生**语义坍缩（Semantic Collapse）**。
*   **现象观察**：在纯行为目标驱动下，LLM 的注意力机制会退化为“条形码状”的**注意力汇聚（Attention Sinks）**，模型倾向于寻找捷径（如 ID 类特征）而忽略了预训练的语义知识，导致泛化能力下降。

### 2. 方法论：KARMA 框架
KARMA（Knowledge-Action Regularized Multimodal Alignment）的核心思想是将**语义重构作为“仅训练期”的正则化项**。
*   **核心架构**：采用连续词元（Continuous-token）接口，将商品信息压缩为嵌入向量，输入 Decoder-only Transformer（如 Qwen3）进行建模。
*   **双重解码任务（L_dec）**：
    1.  **历史条件语义生成（Task 1）**：根据用户历史行为序列，直接生成目标商品的文本描述。这旨在锚定 LLM 原生的词元分布，防止知识遗忘。
    2.  **嵌入条件语义重构（Task 2）**：强制要求模型生成的“下一兴趣嵌入”（$h_t$）能够重新解码出目标商品的语义。这为嵌入向量设立了“语义瓶颈”，防止其演变为无意义的 ID 捷径。
*   **多模态扩展**：引入扩散模型（Diffusion/Flow-matching）头，用于重构商品的视觉语义特征。
*   **推理效率**：所有解码头仅在训练时存在，**在线推理时完全移除**，因此实现了“零额外推理开销”。

### 3. 实验设计
*   **数据集与场景**：使用淘宝搜索的大规模匿名日志数据，涵盖用户历史交互、点击商品及曝光未点击的负样本。
*   **评估指标**：
    *   **行为指标**：gAUC（精排）、HR@K（召回/粗排）。
    *   **语义指标**：JS@50（Jaccard 相似度），用于探测检索结果与真实目标的语义重合度。
*   **对比基准**：主要对比仅使用行为目标（Pairwise-CE）训练的基线模型。
*   **模型配置**：使用 Qwen3-0.6B 作为用户解码器，Qwen3-1.7B/2B 作为商品编码器。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **模型规模**：提到了使用了 0.6B 到 2B 参数规模的 Qwen3 系列模型，这暗示了实验是在大规模工业级集群上完成的。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **离线实验**：进行了详尽的消融实验，验证了 Task 1、Task 2 以及多模态重构的增量贡献。
    *   **定性分析**：通过可视化注意力图（Attention Maps）直观展示了 KARMA 如何修复“注意力汇聚”现象。
    *   **在线实验**：在淘宝生产环境进行了为期 **14 天的 A/B 测试**。
*   **充分性评价**：实验设计非常充分，涵盖了从底层机制分析（注意力图）到顶层业务指标（GMV）的全方位验证，具有很强的说服力。

### 6. 主要结论与发现
*   **缓解坍缩**：KARMA 有效修复了 LLM 在微调过程中的语义退化，使注意力分布更加合理。
*   **性能提升**：在离线实验中，语义可解码性正则化带来了 HR@200 高达 **+22.5** 的提升；在线上，带动了 **+0.9% 的 GMV 增长**。
*   **生成 vs 检索**：研究发现扩散模型适合作为语义正则化项，但不适合直接生成检索嵌入（因为检索需要“均值寻求”以聚合意图，而扩散模型是“众数寻求”以追求保真度）。

### 7. 优点
*   **工业实用性极强**：通过“训练期正则、推理期移除”的设计，在不增加任何线上延迟的前提下提升了模型能力。
*   **理论洞察深刻**：准确识别并定义了“知识-行为鸿沟”和“语义坍缩”问题，并提供了定量的诊断工具（JS@50 和注意力图分析）。
*   **多模态融合**：成功将视觉特征重构整合进统一的正则化框架中。

### 8. 不足与局限
*   **数据局限性**：实验仅在淘宝单一电商场景下完成，虽然规模巨大，但尚未在其他领域（如新闻推荐、短视频）验证通用性。
*   **算力细节缺失**：缺乏具体的训练资源消耗说明，对于其他研究者复现该规模的实验缺乏参考。
*   **开源限制**：由于涉及商业系统，核心代码和数据集未开源。

（完）
