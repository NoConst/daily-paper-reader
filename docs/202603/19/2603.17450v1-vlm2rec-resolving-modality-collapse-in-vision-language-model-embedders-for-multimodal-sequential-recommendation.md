---
title: "VLM2Rec: Resolving Modality Collapse in Vision-Language Model Embedders for Multimodal Sequential Recommendation"
title_zh: VLM2Rec：解决多模态序列推荐中视觉-语言模型嵌入器的模态崩溃问题
authors: "Junyoung Kim, Woojoo Kim, Jaehyung Lim, Dongha Kim, Hwanjo Yu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17450v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: VLM 作为推荐任务的多模态编码器
tldr: 针对多模态序列推荐中预训练编码器语义能力受限及协同过滤信号融合不足的问题，本文提出VLM2Rec框架。该框架利用视觉语言模型（VLM）作为嵌入器，并针对微调过程中出现的“模态崩溃”现象，引入弱模态惩罚对比学习和跨模态关系拓扑正则化，以平衡模态利用并保持几何一致性，显著提升了推荐的准确性与鲁棒性。
selection_source: fresh_fetch
motivation: 传统的序列推荐编码器语义容量有限，且在将VLM适配为嵌入器时存在严重的模态崩溃问题，导致优化失衡。
method: 提出了包含弱模态惩罚对比学习和跨模态关系拓扑正则化的VLM2Rec框架，旨在纠正梯度不平衡并维持模态间的几何一致性。
result: 在多种场景下的广泛实验表明，VLM2Rec在准确性和鲁棒性方面均优于现有的最先进基准模型。
conclusion: 通过解决VLM在微调过程中的模态崩溃问题，可以有效提升多模态序列推荐系统的性能。
---

## 摘要
多模态环境下的序列推荐（SR）通常依赖于小型且冻结的预训练编码器，这限制了语义容量，并阻碍了协同过滤（CF）信号充分融入物品表示。受大语言模型（LLMs）作为高容量嵌入器取得成功的启发，我们研究了将视觉-语言模型（VLMs）作为感知协同过滤信号的多模态编码器用于序列推荐。然而，我们发现标准的对比监督微调（SFT）在使视觉-语言模型适应嵌入生成并注入协同过滤信号时，会放大其固有的模态崩溃。在这种状态下，优化过程由单一模态主导，而另一模态则发生退化，最终损害了推荐准确性。为解决这一问题，我们提出了 VLM2Rec，这是一个基于视觉-语言模型嵌入器的多模态序列推荐框架，旨在确保模态利用的平衡。具体而言，我们引入了弱模态惩罚对比学习以纠正优化过程中的梯度不平衡，并引入了跨模态关系拓扑正则化以保持模态间的几何一致性。大量实验表明，VLM2Rec 在多种场景下的准确性和鲁棒性均持续优于最先进的基准模型。

## Abstract
Sequential Recommendation (SR) in multimodal settings typically relies on small frozen pretrained encoders, which limits semantic capacity and prevents Collaborative Filtering (CF) signals from being fully integrated into item representations. Inspired by the recent success of Large Language Models (LLMs) as high-capacity embedders, we investigate the use of Vision-Language Models (VLMs) as CF-aware multimodal encoders for SR. However, we find that standard contrastive supervised fine-tuning (SFT), which adapts VLMs for embedding generation and injects CF signals, can amplify its inherent modality collapse. In this state, optimization is dominated by a single modality while the other degrades, ultimately undermining recommendation accuracy. To address this, we propose VLM2Rec, a VLM embedder-based framework for multimodal sequential recommendation designed to ensure balanced modality utilization. Specifically, we introduce Weak-modality Penalized Contrastive Learning to rectify gradient imbalance during optimization and Cross-Modal Relational Topology Regularization to preserve geometric consistency between modalities. Extensive experiments demonstrate that VLM2Rec consistently outperforms state-of-the-art baselines in both accuracy and robustness across diverse scenarios.

---

## 论文详细总结（自动生成）

以下是对论文《VLM2Rec: Resolving Modality Collapse in Vision-Language Model Embedders for Multimodal Sequential Recommendation》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：序列推荐（SR）目前正从传统的基于ID的方法转向利用多模态信息（文本、图像）。现有方法多采用冻结的小型预训练编码器（如CLIP），这限制了模型捕获复杂语义和协同过滤（CF）信号的能力。
*   **核心问题**：受大语言模型（LLM）作为嵌入器的启发，研究者尝试将视觉-语言模型（VLM）引入SR。然而，研究发现直接对VLM进行标准的对比监督微调（SFT）会导致**“SFT悖论”**：微调反而加剧了**模态崩溃（Modality Collapse）**。
*   **现象描述**：在优化过程中，模型倾向于走捷径，过度依赖易于学习的模态（通常是文本），导致弱势模态（视觉）的梯度被忽略，其表示空间发生坍缩，无法有效区分负样本，最终损害推荐性能。

### 2. 方法论
VLM2Rec 框架旨在不增加复杂融合模块的前提下，通过**目标函数层面的干预**解决模态崩溃：
*   **外部融合输入**：不采用交错输入，而是将文本序列和图像序列分别输入VLM，以减少输入端的模态干扰。
*   **弱模态惩罚对比学习 ($L_{WPCL}$)**：
    *   **用户自适应模态间隙估计**：动态计算每个用户在不同模态下的判别边际（Margin），识别出当前的强、弱模态。
    *   **间隙引导的动态惩罚**：根据模态间隙计算惩罚权重 $w_{pen}$。在对比损失中，对弱势模态施加更大的负样本推送压力，强制模型优化弱势模态的判别能力。
*   **跨模态关系拓扑正则化 ($L_{CRTR}$)**：
    *   为了防止 $L_{WPCL}$ 的激进推送导致空间畸变，该模块通过 KL 散度对齐文本和视觉空间的“关系拓扑”（即序列与候选物品之间的相似度分布），确保模态间的几何一致性，同时保留各自的语义特性。
*   **最终目标**：结合 $L_{WPCL}$ 和 $L_{CRTR}$ 进行联合优化。

### 3. 实验设计
*   **数据集**：使用了 Amazon 的四个域：Toys、Beauty、Clothing、Sports。
*   **评估任务**：
    *   **任务 1（直接推荐）**：直接利用 VLM 生成的嵌入进行向量相似度检索。
    *   **任务 2（下游模型初始化）**：将生成的嵌入作为传统 SR 模型（如 SASRec, GRU4Rec）的初始特征。
*   **基准模型 (Baselines)**：
    *   **ID类**：SASRec, GRU4Rec。
    *   **小型编码器**：BERT, CLIP。
    *   **LLM/VLM类**：LLM2Rec, LLMEmb, NoteLLM-2, VLM Prompt 等。

### 4. 资源与算力
*   **硬件使用**：主要在单张 **NVIDIA RTX 3090** 上运行；对于全参数微调或超大型模型实验，使用了单张 **A100 80GB**。
*   **技术细节**：采用了 **LoRA**（低秩自适应）技术进行高效微调（rank=16）。
*   **效率优化**：论文提到通过对用户进行采样（如仅使用 1024 个用户的数据），可以在大幅缩短训练时间的同时保持极具竞争力的性能。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个数据集上进行了详尽的对比实验，涵盖了 10 余种基准方法。
*   **实验维度**：
    *   **消融实验**：验证了 $L_{WPCL}$、停止梯度操作和 $L_{CRTR}$ 的必要性。
    *   **鲁棒性分析**：测试了不同 VLM 家族（Qwen2.5-VL, InternVL3, Llava）及不同参数规模（2B 到 32B）。
    *   **泛化性测试**：包括跨域推荐（Zero-shot）和冷启动物品分析。
*   **评价**：实验设计全面，通过梯度动力学分析和几何指标（Alignment/Uniformity）深入揭示了模态崩溃的本质，论证过程客观且具有说服力。

### 6. 主要结论与发现
*   **SFT 悖论确认**：标准对比微调确实会使 VLM 的视觉模态性能退化至基准线以下。
*   **VLM2Rec 的有效性**：通过平衡梯度贡献，VLM2Rec 成功恢复了视觉模态的判别力，实现了多模态的协同增益。
*   **语义与 CF 的融合**：VLM2Rec 生成的嵌入在冷启动和跨域场景下表现优异，证明其不仅捕获了协同过滤信号，还保留了深层的语义推理能力。

### 7. 优点
*   **理论深刻**：首次揭示并量化了 VLM 在序列推荐微调中的模态崩溃问题。
*   **架构简洁**：无需引入复杂的注意力融合层或额外的参数，仅通过损失函数改进即可实现 SOTA 性能。
*   **实用性强**：支持少样本微调，且生成的嵌入具有良好的可移植性，能显著提升现有轻量级推荐模型的表现。

### 8. 不足与局限
*   **计算开销**：尽管有采样策略，但 VLM 处理图像 Token 的计算成本仍显著高于纯文本 LLM。
*   **序列长度限制**：实验中最大序列长度设为 10，对于极长序列的建模能力尚待验证。
*   **模态局限**：目前仅聚焦于文本和图像，未涉及视频或音频等更多元的多模态场景。

（完）
