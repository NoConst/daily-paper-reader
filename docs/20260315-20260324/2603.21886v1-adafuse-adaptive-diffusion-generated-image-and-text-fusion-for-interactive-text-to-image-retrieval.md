---
title: "ADaFuSE: Adaptive Diffusion-generated Image and Text Fusion for Interactive Text-to-Image Retrieval"
title_zh: ADaFuSE：用于交互式文本到图像检索的自适应扩散生成图像与文本融合
authors: "Zhuocheng Zhang, Xingwu Zhang, Kangheng Liang, Guanxuan Li, Richard Mccreadie, Zijun Long"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21886v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 交互式文本到图像检索及多模态视图融合
tldr: 针对交互式文本图像检索（I-TIR）中扩散模型生成的图像与文本融合时存在的噪声干扰问题，本文提出了ADaFuSE。该模型通过轻量级的双分支融合机制（自适应门控和语义感知专家混合），动态平衡模态可靠性并捕捉细粒度跨模态特征。实验证明，ADaFuSE在不修改骨干编码器的情况下，显著提升了检索性能和鲁棒性，成为微调方法的有效替代方案。
selection_source: fresh_fetch
motivation: 现有的I-TIR框架采用简单的嵌入相加进行多模态融合，容易引入扩散模型产生的生成噪声，导致检索性能下降。
method: 提出一种轻量级的双分支融合模型，结合自适应门控分支动态平衡模态权重，以及语义感知专家混合分支捕捉跨模态细节。
result: "在四个标准基准测试中达到SOTA性能，Hits@10提升高达3.49%，且对噪声和长查询具有更强的鲁棒性。"
conclusion: 生成式增强结合合理的融合机制，为交互式检索提供了一种简单且泛化性强的微调替代方案。
---

## 摘要
交互式文本到图像检索（I-TIR）的最新进展利用扩散模型来弥合文本信息需求与待搜索图像之间的模态差距，从而提高了检索效果。然而，现有框架通常通过简单的嵌入相加来融合用户反馈的多模态视图。在这项工作中，我们证明了这种静态且无差别的融合会不加区分地引入扩散模型产生的生成噪声，导致多达 55.62% 的样本出现性能下降。我们进一步提出了 ADaFuSE（具有语义感知专家的自适应扩散-文本融合），这是一种轻量级融合模型，旨在为扩散增强的 I-TIR 对齐和校准多模态视图，且无需修改骨干编码器即可插入现有框架。具体而言，我们引入了一种双分支融合机制，该机制采用自适应门控分支来动态平衡模态可靠性，并结合语义感知混合专家分支来捕捉细粒度的跨模态细微差别。通过对四个标准 I-TIR 基准的全面评估，ADaFuSE 实现了最先进的性能，在仅增加 5.29% 参数的情况下，Hits@10 指标比 DAR 高出多达 3.49%，同时对噪声和较长的交互式查询表现出更强的鲁棒性。这些结果表明，生成式增强与原则性融合相结合，为交互式检索提供了一种简单、通用的微调替代方案。

## 速览
**TLDR**：针对交互式文本图像检索（I-TIR）中扩散模型生成的图像与文本融合时存在的噪声干扰问题，本文提出了ADaFuSE。该模型通过轻量级的双分支融合机制（自适应门控和语义感知专家混合），动态平衡模态可靠性并捕捉细粒度跨模态特征。实验证明，ADaFuSE在不修改骨干编码器的情况下，显著提升了检索性能和鲁棒性，成为微调方法的有效替代方案。 \
**Motivation**：现有的I-TIR框架采用简单的嵌入相加进行多模态融合，容易引入扩散模型产生的生成噪声，导致检索性能下降。 \
**Method**：提出一种轻量级的双分支融合模型，结合自适应门控分支动态平衡模态权重，以及语义感知专家混合分支捕捉跨模态细节。 \
**Result**：在四个标准基准测试中达到SOTA性能，Hits@10提升高达3.49%，且对噪声和长查询具有更强的鲁棒性。 \
**Conclusion**：生成式增强结合合理的融合机制，为交互式检索提供了一种简单且泛化性强的微调替代方案。

---

## Abstract
Recent advances in interactive text-to-image retrieval (I-TIR) use diffusion models to bridge the modality gap between the textual information need and the images to be searched, resulting in increased effectiveness. However, existing frameworks fuse multi-modal views of user feedback by simple embedding addition. In this work, we show that this static and undifferentiated fusion indiscriminately incorporates generative noise produced by the diffusion model, leading to performance degradation for up to 55.62% samples. We further propose ADaFuSE (Adaptive Diffusion-Text Fusion with Semantic-aware Experts), a lightweight fusion model designed to align and calibrate multi-modal views for diffusion-augmented I-TIR, which can be plugged into existing frameworks without modifying the backbone encoder. Specifically, we introduce a dual-branch fusion mechanism that employs an adaptive gating branch to dynamically balance modality reliability, alongside a semantic-aware mixture-of-experts branch to capture fine-grained cross-modal nuances. Via thorough evaluation over four standard I-TIR benchmarks, ADaFuSE achieves state-of-the-art performance, surpassing DAR by up to 3.49% in Hits@10 with only a 5.29% parameter increase, while exhibiting stronger robustness to noisy and longer interactive queries. These results show that generative augmentation coupled with principled fusion provides a simple, generalizable alternative to fine-tuning for interactive retrieval.

---

## 论文详细总结（自动生成）

以下是对论文《ADaFuSE: Adaptive Diffusion-generated Image and Text Fusion for Interactive Text-to-Image Retrieval》的结构化深入总结：

### 1. 核心问题与研究背景
*   **核心问题**：在交互式文本到图像检索（I-TIR）中，现有方法（如 DAR）利用扩散模型生成图像作为“视觉代理”来增强文本查询。然而，这些方法通常采用**静态加权求和**的方式融合文本和生成图像的嵌入。
*   **研究动机**：作者发现这种无差别的静态融合存在严重缺陷：
    1.  **生成噪声干扰**：扩散模型具有随机性，生成的图像可能偏离用户真实意图。实验显示，在超过 50% 的样本中，引入生成图像反而会导致检索排名大幅下降（降幅高达 7500 位）。
    2.  **缺乏实例自适应性**：不同查询对文本和视觉信息的依赖程度不同，固定权重无法处理复杂的交互场景。

### 2. 方法论
ADaFuSE 提出了一种轻量级的双分支融合架构，旨在动态校准多模态表示：
*   **核心思想**：通过“自适应门控”筛选可靠信号，通过“混合专家（MoE）”捕捉细粒度特征，且无需修改预训练的骨干编码器（如 BLIP）。
*   **关键技术细节**：
    1.  **投影块（Projection Blocks）**：将原始文本和图像嵌入映射到更高维的任务特定潜空间，以恢复预训练过程中可能丢失的细粒度视觉细节。
    2.  **自适应门控分支（Adaptive Gating Branch）**：利用联合上下文（文本+图像特征）预测一个动态标量权重 $\lambda$。当生成图像被判定为不可靠或含有噪声时，系统会自动降低其权重，转而依赖文本。
    3.  **语义感知混合专家分支（MoE Branch）**：由 $K$ 个轻量级前馈网络（专家）组成。路由网络根据当前上下文为专家分配权重，生成补偿性特征 $h_{res}$，用于捕捉非线性的跨模态细微差别（如“款式相同但袖子更短”）。
    4.  **残差融合**：最终表示由门控融合结果与 MoE 输出的残差项相加并归一化得到。

### 3. 实验设计
*   **数据集**：
    *   **DA-VisDial**：用于训练。
    *   **测试基准**：VisDial 验证集（分布内），以及三个分布外（OOD）基准：ChatGPT_BLIP2、Human_BLIP2、Flan-Alpaca-XXL_BLIP2。
*   **对比方法（Baselines）**：
    *   **ChatIR**：仅使用对话文本的强基准。
    *   **DAR**：目前最先进的扩散增强模型（采用静态融合）。
*   **评价指标**：Hits@10（前10名检索结果的召回率）。

### 4. 资源与算力
*   **参数量**：ADaFuSE 非常轻量，仅比 DAR 增加了 **5.29%** 的参数。
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及训练时长。但提到该模型是“轻量级”且“无需微调骨干编码器”，暗示其训练成本相对较低。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个标准基准上进行了全面评估，涵盖了从第 0 轮到第 10 轮的多次交互过程。
*   **分析维度**：
    *   **性能对比**：展示了在所有数据集和所有交互轮次下的领先地位。
    *   **鲁棒性分析**：专门分析了“性能退化率”，证明 ADaFuSE 能显著抑制噪声。
    *   **可视化分析**：通过散点图展示了门控权重与模态相似度之间的相关性。
*   **评价**：实验设计较为充分，通过跨数据集（OOD）的测试验证了模型的泛化能力，对比实验在相同骨干网络下进行，保证了公平性。

### 6. 主要结论与发现
*   **性能提升**：ADaFuSE 在 Hits@10 指标上比 DAR 提升了高达 **3.49%**。
*   **抑制退化**：相比 DAR 超过 50% 的退化率，ADaFuSE 显著降低了因生成图像噪声导致的排名下降（平均排名下降从 7500 降至约 20）。
*   **长查询优势**：随着交互轮次增加和文本变长，ADaFuSE 的领先优势扩大，证明其在处理复杂、潜在冲突的反馈时更具鲁棒性。
*   **自适应性**：模型学会了在图像质量高时增加其权重，在图像与文本冲突时保持保守。

### 7. 优点
*   **即插即用**：作为轻量级模块，可直接集成到现有 I-TIR 框架中，无需昂贵的骨干网络微调。
*   **针对性强**：直接解决了扩散增强检索中被忽视的“生成噪声”问题。
*   **高效性**：以极小的参数增长换取了显著的性能和鲁棒性提升。

### 8. 不足与局限
*   **硬件细节缺失**：未提供具体的训练资源消耗数据，不便于复现时的成本评估。
*   **专家数量敏感性**：虽然使用了 MoE，但文中未深入探讨专家数量 $K$ 的选择对性能的具体影响（消融实验细节略显单薄）。
*   **依赖生成质量**：尽管有门控机制，但如果扩散模型在所有轮次生成的图像质量都极差，该模型的效果上限仍受限于文本模态。

（完）
