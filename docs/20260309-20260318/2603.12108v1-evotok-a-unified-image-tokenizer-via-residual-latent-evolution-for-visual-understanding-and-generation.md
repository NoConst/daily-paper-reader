---
title: "EvoTok: A Unified Image Tokenizer via Residual Latent Evolution for Visual Understanding and Generation"
title_zh: EvoTok：通过残差潜空间演化实现视觉理解与生成的统一图像分词器
authors: "Yan Li, Ning Liao, Xiangyu Zhao, Shaofeng Zhang, Xiaoxing Wang, Yifan Yang, Junchi Yan, Xue Yang"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12108v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 统一多模态大语言模型与图像标记化
tldr: 针对多模态大模型在视觉理解（高层语义）与生成（底层像素）之间存在的粒度差异挑战，本文提出了EvoTok统一图像分词器。该方法通过残差向量量化将图像编码为级联的残差序列，形成从底层细节到高层语义演进的轨迹。尽管仅在1300万张图像的小规模数据集上训练，EvoTok在重建质量、视觉理解及图像生成任务上均表现出色，为统一视觉理解与生成提供了有效的解决方案。
selection_source: fresh_fetch
motivation: 现有的统一模型在处理理解与生成任务时，常因共享或解耦特征空间而导致干扰或不一致性。
method: 提出一种基于残差潜空间演化的分词器，通过级联残差序列在共享空间内实现从像素细节到语义抽象的平滑过渡。
result: 在ImageNet-1K上实现了0.43的rFID，并在多个视觉理解和生成基准测试中展现出极具竞争力的性能。
conclusion: 将视觉表示建模为演进轨迹是统一视觉理解与生成任务的一种有效且原则性的方法。
---

## 摘要
统一多模态大语言模型（MLLMs）的发展从根本上受到视觉理解与生成之间粒度差异的挑战：理解需要高层语义抽象，而图像生成则需要细粒度的像素级表示。现有方法通常在同一组表示上强制执行这两种监督，或者在独立的特征空间中解耦这两种监督，这分别会导致干扰和不一致。在这项工作中，我们提出了 EvoTok，这是一种统一的图像分词器，通过共享潜空间内的残差演化过程来协调这些需求。EvoTok 并非为像素和语义维护独立的分词空间，而是通过残差向量量化（RVQ）将图像编码为级联的残差分词序列。该残差序列形成了一个演化轨迹，其中早期阶段捕捉低层细节，而更深阶段则逐步向高层语义表示过渡。尽管仅在 1300 万张图像的相对适度的数据集上进行训练（远小于许多先前统一分词器使用的十亿级数据集），EvoTok 在 256x256 分辨率的 ImageNet-1K 上实现了 0.43 rFID 的强大重建质量。当与大语言模型集成时，EvoTok 在 9 个视觉理解基准测试中的 7 个上表现出极具前景的性能，并在 GenEval 和 GenAI-Bench 等图像生成基准测试中取得了显著成果。这些结果表明，将视觉表示建模为演化轨迹，为统一视觉理解与生成提供了一种有效且原则性的解决方案。

## Abstract
The development of unified multimodal large language models (MLLMs) is fundamentally challenged by the granularity gap between visual understanding and generation: understanding requires high-level semantic abstractions, while image generation demands fine-grained pixel-level representations. Existing approaches usually enforce the two supervision on the same set of representation or decouple these two supervision on separate feature spaces, leading to interference and inconsistency, respectively. In this work, we propose EvoTok, a unified image tokenizer that reconciles these requirements through a residual evolution process within a shared latent space. Instead of maintaining separate token spaces for pixels and semantics, EvoTok encodes an image into a cascaded sequence of residual tokens via residual vector quantization. This residual sequence forms an evolution trajectory where earlier stages capture low-level details and deeper stages progressively transition toward high-level semantic representations. Despite being trained on a relatively modest dataset of 13M images, far smaller than the billion-scale datasets used by many previous unified tokenizers, EvoTok achieves a strong reconstruction quality of 0.43 rFID on ImageNet-1K at 256x256 resolution. When integrated with a large language model, EvoTok shows promising performance across 7 out of 9 visual understanding benchmarks, and remarkable results on image generation benchmarks such as GenEval and GenAI-Bench. These results demonstrate that modeling visual representations as an evolving trajectory provides an effective and principled solution for unifying visual understanding and generation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **EvoTok** 的统一图像分词器，旨在通过残差潜空间演化（Residual Latent Evolution）解决多模态大语言模型（MLLM）在视觉理解与生成任务之间的粒度冲突。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心矛盾**：视觉理解需要高层语义抽象（忽略细节），而图像生成需要细粒度的像素级表示（保留细节）。
*   **现有方法局限**：
    *   **纠缠式（Entangled）**：在同一特征空间强制执行两种任务，导致优化冲突和性能干扰。
    *   **解耦式（Decoupled）**：使用独立的编码器或特征层，导致理解与生成之间缺乏一致性，无法共享视觉结构和语义先验。
*   **研究目标**：设计一个既能解耦任务特征、又能保持跨任务一致性的统一分词器。

### 2. 方法论
*   **核心思想**：将图像表示建模为共享潜空间内的**残差演化轨迹**。利用级联的残差向量量化（RVQ），使图像特征随残差层数加深从底层像素向高层语义演进。
*   **关键技术细节**：
    *   **残差演化过程**：图像经编码器提取特征后，通过 $L$ 级残差量化。早期阶段（如前 4 层）捕捉空间结构和感知细节，用于生成任务；深层阶段（如全部 16 层）累积特征以形成对齐预训练视觉模型（如 SigLIP2）的高层语义，用于理解任务。
    *   **特征定义**：定义像素级特征 $f_{pix}$ 为早期残差阶段的部分和，语义特征 $f_{sem}$ 为完整轨迹的累加和。
    *   **统一训练目标**：损失函数包含像素重建损失（重建、感知、对抗损失）、语义对齐损失（与 SigLIP2 特征的余弦相似度）以及 VQ 损失。
*   **任务集成**：
    *   **理解**：将 $f_{sem}$ 投影至 LLM 空间，遵循 LLaVA 范式。
    *   **生成**：LLM 自回归预测残差分词序列，通过 RQ-Transformer 头部生成像素级代码。

### 3. 实验设计
*   **数据集**：
    *   **分词器训练**：CC12M 和 ImageNet-1K（共约 13M 图像），远小于同类模型常用的十亿级数据集。
    *   **理解/生成微调**：使用了 LLaVA-Pretrain、Cambrian-10M、ShareGPT-4o 等开源数据集。
*   **Benchmark（基准测试）**：
    *   **重建**：ImageNet-1K 验证集（指标：rFID）。
    *   **理解**：SEEDBench, MMMU, MME, GQA, AI2D 等 9 个主流基准。
    *   **生成**：GenEval, GenAI-Bench。
*   **对比方法**：包括理解专用模型（Qwen-VL, LLaVA-v1.5）、生成专用模型（SDXL, DALL-E 3）以及统一模型（VILA-U, UniTok, TokenFlow, Janus 等）。

### 4. 资源与算力
*   **算力投入**：所有训练实验在 **16 张 GPU** 上完成（未明确具体型号，推测为 A100 或 H100 级别）。
*   **训练时长**：分词器训练了 500k 步，全局 Batch Size 为 256。
*   **模型规模**：语言模型采用 Qwen-2.5-7B-Instruct。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了重建质量评估、9 项理解任务、2 项生成任务，并进行了详尽的消融实验。
*   **消融实验**：对比了演化深度（$L_{pix}$ 与 $L_{sem}$ 的分配）、演化方向（像素到语义 vs 语义到像素）、以及不同的解耦策略（不同分支、不同层等）。
*   **客观性**：实验对比了在不同数据规模下的表现，证明了 EvoTok 在小规模数据（13M）下即可超越许多在大规模数据（1B）上训练的模型，对比具有说服力。

### 6. 主要结论与发现
*   **高效性**：EvoTok 在 ImageNet-1K 上实现了 **0.43 rFID**，优于大多数统一分词器。
*   **理解能力**：在 9 个理解基准中的 7 个上取得领先，尤其在推理密集型任务（如 AI2D, MMMU）中优势明显。
*   **生成能力**：在复杂组合生成任务（如 GenEval 中的位置和颜色属性）中表现卓越，证明了残差轨迹保留了丰富的生成先验。
*   **演化特性**：t-SNE 可视化证实了特征从底层感知到高层语义的平滑过渡。

### 7. 优点
*   **设计优雅**：通过残差演化轨迹巧妙地在单一潜空间内统一了两种截然不同的表征需求。
*   **数据效率极高**：仅需 13M 数据即可达到甚至超过 1B 数据训练模型的效果。
*   **任务平衡性好**：消融实验证明了“像素到语义”的演化路径比“纠缠式”或“语义到像素”更稳定且高效。

### 8. 不足与局限
*   **分辨率限制**：目前实验主要集中在 256x256 分辨率，对于更高分辨率（如 1024x1024）的扩展性有待验证。
*   **自回归生成的固有缺陷**：虽然在组合性上表现好，但在生成速度上可能慢于扩散模型（Diffusion Models）。
*   **依赖预训练教师模型**：语义对齐高度依赖于 SigLIP2 的质量，若教师模型存在偏差，可能会传递至 EvoTok。

（完）
