---
title: "Think While Watching: Online Streaming Segment-Level Memory for Multi-Turn Video Reasoning in Multimodal Large Language Models"
title_zh: 边看边想：多模态大语言模型中用于多轮视频推理的在线流式分段级记忆
authors: "Lu Wang, Zhuoran Jin, Yupu Hao, Yubo Chen, Kang Liu, Yulong Ao, Jun Zhao"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11896v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大模型中的记忆锚定流式视频推理
tldr: 针对多模态大模型在在线流式视频推理中感知与生成不并行、长程记忆衰减的问题，本文提出“边看边想”（Think While Watching）框架。该框架通过片段级记忆锚定实现持续推理，并结合三阶段多轮思维链数据集与流式因果掩码策略。实验表明，该方法在StreamingBench等基准上显著提升了准确率，并在多轮对话中大幅降低了推理开销，实现了高效的实时视频交互。
selection_source: fresh_fetch
motivation: 现有流式视频理解方法采用交替感知生成模式，导致无法并发处理且长程依赖建模能力随视频流增长而减弱。
method: 提出片段级记忆锚定框架，通过流式因果掩码和位置编码确保因果性，并采用重叠感知与思考的高效推理流水线。
result: "在StreamingBench和OVO-Bench上分别取得2.6%和3.79%的性能提升，且在多轮场景下减少了56%的Token输出。"
conclusion: 该研究通过片段级记忆管理和并行推理机制，显著增强了多模态大模型处理长视频流和多轮交互的能力。
---

## 摘要
多模态大语言模型（MLLMs）在离线视频理解方面表现出强大的性能，但大多数模型局限于离线推理或在线推理能力较弱，导致难以在持续到达的视频流上进行多轮交互。现有的流式方法通常采用交替的感知-生成范式，这阻碍了感知与生成的并发进行，并随着视频流的增长导致早期记忆衰减，从而损害了长程依赖建模。我们提出了“边看边想”（Think While Watching），这是一个以记忆为锚点的流式视频推理框架，能够在多轮交互过程中保持连续的分段级记忆。我们构建了一个三阶段、多轮的思维链数据集，并采用了阶段匹配的训练策略，同时通过分段级流式因果掩码和流式位置编码来强制执行严格的因果性。在推理过程中，我们引入了一种高效的流水线，使“观看”与“思考”重叠，并自适应地选择最佳的注意力后端。在单轮和多轮流式输入协议下，我们的方法均取得了优异的结果。基于 Qwen3-VL 构建，该方法在 StreamingBench 上的单轮准确率提升了 2.6%，在 OVO-Bench 上提升了 3.79%。在多轮设置下，它在保持性能的同时减少了 56% 的输出 token。代码可在以下地址获取：https://github.com/wl666hhh/Think_While_Watching/

## Abstract
Multimodal large language models (MLLMs) have shown strong performance on offline video understanding, but most are limited to offline inference or have weak online reasoning, making multi-turn interaction over continuously arriving video streams difficult. Existing streaming methods typically use an interleaved perception-generation paradigm, which prevents concurrent perception and generation and leads to early memory decay as streams grow, hurting long-range dependency modeling. We propose Think While Watching, a memory-anchored streaming video reasoning framework that preserves continuous segment-level memory during multi-turn interaction. We build a three-stage, multi-round chain-of-thought dataset and adopt a stage-matched training strategy, while enforcing strict causality through a segment-level streaming causal mask and streaming positional encoding. During inference, we introduce an efficient pipeline that overlaps watching and thinking and adaptively selects the best attention backend. Under both single-round and multi-round streaming input protocols, our method achieves strong results. Built on Qwen3-VL, it improves single-round accuracy by 2.6% on StreamingBench and by 3.79% on OVO-Bench. In the multi-round setting, it maintains performance while reducing output tokens by 56%. Code is available at: https://github.com/wl666hhh/Think_While_Watching/

---

## 论文详细总结（自动生成）

以下是对论文《Think While Watching: Online Streaming Segment-Level Memory for Multi-Turn Video Reasoning in Multimodal Large Language Models》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态大语言模型（MLLM）在处理**在线流式视频**和**多轮交互**时存在两大瓶颈：
    1.  **记忆侵蚀（Memory Erosion）**：传统的“交替感知-生成”模式在处理长视频流时，早期的视觉线索和对话背景容易被遗忘，导致多轮对话的一致性差。
    2.  **串行化瓶颈（Serialization Bottleneck）**：感知与生成过程是互斥的，即模型在生成文本回复时必须停止视频摄取，这导致了严重的响应延迟和积压。
*   **研究动机**：开发一种能够“边看边想”的框架，使模型在持续观察视频流的同时，能够维护持久的记忆并支持实时的多轮推理交互。

### 2. 方法论
*   **核心思想**：提出 **Think While Watching (TWW)** 框架，通过维护“片段级记忆笔记（Segment-level Memory Notes）”作为持久状态，并利用解耦的位置编码实现感知与生成的并行化。
*   **关键技术细节**：
    *   **片段级记忆笔记**：模型为每个视频片段生成简短的文本笔记并存入记忆库。回答问题时，通过注意力机制隐式检索相关笔记。
    *   **流式因果掩码（Streaming Causal Mask）**：在片段级别强制执行严格的因果性，确保模型只能看到当前及过去的视频片段，防止信息泄露。
    *   **流式位置编码（Streaming Positional Encoding）**：基于 MRoPE 改进，将输入流和输出流的位置编码解耦。即使输出长度未知，新到达的视频片段也能分配正确的位置索引，从而支持并行处理。
    *   **三阶段训练策略**：
        1.  **阶段1**：学习编写记忆笔记并回答单轮问题。
        2.  **阶段2**：扩展到多轮对话，增强上下文一致性。
        3.  **阶段3**：针对长视频进行长程记忆、不确定性处理和抗干扰学习。
    *   **双 KV 缓存推理流水线**：在推理时采用双 KV 缓存机制，使视频摄取（Watching）与文本解码（Thinking）在时间上重叠，大幅降低首个 Token 的响应延迟（TTFT）。

### 3. 实验设计
*   **数据集与场景**：
    *   **在线流式基准**：StreamingBench（评估实时理解、全源理解等）和 OVO-Bench（评估前向/后向推理）。
    *   **离线泛化评估**：Video-MME 和 LV-Bench。
    *   **自建数据集**：构建了一个包含单轮、多轮及长程 CoT 的三阶段流式数据集（来源于 VideoChatOnline-IT 和 YouTube 长视频）。
*   **对比方法**：
    *   **闭源模型**：Gemini 1.5 Pro, GPT-4o。
    *   **开源流式模型**：Flash-VStream, VideoLLM-online, Dispider, StreamAgent 等。
    *   **基座模型**：Qwen3-VL (2B, 4B, 8B) 的 Instruct 和 Thinking 版本。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA RTX A6000 GPU (每台 48GB 显存)**。
*   **训练细节**：采用全参数监督微调（Full-parameter SFT），使用 DeepSpeed ZeRO-3 优化器，精度为 bf16。学习率为 1.0 × 10⁻⁵，采用余弦衰减调度。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2B 到 8B 三种参数规模的模型，并在 4 个主流视频理解基准上进行了测试。
*   **消融实验**：针对记忆笔记的有效性、片段分割粒度（15s/30s/60s）、视觉帧掩码比例等进行了详细消融。
*   **充分性评价**：实验设计较为全面，不仅对比了准确率，还深入分析了注意力分布、首词延迟（TTFT）和 Token 消耗量。通过多阶段训练的对比，客观证明了长程训练对流式推理的必要性。

### 6. 主要结论与发现
*   **性能提升**：在 Qwen3-VL-4B 基础上，TWW 在 StreamingBench 上提升了 **2.6%** 的准确率，在 OVO-Bench 上提升了 **3.79%**。
*   **效率优化**：在多轮流式协议下，该方法在保持准确率的同时，将输出 Token 数量减少了 **56%**。
*   **延迟降低**：通过并行推理流水线，首词延迟（TTFT）相比传统的批处理模式降低了约 **92.6%**。
*   **长程能力**：阶段 3 的训练显著改变了模型的注意力分布，使其能够更多地关注远距离的记忆笔记而非仅仅依赖最近的视觉帧。

### 7. 优点（亮点）
*   **架构创新**：成功实现了感知与生成的解耦，解决了流式 MLLM 中长期存在的串行化瓶颈。
*   **记忆机制**：片段级记忆笔记既压缩了长视频信息，又保留了关键细节，为多轮对话提供了稳定的上下文。
*   **实用性强**：提出的双 KV 缓存和自适应注意力后端切换，使其非常适合实际的实时交互部署。

### 8. 不足与局限
*   **分割策略**：目前采用固定的时间片段分割，未来可能需要根据场景变化动态调整分割边界（Adaptive Segmentation）。
*   **模态限制**：目前主要侧重于视觉信息，尚未充分整合音频线索（如语音转录）。
*   **极端长视频挑战**：在极长视频流中，记忆笔记的累积仍可能导致显存压力或细微属性的丢失（如身份关联错误）。

（完）
