---
title: "SSP-SAM: SAM with Semantic-Spatial Prompt for Referring Expression Segmentation"
title_zh: SSP-SAM：结合语义-空间提示的 SAM 用于指代性表达分割
authors: "Wei Tang, Xuejing Liu, Yanpeng Sun, Zechao Li"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18086v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 用于指代语义分割的语义空间提示
tldr: 针对Segment Anything Model (SAM)在理解自然语言方面的局限性，本文提出SSP-SAM框架。该框架通过引入语义-空间提示（SSP）编码器，利用视觉和语言注意力适配器提取关键特征，为SAM生成高质量的引导提示。SSP-SAM不仅在指代指代分割（RES）任务中表现出色，还天然支持广义指代分割（GRES），在多个基准测试中实现了高精度分割和强大的开放词汇处理能力。
selection_source: fresh_fetch
motivation: SAM虽然具备强大的通用分割能力，但因缺乏对自然语言的深度理解而难以直接应用于指代指代分割任务。
method: 提出SSP-SAM框架，通过在SSP编码器中集成视觉和语言注意力适配器，生成高质量的语义-空间提示来引导SAM生成精确掩码。
result: "在RES和GRES基准测试中均取得优异成绩，特别是在高精度阈值Pr@0.9和开放词汇场景下表现突出。"
conclusion: SSP-SAM通过有效的多模态提示机制充分释放了SAM的分割潜力，为语言引导的图像分割提供了一种高效且灵活的解决方案。
---

## 摘要
Segment Anything Model (SAM) 在通用图像分割方面表现卓越，但在理解自然语言方面的能力有限，这限制了其在指代性表达分割 (RES) 中的直接应用。为此，我们提出了 SSP-SAM，这是一个通过集成语义-空间提示 (SSP) 编码器来充分利用 SAM 分割能力的框架。具体而言，我们在 SSP 编码器中融入了视觉和语言注意力适配器，用以突出视觉特征中的显著对象和语言特征中的判别性短语。这种设计增强了提示生成器的指代目标表示，从而产生高质量的 SSP，使 SAM 能够在语言引导下生成精确的掩码。尽管 SSP-SAM 并非专门为广义指代性表达分割 (GRES) 设计（在 GRES 中，指代目标可能对应零个、一个或多个物体），但它无需额外修改即可自然地支持这种更灵活的设置。在广泛使用的 RES 和 GRES 基准测试上的大量实验证实了我们方法的优越性。值得注意的是，我们的方法生成了高质量的分割掩码，即使在 Pr@0.9 等严格阈值下也能达到极高的精度。在 PhraseCut 数据集上的进一步评估表明，与现有的最先进 RES 方法相比，该方法在开放词汇场景中表现出更优的性能。代码和检查点可在以下网址获取：https://github.com/WayneTomas/SSP-SAM。

## Abstract
The Segment Anything Model (SAM) excels at general image segmentation but has limited ability to understand natural language, which restricts its direct application in Referring Expression Segmentation (RES). Toward this end, we propose SSP-SAM, a framework that fully utilizes SAM's segmentation capabilities by integrating a Semantic-Spatial Prompt (SSP) encoder. Specifically, we incorporate both visual and linguistic attention adapters into the SSP encoder, which highlight salient objects within the visual features and discriminative phrases within the linguistic features. This design enhances the referent representation for the prompt generator, resulting in high-quality SSPs that enable SAM to generate precise masks guided by language. Although not specifically designed for Generalized RES (GRES), where the referent may correspond to zero, one, or multiple objects, SSP-SAM naturally supports this more flexible setting without additional modifications. Extensive experiments on widely used RES and GRES benchmarks confirm the superiority of our method. Notably, our approach generates segmentation masks of high quality, achieving strong precision even at strict thresholds such as Pr@0.9. Further evaluation on the PhraseCut dataset demonstrates improved performance in open-vocabulary scenarios compared to existing state-of-the-art RES methods. The code and checkpoints are available at: https://github.com/WayneTomas/SSP-SAM.

---

## 论文详细总结（自动生成）

以下是对论文 **《SSP-SAM: SAM with Semantic-Spatial Prompt for Referring Expression Segmentation》** 的结构化深入分析：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：Segment Anything Model (SAM) 虽然在通用图像分割上表现卓越，但其对自然语言（尤其是自由格式的指代性表达）的理解能力较弱，无法直接根据文本指令准确分割目标。
*   **研究背景**：
    *   **现有方法的局限**：目前的 SAM 增强方案多为两阶段（Pre-SAM 或 Post-SAM），割裂了语言理解与掩码预测，导致细粒度对齐不足；或者依赖复杂的多模态大模型（MLLM），计算开销巨大。
    *   **任务扩展**：除了传统的单目标指代分割（RES），现实场景中还存在多目标或无目标的广义指代分割（GRES）需求。
*   **研究动机**：开发一种轻量级、单阶段的框架，通过将语言语义与空间信息深度融合为“提示（Prompt）”，充分释放 SAM 原生的强大分割潜力。

### 2. 方法论：核心思想与技术细节
SSP-SAM 的核心是 **语义-空间提示（SSP）编码器**，它将 CLIP 的多模态特征转化为 SAM 可理解的提示。
*   **核心思想**：利用视觉和语言双重注意力适配器，提取图像中的显著对象和文本中的关键判别词，生成高质量的语义-空间提示。
*   **关键技术细节**：
    1.  **视觉注意力适配器 (Visual Attention Adapter)**：
        *   **句子级筛选**：计算图像特征与整句嵌入的相似度，初步过滤无关区域。
        *   **单词级精炼**：通过交叉注意力捕捉单词语义，并应用**高斯映射 (Gaussian-like mapping)** 增强显著对象与背景的区分度，将空间位置信息融入语义表示。
    2.  **语言注意力适配器 (Linguistic Attention Adapter)**：
        *   利用 Bi-GRU 提取上下文特征，通过注意力机制动态调整单词权重，突出属性词（如颜色、大小）和关系词（如左边、后面）。
    3.  **提示生成器 (Prompt Generator)**：
        *   将上述融合特征与一组**可学习的特殊 Token** 输入 Transformer 编码器，生成最终引导 SAM 解码器的语义-空间提示。
    4.  **辅助任务 (Auxiliary REC)**：
        *   引入指代性表达理解（REC，即画框）作为辅助任务，利用大规模的边界框标注数据进行预训练，提升模型定位精度。

### 3. 实验设计
*   **数据集**：
    *   **经典 RES**：RefCOCO, RefCOCO+, RefCOCOg, ReferIt。
    *   **广义 GRES**：gRefCOCO（包含多目标和无目标场景）。
    *   **开放词汇 (Open-vocabulary)**：PhraseCut（测试模型对未见类别的泛化能力）。
*   **Benchmark 与对比方法**：
    *   对比了非 SAM 方法（如 LAVT, CGFormer, PolyFormer）和基于 SAM 的方法（如 RISAM, Prompt-RIS, LISA, GSVA）。
    *   评估指标包括 gIoU（平均交并比）、cIoU（累积交并比）、Pr@X（高精度阈值下的准确率）和 N-Acc（无目标预测准确率）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA Tesla V100 GPU**。
*   **训练时长**：
    *   **预训练阶段**：在合并的 REC 数据集上训练 20 个 epoch（Batch Size 128）。
    *   **微调阶段**：在目标数据集上训练 80 个 epoch（Batch Size 64）。
*   **模型规模**：SSP-SAM-224 的可训练参数量仅为 **18.37M**，远低于基于 MLLM 的方法（如 LISA 的 288M+）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 6 个主流数据集上进行了详尽测试，涵盖了从单目标到多目标、从闭集到开放词汇的全场景。
*   **消融实验**：设计了 7 组消融实验，验证了：
    *   训练策略（预训练与辅助任务的必要性）。
    *   SAM 解码器是否冻结的影响。
    *   各个组件（V.A.D, L.A.D, P.G.）的贡献。
    *   提示生成器的类型、层数及 Token 数量。
*   **客观性与公平性**：实验对比了不同分辨率（224/336）和不同 CLIP 后干网络（EVA-CLIP, SigLIP2），证明了方法的通用性。通过 Pr@0.9 等严格指标展示了其在掩码质量上的绝对优势。

### 6. 主要结论与发现
*   **性能领先**：SSP-SAM 在 RefCOCO 系列和 gRefCOCO 上均达到了 SOTA 水平，尤其在严格的 **Pr@0.9** 指标上大幅领先（比 CGFormer 高出约 9%）。
*   **天然支持 GRES**：无需针对多目标或无目标场景做特殊架构修改，模型即可通过 SSP 自动处理复杂指代。
*   **高效推理**：推理速度比基于 MLLM 的方法快 50 倍以上，且参数量极小。
*   **高质量掩码**：定性分析显示，SSP-SAM 生成的掩码边界比人工标注（Ground Truth）更精细（如能准确分割咖啡杯把手、动物尾巴等细节）。

### 7. 优点（亮点）
*   **轻量且高效**：通过简单的适配器结构实现了强大的多模态引导，避免了引入大语言模型的沉重负担。
*   **高精度分割**：充分利用了 SAM 的空间感知能力，生成的掩码质量极高，解决了传统 RES 方法边缘模糊的问题。
*   **泛化能力强**：在 PhraseCut 上的零样本实验证明了其优秀的开放词汇分割能力。
*   **辅助任务设计巧妙**：通过 REC 任务盘活了大量的边界框标注数据，弥补了分割标注稀缺的短板。

### 8. 不足与局限
*   **对空间词的依赖**：在 RefCOCO+（禁止使用方位词）上的提升幅度相对较小，说明模型在纯外观推理上仍有进步空间。
*   **复杂逻辑理解**：在处理包含否定逻辑（如“没有云的天空”）或极其复杂的长句时，仍可能出现定位偏差。
*   **失败案例**：在开放词汇场景下，面对极多目标（如“许多自行车”）或局部描述（如“黑头发”）时，分割精度会有所下降。

（完）
