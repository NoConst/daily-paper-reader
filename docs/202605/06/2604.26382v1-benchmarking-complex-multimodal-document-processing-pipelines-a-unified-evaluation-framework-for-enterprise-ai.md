---
title: "Benchmarking Complex Multimodal Document Processing Pipelines: A Unified Evaluation Framework for Enterprise AI"
title_zh: 评测复杂多模态文档处理流水线：一种面向企业级 AI 的统一评估框架
authors: "Saurabh K. Singh, Sachin Raj"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26382v1"
tags: ["query:cir-ml"]
score: 7.5
evidence: 评估密集嵌入和混合检索流水线
tldr: 本研究针对企业级多模态文档处理流水线（RAG）缺乏整体评估的问题，提出了EnterpriseDocBench框架。该框架涵盖解析、索引、检索和生成四个阶段，并在六个企业领域语料上测试了BM25、稠密向量及混合检索流水线。研究发现混合检索表现最优，但各阶段质量关联性极弱，且系统虽能保证事实准确性，但在回答完整性上存在显著短板。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26382v1/fig-001.webp\", \"caption\": \"Table 1: Public data sources by domain. All sources permit research use and redistribution with attribution. “Has QA” indicates pre-existing annotations. “Pilot n” is the current document count. Domain totals sum to 1,417; the remaining 42 documents in the full 1,459-document corpus are cross-domain or uncategorized. “—” means a domain isn’t yet represented. † Manufacturing (n = 4) is excluded from domain-stratified statistical analyses (§6.1, §6.5) because the sample is too small; we keep it in this table to document the source plan and flag it as a Phase 2 expansion target.\", \"page\": 6, \"index\": 1, \"width\": 1126, \"height\": 401}, {\"url\": \"assets/figures/arxiv/2604.26382v1/fig-002.webp\", \"caption\": \"Table 4: Hyperparameter specifications. First three columns are implemented and empirically evaluated; columns marked † are reference architectures not yet integrated. Agentic Routing (†) is omitted from the table because the design is preliminary; see notes below. Temperature = 0.7 was chosen to simulate realistic enterprise deployment, where some response variation is acceptable. Standard factual-QA benchmarks typically use 0.0–0.1, which would reduce stochastic hallucination\", \"page\": 16, \"index\": 2, \"width\": 1021, \"height\": 354}]"
motivation: 现有的企业文档AI研究多关注单一环节，缺乏对从解析到生成的整个处理流水线的系统性评估框架。
method: 开发了EnterpriseDocBench框架，利用跨领域的企业文档语料，对不同检索策略结合GPT-5生成的端到端性能进行多维度量化评估。
result: "混合检索在性能上略优于BM25，且发现各阶段间的质量关联性极低，系统在回答准确率达85.5%的同时，完整性得分仅为0.40。"
conclusion: 评估结果表明流水线质量并非简单的线性级联，企业应用中回答的完整性比单纯的准确性更值得关注。
---

## 摘要
现今大多数企业级文档 AI 均采用流水线架构，包括解析、索引、检索和生成四个阶段。虽然每个阶段都已被深入研究，但对整个系统进行整体评估仍然极具挑战。为此，我们构建了 EnterpriseDocBench，旨在同一语料库上评估解析保真度、索引效率、检索相关性和生成可靠性。该语料库由来自六个企业领域的公开且获得许可的文档构建（当前试点阶段涵盖五个领域）。我们测试了三条流水线——BM25、稠密嵌入（dense embedding）和混合模式，均配合相同的 GPT-5 生成器。主要实验结果显示：混合检索以微弱优势领先于 BM25（nDCG@5 分别为 0.92 和 0.91），且两者均优于稠密嵌入（0.83）。幻觉现象并不随文档长度单调增长：短文档和极长文档的幻觉率均高于中等长度文档（分别为 28.1% 和 23.8%，而中等长度为 9.2%）。跨阶段相关性非常微弱：解析到检索的 r=0.14，解析到生成的 r=0.17，检索到生成的 r=0.02。如果质量如普遍假设的那样呈级联式影响，这些数值应当更高，但事实并非如此。考虑到设计局限性（如解析方式固定、生成器共享、采用自动化代理指标），我们并未夸大研究结果。一个令人惊讶的发现是：陈述性主张的事实准确率达 85.5%，但回答完整性平均仅为 0.40。这意味着系统在回答时虽然准确，但存在内容遗漏，这种差距在实际部署中比单纯的准确率指标更为关键。此外，我们还描述了三种尚未实现端到端集成的参考架构（ColPali、ColQwen2 以及基于代理复杂度的路由）。相关框架、指标、基准和采集脚本将在论文被接收后开源。

## Abstract
Most enterprise document AI today is a pipeline. Parse, index, retrieve, generate. Each of those stages has been studied to death on its own -- what's still hard is evaluating the system as a whole.   We built EnterpriseDocBench to take a swing at it: parsing fidelity, indexing efficiency, retrieval relevance, and generation groundedness, all on the same corpus. The corpus is built from public, permissively licensed documents across six enterprise domains (five represented in the current pilot). We ran three pipelines through it -- BM25, dense embedding, and a hybrid -- all with the same GPT-5 generator.   The headline numbers: hybrid retrieval narrowly beats BM25 (nDCG@5 of 0.92 vs. 0.91), and both beat dense embedding (0.83). Hallucination doesn't grow monotonically with document length -- short documents and very long ones both hallucinate more than medium ones (28.1% and 23.8% vs. 9.2%). Cross-stage correlations are very weak: parsing->retrieval r=0.14, parsing->generation r=0.17, retrieval->generation 0.02. If quality were cascading the way most of us assume, those numbers would be much higher; they aren't. Design caveats are real (parsing fixed, generator shared, automated proxy metrics) and we don't oversell the result.   One result that genuinely surprised us: factual accuracy on stated claims is 85.5%, but answer completeness averages 0.40. The system is right when it answers -- it just leaves things out. That gap matters more for real deployments than the headline accuracy number does.   We also describe three reference architectures (ColPali, ColQwen2, agentic complexity-based routing) which are not yet integrated end-to-end. Framework, metrics, baselines, and collection scripts will be released open-source on acceptance.