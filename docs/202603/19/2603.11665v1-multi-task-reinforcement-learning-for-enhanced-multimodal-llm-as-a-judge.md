---
title: Multi-Task Reinforcement Learning for Enhanced Multimodal LLM-as-a-Judge
title_zh: 用于增强型多模态大语言模型评测器的多任务强化学习
authors: "Junjie Wu, Xuan Kan, Zihao He, Shunwen Tan, Bo Pan, Kaitai Zhang"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11665v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大语言模型优化与评估
tldr: 针对多模态大模型（MLLM）作为裁判时在多任务场景下泛化能力不足的问题，本文提出了MT-RL-Judge框架。该框架利用多任务强化学习（MT-RL）同时在多个任务上优化裁判模型，显著提升了判断的一致性以及与人类偏好的一致性。实验证明，该方法在分布外任务上表现出极强的鲁棒性和泛化能力，为构建更可靠的多模态评估工具提供了新思路。
selection_source: fresh_fetch
motivation: 现有的多模态裁判模型多针对单一任务优化，难以在多样化的评估语境中保持良好的泛化能力。
method: 提出MT-RL-Judge框架，通过多任务强化学习联合优化裁判模型，以增强其跨任务的泛化性能。
result: 实验结果显示，MT-RL-Judge在判断一致性和人类偏好相关性方面均优于强基线模型。
conclusion: 该研究证明了多任务强化学习能有效提升多模态裁判模型的可靠性及其在未知任务上的泛化表现。
---

## 摘要
多模态大语言模型（MLLMs）因其在各种视觉任务中与人类判断的高度一致性，已被广泛用作多模态大语言模型评测器（MLLM-as-a-Judge）。然而，大多数现有的评测模型针对单任务场景进行了优化，难以泛化到多样化的语境中，而这正是可靠评估的关键要求。为了解决这一局限性，我们提出了用于多模态大语言模型评测器的多任务强化学习（MT-RL-Judge），这是一个利用强化学习（RL）的泛化能力，在多个任务上联合优化评测模型的框架。与多个强基准模型的对比实验结果表明，MT-RL-Judge 在判断一致性以及与人类偏好的相关性方面均优于强基准模型。此外，我们的方法在分布外（OOD）任务上表现出鲁棒的泛化能力，进一步验证了其有效性。

## Abstract
Multimodal Large Language Models (MLLMs) have been widely adopted as MLLM-as-a-Judges due to their strong alignment with human judgment across various visual tasks. However, most existing judge models are optimized for single-task scenarios and struggle to generalize to diverse contexts, which is a critical requirement for reliable evaluation. To address this limitation, we propose Multi-Task Reinforcement Learning for MLLM-as-a-Judge (MT-RL-Judge), a framework that jointly optimizes the judge model across multiple tasks, leveraging the generalization capabilities of RL. Experimental results against several strong baselines demonstrate that MT-RL-Judge outperforms strong baselines in both judgment consistency and correlation with human preferences. Furthermore, our approach exhibits robust generalization on out-of-distribution tasks, further validating its effectiveness.

---

## 论文详细总结（自动生成）

这是一份关于论文《Multi-Task Reinforcement Learning for Enhanced Multimodal LLM-as-a-Judge》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：随着多模态大模型（MLLM）生成内容的爆发，如何高效、低成本地评估这些内容的质量和安全性成为工业界的瓶颈。人工评估成本高昂，而“MLLM-as-a-Judge”（以模型作为裁判）成为主流趋势。
*   **核心问题**：
    1.  **泛化性差**：现有的裁判模型多针对特定任务（如仅针对图像质量或仅针对安全合规）进行微调，难以应对多样化的评估场景。
    2.  **格式过拟合**：通过监督微调（SFT）训练的模型往往只是记住了特定的指令格式（如逐点评分），当输入格式稍作改变（如变为成对比较）时，性能会剧烈下降。
    3.  **缺乏推理逻辑**：SFT 模型倾向于学习输入与标签之间的表面统计关联，而非内在的评估逻辑。

### 2. 论文提出的方法论：MT-RL-Judge
*   **核心思想**：提出一个统一的多任务强化学习框架，通过联合优化多个评估任务，并引入“先推理后判断”的机制，使模型内化评估逻辑而非死记硬背模板。
*   **关键技术细节**：
    *   **统一架构**：将图文对齐、安全检测、视觉质量等多种任务整合进一个模型，降低工业部署的维护成本。
    *   **强化学习算法**：采用 **GRPO（Group Relative Policy Optimization）** 算法。该算法通过在一组生成的输出中计算相对奖励，无需额外的价值函数（Value Function），显著降低了训练开销。
    *   **奖励函数设计**：
        *   **格式奖励 ($R_{For}$)**：鼓励模型遵循“推理-结论”的结构（即必须先写 `<thinking>` 过程）。
        *   **准确率奖励 ($R_{Acc}$)**：基于模型最终判断是否与人类标注一致给出 0/1 奖励。
        *   **复合奖励**：$R_{total} = (1 - \alpha) \cdot R_{Acc} + \alpha \cdot R_{For}$。
    *   **推理增强**：强制模型在给出最终 Verdict 之前生成推理链（Reasoning Trace），提升了判断的可靠性和可解释性。

### 3. 实验设计
*   **数据集与场景**：涵盖三大类能力，共 6 个基准数据集：
    *   **图文对齐**：SeeTRUE, ImageReward。
    *   **安全合规**：UnsafeBench。
    *   **视觉质量**：AGIN-Naturalness（自然度）, AGIN-Technical（技术质量）, AGIN-Rationality（合理性）。
*   **分布外（OOD）测试**：使用 **MJ-Bench**。该测试要求模型进行“成对比较”（Pairwise），而训练数据全是“逐点评估”（Pointwise），以此验证模型的逻辑迁移能力。
*   **对比方法**：
    *   Off-the-shelf（开箱即用的 Qwen3-VL 提示词工程）。
    *   SFT-Single（单任务监督微调）。
    *   SFT-Unified（多任务混合监督微调）。
    *   RL-Single（单任务强化学习）。

### 4. 资源与算力
*   **底座模型**：Qwen3-VL-30B-A3B-Instruct。
*   **训练框架**：SFT 阶段使用 LLaMA-Factory，RL 阶段使用 EasyR1。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到了使用了 Flash Attention 2 技术，且 SFT 阶段采用了全参数微调。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个核心任务上进行了完整的对比实验，并额外增加了 MJ-Bench 的泛化性实验。
*   **充分性评价**：
    *   **客观性**：对比了 SFT 和 RL 在单任务与多任务下的表现，证明了多任务协同并非简单的性能堆砌，而是有相互促进作用。
    *   **公平性**：所有对比方法均基于相同的 Qwen3-VL 底座，确保了架构一致性。
    *   **局限性**：虽然覆盖了主流视觉评估维度，但对于视频评估或更复杂的长文本-多图评估尚未涉及。

### 6. 主要结论与发现
1.  **RL 优于 SFT**：强化学习通过激励推理过程，在 SeeTrue 和 AGIN-Rat 等逻辑密集型任务上显著超过了 SFT。
2.  **多任务协同效应**：统一训练（Unified Training）不仅没有降低性能，反而通过跨领域的知识共享提升了模型对评估准则的理解。
3.  **极强的泛化能力**：在 MJ-Bench 的 OOD 测试中，SFT 模型在面对未见的成对比较格式时性能崩溃（甚至低于零样本模型），而 MT-RL-Judge 依然保持高准确率，证明其掌握了“评估逻辑”而非“模板”。

### 7. 优点
*   **工业友好**：一个模型解决所有评估问题，大幅简化了生产环境的推理流水线。
*   **解决过拟合**：通过 GRPO 和推理链设计，有效解决了大模型在微调中容易产生的“指令遵循过拟合”问题。
*   **可解释性**：强制生成的推理步骤为自动化评估提供了透明度，便于人工回溯。

### 8. 不足与局限
*   **奖励函数单一**：目前仅采用 0/1 奖励，对于某些具有主观性、连续性的评估维度（如美学评分），这种硬性分类奖励可能不够精细。
*   **推理开销**：虽然 GRPO 训练高效，但在推理阶段生成长推理链会增加响应延迟和计算成本。
*   **算力信息缺失**：论文未披露具体的训练资源消耗，不便于其他研究者评估复现成本。

（完）
