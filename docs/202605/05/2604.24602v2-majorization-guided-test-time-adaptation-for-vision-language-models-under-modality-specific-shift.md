---
title: Majorization-Guided Test-Time Adaptation for Vision-Language Models under Modality-Specific Shift
title_zh: 模态特定偏移下视觉-语言模型的优序指导测试时自适应
authors: "Lixian Chen, Yanhui Chen, Junyi Lin"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24602v2"
tags: ["query:cir-ml"]
score: 9.5
evidence: 解决融合中的模态特定偏移和不可靠模态主导问题
tldr: 本研究针对视觉语言模型在部署时面临的模态非对称偏移问题，指出传统的基于熵的测试时自适应（TTA）可能因不可靠模态主导融合而失效。为此，提出基于主化理论的MG-MTTA方法，通过更新轻量级门控并结合可靠性感知先验，有效解决了模态主导导致的错误。实验证明，该方法在文本偏移和联合偏移场景下显著提升了模型准确率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24602v2/fig-001.webp\", \"caption\": \"Figure 1: Motivation. Under modality-specific shift, biased fusion can produce a sharper but less reliable posterior. Entropy-only adaptation may further amplify an incorrect fused decision, whereas MG-MTTA uses reliability and conflict cues to guide the fusion gate toward a more reliable adaptation direction.\", \"page\": 2, \"index\": 1, \"width\": 967, \"height\": 521}, {\"url\": \"assets/figures/arxiv/2604.24602v2/fig-002.webp\", \"caption\": \"Table 11: Failure-oriented objective ablation under the textual and joint L5 strongest probes. Accuracy is reported as global top-1 accuracy (%). Majorization reports raw averaged majorization ratios; for Source-only, the value equals 1 because no adaptation update is applied and the pre/post posterior is unchanged.\", \"page\": 14, \"index\": 2, \"width\": 490, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2604.24602v2/fig-003.webp\", \"caption\": \"Figure 5: Case-wise analysis under the L5 strongest probe. The examples show recovery under severe textual stress, a lowerentropy but still incorrect prediction, and recovery under high cross-modality conflict. They distinguish genuine recovery from confidence amplification in the modality-dominance regime described in Sec. 3.5.\", \"page\": 8, \"index\": 3, \"width\": 1032, \"height\": 320}, {\"url\": \"assets/figures/arxiv/2604.24602v2/fig-004.webp\", \"caption\": \"Table 7: Level-wise results under textual and joint stress. Textual and Joint report level-wise top-1 accuracy (%) for L1–L4. Textual Avg. and Joint Avg. are arithmetic means over L1–L4, and Avg. is the mean of these two averages. The majorization columns report raw L1–L4 averages of the majorization ratio, rather than values normalized to Source-only. We do not report a unified L1–L4 conflict-error aggregate here, because conflict error is used only in the strongest-probe analysis.\", \"page\": 12, \"index\": 4, \"width\": 960, \"height\": 181}, {\"url\": \"assets/figures/arxiv/2604.24602v2/fig-005.webp\", \"caption\": \"Table 8: Hyperparameter ranges screened in pilot runs and final values used in the main experiments.\", \"page\": 12, \"index\": 5, \"width\": 506, \"height\": 528}, {\"url\": \"assets/figures/arxiv/2604.24602v2/fig-006.webp\", \"caption\": \"Figure 2: Overview of MG-MTTA. A frozen vision-language backbone produces modality-level predictions from shifted visual and textual inputs. Running anchors and cross-modality conflict define a reliability-aware prior for a lightweight gate or adapter. The fused prediction is obtained by logit-level convex fusion followed by softmax, and only the lightweight module is updated with entropy, gate regularization, and diversity objectives.\", \"page\": 5, \"index\": 6, \"width\": 1009, \"height\": 363}]"
motivation: 传统的基于熵的测试时自适应在视觉语言模型面临非对称模态偏移时，容易因不可靠模态主导融合而导致预测错误。
method: 提出MG-MTTA方法，将自适应视为受约束的去混合问题，通过更新轻量级门控并结合可靠性感知先验来优化融合预测。
result: 在ImageNet基准测试中，该方法在文本偏移和联合偏移场景下均显著提升了Top-1准确率，同时保持了视觉基准的竞争力。
conclusion: 多模态测试时自适应的关键在于控制模态可靠性，而非仅仅追求降低预测熵。
---

## 摘要
视觉-语言模型在零样本设置下迁移效果良好，但在部署时，视觉和文本分支往往会发生非对称偏移。在这种情况下，基于熵的测试时自适应可能会在增加误差的同时使融合后的后验分布变得尖锐，因为不可靠的模态仍可能主导融合过程。我们通过多模态后验的优序视角研究了这种失效模式，并将自适应建模为融合预测上的约束解混问题。基于这一视角，我们提出了 MG-MTTA，它保持骨干网络冻结，仅更新轻量级门控或适配器。该目标函数结合了融合后验熵最小化与可靠性感知门控先验，后者构建自基于锚点的模态一致性和跨模态冲突。我们的分析给出了熵减保持正确排名的条件，以及表征模态主导失效的阈值。在基于 ImageNet 的基准测试中，MG-MTTA 在保持语义的文本偏移下将 top-1 准确率从 57.97 提升至 66.51，在视觉-文本联合偏移下从 21.68 提升至 26.27，同时在纯视觉基准测试中保持了竞争力。这些结果表明，多模态测试时自适应应当控制模态可靠性，而不仅仅是预测熵。

## Abstract
Vision-language models transfer well in zero-shot settings, but at deployment the visual and textual branches often shift asymmetrically. Under this condition, entropy-based test-time adaptation can sharpen the fused posterior while increasing error, because an unreliable modality may still dominate fusion. We study this failure mode through a majorization view of multimodal posteriors and cast adaptation as a constrained de-mixing problem on the fused prediction. Based on this view, we propose MG-MTTA, which keeps the backbone frozen and updates only a lightweight gate or adapter. The objective combines fused-posterior entropy minimization with a reliability-aware gate prior built from anchor-based modality consistency and cross-modal conflict. Our analysis gives conditions under which entropy reduction preserves the correct ranking and a threshold that characterizes modality-dominance failure. On the ImageNet-based benchmark, MG-MTTA improves top-1 accuracy from 57.97 to 66.51 under semantics-preserving textual shift and from 21.68 to 26.27 under joint visual-textual shift, while remaining competitive in the visual-only benchmark. These results show that multimodal test-time adaptation should control modality reliability, not just prediction entropy.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **MG-MTTA**（Majorization-Guided Multimodal Test-Time Adaptation）的方法，旨在解决视觉-语言模型（VLM）在测试时面临的非对称模态偏移问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在实际部署中，视觉和文本模态往往会发生不同程度的偏移（如视觉模糊、文本表达变体）。传统的基于**熵最小化**的测试时自适应（TTA）在多模态场景下会失效：如果一个模态变得不可靠但其预测分布依然“尖锐”，熵最小化会进一步放大这种错误的置信度，导致不可靠模态主导融合决策。
*   **研究动机**：作者试图从**优序理论（Majorization Theory）**的角度重新审视多模态后验分布的几何特性，将自适应过程建模为一个受约束的“去混合（De-mixing）”问题，从而在降低预测不确定性的同时，确保融合过程向更可靠的模态倾斜。

### 2. 方法论
*   **核心思想**：将观测到的偏移后验分布视为原始“干净”后验分布通过**双倍随机矩阵（Doubly Stochastic Matrix）**混合后的结果。自适应的目标是逆转这一混合过程。
*   **关键技术细节**：
    *   **轻量级更新**：保持预训练骨干网络（如 CLIP）冻结，仅更新一个轻量级的融合门控（Gate）或适配器（Adapter）。
    *   **可靠性感知门控先验**：构建一个先验分布来指导门控权重。该先验由两个信号组成：
        1.  **基于锚点的一致性**：维护每个模态的运行锚点（Running Anchors），通过计算当前预测与锚点之间的距离（基于排序后的分布 profile）来评估模态可靠性。
        2.  **跨模态冲突**：利用 Jensen-Shannon 散度和排名不一致性来衡量视觉与文本分支之间的冲突，并据此调整门控。
    *   **优化目标**：损失函数由三部分组成：
        1.  **融合熵最小化**：使最终预测更清晰。
        2.  **门控正则化**：将学习到的门控权重推向可靠性先验。
        3.  **多样性损失**：防止批次预测坍缩到单一类别。
*   **理论支撑**：论文推导了熵减能够保持正确排名的数学条件，并给出了一个表征“模态主导失效”的阈值。

### 3. 实验设计
*   **数据集与场景**：
    *   **视觉偏移**：CIFAR-100C 和 ImageNet-C（15 种常见损坏）。
    *   **文本偏移**：构建了 L1-L5 五个等级的语义保持型文本压力测试（如字符替换、格式变化）。
    *   **联合偏移**：同时施加视觉损坏和文本偏移。
*   **基准（Benchmark）**：以 ImageNet 标签空间为核心的统一评估协议。
*   **对比方法**：
    *   **内部基线**：Source-only（无自适应）、Entropy-only（仅熵最小化）、Entropy + Diversity。
    *   **外部基线**：TENT、SAR、TPT、VTE、BATCLIP 等针对视觉或提示词自适应的方法。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **效率暗示**：由于该方法仅更新极少数参数（门控或适配器），且骨干网络冻结，其计算开销远低于全参数微调，符合测试时自适应（TTA）对实时性的要求。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 15 种视觉损坏、5 个等级的文本偏移以及它们的组合，实验覆盖面较广。
*   **消融实验**：对损失函数的各个组件（熵、门控正则、多样性）以及更新范围（仅门控、仅适配器等）进行了详细消融。
*   **泛化性验证**：在 CLIP ViT-B/16 和 ViT-B/32 两种骨干网络上均进行了验证。
*   **客观性**：作者专门区分了“语义保持偏移”和“语义冲突偏移”，并将后者作为失效分析而非主榜单，这种分类评估增强了实验的科学性和公平性。

### 6. 主要结论与发现
*   **熵最小化的局限性**：在多模态非对称偏移下，单纯降低熵往往会导致性能下降（如文本偏移下 Entropy-only 表现差于 Source-only）。
*   **可靠性控制的重要性**：MG-MTTA 在文本偏移（+8.54%）和联合偏移（+4.59%）中显著优于基线，证明了引入可靠性先验的必要性。
*   **理论一致性**：实验结果验证了优序理论的预测，即只有当自适应方向与“去混合”方向一致时，熵减才是有益的。

### 7. 优点
*   **理论深刻**：引入优序理论和双倍随机混合模型，为多模态融合失效提供了严谨的数学解释。
*   **针对性强**：精准捕捉到了 VLM 在现实部署中常见的“非对称偏移”痛点。
*   **轻量高效**：仅需更新极少量参数，易于集成到现有冻结权重的预训练模型中。
*   **诊断工具**：提出了主要化比例（Majorization ratio）等指标，为分析 TTA 行为提供了新工具。

### 8. 不足与局限
*   **视觉偏移提升有限**：在纯视觉损坏场景下，MG-MTTA 虽然保持了竞争力，但相比于专门针对视觉 TTA 的 SOTA 方法（如 BATCLIP），提升并不显著。
*   **依赖运行锚点**：锚点的准确性受批次大小和初始置信度的影响，在极小批次或极高初始噪声下可能不够稳健。
*   **超参数敏感性**：虽然给出了推荐配置，但可靠性温度 $\tau$ 和冲突权重 $\lambda_c$ 等参数在不同分布下可能需要微调。

（完）
