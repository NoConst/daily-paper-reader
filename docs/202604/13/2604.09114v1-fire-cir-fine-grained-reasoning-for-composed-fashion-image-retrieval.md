---
title: "FIRE-CIR: Fine-grained Reasoning for Composed Fashion Image Retrieval"
title_zh: FIRE-CIR：面向组合式时尚图像检索的细粒度推理
authors: "François Gardères, Camille-Sovanneary Gauthier, Jean Ponce, Shizhe Chen"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09114v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 引入 FIRE-CIR 用于时尚组合图像检索中的组合推理
tldr: 针对组合图像检索（CIR）中模型难以区分“保留”与“改变”属性的问题，本文提出 FIRE-CIR 模型。该模型引入了细粒度的视觉推理机制，通过自动生成属性相关的视觉问题并验证图像中的视觉证据，实现了对检索结果的显式重排序。此外，研究者构建了大规模时尚 VQA 数据集以训练该推理系统。实验证明，FIRE-CIR 在 Fashion IQ 榜单上达到 SOTA 水平，并提供了具有解释性的属性级检索依据。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在处理时尚图像检索时，缺乏对修改文本中“保留哪些、改变哪些”属性的显式推理能力，导致结果欠佳且缺乏解释性。
method: 采用问题驱动的视觉推理，通过生成属性焦点问题并对比参考图与候选图的视觉证据，结合新构建的时尚 VQA 数据集进行训练和重排序。
result: 在 Fashion IQ 基准测试中，FIRE-CIR 的检索精度优于当前所有主流方法，并能提供细粒度的属性级推理反馈。
conclusion: 显式视觉推理是提升细粒度组合图像检索性能和透明度的有效途径，为时尚领域的精准检索奠定了基础。
---

## 摘要
组合式图像检索 (CIR) 旨在检索一张目标图像，该图像描绘了根据文本描述修改后的参考图像。尽管近期的视觉语言模型 (VLM) 通过将图像和文本嵌入到共享空间进行检索，在 CIR 任务上取得了显著进展，但它们往往无法推理出哪些部分需要保留，哪些部分需要改变。这一局限性阻碍了模型的可解释性，并导致结果不尽如人意，尤其是在时尚等细粒度领域。在本文中，我们提出了 FIRE-CIR，这是一个为时尚 CIR 引入组合推理和可解释性的模型。FIRE-CIR 不再仅仅依赖嵌入相似度，而是执行问题驱动的视觉推理：它根据修改文本自动生成关注属性的视觉问题，并在参考图像和候选图像中验证相应的视觉证据。为了训练这种推理系统，我们自动构建了一个大规模的时尚领域专用视觉问答数据集，其中包含需要单图像或双图像分析的问题。在检索过程中，我们的模型利用这种显式推理对候选结果进行重排序，过滤掉与预期修改不符的图像。在 Fashion IQ 基准测试上的实验结果表明，FIRE-CIR 在检索准确率上优于现有最先进的方法。它还为检索决策提供了可解释的、属性级的见解。

## Abstract
Composed image retrieval (CIR) aims to retrieve a target image that depicts a reference image modified by a textual description. While recent vision-language models (VLMs) achieve promising CIR performance by embedding images and text into a shared space for retrieval, they often fail to reason about what to preserve and what to change. This limitation hinders interpretability and yields suboptimal results, particularly in fine-grained domains like fashion. In this paper, we introduce FIRE-CIR, a model that brings compositional reasoning and interpretability to fashion CIR. Instead of relying solely on embedding similarity, FIRE-CIR performs question-driven visual reasoning: it automatically generates attribute-focused visual questions derived from the modification text, and verifies the corresponding visual evidence in both reference and candidate images. To train such a reasoning system, we automatically construct a large-scale fashion-specific visual question answering dataset, containing questions requiring either single- or dual-image analysis. During retrieval, our model leverages this explicit reasoning to re-rank candidate results, filtering out images inconsistent with the intended modifications. Experimental results on the Fashion IQ benchmark show that FIRE-CIR outperforms state-of-the-art methods in retrieval accuracy. It also provides interpretable, attribute-level insights into retrieval decisions.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **FIRE-CIR**，一种专为时尚领域设计的组合式图像检索（Composed Image Retrieval, CIR）模型，旨在通过细粒度的视觉推理解决现有模型在处理“保留什么”与“改变什么”时的模糊性问题。

以下是对该论文的结构化总结：

### 1. 论文的核心问题与研究动机
*   **核心问题**：现有的 CIR 方法（如基于 CLIP 的模型）主要依赖全局嵌入相似度，难以精确捕捉修改文本中的细粒度属性变化。
*   **研究动机**：
    *   **缺乏区分度**：模型往往无法准确区分参考图中哪些特征应保留，哪些应按文本修改，导致检索结果虽然视觉相似但属性错误。
    *   **缺乏可解释性**：黑盒式的特征融合无法解释为何某张图像被排在前面。
    *   **领域局限性**：通用 VLM 在时尚领域的专业词汇和细微视觉差异上表现欠佳。

### 2. 方法论
FIRE-CIR 采用**问题驱动的视觉推理**框架，将检索任务转化为一系列显式的验证过程：
*   **视觉问题生成 (VQG)**：利用大语言模型（如 GPT-5-mini）将修改文本分解为多个“是/否”问题。每个问题针对一个特定属性，并自动判断该问题是仅需观察候选图（单图分析），还是需要对比参考图与候选图（双图分析，如“裙子是否比参考图更长”）。
*   **时尚专用 VQA 模型**：
    *   **模型架构**：基于 InternVL-3-1B，使用 LoRA（低秩自适应）进行微调。
    *   **数据集构建**：自动构建了一个包含 440 万个问答对的大规模时尚 VQA 数据集。为了克服 VQA 中的语言偏见，通过大模型自动标注，确保了“是”与“否”答案的严格平衡。
*   **推理与重排序**：
    *   **VQA 评分**：计算候选图像满足所有生成问题的平均概率。
    *   **分数融合**：将原始 CIR 模型的相似度分数与 VQA 推理分数结合。引入了一个类 Sigmoid 函数 $\sigma_k$ 来增强对 VQA 预测误差的鲁棒性。
    *   **插件化设计**：该推理模块可作为插件集成到任何现有的 CIR 模型中，对前 $n$ 个候选结果进行重排序。

### 3. 实验设计
*   **数据集**：
    *   **Fashion IQ**：标准的时尚 CIR 基准。
    *   **Refined-FashionIQ** & **enhFashionIQ**：针对 Fashion IQ 标注噪声进行改进和增强的衍生数据集。
*   **Benchmark 与对比方法**：
    *   对比了 SQUARE、ReRanking、DetailFusion、CLIP4CIR、SPRC、FashionBLIP-2 等 SOTA 方法。
    *   特别对比了同类重排序模型 VQA4CIR。
*   **评价指标**：Recall@10、Recall@50 和平均倒数排名 (MRR)。

### 4. 资源与算力
*   **训练算力**：使用了 **2 台 NVIDIA H100 GPU** 进行微调，Batch Size 为 512。
*   **推理算力**：推理实验在 **1 台 NVIDIA H100 GPU** 上完成。
*   **效率指标**：重排序 250 张候选图时，每条查询耗时约 9.5 秒（单卡）；若减少重排序数量至 70 张，耗时可降至 2.65 秒，同时保持显著的性能提升。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了广泛的定量评估，涵盖了三个不同的数据集和三种服装类别（连衣裙、衬衫、上衣）。
*   **消融实验**：
    *   验证了多问题分解、VQA 微调、分数融合方法各自的贡献。
    *   测试了不同 VLM 主干网络（LLaVA vs. InternVL）的影响。
    *   分析了训练数据量对模型性能的增长曲线。
*   **充分性评价**：实验设计客观且公平，通过在多个增强数据集上的测试，证明了模型在处理高质量标注时的潜力，消融实验充分支撑了各模块的有效性。

### 6. 主要结论与发现
*   **性能领先**：FIRE-CIR 在 Fashion IQ 上达到了 SOTA 水平，显著提升了 Recall@10 和 MRR。
*   **双图推理的重要性**：约 38% 的视觉问题涉及双图对比，证明了显式对比参考图与候选图对 CIR 任务至关重要。
*   **通用性**：作为重排序插件，FIRE-CIR 能够一致性地提升 CLIP4CIR、SPRC 和 FashionBLIP-2 等不同基础模型的表现。
*   **可解释性**：通过展示 VQA 的回答轨迹，用户可以清晰看到模型是因为哪个属性（如颜色、袖长、图案）而选择了某张图。

### 7. 优点
*   **细粒度推理**：将模糊的文本修改转化为具体的属性验证，极大地提高了检索的精确度。
*   **鲁棒的数据集构建**：通过自动标注和平衡策略，解决了 VQA 训练中常见的语言先验偏见问题。
*   **灵活性**：支持在推理速度和检索精度之间进行权衡（通过调整重排序数量 $n$）。
*   **高度透明**：提供了属性级的推理反馈，这在时尚电商应用中具有极高的实用价值。

### 8. 不足与局限
*   **推理延迟**：相比于纯嵌入检索，VQA 重排序增加了计算开销，在大规模实时检索场景下可能需要更多的硬件资源或更精细的级联策略。
*   **LLM 依赖**：问题生成阶段依赖 LLM，虽然文中显示幻觉率较低（6%），但在极端复杂的长文本描述下，仍可能存在属性遗漏或误解。
*   **属性覆盖**：目前的推理主要关注修改点，未来若能加入对“未修改属性”的负向约束（即确保不该变的地方也没变），效果可能更佳。

（完）
