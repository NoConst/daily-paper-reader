---
title: "FileGram: Grounding Agent Personalization in File-System Behavioral Traces"
title_zh: FileGram：基于文件系统行为轨迹的智能体个性化
authors: "Shuai Liu, Shulin Tian, Kairui Hu, Yuhao Dong, Zhe Yang, Bo Li, Jingkang Yang, Chen Change Loy, Ziwei Liu"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04901v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 将智能体记忆和个性化建立在行为轨迹基础上
tldr: 针对本地文件系统AI代理在个性化方面面临的数据匮乏和隐私限制问题，本文提出FileGram框架。该框架包含用于模拟真实工作流的数据引擎FileGramEngine、用于评估记忆系统的诊断基准FileGramBench，以及基于原子操作和内容增量构建用户画像的底层记忆架构FileGramOS。研究填补了文件系统行为追踪在代理个性化中的空白，为开发更懂用户的协作AI提供了新思路。
selection_source: fresh_fetch
motivation: 现有个性化方法过度依赖对话交互，忽视了文件系统中丰富的行为轨迹，且受限于隐私和多模态数据获取的难度。
method: 提出了包含数据模拟引擎、诊断性基准测试和基于原子动作及内容增量的三通道记忆架构的综合框架。
result: 实验表明FileGramBench对现有记忆系统极具挑战性，而FileGramEngine和FileGramOS在用户画像重建和行为追踪方面表现出色。
conclusion: FileGram通过将代理个性化植根于文件系统行为轨迹，为构建具备长期记忆和深度个性化的文件系统代理奠定了基础。
---

## 摘要
在本地文件系统中运行的协同 AI 智能体正迅速成为人机交互的一种范式；然而，由于严格的隐私壁垒以及联合采集多模态真实世界轨迹的困难阻碍了大规模的训练与评估，有效的个性化仍受限于严重的数据约束，且现有方法仍以交互为中心，忽视了文件系统操作中密集的行为轨迹。为填补这一空白，我们提出了 FileGram，这是一个将智能体记忆与个性化锚定在文件系统行为轨迹中的综合框架，包含三个核心组件：(1) FileGramEngine，一个可扩展的人格驱动数据引擎，能够模拟真实的流程并大规模生成细粒度的多模态动作序列；(2) FileGramBench，一个基于文件系统行为轨迹的诊断性基准测试，用于评估记忆系统在画像重构、轨迹解耦、人格漂移检测以及多模态锚定方面的表现；(3) FileGramOS，一种自下而上的记忆架构，它直接从原子动作和内容增量（而非对话摘要）中构建用户画像，并将这些轨迹编码为程序性、语义性和情节性通道，并具备查询时抽象能力。大量实验表明，FileGramBench 对最先进的记忆系统仍具挑战性，且 FileGramEngine 和 FileGramOS 是有效的。通过开源该框架，我们希望支持未来对以记忆为中心的个性化文件系统智能体的研究。

## Abstract
Coworking AI agents operating within local file systems are rapidly emerging as a paradigm in human-AI interaction; however, effective personalization remains limited by severe data constraints, as strict privacy barriers and the difficulty of jointly collecting multimodal real-world traces prevent scalable training and evaluation, and existing methods remain interaction-centric while overlooking dense behavioral traces in file-system operations; to address this gap, we propose FileGram, a comprehensive framework that grounds agent memory and personalization in file-system behavioral traces, comprising three core components: (1) FileGramEngine, a scalable persona-driven data engine that simulates realistic workflows and generates fine-grained multimodal action sequences at scale; (2) FileGramBench, a diagnostic benchmark grounded in file-system behavioral traces for evaluating memory systems on profile reconstruction, trace disentanglement, persona drift detection, and multimodal grounding; and (3) FileGramOS, a bottom-up memory architecture that builds user profiles directly from atomic actions and content deltas rather than dialogue summaries, encoding these traces into procedural, semantic, and episodic channels with query-time abstraction; extensive experiments show that FileGramBench remains challenging for state-of-the-art memory systems and that FileGramEngine and FileGramOS are effective, and by open-sourcing the framework, we hope to support future research on personalized memory-centric file-system agents.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **FileGram**，一个旨在通过文件系统行为轨迹（File-System Behavioral Traces）实现 AI 智能体个性化的综合框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 AI 智能体个性化方法主要以“对话交互”为中心，忽视了用户在文件系统操作中留下的密集行为信号（如文件读取顺序、组织习惯、内容增量等）。
*   **研究动机**：
    *   **数据瓶颈**：由于隐私保护和多模态数据采集困难，缺乏大规模、高质量的真实文件系统操作数据集。
    *   **评估缺失**：现有基准测试侧重于对话召回或孤立的 GUI 任务成功率，缺乏对长期行为模式理解的评估。
    *   **方法论缺陷**：主流记忆架构依赖“自上而下”的对话摘要，容易丢失底层的操作细节（如目录深度、编辑频率）。

### 2. 方法论：FileGram 框架
FileGram 由三个核心组件构成：
*   **FileGramEngine（数据生成引擎）**：
    *   **核心思想**：通过模拟器生成受控的、人格驱动的行为轨迹。
    *   **技术细节**：定义了 20 种具有不同行为维度（如消费模式、生产风格、组织偏好等）的用户画像，在沙盒环境中执行 32 类任务。
    *   **输出**：将原始 Shell 命令抽象为原子动作（Atomic Actions）并记录内容增量（Content Deltas），生成 640 条长程多模态轨迹。
*   **FileGramBench（诊断性基准测试）**：
    *   包含 4.6K 个 QA 对，分为四个轨道：**理解**（画像重构）、**推理**（行为预测）、**检测**（异常与漂移检测）、**多模态锚定**（基于渲染文档和屏幕录像的评估）。
*   **FileGramOS（底层记忆架构）**：
    *   **核心思想**：采用“自下而上”的架构，直接从原子动作中提取特征，而非依赖对话摘要。
    *   **三通道设计**：
        1.  **程序性通道（Procedural）**：提取 17 维行为指纹（如搜索比例、目录深度、编辑幅度），通过统计学方法建立行为基准。
        2.  **语义通道（Semantic）**：利用 VLM 对文件内容和编辑差异进行描述性总结。
        3.  **情节通道（Episodic）**：对操作序列进行语义分段和聚类，识别行为模式的转变或异常。

### 3. 实验设计
*   **数据集/场景**：使用 FileGramEngine 生成的 640 条模拟轨迹，涵盖文本和多模态（PDF、图像、视频、音频）文件。此外，还收集了真实人类的屏幕录像进行“模拟到真实”（Sim-to-Real）的验证。
*   **对比方法**：对比了 12 种方法，包括：
    *   **上下文基准**：Full Context, Naive RAG, VisRAG。
    *   **文本记忆系统**：Mem0, Zep, MemOS, EverMemOS, SimpleMem。
    *   **多模态记忆系统**：MMA, MemU。
*   **评测指标**：准确率（Accuracy）以及基于 LLM Judge 的 Likert 5 分制评分。

### 4. 资源与算力
*   **模型使用**：实验主要使用 **Gemini 2.5-Flash** 作为统一的 QA 后端和视频描述生成器；数据生成使用了 **Claude Haiku 4.5**。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练总时长。由于该方法主要基于推理和启发式特征提取，而非大规模模型预训练，其算力消耗主要集中在 LLM API 的调用上。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 20 个画像、32 个任务、640 条轨迹和 4.6K 个测试问题，规模较大。
*   **消融实验**：对 FileGramOS 的三个记忆通道进行了详细的消融分析，验证了程序性通道在行为识别中的主导作用。
*   **客观性与公平性**：通过交叉模型验证（使用不同 LLM 作为后端）证明了行为信号的有效性与模型无关；使用了严格的过滤机制防止数据泄露（如删除模型名称等元数据）。

### 6. 主要结论与发现
*   **底层架构的优越性**：FileGramOS（59.6%）显著优于最强的叙事摘要类方法 EverMemOS（49.9%）。
*   **摘要导致信息丢失**：传统的“叙事优先”记忆系统在摄取阶段会过早地平滑掉行为特征（如将不同的操作习惯都总结为“高效、有条理”），导致无法区分细微的人格差异。
*   **Sim-to-Real 鸿沟**：所有方法在处理真实人类屏幕录像时准确率均大幅下降（跌至个位数），表明从结构化日志到非结构化视觉信号的转换极具挑战。
*   **检测与解释的差距**：现有系统能检测到行为异常，但很难准确解释行为漂移的具体维度和方向。

### 7. 优点
*   **开创性领域**：首次将智能体个性化研究从对话层面深入到文件系统操作层面。
*   **系统性框架**：提供了从数据生成、基准测试到参考架构的完整闭环。
*   **多通道记忆设计**：通过程序性指纹解决了语义摘要模糊的问题，增强了行为识别的精确度。

### 8. 不足与局限
*   **合成数据偏差**：轨迹由单一 LLM 生成，可能存在风格过于统一的问题，无法完全模拟人类操作的随机性和复杂性。
*   **任务覆盖范围**：目前未涵盖代码开发、实时协作或系统管理等更复杂的文件系统交互场景。
*   **隐私与伦理**：虽然使用了合成数据，但该技术若应用于现实，对用户行为的深度追踪可能引发严重的隐私担忧。
*   **Sim-to-Real 表现差**：在真实视觉环境下的泛化能力尚处于起步阶段。

（完）
