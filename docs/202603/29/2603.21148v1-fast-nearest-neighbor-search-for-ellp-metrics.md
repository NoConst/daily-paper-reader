---
title: Fast Nearest Neighbor Search for $\ell_p$ Metrics
title_zh: $\ell_p$ 度量下的快速最近邻搜索
authors: "Robert Krauthgamer, Nir Petruschka"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21148v1"
tags: ["query:cir-ml"]
score: 7.0
evidence: 针对大规模应用的快速最近邻搜索
tldr: "本研究针对高维 $\\ell_p$ 空间（$p>2$）中的最近邻搜索（NNS）问题，提出了一种新型随机化数据结构。在现代大规模应用中，快速在线查询至关重要，该方法在保证 $\\text{poly}(dn)$ 空间复杂度的前提下，实现了 $\\text{poly}(d \\log n)$ 的极速查询响应。其核心贡献是达到了 $p^{O(1) + \\log\\log p}$ 的近似比，在快速查询领域优于或补充了现有的前沿研究成果。"
selection_source: fresh_fetch
motivation: 现代大规模应用需要能够在多维 $\ell_p$ 空间中实现极速在线响应且占用合理空间的最近邻搜索算法。
method: "提出了一种针对 $p>2$ 的 $\\ell_p$ 度量空间的随机化数据结构，优化了预处理与查询流程。"
result: "在保持多项式空间复杂度的同时，实现了 $p^{O(1) + \\log\\log p}$ 的近似比和极快的查询时间。"
conclusion: 该研究在快速查询机制下改进了 $\ell_p$ 空间最近邻搜索的性能，提供了优于或可比于当前最先进技术的结果。
---

## 摘要
最近邻搜索（NNS）问题旨在设计一种数据结构，对位于度量空间 $\mathcal{M}$ 中的 $n$ 点数据集 $X$ 进行预处理，以便在给定查询点 $q \in \mathcal{M}$ 时，能够快速返回 $X$ 中到 $q$ 距离最小的点。此类数据结构的效率主要根据其空间占用和响应查询所需的时间来评估。我们专注于快速查询时间范畴，这对于现代大规模应用至关重要，因为此类应用的数据集规模巨大且查询必须在线处理，其查询时间通常建模为 $\text{poly}(d \log n)$。我们的主要成果是针对 $\ell_p$ 空间（$p>2$）中的 NNS 提出了一种随机化数据结构，该结构在保证快速查询时间和 $\text{poly}(dn)$ 空间的同时，实现了 $p^{O(1) + \log\log p}$ 的近似比。我们的数据结构改进了（或在某些方面与）[Bartal and Gottlieb, TCS 2019] 和 [Krauthgamer, Petruschka and Sapir, FOCS 2025] 中针对快速查询时间范畴的最先进成果相比具有不可比性。

## Abstract
The Nearest Neighbor Search (NNS) problem asks to design a data structure that preprocesses an $n$-point dataset $X$ lying in a metric space $\mathcal{M}$, so that given a query point $q \in \mathcal{M}$, one can quickly return a point of $X$ minimizing the distance to $q$. The efficiency of such a data structure is evaluated primarily by the amount of space it uses and the time required to answer a query. We focus on the fast query-time regime, which is crucial for modern large-scale applications, where datasets are massive and queries must be processed online, and is often modeled by query time $\text{poly}(d \log n)$. Our main result is such a randomized data structure for NNS in $\ell_p$ spaces, $p>2$, that achieves $p^{O(1) + \log\log p}$ approximation with fast query time and $\text{poly}(dn)$ space. Our data structure improves, or is incomparable to, the state-of-the-art for the fast query-time regime from [Bartal and Gottlieb, TCS 2019] and [Krauthgamer, Petruschka and Sapir, FOCS 2025].