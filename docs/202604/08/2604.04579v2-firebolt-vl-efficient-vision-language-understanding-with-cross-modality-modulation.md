---
title: "Firebolt-VL: Efficient Vision-Language Understanding with Cross-Modality Modulation"
title_zh: Firebolt-VL：基于跨模态调制的高效视觉语言理解
authors: "Quoc-Huy Trinh, Mustapha Abdullahi, Bo Zhao, Debesh Jha"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04579v2"
tags: ["query:cir-ml"]
score: 9.0
evidence: 具有跨模态调制的低功耗视觉语言模型
tldr: Firebolt-VL 是一款高效的多模态大语言模型，旨在解决现有模型计算成本高且难以捕捉细粒度视觉信息的问题。该模型采用 Liquid Foundation Model (LFM) 替代传统的 Transformer 解码器，并引入 Token-Grid 相关性模块，通过线性复杂度的状态空间模型和 FiLM 调节实现跨模态调制。Firebolt-VL 在保持推理效率的同时，显著提升了对细粒度视觉区域的理解能力。
selection_source: fresh_fetch
motivation: 现有的多模态模型因 Transformer 的二次复杂度导致计算开销巨大，且小模型在处理细粒度视觉推理任务时表现不佳。
method: 采用 Liquid Foundation Model (LFM) 作为解码器，并结合 Token-Grid 相关性模块与 FiLM 调节技术，实现高效的跨模态特征融合。
result: 实验表明 Firebolt-VL 在多个基准测试中实现了准确的细粒度理解，并显著提高了推理效率。
conclusion: Firebolt-VL 证明了通过线性时间复杂度的架构设计，可以在资源受限的环境下实现高性能的视觉语言理解。
---

## 摘要
多模态大语言模型（MLLMs）的最新进展在视觉语言理解方面取得了显著进步，但其高昂的计算成本限制了在个人助手、文档理解和智能摄像头等资源受限场景中的部署。大多数现有方法依赖于基于 Transformer 的交叉注意力机制，其二次复杂度阻碍了效率。此外，小型视觉语言模型往往难以精确捕捉细粒度的、与任务相关的视觉区域，导致在细粒度推理任务上的性能下降，限制了其在现实世界中的有效性。为了解决这些问题，我们推出了 Firebolt-VL，这是一种高效的视觉语言模型，它将基于 Transformer 的解码器替换为液体基础模型（Liquid Foundation Model, LFM）解码器。为了进一步增强视觉定位，我们提出了一个 Token-Grid 相关性模块，该模块计算文本 token 与图像块（patches）之间的轻量级相关性，并通过带有 FiLM 条件的状态空间模型（SSM）进行调制。这使得模型能够在保持线性时间推理的同时，选择性地强调与文本提示相关的视觉区域。在多个基准测试上的实验结果表明，Firebolt-VL 在显著提高效率的同时，实现了准确的细粒度理解。我们的模型和代码可在以下网址获取：https://fireboltvl.github.io

## Abstract
Recent advances in multimodal large language models (MLLMs) have enabled impressive progress in vision-language understanding, yet their high computational cost limits deployment in resource-constrained scenarios such as personal assistants, document understanding, and smart cameras. Most existing methods rely on Transformer-based cross-attention, whose quadratic complexity hinders efficiency. Moreover, small vision-language models often struggle to precisely capture fine-grained, task-relevant visual regions, leading to degraded performance on fine-grained reasoning tasks that limit their effectiveness in the real world. To address these issues, we introduce Firebolt-VL, an efficient vision-language model that replaces the Transformer-based decoder with a Liquid Foundation Model (LFM) decoder. To further enhance visual grounding, we propose a Token-Grid Correlation Module, which computes lightweight correlations between text tokens and image patches and modulates via the state-space model with FiLM conditioning. This enables the model to selectively emphasize visual regions relevant to the textual prompt while maintaining linear-time inference. Experimental results across multiple benchmarks demonstrate that Firebolt-VL achieves accurate, fine-grained understanding with significantly improved efficiency. Our model and code are available at: https://fireboltvl.github.io

---

## 论文详细总结（自动生成）

这是一份关于论文《Firebolt-VL: Efficient Vision-Language Understanding with Cross-Modality Modulation》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态大语言模型（MLLMs）主要面临两大挑战：
    1.  **计算开销巨大**：基于 Transformer 的架构具有二次计算复杂度，导致在资源受限设备（如智能手机、摄像头）上推理缓慢。
    2.  **细粒度感知不足**：小型模型在处理需要精确视觉定位的任务（如 OCR、复杂图表分析）时，往往无法准确捕捉与任务相关的局部视觉区域。
*   **研究背景**：为了在保持高性能的同时降低功耗，研究者们开始探索轻量级骨干网络（如 MobileVLM）或混合专家模型（MoE），但这些方法仍未从根本上解决 Transformer 注意力机制的效率瓶颈。

### 2. 方法论：核心思想与技术细节
Firebolt-VL 提出了一个结合了线性时间复杂度架构与动态视觉调制的框架：
*   **Liquid Foundation Model (LFM) 解码器**：弃用了传统的 Transformer，采用基于“液体”假设的 LFM2 作为语言解码器。LFM 具有线性推理时间复杂度，显著降低了长序列处理的内存和计算开销。
*   **跨模态调制器 (CMM)**：这是本文的核心创新，旨在替代昂贵的交叉注意力机制。
    *   **Token-Grid 相关性计算**：计算文本 Token 与图像网格（Grid）之间的点积相关性，识别出对当前指令最重要的图像块。
    *   **Top-k 稀疏化**：仅保留每个 Token 相关性最高的 $k$ 个视觉区域，减少噪声并提升效率。
    *   **FiLM 调制与 SSM 融合**：利用特征线性调制（FiLM）根据视觉上下文对文本特征进行缩放和偏移，随后通过状态空间模型（SSM，具体为 S4 变体）进行序列建模。这种设计实现了视觉与文本特征的深度交互，且保持了近乎线性的复杂度。

### 3. 实验设计
*   **数据集**：
    *   **第一阶段（预训练）**：使用 CC3M 数据集初始化 CMM 模块。
    *   **第二阶段（指令微调）**：使用 LLaVA-CoT 和处理后的 MMPR-v1.2 数据集，通过思维链（CoT）格式增强推理能力。
*   **基准测试 (Benchmarks)**：涵盖了 VQAv2（视觉问答）、POPE（物体幻觉）、AI2D（图表理解）、MMMU（多学科推理）、MME（综合感知）、SQA-Image（科学问答）和 MMB（综合能力）。
*   **对比方法**：
    *   **大型模型 (>7B)**：IDEFICS, OpenFlamingo v2, MiniGPT-4, Chameleon, FastV。
    *   **小型模型 (<3B)**：Kosmos-2, MobileVLM (v1/v2), MoE-LLaVA, LLaVA-Phi, SmolVLM2。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA H100 80GB GPU**。
*   **训练配置**：
    *   第一阶段：Batch size 128，学习率 $5 \times 10^{-4}$。
    *   第二阶段：Batch size 8，学习率 $1 \times 10^{-4}$。
*   **时长**：每个阶段训练 5 个 Epoch，通常在第 2 个 Epoch 即可选出最佳模型。

### 5. 实验数量与充分性
*   **实验规模**：论文在 7 个主流多模态基准上进行了定量评估，并提供了丰富的定性可视化对比。
*   **消融实验**：
    1.  **SSM 变体对比**：对比了 Mamba、S4D 和 S4，发现 S4 在捕捉空间几何关系上表现最优。
    2.  **融合策略对比**：对比了直接拼接（Prepend）、交叉注意力（Cross-attend）和 CMM，证明 CMM 在感知任务（MME）上提升巨大。
    3.  **Top-k 参数分析**：测试了 $k=1$ 到 $5$ 的影响，发现 $k=4$ 是性能与噪声之间的最佳平衡点。
*   **评价**：实验设计较为全面，涵盖了准确性、推理延迟和吞吐量等多个维度，对比对象既包括同量级的小模型，也包括跨量级的大模型，具有较强的说服力。

### 6. 主要结论与发现
*   **性能卓越**：Firebolt-VL 仅有 **0.8B 参数**，但在 MME 感知评分（1376.2）和 VQAv2 上超越了许多 7B 甚至 9B 规模的模型。
*   **效率极高**：其吞吐量达到 **46.67 tokens/s**，在同类轻量级模型中处于领先地位，且在准确性-延迟权衡曲线（Accuracy-Latency trade-off）中表现最优。
*   **细粒度能力强**：定性分析显示，CMM 模块使模型能准确定位到图像中的微小细节（如搜索栏文字、食品标签），减少了小模型常见的“泛泛而谈”现象。

### 7. 优点
*   **架构创新**：成功将 Liquid Foundation Model 和 SSM 引入多模态领域，打破了 Transformer 的复杂度限制。
*   **轻量且精准**：通过 CMM 模块实现了高效的视觉引导，解决了小模型在细粒度任务上的短板。
*   **实用性强**：模型规模极小（<1B），非常适合端侧部署。

### 8. 不足与局限
*   **输入限制**：目前仅支持单图输入，尚未扩展到多图或视频序列。
*   **物体幻觉风险**：在 POPE 基准上的表现略逊于部分基于注意力的模型，论文推测这可能是因为 SSM 的隐式状态传播在抑制幻觉方面不如显式注意力机制有效。
*   **训练数据规模**：相比于一些使用数亿级数据的模型，Firebolt-VL 使用的数据规模相对较小，进一步扩大数据规模可能会有更多潜力。

（完）
