---
title: Negative Data Mining for Contrastive Learning in Dense Retrieval at IKEA.com
title_zh: IKEA.com 稠密检索中对比学习的负样本挖掘
authors: "Eva Agapaki, Amritpal Singh Gill"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00353v1"
tags: ["query:cir-ml"]
score: 9.5
evidence: 密集检索对比学习中的负样本挖掘
tldr: "本研究针对宜家电商检索系统，提出了一种改进对比学习效果的负样本挖掘方法。通过利用产品层级分类和属性构建结构化负采样策略，并引入LLM作为评判者进行大规模相关性评估。实验显示该方法在离线测试中提升了2.6%的准确率，但在A/B测试中未见显著线上提升。研究揭示了高零点击率等用户行为对检索效果转化的影响，强调了训练数据需贴合真实搜索意图的重要性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00353v1/fig-001.webp\", \"caption\": \"Figure 1: (a) Training data generation pipeline consisting of three stages and (b) data mixtures visualization.\", \"page\": 2, \"index\": 1, \"width\": 537, \"height\": 314}]"
motivation: 旨在解决对比学习在密集检索中对高质量负样本的依赖问题，以提升宜家电商平台的搜索相关性。
method: 结合产品分类学与属性进行结构化负采样，并利用大语言模型对候选产品进行自动化相关性评分以生成训练数据。
result: "离线实验实现了2.6%的准确率提升，但线上A/B测试在长尾查询上未表现出显著的用户参与度差异。"
conclusion: 硬负样本挖掘虽重要，但必须结合真实用户搜索行为和零点击模式来弥合离线评估与线上业务指标之间的差距。
---

## 摘要
对比学习是现代检索系统的核心组件，但其有效性在很大程度上取决于训练过程中使用的负样本质量。在这项工作中，我们提出了一种系统化的方法，通过结构化负采样策略和可扩展的 LLM-as-a-judge 相关性评估，来改进宜家（IKEA）产品搜索的稠密检索。基于宜家搜索引擎的延迟交互（late-interaction）检索架构，我们提出了两项主要贡献：(1) 利用产品层级分类和产品属性生成语义上具有挑战性的负样本的结构化负采样策略；(2) 用于生成训练数据的全面基于 LLM 的评估方法。我们的基于 LLM 的评估系统不是依赖稀疏的人工标注或随机采样，而是为每个查询的所有候选产品分配评分。我们的方法在加拿大市场的线下真实用户查询实验中，平均类别准确率提升了 2.6%。然而，我们在长尾查询上的 A/B 测试显示，改进模型与基准模型在用户参与度指标上没有统计学显著差异 (p > 0.05)。我们将这一差距归因于用户搜索行为：67% 的热门搜索显示零点击率超过 50%，这表明很大比例的搜索会话无论结果排名如何，都没有产生产品互动。这些发现强调了难负样本挖掘的重要性，同时也表明需要将训练数据和线下评估建立在真实用户搜索行为的基础上——包括查询意图分布和零点击模式——以弥合线下检索质量与线上用户参与度之间的差距。

## Abstract
Contrastive learning is a core component of modern retrieval systems, but its effectiveness heavily relies on the quality of negative examples used during training. In this work, we present a systematic approach to improving dense retrieval for IKEA product search through structured negative sampling strategies and scalable LLM-as-a-judge relevance evaluation.   Building on IKEA Search Engine's late-interaction retrieval architectures, we introduce two key contributions: (1) structured negative sampling strategies that leverage product hierarchical taxonomy and product attributes to generate semantically challenging negatives, and (2) a comprehensive LLM-based evaluation methodology for generating training data. Rather than relying on sparse human annotations or random sampling, our LLM-based evaluation system allocates a score for all candidate products against each query.   Our methodology achieves +2.6\% average category accuracy on offline real user query experiments on the Canada market. However, our A/B test on long-tail queries showed no statistically significant differences in user engagement metrics between the improved and baseline models ($p > 0.05$). We trace this gap to user search behavior: 67\% of popular searches exhibit zero-click rates above 50\%, indicating that a substantial proportion of search sessions result in no product engagement regardless of result ranking. These findings underscore the importance of hard negative mining but also the need for grounding training data and offline evals in real user search behavior -- including query intent distribution and zero-click patterns -- to bridge the gap between offline retrieval quality and online user engagement.

---

## 论文详细总结（自动生成）

这是一份关于论文《Negative Data Mining for Contrastive Learning in Dense Retrieval at IKEA.com》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：在电商搜索场景下，如何通过改进对比学习中的**负样本挖掘（Hard Negative Mining, HNM）**策略来提升稠密检索（Dense Retrieval）的性能，并探究线下评估指标的提升为何往往难以转化为线上业务指标的增长。
*   **研究背景**：稠密检索模型（如基于 ColBERT 的延迟交互模型）高度依赖高质量的负样本。传统的随机采样无法提供足够的区分度，而人工标注成本极高。宜家（IKEA）希望利用大语言模型（LLM）自动化生成高质量训练数据，并解决线下实验与线上 A/B 测试之间的“表现鸿沟”。

### 2. 方法论
论文提出了一个自动化的训练数据生成流水线，核心思想是结合**产品分类学（Taxonomy）**与 **LLM 评估**：
*   **类别特征提取**：利用 LLM 从 373 个叶子类别的产品描述中提取特定属性（如颜色、材质、尺寸、生活场景等），确保属性与类别逻辑匹配。
*   **合成查询生成**：根据提取的属性，让 LLM 生成不同复杂度（简单、中等、复杂）和不同意图（属性驱动、意图驱动、产品名驱动）的查询。
*   **结构化负采样策略**：
    *   **仅属性负样本**：同类别但属性不同（如：查询“白色沙发”，负样本为“黑色沙发”）。
    *   **跨类别负样本**：属性匹配但类别错误（如：查询“白色沙发”，负样本为“白色桌子”）。
    *   **多属性负样本**：针对复杂查询，负样本仅违反其中一个特定属性。
*   **LLM-as-a-Judge 标注**：使用 LLM 对“查询-产品对”进行 1-5 分的相关性评分，分值 $\ge 4$ 为正样本，$\le 2$ 为负样本，以此构建训练三元组。
*   **检索架构**：采用延迟交互（Late Interaction）架构，通过计算查询和文档 Token 嵌入的最大相似度之和来衡量相关性。

### 3. 实验设计
*   **数据集**：宜家加拿大市场数据，包含 24,350 个产品和 373 个叶子类别。
*   **评估基准（Benchmark）**：
    1.  **合成查询基准**：70 个由 LLM 生成的查询，用于测试分布内性能。
    2.  **真实查询基准**：从生产日志中抽取的 100 个真实用户查询（涵盖单属性、多属性和产品名）。
    3.  **线上 A/B 测试**：在加拿大市场进行为期 2 周的 50/50 流量实验，专注于长尾查询（$\ge 3$ 个单词）。
*   **对比方法**：
    *   **Baseline**：随机负采样（7.2M 三元组）。
    *   **HNM**：基础数据 + LLM 标注的硬负样本（7.9M 三元组）。
    *   **HNM+QE / Full**：在 HNM 基础上增加不同规模的 LLM 查询扩展数据。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA A100 GPU**。
*   **训练时长**：每个模型配置大约需要 **18 小时** 完成训练。
*   **超参数**：学习率 $3 \times 10^{-6}$，Batch Size 为 32，最大文档长度 512 Token，训练步数上限 500,000 步。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 种主要模型变体进行了对比。为了确保统计鲁棒性，每种配置都使用不同的随机种子**重复训练了 5 次**，并报告了均值和标准差。
*   **充分性评价**：实验设计较为全面，涵盖了从合成数据到真实日志，再到线上生产环境的全链路评估。通过人工评估验证了 LLM 标注与人类的一致性高达 93%，证明了离线评估基准的可靠性。

### 6. 主要结论与发现
*   **硬负样本有效性**：HNM 策略在离线测试中显著提升了类别准确率（Cat@10 提升约 2.6% - 4.3%）。
*   **数据质量 > 数量**：简单的 HNM 模型优于数据量更大的 QE 扩展模型，说明盲目增加合成数据可能引入噪声或导致过拟合。
*   **线下线上鸿沟**：尽管离线指标提升明显，但线上 A/B 测试在点击率、加购率等指标上**无显著差异 (p > 0.05)**。
*   **原因分析**：
    1.  **意图广度差异**：真实用户查询涉及的产品类别平均是合成查询的 7 倍，合成数据过于“窄意图”。
    2.  **零点击行为**：67% 的热门搜索存在超过 50% 的零点击率，用户可能在结果页已满足需求或直接流失，导致排名改进难以体现。

### 7. 优点
*   **系统化的负采样**：将产品层级分类学与 LLM 结合，生成了比随机采样更具语义挑战性的负样本。
*   **全链路分析**：不仅关注模型指标，还深入探讨了用户行为（如零点击、意图分布）对搜索效果的影响。
*   **工业界参考价值**：真实记录了模型上线后的“失败”经验，对弥合学术研究与工业应用之间的差距具有重要意义。

### 8. 不足与局限
*   **样本规模限制**：真实查询评估集仅包含 100 个查询，样本量较小，可能无法完全覆盖复杂的搜索场景。
*   **A/B 测试局限**：实验仅针对长尾查询，且标准 A/B 测试在检测排名改进方面可能不如交替实验（Interleaving）灵敏。
*   **LLM 偏差风险**：合成查询和标注完全依赖 LLM，可能继承了 LLM 的系统性偏差（如评分过于宽松或意图过于单一）。

（完）
