---
title: "Personalized RewardBench: Evaluating Reward Models with Human Aligned Personalization"
title_zh: Personalized RewardBench：利用符合人类对齐的个性化评估奖励模型
authors: "Qiyao Ma, Dechen Gao, Rui Cai, Boqi Zhao, Hanchu Zhou, Junshan Zhang, Zhe Zhao"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07343v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 评估大语言模型个性化和人类对齐的奖励模型
tldr: 随着大语言模型对多元化对齐的需求增加，评估奖励模型（RM）捕捉个人偏好的能力变得至关重要。本文提出 Personalized RewardBench，通过构建基于特定用户准则的响应对，专门评估 RM 的个性化建模能力。研究发现现有顶尖 RM 在个性化任务上表现欠佳，且该基准与下游任务（如 BoN 和 PPO）的性能高度相关，为评估个性化对齐提供了有效工具。
selection_source: fresh_fetch
motivation: 现有的奖励模型评估基准主要关注通用质量，缺乏对模型捕捉个体用户差异化偏好能力的有效衡量。
method: 提出 Personalized RewardBench，利用特定用户准则构建响应对，确保偏好区分完全基于个性化需求而非通用质量。
result: "实验显示现有最先进的奖励模型在个性化偏好识别上存在显著挑战，最高准确率仅为 75.94%。"
conclusion: 该基准与下游个性化对齐任务的性能具有强相关性，是评估奖励模型在实际应用中表现的可靠指标。
---

## 摘要
多元化对齐已成为大语言模型（LLMs）开发中的一个关键前沿领域，而奖励模型（RMs）则是捕捉多样化人类价值观的核心机制。虽然针对通用回答质量的基准测试已非常普遍，但评估奖励模型在考虑个体用户偏好方面的表现仍然是一个亟待解决的挑战。为了填补这一空白，我们推出了 Personalized RewardBench，这是一个旨在严格评估奖励模型建模个性化偏好能力的新型基准。我们根据对用户特定准则的严格遵守（或违反）来构建“选中”和“拒绝”的回答对，确保偏好区分是针对个人量身定制的。特别是，人工评估证实，回答对之间的主要区分因素严格在于个人偏好，而两份回答都保持了较高的通用质量（例如正确性、相关性和帮助性）。广泛的测试表明，现有的最先进奖励模型在个性化方面面临巨大挑战，最高准确率仅为 75.94%。至关重要的是，由于有效的奖励模型基准应该能够预测奖励模型在下游任务中的表现，我们通过实验证明，与现有基准相比，我们的基准在 Best-of-N (BoN) 采样和近端策略优化 (PPO) 中与下游性能的相关性显著更高。这些发现确立了 Personalized RewardBench 是评估奖励模型在下游应用中表现的稳健且准确的代理指标。

## Abstract
Pluralistic alignment has emerged as a critical frontier in the development of Large Language Models (LLMs), with reward models (RMs) serving as a central mechanism for capturing diverse human values. While benchmarks for general response quality are prevalent, evaluating how well reward models account for individual user preferences remains an open challenge. To bridge this gap, we introduce Personalized RewardBench, a novel benchmark designed to rigorously assess reward models' capacity to model personalized preferences. We construct chosen and rejected response pairs based on strict adherence to (or violation of) user-specific rubrics, ensuring that preference distinctions are uniquely tailored to the individual. In particular, human evaluations confirm that the primary discriminative factor between pairs is strictly personal preference, with both responses maintaining high general quality (e.g., correctness, relevance and helpfulness). Extensive testing reveals that existing state-of-the-art reward models struggle significantly with personalization, peaking at an accuracy of just 75.94%. Crucially, because an effective reward model benchmark should predict a reward model's performance on downstream tasks, we conduct experiments demonstrating that our benchmark exhibits a significantly higher correlation with downstream performance in both Best-of-N (BoN) sampling and Proximal Policy Optimization (PPO) compared to existing baselines. These findings establish Personalized RewardBench as a robust and accurate proxy for evaluating reward models' performance in downstream applications.

---

## 论文详细总结（自动生成）

以下是对论文《Personalized RewardBench: Evaluating Reward Models with Human Aligned Personalization》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然大语言模型（LLM）的对齐技术（如 RLHF）已取得显著进展，但现有的奖励模型（RM）评估基准（如 RewardBench）主要侧重于通用质量（如事实正确性、帮助性、无害性），忽略了**个性化偏好**。
*   **核心挑战**：不同用户对同一问题的“好回答”定义不同。现有的 RM 往往无法捕捉用户独特的背景、风格和特定需求。此外，现有的 RM 评估指标与下游任务（如 BoN 采样、PPO 训练）的实际表现之间存在“代理差距”（Proxy Gap）。
*   **整体含义**：本文旨在填补个性化奖励模型评估的空白，提出一个能够反映真实用户偏好且能预测下游性能的新基准。

### 2. 方法论
*   **核心思想**：构建一个包含用户画像（User Profile）和特定评价准则（Personal Rubrics）的偏好数据集，通过控制变量法确保“选中（Chosen）”和“拒绝（Rejected）”回答的唯一区别在于是否符合个性化准则，而非通用质量的高低。
*   **关键技术细节**：
    1.  **数据构造**：利用 LaMP-QA 数据集，提取用户历史交互作为画像 $u$。
    2.  **响应对生成**：
        *   **选中回答 ($y_c$)**：LLM 根据查询 $q$、画像 $u$ 和**符合**个性化准则 $R_u$ 生成。
        *   **拒绝回答 ($y_r$)**：LLM 根据查询 $q$、画像 $u$ 但**违反**个性化准则 $\neg R_u$ 生成。
    3.  **Planner 模块**：设计了一个规划器，从用户历史中推断出个性化准则 $\tilde{R}_u$，以解决 RM 无法直接处理原始长历史画像的问题。
    4.  **评估流程**：对于标量 RM，比较 $RM(q, y_c)$ 与 $RM(q, y_r)$ 的得分；对于生成式 RM，通过 Prompt 让模型直接在两个选项中做出选择。

### 3. 实验设计
*   **数据集与场景**：涵盖三个高方差领域：艺术与娱乐、生活方式与个人发展、社会与文化。
*   **Benchmark 构成**：Personalized RewardBench，包含经过人工验证的偏好对。
*   **对比方法**：
    *   **标量 RM**：Skywork-Reward 系列、InternLM2-Reward 系列。
    *   **生成式 RM**：RM-R1、R3、Claude-3.5-Sonnet、GPT-4o (5.1)、Gemini-1.5-Flash。
    *   **微调个性化 RM**：Bradley-Terry、GPO、VPL、PAL、SynthesizeMe。
*   **下游验证**：使用 Qwen2.5-0.5B 作为策略模型，分别进行 Best-of-16 (BoN) 采样和 PPO 微调，验证 RM 排名与策略模型实际表现的相关性。

### 4. 资源与算力
*   **硬件设备**：论文明确提到使用了 **4 张 NVIDIA A6000 GPU** 进行模型训练和推理。
*   **训练细节**：PPO 训练时 KL 散度系数设为 0，温度设为 1.0。
*   **模型调用**：使用 Gemini-1.5-Flash 生成响应对，使用 Qwen2.5-32B-Instruct 作为 LLM-as-a-Judge 进行下游评分。

### 5. 实验数量与充分性
*   **实验规模**：基准测试涵盖了数千个测试样本（艺术 767、生活 989、社会 1074）。
*   **人工评估**：对响应对进行了严格的人工打分（事实性、相关性、帮助性、个性化准则），验证了数据构造的有效性。
*   **消融与相关性分析**：进行了用户画像注入方式的消融实验，并使用了 NDCG、RBO、加权 $\tau$ 和 Spearman’s $\rho$ 四种指标衡量基准与下游任务的相关性。
*   **充分性评价**：实验设计非常全面，不仅对比了不同架构的模型，还通过下游任务闭环验证了基准的实用价值，具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **性能瓶颈**：现有最强模型（如 Gemini-1.5-Flash）在个性化偏好识别上的准确率仅为 **75.94%**，远低于 Oracle（提供真实准则时）的 98%+，说明现有 RM 缺乏个性化推理能力。
*   **规模悖论**：模型参数量增加并不一定提升个性化表现（例如 InternLM2-20B 在某些任务上弱于 7B 版本）。
*   **画像处理**：直接注入原始用户画像往往会导致性能下降，而通过 Planner 提取结构化准则能显著提升 RM 的判断准确度。
*   **高相关性**：Personalized RewardBench 与下游 BoN 和 PPO 的性能表现具有极强的正相关性，远优于现有的个性化基准（如基于 Chatbot Arena 的变体）。

### 7. 优点
*   **隔离性强**：通过构造高质量的负样本（仅违反个性化准则），排除了模型通过“比烂”（识别低质量文本）来刷分的可能性。
*   **下游预测力**：首次系统性地证明了个性化 RM 基准与 PPO/BoN 实际对齐效果之间的高相关性。
*   **实用性**：引入 Planner 机制，为 RM 如何利用长程用户历史提供了可行的技术路径。

### 8. 不足与局限
*   **数据多样性限制**：每个用户在数据集中仅对应一个查询，无法评估模型在同一用户多次交互中的长期演化偏好。
*   **负样本生成偏差**：负样本是由 LLM 故意违反准则生成的，可能与真实人类在不经意间违反准则的分布存在差异。
*   **领域覆盖**：虽然涵盖了三个领域，但对于医疗、法律等专业性极强且对个性化要求极高的垂直领域尚未涉及。

（完）
