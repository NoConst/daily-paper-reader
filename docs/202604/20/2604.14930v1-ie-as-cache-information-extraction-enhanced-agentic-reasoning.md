---
title: "IE as Cache: Information Extraction Enhanced Agentic Reasoning"
title_zh: IE 作为缓存：信息抽取增强的智能体推理
authors: "Hang Lv, Sheng Liang, Hongchao Gu, Wei Guo, Defu Lian, Yong Liu, Hao Wang, Enhong Chen"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14930v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 将信息抽取重新利用为认知缓存，以增强智能体推理
tldr: 本研究针对传统信息抽取（IE）仅被视为最终目标而非推理中间过程的问题，提出了IE-as-Cache框架。该框架借鉴计算机分级存储概念，将IE转化为智能体的“认知缓存”，通过查询驱动的抽取与缓存感知推理，动态维护精简的中间信息并过滤噪声。实验证明，该方法在多个大模型和基准测试上显著提升了推理准确率，为IE在复杂推理任务中的下游应用提供了新方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14930v1/fig-001.webp\", \"caption\": \"Fig. 1. Cognitive analogy between hierarchical computer memory and text reasoning. Information Extraction functions as a bridge between raw text and the reasoning agent, mirroring the role of a hardware cache.\", \"page\": 1, \"index\": 1, \"width\": 529, \"height\": 291}, {\"url\": \"assets/figures/arxiv/2604.14930v1/fig-002.webp\", \"caption\": \"Fig. 2. Comparison of context mechanisms. Unlike (a) and (b) which treat raw text as a static read-only block, (c) IE-as-Cache introduces a dynamic Read-Write Cache. This mechanism filters noise by keeping the full text external and only maintaining semantically critical information in the active memory.\", \"page\": 2, \"index\": 2, \"width\": 1076, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2604.14930v1/fig-003.webp\", \"caption\": \"TABLE I MAIN EXPERIMENTAL RESULTS ACROSS THREE DATASETS. WE REPORT EXACT MATCH SCORE FOR TACT AND CALENDAR, AND ROUGE-1 SCORE FOR QMSUM. BEST RESULTS ARE MARKED IN BOLD.\", \"page\": 4, \"index\": 3, \"width\": 529, \"height\": 317}]"
motivation: 传统信息抽取通常被视为孤立的最终任务，未能充分发挥其在多步推理过程中作为可重用中间结构的作用。
method: 提出IE-as-Cache框架，结合查询驱动的抽取与缓存感知推理，将结构化信息作为动态维护的认知缓存。
result: 在挑战性基准测试中，该方法显著提高了多种大语言模型的推理准确性，并有效过滤了冗余噪声。
conclusion: 将信息抽取重新定位为可重用的认知资源，能有效增强智能体的推理能力，是未来IE应用研究的重要方向。
---

## 摘要
信息抽取（IE）旨在从非结构化文本中提取结构化的、与决策相关的信息，作为下游理解和推理的基础。然而，传统上它仅被视为一个终点目标：一旦提取完成，生成的结构通常被孤立地使用，而不是在多步推理过程中进行维护和重用。超越这一局限，我们提出了 IE-as-Cache 框架，将 IE 重新定位为一种认知缓存，以增强智能体推理。受计算机分级存储器的启发，我们的方法将查询驱动的抽取与缓存感知推理相结合，动态地维护紧凑的中间信息并过滤噪声。在多种大语言模型（LLMs）上的挑战性基准测试实验表明，该方法显著提高了推理准确性，这表明 IE 可以被有效地重新利用为一种可重用的认知资源，并为未来关于 IE 下游应用的研究提供了一个极具前景的方向。

## Abstract
Information Extraction aims to distill structured, decision-relevant information from unstructured text, serving as a foundation for downstream understanding and reasoning. However, it is traditionally treated merely as a terminal objective: once extracted, the resulting structure is often consumed in isolation rather than maintained and reused during multi-step inference. Moving beyond this, we propose \textit{IE-as-Cache}, a framework that repurposes IE as a cognitive cache to enhance agentic reasoning. Drawing inspiration from hierarchical computer memory, our approach combines query-driven extraction with cache-aware reasoning to dynamically maintain compact intermediate information and filter noise. Experiments on challenging benchmarks across diverse LLMs demonstrate significant improvements in reasoning accuracy, indicating that IE can be effectively repurposed as a reusable cognitive resource and offering a promising direction for future research on downstream uses of IE.

---

## 论文详细总结（自动生成）

这是一份关于论文《IE as Cache: Information Extraction Enhanced Agentic Reasoning》的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：在处理长篇、含有大量噪声的非结构化文本时，大语言模型（LLM）常面临“中间信息丢失”和易受无关干扰信息影响的问题。
*   **核心问题**：传统的信息抽取（IE）技术通常被视为任务的终点（即提取出结构化数据即告结束）。本研究探讨：能否将 IE 重新定位为一种**动态的认知缓存**，作为智能体（Agent）在多步推理过程中的中间基础设施？
*   **整体含义**：借鉴计算机体系结构中 CPU 缓存（Cache）的概念，将原始文本视为外部存储，将提取的结构化信息视为工作内存中的缓存，从而提升智能体在复杂任务中的推理效率和准确性。

### 2. 方法论：IE-as-Cache 框架
*   **核心思想**：建立一个“读-写”缓存机制，智能体不再反复扫描原始长文本，而是维护并操作一个紧凑的、查询相关的结构化缓存。
*   **关键技术细节**：
    1.  **查询驱动的 Schema 解耦抽取**：首先根据用户查询（Query）动态生成抽取模式（Schema），然后基于该 Schema 从原始文本中提取初始信息。这种两阶段法比直接提取更具针对性。
    2.  **缓存感知推理（Cache-Aware Reasoning）**：智能体在推理循环中优先访问 Cache。如果 Cache 信息不足，则触发 `seek_information` 动作。
    3.  **动态缓存更新（Write-Through Update）**：当获取新证据后，通过一个专门的 LLM 调用来执行缓存维护，包括：**合并**兼容信息、**删除**冗余或偏离目标的内容、**剪枝**低效细节。
*   **算法流程**：
    *   初始化：Query + Raw Text → 生成 Schema → 初始抽取 → 建立初始 Cache。
    *   推理循环：智能体基于 Cache 生成推理轨迹 → 若需更多信息 → 执行按需抽取 → **更新/维护 Cache** → 重复直至得出答案。

### 3. 实验设计
*   **数据集与场景**：
    1.  **TACT**：复杂的逻辑问答，包含大量干扰项。
    2.  **Calendar Scheduling**：日程规划任务，需处理自然语言描述中的冲突约束。
    3.  **QMSUM**：查询式会议摘要，需从冗长的对话记录中提取关键点。
*   **对比方法（Baselines）**：
    *   **Generic**：标准提示词，直接输入全文。
    *   **CoT (Chain of Thought)**：思维链推理。
    *   **ReAct**：标准的“推理-动作”智能体框架。
    *   **IE-as-Tool**：将文本转为表格并使用 Pandas 工具查询（仅用于 TACT 数据集）。
*   **测试模型**：涵盖了 GPT-4o, GPT-4o-mini, Llama3.1-8B, Qwen3-8B 以及轻量级的 Qwen3-0.6B。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练/推理的总时长。
*   **实现细节**：实验主要基于 API 调用（针对 GPT 系列）和开源模型推理。对于闭源模型，作者运行了三次并取平均值以消除随机性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了问答、规划、摘要三大类典型推理任务，使用了从 0.6B 到超大规模参数的多种模型，实验设置较为全面。
*   **充分性分析**：
    *   **消融实验**：专门验证了“动态更新”机制的有效性（对比了静态抽取）。
    *   **质量分析**：通过 ROUGE 和 Sentence-T5 相似度量化了中间抽取质量与最终推理准确率的正相关性。
    *   **客观性**：引入了“Oracle Schema”（金标准模式）进行对比，证明了该框架在非完美抽取下依然具有极强的鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：IE-as-Cache 在所有基准测试中均显著优于 Generic、CoT 和 ReAct。在 TACT 逻辑问答中，GPT-4o 的表现比专门的 IE-as-Tool 高出 10 个百分点。
*   **小模型救星**：该框架对小参数模型的提升尤为显著（如 Qwen-0.6B 在 TACT 上准确率翻倍），证明结构化缓存能有效缓解小模型处理长上下文时的认知负担。
*   **动态维护是关键**：消融实验显示，缺乏动态更新机制会导致性能大幅下降，证明了“认知缓存”必须是可读写的。

### 7. 优点
*   **视角独特**：将 IE 从“静态产物”转变为“动态资源”，为长文本推理提供了新的架构思路。
*   **抗噪性强**：通过结构化过滤，有效解决了 LLM 容易被长文本中无关信息误导（Distraction）的问题。
*   **高度通用**：不依赖预定义的固定 Schema，能够根据用户查询动态调整抽取重点。

### 8. 不足与局限
*   **模型能力底线**：在极度复杂的约束满足任务（如日程规划）中，超小模型（0.6B）即便有缓存辅助，表现依然受限。
*   **Schema 依赖风险**：如果第一步生成的 Schema 存在偏差，可能会导致缓存中丢失关键维度，进而影响后续所有推理步骤。
*   **Token 开销**：虽然减少了重复阅读全文的压力，但频繁的“抽取-更新”循环会增加 LLM 的调用次数，论文未详细讨论其在 Token 消耗和响应延迟上的权衡。

（完）
