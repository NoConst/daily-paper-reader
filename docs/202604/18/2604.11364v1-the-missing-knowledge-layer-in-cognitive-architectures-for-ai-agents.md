---
title: The Missing Knowledge Layer in Cognitive Architectures for AI Agents
title_zh: AI 智能体认知架构中缺失的知识层
authors: Michaël Roynard
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11364v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: AI智能体的认知架构与记忆系统
tldr: 本研究指出当前主流AI认知架构（如CoALA和JEPA）缺失独立的知识层，导致事实与经验在持久化语义上产生混淆。作者提出了一种四层架构分解方案（知识、记忆、智慧、智能），为每层定义了不同的持久化机制（如无限更替、遗忘衰减等）。通过Python和Rust实现验证了该架构的可行性，填补了现有AI智能体在处理事实性知识与感官经验时的架构空白。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11364v1/fig-001.webp\", \"caption\": \"Table 1. Four-layer decomposition with persistence semantics. Each layer requires a different update mechanism. Treating any two identically produces the category error described in section 1.\", \"page\": 5, \"index\": 1, \"width\": 658, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2604.11364v1/fig-002.webp\", \"caption\": \"Fig. 1. Four-layer cognitive architecture. Solid arrows (top) denote working pipelines active during inference. Dashed arrows (bottom) denote offline consolidation pipelines. The layers are co-equal substrates with distinct persistence semantics, not a strict hierarchy.\", \"page\": 5, \"index\": 2, \"width\": 700, \"height\": 253}, {\"url\": \"assets/figures/arxiv/2604.11364v1/fig-003.webp\", \"caption\": \"Table 4. Gap analysis: how CoALA and JEPA handle each cognitive substrate versus the four-layer decomposition.\", \"page\": 14, \"index\": 3, \"width\": 562, \"height\": 336}]"
motivation: 现有AI认知架构因缺乏显式的知识层，导致系统错误地对事实应用衰减机制或混淆了事实与经验的处理逻辑。
method: 提出一种包含知识、记忆、智慧和智能的四层分解模型，并为各层设计了互不相同的持久化语义机制。
result: 通过Python和Rust的配套实现证明了架构分离的可行性，并揭示了当前主流框架在处理矛盾解决和知识持久化方面的不足。
conclusion: AI智能体架构必须在工程实现中区分不同的持久化语义，而现有的框架尚未能满足这一关键的架构需求。
---

## 摘要
AI 智能体领域最具影响力的两个认知架构框架 CoALA [21] 和 JEPA [12] 都缺乏一个具有独立持久化语义（persistence semantics）的显式知识层。这一缺失导致了范畴错误：系统对事实性陈述应用认知衰减，或者以相同的更新机制处理事实和经验。我们调研了现有存储系统的持久化语义，并确定了八个收敛点，从 Karpathy 的 LLM 知识库 [10] 到 BEAM 基准测试中接近零的矛盾解决得分 [22]，这些都指向了相关的架构缺陷。我们提出了一个四层分解模型（知识、记忆、智慧、智能），其中每一层都具有根本不同的持久化语义：分别是无限期更替（indefinite supersession）、艾宾浩斯衰减（Ebbinghaus decay）、证据门控修正（evidence-gated revision）和瞬时推理（ephemeral inference）。Python 和 Rust 的配套实现证明了这种架构分离是可行的。我们借用认知科学的术语作为有用的类比（知识/记忆的区别呼应了 Tulving 的三分法），但我们的分层是基于持久化语义需求而建立的工程构件，而非基于神经架构。我们认为，这些区别要求在工程实现中采用不同的持久化语义，而目前没有任何框架或系统能提供这一点。

## Abstract
The two most influential cognitive architecture frameworks for AI agents, CoALA [21] and JEPA [12], both lack an explicit Knowledge layer with its own persistence semantics. This gap produces a category error: systems apply cognitive decay to factual claims, or treat facts and experiences with identical update mechanics. We survey persistence semantics across existing memory systems and identify eight convergence points, from Karpathy's LLM Knowledge Base [10] to the BEAM benchmark's near-zero contradiction-resolution scores [22], all pointing to related architectural gaps. We propose a four-layer decom position (Knowledge, Memory, Wisdom, Intelligence) where each layer has fundamentally different persistence semantics: indefinite supersession, Ebbinghaus decay, evidence-gated revision, and ephemeral inference respectively. Companion implementations in Python and Rust demonstrate the architectural separation is feasible. We borrow terminology from cognitive science as a useful analogy (the Knowledge/Memory distinction echoes Tulving's trichotomy), but our layers are engineering constructs justified by persistence-semantics requirements, not by neural architecture. We argue that these distinctions demand distinct persistence semantics in engineering implementations, and that no current framework or system provides this.

---

## 论文详细总结（自动生成）

这篇论文对当前 AI 智能体认知架构中的记忆系统提出了深刻的批判与改进方案。以下是详细的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：论文指出当前主流的 AI 认知架构（如 CoALA 和 JEPA）存在“范畴错误”（Category Error）。它们将**知识（Knowledge）**与**记忆（Memory）**混为一谈，导致系统对永恒的事实（如科学发现）应用了本该属于经验的“认知衰减”（遗忘机制），或者对瞬时的经验应用了永久存储。
*   **研究背景**：现有的智能体存储系统（如 Mem0、NornicDB）通常采用统一的 CRUD 操作或简单的半衰期模型。作者认为，事实不应随时间淡化，而应被新证据“更替”（Supersession）；只有感官经验才应遵循遗忘曲线。

### 2. 方法论：四层架构分解
作者提出了一种基于**持久化语义（Persistence Semantics）**差异的四层分解模型：
*   **知识层 (Knowledge)**：存储“世界真相”。
    *   **语义**：无限期保存，通过“更替”更新。
    *   **技术细节**：采用追加模式（Append-only）并记录溯源（Provenance）。当新事实与旧事实冲突时，建立链接并标记旧事实被更替，而非删除。
*   **记忆层 (Memory)**：存储“发生了什么”。
    *   **语义**：遵循艾宾浩斯遗忘曲线（Ebbinghaus decay）。
    *   **技术细节**：采用双时态（Bi-temporal）事件溯源模型，记录四个时间戳（系统创建/过期、现实有效/失效），用于解决时间冲突。
*   **智慧层 (Wisdom)**：存储“行动准则/经验教训”。
    *   **语义**：耐用且受证据门控修正（Evidence-gated revision）。
    *   **技术细节**：设置稳定性层级（Stability Tiers），只有当证据（如成功次数、无矛盾周期）超过阈值时，才更新或固化行为模式。
*   **智能层 (Intelligence)**：推理与规划能力。
    *   **语义**：瞬时性（Ephemeral）。仅在推理时存在，不留直接痕迹，通过调度前三层来产生结果。

**实现工具**：作者提供了两个配套的开源实现：`knowledge-base`（Python/SQLite，侧重更替逻辑）和 `memory-engine`（Rust，侧重双时态和遗忘算法）。

### 3. 实验设计
*   **数据集/场景**：使用了 **BEAM 基准测试**（Beyond a Million tokens）的 100K Token 切片。
*   **测试维度**：重点测试了现有系统得分最低的两个类别：**矛盾解决（Contradiction Resolution）**和**时间推理（Temporal Reasoning）**。
*   **对比方法**：
    *   **分类路由（Typed Routing）**：使用预言机（Oracle）将查询定向到专门的知识库或记忆引擎。
    *   **扁平基准（Flat Baseline）**：使用单一的、无类型区分的全文搜索（FTS5）存储。
*   **生成模型**：使用 Gemma 2 26B 本地模型进行推理和评分。

### 4. 资源与算力
*   **算力说明**：文中未详细列出具体的 GPU 型号、数量或训练时长。
*   **推理细节**：实验使用了 Gemma 2 26B 模型。作者强调其开发的 Rust 引擎极其轻量，内存占用小于 5MB，且核心操作（衰减计算、图遍历）不依赖 LLM 调用，保证了低延迟和离线运行能力。

### 5. 实验数量与充分性
*   **实验规模**：进行了初步实验（Pilot Evaluation），样本量 $N=80$（包含 20 个对话场景）。
*   **充分性评价**：
    *   **局限性**：实验规模较小，且在路由环节使用了“预言机分类器”（即假设系统能完美区分查询类型），这在实际应用中是一个挑战。
    *   **客观性**：实验针对性强，直接回应了论文关于“持久化语义”影响性能的假设，结果具有方向性的参考价值，但尚需大规模自动化测试验证。

### 6. 主要结论与发现
*   **性能提升**：分类路由架构在 BEAM 测试中的总准确率比扁平基准提高了 **12.8%**（46.3% vs 33.4%）。
*   **特定领域优势**：在时间推理上提升最显著（+15.0%），证明了双时态模型在处理历史事实顺序上的有效性；在矛盾解决上提升了 10.6%，验证了更替机制优于简单的覆盖或删除。
*   **架构必要性**：实验证明，当查询能被正确路由到具备相应持久化语义的存储层时，智能体的认知能力会有质的提升。

### 7. 优点
*   **理论深刻**：精准识别了 AI 记忆研究中“事实”与“经验”混淆的痛点，提出了清晰的工程化分类标准。
*   **工程落地**：不仅有理论，还提供了高性能的 Rust 和 Python 实现，强调“无 LLM 依赖”的确定性算法，具有很高的实用价值。
*   **前瞻性**：整合了 2026 年初多个独立社区的观察结果，反映了智能体架构演进的前沿趋势。

### 8. 不足与局限
*   **路由依赖**：该架构的成功高度依赖于如何准确地将用户查询分类到“知识”、“记忆”或“智慧”层，论文中对此分类器的实现细节描述较少。
*   **实验覆盖面**：目前的实验仅限于 BEAM 的一小部分，缺乏在大规模、多任务场景下的端到端性能评估。
*   **复杂度增加**：相比于扁平化的向量数据库，四层架构增加了系统的开发和维护复杂度。

（完）
