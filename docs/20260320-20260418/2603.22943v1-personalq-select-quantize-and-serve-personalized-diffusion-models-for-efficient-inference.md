---
title: "PersonalQ: Select, Quantize, and Serve Personalized Diffusion Models for Efficient Inference"
title_zh: PersonalQ：用于高效推理的个性化扩散模型选择、量化与部署
authors: "Qirui Wang, Qi Guo, Yiding Sun, Junkai Yang, Dongxu Zhang, Shanmin Pang, Qing Guo"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22943v1"
tags: ["query:cir-ml"]
score: 8.0
evidence: 个性化扩散模型与意图感知混合检索
tldr: 本研究针对个性化扩散模型在推理服务中面临的意图识别歧义和量化精度损失两大挑战，提出了PersonalQ框架。该框架包含Check-in模块，通过混合检索、LLM重排及主动澄清实现精准的模型选择与提示词重写；同时引入触发词感知量化（TAQ），在交叉注意力机制中对关键触发词表征采用混合精度保护。PersonalQ在提升意图对齐准确性的同时，显著优化了模型压缩与生成质量的权衡，支持高效、高保真的个性化模型部署。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22943v1/fig-001.webp\", \"caption\": \"Fig. 2. Trigger token (e.g., <sks>) is vulnerable under quantization. Following the token-specific sensitivity test in Sec. III-B, we quantize only the cross-attention Key/Value rows of the trigger token while keeping all other rows in full precision. Under 4-bit quantization, the trigger token exhibits much larger degradation than ordinary words, motivating our TAQ method.\", \"page\": 2, \"index\": 1, \"width\": 497, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2603.22943v1/fig-002.webp\", \"caption\": \"TABLE I QUANTITATIVE COMPARISON. RESULTS OF DIFFERENT QUANTIZATION METHODS FOR PERSONALIZED DIFFUSION CHECKPOINTS.\", \"page\": 4, \"index\": 2, \"width\": 518, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2603.22943v1/fig-003.webp\", \"caption\": \"Fig. 4. Qualitative comparison. (a) SDXL-Turbo with W8A8 (8-bit weights / 8-bit activations). (b) Stable Diffusion v1.5 with W8A8 (8-bit weights / 8-bit activations); Each column corresponds to a different personalized checkpoint.\", \"page\": 5, \"index\": 3, \"width\": 1056, \"height\": 316}]"
motivation: 解决个性化扩散模型在服务时因自然语言歧义导致的路由错误，以及标准量化方法对个性化概念表征的破坏问题。
method: 结合LLM重排与澄清机制进行模型选择，并采用触发词感知的混合精度量化（TAQ）来保护交叉注意力中的关键权重。
result: 实验证明PersonalQ在提升意图对齐度的同时，比现有扩散模型量化方法具有更优的压缩质量比。
conclusion: 该框架通过统一触发词信号连接选择与量化过程，为大规模、低延迟地提供高质量个性化生成服务提供了有效方案。
---

## 摘要
个性化文本生成图像允许用户将扩散模型微调为特定概念的检查点仓库，但高效地部署这些仓库面临两个挑战：首先，自然语言请求通常具有歧义，可能被错误地路由到视觉相似的检查点；其次，标准的训练后量化（PTQ）可能会扭曲编码个性化概念的脆弱表征。我们提出了 PersonalQ，这是一个统一的框架，通过共享信号——检查点的触发词（trigger token）——将检查点选择与量化联系起来。Check-in 通过结合意图感知的混合检索与基于大语言模型（LLM）的检查点上下文重排序，执行意图对齐的选择，并仅在多个意图均合理时提出简短的澄清问题；随后，它通过插入所选检查点的规范触发词来重写提示词。作为补充，触发词感知量化（TAQ）在交叉注意力机制中应用触发词感知的混合精度，在保留受触发词调节的键/值行（及其注意力权重）的同时，对剩余路径进行激进量化，以实现内存高效的推理。实验表明，PersonalQ 在意图对齐方面优于检索和重排序基准，而 TAQ 在压缩与质量的权衡上始终优于先前的扩散模型训练后量化方法，从而在不牺牲保真度的情况下实现了个性化检查点的可扩展部署。

## Abstract
Personalized text-to-image generation lets users fine-tune diffusion models into repositories of concept-specific checkpoints, but serving these repositories efficiently is difficult for two reasons: natural-language requests are often ambiguous and can be misrouted to visually similar checkpoints, and standard post-training quantization can distort the fragile representations that encode personalized concepts. We present PersonalQ, a unified framework that connects checkpoint selection and quantization through a shared signal -- the checkpoint's trigger token. Check-in performs intent-aligned selection by combining intent-aware hybrid retrieval with LLM-based reranking over checkpoint context and asks a brief clarification question only when multiple intents remain plausible; it then rewrites the prompt by inserting the selected checkpoint's canonical trigger. Complementing this, Trigger-Aware Quantization (TAQ) applies trigger-aware mixed precision in cross-attention, preserving trigger-conditioned key/value rows (and their attention weights) while aggressively quantizing the remaining pathways for memory-efficient inference. Experiments show that PersonalQ improves intent alignment over retrieval and reranking baselines, while TAQ consistently offers a stronger compression-quality trade-off than prior diffusion PTQ methods, enabling scalable serving of personalized checkpoints without sacrificing fidelity.