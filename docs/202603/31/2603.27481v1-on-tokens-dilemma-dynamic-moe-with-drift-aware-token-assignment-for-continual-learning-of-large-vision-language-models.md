---
title: "On Token's Dilemma: Dynamic MoE with Drift-Aware Token Assignment for Continual Learning of Large Vision Language Models"
title_zh: 论 Token 的困境：用于大视觉语言模型持续学习的漂移感知 Token 分配动态 MoE
authors: "Chongyang Zhao, Mingsong Li, Haodong Lu, Dong Gong"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27481v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 大型视觉语言模型的持续学习
tldr: 针对大视觉语言模型在持续指令微调中的遗忘问题，本文揭示了MoE架构中的“路由漂移”现象，即旧标记被错误分配给新专家。为此提出LLaVA-DyMoE框架，通过感知漂移的标记分配策略，引导歧义和旧标记避开新专家，并结合路由正则化增强专家专业化，在保持新知识学习的同时有效缓解了灾难性遗忘。
selection_source: fresh_fetch
motivation: MoE架构在持续学习中面临路由漂移问题，即旧任务标记在训练新任务时会被错误地路由至新专家，导致旧知识丢失。
method: 提出LLaVA-DyMoE框架，通过标记级分配引导和路由分数正则化，动态调整标记到专家的映射，保护旧路由模式并促进新专家专业化。
result: "实验结果显示，该方法在平均最终准确率上比基线提升了7%以上，同时将遗忘率降低了12%。"
conclusion: 通过感知标记特性的动态路由分配策略，可以有效解决MoE模型在多模态持续学习中的路由漂移难题，显著提升模型的知识保持能力。
---

## 摘要
多模态持续指令微调旨在通过从新数据中学习来不断增强大视觉语言模型（LVLMs），同时不遗忘先前获得的知识。混合专家（MoE）架构通过增量添加新专家并扩展路由，同时保持现有专家冻结，自然地促进了这一过程。然而，尽管存在专家隔离，基于 MoE 的持续学习者仍会因路由漂移（routing-drift）而遭受遗忘：旧任务的 token 会被错误地吸引到新添加的专家中，从而降低在先前任务上的性能。我们在 token 级别分析了这种失效模式，并揭示了 token 的困境：新任务数据中的模糊 token 和旧 token 提供的学习收益极小，但当它们被路由到新专家时，由于训练期间模糊的路由分配，会导致遗忘。受此启发，我们提出了 LLaVA-DyMoE，这是一个通过漂移感知 token 分配来增量扩展 MoE 的动态 MoE 框架。我们通过路由分数分布来表征 token 类型，并应用针对性的正则化。具体而言，token 级分配引导将模糊 token 和旧 token 引导离开新专家，以保留已建立的路由模式并缓解路由漂移，而互补的路由分数正则化则强制执行专家组分离并促进新专家的专业化。大量实验表明，我们的 LLaVA-DyMoE 有效地减轻了路由漂移引起的遗忘，与基准模型相比，平均最终准确率提高了 7% 以上，遗忘率降低了 12%。项目页面见 https://zhaoc5.github.io/DyMoE。

## Abstract
Multimodal Continual Instruction Tuning aims to continually enhance Large Vision Language Models (LVLMs) by learning from new data without forgetting previously acquired knowledge. Mixture of Experts (MoE) architectures naturally facilitate this by incrementally adding new experts and expanding routers while keeping the existing ones frozen. However, despite expert isolation, MoE-based continual learners still suffer from forgetting due to routing-drift: old-task tokens become mistakenly attracted to newly added experts, degrading performance on prior tasks. We analyze the failure mode at the token level and reveal the token's dilemma: ambiguous and old tokens in new-task data offer minimal learning benefit yet induce forgetting when routed to new experts, due to their ambiguous routing assignment during training. Motivated by this, we propose LLaVA-DyMoE, a dynamic MoE framework that incrementally expands the MoE with drift-aware token assignment. We characterize token types via their routing score distributions and apply targeted regularization. Specifically, a token-level assignment guidance steers ambiguous and old tokens away from new experts to preserve established routing patterns and alleviate routing-drift, while complementary routing score regularizations enforce expert-group separation and promote new-expert specialization. Extensive experiments demonstrate that our LLaVA-DyMoE effectively mitigates routing-drift-induced forgetting, achieving over a 7% gain in mean final accuracy and a 12% reduction in forgetting compared to baselines. The project page is https://zhaoc5.github.io/DyMoE.

---

## 论文详细总结（自动生成）

### 论文结构化总结：LLaVA-DyMoE

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：大视觉语言模型（LVLMs）在实际应用中需要不断学习新任务（持续学习），但直接微调会导致“灾难性遗忘”。混合专家（MoE）架构因其模块化特性，被认为是解决该问题的理想方案。
*   **核心问题**：论文指出，即使冻结了旧专家，动态扩展的 MoE 仍会发生遗忘。其根源在于**路由漂移（Routing-drift）**：新训练的路由器参数会将旧任务的 Token 错误地分配给未经训练的新专家。
*   **Token 的困境**：作者发现新任务数据中包含“模糊 Token”和“旧 Token”，它们对学习新知识贡献极小，却极易诱发路由漂移，导致模型在旧任务上性能崩溃。

#### 2. 方法论：LLaVA-DyMoE
*   **核心思想**：通过“漂移感知”的 Token 分配策略，在训练过程中动态识别并引导 Token 的流向，保护旧路由模式。
*   **关键技术细节**：
    *   **Token 分配引导 (TAG)**：通过计算 Token 对新旧专家组的路由分数差异（相对差异 $D_{rel}$），识别出“模糊 Token”。若 Token 表现出高度模糊性或偏向旧专家，TAG 会强制将其路由至旧专家组，屏蔽其对新专家的访问。
    *   **路由分数正则化 (RSR)**：
        *   **排他性损失 ($L_{exc}$)**：通过最小化新旧专家组权重的乘积，强制实现 Token 路由的“非此即彼”，减少跨组干扰。
        *   **专业化损失 ($L_{spe}$)**：鼓励新 Token 充分利用新专家，提升模型对新任务的塑性（Plasticity）。
*   **算法流程**：在 LLaVA 的 Transformer 层中插入基于 LoRA 的动态专家。训练时仅更新新专家和扩展的路由器参数，并施加 TAG 和 RSR 正则化。

#### 3. 实验设计
*   **数据集/场景**：使用 **CoIN Benchmark**，包含 8 个连续的视觉问答（VQA）任务：ScienceQA, TextVQA, ImageNet, GQA, VizWiz, RefCOCO, VQAv2, OCR-VQA。
*   **对比方法**：
    *   **参数高效微调**：LoRA, IncLoRA, O-LoRA（正交子空间）。
    *   **持续学习经典方法**：EWC（权重约束）, LwF（蒸馏）。
    *   **MoE 基准**：MoELoRA（固定专家数）, IncMoELoRA（增量专家）。
*   **评估指标**：平均最终准确率（MFN）、平均中间准确率（MAA）、向后迁移/遗忘率（BWT）。

#### 4. 资源与算力
*   **硬件设备**：使用了 **4 台 NVIDIA H100 GPU**。
*   **训练细节**：采用 DeepSpeed ZeRO-2 优化器，BF16 精度训练。
*   **效率表现**：相比基线方法，LLaVA-DyMoE 的训练时间仅增加约 **4.4%**（平均每个任务约 77 分钟），且由于正则化仅在训练期生效，**推理速度完全不受影响**。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 8 个任务的长序列持续学习实验，总样本量达 56.9 万。
*   **消融实验**：非常详尽，涵盖了 TAG、RSR 各组件的有效性、模糊阈值 $\tau$ 的敏感度、损失权重 $\alpha$、专家数量、Top-K 值以及 LoRA Rank 的影响。
*   **鲁棒性验证**：测试了不同的**任务顺序**（正向、反向、字母序）和**指令模板**（Diverse, 10Type），证明了方法的稳定性。
*   **兼容性验证**：展示了该方法可以与数据回放（Replay）和任务级路由方法结合，进一步提升性能。实验设计客观、公平且充分。

#### 6. 主要结论与发现
*   **性能领先**：LLaVA-DyMoE 在 MFN 指标上比最强基线提升了 **7.35%**，在 BWT（遗忘率）上降低了 **12%**。
*   **路由漂移是关键**：实验证实，通过在 Token 级别纠正路由偏差，比单纯在参数级别进行隔离能更有效地解决 MoE 的遗忘问题。
*   **Token 差异化处理**：并非所有 Token 都需要参与新专家的训练，保护“模糊 Token”不被误导是维持稳定性的核心。

#### 7. 优点
*   **理论深刻**：首次提出了“Token 的困境”并深入分析了 MoE 持续学习中的路由漂移现象。
*   **架构优雅**：无需复杂的任务预测器，仅依靠 Token 自身的路由分布即可实现自适应分配。
*   **高兼容性**：作为一种路由正则化手段，它与现有的数据驱动方法（如回放）完全正交，具有很强的实用价值。

#### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 7B 和 13B 参数规模，尚未验证其在超大规模模型（如 70B 或更大）上的表现。
*   **任务多样性**：虽然涵盖了 8 个 VQA 任务，但任务形式相对单一（均为问答类），对于长文本生成、多轮对话等更复杂的持续学习场景探讨不足。
*   **超参数微调**：虽然表现稳健，但模糊阈值 $\tau$ 在面对分布差异极大的新领域时，可能仍需要一定的经验性调整。

（完）
