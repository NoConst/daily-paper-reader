---
title: "DataProphet: Demystifying Supervision Data Generalization in Multimodal LLMs"
title_zh: DataProphet：揭秘多模态大语言模型中的监督数据泛化
authors: "Xuan Qi, Luxi He, Dan Roth, Xingyu Fu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19688v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大语言模型中的监督数据泛化
tldr: 本研究探讨了多模态大模型（MLLM）监督数据选择的泛化性问题，发现传统的基于任务相似性的直觉选择方法并不可靠。为此，作者提出了DataProphet，这是一种无需训练的评估指标，通过结合多模态困惑度、相似度和数据多样性来预测训练数据对目标基准的影响。实验表明，该指标与实际训练效果高度相关，在数据选择任务上显著优于现有基准甚至实验基准，为高效数据筛选提供了新方案。
selection_source: fresh_fetch
motivation: 旨在解决多模态大模型训练中，如何无需实际训练即可准确预估特定监督数据对目标任务性能提升的影响。
method: 提出了DataProphet指标，综合利用多模态困惑度、数据相似性以及多样性来量化数据集的潜在贡献。
result: "DataProphet与实际性能排名的相关性高达86.0%，且在数据选择效果上超越了均匀选择和现有的基于训练的基准方法。"
conclusion: 研究证明了特定数据集的特性比宽泛的任务类别更能决定泛化效果，且DataProphet为高效的数据筛选提供了可靠的无训练方案。
---

## 摘要
为多模态大语言模型（MLLMs）选择监督数据的传统做法是优先考虑与目标基准相似的数据集，例如文本密集型或以视觉为中心的任务。然而，这种直观的相似性是否能可靠地预测下游性能的提升仍不清楚。在这项工作中，我们迈出了回答一个实际问题的第一步：我们能否在进行任何训练之前，估计训练数据集对目标基准的影响？为了研究这个问题，我们对涵盖 7 个不同任务的 14 个视觉语言数据集的迁移情况进行了深入分析。我们的结果表明，直观的任务相似性是迁移能力的一个不可靠预测指标，泛化更多地取决于具体的数据集，而非其广泛的任务类别。受此发现启发，我们提出了 DATAPROPHET，这是一种简单且有效的无需训练的指标，它结合了多模态困惑度、相似性和数据多样性。实验表明，DATAPROPHET 生成的监督数据排名与基于实际训练后性能提升的排名高度相关，Kendall's tau 系数达到 86.0%。此外，DATAPROPHET 能够实现更好的监督数据选择，相比均匀选择提升高达 6.9%，相比最先进的基于训练的基准提升 1.4%，甚至比基于实验性能的理想选择高出 0.2%。我们的代码和数据将会发布。

## Abstract
Conventional wisdom for selecting supervision data for multimodal large language models (MLLMs) is to prioritize datasets that appear similar to the target benchmark, such as text-intensive or vision-centric tasks. However, it remains unclear whether such intuitive similarity reliably predicts downstream performance gains. In this work, we take a first step toward answering a practical question: can we estimate the influence of a training dataset on a target benchmark before any training is performed? To investigate this question, we conduct an in-depth analysis of transfer across 14 vision-language datasets spanning 7 diverse tasks. Our results show that intuitive task similarity is an unreliable predictor of transferability, and that generalization depends more on the specific dataset than on its broad task category. Motivated by this finding, we propose DATAPROPHET, a simple and effective training-free metric that combines multimodal perplexity, similarity, and data diversity. Experiments show that DATAPROPHET produces supervision-data rankings that strongly correlate with rankings based on actual post-training performance gains, achieving a Kendall's tau of 86.0%. Moreover, DATAPROPHET enables better supervision-data selection, yielding up to 6.9% improvement over uniform selection, 1.4% over a state-of-the-art training-based baseline, and 0.2% above oracle selection based on experimental performance. Our code and data will be released.

---

## 论文详细总结（自动生成）

这篇论文《DataProphet: Demystifying Supervision Data Generalization in Multimodal LLMs》深入探讨了多模态大语言模型（MLLM）在监督微调阶段，训练数据如何影响目标任务性能的内在机制。

### 1. 核心问题与整体含义
*   **研究动机**：在MLLM训练中，人们通常凭直觉选择与目标任务“相似”的数据（如用OCR数据提升图表理解）。但这种直觉是否可靠？能否在不进行实际训练的情况下，预知某数据集对特定目标的贡献？
*   **核心问题**：论文旨在揭示监督数据泛化的真实规律，并提出一种**无需训练（Training-free）**的指标来量化和预测训练数据对目标基准的性能提升（Influence）。

### 2. 方法论
*   **核心思想**：提出 **DataProphet** 指标，通过综合考虑数据的难度（困惑度）、领域匹配度（相似性）和覆盖范围（多样性）来预测数据价值。
*   **关键技术细节**：
    *   **多模态困惑度 (Multimodal Perplexity, PPL)**：利用基础模型计算源数据集 $s$ 和目标数据集 $t$ 的 PPL。高 PPL 通常代表数据更具挑战性，蕴含更多学习空间。
    *   **跨数据集相似性 (Similarity)**：分别计算问题（Q）、答案（A）和图像（I）在模型嵌入空间中的余弦相似度。
    *   **数据多样性 (Diversity)**：对源数据的提问进行聚类（K-means），通过轮廓系数（Silhouette）和信息熵（Entropy）衡量数据的覆盖面和平衡性。
*   **公式表达**：
    $$M(s \to t) = QSim \cdot ASim \cdot ISim \cdot \frac{PPL(s)}{PPL(t)} \cdot (Sil + H)$$
    该公式采用乘积形式，意味着任何一个维度的缺失或薄弱都会显著降低该数据的预测得分。

### 3. 实验设计
*   **数据集与场景**：涵盖 7 大类任务（OCR、图表、文档、通用VQA、空间推理、计数、地图推理），共 14 个代表性数据集（如 OCR-VQA, ChartQA, DocVQA 等）。
*   **Benchmark**：以模型在目标任务上的**相对性能提升（Relative Improvement）**作为真值（Ground Truth）。
*   **对比方法**：
    *   **Uniform**：均匀采样。
    *   **Oracle**：基于实际训练结果的最优选择（理想上限）。
    *   **ICONS**：目前最先进的基于梯度/训练的数据选择方法。
*   **应用场景**：包括真实数据重加权、合成数据筛选（GPT-5 与 Gemini 2.5 Pro 生成）以及 RL（强化学习）后处理数据选择。

### 4. 资源与算力
*   **基础模型**：主要使用 **InternVL3-2B**，RL 阶段使用 **Qwen2.5VL-3B-Instruct**。
*   **算力细节**：文中提到所有实验在“固定计算预算”下进行（如每个源数据集采样 20K 样本进行 SFT）。虽然未明确列出具体的 GPU 型号（如 A100 数量）和总训练时长，但强调了其方法是 **Training-free** 的，相比需要计算梯度的 ICONS 等方法，计算开销极低。

### 5. 实验数量与充分性
*   **实验规模**：进行了 14×14 的交叉训练与评估（共 196 组完整训练任务）来构建数据影响矩阵。
*   **充分性**：实验覆盖了从 SFT 到 RL、从真实数据到合成数据的多个维度。消融实验逐一验证了 PPL、相似度和多样性的贡献。
*   **客观性**：通过 Kendall’s $\tau$ 相关系数衡量预测排名与真实排名的一致性，结果显示相关性高达 **86.0%**，证明了实验的严谨性。

### 6. 主要结论与发现
*   **直觉不可靠**：任务类别的相似性并不能保证性能迁移。例如，OCR 数据对空间推理任务的帮助反而大于对图表任务的帮助。
*   **数据集特异性**：泛化效果取决于具体的数据集特征，而非宏观的任务标签。
*   **DataProphet 的有效性**：在数据选择任务中，DataProphet 相比均匀采样提升了 **6.9%**，甚至在某些情况下超过了基于真实实验结果的 Oracle 选择（+0.2%）。
*   **合成数据质量**：实验发现 Gemini 2.5 Pro 生成的合成数据在 DataProphet 指标下优于 GPT-5，且实际训练效果也更好。

### 7. 优点
*   **高效节能**：无需预训练或计算梯度，仅需推理即可完成数据筛选，极大地降低了数据准备成本。
*   **洞察深刻**：挑战了“任务相似即有效”的传统认知，为多模态数据配比提供了科学依据。
*   **通用性强**：不仅适用于 SFT，还能指导合成数据生成和 RL 阶段的 Prompt 选择。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 2B-3B 规模的模型上，尚未在大规模模型（如 70B+）上验证该指标的缩放定律（Scaling Laws）。
*   **任务形式单一**：主要针对 VQA（视觉问答）形式的任务，对于多轮对话、长视频理解等更复杂的多模态场景覆盖不足。
*   **指标依赖性**：DataProphet 的准确性高度依赖于基础模型本身的编码能力和 PPL 计算的准确性。

（完）
