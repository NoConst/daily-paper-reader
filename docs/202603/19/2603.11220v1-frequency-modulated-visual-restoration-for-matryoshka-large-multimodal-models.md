---
title: Frequency-Modulated Visual Restoration for Matryoshka Large Multimodal Models
title_zh: 针对俄罗斯套娃大多模态模型的频率调制视觉恢复
authors: "Qingtao Pan, Zhihao Dou, Shuo Li"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11220v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 大型多模态模型中的视觉Token减少
tldr: 针对大语言多模态模型（LMMs）中视觉标记过多导致的计算开销问题，本文提出了一种名为FMVR的频率调制视觉恢复策略。该方法通过解耦视觉表示的高低频成分并进行调制，在大幅减少视觉标记数量的同时，有效保留并恢复了视觉语义信息。结合Matryoshka表示学习，FMVR实现了推理时视觉标记数量的弹性调整，在显著降低计算量的同时保持了极高的模型性能。
selection_source: fresh_fetch
motivation: 现有的视觉标记缩减方法往往会导致视觉语义信息的丢失，难以在有限的计算预算下平衡效率与性能。
method: 提出FMVR策略，利用AvgPool和MaxPool提取并调制高低频成分以增强视觉语义，并结合Matryoshka学习实现标记数量的弹性缩减。
result: "在14个图像和视频基准测试中，FMVR-LLaVA在减少89%计算量的情况下，几乎完全保留了原始模型的准确率。"
conclusion: FMVR是一种简单高效的即插即用策略，为大模型在不同计算资源约束下的灵活部署提供了有效方案。
---

## 摘要
大多模态模型 (LMMs) 由于视觉 token 数量庞大，难以适应不同的计算预算。先前的方法尝试在 LLMs 之前或内部减少视觉 token 的数量，但这些策略不可避免地会导致视觉语义的丢失。为了解决这些问题，我们引入了 FMVR，这是一种即插即用且极其简单的频率调制视觉恢复策略，旨在提升 LMMs 在视觉 token 减少情况下的推理能力。具体而言，FMVR 通过平均池化 (AvgPool) 和最大池化 (MaxPool) 将较少视觉 token 的视觉表示解耦为低频和高频分量。随后使用轻量级可学习参数对导出的频率进行调制。来自 AvgPool 的高频分量作为显著性滤波器来增强显著视觉语义，而来自 MaxPool 的低频分量作为反显著性滤波器来加强微弱视觉语义。这使得模型能够保留由少量视觉 token 主导的视觉语义，并恢复被稀释的视觉语义。此外，我们将 FMVR 注入到俄罗斯套娃表示学习 (Matryoshka Representation Learning) 中，以学习从粗到细的视觉 token 集合，从而能够在推理过程中弹性调整视觉 token 的数量，同时保持相当的性能。在 10 个基于图像和 4 个基于视频的基准测试上的实验表明，FMVR-LLaVA 将 LLaVA-1.5-7B 的 FLOPs 降低了 89%，同时保持了几乎 100% 的原始准确率。代码将开源。

## Abstract
Large Multimodal Models (LMMs) struggle to adapt varying computational budgets due to numerous visual tokens. Previous methods attempted to reduce the number of visual tokens before or within LLMs. However, these strategies inevitably result in the loss of visual semantic. To address these issues, we introduce FMVR, a plug-and-play and extremely simple Frequency-Modulated Visual Restoration strategy to boost the reasoning ability of LMMs under visual token reduction. Specifically, FMVR disentangles the visual representation of fewer visual tokens into low- and high-frequency components through AvgPool and MaxPool. The derived frequencies are subsequently modulated using lightweight learnable parameters. The high-frequency from AvgPool acts as a saliency filter to enhance saliency visual semantics, while the low-frequency from MaxPool acts as an anti-saliency filter to strengthen weak visual semantics. It enables the preservation of visual semantics dominated by few visual tokens and the restoration of diluted visual semantics. Additionally, we inject FMVR into Matryoshka Representation Learning to learn coarse-to-fine visual token sets, thus enabling to elastically adjust the number of visual tokens during inference while maintaining comparable performance. Experiments across 10 image-based and 4 video-based bench marks demonstrate that FMVR-LLaVA reduce the FLOPs of LLaVA-1.5-7B by 89%, while maintaining almost 100% of the original accuracy. The code will be open.

---

## 论文详细总结（自动生成）

以下是对论文《Frequency-Modulated Visual Restoration for Matryoshka Large Multimodal Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大多模态模型（LMMs）在处理图像时会产生大量的视觉 Token（如 LLaVA-1.5 处理一张图产生 576 个 Token），这导致了巨大的计算开销（计算量随序列长度呈平方级增长）。
*   **研究背景**：现有的 Token 压缩或剪枝方法虽然能减少计算量，但往往会导致视觉语义信息的丢失，尤其是在 Token 数量极少的情况下，模型难以捕捉细微的视觉特征。
*   **研究动机**：开发一种既能大幅减少视觉 Token 数量，又能通过“视觉恢复”机制保持甚至增强模型推理能力的方案，并支持在推理时根据计算预算弹性调整 Token 数量。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出 **FMVR（频率调制视觉恢复）** 策略。该方法通过频率解耦技术，从压缩后的 Token 中提取并增强高频（显著性）和低频（弱语义）成分，从而补偿 Token 减少带来的信息损失。
*   **关键技术细节**：
    *   **频率解耦与调制**：
        *   **AvgPool 单元（显著性增强）**：利用平均池化提取低频成分，通过原特征与低频的残差获得高频成分。高频成分作为“显著性滤波器”，通过可学习参数调制后，增强图像中的关键区域。
        *   **MaxPool 单元（反显著性增强）**：利用最大池化提取显著特征，通过残差获得低频成分。此处的低频成分作为“反显著性滤波器”，旨在挖掘并强化被忽视的微弱视觉语义，防止强特征过度主导。
    *   **结合俄罗斯套娃表示学习（MRL）**：将 FMVR 注入 MRL 框架，构建嵌套的视觉 Token 集合（如 576、144、36、9、1 个 Token）。这使得单一模型在推理时可以根据需求自由选择 Token 规模。
    *   **即插即用**：FMVR 模块极其轻量，可直接集成到现有的 LMM 架构（如 LLaVA, Qwen-VL）中。

### 3. 实验设计
*   **数据集与场景**：
    *   **图像基准（10个）**：包括 VQAv2, GQA, VizWiz, ScienceQA, POPE, MME, MMBench, MMBench-CN, MMVet, TextVQA。
    *   **视频基准（4个）**：MSVD-QA, MSRVTT-QA, ActivityNet-QA 以及视频生成性能基准。
*   **对比方法**：
    *   **基准模型**：LLaVA-1.5, LLaVA-NeXT, Qwen-VL, BLIP-2, InstructBLIP 等。
    *   **Token 缩减方法**：FastV, PyramidDrop, SparseVLM, Prumerge+, VisionZip, DART, M3, MQT-LLaVA 等。

### 4. 资源与算力
*   **算力设备**：使用了 **4 台 NVIDIA H100 GPU**。
*   **训练时长**：采用两阶段训练。第一阶段在 LLaVA-558K 数据集上训练 1 个 epoch；第二阶段在 LLaVA-665K（针对 LLaVA-1.5）或 LLaVA-1M（针对 LLaVA-NeXT）上微调 1 个 epoch。
*   **效率提升**：FMVR-LLaVA 在使用 36 个 Token 时，相比原始 LLaVA-1.5 实现了 **8.9 倍的 FLOPs 降低**，预填（Prefill）时间缩短了约 3.2 倍。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从 7B 到 13B 参数规模的模型，并在图像和视频两大领域共 14 个主流基准上进行了测试。
*   **消融实验**：针对 AvgPool 和 MaxPool 单元的有效性、不同的 Token 采样策略（空间采样、顺序采样等）进行了详细对比。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了准确率，还详细分析了计算量（FLOPs）、推理延迟（Prefill Time）和内存占用，并提供了 Grad-CAM 视觉解释，证明了方法的有效性。

### 6. 主要结论与发现
*   **性能保持**：FMVR-LLaVA 在仅使用 144 个 Token 时，性能甚至超过了使用 576 个 Token 的原始 LLaVA-1.5（平均分高出 0.8%）。
*   **极端压缩下的鲁棒性**：即使压缩到仅剩 1 个 Token，FMVR 依然能保持一定的推理能力，显著优于其他剪枝方法。
*   **高分辨率与视频优势**：在处理高分辨率图像（2880 Token 缩减至 180）和视频任务时，FMVR 表现出极高的计算效率和语义保留能力。
*   **计算开销极低**：FMVR 模块引入的额外计算量几乎可以忽略不计（仅占总 FLOPs 的极小比例）。

### 7. 优点
*   **简单且高效**：通过简单的池化操作实现频率调制，逻辑清晰，易于实现。
*   **弹性推理**：支持 Matryoshka 嵌套结构，解决了模型部署时对不同计算预算的适应性问题。
*   **语义恢复机制**：首次在 Token 压缩领域明确提出“视觉恢复”概念，通过增强弱语义有效缓解了幻觉问题。

### 8. 不足与局限
*   **极端情况限制**：虽然在 1 个 Token 下表现优于同类，但在处理极其复杂的图像（如包含大量微小文字或密集物体的场景）时，极低 Token 数仍会导致错误。
*   **架构依赖**：实验主要基于 LLaVA 和 Qwen 框架，虽然证明了通用性，但在其他非 Transformer 架构或更激进的视觉编码器上的表现尚待验证。
*   **训练成本**：虽然推理高效，但实现 Matryoshka 结构需要完整的两阶段微调，对于已有模型的直接适配仍需一定的训练资源。

（完）
