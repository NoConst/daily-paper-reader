---
title: "OPERA: Online Data Pruning for Efficient Retrieval Model Adaptation"
title_zh: OPERA：面向高效检索模型适配的在线数据剪枝
authors: "Haoyang Fang, Shuai Zhang, Yifei Ma, Hengyi Wang, Cuixiong Hu, Katrin Kirchhoff, Bernie Wang, George Karypis"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17205v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 密集检索器的在线数据剪枝
tldr: "本研究针对稠密检索器领域微调中训练数据贡献不均的问题，提出了OPERA在线数据剪枝框架。该框架包含静态剪枝和两阶段动态剪枝策略，通过在训练过程中动态调整采样概率，优先处理高质量样本并兼顾查询多样性。实验证明，该方法在提升排序与检索性能的同时，将训练时间缩短了50%以上，且在LLM检索器上表现出良好的通用性。"
selection_source: fresh_fetch
motivation: 领域微调中并非所有训练对都有同等贡献，全量训练存在效率低下且低质量数据可能损害检索效果的问题。
method: 提出OPERA框架，利用静态剪枝筛选高相似度对，并结合两阶段动态剪枝在训练中自适应调节查询和文档的采样权重。
result: 动态剪枝在多个领域数据集上显著提升了NDCG和召回率，且仅需不到一半的训练时间即可达到基准性能。
conclusion: OPERA通过智能数据剪枝实现了检索模型适配的高效性与有效性，是提升大规模检索模型训练效率的通用方案。
---

## 摘要
领域特定微调对于稠密检索器至关重要，但并非所有训练对对学习过程的贡献都相同。我们提出了 OPERA，这是一个利用这种异质性来提高检索模型适配有效性和效率的数据剪枝框架。我们首先研究了静态剪枝（SP），它仅保留高相似度的查询-文档对，揭示了内在的质量-覆盖率权衡：排序（NDCG）有所提高，但由于查询多样性降低，检索（Recall）可能会下降。为了解决这一权衡，我们提出了一种两阶段动态剪枝（DP）策略，该策略在整个训练过程中自适应地调节查询和文档层面的采样概率，在优先考虑高质量样本的同时保持对完整训练集的访问。在跨越六个领域的八个数据集上的评估证明了这两种方法的有效性：SP 相比标准微调提高了排序性能（NDCG@10 +0.5%），而 DP 在排序（NDCG@10 +1.9%）和检索（Recall@20 +0.7%）上均取得了最强性能，在所有方法中的平均排名为 1.38。这些发现可以扩展到基于大语言模型（LLM）的稠密检索器 Qwen3-Embedding，证实了其与架构无关的优势。值得注意的是，DP 在不到标准微调所需训练时间 50% 的情况下，即可达到相当的性能。

## Abstract
Domain-specific finetuning is essential for dense retrievers, yet not all training pairs contribute equally to the learning process. We introduce OPERA, a data pruning framework that exploits this heterogeneity to improve both the effectiveness and efficiency of retrieval model adaptation. We first investigate static pruning (SP), which retains only high-similarity query-document pairs, revealing an intrinsic quality-coverage tradeoff: ranking (NDCG) improves while retrieval (Recall) can degrade due to reduced query diversity. To resolve this tradeoff, we propose a two-stage dynamic pruning (DP) strategy that adaptively modulates sampling probabilities at both query and document levels throughout training, prioritizing high-quality examples while maintaining access to the full training set. Evaluations across eight datasets spanning six domains demonstrate the effectiveness of both approaches: SP improves ranking over standard finetuning (NDCG@10 +0.5\%), while DP achieves the strongest performance on both ranking (NDCG@10 +1.9\%) and retrieval (Recall@20 +0.7\%), with an average rank of 1.38 across all methods. These findings scale to Qwen3-Embedding, an LLM-based dense retriever, confirming architecture-agnostic benefits. Notably, DP reaches comparable performance in less than 50\% of the training time required by standard finetuning.

---

## 论文详细总结（自动生成）

以下是对论文《OPERA: Online Data Pruning for Efficient Retrieval Model Adaptation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
稠密检索器（Dense Retrievers）在特定领域应用时通常需要微调，但并非所有训练数据对模型性能的提升都有同等贡献。现有的数据剪枝方法多针对分类或生成任务（样本独立同分布），而检索任务具有独特的**两阶段采样结构**（先选查询，再选正/负文档）。
论文指出，简单地根据质量过滤数据会面临**质量-覆盖率权衡（Quality-Coverage Tradeoff）**：虽然能提升排序性能（NDCG），但会因为减少了查询的多样性而损害检索召回率（Recall）。因此，研究如何针对检索任务的层级结构进行高效、在线的数据剪枝是本文的核心动机。

### 2. 论文提出的方法论：OPERA 框架
OPERA 包含两种策略，旨在利用训练数据的异质性提升效率：
*   **静态剪枝 (Static Pruning, SP)：** 在训练前计算查询-文档对的余弦相似度，仅保留相似度最高的前 $k$ 个样本。这被证明能显著提升排序指标，但会牺牲召回率。
*   **动态剪枝 (Dynamic Pruning, DP)：** 这是本文的核心贡献，采用两阶段自适应采样：
    *   **层级化剪枝：** 分别在查询（Query）和文档（Document）两个粒度上进行采样概率调节。
    *   **软剪枝机制：** 不直接丢弃低质量数据，而是降低其采样权重，确保模型仍能接触到全量数据以维持覆盖率。
    *   **动态阈值调度：** 使用余弦退火调度（Cosine Scheduling）随训练进程调整采样强度，初期关注多样性，后期聚焦高质量样本。
    *   **高效更新：** 引入更新间隔（Update Interval），每隔若干步更新一次查询得分，以降低计算开销。

### 3. 实验设计
*   **数据集：** 涵盖 6 个领域的 8 个数据集，包括 NFCorpus（营养）、TripClick（医疗）、FiQA（金融）、ANTIQUE（非事实问答）、TriviaQA/HotpotQA（事实问答）和 FEVER（事实核查）。
*   **模型架构：** 验证了编码器架构（BGE-large-en-v1.5，335M）和解码器架构（Qwen3-Embedding-0.6B）。
*   **对比方法 (Benchmarks)：** 
    *   标准全量微调 (Standard Finetuning, FT)
    *   预训练模型 (Pretrained)
    *   InfoBatch（通用的动态剪枝基准）
    *   随机剪枝 (Random Pruning)
*   **评估指标：** NDCG@10（排序性能）和 Recall@20（检索覆盖率）。

### 4. 资源与算力
*   **硬件说明：** 论文未明确指出具体的 GPU 型号（如 A100 或 H100）及具体数量。
*   **训练配置：** 提到 BGE 模型使用 FP16 精度，Qwen3 使用 BF16 精度；Batch Size 为 64；训练迭代次数在 8,000 到 32,000 次之间。
*   **效率提升：** 明确指出 DP 方法在不到 50% 的标准训练时间内即可达到相当的性能，且引入的额外计算开销仅为 1.64% - 4.5%。

### 5. 实验数量与充分性
*   **实验规模：** 进行了跨 8 个数据集的对比实验，涵盖了预训练阶段“见过”和“未见过”的数据。
*   **消融实验：** 针对层级化剪枝（仅查询 vs 仅文档 vs 两者结合）、更新间隔、静态剪枝保留率等进行了详细消融。
*   **鲁棒性验证：** 专门设计了“去噪能力”实验，在含有噪声标签的数据集上验证 OPERA 的稳健性。
*   **充分性评价：** 实验设计非常充分，不仅覆盖了多种领域和架构，还从理论分析（引理与定理证明）和可视化（采样权重演变）角度提供了深度见解，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **解决了权衡问题：** 动态剪枝（DP）成功解决了静态剪枝中“排序提升但召回下降”的矛盾，在所有指标上均优于标准微调。
*   **效率倍增：** DP 显著加快了收敛速度，节省了超过一半的训练时间。
*   **架构无关：** 方法在传统的双编码器和最新的 LLM 嵌入模型上均有效。
*   **去噪效果：** 在标签含有噪声的情况下，OPERA 表现出比标准微调更强的鲁棒性，尤其是 SP 和 DP 结合的两阶段流水线效果最佳。

### 7. 优点
*   **针对性强：** 首次针对检索任务的层级采样结构设计剪枝算法，而非照搬分类任务的方法。
*   **理论支撑：** 提供了数学证明，阐明了剪枝在何种条件下能改善查询表示。
*   **实用性高：** 兼容现有的固定迭代训练框架，且计算开销极低，易于在工业界部署。

### 8. 不足与局限
*   **语言限制：** 实验主要集中在英语数据集上，未验证多语言环境下的表现。
*   **超参数敏感性：** 虽然论文使用了统一的超参数，但动态采样的强度（$\alpha, \beta$）和调度策略可能仍需针对特定新任务进行微调。
*   **模型规模：** 实验中使用的 LLM 嵌入模型规模较小（0.6B），尚未在 7B 或更大规模的模型上进行大规模验证。

（完）
