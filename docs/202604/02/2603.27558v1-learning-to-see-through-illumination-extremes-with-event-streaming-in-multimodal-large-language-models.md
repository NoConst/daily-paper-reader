---
title: Learning to See through Illumination Extremes with Event Streaming in Multimodal Large Language Models
title_zh: 在多模态大语言模型中利用事件流实现在极端光照条件下的视觉感知
authors: "Baoheng Zhang, Jiahui Liu, Gui Zhao, Weizhou Zhang, Yixuan Ma, Jun Jiang, Yingxian Chen, Wilton W. T. Fok, Xiaojuan Qi, Hayden Kwok-Hay So"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27558v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 用于视觉语言推理的多模态大语言模型
tldr: 针对多模态大语言模型在极端光照下因RGB信息丢失而失效的问题，本文提出了Event-MLLM。该模型通过动态融合事件流与RGB帧，利用事件相机的高动态范围特性增强视觉推理。核心贡献包括：一个可学习的光照指示器用于自适应调节融合，以及光照校正损失函数以在潜在空间对齐正常光照语义。此外，研究者构建了首个多光照事件指令数据集，实验证明该模型在极端光照下的感知与推理能力显著优于现有基准。
selection_source: fresh_fetch
motivation: 传统多模态大模型在极端光照条件下由于RGB图像的结构和语义信息严重受损，导致视觉推理任务失败。
method: 提出Event-MLLM模型，利用光照指示器动态调节事件与RGB的融合，并通过光照校正损失将退化特征与正常光照语义对齐。
result: 在涵盖17种亮度等级的新型多光照事件指令基准测试中，该模型在推理、计数和识别任务上均显著优于现有通用及专用模型。
conclusion: 通过引入事件流信息并结合自适应融合机制，可以显著提升多模态大模型在全光照环境下的鲁棒感知与推理能力。
---

## 摘要
多模态大语言模型 (MLLMs) 在标准条件下表现出强大的视觉语言推理能力，但在极端光照下会失效，因为此时 RGB 输入会不可逆地丢失结构和语义信息。我们提出了 Event-MLLM，这是一种事件增强模型，通过动态融合事件流与 RGB 帧来实现全光照视觉推理。该方法由两个关键组件驱动：光照指示器（Illumination Indicator）——一个源自 DINOv2 分支的可学习信号，用于表征曝光退化并自适应地调节事件-RGB 融合；以及光照校正损失（Illumination Correction Loss），用于在潜空间中将融合特征与未退化（正常光照）的语义对齐，从而补偿极端光照下丢失的信息。我们构建了首个用于 MLLMs 的多光照事件指令语料库，包含跨越不同场景和 17 种亮度倍率（0.05x - 20x）的 2,241 个事件-RGB 样本（每个样本约包含 6 个问答对），以及一个用于极端光照下推理、计数和细粒度识别的指令遵循基准测试。实验表明，Event-MLLM 显著优于通用型、光照自适应型以及仅事件型的基线模型，在挑战性光照条件下的鲁棒多模态感知与推理方面达到了新的最先进水平。

## Abstract
Multimodal Large Language Models (MLLMs) perform strong vision-language reasoning under standard conditions but fail in extreme illumination, where RGB inputs lose irrevocable structure and semantics. We propose Event-MLLM, an event-enhanced model that performs all-light visual reasoning by dynamically fusing event streams with RGB frames. Two key components drive our approach: an Illumination Indicator - a learnable signal derived from a DINOv2 branch that represents exposure degradation and adaptively modulates event-RGB fusion - and an Illumination Correction Loss that aligns fused features with non-degraded (normal-light) semantics in the latent space, compensating for information lost in extreme lighting. We curate the first multi-illumination event-instruction corpus for MLLMs, with 2,241 event-RGB samples (around 6 QA pairs each) across diverse scenes and 17 brightness rates (0.05x - 20x), plus an instruct-following benchmark for reasoning, counting, and fine-grained recognition under extreme lighting. Experiments show that Event-MLLM markedly outperforms general-purpose, illumination-adaptive, and event-only baselines, setting a new state of the art in robust multimodal perception and reasoning under challenging illumination.

---

## 论文详细总结（自动生成）

以下是对论文《Learning to See through Illumination Extremes with Event Streaming in Multimodal Large Language Models》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的多模态大语言模型（MLLMs）在标准光照下表现优异，但在极端光照（如极暗或过曝）下，RGB图像会不可逆地丢失结构和语义信息，导致模型产生幻觉或推理错误。
*   **核心问题**：如何增强 MLLM 在全光照条件下的鲁棒性，使其在视觉信息严重退化时仍能进行准确的推理。
*   **背景**：事件相机（Event Camera）具有超高动态范围（>120dB）和微秒级响应，能捕捉亮度变化而非绝对强度，因此在极端光照下仍能保留丰富的结构线索。

### 2. 方法论
Event-MLLM 采用双流输入架构，通过动态融合事件流与 RGB 帧来补偿视觉损失。
*   **核心思想**：利用事件信息作为“补丁”，在特征空间中修复受损的 RGB 语义，并将其对齐到正常光照的语义流中。
*   **关键技术**：
    *   **光照指示器（Illumination Indicator）**：利用冻结的 DINOv2 编码器提取 RGB 图像的光照特征，通过一个可学习的 MLP 生成信号，用于量化曝光退化程度，并指导事件特征与 RGB 特征的自适应融合权重。
    *   **光照校正损失（Illumination Correction Loss, $L_{IC}$）**：在训练阶段，使用“三元组”输入（正常光照、极端光照、事件流）。通过计算融合特征与正常光照特征之间的均方误差（MSE），强制模型在潜在空间中将退化特征对齐到非退化语义。
    *   **两阶段训练流程**：
        1.  **自适应特征融合阶段**：训练融合模块，使模型学会从事件流中提取补充信息以重建正常光照下的特征表示。
        2.  **指令遵循微调阶段**：使用 LoRA 技术对整个 MLLM 进行微调，实现视觉特征与语言空间的对齐。

### 3. 实验设计
*   **数据集与场景**：构建了首个多光照事件指令数据集，包含 2,241 个样本和 10,129 个问答对。涵盖室内外多种场景，并人工合成了 17 种亮度等级（从 0.05x 极暗到 20x 极亮）。
*   **Benchmark**：设计了两个任务：**多项选择（Multi-Choice）**用于评估场景理解，**物体计数（Object-Counting）**用于评估细粒度感知。
*   **对比方法**：
    *   通用模型：LLaVA-7B, DeepSeek-VL, InternVL。
    *   事件专用模型：EventGPT。
    *   光照感知模型：Q-Instruct。
    *   基线模型：基于 Qwen-3B/7B 的消融基线。

### 4. 资源与算力
*   **硬件设备**：使用了 **NVIDIA RTX 5090D** 和 **NVIDIA A800** GPU。
*   **训练细节**：
    *   特征融合阶段：Adam 优化器，Batch Size 为 4，学习率 0.001，训练 30 个 Epoch。
    *   LoRA 微调阶段：训练 1 个 Epoch。
*   **模型规模**：主要基于 Qwen-3B 和 Qwen-7B 骨干网络进行实验。

### 5. 实验数量与充分性
*   **实验规模**：在 17 种不同的亮度倍率下进行了详尽测试，涵盖了从极度欠曝到极度过曝的连续区间。
*   **消融实验**：对比了有无光照校正损失、有无 LoRA 微调的效果，并探讨了“前融合”（像素级）与“后融合”（特征级）的差异。
*   **可视化分析**：通过 t-SNE 聚类分析和余弦相似度表格，直观展示了融合特征如何向正常光照特征靠拢。
*   **评价**：实验设计较为充分，通过多维度指标（Accuracy, F1, MAE）和跨模型规模（3B vs 7B）的对比，验证了方法的客观性和普适性。

### 6. 主要结论与发现
*   **性能领先**：Event-MLLM 在所有亮度等级下均显著优于现有 SOTA 模型，尤其在极端光照下，准确率提升明显。
*   **语义对齐有效性**：$L_{IC}$ 损失能有效引导模型在推理时自主纠正光照退化，无需正常光照参考图。
*   **模型容量影响**：更强大的语言模型（如 7B）对残余特征噪声的容忍度更高，能更稳定地整合多模态线索。
*   **融合策略**：简单的像素级融合效果极差，说明 MLLM 的视觉编码器难以处理非自然分布的图像，而特征级自适应融合是关键。

### 7. 优点
*   **创新性**：首次将事件相机引入 MLLM 领域，解决了传统 RGB 模型在极端环境下的“致盲”问题。
*   **数据集贡献**：填补了社区在多光照事件-指令数据集上的空白。
*   **鲁棒性**：通过轻量级的插件设计（指示器+损失函数），实现了对全光照环境的稳健覆盖。

### 8. 不足与局限
*   **硬件依赖**：该方法依赖于事件相机这一特殊传感器，目前在主流消费级设备上普及率较低。
*   **数据稀疏性处理**：当事件信号非常稀疏或间歇性缺失时，模型的表现尚未得到充分讨论。
*   **泛化限制**：虽然涵盖了 17 种亮度，但对于更复杂的真实世界退化（如运动模糊与极端光照叠加）的泛化能力仍有待进一步验证。

（完）
