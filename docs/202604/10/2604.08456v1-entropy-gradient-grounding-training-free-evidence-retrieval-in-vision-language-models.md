---
title: "Entropy-Gradient Grounding: Training-Free Evidence Retrieval in Vision-Language Models"
title_zh: 熵梯度定位：视觉语言模型中无需训练的证据检索
authors: "Marcel Gröpl, Jaewoo Jung, Seungryong Kim, Marc Pollefeys, Sunghwan Hong"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08456v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 视觉语言模型中针对组合查询的证据检索
tldr: 针对预训练视觉语言模型在处理微小细节和多区域线索时的局限性，本文提出了一种无需训练的证据检索方法。该方法通过将模型预测分布的熵反向传播至视觉标记，生成熵梯度相关性图以定位关键区域。结合多区域提取和迭代缩放策略，该方法在不依赖辅助检测器的情况下，显著提升了模型在复杂视觉任务中的表现和可解释性。
selection_source: fresh_fetch
motivation: 预训练视觉语言模型在处理依赖微小视觉细节或多区域组合线索的复杂查询时，往往难以准确识别关键证据。
method: 利用模型预测的熵作为监督信号，通过反向传播生成熵梯度图来定位相关区域，并引入迭代缩放和空间熵停止准则进行优化。
result: 在七个基准测试和四种模型架构上的实验表明，该方法在细节敏感和高分辨率设置下取得了持续的性能提升。
conclusion: 熵梯度定位提供了一种高效、无需训练且具有模型内在一致性的证据检索机制，增强了视觉语言模型的细粒度理解能力。
---

## 摘要
尽管取得了快速进展，但当答案依赖于微小的视觉细节或需要结合分布在多个区域的线索（如文档和组合查询）时，预训练视觉语言模型仍然面临挑战。我们通过将定位建模为测试时的证据检索来解决这一问题：给定一个查询，模型应主动识别下一步该看哪里以消除歧义。为此，我们提出了一种无需训练、模型内在的定位方法，该方法利用不确定性作为监督。具体而言，我们计算模型下一个 token 分布的熵，并将其反向传播到视觉 token 嵌入，从而获得熵梯度相关性图，而无需辅助检测器或注意力图启发式方法。随后，我们提取并排序多个连贯区域以支持多证据查询，并引入了一种带有空间熵停止规则的迭代缩放与重新定位程序，以避免过度细化。在四种 VLM 架构上的七个基准测试实验表明，该方法相较于现有方法取得了持续的改进，在注重细节和高分辨率设置下提升最为显著，同时还能产生更具解释性的证据定位。

## Abstract
Despite rapid progress, pretrained vision-language models still struggle when answers depend on tiny visual details or on combining clues spread across multiple regions, as in documents and compositional queries. We address this by framing grounding as test-time evidence retrieval: given a query, the model should actively identify where to look next to resolve ambiguity. To this end, we propose a training-free, model-intrinsic grounding method that uses uncertainty as supervision. Specifically, we compute the entropy of the model's next-token distribution and backpropagate it to the visual token embeddings to obtain an entropy-gradient relevance map, without auxiliary detectors or attention-map heuristics. We then extract and rank multiple coherent regions to support multi-evidence queries, and introduce an iterative zoom-and-reground procedure with a spatial-entropy stopping rule to avoid over-refinement. Experiments on seven benchmarks across four VLM architectures demonstrate consistent improvements over existing methods, with the largest gains on detail-critical and high-resolution settings, while also producing more interpretable evidence localizations.

---

## 论文详细总结（自动生成）

以下是对论文《Entropy-Gradient Grounding: Training-Free Evidence Retrieval in Vision-Language Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
预训练视觉语言模型（VLM）在处理需要**微小视觉细节**（如文档中的小字）或**分散在多个区域的线索**（如复杂的组合查询）时经常失败。作者认为，这些失败并非源于生成能力的不足，而是由于“选择性感知”的缺陷——模型未能识别出关键的视觉证据。现有的无需训练（Training-free）的定位方法通常依赖于启发式的注意力图（Attention Maps），这些图往往存在噪声、难以跨模型泛化，且通常只能关注单一显著区域。

### 2. 论文提出的方法论
论文提出了一种名为 **熵梯度定位（Entropy-Gradient Grounding）** 的方法，将定位视为测试时的证据检索过程：
*   **核心思想**：利用模型预测的不确定性作为监督信号。如果某个视觉区域对模型消除歧义至关重要，那么该区域的特征变化将显著影响模型输出分布的熵。
*   **关键技术细节**：
    1.  **熵梯度图生成**：计算模型在解码第一步时下一个 token 分布的 **Shannon 熵**。通过反向传播将该熵的梯度传导至视觉嵌入（Visual Embeddings），计算其 $\ell_2$ 范数生成显著性图。
    2.  **多区域选择与排序**：对梯度图进行高斯平滑，利用“拐点法”（Elbow Method）自动确定阈值并二值化，提取连贯的连通分量（ROI），并根据梯度强度进行排序。
    3.  **迭代细化（Iterative Refinement）**：引入“缩放-再定位”循环。模型在选定的 ROI 上重新运行定位程序，以发现更细微或被遮蔽的次要证据。
    4.  **空间熵停止准则**：为了防止过度缩放，引入“空间熵”来衡量掩码的集中程度。当空间熵不再下降（即区域不再变得更集中）时，停止迭代。

### 3. 实验设计
*   **数据集/场景**：涵盖了 7 个基准测试，分为两类：
    *   **细粒度理解**：TextVQA（场景文本）、V*（高分辨率视觉搜索）、DocVQA（文档理解）、InfoQA（信息图表）、POPE（幻觉检测）。
    *   **通用/现实世界推理**：GQA（组合推理）、RWQA（现实世界空间推理）。
*   **基准模型（Backbones）**：LLaVA-1.5 (7B)、LLaVA-1.6 (Mistral 7B)、InternVL-3.5 (8B)、Qwen2.5-VL (7B)。
*   **对比方法**：
    *   **无需训练方法**：Vanilla VLM（原始模型）、ViCrop（基于注意力的缩放方法）。
    *   **需训练方法（作为参考）**：TEVA、SEAL、ZoomEye。

### 4. 资源与算力
*   **训练算力**：该方法是 **无需训练（Training-free）** 的，因此不需要训练算力。
*   **推理算力**：文中提到在对比 ZoomEye 时，由于显存限制，在拥有 **80GB 显存的 A100 GPU** 上运行 Qwen 2.5 7B 版本的 ZoomEye 会出现 OOM（显存溢出），而本文方法由于仅涉及梯度回传，显存占用相对可控。
*   **推理耗时**：单次迭代在 LLaVA-1.6 上约需 2.04 秒，带停止准则的迭代过程约需 3.37 秒。

### 5. 实验数量与充分性
*   **实验规模**：作者在 4 种主流 VLM 架构和 7 个不同性质的数据集上进行了广泛测试，实验设置非常全面。
*   **消融实验**：非常充分，涵盖了停止准则对比（空间熵 vs 置信度）、损失函数选择（熵 vs 最大概率）、区域数量 $K$ 的影响、梯度计算的层级分析、以及不同 token 位置的梯度效果。
*   **客观性**：通过与需训练的最优方法（如 TEVA）对比，展示了该方法在无需额外参数更新的情况下仍能达到甚至超越部分需训练模型的性能，评估过程较为公正。

### 6. 主要结论与发现
*   **性能提升**：在所有 VLM 架构上均实现了性能提升，尤其在 V*（提升高达 15.7%）和 DocVQA 等细节敏感任务中表现卓越。
*   **多区域优势**：相比于 ViCrop 等单区域缩放方法，本文的多区域提取策略在处理文档等复杂布局时更具鲁棒性。
*   **模型内在一致性**：实验发现，越强大的模型（如 Qwen2.5-VL）生成的熵梯度图越精确，说明该方法能有效提取模型内部的知识。
*   **通用性**：该方法在提升细粒度能力的同时，并未损害模型在通用 QA 任务上的表现。

### 7. 优点
*   **无需训练**：直接利用预训练模型的内在能力，无需昂贵的微调或辅助检测器。
*   **模型无关性**：适用于各种基于 Transformer 的 VLM 架构。
*   **可解释性强**：熵梯度图直接反映了模型决策的视觉依据，比注意力图更清晰、更具指向性。
*   **自适应性**：通过空间熵停止准则，实现了自动化的推理深度控制。

### 8. 不足与局限
*   **推理开销**：由于涉及反向传播和多次前向推理，其推理延迟高于原始模型（约增加 2-3 倍耗时）。
*   **依赖基座模型**：如果基座模型本身完全忽略了某个区域（即该区域对输出分布的熵没有任何贡献），梯度信号将无法找回该区域。
*   **推理与感知的解耦**：即使定位准确，如果下游语言模型推理能力不足，仍可能给出错误答案（即“看对了但想错了”）。

（完）
