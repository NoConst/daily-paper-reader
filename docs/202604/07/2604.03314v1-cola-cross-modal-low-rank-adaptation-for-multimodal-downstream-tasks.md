---
title: "CoLA: Cross-Modal Low-rank Adaptation for Multimodal Downstream Tasks"
title_zh: CoLA：面向多模态下游任务的跨模态低秩自适应
authors: "Wish Suharitdamrong, Tony Alex, Muhammad Awais, Sara Ahmed"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.03314v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 使用跨模态LoRA适配多模态基础模型
tldr: 针对双流架构基础模型在多模态任务中难以捕捉跨模态交互的问题，本文提出了CoLA（跨模态低秩自适应）框架。CoLA在标准LoRA的模态内自适应路径基础上，引入了专门的模态间自适应路径，实现了模态特定学习与跨模态交互的解耦。在视觉-语言和音视频基准测试中，CoLA在保持参数高效的同时性能显著优于LoRA，并首次实现了视觉定位的多任务PEFT框架。
selection_source: fresh_fetch
motivation: 现有的PEFT方法如LoRA在多模态任务中各模态独立运行，限制了其捕捉模态间复杂交互的能力。
method: 引入一种双路径设计，在传统的模态内自适应路径旁增加专用的模态间自适应路径，以增强跨模态协作。
result: "在RefCOCO和AVE等多个视觉-语言及音视频基准测试中，CoLA相比LoRA取得了2%-3%的性能提升。"
conclusion: CoLA通过创新的跨模态低秩设计，有效解决了双流模型在多模态下游任务中的高效适配难题。
---

## 摘要
基础模型彻底改变了人工智能领域，但如何高效地将它们适配到多模态任务中，特别是在由 DINO 和 BERT 等单模态编码器组成的双流架构中，仍然是一个重大挑战。诸如低秩自适应（LoRA）之类的参数高效微调（PEFT）方法实现了轻量化适配，但它们在每个模态内孤立运行，限制了其捕获跨模态交互的能力。在本文中，我们通过跨模态低秩自适应（CoLA）迈出了弥补这一差距的一步。CoLA 是一种新型 PEFT 框架，它通过在标准模态内适配路径的基础上引入专门的模态间适配路径来扩展 LoRA。这种双路径设计使 CoLA 能够有效地将单模态基础模型适配到多模态任务中，且模态特定学习与跨模态学习之间互不干扰。我们在多项视觉-语言（RefCOCO、RefCOCO+、RefCOCOg）和音频-视觉（AVE、AVS）基准测试中对 CoLA 进行了评估。结果显示，CoLA 在保持参数效率的同时，始终优于 LoRA，分别实现了约 3% 和 2% 的相对增益。值得注意的是，CoLA 实现了首个用于视觉定位的多任务 PEFT 框架，弥补了高效多模态适配中的一个关键空白。

## Abstract
Foundation models have revolutionized AI, but adapting them efficiently for multimodal tasks, particularly in dual-stream architectures composed of unimodal encoders, such as DINO and BERT, remains a significant challenge. Parameter-Efficient Fine-Tuning (PEFT) methods like Low-Rank Adaptation (LoRA) enable lightweight adaptation, yet they operate in isolation within each modality, limiting their ability in capturing cross-modal interactions. In this paper, we take a step in bridging this gap with Cross-Modal Low-Rank Adaptation (CoLA), a novel PEFT framework that extends LoRA by introducing a dedicated inter-modal adaptation pathway alongside the standard intra-modal one. This dual-path design enables CoLA to adapt unimodal foundation models to multimodal tasks effectively, without interference between modality-specific and cross-modal learning. We evaluate CoLA across a range of vision-language (RefCOCO, RefCOCO+, RefCOCOg) and audio-visual (AVE, AVS) benchmarks, where it consistently outperforms LORA, achieving a relative gain of around 3\% and 2\%, respectively, while maintaining parameter efficiency. Notably, CoLA enables the first multi-task PEFT framework for visual grounding, bridging a key gap in efficient multimodal adaptation.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **CoLA (Cross-Modal Low-rank Adaptation)**，一种旨在提升双流架构基础模型在多模态下游任务中适配效率和性能的新型参数高效微调（PEFT）框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：虽然 LoRA 等 PEFT 方法在单模态模型适配中表现出色，但在处理多模态任务（如视觉-语言、音视频）时，若直接应用于双流架构（即两个独立的单模态编码器），各模态的适配过程是孤立的。
*   **研究动机**：现有的 LoRA 缺乏“跨模态意识”，无法捕捉模态间的互补信息。为了在保持参数高效的同时增强模态间交互，需要一种能够打破模态壁垒、实现动态信息交换的适配机制。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：在标准 LoRA 的“模态内自适应路径”之外，引入一条专门的“模态间融合路径”。
*   **关键技术细节**：
    *   **双路径设计**：对于预训练权重 $W_0$，CoLA 的输出公式为 $h = W_0x + \Delta W_L x + \Delta W_C x$。其中 $\Delta W_L$ 是标准的模态内 LoRA，而 $\Delta W_C$ 是新增的跨模态路径。
    *   **动态权重生成（Hypernetwork）**：跨模态路径 $\Delta W_C$ 由公式 $\lambda B_C \Phi A_C$ 组成。其中 $\Phi$ 是一个由**超网络（Hypernetwork）**根据另一模态的特征动态生成的 $r \times r$ 矩阵。这使得适配权重能够随输入内容动态调整。
    *   **渐进式特征传播（Progressive Propagation）**：跨模态特征在双编码器之间不是一次性融合，而是随着 Transformer 的层级（自注意力、输出投影、FFN）逐阶段更新和交换，确保每一层都能获得最相关的跨模态上下文。
    *   **可学习缩放因子 $\lambda$**：用于自动调节跨模态交互在不同层和不同组件中的强度。

### 3. 实验设计
*   **数据集与场景**：
    *   **视觉-语言任务**：指代性目标定位（REC）和分割（RES），使用 RefCOCO、RefCOCO+、RefCOCOg 数据集。
    *   **音频-视觉任务**：音视频事件定位（AVE）和音视频分割（AVS），使用 AVE 和 AVSBench-S4 数据集。
*   **基准模型（Backbones）**：ViT-B、DINOv2、BERT-B、SwinV2-L 以及音频模型 SSLAM。
*   **对比方法**：
    *   **直接对比**：秩匹配（r=16）和参数量匹配（r=54/48）的 LoRA。
    *   **SOTA 对比**：包括全量微调方法（如 EEVG, TransVG++）和专门的 PEFT 方法（如 HiVG, DETRIS, LAVisH, STG-CMA）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练细节**：提供了超参数设置，如视觉-语言任务训练 150 个 Epoch，Batch Size 为 80；音视频任务训练 15-50 个 Epoch。
*   **效率分析**：论文在附录中对比了 CoLA 与 LoRA 的 GFLOPs、显存占用和推理速度。CoLA 相比 LoRA 增加了约 30% 的显存占用，推理速度略有下降（从 9.13 samples/s 降至 7.99 samples/s）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 大类多模态场景、5 个主流数据集、多种骨干网络架构。
*   **消融实验**：针对低秩矩阵共享策略、特征传播策略（均匀 vs 模块化 vs 渐进式）以及跨模态影响力的层级分布进行了深入分析。
*   **充分性评价**：实验设计非常充分且客观。特别是通过“参数量匹配”的 LoRA 对比，有力地证明了性能提升源于**架构设计的创新**（跨模态交互），而非单纯因为增加了参数量。

### 6. 主要结论与发现
*   **性能提升**：CoLA 在视觉-语言任务上平均提升约 3%，在音视频任务上提升约 2%，且在多个指标上超越了全量微调的 SOTA 模型。
*   **路径分离的重要性**：实验证明，将模态内适配与模态间融合的低秩矩阵解耦（不共享 A/B 矩阵）效果最好。
*   **动态交互规律**：通过可视化 $\lambda$ 发现，视觉定位任务在浅层更依赖跨模态信息（识别物体），而音视频任务在深层更依赖跨模态信息（语义融合）。

### 7. 优点
*   **通用性强**：设计对称且灵活，可应用于任何模态组合（视觉、语言、音频）和任何线性层组件。
*   **首创性**：实现了首个用于视觉定位多任务学习（同时做定位和分割）的 PEFT 框架。
*   **动态性**：引入超网络生成动态权重，比静态的 Adapter 或 LoRA 更能捕捉复杂的跨模态关联。

### 8. 不足与局限
*   **推理开销**：与标准 LoRA 不同，CoLA 的跨模态路径 $\Delta W_C$ 依赖于实时输入特征，因此**无法在推理前合并到预训练权重中**，这会带来额外的计算延迟和显存开销。
*   **超参数敏感性**：引入了额外的超参数（如 $\lambda$ 的初始值、超网络的缩减因子 $\gamma$），可能需要针对不同任务进行微调。
*   **模型规模限制**：虽然在 ViT 和 Swin 上表现良好，但尚未在大规模多模态大模型（如 LLaVA 等）上验证其扩展性。

（完）
