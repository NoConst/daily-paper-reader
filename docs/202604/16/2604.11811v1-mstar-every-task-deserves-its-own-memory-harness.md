---
title: "M$^\\star$: Every Task Deserves Its Own Memory Harness"
title_zh: M$^\star$：每个任务都值得拥有专属的内存架构
authors: "Wenbo Pan, Shujie Liu, Xiangyang Zhou, Shiwei Zhang, Wanlu Shi, Mirror Xu, Xiaohua Jia"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.11811v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 大语言模型智能体的任务优化记忆机制
tldr: "针对大语言模型智能体在不同任务中对记忆系统需求各异的问题，本文提出 M* 框架。该框架通过可执行程序演化技术，自动为特定任务发现最优的记忆管理程序。M* 将记忆系统建模为包含数据模式、存储逻辑和工作流指令的 Python 程序，并利用反射式代码演化进行迭代优化。实验证明，M* 在对话、规划和推理等多个领域均优于固定记忆基准，揭示了任务定制化记忆机制的必要性。"
selection_source: fresh_fetch
motivation: 现有的智能体记忆系统多为针对特定领域设计的固定架构，难以在不同任务间有效迁移或通用。
method: 将记忆系统建模为包含数据模式、存储逻辑和指令的 Python 程序，并采用基于种群搜索和失败分析的反射式代码演化方法进行自动优化。
result: "在对话、具身规划和专家推理等四个基准测试中，M* 的性能均显著优于现有的固定记忆基准。"
conclusion: 针对特定任务定制化的记忆机制比通用记忆范式更具优势，且不同任务演化出的记忆程序在结构上具有显著差异。
---

## 摘要
大语言模型智能体依赖专门的内存系统，在长程交互中积累并复用知识。近期的架构通常采用针对特定领域定制的固定内存设计，例如用于对话的语义检索或用于编程的技能复用。然而，为某一目的优化的内存系统往往难以迁移到其他任务中。为解决这一局限性，我们提出了 M$^\star$，这是一种通过可执行程序演化自动发现任务优化内存架构的方法。具体而言，M$^\star$ 将智能体内存系统建模为用 Python 编写的内存程序。该程序封装了数据模式（Schema）、存储逻辑（Logic）以及智能体工作流指令（Instructions）。我们使用反思性代码演化方法联合优化这些组件；该方法采用基于种群的搜索策略，并通过分析评估失败案例来迭代改进候选程序。我们在涵盖对话、具身规划和专家推理的四个不同基准测试上对 M$^\star$ 进行了评估。结果表明，在所有评估任务中，M$^\star$ 的性能均稳健地优于现有的固定内存基准。此外，演化出的内存程序针对每个领域表现出结构迥异的处理机制。这一发现表明，针对特定任务定制内存机制可以探索广阔的设计空间，并提供优于通用内存范式的解决方案。

## Abstract
Large language model agents rely on specialized memory systems to accumulate and reuse knowledge during extended interactions. Recent architectures typically adopt a fixed memory design tailored to specific domains, such as semantic retrieval for conversations or skills reused for coding. However, a memory system optimized for one purpose frequently fails to transfer to others. To address this limitation, we introduce M$^\star$, a method that automatically discovers task-optimized memory harnesses through executable program evolution. Specifically, M$^\star$ models an agent memory system as a memory program written in Python. This program encapsulates the data Schema, the storage Logic, and the agent workflow Instructions. We optimize these components jointly using a reflective code evolution method; this approach employs a population-based search strategy and analyzes evaluation failures to iteratively refine the candidate programs. We evaluate M$^\star$ on four distinct benchmarks spanning conversation, embodied planning, and expert reasoning. Our results demonstrate that M$^\star$ improves performance over existing fixed-memory baselines robustly across all evaluated tasks. Furthermore, the evolved memory programs exhibit structurally distinct processing mechanisms for each domain. This finding indicates that specializing the memory mechanism for a given task explores a broad design space and provides a superior solution compared to general-purpose memory paradigms.

---

## 论文详细总结（自动生成）

这是一份关于论文《M$^\star$: Every Task Deserves Its Own Memory Harness》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）智能体在长程交互中需要记忆系统来积累知识。然而，现有的记忆架构（如向量检索、技能库或关系数据库）通常是**固定且针对特定领域设计**的。例如，为对话优化的语义检索系统在处理需要结构化逻辑的法律查询或具身规划任务时往往表现不佳。
论文的核心问题是：**如何为给定的特定任务自动发现最合适的记忆架构？** 论文提出了 **M$^\star$**，旨在通过自动化的可执行程序演化，为每个任务定制专属的“记忆架构（Memory Harness）”。

### 2. 方法论：核心思想与技术细节
M$^\star$ 将记忆系统视为一个用 Python 编写的**内存程序（Memory Program）**，并对其进行联合优化。

*   **核心思想**：将记忆系统的设计空间抽象为三个维度：
    *   **Schema（模式）**：定义存储和检索的数据格式（如 Python dataclasses）。
    *   **Logic（逻辑）**：定义后端操作，如 SQL 表管理、向量嵌入计算或 LLM 二次处理。
    *   **Instructions（指令）**：提供工作流指南，告知智能体何时以及如何读写记忆。
*   **关键技术：反射式代码演化（Reflective Code Evolution）**：
    *   **种群搜索**：维护一个程序池，根据验证集得分通过 Softmax 温度采样选择优秀程序进行变异。
    *   **LLM 反思器（Reflector）**：利用 LLM 分析评估中的失败案例，诊断错误根源并生成代码补丁（Patch）。
    *   **质量检查与自动修复**：通过静态检查（白名单、编译）、冒烟测试（读写循环）和执行限制（耗时、字符数）确保程序质量，失败时触发 LLM 自动修复。
    *   **代表性子集选择**：使用 K-means 聚类和设施选址（Facility Location）算法选择最具代表性的验证样本和经验片段，以降低评估成本。

### 3. 实验设计
*   **数据集/场景**：
    *   **LoCoMo**：多轮对话问答，考察长程事实回收。
    *   **ALFWorld**：具身智能任务，在模拟家庭环境中完成目标。
    *   **HealthBench**：医疗问答，涉及健康数据解释和紧急转诊识别。
    *   **PRBench**：专业推理，涵盖法律分析和财务估值。
*   **对比方法（Baselines）**：
    *   **检索类**：Vector Search, G-Memory, Mem0。
    *   **自演化类**：Trajectory Retrieval, ReasoningBank, Dynamic Cheatsheet。
    *   **提示词优化类**：GEPA, GEPA + Vector Search。
*   **评估指标**：Token-level F1（对话）、成功率（具身）、专家准则评分（医疗/法律/金融）。

### 4. 资源与算力
*   **模型使用**：任务智能体使用 **GPT-5.4 Mini**，代码演化/反思器使用 **GPT-5.3-Codex**。
*   **算力/成本**：
    *   论文未明确提及具体的 GPU 硬件型号，实验主要基于 API 调用。
    *   **演化成本**：每个基准运行 20 次迭代。总成本在 $12 到 $90 之间（取决于是否需要 LLM Judge 评分）。
    *   **时长**：LoCoMo 约 5 小时，PRBench 约 15 小时，ALFWorld 因环境交互开销需约 100 小时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖 4 个大类基准、6 个细分领域配置。
*   **充分性**：
    *   **消融实验**：验证了指令优化、代码演化和种群多样性对性能的贡献。
    *   **稳定性分析**：在 3 个基准上进行了 5 次独立随机种子运行，变异系数（CV）低于 9%，证明结果稳健。
    *   **跨任务迁移实验**：将 A 任务演化的程序运行在 B 任务上，结果显示性能显著下降，有力证明了“任务特异性优化”的必要性。
*   **客观性**：使用了留出测试集（Held-out test set），且演化过程中的验证集与测试集不重叠。

### 6. 主要结论与发现
*   **性能领先**：M$^\star$ 在 8 个配置中的 7 个达到了最优性能，相对最强基准提升最高达 31%。
*   **结构异质性**：不同任务演化出的记忆程序在结构上迥然不同。例如，ALFWorld 倾向于使用确定性的动作缓存（Action Cache），而 LoCoMo 则演化出了结合 SQL 和向量检索的混合索引。
*   **演化动力学**：演化通常经历“修复结构错误 -> 发现关键索引策略 -> 微调精度”三个阶段。
*   **任务特异性**：不存在通用的最优记忆架构，针对特定任务定制的程序优于任何通用基准。

### 7. 优点
*   **设计空间广阔**：突破了以往仅优化提示词或选择预定义模块的限制，允许生成任意 Python 逻辑。
*   **闭环优化**：结合了代码执行反馈、LLM 反思和自动化修复，形成了一个高效的自进化循环。
*   **实用性强**：提供的工具包（Toolkit）支持 SQL、向量库和 LLM 调用，能够生成工业级可用的记忆组件。

### 8. 不足与局限
*   **API 依赖**：高度依赖高性能 LLM（如 GPT-5 系列）进行代码生成和反思，成本和可访问性受限。
*   **演化开销**：虽然推理成本与基准持平，但 20 轮演化过程涉及大量 API 调用，对于预算有限的场景可能较贵。
*   **长尾权衡**：演化目标是优化整体得分，可能会为了提升高频类别的表现而牺牲某些低频边缘案例的性能。
*   **环境依赖**：在具身智能任务（如 ALFWorld）中，环境交互的延迟是主要的效率瓶颈。

（完）
