---
title: "CycleCap: Improving VLMs Captioning Performance via Self-Supervised Cycle Consistency Fine-Tuning"
title_zh: CycleCap：通过自监督循环一致性微调提升 VLM 的图像描述性能
authors: "Marios Krestenitis, Christos Tzelepis, Konstantinos Ioannidis, Steafanos Vrochidis, Ioannis Kompatsiaris, Georgios Tzimiropoulos, Shaogang Gong, Ioannis Patras"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18282v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 用于VLM图文对齐的自监督循环一致性微调
tldr: CycleCap 提出了一种基于自监督循环一致性的视觉语言模型（VLM）微调方法。该方法利用 VLM 生成描述，再通过预训练的文本生成图像模型重建图像，并以原始图像与重建图像的相似度作为奖励信号，通过 GRPO 算法进行优化。该方法无需标注数据，仅需原始图像即可显著提升模型的描述准确性并减少幻觉，在多个基准测试中表现优于现有方法。
selection_source: fresh_fetch
motivation: 现有的 VLM 微调方法高度依赖昂贵的人工标注数据，且模型容易产生通用化或幻觉性的描述。
method: 引入 CycleCap 微调方案，利用 GRPO 算法和基于图像重建相似度的实时奖励信号，实现自监督的循环一致性训练。
result: 在 1B 到 7B 参数规模的四个 VLM 上，CycleCap 在图像描述和幻觉检测基准测试中均取得了持续的性能提升。
conclusion: CycleCap 证明了利用循环一致性作为直接的自监督训练信号，可以在无需标注数据集的情况下有效增强 VLM 的图文对齐能力。
---

## 摘要
视觉语言模型（VLMs）在图像描述、视觉问答和视觉推理方面取得了显著进展。然而，它们仍然容易出现视觉-语言失配，经常产生过于笼统或幻觉性的描述。现有方法通过指令微调（需要昂贵的大规模标注数据集）或复杂的测试时框架进行描述细化来解决这一问题。在这项工作中，我们从循环一致性的角度重新审视图像-文本对齐：给定一张图像和由图像到文本模型生成的描述，通过文本到图像模型的反向映射应该重建出一张与原始图像高度匹配的图像。在我们的设置中，VLM 作为图像到文本组件，而预训练的文本到图像模型通过从生成的描述中重建图像来闭合循环。基于此，我们引入了 CycleCap，这是一种微调方案，利用群体相对策略优化（GRPO）来改进图像描述，其奖励基于原始图像与重建图像之间的相似度，并进行实时计算。与以往使用循环一致性损失构建偏好数据集的工作不同，我们的方法直接将循环一致性作为自监督训练信号。这使得仅使用原始图像成为可能，消除了对精选图像-文本数据集的需求，同时引导 VLM 生成更准确且有依据的文本描述。CycleCap 应用于参数量从 1B 到 7B 的四种 VLM，在图像描述和幻觉基准测试中均取得了持续的改进，超越了依赖监督循环一致性训练的最先进方法。

## Abstract
Visual-Language Models (VLMs) have achieved remarkable progress in image captioning, visual question answering, and visual reasoning. Yet they remain prone to vision-language misalignment, often producing overly generic or hallucinated descriptions. Existing approaches address this via instruction tuning-requiring costly, large-scale annotated datasets or via complex test-time frameworks for caption refinement. In this work, we revisit image-text alignment through the lens of cycle consistency: given an image and a caption generated by an image-to-text model, the backward mapping through a text-to-image model should reconstruct an image that closely matches the original. In our setup, a VLM serves as the image-to-text component, while a pre-trained text-to-image model closes the loop by reconstructing the image from the generated caption. Building on this, we introduce CycleCap, a fine-tuning scheme to improve image captioning using Group Relative Policy Optimization (GRPO) with a reward based on the similarity between the original and reconstructed images, computed on-the-fly. Unlike previous work that uses cycle consistency loss for preference dataset construction, our method leverages cycle consistency directly as a self-supervised training signal. This enables the use of raw images alone, eliminating the need for curated image-text datasets, while steering the VLM to produce more accurate and grounded text descriptions. Applied to four VLMs ranging from 1B to 7B parameters, CycleCap yields consistent improvements across captioning and hallucination benchmarks, surpassing state-of-the-art methods that rely on supervised cycle consistency training.

---

## 论文详细总结（自动生成）

### 论文总结：CycleCap —— 通过自监督循环一致性微调提升 VLM 的图像描述性能

#### 1. 核心问题与整体含义（研究动机和背景）
视觉语言模型（VLMs）在图像描述（Captioning）任务中虽有长足进步，但仍面临**视觉-语言失配**的问题，表现为生成的描述过于笼统或存在严重的“幻觉”（描述不存在的物体）。
*   **研究动机**：现有的改进方法（如指令微调）高度依赖大规模、高质量的人工标注数据集，成本极高。
*   **核心思想**：借鉴 CycleGAN 的循环一致性（Cycle Consistency）理念，认为一个好的图像描述应该能够让“文本转图像”模型重新构建出与原图高度相似的图像。通过这种闭环反馈，可以实现无需人工标注的自监督学习。

#### 2. 方法论：核心思想与技术细节
CycleCap 提出了一种基于强化学习的微调框架，其核心流程如下：
*   **循环路径**：
    1.  **前向（I2T）**：VLM 模型（如 Qwen2-VL）针对输入图像 $x$ 生成一组（$n$个）候选描述 $\{y_i\}$。
    2.  **后向（T2I）**：使用预训练且冻结的扩散模型（如 Stable Diffusion 3 或 FLUX.1）根据描述 $y_i$ 重建图像 $x'_i$。
*   **奖励函数（Reward）**：计算原始图像 $x$ 与重建图像 $x'_i$ 之间的相似度。论文主要采用 **DreamSim** 指标，因为它能更好地捕捉人类感知的语义和结构一致性。
*   **优化算法（GRPO）**：采用群体相对策略优化（Group Relative Policy Optimization）。
    *   计算同一组内 $n$ 个描述的奖励均值和标准差。
    *   通过优势函数（Advantage）引导模型增加高奖励描述的概率，减少低奖励描述的概率。
    *   引入 KL 散度约束，防止模型偏离原始预训练分布。

#### 3. 实验设计
*   **训练数据集**：使用 COCO 2014 训练集的约 8.3 万张原始图像，**不使用其对应的标签文本**。
*   **评估基准（Benchmarks）**：
    *   **CompreCap**：评估描述的完整性（物体、属性、关系）。
    *   **CAPability**：多维度评估描述的准确性与详尽程度。
    *   **CapsBench**：通过 LLM 回答基于描述的视觉问题来测试视觉接地能力。
    *   **MMHal**：专门测试模型的幻觉程度。
*   **对比方法**：
    *   各模型的 Baseline（原始版本）。
    *   **RICO-Flash**：基于 GPT-4o 反馈构建偏好数据集进行微调的方法。
    *   **CyclePref**：利用 11 个模型集成并使用 DPO 算法的方法。

#### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA A100 GPU**。
*   **训练时长**：根据模型规模不同，训练总时长在 **270（1B 模型）到 430（7B 模型）GPU 小时**之间。
*   **配置**：采用 LoRA 适配器（Rank=64）进行高效微调，GRPO 采样数 $n=8$。

#### 5. 实验数量与充分性
*   **实验覆盖面**：论文在 4 种不同规模的 VLM（InternVL3-1B, Qwen2-VL-2B, Qwen2.5-VL-3B, Qwen2-VL-7B）上进行了验证，证明了方法的普适性。
*   **消融实验**：
    *   对比了不同的相似度度量（LPIPS, CLIP, DreamSim），发现 DreamSim 效果最好。
    *   对比了不同的 T2I 后端（SD3, FLUX.1），证明框架对生成模型具有鲁棒性。
    *   分析了 GRPO 采样数量 $n$ 对性能与成本的影响。
*   **充分性评价**：实验设计较为全面，不仅对比了 Baseline，还与当前最先进的（SOTA）监督/半监督循环一致性方法进行了公平对比，实验结果具有统计说服力。

#### 6. 主要结论与发现
*   **性能提升**：CycleCap 在所有测试的 VLM 上均实现了显著的性能增长，尤其在物体覆盖率和属性描述准确性上提升明显。
*   **超越监督学习**：尽管 CycleCap 是完全自监督的（不使用人工标注），但其表现超越了依赖昂贵偏好数据集（如 RICO 和 CyclePref）的方法。
*   **减少幻觉**：在 MMHal 基准上的得分提升表明，循环一致性约束能有效抑制模型生成与图像无关的虚假信息。
*   **可扩展性**：该方法可以叠加在已经经过其他方法优化的模型（如 RICO-Flash）之上，进一步提升其性能。

#### 7. 优点（亮点）
*   **完全自监督**：摆脱了对高质量图文对数据集的依赖，仅需原始图像即可训练，极大地降低了数据获取成本。
*   **实时反馈**：与以往先构建数据集再训练的两阶段方法不同，CycleCap 在训练过程中实时生成图像并计算奖励，优化更直接。
*   **即插即用**：该框架不改变 VLM 架构，适用于各种基于 Transformer 的多模态模型。

#### 8. 不足与局限
*   **计算开销**：训练期间需要运行 T2I 扩散模型进行图像重建，这增加了训练时的计算负担和显存占用。
*   **依赖 T2I 模型质量**：如果 T2I 模型本身无法准确理解某些复杂指令，可能会提供错误的奖励信号。
*   **图像空间局限性**：目前的奖励完全基于图像像素/特征空间的相似度，尚未引入跨模态（如文本语义空间）的辅助度量。
*   **随机性风险**：扩散模型的生成具有随机性，虽然论文通过固定随机种子缓解，但这可能限制了奖励信号的多样性。

（完）
