---
title: "CLAY: Conditional Visual Similarity Modulation in Vision-Language Embedding Space"
title_zh: CLAY：视觉语言嵌入空间中的条件视觉相似度调制
authors: "Sohwi Lim, Lee Hyoseok, Jungjoon Park, Tae-Hyun Oh"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11539v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 用于多条件图像检索的文本条件相似性空间
tldr: 针对人类视觉相似性感知的自适应性和主观性，本文提出了CLAY方法。该方法无需额外训练，通过将预训练视觉语言模型（VLM）的嵌入空间重构为文本条件相似性空间，实现了高效的多条件图像检索。CLAY通过分离文本调节与视觉特征提取过程，在保持高检索精度的同时显著提升了计算效率，并配套推出了评估数据集CLAY-EVAL。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11539v1/fig-001.webp\", \"caption\": \"Figure 2. Illustration of the concept of CLAY. Our method adaptively computes conditional similarity between images by modulating the original similarity space into a conditional similarity space within the representation space of VLMs.\", \"page\": 2, \"index\": 1, \"width\": 492, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2604.11539v1/fig-002.webp\", \"caption\": \"Figure 5. Qualitative comparison of our method with competing methods. For each query image and condition text pair, we compare the top-5 retrieved results from (a) CLIP-B, (b) InstructBLIP, (c) GeneCIS, and (d) our method. We also report Average Precision (AP) in each retrieval result. Green boxes indicate correctly retrieved images, while incorrect retrievals are shown in red boxes.\", \"page\": 7, \"index\": 2, \"width\": 983, \"height\": 459}, {\"url\": \"assets/figures/arxiv/2604.11539v1/fig-003.webp\", \"caption\": \"Figure 7. Representation space visualization with t-SNE. We report t-SNE of CLIP-B and ours (CLIP-B) on CLAY-Human under condition (a) action, (b) background, and (c) age. The features with the same label are shown in the same color for easy interpretation. Compared to the fixed representation space in CLIP-B, our method forms more discriminative spaces compliant with given conditions.\", \"page\": 8, \"index\": 3, \"width\": 1025, \"height\": 205}, {\"url\": \"assets/figures/arxiv/2604.11539v1/fig-004.webp\", \"caption\": \"Figure 3. Conditional similarity computation pipeline. (a) Given a condition, we construct the manifold-aware textual subspace with the condition text features in advance, and generate the condition-aware projection matrix Pc. (b) At inference, we compute the conditional similarity between the query and database images by projecting the visual features onto the textual subspace with Pc.\", \"page\": 4, \"index\": 4, \"width\": 1028, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2604.11539v1/fig-005.webp\", \"caption\": \"Figure 4. Our CLAY-EVAL dataset statistics. We construct a synthetic dataset with diverse condition annotations, consisting of (a) object entity and (b) human entity. For both, the left column shows sample images demonstrating visual naturalness, and the right column visualizes the distributions of key attributes, showing diversity. Percentages are truncated to one decimal place and annotation text labels are abbreviated.\", \"page\": 5, \"index\": 5, \"width\": 496, \"height\": 445}]"
motivation: 传统的图像检索系统通常采用固定的单一度量标准，无法根据用户的特定兴趣或多重条件灵活调整视觉相似性判断。
method: CLAY在无需重新训练的情况下，将预训练VLMs的嵌入空间转化为文本条件相似性空间，通过分离文本调节和视觉特征提取实现高效检索。
result: 在标准数据集及新构建的CLAY-EVAL数据集上的实验表明，CLAY在检索准确率和计算效率上均优于现有方法。
conclusion: CLAY为实现灵活、多条件的视觉相似性检索提供了一种高效且无需训练的新范式。
---

## 摘要
人类对视觉相似性的感知本质上是自适应且主观的，取决于用户的兴趣和关注点。然而，大多数图像检索系统未能体现这种灵活性，它们依赖于固定的、单一的度量标准，无法同时整合多个条件。为了解决这一问题，我们提出了 CLAY，这是一种自适应相似度计算方法，它在无需额外训练的情况下，将预训练视觉语言模型（VLMs）的嵌入空间重新构建为文本条件相似度空间。这种设计将文本条件化过程与视觉特征提取分离，从而在固定视觉嵌入的情况下实现高效且多条件的检索。我们还构建了一个合成评估数据集 CLAY-EVAL，用于在各种条件检索设置下进行全面评估。在标准数据集和我们提出的数据集上的实验表明，与以往的工作相比，CLAY 实现了较高的检索准确率和显著的计算效率。

## Abstract
Human perception of visual similarity is inherently adaptive and subjective, depending on the users' interests and focus. However, most image retrieval systems fail to reflect this flexibility, relying on a fixed, monolithic metric that cannot incorporate multiple conditions simultaneously. To address this, we propose CLAY, an adaptive similarity computation method that reframes the embedding space of pretrained Vision-Language Models (VLMs) as a text-conditional similarity space without additional training. This design separates the textual conditioning process and visual feature extraction, allowing highly efficient and multi-conditioned retrieval with fixed visual embeddings. We also construct a synthetic evaluation dataset CLAY-EVAL, for comprehensive assessment under diverse conditioned retrieval settings. Experiments on standard datasets and our proposed dataset show that CLAY achieves high retrieval accuracy and notable computational efficiency compared to previous works.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **CLAY**，一种针对视觉语言模型（VLM）嵌入空间进行自适应相似度调制的创新方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：人类对图像相似性的判断是主观且随场景变化的（例如，有时关注颜色，有时关注物体的种类或动作）。
*   **核心问题**：现有的图像检索系统大多依赖固定的度量标准，缺乏灵活性。现有的条件检索方法要么需要昂贵的重新训练（训练成本高），要么在推理时需要针对每个新条件重新编码整个数据库（计算效率低），且难以处理多条件组合检索。
*   **整体含义**：CLAY 旨在不进行任何额外训练的情况下，将预训练 VLM 的静态空间转化为可随文本条件动态调整的相似度空间，实现高效、灵活的条件图像检索。

### 2. 方法论
*   **核心思想**：将文本条件建模为 VLM 嵌入空间中的一个“子空间”，通过将图像特征投影到该子空间来提取与条件相关的视觉属性。
*   **关键技术细节**：
    *   **解耦设计**：将视觉特征提取与文本条件处理分离。视觉特征保持固定，仅通过投影矩阵进行调制。
    *   **流形感知子空间构建**：考虑到 VLM 嵌入分布在单位超球面上（非欧几里得几何），CLAY 使用对数映射（Logarithm Map）将特征映射到切空间（Tangent Space），在此空间进行奇异值分解（SVD）以构建投影矩阵 $P_c$。
    *   **特征对齐（旋转）**：为了缓解“模态间隙”和“锥状效应”，在投影前通过正交旋转变换（Householder 变换）将视觉特征的均值与文本特征的均值对齐。
    *   **推理流程**：1. 使用 LLM 生成与条件相关的文本描述；2. 构建投影矩阵；3. 将查询图像和数据库图像特征投影到该子空间；4. 计算余弦相似度。

### 3. 实验设计
*   **数据集/场景**：
    *   **真实世界数据集**：Stanford40（动作）、OxfordPets（品种）、Flowers102、StanfordCars、FGVC-Aircraft、Food-101 等细粒度分类数据集。
    *   **合成数据集**：CLEVR4（形状、颜色、数量等）。
    *   **新数据集 CLAY-EVAL**：论文自建的合成数据集，包含“物体”和“人物”两个实体，带有丰富的属性标注（如年龄、背景、子类别等），专门用于评估多条件检索。
*   **对比方法（Benchmark）**：
    *   基础模型：CLIP、SigLIP。
    *   条件检索/组合检索方法：GeneCIS、SEARLE、MagicLens。
    *   多模态大模型（MLLM）：InstructBLIP、Qwen3-VL-Embedding、VLM2Vec。

### 4. 资源与算力
*   **训练资源**：论文强调 **Training-free（无需训练）**，因此核心方法本身不消耗训练算力。
*   **推理/生成资源**：
    *   数据集生成：使用了 **FLUX.1-dev** 扩散模型生成 CLAY-EVAL 数据集（通常需要 A100 或同级别 GPU）。
    *   文本生成：使用 ChatGPT-5 生成条件提示词。
    *   推理效率：论文给出了延迟测试，CLAY 在处理后续条件时仅需 0.092s，远快于需要重新编码的方法（约 1.6s-13s）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在超过 10 个单条件数据集上进行了定量对比。
    *   在 CLAY-EVAL 上进行了多条件组合检索实验。
    *   进行了消融实验（验证旋转操作和流形建模的有效性）。
    *   提供了 t-SNE 可视化分析，展示了特征空间在调制前后的变化。
*   **充分性评价**：实验设计非常充分。它不仅对比了传统的检索模型，还对比了最新的基于 MLLM 的嵌入模型，并从准确率和计算效率两个维度（Pareto 前沿）证明了优越性。

### 6. 主要结论与发现
*   **性能领先**：CLAY 在几乎所有测试数据集上的 mAP（平均精度）均显著优于现有方法。
*   **效率优势**：通过固定数据库特征并仅调制投影矩阵，CLAY 解决了对称相似度计算中的计算瓶颈。
*   **流形的重要性**：实验证明，考虑超球面几何结构（流形感知）比简单的欧几里得投影能更准确地捕捉条件相关性。
*   **多条件支持**：CLAY 能够自然地扩展到多个文本条件的交集检索，表现稳定。

### 7. 优点
*   **无需训练**：极大地降低了部署成本，具有很强的通用性。
*   **即插即用**：可以适配任何预训练的 VLM（如 CLIP, SigLIP）。
*   **数学严谨性**：引入了切空间投影和特征旋转，解决了 VLM 空间固有的几何偏差问题。
*   **高效性**：适合大规模数据库的实时检索。

### 8. 不足与局限
*   **属性依赖性**：CLAY 的效果受限于基础 VLM 预训练时学到的特征。如果基础模型未编码某些细微属性（如极小物体），CLAY 也无法通过投影找回这些信息。
*   **LLM 依赖**：子空间的构建依赖于 LLM 生成的高质量提示词，提示词的质量可能影响检索精度。
*   **复杂场景限制**：目前主要针对核心属性（颜色、动作等）进行调制，对于涉及多个复杂交互物体的场景，其表现尚待进一步验证。

（完）
