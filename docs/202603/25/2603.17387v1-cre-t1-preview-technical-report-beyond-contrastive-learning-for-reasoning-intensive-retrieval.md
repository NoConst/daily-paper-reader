---
title: "CRE-T1 Preview Technical Report: Beyond Contrastive Learning for Reasoning-Intensive Retrieval"
title_zh: CRE-T1 预览版技术报告：超越对比学习，实现推理性检索
authors: "Guangzhi Wang, Yinghao Jiao, Zhi Liu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17387v1"
tags: ["query:cir-ml"]
score: 8.0
evidence: 超越对比学习的推理密集型检索
tldr: 针对推理密集型检索中对比学习无法处理隐式逻辑关系的局限，本文提出生成式检索模型T1。该模型通过动态生成推理轨迹并利用特定标记进行语义聚合，将静态对齐转变为动态推理。采用三阶段训练及GRPO强化学习，T1-4B在BRIGHT基准测试中表现优异，证明了动态推理在提升复杂检索性能方面的有效性。
selection_source: fresh_fetch
motivation: 传统的对比学习范式属于静态表示整合，难以处理查询与文档之间需要隐式推理或存在词汇不匹配的复杂检索场景。
method: 提出T1模型，在查询端生成推理轨迹并使用特殊标记聚合语义，并通过包含GRPO强化学习的三阶段课程进行训练以优化推导策略。
result: T1-4B在BRIGHT基准测试中超越了更大规模的对比学习模型，并达到了与多阶段检索流水线相当的性能水平。
conclusion: 研究证明将静态表示对齐替换为动态推理生成，能显著提升模型在推理密集型检索任务中的表现。
---

## 摘要
推理性检索的核心挑战在于识别查询与文档之间的隐式推理关系，而非表层的语义或词汇相似性。对比学习范式本质上是一种静态表示巩固技术：在训练期间，它将层级相关性概念编码为向量空间中固定的几何结构；而在推理时，它无法根据每个查询的具体推理需求动态调整相关性判断。因此，当查询与文档之间存在词汇不匹配，或需要隐式推理来建立相关性时，性能会显著下降。本文提出了 Thought 1 (T1)，这是一种生成式检索模型，将相关性建模从静态对齐转变为动态推理。在查询端，T1 为每个查询动态生成中间推理轨迹，以桥接隐式推理关系，并使用 <embtoken> 作为推理输出的语义聚合点。在文档端，它采用“指令 + 文本 + <embtoken>”的编码格式，以支持高吞吐量索引。为了将动态推理能力内化到向量表示中，我们采用了三阶段训练课程，并在第三阶段引入了 GRPO，使模型能够通过试错强化学习为不同查询学习最优推导策略。在 BRIGHT 基准测试中，T1-4B 在原始查询设置下表现出强劲性能，整体优于采用对比学习训练的更大规模模型，并达到了与多阶段检索流水线相当的性能。结果表明，用动态推理生成取代静态表示对齐可以有效提升推理性检索的性能。

## Abstract
The central challenge of reasoning-intensive retrieval lies in identifying implicitreasoning relationships between queries and documents, rather than superficial se-mantic or lexical similarity. The contrastive learning paradigm is fundamentallya static representation consolidation technique: during training, it encodes hier-archical relevance concepts into fixed geometric structures in the vector space,and at inference time it cannot dynamically adjust relevance judgments accord-ing to the specific reasoning demands of each query. Consequently, performancedegrades noticeably when vocabulary mismatch exists between queries and doc-uments or when implicit reasoning is required to establish relevance. This pa-per proposes Thought 1 (T1), a generative retrieval model that shifts relevancemodeling from static alignment to dynamic reasoning. On the query side, T1 dy-namically generates intermediate reasoning trajectories for each query to bridgeimplicit reasoning relationships and uses <embtoken> as a semantic aggregationpoint for the reasoning output. On the document side, it employs an instruction+ text + <embtoken> encoding format to support high-throughput indexing. Tointernalize dynamic reasoning capabilities into vector representations, we adopt athree-stage training curriculum and introduce GRPO in the third stage, enablingthe model to learn optimal derivation strategies for different queries through trial-and-error reinforcement learning. On the BRIGHT benchmark, T1-4B exhibitsstrong performance under the original query setting, outperforming larger modelstrained with contrastive learning overall, and achieving performance comparableto multi-stage retrieval pipelines. The results demonstrate that replacing static rep-resentation alignment with dynamic reasoning generation can effectively improvereasoning-intensive retrieval performance.

---

## 论文详细总结（自动生成）

以下是对论文《CRE-T1 Preview Technical Report: Beyond Contrastive Learning for Reasoning-Intensive Retrieval》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：传统的对比学习（Contrastive Learning）范式在处理“推理密集型检索”时存在局限。对比学习本质上是**静态**的表示整合，将相关性编码为向量空间中固定的几何结构，无法在推理阶段根据查询（Query）的具体逻辑需求动态调整。
*   **核心问题**：当查询与文档之间存在词汇不匹配（Vocabulary Mismatch）或需要隐式推理（如因果、多跳逻辑）时，静态对齐模型性能会显著下降。
*   **研究目标**：提出一种从“静态对齐”转向“动态推理”的检索模型 **Thought 1 (T1)**，使模型在生成向量前先进行显式推理。

### 2. 论文提出的方法论
*   **核心思想**：将查询理解与文本表示融合。在查询端，模型先生成一段推理轨迹（Reasoning Trajectory），再将其压缩为检索向量；在文档端保持高效的单次编码。
*   **关键技术细节**：
    *   **非对称架构**：查询端采用“自回归生成推理 + 语义聚合”；文档端采用“指令 + 文本”的快速编码。
    *   **语义聚合点**：引入特殊标记 `<emb token>`。查询向量 $e_q$ 是推理序列结束后该 token 的隐藏状态；文档向量 $e_d$ 则是文档末尾该 token 的隐藏状态。
    *   **三阶段训练课程**：
        1.  **阶段1（任务感知）**：使用 MS MARCO 数据集进行冷启动，确立输出格式和基础检索能力。
        2.  **阶段2（推理对齐）**：引入 ReasonEmbed 数据，利用 GLM-4.5 生成高质量推理路径，进行思维链（CoT）注入。
        3.  **阶段3（推理优化）**：采用 **GRPO（群体相对策略优化）** 强化学习。
*   **算法流程/公式**：
    *   引入**可微排序奖励（Differentiable Ranking Reward）**：通过 Sigmoid 函数构造连续可微的排名函数 $Rank(p)$，计算归一化奖励 $R_{rank}$，使强化学习能够直接优化排序质量。

### 3. 实验设计
*   **数据集/场景**：主要使用 **BRIGHT** 基准测试，这是一个专门针对推理密集型检索设计的挑战性榜单，涵盖数学、科学、法律等领域。
*   **Benchmark 子任务**：包括 StackExchange（生物、地球、经济等）、编程（LeetCode、Pony）、定理证明（TheoQ、TheoT）等 12 个子任务。
*   **对比方法**：
    *   **检索模型**：ReasonIR-8B、RaDeR-7B、字节跳动的 Seed1.5-Embedding。
    *   **流水线组合**：检索 + GPT-4 查询重写、检索 + QwenRerank 重排序。
    *   **基准对比**：基于相同 4B 底座的传统对比学习模型。

### 4. 资源与算力
*   **硬件环境**：使用 **4 台 NVIDIA A800 GPU**。
*   **训练时长**：
    *   阶段 1：20 小时。
    *   阶段 2：8 小时。
    *   阶段 3（GRPO）：20 小时。
*   **模型底座**：Qwen3-Instruct-4B。

### 5. 实验数量与充分性
*   **实验规模**：在 BRIGHT 的 12 个子任务上进行了全面评估。
*   **消融实验**：详细对比了 T1-4B 在三个训练阶段（Stage 1/2/3）的性能演进，验证了 GRPO 的有效性。
*   **客观性**：对比了不同规模的模型（如 7B、8B）以及商业闭源模型，并探讨了单模型与“检索+重排”复杂流水线的差异。实验设计较为公平，涵盖了原始查询和经过重写的查询等多种场景。

### 6. 论文的主要结论与发现
*   **性能超越**：T1-4B 在原始查询设置下的平均 nDCG@10 达到 37.1，显著超过了 ReasonIR-8B (24.4) 和 RaDeR-7B (25.5)。
*   **等效流水线**：T1-4B 单模型的表现与“检索 + 强力重排序（Rerank）”的组合性能相当，这意味着通过训练内化推理能力可以省去推理时的重排序开销。
*   **范式转换有效性**：证明了将相关性建模从“预编码固定模式”转变为“按需生成推理路径”能更有效地处理隐式逻辑关联。

### 7. 优点
*   **创新范式**：打破了对比学习的静态限制，提出了“先推理再表示”的动态生成模式。
*   **训练策略**：成功将 GRPO 强化学习应用于检索任务，通过可微排序奖励解决了离散排序信号难以优化的难题。
*   **效率平衡**：通过非对称设计，在提升查询端推理能力的同时，保证了文档端索引的高吞吐量。

### 8. 不足与局限
*   **领域差异**：在符号敏感或格式要求极高的领域（如 LeetCode 编程、AoPS 数学竞赛），T1 的提升较小甚至出现退化，说明通用推理奖励可能无法完全覆盖特定领域的微观匹配需求。
*   **RL 噪声**：强化学习在处理分布外（OOD）领域时可能存在奖励估计噪声，导致过度优化或轻微的遗忘。
*   **算力开销**：虽然索引效率高，但查询端生成推理序列会增加一定的首字延迟（Latency），需要在性能与响应速度间权衡。

（完）
