---
title: "WRF4CIR: Weight-Regularized Fine-Tuning Network for Composed Image Retrieval"
title_zh: WRF4CIR：用于组合图像检索的权重正则化微调网络
authors: "Yizhuo Xu, Chaojian Yu, Yuanjie Shao, Tongliang Liu, Qinmu Peng, Xinge You"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05583v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 组合图像检索 (CIR) 与视觉语言预训练微调
tldr: 针对组合图像检索（CIR）中视觉语言预训练模型在有限三元组数据下容易严重过拟合的问题，本文提出了WRF4CIR。该方法通过在微调过程中对模型权重施加与梯度下降方向相反的对抗扰动进行正则化，增加了拟合难度，从而显著缩小了泛化差距，在多个基准数据集上取得了领先的检索性能。
selection_source: fresh_fetch
motivation: 现有的基于视觉语言预训练模型的CIR方法在有限的标注数据下普遍存在严重的过拟合问题。
method: 提出WRF4CIR网络，在微调时通过向模型权重添加对抗性扰动来实现权重正则化。
result: 实验证明该方法显著缩小了模型在不同数据集上的泛化差距，并超越了现有主流方法。
conclusion: 通过权重正则化增加训练难度是解决CIR任务中过拟合问题并提升模型泛化能力的有效途径。
---

## 摘要
组合图像检索（CIR）任务旨在根据参考图像和修改文本检索目标图像。目前的 CIR 方法主要依赖于对视觉-语言预训练模型进行微调。然而，我们发现这些方法通常面临严重的过拟合问题，给三元组数据有限的 CIR 带来了挑战。为了更好地理解这一问题，我们对基于 VLP 的 CIR 中的过拟合现象进行了系统研究，揭示了在不同模型和数据集上存在的一个显著且此前被忽视的泛化差距。受这些发现的启发，我们提出了 WRF4CIR，一种用于 CIR 的权重正则化微调网络。具体而言，在微调过程中，我们对模型权重应用对抗性扰动进行正则化，这些扰动是沿着梯度下降的反方向生成的。直观地说，WRF4CIR 增加了拟合训练数据的难度，这有助于缓解在有限三元组监督下的 CIR 过拟合问题。在基准数据集上的广泛实验表明，WRF4CIR 显著缩小了泛化差距，并相比现有方法取得了实质性的改进。

## Abstract
Composed Image Retrieval (CIR) task aims to retrieve target images based on reference images and modification texts. Current CIR methods primarily rely on fine-tuning vision-language pre-trained models. However, we find that these approaches commonly suffer from severe overfitting, posing challenges for CIR with limited triplet data. To better understand this issue, we present a systematic study of overfitting in VLP-based CIR, revealing a significant and previously overlooked generalization gap across different models and datasets. Motivated by these findings, we introduce WRF4CIR, a Weight-Regularized Fine-tuning network for CIR. Specifically, during the fine-tuning process, we apply adversarial perturbations to the model weights for regularization, where these perturbations are generated in the opposite direction of gradient descent. Intuitively, WRF4CIR increases the difficulty of fitting the training data, which helps mitigate overfitting in CIR under limited triplet supervision. Extensive experiments on benchmark datasets demonstrate that WRF4CIR significantly narrows the generalization gap and achieves substantial improvements over existing methods.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《WRF4CIR: Weight-Regularized Fine-Tuning Network for Composed Image Retrieval》** 的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：在组合图像检索（CIR）任务中，基于视觉-语言预训练模型（VLP）的微调方法普遍存在严重的**过拟合（Overfitting）**现象。
*   **研究动机**：作者通过系统研究发现，由于 CIR 标注数据（参考图+修改文本+目标图的三元组）获取成本高、规模有限，模型在训练集上表现极佳（Recall 接近 90%），但在验证集上表现平平（Recall 约 50%），存在巨大的**泛化差距（Generalization Gap）**。现有的数据增强方法对缓解此问题效果有限，因此需要一种更有效的微调策略。

### 2. 方法论：WRF4CIR
*   **核心思想**：引入**权重正则化（Weight Regularization）**，通过在微调过程中对模型权重施加对抗性扰动，使模型在“更平坦”的权重损失景观（Weight Loss Landscape）中寻找最优解，从而提高泛化能力。
*   **关键技术细节**：
    *   **对抗性权重扰动**：在每个 mini-batch 训练时，计算当前权重的梯度，并沿着梯度的**反方向**（即增加损失的方向）对权重施加微小扰动 $\delta$。
    *   **扰动约束**：扰动的大小 $\delta$ 与该层权重的范数成正比，并由超参数 $\gamma$ 控制强度，确保扰动不会破坏训练稳定性。
    *   **算法流程**：
        1.  **前向与反向传播**：计算原始模型的梯度。
        2.  **生成扰动**：根据梯度方向生成对抗性权重扰动并应用到模型上。
        3.  **再次计算梯度**：在扰动后的模型上再次进行前向和反向传播。
        4.  **参数更新**：利用扰动后计算得到的梯度来更新原始模型的参数，随后移除扰动。
*   **直观理解**：这种方法人为增加了拟合训练数据的难度，迫使模型学习更具鲁棒性的特征，而非记住特定的训练样本。

### 3. 实验设计
*   **数据集**：
    *   **FashionIQ**：包含 Dress、Shirt、TopTee 三个类别，侧重于时尚领域的属性修改。
    *   **CIRR**：基于 NLVR2 的真实场景图像，侧重于开放领域的复杂语义修改。
    *   **Shoes**：用于分析过拟合现象的辅助数据集。
*   **Benchmark 与对比方法**：
    *   对比了包括 **CLIP4CIR**、**BLIP4CIR**、**CASE**、**SPRC**（当前 SOTA 之一）、**CCIN**、**RUNC** 等多种基于 VLP 的主流方法。
    *   还对比了利用大语言模型（LLM）生成额外数据的增强方法（如 **SPN4CIR**）。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA RTX A100 (40GB)** GPU。
*   **软件框架**：PyTorch。
*   **训练细节**：
    *   主干网络采用 **BLIP-2 ViT-G**。
    *   学习率 1e-5，采用余弦退火调度。
    *   **训练时长**：在 FashionIQ 上，基准方法约为 329s/epoch，而 WRF4CIR（全对抗模式）约为 **573s/epoch**。由于需要两次前向/后向传播，训练耗时增加了约 74%。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **多维度验证**：在 3 个数据集、2 种主流 VLP 主干（CLIP, BLIP-2）、3 种融合策略（Late-fusion, Early-fusion, Text Inversion）下均验证了有效性。
    *   **消融实验**：详细探讨了扰动强度 $\gamma$、扰动方向（随机 vs 对抗）、数据规模（20% 到 100%）对性能的影响。
    *   **参数高效微调（PEFT）**：验证了在 LoRA 微调模式下该方法依然能显著缩小泛化差距。
*   **评价**：实验设计非常充分且客观。作者不仅报告了最终精度，还通过可视化“学习曲线”和“损失景观图”深入剖析了方法奏效的原因，具有很强的说服力。

### 6. 主要结论与发现
*   **过拟合是 CIR 的瓶颈**：实验证明，过拟合是限制当前 VLP 模型在 CIR 任务上性能的核心因素。
*   **WRF4CIR 效果显著**：在 FashionIQ 上平均 Recall 提升了约 2.5%，在 CIRR 上提升了约 1.5%，甚至超过了使用额外 LLM 生成数据的方法。
*   **数据效率提升**：使用 WRF4CIR 仅需 60% 的训练数据即可达到基准方法 100% 数据的性能。
*   **平坦性与泛化相关**：可视化证明，WRF4CIR 确实引导模型走向了更平坦的损失区域。

### 7. 优点（亮点）
*   **即插即用**：作为一种微调策略，它可以无缝集成到任何基于梯度的 CIR 模型中，无需修改模型架构。
*   **无需额外资源**：相比于需要 LLM 生成数万条额外三元组的方法，该方法仅通过优化训练过程实现性能突破，更加高效、纯粹。
*   **推理零成本**：扰动仅在训练阶段存在，推理时的模型参数是固定的，不增加任何检索延迟。

### 8. 不足与局限
*   **训练开销增加**：由于每个 step 需要两次梯度计算，训练时间显著增加（接近翻倍）。
*   **超参数敏感性**：扰动强度 $\gamma$ 需要针对不同数据集进行微调，虽然文中给出了推荐范围（0.001 左右），但在新任务上可能需要重新搜索。
*   **极端数据匮乏**：虽然缓解了过拟合，但在极小规模数据（如低于 20%）下，性能提升的绝对值虽然存在，但模型整体表现依然受限。

（完）
