---
title: "CoME-VL: Scaling Complementary Multi-Encoder Vision-Language Learning"
title_zh: CoME-VL：扩展互补多编码器视觉-语言学习
authors: "Ankan Deria, Komal Kumar, Xilin He, Imran Razzak, Hisham Cholakkal, Fahad Shahbaz Khan, Salman Khan"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03231v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 扩展对比学习与自监督视觉编码器在视觉语言模型中的融合
tldr: 针对现有视觉语言模型（VLM）过度依赖单一对比学习编码器（如CLIP）而缺乏密集语义的问题，本文提出CoME-VL框架。该框架通过模块化融合对比学习编码器与自监督DINO编码器，利用熵引导的多层聚合和正交约束投影减少冗余，并结合RoPE增强的交叉注意力机制生成紧凑的融合视觉Token。实验表明，CoME-VL在视觉理解和定位任务上显著优于单编码器基准，并在RefCOCO等任务上达到SOTA水平。
selection_source: fresh_fetch
motivation: 现有的对比学习视觉编码器虽擅长跨模态对齐，但缺乏自监督编码器所具备的丰富密集语义和识别鲁棒性。
method: 提出一种模块化融合框架，通过熵引导聚合、正交约束投影及RoPE增强的交叉注意力机制，高效整合异构视觉特征并注入大语言模型。
result: "在视觉理解和定位任务上分别平均提升4.9%和5.4%，并在RefCOCO检测任务上取得SOTA性能。"
conclusion: 融合互补的对比学习与自监督视觉表征能显著提升VLM的综合性能，为多编码器视觉语言学习提供了有效的扩展方案。
---

## 摘要
最近的视觉-语言模型（VLM）通常依赖于使用对比图文目标训练的单一视觉编码器，例如 CLIP 风格的预训练。虽然对比编码器在跨模态对齐和检索方面非常有效，但自监督视觉编码器通常能捕捉到更丰富的密集语义，并在识别和理解任务中表现出更强的鲁棒性。在这项工作中，我们研究了如何扩展这些互补视觉表示的融合，以进行视觉-语言建模。我们提出了 CoME-VL：互补多编码器视觉-语言（Complementary Multi-Encoder Vision-Language），这是一个模块化融合框架，它将对比训练的视觉编码器与自监督 DINO 编码器相结合。我们的方法通过以下方式执行表示级融合：(i) 采用正交约束投影的熵引导多层聚合以减少冗余；(ii) 采用 RoPE 增强的交叉注意力机制来对齐异构 token 网格并生成紧凑的融合视觉 token。融合后的 token 可以注入到仅解码器（decoder-only）的 LLM 中，且对标准 VLM 流线的改动极小。在多种视觉-语言基准测试上的广泛实验表明，CoME-VL 一贯优于单一编码器基线。特别是，我们观察到在视觉理解任务上平均提升了 4.9%，在定位任务上平均提升了 5.4%。我们的方法在 RefCOCO 检测任务上达到了最先进的性能，同时比基线有了大幅提升。最后，我们对层合并、非冗余特征混合和融合容量进行了消融研究，以评估互补的对比信号和自监督信号如何影响 VLM 的性能。

## Abstract
Recent vision-language models (VLMs) typically rely on a single vision encoder trained with contrastive image-text objectives, such as CLIP-style pretraining. While contrastive encoders are effective for cross-modal alignment and retrieval, self-supervised visual encoders often capture richer dense semantics and exhibit stronger robustness on recognition and understanding tasks. In this work, we investigate how to scale the fusion of these complementary visual representations for vision-language modeling. We propose CoME-VL: Complementary Multi-Encoder Vision-Language, a modular fusion framework that integrates a contrastively trained vision encoder with a self-supervised DINO encoder. Our approach performs representation-level fusion by (i) entropy-guided multi-layer aggregation with orthogonality-constrained projections to reduce redundancy, and (ii) RoPE-enhanced cross-attention to align heterogeneous token grids and produce compact fused visual tokens. The fused tokens can be injected into a decoder-only LLM with minimal changes to standard VLM pipelines. Extensive experiments across diverse vision-language benchmarks demonstrate that CoME-VL consistently outperforms single-encoder baselines. In particular, we observe an average improvement of 4.9% on visual understanding tasks and 5.4% on grounding tasks. Our method achieves state-of-the-art performance on RefCOCO for detection while improving over the baseline by a large margin. Finally, we conduct ablation studies on layer merging, non-redundant feature mixing, and fusion capacity to evaluate how complementary contrastive and self-supervised signals affect VLM performance.

---

## 论文详细总结（自动生成）

以下是对论文《CoME-VL: Scaling Complementary Multi-Encoder Vision-Language Learning》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：当前的视觉-语言模型（VLM）大多依赖单一的视觉编码器（如 CLIP 或 SigLIP），这些编码器通过对比学习进行预训练，擅长全局语义对齐，但在细粒度空间感知、精确物体定位和密集语义理解方面存在局限。
*   **核心问题**：如何有效地融合“对比学习编码器”（语义强）与“自监督编码器”（空间结构强）的互补特征，并解决多编码器融合带来的特征冗余、Token 数量激增以及异构分辨率对齐等挑战。
*   **研究动机**：通过扩展视觉端的表征能力，打破视觉编码器成为 VLM 性能瓶颈的现状，提升模型在视觉理解和定位（Grounding）任务上的综合表现。

### 2. 方法论
CoME-VL 提出了一个模块化的多编码器融合框架，核心技术包括：
*   **熵引导的层选择（Entropy-guided Selection）**：通过分析 Token 熵随层深度的变化，发现 SigLIP2 在全层保持高熵（捕捉多样语义），而 DINOv3 的熵在深层显著降低（聚焦空间结构）。据此选择 SigLIP2 的全层与 DINOv3 的中深层（10-23层）进行融合。
*   **正交正则化多层混合（Orthogonality-Regularized Mixing）**：为减少相邻层之间的信息冗余，引入“正交层（OL）”。在聚合特征前，通过施加正交约束的线性投影，确保混合特征占据互补的子空间，提高表征效率。
*   **RoPE 增强的交叉注意力对齐**：
    *   不直接拼接不同编码器的 Token（避免 LLM 上下文过长），而是以 SigLIP2 特征为 Query，DINOv3 特征为 Key/Value 进行交叉注意力融合。
    *   引入 **2D 旋转位置编码（RoPE）**，使注意力机制能够感知相对空间偏移，从而在不同分辨率的 Token 网格间建立精确的空间对应关系。
*   **注入 LLM**：融合后的紧凑视觉 Token 通过门控残差连接注入到仅解码器架构的 LLM（如 Qwen2-7B）中。

### 3. 实验设计
*   **数据集**：
    *   **PixMo 平台**：涵盖图表（Charts）、图表（Diagrams）、表格（Tables）、计数（Counting）和指向（Pointing）等任务。
    *   **RefCOCO**：用于验证物体检测和定位能力（val, testA, testB 分割集）。
*   **基准模型（Benchmark）**：以 **Molmo-7B** 为主要基线。
*   **对比方法**：包括 LLaVA-1.5/13B、InternVL2-8B、Qwen2-VL-7B、Pixtral-12B、Kosmos-2 等主流 VLM，以及专门的特征合并方法如 CLIP-to-DINO。

### 4. 资源与算力
*   **硬件设备**：使用了 **64 块 AMD Instinct MI210 GPU**（每块 64GB 显存）。
*   **训练时长**：总计训练了 **7 天**。
*   **技术细节**：采用 BF16 精度和全分片数据并行（FSDP）技术，序列长度设为 2304。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括 PixMo 全任务对比、RefCOCO 定位精度测试、不同 DINO 版本（v1/v2/v3）的对比实验。
*   **消融实验**：针对“层选择范围”、“正交层（OL）的贡献”、“RoPE 对齐的作用”以及“推理效率”进行了详细消融。
*   **充分性评价**：实验设计较为全面，不仅关注准确率提升，还分析了特征冗余度和推理延迟。通过对比多种 SOTA 模型，证明了该方法在理解和定位任务上的双重优势，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升**：CoME-VL 在视觉理解任务上平均提升 **4.9%**，在定位任务上平均提升 **5.4%**。
*   **互补性验证**：实验证实 SigLIP2 提供语义对齐，而 DINOv3 显著增强了计数和坐标预测的精度。
*   **层级特性**：发现 DINO 的深层特征对定位至关重要，而简单地拼接所有层会引入冗余，正交化处理能有效提升性能。
*   **SOTA 表现**：在 RefCOCO 任务上超越了现有的特征融合方法，证明了其在密集预测任务中的潜力。

### 7. 优点
*   **科学的层选择**：引入熵分析作为层选择的理论依据，而非经验性的随机选择。
*   **计算效率平衡**：通过交叉注意力机制控制了输入 LLM 的 Token 数量，相比直接拼接特征的方法（如 COMM），推理速度更快且显存占用更低。
*   **即插即用**：该模块化设计可以轻松集成到现有的仅解码器 VLM 流线中，对原有架构改动较小。

### 8. 不足与局限
*   **推理开销**：尽管比拼接法高效，但相比单编码器基线，推理时间仍从 1.26s 增加到了 1.52s（约增加 20%）。
*   **训练成本**：需要大规模 GPU 集群长时间训练，对于资源受限的研究团队门槛较高。
*   **模型规模局限**：实验主要集中在 7B 参数规模，尚未在超大规模模型（如 70B+）上验证其扩展性（Scaling Law）表现。

（完）
