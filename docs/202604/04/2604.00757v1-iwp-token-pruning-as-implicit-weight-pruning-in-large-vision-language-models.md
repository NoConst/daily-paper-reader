---
title: "IWP: Token Pruning as Implicit Weight Pruning in Large Vision Language Models"
title_zh: IWP：大规模视觉语言模型中作为隐式权重剪枝的 Token 剪枝
authors: "Dong-Jae Lee, Sunghyun Baek, Junmo Kim"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00757v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 大型视觉语言模型的令牌剪枝框架
tldr: 本研究针对大型视觉语言模型（LVLM）中视觉标记过多导致的计算开销问题，提出了一种名为IWP的无需训练的标记剪枝框架。该方法从注意力机制的对偶形式出发，将注意力视为隐式线性层，并将标记剪枝转化为对隐式权重矩阵的最优子集选择问题。通过引入衡量信息量和重复度的指标及PC-MMR算法，在保持性能的同时显著提升了推理效率，为LVLM压缩提供了新视角。
selection_source: fresh_fetch
motivation: 现有的LVLM标记剪枝方法多基于经验性策略，缺乏对注意力机制内部原理的理论支撑，难以在效率和性能间取得最优平衡。
method: 将注意力重构为由标记KV对生成的隐式线性层权重，并利用Progressive Chunked MMR算法筛选出能最佳近似原始权重的标记子集。
result: 实验结果表明，该方法在多个视觉理解基准测试中均优于现有的剪枝技术，实现了更优的性能与计算成本权衡。
conclusion: IWP框架证明了从隐式权重剪枝角度优化视觉标记的可行性，为开发高效的大型视觉语言模型提供了理论依据和实践路径。
---

## 摘要
大规模视觉语言模型在图像和视频理解任务中展现出卓越的性能，但其计算成本随视觉 Token 数量的增加而迅速增长。现有的 Token 剪枝方法主要通过经验性手段缓解这一问题，却忽视了注意力的内部机制。在本文中，我们提出了一种基于注意力对偶形式视角的新型免训练 Token 剪枝框架。我们将注意力重新表述为一个隐式线性层，其权重矩阵是若干秩为 1 的外积之和，每个外积由单个 Token 的键值对生成。因此，Token 剪枝被简化为选择这些秩为 1 更新的最优子集，以最佳程度近似原始对偶权重矩阵。通过将这一视角扩展到 LVLM 中的标准 Softmax 注意力，我们推导出一个能够同时量化 Token 信息量级和信息重复度的新型指标。为了利用该指标高效选择子集，我们引入了渐进式分块最大边际相关性（Progressive Chunked Maximal Marginal Relevance）。广泛的实验表明，我们的方法在性能和效率之间实现了更好的权衡，同时为现有的剪枝方法提供了全新的视角。

## Abstract
Large Vision Language Models show impressive performance across image and video understanding tasks, yet their computational cost grows rapidly with the number of visual tokens. Existing token pruning methods mitigate this issue through empirical approaches while overlooking the internal mechanism of attention. In this paper, we propose a novel training free token pruning framework grounded in the dual form perspective of attention. We reformulate attention as an implicit linear layer whose weight matrix is the sum of rank 1 outer products, each generated by a single token's key value pair. Token pruning thus reduces to selecting an optimal subset of these rank 1 updates that best approximates the original dual weight matrix. Extending this perspective to standard softmax attention in LVLMs, we derive a novel metric quantifying both a token's information magnitude and information duplication. To efficiently select the subset with the proposed metric, we introduce Progressive Chunked Maximal Marginal Relevance. Extensive experiments demonstrate that our method achieves a better trade off between performance and efficiency, while providing another perspective on existing pruning approaches.

---

## 论文详细总结（自动生成）

### 论文分析报告：IWP - 大规模视觉语言模型中作为隐式权重剪枝的 Token 剪枝

#### 1. 核心问题与整体含义（研究动机和背景）
随着大规模视觉语言模型（LVLM）向高分辨率图像和长视频理解演进，视觉 Token 的数量呈爆炸式增长。由于自注意力机制的计算复杂度随序列长度呈平方级增长，过多的视觉 Token 导致了巨大的计算和内存开销。
*   **研究动机**：现有的 Token 剪枝方法（如基于注意力分数或多样性采样）大多是经验性的，缺乏对注意力机制内部运作原理的深入理解。
*   **核心问题**：如何建立一个有理论支撑的、无需训练的剪枝框架，从注意力机制的本质出发，识别并保留对模型输出贡献最大的视觉 Token？

#### 2. 方法论：核心思想与关键技术
论文提出了 **IWP (Implicit Weight Pruning)** 框架，其核心思想是将注意力机制重新解释为**对偶形式（Dual Form）**的线性变换。

*   **核心思想**：在线性注意力中，$qK^\top V$ 可以等价地写为 $q(\sum k_i^\top v_i)$。这意味着每个 Token $i$ 实际上是为注意力层贡献了一个**秩为 1 的隐式权重更新矩阵**（$\Delta W_i = k_i^\top v_i$）。Token 剪枝的本质就是选择一个最优的更新子集，以最少的 Token 数量近似原始的完整权重矩阵。
*   **关键技术细节**：
    1.  **信息量级指标（Information Magnitude）**：通过扩展到 Softmax 注意力，推导出衡量 Token 重要性的指标 $Score_i = \kappa(q, k_i)\|v_i\|_2$。该指标综合考虑了查询向量（Query）的对齐程度、键（Key）的能量以及值（Value）的量级。
    2.  **信息重复度指标（Information Duplication）**：定义了两个 Token 诱导的权重更新之间的 Frobenius 内积，用于量化信息冗余。
    3.  **PC-MMR 算法（Progressive Chunked Maximal Marginal Relevance）**：为了解决标准 MMR 算法在推理时计算量大的问题，提出了一种渐进式分块选择策略。它以分块方式迭代选择 Token，在最大化信息量级的同时，通过惩罚项抑制与已选 Token 冗余度高的候选者。

#### 3. 实验设计
*   **数据集与场景**：
    *   **图像理解**：涵盖 10 个主流基准测试，包括文本密集型（DocVQA, InfoVQA, TextVQA）、知识推理型（MMMU, MMBench, SciQA, MMStar）以及感知与幻觉评估（MME, POPE）。
    *   **视频理解**：包括 EgoSchema, Video-MME, MLVU, NExT-QA。
*   **模型（Backbones）**：LLaVA-OneVision-7B, Qwen2.5-VL-7B, 以及更大规模的 Qwen2.5-VL-32B。
*   **对比方法**：FastV, PACT, VisionZip, DivPrune, CDPruner 等最先进的剪枝方法。
*   **剪枝比例**：测试了 35.3%、22.2% 和 11.1% 三种不同的 Token 留存预算。

#### 4. 资源与算力
*   **GPU 型号**：性能分析和推理效率测试是在单张 **NVIDIA RTX A6000** GPU 上完成的。
*   **训练时长**：由于该方法是**免训练（Training-free）**的插件式方法，因此不需要额外的训练算力投入。
*   **推理效率**：实验记录了生成时间（Gen.time）和显存占用（VRAM），证明了该方法在减少计算开销方面的实际效果。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 14 个不同的图像和视频基准测试上进行了广泛实验，并针对不同模型架构（LLaVA 和 Qwen 系列）验证了泛化性。
*   **消融实验**：非常充分。作者详细分析了：
    1.  信息量级指标中各组件（Query 对齐、K/V 量级）的贡献。
    2.  不同表示空间（Key, Value, Hidden state）对冗余度测量的影响。
    3.  旋转位置编码（RoPE）对剪枝效果的影响。
    4.  剪枝层数选择的敏感性分析。
    5.  PC-MMR 算法超参数的调节。
*   **客观性**：通过在不同预算下与多种 SOTA 方法对比，并使用官方评估框架（lmms-eval），实验结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能领先**：IWP 在所有测试的 Token 预算下均优于现有的经验性剪枝方法，尤其在极低预算（11.1%）下优势更明显。
*   **架构鲁棒性**：与某些依赖特定架构特征的方法不同，IWP 基于注意力机制的通用数学性质，在不同模型（如支持动态分辨率的 Qwen2.5-VL）上表现稳定。
*   **理论验证**：实验证明，从对偶权重更新的角度衡量 Token 重要性，比单纯依赖注意力分数或特征相似度更准确。

#### 7. 优点
*   **理论深刻**：将 Token 剪枝与隐式权重更新联系起来，为该领域提供了坚实的数学基础。
*   **免训练且即插即用**：无需微调即可直接应用于现有的预训练 LVLM。
*   **效率与性能平衡**：PC-MMR 算法在保证剪枝质量的同时，显著降低了选择 Token 过程中的计算延迟。
*   **多模态通用性**：虽然本文侧重视觉，但其推导过程是模态无关的，具有扩展到音频、点云等其他连续模态的潜力。

#### 8. 不足与局限
*   **模态覆盖有限**：目前仅在视觉模态上进行了验证，尚未在纯文本或其他多模态输入（如音频）上进行大规模测试。
*   **超参数依赖**：PC-MMR 算法引入了惩罚强度 $\lambda$、增长因子 $g$ 等超参数，虽然有消融实验指导，但在不同任务中可能仍需微调以达到最优。
*   **层级固定**：目前实验主要集中在固定层（如第 4 层）进行一次性剪枝，尚未深入探讨多层渐进式剪枝的动态优化。

（完）
