---
title: Intrinsic Concept Extraction Based on Compositional Interpretability
title_zh: 基于组合可解释性的内在概念提取
authors: "Hanyu Shi, Hong Tao, Guoheng Huang, Jianbin Jiang, Xuhang Chen, Chi-Man Pun, Shanhu Wang, Pan Pan"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11795v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 组合式且可解释的内在概念提取
tldr: 本研究针对现有无监督概念提取方法无法提取可组合内在概念的问题，提出了“组合可解释内在概念提取（CI-ICE）”新任务。通过提出的 HyperExpress 方法，利用扩散模型的层次化建模能力，在双曲空间中实现对象级和属性级概念的解耦与提取。该方法通过概念优化映射确保了概念的可组合性与解释性，为单图概念提取提供了高效的解决方案。
selection_source: fresh_fetch
motivation: 现有无监督概念提取方法难以从单张图像中提取出具有可组合性的内在对象级和属性级概念。
method: 提出 HyperExpress 方法，利用双曲空间的层次建模能力实现概念解耦，并通过概念级优化映射维持复杂的概念间关系。
result: 实验证明该方法在从单张图像中提取具有组合可解释性的内在概念方面表现出卓越的性能。
conclusion: HyperExpress 成功解决了 CI-ICE 任务，实现了对单图概念的精确提取与重构，增强了生成模型的可解释性。
---

## 摘要
无监督概念提取旨在从单张图像中提取概念；然而，现有方法无法提取具有组合性的内在概念。为解决这一问题，本文引入了一项名为“组合式且可解释的内在概念提取”（CI-ICE）的新任务。CI-ICE 任务旨在利用基于扩散的文本生成图像模型，从单张图像中提取可组合的对象级和属性级概念，从而可以通过这些概念的组合来重建原始概念。为了实现这一目标，我们提出了一种名为 HyperExpress 的方法，通过两个核心方面来解决 CI-ICE 任务。具体而言，首先，我们提出了一种概念学习方法，利用双曲空间固有的层次化建模能力来实现准确的概念解耦，同时保留概念间的层次结构和关系依赖；其次，我们引入了一种逐概念优化方法，对概念嵌入空间进行映射，以在确保概念组合性的同时维持复杂的概念间关系。我们的方法在从单张图像中提取具有组合可解释性的内在概念方面表现出了卓越的性能。

## Abstract
Unsupervised Concept Extraction aims to extract concepts from a single image; however, existing methods suffer from the inability to extract composable intrinsic concepts. To address this, this paper introduces a new task called Compositional and Interpretable Intrinsic Concept Extraction (CI-ICE). The CI-ICE task aims to leverage diffusion-based text-to-image models to extract composable object-level and attribute-level concepts from a single image, such that the original concept can be reconstructed through the combination of these concepts. To achieve this goal, we propose a method called HyperExpress, which addresses the CI-ICE task through two core aspects. Specifically, first, we propose a concept learning approach that leverages the inherent hierarchical modeling capability of hyperbolic space to achieve accurate concept disentanglement while preserving the hierarchical structure and relational dependencies among concepts; second, we introduce a concept-wise optimization method that maps the concept embedding space to maintain complex inter-concept relationships while ensuring concept composability. Our method demonstrates outstanding performance in extracting compositionally interpretable intrinsic concepts from a single image.

---

## 论文详细总结（自动生成）

### 论文总结：基于组合可解释性的内在概念提取 (HyperExpress)

#### 1. 核心问题与整体含义（研究动机和背景）
现有的**无监督概念提取 (UCE)** 方法虽然能从单张图像中识别对象，但在提取“内在概念”（如物体的颜色、材质等属性）时存在局限。主要问题在于：
*   **缺乏组合性 (Compositionality)**：提取出的对象级和属性级概念往往是孤立的，无法通过简单的数学组合（如向量相加）重新构建出原始图像内容。
*   **可解释性不足**：由于嵌入空间缺乏约束，用户难以理解模型是如何将复杂视觉特征分解为基本元素的。
为此，本文提出了新任务 **CI-ICE（组合式且可解释的内在概念提取）**，旨在从单图中提取出既能准确解耦、又能在嵌入空间中通过线性组合还原原始语义的内在概念。

#### 2. 方法论：核心思想与关键技术
论文提出了 **HyperExpress** 框架，核心思想是利用**双曲几何 (Hyperbolic Geometry)** 的层次建模能力和**水平球投影 (Horosphere Projection)** 的等距特性。

*   **双曲概念学习 (Concept Learning)**：
    *   **双曲对比学习 (HCL)**：在庞加莱球 (Poincaré ball) 空间中，利用双曲距离约束，将对象级概念（如“机器人”）与属性级概念（如“金色”、“金属”）在空间位置上进行区分。
    *   **双曲蕴含学习 (HEL)**：在洛伦兹模型 (Lorentz model) 下引入“蕴含锥 (Entailment cone)”，确保属性概念在逻辑上被包含在对象概念的范围内，从而建立概念间的关联依赖。
*   **概念级优化 (Concept-wise Optimization)**：
    *   **水平球投影 (HP) 模块**：这是确保组合性的关键。通过将双曲嵌入映射到零曲率的水平球上，该模块赋予了嵌入空间类似于欧几里得空间的性质（如支持向量加法），同时不破坏原有的双曲层次结构。
*   **损失函数**：结合了重构损失 ($L_{recon}$)、双曲三元组损失 ($L_{triplet}$)、双曲蕴含损失 ($L_{entail}$) 以及基于 Wasserstein 距离的注意力对齐损失 ($L_{attention}$)。

#### 3. 实验设计
*   **数据集与场景**：使用了 **UCEBench**（无监督概念提取基准）和 **ICBench**（内在概念基准），以及 D1 图像数据集。
*   **评估指标**：
    *   **SIM_I / SIM_C**：身份相似度与组合相似度。
    *   **ACC_k**：Top-k 准确率，衡量概念解耦能力。
    *   **SIM_T-T / SIM_T-V**：文本-文本及文本-视觉相似度（利用 GPT-4o 生成的描述作为真值）。
*   **对比方法**：包括 Break-A-Scene、ConceptExpress、AutoConcept 和最新的 ICE (Intrinsic Concept Extraction)。

#### 4. 资源与算力
论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）及具体的训练时长。通常此类基于扩散模型（如 Stable Diffusion）的微调任务需要高性能显存（如 A100 或 V100 级别），但文中仅提及了算法流程和参数设置。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流基准测试上进行了定量对比，并提供了丰富的定性生成结果（对比图）。
*   **消融实验**：针对 HCL、HEL 和 HP 三个核心模块分别做了消融分析，验证了每个组件对最终性能（尤其是 ACC 准确率）的贡献。
*   **客观性**：实验涵盖了从单概念重构到多概念组合的多种场景，并引入了用户研究（见附录）来评估人类对“可解释性”的主观感受，实验设计较为全面、客观。

#### 6. 主要结论与发现
*   **解耦能力显著提升**：HyperExpress 在 ACC_1 和 ACC_3 指标上大幅领先现有方法（如在 UCEBench 上 ACC_1 达到 50.4%，远高于 ICE 的 32.5%），证明了双曲空间在处理层次化概念时的优越性。
*   **组合路径可解释**：定性结果显示，HyperExpress 能够清晰地展示“对象 + 颜色 + 材质 = 最终图像”的逻辑路径，而对比方法生成的组合图像往往语义模糊。
*   **性能权衡**：虽然在某些重构相似度（SIM_I）上略低于 ICE，但换取了极高的组合性和逻辑严密性。

#### 7. 优点（亮点）
*   **数学建模新颖**：首次将双曲几何的蕴含关系与水平球投影引入 UCE 任务，解决了嵌入空间“不可加”的痛点。
*   **任务定义前瞻**：提出的 CI-ICE 任务更符合人类对 AI 可解释性的直观需求（即“可拆解、可组装”）。
*   **逻辑一致性强**：通过蕴含锥约束，确保了模型不会提取出逻辑不通的属性组合。

#### 8. 不足与局限
*   **重构精度略降**：为了满足组合性约束，模型在还原原始图像细节的绝对精度上（SIM_I）相比 ICE 有轻微下降。
*   **计算复杂度**：双曲空间的指数映射和洛伦兹变换增加了计算开销，且对超参数（如曲率 $\kappa$ 和边距 $\gamma$）可能较为敏感。
*   **应用限制**：目前主要针对具有清晰对象和属性的图像，对于背景极其复杂或概念边界模糊的自然场景，提取效果可能受限。

（完）
