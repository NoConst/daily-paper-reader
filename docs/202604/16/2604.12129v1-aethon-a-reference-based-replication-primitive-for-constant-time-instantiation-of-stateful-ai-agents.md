---
title: "Aethon: A Reference-Based Replication Primitive for Constant-Time Instantiation of Stateful AI Agents"
title_zh: Aethon：一种用于有状态 AI 智能体常数时间实例化的基于引用的复制原语
authors: "Swanand Rao, Kiran Kashalkar, Parvathi Somashekar, Priya Krishnan"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.12129v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 具有分层记忆的有状态AI智能体实例化
tldr: 随着AI从无状态推理转向有状态智能体执行，传统实例化方法因内存和延迟开销大而受限。本文提出Aethon，一种基于引用的复制原语，通过将智能体表示为稳定定义、分层内存和局部上下文的组合视图，实现近乎常数时间的实例化。该方法利用写时复制语义解耦了创建成本与继承结构，为大规模、轻量化且可治理的智能体编排提供了新的系统抽象。
selection_source: fresh_fetch
motivation: 现有的智能体实例化模型依赖于完全物化对象，导致在处理复杂、有状态的AI智能体时面临显著的延迟和内存开销。
method: 提出Aethon原语，通过引用而非复制的方式，利用分层继承和写时复制（CoW）语义来构建智能体的执行身份。
result: 实现了近乎常数时间的智能体实例化，显著降低了创建成本，并增强了多智能体协作与治理的扩展性。
conclusion: 基于引用的实例化是生产级智能体软件更合适的系统抽象，推动了AI基础设施向轻量化和组合化方向演进。
---

## 摘要
从无状态模型推理向有状态智能体执行的转变，正在重塑现代 AI 基础设施底层的系统假设。虽然大语言模型已使持久化、使用工具且具备协作能力的智能体在技术上成为可能，但现有的运行时架构仍受限于重物化（materialization-heavy）的实例化模型，这带来了显著的延迟和内存开销。本文介绍了 Aethon，一种用于近常数时间实例化有状态 AI 智能体的基于引用的复制原语。Aethon 不再将智能体重构为完全物化的对象，而是将每个实例表示为基于稳定定义、分层内存和本地上下文叠加层的组合视图。通过将实例化从复制转向引用，Aethon 实现了创建成本与继承结构的解耦。我们展示了 Aethon 的概念框架、系统架构和内存模型，包括分层继承和写时复制（copy-on-write）语义。我们分析了其对复杂性、可扩展性、多智能体编排以及企业治理的影响。我们认为，基于引用的实例化不仅是一种优化，更是生产级智能体软件更合适的系统抽象。Aethon 指向了一类新型 AI 基础设施，其中智能体成为轻量级、可组合的执行身份，能够在大规模环境下被生成、特化和治理。

## Abstract
The transition from stateless model inference to stateful agentic execution is reshaping the systems assumptions underlying modern AI infrastructure. While large language models have made persistent, tool-using, and collaborative agents technically viable, existing runtime architectures remain constrained by materialization-heavy instantiation models that impose significant latency and memory overhead.   This paper introduces Aethon, a reference-based replication primitive for near-constant-time instantiation of stateful AI agents. Rather than reconstructing agents as fully materialized objects, Aethon represents each instance as a compositional view over stable definitions, layered memory, and local contextual overlays. By shifting instantiation from duplication to reference, Aethon decouples creation cost from inherited structure.   We present the conceptual framework, system architecture, and memory model underlying Aethon, including layered inheritance and copy-on-write semantics. We analyze its implications for complexity, scalability, multi-agent orchestration, and enterprise governance. We argue that reference-based instantiation is not merely an optimization, but a more appropriate systems abstraction for production-scale agentic software.   Aethon points toward a new class of AI infrastructure in which agents become lightweight, composable execution identities that can be spawned, specialized, and governed at scale.

---

## 论文详细总结（自动生成）

### 论文总结：Aethon —— 用于有状态 AI 智能体常数时间实例化的基于引用的复制原语

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：AI 系统正从“无状态推理”（单次请求-响应）转向“有状态执行”（持久、连续、具备上下文的智能体）。现有的系统通常采用**显式物化（Explicit Materialization）**模型来创建智能体实例，即每次创建新实例时都要重新加载配置、绑定工具、重建内存。
*   **研究动机**：
    *   **延迟与开销**：物化模型的成本随智能体定义的复杂度（指令、工具、上下文大小）线性增加，导致高延迟和内存浪费。
    *   **扩展性受限**：在“一用户一智能体”或复杂的多智能体编排场景下，频繁的实例化成为系统瓶颈。
    *   **治理难题**：深拷贝后的实例往往变成黑盒，难以追踪其继承关系（Lineage）和状态演变。
*   **整体含义**：Aethon 提出了一种新的系统抽象，将智能体实例化从“复制”转变为“引用”，旨在实现近乎常数时间的实例化，为大规模生产级智能体提供运行时基础。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：将智能体实例视为**组合视图（Compositional View）**而非独立对象。实例不需要物理拥有所有数据，只需拥有指向共享结构的引用和本地差异（Deltas）。
*   **数学表示**：有效实例 $A_i$ 被定义为多个层的组合函数：
    $$A_i = f(D, M_s, M_i, C_i)$$
    *   $D$：规范定义（角色语义、指令、策略）。
    *   $M_s$：共享内存层（组织知识、公共上下文）。
    *   $M_i$：局部状态（实例特有的差异）。
    *   $C_i$：上下文绑定（用户范围、任务元数据）。
*   **关键技术细节**：
    *   **分层内存模型**：内存按作用域（组织级、家族级、用户级、任务级）分层，避免冗余存储。
    *   **写时复制（Copy-on-Write, CoW）语义**：多个实例共享相同的内存层，仅在发生修改时才创建本地副本，确保隔离性。
    *   **延迟实现（Deferred Realization）**：实例化仅创建轻量级引用记录，真正的状态组合推迟到执行阶段。
    *   **三大子系统**：
        1.  **定义底座**：存储版本化的稳定结构。
        2.  **引用底座**：管理轻量级身份和继承关系。
        3.  **解析底座**：在执行时将引用解析为有效的运行时视图。

#### 3. 实验设计
*   **实验性质**：本文属于**系统架构与理论分析类论文**，侧重于提出新的原语和架构设计。
*   **对比方法**：文中主要在理论层面将 Aethon 的“基于引用的模型”与传统的“基于物化的模型”进行对比。
*   **Benchmark**：主要使用**渐进复杂度分析**作为基准。
    *   传统模型复杂度：$O(d + m + b)$（$d$ 为定义大小，$m$ 为内存大小，$b$ 为绑定大小）。
    *   Aethon 复杂度：$O(1)$（相对于继承结构的大小是常数级的）。
*   **缺失部分**：文中未提供具体的实测性能数据表（如毫秒级延迟对比或内存占用曲线图）。

#### 4. 资源与算力
*   **算力说明**：文中**未明确说明**使用了具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于该研究关注的是“运行时实例化逻辑”和“系统架构设计”，而非模型本身的训练，因此其核心贡献在于软件工程和分布式系统层面，不涉及大规模深度学习训练算力。

#### 5. 实验数量与充分性
*   **实验组数**：文中未列出具体的实验组（如不同数据集上的消融实验）。
*   **充分性评价**：从**系统设计**角度看，论文逻辑严密，对内存模型、复杂度和治理能力的论证较为充分。但从**实证科学**角度看，缺乏在真实高并发生产环境下的基准测试数据，这使得其“常数时间”的宣称目前主要停留在架构设计和理论推导阶段。

#### 6. 主要结论与发现
*   **实例化效率**：通过引用机制，可以将智能体创建成本与继承结构的规模解耦，实现近常数时间的实例化。
*   **内存优化**：利用 CoW 和分层存储，内存增长随“语义差异”而非“实例数量”增加，极大提升了空间效率。
*   **治理与审计**：显式的继承链（Lineage）使得智能体的行为溯源、版本回滚和权限控制变得更加透明和可控。
*   **编排范式**：轻量化实例化使得“细粒度多智能体协作”成为可能，开发者可以根据语义需求而非基础设施成本来拆分任务。

#### 7. 优点
*   **架构前瞻性**：敏锐捕捉到 AI 从推理向智能体转变过程中，底层基础设施的不适配问题。
*   **借鉴成熟技术**：巧妙地将操作系统中的虚拟内存、Fork 语义和 CoW 技术引入到 AI 智能体领域。
*   **解决实际痛点**：针对企业级应用中的冷启动延迟、内存爆炸和合规审计问题提出了系统性方案。

#### 8. 不足与局限
*   **缺乏实证数据**：没有提供量化的性能对比实验，难以直观评估在不同负载下的实际收益。
*   **解析开销转移**：虽然实例化变快了，但将复杂度转移到了“解析阶段（Resolution）”，如果解析逻辑过于复杂，可能会抵消部分性能优势。
*   **开发工具链缺失**：基于引用的系统比扁平系统更难调试，需要配套强大的可视化和内省工具，论文对此仅作为“未来工作”提及。
*   **外部副作用处理**：对于智能体触发的外部系统（如 CRM、数据库）操作，Aethon 的引用模型难以直接解决事务一致性和回滚问题。

（完）
