---
title: "RECIPER: A Dual-View Retrieval Pipeline for Procedure-Oriented Materials Question Answering"
title_zh: RECIPER：一种面向过程的材料学问答双视图检索流水线
authors: "Zhuoyu Wu, Wenhui Ou, Pei-Sze Tan, Wenqi Fang, Sailaja Rajanala, Raphaël C. -W. Phan"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11229v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 用于密集检索的双视图检索流水线
tldr: 针对材料科学论文中合成步骤分散、传统段落检索效果不佳的问题，本文提出了 RECIPER 双视图检索框架。该方法通过同时索引原始段落和由大模型提取的程序化摘要，并结合轻量级词法重排序，显著提升了检索精度。实验表明，RECIPER 在多个指标上优于单一检索模型，并有效增强了下游问答任务的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11229v1/fig-001.webp\", \"caption\": \"Fig. 1: Overview of RECIPER Framework. In this framework, the user query is first transformed into feature vectors, which are then used to retrieve the most relevant Paragraph and Recipe embedding vectors from the Chunk Vector Database. Recipe vectors pass through a Threshold Screener to filter out highly similar entries and increase content diversity. The filtered Recipes are then combined with the Paragraphs in a Rule-based Re-rank module and merged with the original query before being fed into the LLM, producing a precise, well-grounded answer.\", \"page\": 2, \"index\": 1, \"width\": 1054, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2604.11229v1/fig-002.webp\", \"caption\": \"Table 3: Overall QA performance across models and retrieval modes. Symbols indicate retrieval mode: ♦ RECIPER, • Paragraph-Dense RAG, ⋆ NoRAG. Metrics are BERT-F1 (F1 score of BERTScore), R-L (ROUGE-L), Cos (Cosine similarity), and BLT (BLEURT). The best value per metric is highlighted with a yellow block and in bold font.\", \"page\": 4, \"index\": 2, \"width\": 486, \"height\": 652}, {\"url\": \"assets/figures/arxiv/2604.11229v1/fig-003.webp\", \"caption\": \"Table 1: Retrieval performance comparison and ablation study. The upper block reports paragraph-based baselines, while the lower block analyzes recipe-based and dual-view variants under the BGE backbone. Recipe-only retrieval is weak, but combining it with paragraph retrieval improves performance, showing that procedural and contextual signals are complementary. RECIPER achieves the best overall results, especially on early-rank metrics, indicating more effective integration of procedural and contextual evidence.\", \"page\": 4, \"index\": 3, \"width\": 1075, \"height\": 324}]"
motivation: 材料科学论文中的合成细节往往分散在长文中，传统的段落级密集检索难以准确捕捉这些程序化信息。
method: 提出一种双视图检索流水线，结合段落级上下文和 LLM 提取的程序化摘要，并利用轻量级词法重排序整合候选流。
result: "在多个检索骨干模型上，RECIPER 显著提升了 Recall@1、nDCG@10 和 MRR 等指标，其中 Recall@1 平均提升了 3.73%。"
conclusion: 程序化摘要可作为材料科学领域程序导向问答任务中有效的补充检索信号，显著优化检索与问答效果。
---

## 摘要
从材料科学论文中检索面向过程的证据具有挑战性，因为关键的合成细节通常分散在冗长且背景复杂的文档中，且仅靠段落级的稠密检索难以有效捕捉。我们提出了 RECIPER，这是一种双视图检索流水线，它同时对段落级上下文和由大语言模型提取的紧凑过程摘要进行索引，随后通过轻量级的词法重排序合并这两个候选流。在四种稠密检索主干网络上，RECIPER 相比于仅使用段落的稠密检索，在早期排名检索性能上均有一致提升，Recall@1 平均提高 3.73，nDCG@10 提高 2.85，MRR 提高 3.13。在使用 BGE-large-en-v1.5 时，其 Recall@1、Recall@5 和 Recall@10 分别达到 86.82%、97.07% 和 97.85%。我们进一步观察到下游问答任务在自动评估指标下的表现有所提升，这表明过程摘要可以作为面向过程的材料学问答中有用的补充检索信号。代码和数据可在 https://github.com/ReaganWu/RECIPER 获取。

## Abstract
Retrieving procedure-oriented evidence from materials science papers is difficult because key synthesis details are often scattered across long, context-heavy documents and are not well captured by paragraph-only dense retrieval. We present RECIPER, a dual-view retrieval pipeline that indexes both paragraph-level context and compact large language model-extracted procedural summaries, then combines the two candidate streams with lightweight lexical reranking. Across four dense retrieval backbones, RECIPER consistently improves early-rank retrieval over paragraph-only dense retrieval, achieving average gains of +3.73 in Recall@1, +2.85 in nDCG@10, and +3.13 in MRR. With BGE-large-en-v1.5, it reaches 86.82%, 97.07%, and 97.85% on Recall@1, Recall@5, and Recall@10, respectively. We further observe improved downstream question answering under automatic metrics, suggesting that procedural summaries can serve as a useful complementary retrieval signal for procedure-oriented materials question answering. Code and data are available at https://github.com/ReaganWu/RECIPER.

---

## 论文详细总结（自动生成）

以下是对论文《RECIPER: A Dual-View Retrieval Pipeline for Procedure-Oriented Materials Question Answering》的结构化深入总结：

### 1. 核心问题与研究背景
*   **核心问题**：在材料科学领域，合成步骤（Procedures）和实验细节往往分散在冗长、背景复杂的学术论文中。传统的基于段落分块（Chunking）的稠密检索（Dense Retrieval）容易割裂步骤间的逻辑联系，导致难以准确检索到面向过程的证据。
*   **研究背景**：虽然检索增强生成（RAG）能缓解大模型的幻觉问题，但在处理精细的科学查询时，标准检索器往往无法有效捕捉结构化的合成逻辑。

### 2. 论文提出的方法论：RECIPER
RECIPER 提出了一种**双视图（Dual-View）检索框架**，其核心思想是将论文表示为两种互补的视图：
*   **双视图构建**：
    1.  **段落视图（Paragraph View）**：保留原始文本的详细上下文。
    2.  **配方视图（Recipe View）**：利用 LLM（具体为 `DeepSeek-R1-Distill-Qwen-32B`）从全文中提取紧凑的、以步骤为导向的“配方”摘要，涵盖材料、操作和条件。
*   **检索与处理流程**：
    1.  **独立检索**：针对用户查询，分别从段落向量库和配方向量库中检索 Top-K 候选。
    2.  **流感知去重（Stream-Aware Deduplication）**：合并两个候选列表，并针对同一文档在同一流中的重复项进行去重，确保证据的多样性。
    3.  **轻量级词法重排序（Lexical Reranking）**：引入查询词覆盖率评分（Query-coverage score），计算查询词在候选文本（标题+正文）中的重合度，通过公式 $\hat{s}_i = s_i + \lambda o_i$ 调整最终排名，以强化词法对齐。

### 3. 实验设计
*   **数据集**：构建了一个材料科学 QA 基准，包含 300 多篇研究论文和 1,024 个由 GPT-5 生成的查询-文档对，侧重于合成步骤、材料属性和特征行为。
*   **检索基准（Baselines）**：
    *   传统方法：BM25。
    *   稠密检索模型：`all-MiniLM-L6-v2`、`Contriever`、`BGE-large-en-v1.5`、`E5-large-v2`。
    *   消融变体：仅段落检索、仅配方检索、朴素混合检索、RRF 融合等。
*   **下游 QA 评估**：使用了 8 个不同参数量（0.5B 到 40B）的 LLM，包括 GPT-5、DeepSeek-32B、Llama-3.1/3.2、Qwen-2.5/3 等。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。
*   **模型使用**：提到了使用 `DeepSeek-R1-Distill-Qwen-32B` 进行离线的程序化摘要提取。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 种不同的检索主干网络上进行了测试，验证了方法的通用性。
    *   进行了详细的消融实验（Table 1），对比了不同视图组合的效果。
    *   在 8 个主流 LLM 上验证了下游 QA 的增益。
*   **充分性评价**：实验设计较为客观、公平。通过跨模型（Backbone-agnostic）的对比，证明了 RECIPER 的提升并非源于特定的嵌入模型，而是源于双视图架构本身。

### 6. 主要结论与发现
*   **配方视图的价值**：单独使用配方摘要进行检索效果较差，但作为段落检索的**补充信号**时，能显著提升性能。
*   **性能提升**：相比仅段落检索，RECIPER 在 Recall@1 上平均提升 **3.73%**，nDCG@10 提升 **2.85%**。
*   **QA 增益**：更好的检索质量直接转化为更准确的回答，尤其对于**小参数量模型**（如 0.5B-1.5B），检索质量的提升能显著弥补其参数知识的不足。

### 7. 优点
*   **互补性强**：成功结合了段落的“细节深度”和摘要的“逻辑宽度”。
*   **轻量高效**：重排序步骤基于简单的词法覆盖，未引入复杂的交叉编码器（Cross-Encoder），计算开销低。
*   **架构无关**：可以灵活插入现有的任何稠密检索流程中。

### 8. 不足与局限
*   **依赖预处理**：需要预先调用 LLM 提取摘要，增加了索引构建阶段的成本和复杂性。
*   **摘要质量风险**：检索效果高度依赖于 LLM 提取摘要的准确性，若 LLM 在提取步骤时产生幻觉，可能干扰检索。
*   **静态表示**：摘要是静态生成的，可能无法覆盖某些极端冷门或非步骤导向的特定查询需求。

（完）
