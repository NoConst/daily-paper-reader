---
title: Leveraging LLMs and Heterogeneous Knowledge Graphs for Persona-Driven Session-Based Recommendation
title_zh: 利用大语言模型和异构知识图谱进行画像驱动的基于会话的推荐
authors: "Muskan Gupta, Suraj Thapa, Jyotsana Khatri"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06928v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 利用大语言模型进行画像驱动的个性化推荐
tldr: 针对会话式推荐中匿名会话导致个性化不足的问题，本文提出一种画像驱动的推荐框架。该框架结合大语言模型（LLM）生成的语义嵌入与异构知识图谱（KG），通过无监督学习提取潜在用户画像。通过信息提取与利用两阶段架构，将画像特征融入数据驱动的推荐流程，有效解决了稀疏数据下的个性化建模难题。
selection_source: fresh_fetch
motivation: 传统的会话式推荐因会话匿名性难以捕捉用户长期偏好，且LLM在缺乏显式身份时难以直接建模用户画像。
method: 利用LLM初始化项目嵌入并结合异构知识图谱，通过异构深度图信息极大化（HDGI）学习潜在画像，并将其整合进改进的SBRS架构中进行推荐与重排序。
result: "在Amazon Books和Movies & TV数据集上的实验证明，该方法在各项指标上均优于仅依赖会话历史的传统序列模型。"
conclusion: 结合LLM的语义理解能力与知识图谱的结构化关系信号，能显著增强匿名会话中的用户画像建模并提升推荐性能。
---

## 摘要
基于会话的推荐系统 (SBRS) 旨在从交互序列中捕捉用户的短期意图。然而，匿名会话的常见假设限制了个性化，特别是在数据稀疏或冷启动的情况下。大语言模型 (LLM) 增强推荐的最新进展表明，LLM 可以生成丰富的物品表示，但由于会话的匿名性，利用 LLM 建模用户画像仍然具有挑战性。在这项工作中，我们提出了一个画像驱动的 SBRS 框架，该框架显式地建模了从异构知识图谱 (KG) 中推断出的潜在用户画像，并将其集成到数据驱动的推荐流水线中。受近期思维链 (chain-of-thought) 推荐方法的启发，我们的框架采用了由个性化信息提取和个性化信息利用组成的双阶段架构。在个性化信息提取阶段，我们构建了一个异构知识图谱，整合了与时间无关的用户-物品、物品-物品、物品-特征关联以及来自 DBpedia 的元数据。然后，我们在使用 LLM 衍生的物品嵌入初始化的知识图谱上，利用异构深度图最大互信息 (HDGI) 目标，以无监督的方式学习潜在用户画像。在个性化信息利用阶段，学习到的画像表示与 LLM 衍生的物品嵌入一起被整合到改进的数据驱动 SBRS 架构中，以生成相关物品的候选集，随后使用基础序列模型进行重排序，以强调短期会话意图。与仅依赖序列建模或基于文本的用户表示的现有方法不同，我们的方法将用户画像建模建立在源自知识图谱的结构化关系信号之上。在 Amazon Books 和 Amazon Movies & TV 上的实验表明，与使用会话历史衍生用户嵌入的序列模型相比，我们的方法在性能上有一致的提升。

## Abstract
Session-based recommendation systems (SBRS) aim to capture user's short-term intent from interaction sequences. However, the common assumption of anonymous sessions limits personalization, particularly under sparse or cold-start conditions. Recent advances in LLM-augmented recommendation have shown that LLMs can generate rich item representations, but modeling user personas with LLMs remains challenging due to anonymous sessions. In this work, we propose a persona-driven SBRS framework that explicitly models latent user personas inferred from a heterogeneous knowledge graph (KG) and integrates them into a data-driven recommendation pipeline.Our framework adopts a two-stage architecture consisting of personalized information extraction and personalized information utilization, inspired by recent chain-of-thought recommendation approaches. In the personalized information extraction stage, we construct a heterogeneous KG that integrates time-independent user-item, item-item, item-feature association, and metadata from DBpedia. We then learn latent user personas in an unsupervised manner using a Heterogeneous Deep Graph Infomax (HDGI) objective over a KG initialized with LLM-derived item embeddings. In the personalized information utilization stage, the learned persona representations together with LLM-derived item embeddings are incorporated into a modified architecture of data-driven SBRS to generate a candidate set of relevant items, followed by reranking using the base sequential model to emphasize short-term session intent. Unlike prior approaches that rely solely on sequence modeling or text-based user representations, our method grounds user persona modeling in structured relational signals derived from a KG. Experiments on Amazon Books and Amazon Movies & TV demonstrate that our approach consistently improves over sequential models with user embeddings derived using session history.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合大语言模型（LLM）和异构知识图谱（HKG）的画像驱动型会话式推荐框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：传统的会话式推荐系统（SBRS）通常假设会话是匿名的，侧重于捕捉用户的短期意图，但忽略了跨会话的长期稳定偏好（即用户画像）。
*   **研究背景**：
    *   **数据稀疏与冷启动**：在匿名环境下，仅靠简短的交互序列难以实现精准个性化。
    *   **LLM 的局限性**：虽然 LLM 擅长提取物品语义，但在建模用户画像时成本高昂且在缺乏历史数据时效果不佳。
    *   **知识图谱的优势**：异构知识图谱能提供结构化的关联信号（如导演、类别、品牌等），有助于推断潜在的用户偏好。

### 2. 方法论
该框架采用受“思维链（CoT）”启发的两阶段架构：

*   **阶段一：个性化信息提取（Personalized Information Extraction）**
    *   **异构知识图谱构建**：整合用户-物品交互、物品-物品关系（如“常一起购买”）、物品-特征关联以及来自 DBpedia 的外部元数据。
    *   **LLM 初始化**：利用 LLM（如 Qwen-3）对物品标题、描述和属性进行编码，生成初始语义嵌入。
    *   **无监督画像学习（HDGI）**：采用**异构深度图信息极大化（Heterogeneous Deep Graph Infomax）**算法。通过最大化局部节点表示与全局图摘要之间的互信息，在不依赖标签的情况下学习用户节点的潜在画像表示。
*   **阶段二：个性化信息利用（Personalized Information Utilization）**
    *   **知识增强检索**：将学习到的用户画像嵌入与 LLM 衍生的物品嵌入注入到数据驱动的推荐模型中，生成候选集。
    *   **重排序（Reranking）**：使用基础序列模型（如 SASRec）对候选集进行重排序，以重新强调当前会话的短期意图。

### 3. 实验设计
*   **数据集**：使用了 Amazon 平台的两个公开数据集：**Amazon Books** 和 **Amazon Movies & TV**。
*   **基准方法（Baselines）**：
    *   **None**：不使用用户嵌入。
    *   **Random**：随机初始化用户嵌入并在训练中学习。
    *   **LLM**：直接利用 LLM 根据交互历史生成的文本摘要作为用户画像。
    *   **KG-LLM（本文方法）**：基于知识图谱和 LLM 初始化的画像表示。
*   **评估指标**：Hit Rate (HR@K), NDCG@K, MRR。
*   **基础模型**：以 SASRec 作为序列建模的骨干网络。

### 4. 资源与算力
*   **模型使用**：论文明确提到使用了 **Qwen-3-8B** 进行文本生成和描述提取，使用 **Qwen3-Embedding-8B** 生成文本嵌入。
*   **算力细节**：**文中未明确说明**具体的 GPU 型号、数量或具体的训练时长。仅提供了超参数设置（如隐藏层维度 64，Batch Size 128-1024 等）。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同领域的代表性数据集上进行了测试。
*   **对比维度**：对比了四种不同的用户表示策略，并验证了重排序阶段的效果。
*   **充分性评价**：实验设计较为合理，涵盖了从“无画像”到“纯文本画像”再到“图结构画像”的对比。然而，消融实验部分相对简略，若能增加对不同 KG 关系类型贡献度的分析会更具说服力。

### 6. 主要结论与发现
*   **性能提升**：引入基于 KG 的用户画像显著提升了推荐性能，尤其是在 HR@100 指标上表现突出，说明该方法能有效提高候选集的召回质量。
*   **长期偏好的价值**：实验证明，即使在匿名会话假设下，通过异构关系推断出的长期偏好（画像）也能弥补短期序列信号的不足。
*   **重排序的必要性**：在检索后使用序列模型进行重排序，能有效平衡长期偏好与短期意图，进一步提升 NDCG 和 MRR。

### 7. 优点与亮点
*   **结构化与语义结合**：成功将 LLM 的语义理解能力与知识图谱的结构化推理能力结合，解决了 LLM 建模用户画像时的“幻觉”和高成本问题。
*   **无监督学习**：利用 HDGI 进行无监督画像提取，使其能够适应缺乏显式用户标签的匿名会话场景。
*   **两阶段解耦**：提取与利用阶段的分离使得框架具有较好的灵活性，可以适配不同的基础推荐模型。

### 8. 不足与局限
*   **外部知识依赖**：该方法高度依赖于高质量的外部知识图谱（如 DBpedia），在缺乏相关元数据的冷门领域可能难以应用。
*   **实时性挑战**：虽然通过离线学习画像缓解了压力，但 LLM 的调用和大规模异构图的推理在超大规模实时系统中仍可能存在延迟瓶颈。
*   **实验覆盖面**：仅在 Amazon 的两个类目下验证，对于行为模式差异巨大的其他场景（如短视频、新闻推荐）的泛化能力有待验证。

（完）
