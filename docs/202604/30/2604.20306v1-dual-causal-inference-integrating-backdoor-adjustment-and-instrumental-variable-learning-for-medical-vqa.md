---
title: "Dual Causal Inference: Integrating Backdoor Adjustment and Instrumental Variable Learning for Medical VQA"
title_zh: 双重因果推理：集成后门调整与工具变量学习的医疗视觉问答
authors: "Zibo Xu, Qiang Li, Ke Lu, Jin Wang, Weizhi Nie, Yuting Su"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20306v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 处理多模态数据中的跨模态混杂效应和固有偏差
tldr: 本研究针对医疗视觉问答（MedVQA）中模型易受多模态偏见和混杂因素影响的问题，提出了双重因果推理（DCI）框架。该框架首次结合了后门调整（BDA）和工具变量（IV）学习，分别处理可观测和不可观测的混杂因素。通过互信息约束确保IV的有效性，DCI能提取去混杂的表示，显著提升了模型在多个基准数据集上的鲁棒性、可解释性及分布外泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20306v1/fig-001.webp\", \"caption\": \"TABLE I COMPARISON OF ACCURACY WITH SOTA METHODS ON THREE MEDVQA DATASETS. QUESTIONS ARE CATEGORIZED INTO OPEN AND CLOSED TYPES. “OVERALL” REFERS TO THE ACCURACY ACROSS THE ENTIRE DATASET. THE SECOND-BEST RESULTS ARE UNDERLINED.\", \"page\": 7, \"index\": 1, \"width\": 854, \"height\": 358}, {\"url\": \"assets/figures/arxiv/2604.20306v1/fig-002.webp\", \"caption\": \"Fig. 4. Hyperparameter sensitivity of the dictionary size K.\", \"page\": 8, \"index\": 2, \"width\": 507, \"height\": 294}]"
motivation: 现有的MedVQA方法往往过度拟合表面的跨模态相关性，忽略了医疗数据中固有的偏见和混杂效应。
method: 提出DCI框架，利用后门调整减轻可观测偏见，并结合互信息约束的工具变量学习来补偿不可观测的混杂因素。
result: 在SLAKE、VQA-RAD等四个基准数据集上的实验表明，该方法在性能上优于现有技术，特别是在分布外泛化方面表现出色。
conclusion: DCI通过解耦真实的因果效应与虚假的跨模态捷径，显著增强了医疗视觉问答推理的可靠性和可解释性。
---

## 摘要
医疗视觉问答（MedVQA）旨在根据复杂的医学图像和问题生成临床可靠的答案。然而，现有方法往往过拟合于表层的跨模态相关性，忽略了多模态医学数据中嵌入的内在偏差。因此，模型容易受到跨模态混杂效应的影响，严重阻碍了其提供可靠诊断推理的能力。为了解决这一局限性，我们为 MedVQA 提出了一种新颖的双重因果推理（DCI）框架。据我们所知，DCI 是首个集成后门调整（BDA）和工具变量（IV）学习的统一架构，用于共同处理可观测和不可观测的混杂因素。具体而言，我们构建了一个结构因果模型（SCM），其中可观测的跨模态偏差（例如频繁的视觉和文本共现）通过 BDA 得到缓解，而不可观测的混杂因素则利用从共享潜空间学习到的 IV 进行补偿。为了保证 IV 的有效性，我们设计了互信息约束，在最大化其对融合多模态表示的依赖性的同时，最小化其与不可观测混杂因素及目标答案的关联。通过这种双重机制，DCI 提取了捕捉真实因果关系的去混杂表示。在 SLAKE、SLAKE-CP、VQA-RAD 和 PathVQA 四个基准数据集上的广泛实验表明，我们的方法始终优于现有方法，特别是在分布外（OOD）泛化方面。此外，定性分析证实，DCI 通过显式地将真实因果效应与伪跨模态捷径解耦，显著增强了跨模态推理的可解释性和鲁棒性。

## Abstract
Medical Visual Question Answering (MedVQA) aims to generate clinically reliable answers conditioned on complex medical images and questions. However, existing methods often overfit to superficial cross-modal correlations, neglecting the intrinsic biases embedded in multimodal medical data. Consequently, models become vulnerable to cross-modal confounding effects, severely hindering their ability to provide trustworthy diagnostic reasoning. To address this limitation, we propose a novel Dual Causal Inference (DCI) framework for MedVQA. To the best of our knowledge, DCI is the first unified architecture that integrates Backdoor Adjustment (BDA) and Instrumental Variable (IV) learning to jointly tackle both observable and unobserved confounders. Specifically, we formulate a Structural Causal Model (SCM) where observable cross-modal biases (e.g., frequent visual and textual co-occurrences) are mitigated via BDA, while unobserved confounders are compensated using an IV learned from a shared latent space. To guarantee the validity of the IV, we design mutual information constraints that maximize its dependence on the fused multimodal representations while minimizing its associations with the unobserved confounders and target answers. Through this dual mechanism, DCI extracts deconfounded representations that capture genuine causal relationships. Extensive experiments on four benchmark datasets, SLAKE, SLAKE-CP, VQA-RAD, and PathVQA, demonstrate that our method consistently outperforms existing approaches, particularly in out-of-distribution (OOD) generalization. Furthermore, qualitative analyses confirm that DCI significantly enhances the interpretability and robustness of cross-modal reasoning by explicitly disentangling true causal effects from spurious cross-modal shortcuts.

---

## 论文详细总结（自动生成）

以下是对论文《Dual Causal Inference: Integrating Backdoor Adjustment and Instrumental Variable Learning for Medical VQA》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：医疗视觉问答（MedVQA）要求模型根据医学图像和临床问题给出可靠答案。然而，医疗数据中存在严重的偏见（如疾病分布不均、成像设备差异、医生报告风格不同），导致模型倾向于学习“伪相关性”（如仅根据问题中的特定词汇猜测答案），而非真正的因果逻辑。
*   **核心问题**：现有的因果推理方法通常只能处理“可观测”的偏见，而忽略了“不可观测”的混杂因素（如潜在的患者特征或隐藏的数据集对齐间隙）。
*   **研究动机**：提出一种能够同时应对可观测和不可观测混杂因素的统一框架，以提升模型的鲁棒性、可解释性和分布外（OOD）泛化能力。

### 2. 方法论
论文提出了**双重因果推理（DCI）**框架，核心由两个互补模块组成：
*   **后门调整（BDA）模块**：
    *   **目标**：消除可观测的跨模态偏见。
    *   **技术细节**：构建了视觉和文本混杂因素字典。通过“do-算子”进行干预，将偏向性的条件概率替换为边缘概率分布。利用归一化加权几何平均（NWGM）技术，在特征层面近似实现因果干预，从而打破图像/问题与答案之间的伪路径。
*   **工具变量（IV）学习模块**：
    *   **目标**：中和不可观测的混杂因素。
    *   **技术细节**：从观测数据空间合成潜在工具变量 $I$。为了确保 $I$ 的有效性，设计了**互信息（MI）约束**：
        1.  **相关性**：最大化 $I$ 与融合特征 $X$ 的互信息。
        2.  **无混杂性**：通过 CLUB 算法最小化 $I$ 与不可观测混杂因素 $C$ 的互信息，确保两者正交。
        3.  **排他性约束**：确保 $I$ 仅通过 $X$ 影响答案 $A$，而不能直接指向 $A$。
*   **整体流程**：模型首先通过 BDA 净化可观测偏见，随后通过 IV 进一步剔除潜在干扰，最终生成去混杂的表示用于答案解码。

### 3. 实验设计
*   **数据集**：
    *   **VQA-RAD**：放射学图像数据集。
    *   **SLAKE**：双语、多器官数据集。
    *   **SLAKE-CP**：专门设计的偏见敏感版本，用于评估 OOD 性能。
    *   **PathVQA**：大规模病理图像数据集。
*   **Benchmark 与对比方法**：对比了包括 MEVF-BAN、PubMedCLIP、PMC-CLIP、M3AE、MUMC 以及最新的因果方法 CIMB-MVQA 和 DeCoCT 在内的十余种 SOTA 方法。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 张 NVIDIA RTX 4090 GPU**。
*   **训练细节**：采用 AdamW 优化器，Batch size 为 32，学习率为 1e-4，并在前 5% 的步骤中进行线性预热。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个主流数据集上进行了性能对比。
    *   进行了详尽的**消融实验**，分别验证了 BDA、IV 模块以及互信息损失项（LIX, LIC, LIA）的有效性。
    *   进行了**超参数敏感性分析**（如字典大小 $K$ 的影响）。
    *   提供了**定性可视化分析**（Grad-CAM 映射），展示了模型在“一图多问”场景下的注意力转移。
*   **评价**：实验设计非常充分且具有针对性，特别是 SLAKE-CP 的引入增强了结果的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：DCI 在所有基准测试中均达到 SOTA，在 VQA-RAD 和 SLAKE 上分别比次优方法高出 3.1% 和 3.5%。
*   **OOD 鲁棒性**：在 SLAKE-CP 数据集上，DCI 的表现显著优于传统模型和单一因果方法，证明其捕捉的是不变的因果关系而非统计捷径。
*   **减少幻觉**：定性分析显示，DCI 能有效抑制“查询诱导的幻觉”，使模型注意力更精准地集中在病灶区域。

### 7. 优点
*   **理论严密**：首次将后门调整与工具变量学习结合，构建了完整的双重因果防御体系。
*   **动态演进**：视觉字典采用动量更新机制，能够随训练过程捕捉复杂的跨模态交互。
*   **可解释性强**：通过因果解耦，使模型的推理过程更符合医学逻辑。

### 8. 不足与局限
*   **计算开销**：引入互信息估计（如 CLUB 算法）和双重分支结构会增加训练阶段的计算复杂度和内存占用。
*   **字典依赖**：混杂因素字典的大小 $K$ 需要手动调优，且字典的初始化质量可能影响最终效果。
*   **生成式扩展**：目前主要针对判别式（分类式）VQA 任务，在超大规模生成式医疗模型（如基于 LLaMA 的 Med-LLM）上的适配性尚待验证。

（完）
