---
title: "Springdrift: An Auditable Persistent Runtime for LLM Agents with Case-Based Memory, Normative Safety, and Ambient Self-Perception"
title_zh: Springdrift：一个具备基于案例的记忆、规范性安全和环境自感知的可审计 LLM 智能体持久化运行时
authors: Seamus Brady
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04660v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 具有基于案例的记忆和混合检索的大语言模型智能体持久运行时
tldr: Springdrift是一个为长生命周期LLM智能体设计的持久化运行时系统。它通过集成可审计的执行基座、基于案例推理的混合检索内存、确定性规范安全门控以及环境自感知机制，解决了传统系统在跨会话连续性和决策可追溯性方面的不足。该研究提出了“人工雇佣”概念，强调智能体在持续服务中的自主性与问责制，并通过23天的实际部署验证了其自我诊断和跨渠道上下文维护的能力。
selection_source: fresh_fetch
motivation: 现有的LLM系统受限于会话边界，缺乏长期任务连续性、决策可审计性和在复杂环境下的自我诊断能力。
method: 采用基于Gleam/Erlang开发的架构，结合追加式内存、规范演算安全机制及无需工具调用的结构化自状态注入。
result: 在为期23天的部署中，智能体成功实现了跨渠道上下文维护，并能自主诊断基础设施漏洞及分类故障模式。
conclusion: Springdrift证明了通过结合持久化内存与可审计架构，可以构建出具备高度自主性和法证级问责能力的“人工雇佣”型智能体。
---

## 摘要
我们介绍了 Springdrift，这是一个用于长寿命 LLM 智能体的持久化运行时。该系统集成了可审计的执行基座（仅追加内存、受监督进程、基于 Git 的恢复）、具有混合检索功能的基于案例推理（CBR）记忆层（针对稠密余弦基准进行了评估）、用于安全门控且具有可审计公理轨迹的确定性规范演算，以及通过每个周期注入的结构化自我状态表示（感觉中枢，sensorium）实现的持续环境自感知（无需工具调用）。这些特性支持了在受限于会话的系统中难以实现的行为：跨会话任务连续性、跨渠道上下文维护、决策的端到端取证重建以及自诊断行为。我们报告了一个为期 23 天（19 个工作日）的单实例部署，在此期间，该智能体在没有明确指令的情况下，诊断了自身的底层架构漏洞，分类了故障模式，识别了架构漏洞，并维护了跨邮件和网页渠道的上下文。我们为这一类别引入了“人工受托者”（Artificial Retainer）一词：这是一种具有持久记忆、明确授权、特定领域自主权以及在与特定委托人的持续关系中具有取证问责制的非人类系统——它区别于软件助手和自主智能体，借鉴了专业受托关系和受训工作动物的有限自主权。这是一份关于系统设计和部署案例研究的技术报告，而非基准驱动的评估。证据来自具有单一操作员的单个实例，作为这些架构特性在实践中能够支持什么的说明。该系统使用运行在 Erlang/OTP 上的 Gleam 语言实现。代码、构件和脱敏的操作日志将在发布时于 https://github.com/seamus-brady/springdrift 提供。

## Abstract
We present Springdrift, a persistent runtime for long-lived LLM agents. The system integrates an auditable execution substrate (append-only memory, supervised processes, git-backed recovery), a case-based reasoning memory layer with hybrid retrieval (evaluated against a dense cosine baseline), a deterministic normative calculus for safety gating with auditable axiom trails, and continuous ambient self-perception via a structured self-state representation (the sensorium) injected each cycle without tool calls. These properties support behaviours difficult to achieve in session-bounded systems: cross-session task continuity, cross-channel context maintenance, end-to-end forensic reconstruction of decisions, and self-diagnostic behaviour. We report on a single-instance deployment over 23 days (19 operating days), during which the agent diagnosed its own infrastructure bugs, classified failure modes, identified an architectural vulnerability, and maintained context across email and web channels -- without explicit instruction. We introduce the term Artificial Retainer for this category: a non-human system with persistent memory, defined authority, domain-specific autonomy, and forensic accountability in an ongoing relationship with a specific principal -- distinguished from software assistants and autonomous agents, drawing on professional retainer relationships and the bounded autonomy of trained working animals. This is a technical report on a systems design and deployment case study, not a benchmark-driven evaluation. Evidence is from a single instance with a single operator, presented as illustration of what these architectural properties can support in practice. Implemented in approximately Gleam on Erlang/OTP. Code, artefacts, and redacted operational logs will be available at https://github.com/seamus-brady/springdrift upon publication.

---

## 论文详细总结（自动生成）

### Springdrift：可审计持久化 LLM 智能体运行时深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
当前的 LLM 智能体大多受限于“会话边界”（Session-bounded），即交互结束后上下文会被丢弃或压缩，导致缺乏长期任务的连续性、行为的可预测性以及决策的可追溯性。
*   **研究动机**：对于需要运行数周或数月的长寿命智能体，操作员不仅需要其完成任务，更需要其行为可审计、失败可见且决策可取证重建。
*   **核心理念**：论文提出智能体不应是无状态的工具，而应是具有稳定规范承诺、自我观察能力和全量审计日志的持久化运行系统。为此，作者引入了**“人工受托者”（Artificial Retainer）**的概念，将其类比为专业律师或受训工作动物（如导盲犬），强调其在特定领域内的自主权与问责制。

#### 2. 方法论：核心思想与关键技术
Springdrift 是一个基于 **Gleam/Erlang OTP** 构建的运行时环境，其核心架构包含四个集成组件：
*   **可审计执行基座**：
    *   采用**仅追加（Append-only）**的 JSONL 存储，记录所有决策、工具调用和状态更改。
    *   利用 **Git** 进行状态备份，支持法证级的回溯和任意时间点的状态恢复。
    *   基于 Erlang/OTP 的监督树机制，确保进程崩溃时能自动恢复。
*   **环境自感知（Sensorium）**：
    *   每个认知周期都会向上下文注入一个结构化的 XML 块（无需工具调用），包含时钟、系统健康状况（Vitals）、待办任务和子智能体状态。这赋予了智能体“环境觉察”能力。
*   **基于案例推理（CBR）记忆层**：
    *   将经验存储为“问题-方案-结果”案例。
    *   **混合检索算法**：融合了倒排索引（词法）、语义嵌入（向量）、字段评分、时效性、领域匹配和效用评分（基于历史成功率）六个维度的信号。
*   **确定性规范演算（Normative Calculus）**：
    *   受斯多葛学派（Becker）启发，将安全门控分为 $D'$ 定量筛选（基于信号检测理论）和确定性公理化解析。
    *   通过 14 级优先级和 6 条冲突解决公理（如“徒劳公理”、“道德优先公理”），为每个安全决策生成可审计的公理轨迹（Axiom Trails）。

#### 3. 实验设计
论文采用了多维度的评估方式，但强调这主要是一份技术报告而非单纯的基准驱动研究：
*   **CBR 检索评估**：使用包含 800 个合成案例和 200 个查询的数据集。
    *   **对比方法**：稠密余弦相似度（Dense Cosine）基准。
    *   **指标**：P@4（前4名精确度）和 MRR（平均倒数排名）。
*   **规范演算验证**：对 14 级命题空间进行全量组合测试（7,056 对解析），验证其确定性、覆盖率和单调性。
*   **实地部署案例研究**：进行为期 23 天（19 个工作日）的单实例、单操作员部署。
    *   **场景**：日常办公、邮件处理、代码执行和系统自诊断。
    *   **数据量**：记录了 494 条叙事条目、24,035 条周期日志和超过 1000 万个 Token。

#### 4. 资源与算力
*   **模型使用**：使用了 Claude 4.6 (Opus/Haiku) 等模型，并根据查询复杂度进行动态路由。
*   **算力说明**：文中**未明确说明**具体的 GPU 训练算力（如型号或数量），因为该系统主要关注推理侧的运行时架构。
*   **开发规模**：系统由约 62,000 行 Gleam 代码组成，包含约 1,500 个测试用例。

#### 5. 实验数量与充分性
*   **实验规模**：CBR 实验包含 800 个案例，规范演算进行了 7,000 次以上的形式化验证。
*   **充分性评价**：
    *   **形式化验证部分**：非常充分，证明了逻辑框架的严密性。
    *   **部署实验部分**：属于 **$n=1$ 的个案研究**。虽然提供了丰富的定性证据（如智能体发现自身漏洞的案例），但缺乏多用户、多场景的统计学显著性。
    *   **客观性**：作者坦诚实验证据具有偶然性（Anecdotal），旨在展示架构潜力而非证明通用优越性。

#### 6. 主要结论与发现
*   **架构有效性**：持久化运行时支持了跨会话连续性和跨渠道（邮件/网页）的上下文维护。
*   **自诊断能力**：在部署中，智能体自主诊断出了基础设施 Bug（如遥测数据丢失）、分类了子智能体的故障模式，甚至识别出了一个架构级的安全漏洞（控制反转漏洞）。
*   **审计价值**：证明了通过追加式日志和 Git 备份，可以实现对智能体复杂决策链的端到端取证重建。
*   **人工受托者模型**：提出智能体应具备“拒绝权”，即在特定领域内基于规范框架拒绝不合理指令，这是建立长期信任的基础。

#### 7. 优点
*   **高可靠性设计**：利用 Erlang/OTP 的电信级特性处理 LLM 超时和崩溃，极具工程参考价值。
*   **审计深度**：将“可审计性”视为系统的前提而非附加功能，解决了黑盒智能体难以信任的问题。
*   **创新的自感知机制**：Sensorium 机制避免了昂贵的工具调用，使智能体能实时感知自身状态。
*   **形式化安全**：规范演算为 AI 安全提供了一种可解释、非概率的逻辑层。

#### 8. 不足与局限
*   **样本量极小**：核心部署数据仅来自单一实例，结论的普适性有待验证。
*   **合成数据偏差**：CBR 评估使用的是合成数据集，可能在词法结构上偏向于混合检索方法。
*   **存储开销**：JSONL 存储单调增长，长期运行的扩展性和清理机制（Housekeeping）尚未在大规模数据下测试。
*   **主观性**：规范命题（Character Specification）由人工编写，存在主观偏见风险。
*   **缺乏消融实验**：未量化各组件（如 Sensorium 或 CBR）对任务成功率的具体贡献比例。

（完）
