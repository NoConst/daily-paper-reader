---
title: "VISOR: Agentic Visual Retrieval-Augmented Generation via Iterative Search and Over-horizon Reasoning"
title_zh: VISOR：通过迭代搜索与跨视界推理实现的智能体视觉检索增强生成
authors: "Yucheng Shen, Jiulong Wu, Jizhou Huang, Dawei Yin, Lingyong Yan, Min Cao"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09508v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 智能体视觉检索增强生成与迭代搜索
tldr: 针对视觉检索增强生成（VRAG）中多步推理的挑战，本文提出VISOR框架。它解决了视觉证据稀疏和长程搜索漂移两大瓶颈。通过构建结构化证据空间、引入视觉动作评估纠正机制，以及采用滑动窗口动态轨迹和意图注入，VISOR实现了高效的跨页推理。利用基于GRPO的强化学习优化，该系统在多个长程视觉推理基准测试中达到了SOTA水平。
selection_source: fresh_fetch
motivation: 现有的智能体VRAG系统在处理复杂查询时面临视觉证据分散导致的推理困难，以及长程搜索中视觉Token堆积引发的搜索漂移问题。
method: 提出VISOR框架，通过结构化证据空间、视觉动作纠正机制及带有意图注入的滑动窗口动态轨迹，结合GRPO强化学习进行优化。
result: 在ViDoSeek、SlideVQA和MMLongBench等多个视觉问答和长文本基准测试中，VISOR均取得了最先进的性能和更高的推理效率。
conclusion: VISOR通过创新的证据管理和上下文重构机制，显著提升了视觉语言模型在复杂、长程视觉文档检索与推理任务中的表现。
---

## 摘要
视觉检索增强生成 (VRAG) 赋予视觉语言模型在视觉丰富文档上进行检索和推理的能力。为了处理需要多步推理的复杂查询，智能体 VRAG 系统将推理与迭代检索交织在一起。然而，现有的智能体 VRAG 面临两个关键瓶颈：(1) 视觉证据稀疏性：关键证据散布在不同页面中却被孤立处理，阻碍了跨页推理；此外，细粒度的图像内证据通常需要精确的视觉动作，而这些动作的误用会降低检索质量；(2) 长程搜索漂移：检索页面中视觉标记 (visual tokens) 的累积稀释了上下文并导致认知过载，使智能体偏离其搜索目标。为了应对这些挑战，我们提出了 VISOR（通过迭代搜索与跨视界推理实现的视觉检索增强生成），这是一个统一的单智能体框架。VISOR 具有一个用于渐进式跨页推理的结构化证据空间 (Evidence Space)，并结合了视觉动作评估与修正机制来管理视觉动作。此外，我们引入了带有滑动窗口和意图注入的动态轨迹 (Dynamic Trajectory)，以缓解搜索漂移。它们在锚定证据空间的同时丢弃早期的原始交互，防止上下文被视觉标记淹没。我们使用基于群体相对策略优化 (GRPO) 的强化学习流水线训练 VISOR，并针对动态上下文重构定制了状态掩码和信用分配。在 ViDoSeek、SlideVQA 和 MMLongBench 上的广泛实验表明，VISOR 在长程视觉推理任务中实现了最先进的性能和卓越的效率。

## Abstract
Visual Retrieval-Augmented Generation (VRAG) empowers Vision-Language Models to retrieve and reason over visually rich documents. To tackle complex queries requiring multi-step reasoning, agentic VRAG systems interleave reasoning with iterative retrieval.. However, existing agentic VRAG faces two critical bottlenecks. (1) Visual Evidence Sparsity: key evidence is scattered across pages yet processed in isolation, hindering cross-page reasoning; moreover, fine-grained intra-image evidence often requires precise visual actions, whose misuse degrades retrieval quality; (2) Search Drift in Long Horizons: the accumulation of visual tokens across retrieved pages dilutes context and causes cognitive overload, leading agents to deviate from their search objective. To address these challenges, we propose VISOR (Visual Retrieval-Augmented Generation via Iterative Search and Over-horizon Reasoning), a unified single-agent framework. VISOR features a structured Evidence Space for progressive cross-page reasoning, coupled with a Visual Action Evaluation and Correction mechanism to manage visual actions. Additionally, we introduce a Dynamic Trajectory with Sliding Window and Intent Injection to mitigate search drift. They anchor the evidence space while discarding earlier raw interactions, preventing context from being overwhelmed by visual tokens. We train VISOR using a Group Relative Policy Optimization-based Reinforcement Learning (GRPO-based RL) pipeline with state masking and credit assignment tailored for dynamic context reconstruction. Extensive experiments on ViDoSeek, SlideVQA, and MMLongBench demonstrate that VISOR achieves state-of-the-art performance with superior efficiency for long-horizon visual reasoning tasks.

---

## 论文详细总结（自动生成）

### VISOR：通过迭代搜索与跨视界推理实现的智能体视觉检索增强生成总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对**视觉检索增强生成（VRAG）**在处理复杂、多步推理查询时的两个核心瓶颈：
*   **视觉证据稀疏性（Visual Evidence Sparsity）：** 关键证据往往散布在多个页面中，或隐藏在单张图像的细微区域。现有系统通常孤立处理页面，缺乏跨页证据积累机制，且盲目的局部放大（Crop-and-zoom）操作会引入噪声。
*   **长程搜索漂移（Search Drift in Long Horizons）：** 视觉Token数量庞大，多轮交互后上下文窗口迅速饱和，导致早期证据被淹没，智能体逐渐偏离原始查询意图。

**VISOR** 旨在通过一个统一的单智能体框架，在有限的上下文预算内实现高效的跨页面证据整合与精确的视觉动作管理。

#### 2. 方法论：核心思想与关键技术
VISOR 采用迭代式智能体循环，核心组件包括：
*   **结构化证据空间（Evidence Space）：** 智能体在每轮推理中提取页面摘要（pre-crop 和 post-crop），并将其存储在持久化的证据空间中。这使得模型能进行跨页联合推理，而非仅依赖当前看到的图像。
*   **视觉动作评估与纠正机制：** 在执行放大动作前先评估必要性，减少冗余操作；若放大后发现信息无效，则通过提示词引导模型回归之前的推理状态，防止噪声污染。
*   **动态轨迹与滑动窗口（Dynamic Trajectory with Sliding Window）：** 仅保留最近 $W$ 轮（实验设定 $W=2$）的原始交互，而将历史证据摘要固定在上下文顶部。这严格限制了视觉Token的增长。
*   **意图注入（Intent Injection）：** 在每轮观察中重新注入原始查询和证据空间摘要，确保智能体在长程交互中始终锚定目标。
*   **训练流水线：** 
    *   **SFT阶段：** 使用 Qwen3-VL-235B 蒸馏高质量轨迹，培养基础智能体行为。
    *   **RL阶段：** 采用 **GRPO（群体相对策略优化）**。奖励函数设计独特，不仅奖励答案正确性，还通过奖励“验证性搜索”来优化检索精度，惩罚过度搜索或检索不全。

#### 3. 实验设计
*   **数据集：** 
    *   **SlideVQA：** 侧重多页幻灯片的多跳问答。
    *   **ViDoSeek：** 包含大量文档图像，分为提取类和逻辑推理类任务。
    *   **MMLongBench：** 针对长文档的视觉理解基准。
*   **对比方法：** 
    *   **传统方法：** Vanilla RAG, ReAct。
    *   **多智能体架构：** ViDoRAG, M3RAG。
    *   **微调基准：** Search-R1-VL, VRAG-RL, MMSearch-R1, EVisRAG, R1-Router。
*   **评测指标：** 使用 Qwen-max-latest 作为评判模型（LLM-as-Judge），给出二元得分（0或1）并计算平均准确率。

#### 4. 资源与算力
*   **硬件：** 使用单节点 **8 张 NVIDIA A800 GPU**。
*   **模型：** 以 Qwen2.5-VL-7B 和 3B 为基础模型。
*   **训练细节：** SFT 阶段 3 个 Epoch；RL 阶段 100 个 Step。具体训练时长未明确说明，但给出了详细的超参数设置（如学习率、Batch Size 等）。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了三个主流 VRAG 基准测试，并在 7B 和 3B 两种参数规模下进行了验证。
*   **消融实验：** 针对意图注入（II）、证据空间（ES）和滑动窗口（SW）进行了详细的分解实验，验证了各组件的协同效应。
*   **深度分析：** 论文还进行了检索行为分析（完整度 vs. 漂移）、推理效率分析（延迟分解）、训练阶段效果分析以及视觉动作（Bbox）使用率分析。
*   **客观性：** 实验对比了多种最新的 SOTA 方法，并验证了 LLM 评判模型与人类标注的一致性，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能领先：** VISOR 在所有基准测试中均显著优于现有单智能体和多智能体 VRAG 系统，尤其在多跳查询（Multi-hop）上提升巨大。
*   **证据空间的重要性：** 消融实验显示，证据空间是提升性能的关键，它解决了 RL 训练本身无法完全解决的结构化信息留存问题。
*   **效率优势：** 尽管增加了验证步骤，但由于滑动窗口限制了上下文长度，VISOR 的推理速度优于多智能体架构，且与现有的迭代式方法持平。
*   **RL 的作用：** RL 显著降低了非法动作率（从 0.61 降至 0.09），并提高了任务完成率。

#### 7. 优点
*   **架构优雅：** 采用单智能体循环解决了通常需要复杂多智能体协作才能处理的长程推理问题。
*   **上下文高效：** 通过动态重构轨迹，巧妙解决了视觉 Token 导致的上下文爆炸问题。
*   **策略稳健：** 引入验证性搜索策略，使模型在给出答案前更具“审慎性”。
*   **通用性强：** 框架不依赖特定模型，在 3B 和 7B 模型上均表现出良好的扩展性。

#### 8. 不足与局限
*   **视觉理解瓶颈：** 案例分析显示，当涉及极其复杂的图表（如需要精确关联图例与饼图颜色）时，模型仍会出错，这受限于底层 VLM 的视觉感知能力。
*   **评判模型依赖：** 训练和评估高度依赖 LLM Judge，虽然验证了一致性，但仍存在潜在的偏见风险。
*   **任务范围：** 目前主要集中在文档问答，对于更广泛的开放域网页搜索或实时多模态交互的适用性尚待验证。

（完）
