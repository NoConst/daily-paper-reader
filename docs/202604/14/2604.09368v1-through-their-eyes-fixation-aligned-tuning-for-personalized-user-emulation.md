---
title: "Through Their Eyes: Fixation-aligned Tuning for Personalized User Emulation"
title_zh: 通过他们的视线：面向个性化用户仿真的注视对齐微调
authors: "Lingfeng Huang, Huizhong Guo, Tianjun Wei, Yingpeng Du, Zhu Sun"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09368v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 使用视觉语言模型进行个性化用户仿真
tldr: 本研究针对现有推荐系统模拟器忽视视觉界面感知的问题，提出了一种名为FixATE的个性化用户仿真方法。研究发现用户的眼动注视模式具有高度个性化且能预测点击行为。FixATE通过可解释性算子提取视觉语言模型（VLM）的内部注意力，并利用个性化软提示引导模型模拟特定用户的注视习惯。实验证明，该方法在注意力对齐和点击预测准确率上均有显著提升，为构建更真实的视觉感知用户模拟器提供了新路径。
selection_source: fresh_fetch
motivation: 现有推荐系统模拟器多依赖文本输入，忽视了真实用户在视觉界面上的个性化注意力分布对决策的影响。
method: 提出FixATE框架，通过可解释性算子探测VLM内部注意力，并学习个性化软提示来引导模型模拟特定用户的注视模式。
result: 在不同VLM架构上，该方法显著提升了模型注意力与人类注视的对齐度，并提高了点击预测的准确性。
conclusion: 将视觉语言模型的注意力与用户个性化注视模式对齐，是构建更真实、更具感知力的用户模拟器的有效途径。
---

## 摘要
大语言模型（LLM）智能体正越来越多地被部署为可扩展的用户模拟器，用于推荐系统的评估。然而，现有的模拟器主要通过文本或结构化元数据来感知推荐内容，而非真实用户浏览的视觉界面——这是一个关键差距，因为对推荐布局的注意力既是视觉驱动的，又是高度个性化的。我们研究了将视觉语言模型（VLM）的视觉注意力与特定用户的注视模式（gaze patterns）相对齐是否能提高模拟的保真度。对在轮播式推荐场景下收集的真实世界眼动追踪数据集的分析表明，用户表现出稳定的个体注视模式，且这些模式对点击行为具有很强的预测性。基于这一发现，我们提出了面向用户仿真的注视对齐微调（Fixation-Aligned Tuning for user Emulation，简称 FixATE）。该方法首先通过可解释性算子探测 VLM 的内部视觉注意力，以获得与人类注视相当的槽位级（slot-level）相关性分布，随后学习个性化软提示（soft prompts），以引导模型的注意力转向每个用户的特征注视模式。在三种基于可解释性的探测算子和两种架构不同的 VLM 主干网络上进行的实验表明，该方法在注意力对齐和点击预测准确率方面均有一致的提升。这些结果表明，让模型“像用户一样看”是开发能够更忠实地再现用户在推荐界面中感知和行动方式的模拟器的可行路径。

## Abstract
Large language model (LLM) agents are increasingly deployed as scalable user simulators for recommender system evaluation. Yet existing simulators perceive recommendations through text or structured metadata rather than the visual interfaces real users browse-a critical gap, since attention over recommendation layouts is both visually driven and highly personalized. We investigate whether aligning a vision-language model's (VLM's) visual attention with user-specific gaze patterns can improve simulation fidelity. Analysis of a real-world eye-tracking dataset collected in a carousel-based recommendation setting reveals that users exhibit stable individual gaze patterns strongly predictive of click behavior. Building on this finding, we propose Fixation-Aligned Tuning for user Emulation (FixATE). Our approach first probes the VLM's internal visual attention via interpretability operators to obtain a slot-level relevance distribution comparable with human fixation, and then learns personalized soft prompts to steer the model's attention toward each user's characteristic fixation pattern. Experiments across three interpretability-based probing operators and two architecturally distinct VLM backbones demonstrate consistent improvements in both attention alignment and click prediction accuracy. These results suggest that making the model "see like the user" is a viable path toward simulators that more faithfully reproduce how users perceive and act in recommendation interfaces.

---

## 论文详细总结（自动生成）

这是一份关于论文《Through Their Eyes: Fixation-aligned Tuning for Personalized User Emulation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的基于大语言模型（LLM）的用户模拟器在感知推荐系统时存在“感知不对称”。它们通常只处理文本描述或结构化元数据，而忽略了真实用户交互的**视觉界面**（如海报布局、轮播图）。
*   **研究动机**：心理学和眼动实验证明，用户的注意力分配受视觉布局驱动且具有高度的**个体差异性**（个性化）。论文旨在研究：如果让视觉语言模型（VLM）的内部注意力与特定用户的眼动注视（Fixation）模式对齐，是否能提升模拟器还原用户真实点击行为的保真度。

### 2. 方法论：FixATE 框架
FixATE（Fixation-Aligned Tuning for user Emulation）的核心思想是通过微调个性化软提示，引导 VLM 的视觉注意力分布模拟特定用户的注视习惯。
*   **视觉注意力探测**：利用 VLM 的可解释性算子（如 AttnLRP、GLIMPSE）提取模型内部的 Patch 级相关性分数，并根据界面布局将其聚合为“槽位级”（Slot-level）分布，从而与人类的眼动数据（每个推荐位的停留时间）进行对比。
*   **个性化软提示基分解**：
    *   **Prompt Basis**：学习一组共享的软提示基向量，捕捉通用的视觉关注模式（如左上角偏好）。
    *   **个性化系数**：为每个用户学习特定的组合权重，通过对基向量的加权组合生成该用户的专属软提示。
*   **对齐目标函数**：
    *   **注意力对齐损失 ($L_{Attn}$)**：使用加权的 KL 散度，强制模型的槽位注意力分布趋近于用户的真实注视分布。
    *   **行为预测损失 ($L_{NTP}$)**：标准的下文预测损失，确保模型在对齐注意力的同时，能准确预测用户的点击动作。
*   **流程**：输入界面图像 + 用户画像 + 指令 $\rightarrow$ 拼接个性化软提示 $\rightarrow$ 冻结 VLM 主干，仅优化提示参数 $\rightarrow$ 输出预测行为。

### 3. 实验设计
*   **数据集与场景**：
    *   **RecGaze**：核心数据集，包含 83 名用户在 3×5 电影推荐轮播界面上的眼动轨迹和点击记录。
    *   **AdSERP**：泛化性测试数据集，涉及搜索结果页（SERP）的垂直布局。
*   **Benchmark（基准）**：
    *   **Backbone**：未经过注视对齐的原始 VLM（Qwen3-VL-4B 和 InternVL3.5-4B）。
    *   **消融变体**：包括随机软提示、无个性化系数（全员统一提示）、无权重损失函数等。
*   **对比维度**：
    1.  **注意力对齐度**：Cosine 相似度、JS 散度、KL 散度、点击槽位命中率（CSH@k）。
    2.  **行为预测准确性**：Accuracy、LogLoss、AUC。

### 4. 资源与算力
*   **算力说明**：论文提到使用了 `bfloat16` 精度进行训练。
*   **训练细节**：Batch size 为 4，梯度累积步数为 2（等效 Batch size 8），训练轮数为 30 Epoch。
*   **缺失信息**：文中**未明确提及**具体的 GPU 型号（如 A100 或 H100）、GPU 数量以及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 种不同架构的 VLM 主干，3 种主流的可解释性探测算子，以及 2 个不同布局的界面数据集。
*   **充分性**：
    *   **消融实验**：非常详尽，验证了每个损失项和个性化组件的必要性。
    *   **超参数分析**：对提示长度、基向量数量、损失权重 $\lambda$ 进行了敏感性测试。
    *   **统计显著性**：所有核心实验均运行 5 次并取均值，进行了 t 检验（$p < 0.01$），证明了结果的稳健性。
*   **客观性**：通过在不同模型和探测算子上取得一致性提升，排除了特定模型架构带来的偶然性。

### 6. 主要结论与发现
*   **视觉感知至关重要**：实验证明，直接输入图像比将界面转译为带位置标签的文本更能捕捉人类的视觉偏好（如 F 型模式）。
*   **“看”得像，则“做”得像**：模型内部注意力与人类注视的对齐度越高，其点击预测的准确率（Accuracy）和排序性能（AUC）提升越明显。
*   **个性化是关键**：简单的全局对齐效果有限，通过个性化软提示捕捉个体差异是实现高保真仿真的核心。

### 7. 优点（亮点）
*   **首创性**：首次将个性化眼动对齐引入 VLM 用户模拟器，填补了视觉感知层面的空白。
*   **模型无关性**：FixATE 作为一个插件式框架，不改变 VLM 主干参数，兼容多种探测算子。
*   **可解释性驱动**：巧妙地将模型内部的注意力机制作为监督信号，而非仅仅关注最终的输出结果。

### 8. 不足与局限
*   **数据获取成本**：真实世界的个性化眼动数据（Eye-tracking）极其稀缺且采集成本高，限制了该方法在大规模工业场景下的直接应用。
*   **时间维度缺失**：当前模型主要处理单次会话，未能建模用户注意力随时间演变的动态过程。
*   **代理信号限制**：虽然论文讨论了鼠标轨迹作为替代信号的可能性，但实验仍主要依赖昂贵的眼动仪数据。
*   **极端冲突处理**：当用户的注视点与 VLM 的强内容先验（例如模型本身非常讨厌某类电影）发生剧烈冲突时，仅靠软提示可能难以完全扭转模型的注意力。

（完）
