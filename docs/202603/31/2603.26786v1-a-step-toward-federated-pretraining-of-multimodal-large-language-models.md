---
title: A Step Toward Federated Pretraining of Multimodal Large Language Models
title_zh: 迈向多模态大语言模型联邦预训练的一步
authors: "Baochen Xiong, Yifan Xu, Xiaoshan Yang, Yaguang Song, Yaowei Wang, Changsheng Xu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.26786v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 多模态大模型的联邦预训练
tldr: 本文针对多模态大模型（MLLM）预训练中高质量公开数据饱和及私有数据孤岛问题，提出了联邦多模态对齐（Fed-MA）任务。通过冻结视觉编码器和LLM，仅协同训练跨模态投影器，并引入Fed-CMP框架。该框架利用规范可靠性感知聚合解决参数干扰，并结合正交保持动量缓解梯度振荡。实验证明，Fed-CMP在多个联邦预训练场景中均优于现有基线，为利用分布式私有数据进行MLLM预训练开辟了新路径。
selection_source: fresh_fetch
motivation: 多模态大模型预训练面临公开数据枯竭与私有数据难以共享的矛盾，且联邦学习在预训练阶段的应用仍缺乏探索。
method: 提出Fed-CMP框架，通过规范空间分解实现可靠性感知聚合，并利用正交投影动量优化训练稳定性。
result: 在四个构建的联邦预训练场景中，Fed-CMP的性能显著超越了现有的联邦学习基线方法。
conclusion: 该研究通过轻量化的联邦预训练范式，有效解决了分布式多模态数据协同训练中的干扰与振荡问题。
---

## 摘要
多模态大语言模型（MLLMs）的快速演进正受到高质量公共数据饱和的瓶颈限制，而海量的多样化多模态数据仍被困在对隐私敏感的数据孤岛中，无法获取。联邦学习（FL）为解锁这些分布式资源提供了一个极具前景的解决方案，但现有研究主要集中在微调阶段，而基础的预训练阶段在很大程度上仍未被探索。在本文中，我们正式引入了联邦 MLLM 对齐（Fed-MA）任务，这是一种轻量级的预训练范式，它在冻结视觉编码器和 LLM 的同时，协作训练跨模态投影器。我们在该设置中识别出两个关键挑战：（i）聚合本地投影器时的参数干扰；（ii）单遍协作 SGD 中的梯度振荡。为了应对这些挑战，我们提出了 Fed-CMP，这是一个用于联邦 MLLM 预训练的开创性框架。Fed-CMP 采用了典型可靠性感知聚合（Canonical Reliability-Aware Aggregation），该方法构建了一个典型空间，将客户端投影器分解为共享对齐基和客户端特定系数，然后进行可靠性加权融合以抑制参数干扰。此外，Fed-CMP 引入了正交保持动量（Orthogonality-Preserved Momentum），通过正交投影将动量应用于共享对齐基，在保留几何结构的同时累积历史优化方向。我们基于公共数据集构建了四个联邦预训练场景，广泛的实验验证了 Fed-CMP 显著优于现有的基准方法。

## Abstract
The rapid evolution of Multimodal Large Language Models (MLLMs) is bottlenecked by the saturation of high-quality public data, while vast amounts of diverse multimodal data remain inaccessible in privacy-sensitive silos. Federated Learning (FL) offers a promising solution to unlock these distributed resources, but existing research focuses predominantly on fine-tuning, leaving the foundational pre-training phase largely unexplored. In this paper, we formally introduce the Federated MLLM Alignment (Fed-MA) task, a lightweight pre-training paradigm that freezes the vision encoder and LLM while collaboratively training the cross-modal projector. We identify two critical challenges in this setting: (i) parameter interference in aggregating local projectors; and (ii) gradient oscillations in one-pass collaborative SGD. To address these challenges, we propose Fed-CMP, a pioneering framework for federated MLLM pre-training. Fed-CMP employs Canonical Reliability-Aware Aggregation, which constructs a canonical space to decompose client projectors into a shared alignment basis and client-specific coefficients, then performs reliability-weighted fusion to suppress parameter interference. Furthermore, Fed-CMP introduces Orthogonality-Preserved Momentum, which applies momentum to the shared alignment basis via orthogonal projection, accumulating historical optimization directions while preserving geometric structure. We construct four federated pre-training scenarios based on public datasets, and extensive experiments validate that Fed-CMP significantly outperforms existing baselines.

---

## 论文详细总结（自动生成）

以下是对论文《A Step Toward Federated Pretraining of Multimodal Large Language Models》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：多模态大语言模型（MLLM）的性能高度依赖大规模高质量数据。然而，公开数据正趋于饱和，而海量的多样化数据（如医疗、私人设备数据）因隐私和监管限制被困在“数据孤岛”中。
*   **核心问题**：现有的联邦学习（FL）在 MLLM 上的研究多集中在指令微调阶段，而忽略了基础的**预训练（跨模态对齐）阶段**。
*   **挑战**：
    1.  **参数干扰**：非独立同分布（Non-IID）数据导致不同客户端学习到的对齐方向不一致，直接聚合会导致模型性能崩溃。
    2.  **梯度振荡**：预训练通常采用单遍（One-Pass）数据流模式，缺乏历史记忆，导致全局模型在训练过程中出现剧烈波动和灾难性遗忘。

### 2. 方法论：Fed-CMP 框架
论文提出了 **Fed-CMP** 框架，旨在冻结视觉编码器和 LLM 的情况下，协作训练轻量级的跨模态投影器（Projector）。
*   **规范可靠性感知聚合 (CRA)**：
    *   **核心思想**：利用奇异值分解（SVD）将各客户端的投影器权重分解为**共享对齐基**（Shared Basis）和**客户端特定系数**（Coefficients）。
    *   **技术细节**：在规范空间中进行聚合，避免了直接在参数空间聚合导致的冲突。引入**可靠性权重**，根据更新幅度（信息增益）和跨模态对齐质量（映射有效性）对客户端进行加权，抑制低质量客户端的干扰。
*   **正交保持动量 (OPM)**：
    *   **核心思想**：在共享对齐基上应用动量以稳定训练，同时确保其几何结构不被破坏。
    *   **技术细节**：由于正交矩阵的线性组合不再正交，OPM 采用**极分解（Polar Decomposition）**将更新后的矩阵投影回正交流形。同时引入**自适应动量系数**，根据当前更新与历史方向的余弦不相似度动态调整，缓解单遍训练中的振荡。

### 3. 实验设计
*   **数据集与场景**：基于 CC12M 数据集构建了四个联邦预训练场景，通过不同的聚类策略模拟 Non-IID 特性：
    1.  **MI-I**：基于图像特征聚类（异构性最高）。
    2.  **MT-T**：基于文本特征聚类。
    3.  **Mjoint**：图像与文本联合聚类。
    4.  **Mcross**：跨模态相似度聚类。
*   **Benchmark**：在 7 个主流多模态基准上评估：MM-Vet, MMBench, SEED, LLaVA-Bench, POPE, MME, MMVP。
*   **对比方法**：
    *   **非联邦**：本地独立训练（Local Training）。
    *   **经典 FL**：FedAvg, FedAdam, FedProx, MOON。
    *   **模型合并算法**：Task Arithmetic (TA), TIEs, DARE。

### 4. 资源与算力
*   **硬件**：使用了 2 台机器，每台配备 **8 张 NVIDIA A100 (40GB) GPU**（共 16 张）。
*   **时长**：每个实验大约耗时 **10 小时**。
*   **模型配置**：视觉编码器为 CLIP ViT-L/14，语言模型为 Vicuna-v1.5-7B，投影器为两层 MLP。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种异构场景、7 个评估基准、8 种对比算法，并进行了详细的消融实验。
*   **充分性**：实验设计较为全面，不仅验证了最终性能，还通过可视化（t-SNE）和时间稳定性分析（训练曲线）深入探讨了方法的作用机制。
*   **客观性**：对比了多种最新的模型合并技术，并提供了下游指令微调的初步验证（GQA, OCRVQA），证明了预训练对齐质量对后续任务的增益。

### 6. 主要结论与发现
*   **Fed-CMP 显著优于基线**：在所有异构场景下，Fed-CMP 均表现出最强的泛化能力，尤其是在高度异构的图像聚类（MI-I）场景中优势最明显。
*   **稳定性至关重要**：实验观察到传统 FL 方法在单遍预训练中表现出剧烈的性能波动，而 OPM 模块能有效提供平滑且持续上升的性能曲线。
*   **规范空间聚合的有效性**：通过 SVD 分解，模型能更好地捕捉跨客户端的共性对齐方向，减少了参数间的“破坏性干扰”。

### 7. 优点（亮点）
*   **开创性**：率先探索了 MLLM 的**联邦预训练**任务，填补了该领域的空白。
*   **数学严谨性**：针对正交基的动量更新给出了严谨的理论推导（见附录），解决了流形上的优化问题。
*   **轻量化**：仅训练投影器，极大地降低了联邦学习中的通信开销和客户端计算负担。

### 8. 不足与局限
*   **参数范围限制**：目前仅针对 Projector 进行训练，尚未涉及视觉编码器或 LLM 的全参数或高效微调（如 LoRA）在预训练阶段的联邦表现。
*   **计算开销**：服务器端进行 SVD 分解和极分解会带来额外的计算负担，当客户端数量极多时可能成为瓶颈。
*   **理想化假设**：实验在模拟的联邦环境中进行，未充分考虑现实中的掉线（Stragglers）、异步通信及极端系统异构性问题。

（完）
