---
title: "Evo-MedAgent: Beyond One-Shot Diagnosis with Agents That Remember, Reflect, and Improve"
title_zh: Evo-MedAgent：超越单次诊断，具备记忆、反思与改进能力的智能体
authors: "Weixiang Shen, Bailiang Jian, Jun Li, Che Liu, Johannes Moll, Xiaobin Hu, Daniel Rueckert, Hongwei Bran Li, Jiazhen Pan"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14475v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 为医疗智能体提供自我进化的记忆模块以积累经验
tldr: 针对医疗LLM智能体在胸部X射线诊断中无法积累经验、处理病例相互孤立的问题，本文提出Evo-MedAgent。该系统通过自进化记忆模块（包含临床案例检索、启发式规则反思和工具可靠性控制）实现测试时的跨案例学习。实验表明，该方法在无需重新训练的情况下显著提升了模型在ChestAgentBench上的诊断准确率，证明了持续学习机制在医疗AI中的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14475v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of Evo-MedAgent. A tool-augmented LLM agent processes CXR questions sequentially while reading from and writing to a self-evolving memory via test-time learning. Before solving, the agent retrieves relevant episodic cases, procedural rules, and tool governance guidance. After receiving ground-truth feedback, it reflects and refines all three memory stores.\", \"page\": 2, \"index\": 1, \"width\": 816, \"height\": 531}, {\"url\": \"assets/figures/arxiv/2604.14475v1/fig-002.webp\", \"caption\": \"Table 1: Ablation results on ChestAgentBench (tool-free). S = Procedural memory, E = Episodic memory. Full-memory GPT-5-mini MCQ is reported as mean (range) across three case-order permutations (µ=0.79, σ=0.025). Dashes indicate incomplete runs. Best scores are marked in bold.\", \"page\": 4, \"index\": 2, \"width\": 826, \"height\": 207}, {\"url\": \"assets/figures/arxiv/2604.14475v1/fig-003.webp\", \"caption\": \"Figure 2: Cumulative MCQ accuracy over sequential benchmark streams. Left: GPT-5-mini under tool-free baselines and full memory. Right: Gemini-3-Flash under tool-enabled baseline and full memory.\", \"page\": 5, \"index\": 3, \"width\": 829, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2604.14475v1/fig-004.webp\", \"caption\": \"Figure 3: Evo-MedAgent showcase across two chest X-ray cases. Earlier Failure: the agent incorrectly attributes a left pleural effusion to pneumonia; ground truth is haemothorax from a rib spur. Memory Evolution: episodic reflection identifies the missed bone review and distils two new procedural rules. Memory-guided Success: on a later case, the without-memory agent misdiagnoses right lower lobe opacification as atelectasis, while the memory-augmented agent applies the retrieved rules (meniscus sign, absence of air bronchograms) to correctly identify a malignant pleural-based mass.\", \"page\": 6, \"index\": 4, \"width\": 706, \"height\": 740}]"
motivation: 现有的医疗智能体缺乏跨案例学习能力，无法像人类医生一样从过往经验中总结规律并纠正重复的推理错误。
method: 引入包含回顾性案例、自适应启发式规则和工具可靠性控制器的自进化记忆模块，通过反思机制在推理时动态优化。
result: 在ChestAgentBench上，该方法将GPT-5-mini和Gemini-3 Flash的诊断准确率分别提升了11个百分点。
conclusion: Evo-MedAgent证明了无需训练的记忆进化机制能有效增强冻结模型在复杂医疗任务中的诊断性能和工具调用可靠性。
---

## 摘要
工具增强的大语言模型（LLM）智能体可以协调专门的分类器、分割模型和视觉问答模块来解释胸部 X 射线。然而，这些智能体目前仍是孤立地处理每个病例：它们无法跨病例积累经验，无法纠正反复出现的推理错误，也无法在不进行昂贵的强化学习的情况下调整其工具使用行为。放射科医生会随着处理的每个病例自然地进步，而目前的智能体则保持静态。在这项工作中，我们提出了 Evo-MedAgent，这是一个自进化的记忆模块，使医疗智能体具备在测试时进行跨病例学习的能力。我们的记忆由三个互补的存储库组成：(1) 回溯性临床案例（Retrospective Clinical Episodes），用于检索过去相似病例的解题经验；(2) 自适应程序化启发式（Adaptive Procedural Heuristics）库，整理带有优先级标签的诊断规则，并通过反思不断进化，类似于医生完善其内部标准；(3) 工具可靠性控制器（Tool Reliability Controller），用于跟踪每个工具的可信度。在 ChestAgentBench 上，Evo-MedAgent 将 GPT-5-mini 的多选题（MCQ）准确率从 0.68 提高到 0.79，将 Gemini-3 Flash 的准确率从 0.76 提高到 0.87。在强大的基础模型下，进化记忆在定性诊断任务上比协调外部工具更有效地提升了性能。由于 Evo-MedAgent 不需要训练，其每个病例的开销仅限于一次额外的检索和一次反思调用，因此可以部署在任何冻结模型之上。

## Abstract
Tool-augmented large language model (LLM) agents can orchestrate specialist classifiers, segmentation models, and visual question-answering modules to interpret chest X-rays. However, these agents still solve each case in isolation: they fail to accumulate experience across cases, correct recurrent reasoning mistakes, or adapt their tool-use behavior without expensive reinforcement learning. While a radiologist naturally improves with every case, current agents remain static. In this work, we propose Evo-MedAgent, a self-evolving memory module that equips a medical agent with the capacity for inter-case learning at test time. Our memory comprises three complementary stores: (1)~\emph{Retrospective Clinical Episodes} that retrieve problem-solving experiences from similar past cases, (2)~an \emph{Adaptive Procedural Heuristics} bank curating priority-tagged diagnostic rules that evolves via reflection, much like a physician refining their internal criteria, and (3)~a \emph{Tool Reliability Controller} that tracks per-tool trustworthiness. On ChestAgentBench, Evo-MedAgent raises multiple-choice question (MCQ) accuracy from 0.68 to 0.79 on GPT-5-mini, and from 0.76 to 0.87 on Gemini-3 Flash. With a strong base model, evolving memory improves performance more effectively than orchestrating external tools on qualitative diagnostic tasks. Because Evo-MedAgent requires no training, its per-case overhead is bounded by one additional retrieval pass and a single reflection call, making it deployable on top of any frozen model.

---

## 论文详细总结（自动生成）

这是一份关于论文《Evo-MedAgent: Beyond One-Shot Diagnosis with Agents That Remember, Reflect, and Improve》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的医疗大模型（LLM）智能体在处理胸部X射线（CXR）时，通常采用“单次（One-shot）”推理模式。这意味着它们在处理每个病例时都是孤立的，无法像人类放射科医生那样从过去的成功或失败中积累经验、修正重复的逻辑错误或优化工具调用策略。
*   **研究背景**：虽然工具增强型智能体（如 MedRAX）能调用分类、分割等专家模型，但其知识状态是静态的。一旦模型参数固定，除非进行昂贵的重新训练或微调，否则无法纠正其程序性错误（如工具顺序错误、过度信任噪声输出等）。
*   **整体含义**：本文旨在开发一种**自进化记忆模块**，使医疗智能体能够在测试阶段（Test-time）通过处理序列病例实现自我改进，实现“越用越聪明”的临床辅助效果。

### 2. 论文提出的方法论：Evo-MedAgent
核心思想是引入一个**无需训练、自进化的三层记忆架构**，在推理循环中加入“反思-更新”机制。
*   **核心架构（三个存储库）**：
    1.  **情节记忆 (Episodic Memory, E)**：存储过去病例的压缩记录，包括病例描述、工具调用轨迹、最终答案及反思后的行动指南。推理时通过相似度检索（RAG）为当前病例提供先例参考。
    2.  **程序化记忆 (Procedural Memory, S)**：存储抽象化的诊断规则（启发式规则）。每条规则带有优先级标签（0-2级），系统会根据规则在历史病例中的表现（效用值）动态筛选最有效的规则。
    3.  **工具治理记忆 (Tool-governance Memory, G)**：监控每个外部工具的可靠性，将其标记为“信任（TRUSTED）”、“谨慎（CAUTION）”或“避免（AVOID）”，从而优化智能体对工具输出的权重分配。
*   **算法流程**：
    *   **推理阶段**：检索相关情节 -> 筛选高权重程序规则 -> 结合工具治理建议 -> 生成诊断。
    *   **进化阶段**：获取真实反馈（Ground-truth） -> 多模态反思（分析错误原因） -> 更新情节库、修订/新增程序规则、调整工具信任分。

### 3. 实验设计
*   **数据集/场景**：使用 **ChestAgentBench**，包含 2,500 个多选题（MCQ），涵盖 675 个专家策划的 Eurorad 临床病例。
*   **任务类型**：包括检测、分类、定位、比较、关系识别、特征描述和最终诊断。
*   **对比基准（Benchmarks）**：
    *   **Baseline**：无记忆状态的原始模型（分为“启用工具”和“纯视觉/无工具”两种模式）。
    *   **消融版本**：仅使用程序化记忆 (+S)、仅使用情节记忆 (+E)。
*   **底层模型**：采用了 GPT-5-mini、Gemini-3 Flash 和 GPT-5.2 等前沿多模态大模型作为智能体核心。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **开销分析**：作者强调该方法是**训练无关（Training-free）**的。每个病例的额外开销仅限于：一次向量检索（Retrieval pass）和一次 LLM 反思调用（Reflection call）。这种低开销设计使其能够直接部署在任何冻结（Frozen）的商业或开源模型之上。

### 5. 实验数量与充分性
*   **实验规模**：在 2,500 个测试问题上进行了序列化测试，涵盖了多种主流 VLM 模型。
*   **消融实验**：详细对比了 S 和 E 两个模块的贡献，证明了情节记忆在处理重复病理模式上的优势，以及程序化记忆在处理新组合病理时的泛化能力。
*   **鲁棒性测试**：针对测试时学习（TTL）对病例顺序敏感的问题，作者进行了三次随机排列实验（Random Permutations），结果显示性能提升具有统计学意义（标准差较小，$\sigma=0.025$），证明了实验的客观性。
*   **充分性评价**：实验设计较为充分，不仅有定量准确率对比，还提供了定性的案例分析（Showcase）来展示记忆进化的具体过程。

### 6. 论文的主要结论与发现
*   **显著的性能提升**：Evo-MedAgent 将 GPT-5-mini 的 MCQ 准确率从 0.68 提升至 **0.79**，Gemini-3 Flash 从 0.76 提升至 **0.87**。
*   **记忆胜过工具**：一个有趣的发现是，在强力基础模型（如 GPT-5-mini）上，**进化记忆带来的提升甚至超过了调用外部专家工具**。这表明对于定性诊断，优化推理逻辑比增加不完美的外部感知工具更重要。
*   **互补性**：情节记忆和程序化记忆结合（S+E）的效果最好，两者在经验复用和规则泛化上形成了互补。

### 7. 优点与亮点
*   **模拟人类学习**：成功将放射科医生“从病例中学习”的生物学特性工程化为智能体的 TTL 机制。
*   **即插即用**：无需对底层大模型进行昂贵的微调，具有极高的部署灵活性。
*   **透明度与可解释性**：通过自然语言存储程序化规则，人类医生可以直观地看到智能体“学到了什么”，并能手动干预或修正这些规则。

### 8. 不足与局限
*   **反馈依赖性**：实验假设每个病例后都能立即获得真实的 Ground-truth 反馈。在实际临床中，反馈往往是延迟、部分甚至缺失的。
*   **定量任务限制**：对于需要精确测量（如心胸比、结节大小）的任务，记忆模块无法替代确定性的工具，仍需依赖外部算法。
*   **工具治理研究尚浅**：虽然提出了工具治理记忆（G），但论文中对其进行的定量消融分析相对较少，更多侧重于 S 和 E 模块。
*   **模态局限**：目前仅在胸部 X 射线领域验证，其在更复杂的 3D 影像（如 CT/MRI）或多模态数据上的表现尚待观察。

（完）
