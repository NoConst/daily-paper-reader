---
title: "ATP-Bench: Towards Agentic Tool Planning for MLLM Interleaved Generation"
title_zh: ATP-Bench：迈向多模态大语言模型交错生成的智能体工具规划
authors: "Yinuo Liu, Zi Qian, Heng Zhou, Jiahao Zhang, Yajie Zhang, Zhihang Li, Mengyu Zhou, Erchao Zhao, Xiaoxi Jiang, Guanjun Jiang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29902v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大模型交错生成与检索增强
tldr: "本文针对多模态大模型（MLLM）在交错文本图像生成中难以兼顾事实性与创造性的挑战，提出了“智能体工具规划”范式，使模型能自主决定调用生成或检索工具。研究引入了包含7,702个QA对的ATP-Bench评测基准，并开发了多智能体评测系统MAM。实验揭示了现有模型在复杂工具规划上的局限性，为提升交错生成能力提供了方向。"
selection_source: fresh_fetch
motivation: 当前的交错生成方法通常将图像生成与检索割裂开来，缺乏一个能根据需求自主选择合适工具的统一规划机制。
method: 构建了涵盖8大类、25种意图的ATP-Bench基准，并提出MAM多智能体评测框架，在无需参考答案的情况下评估工具调用精度与响应质量。
result: 对10款主流MLLM的评估发现，模型在连贯的交错规划和工具调用时机把握上存在显著困难，表现差异巨大。
conclusion: 智能体工具规划是多模态生成的下一个里程碑，ATP-Bench为优化MLLM的自主规划与交错生成能力提供了系统性的评估工具。
---

## 摘要
文本与图像交错生成是多模态大语言模型（MLLM）的一个重要前沿领域，为传达复杂信息提供了一种更直观的方式。当前的范式要么依赖于图像生成，要么依赖于检索增强，但通常将两者视为互斥的路径，未能将真实性与创造性统一起来。我们认为，该领域的下一个里程碑是智能体工具规划（Agentic Tool Planning），即模型作为中央控制器，自主决定何时、何地以及调用哪些工具，从而为视觉关键型查询生成交错式响应。为了系统地评估这一范式，我们推出了 ATP-Bench，这是一个包含 7,702 个问答对（包括 1,592 个视觉问答对）的新型基准测试，涵盖 8 个类别和 25 种视觉关键意图，并包含经人工验证的查询和标准答案。此外，为了在不依赖端到端执行和不断变化的工具后端的情况下评估智能体规划，我们提出了一种多智能体 MLLM 评测（MAM）系统。MAM 可以在不需要参考标准答案的情况下，评估工具调用的准确性，识别遗漏的工具使用机会，并评估整体响应质量。我们对 10 种最先进的 MLLM 进行了广泛实验，结果表明模型在连贯的交错规划方面表现挣扎，且在工具使用行为上表现出显著差异，这凸显了巨大的提升空间，并为推进交错生成提供了可操作的指导。数据集和代码可在 https://github.com/Qwen-Applications/ATP-Bench 获取。

## Abstract
Interleaved text-and-image generation represents a significant frontier for Multimodal Large Language Models (MLLMs), offering a more intuitive way to convey complex information. Current paradigms rely on either image generation or retrieval augmentation, yet they typically treat the two as mutually exclusive paths, failing to unify factuality with creativity. We argue that the next milestone in this field is Agentic Tool Planning, where the model serves as a central controller that autonomously determines when, where, and which tools to invoke to produce interleaved responses for visual-critical queries. To systematically evaluate this paradigm, we introduce ATP-Bench, a novel benchmark comprising 7,702 QA pairs (including 1,592 VQA pairs) across eight categories and 25 visual-critical intents, featuring human-verified queries and ground truths. Furthermore, to evaluate agentic planning independent of end-to-end execution and changing tool backends, we propose a Multi-Agent MLLM-as-a-Judge (MAM) system. MAM evaluates tool-call precision, identifies missed opportunities for tool use, and assesses overall response quality without requiring ground-truth references. Our extensive experiments on 10 state-of-the-art MLLMs reveal that models struggle with coherent interleaved planning and exhibit significant variations in tool-use behavior, highlighting substantial room for improvement and providing actionable guidance for advancing interleaved generation. Dataset and code are available at https://github.com/Qwen-Applications/ATP-Bench.

---

## 论文详细总结（自动生成）

以下是对论文《ATP-Bench: Towards Agentic Tool Planning for MLLM Interleaved Generation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于多模态大语言模型（MLLM）的**交错图文生成（Interleaved Generation）**任务。
*   **研究动机**：现有的交错生成范式通常将“图像生成”与“图像检索”视为互斥路径。生成式方法（如扩散模型）缺乏事实准确性，而检索式方法（RAG）则缺乏针对特定查询的创造性修改能力。
*   **核心问题**：如何让模型作为一个“中央控制器”，在面对复杂的视觉关键型查询时，自主、连贯地规划何时、何地以及调用哪种工具（引用、生成、搜索、绘图或编辑）来产生最优的图文交错响应。
*   **整体含义**：提出了“**智能体工具规划（Agentic Tool Planning）**”这一新范式，旨在打破参考与生成的界限，实现事实性与创造性的统一。

### 2. 方法论：核心思想与技术细节
论文提出了一个完整的评估框架，包含数据集构建、工具定义和自动化评测系统：
*   **核心思想**：将交错生成转化为一个工具规划问题。模型输出一个包含文本和结构化工具调用标签（如 `<tool>{...}</tool>`）的序列 $R = \{s_1, s_2, \dots, s_m\}$。
*   **五大工具箱（Toolkit）**：
    1.  **Reference**：引用上下文中的已有图像。
    2.  **Diffusion**：合成新的创意图像。
    3.  **Search**：通过搜索引擎获取真实世界的视觉素材。
    4.  **Code**：通过代码生成数据驱动的图表或数学图像。
    5.  **Edit**：对已有图像进行局部修改或标注。
*   **MAM 评测系统（Multi-Agent MLLM-as-a-Judge）**：为了摆脱对端到端执行环境和标准答案的依赖，设计了三个智能体角色：
    *   **精度检查员（Precision Inspector）**：评估现有工具调用的必要性、工具选择正确性、语义位置及参数准确性。
    *   **召回检查员（Recall Inspector）**：识别模型在哪些本该使用视觉支持的地方仅使用了纯文本（遗漏机会）。
    *   **主法官（Chief Judge）**：综合上述报告，给出 0-100 的最终评分（Final Score）。

### 3. 实验设计
*   **数据集（ATP-Bench）**：包含 **7,702 个 QA 对**（含 1,592 个 VQA 对），涵盖学术、手册、食谱、时尚、装修、产品、旅游、百科 8 大类别，细分为 25 种视觉关键意图。
*   **对比方法**：评估了 **10 种最先进的 MLLM**，包括：
    *   闭源模型：Claude 4.5/4, Gemini 3 Pro, GPT-5, GPT-4o, Grok-4.1, Qwen3-VL-Plus。
    *   开源模型：Qwen2.5-VL-72B, InternVL3.5-14B, LLaMA-3.2-11B。
*   **评估指标**：最终得分（FS）、成功率（SR）、遗漏图像数（MI）、工具采用率、以及基于 Ground Truth 的 F1 分数。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**训练这些模型所使用的具体算力（如 GPU 型号或数量），因为该研究主要侧重于**基准测试构建和推理评估**，而非模型训练。
*   **推理成本**：实验涉及对 10 个模型在 7,702 个样本上的推理，并使用 Gemini 2.5 Pro 等模型作为评测法官，具有一定的 API 调用成本。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的跨模型对比（10 个模型）、跨类别分析（8 个领域）、Few-shot 实验、工具边界消融实验、以及评测模型一致性实验。
*   **充分性与客观性**：
    *   **充分性**：实验涵盖了当前市面上最顶尖的开源与闭源模型，样本量大且覆盖面广。
    *   **客观性**：通过“人工一致性研究”验证了 MAM 评测系统的可靠性（一致性达 84%-88%），并对比了不同模型（Claude, Gemini, GPT）作为法官的评分相关性，确保了评测框架的公正性。

### 6. 主要结论与发现
*   **性能排名**：**Gemini 3 Pro** 在所有指标和大多数类别中均处于领先地位（FS: 79.88, SR: 81.77）。
*   **任务难度**：学术和百科类任务相对简单，而**旅游（Travel）**和**装修（Renovation）**类任务最具挑战性，模型极易遗漏工具调用或执行错误。
*   **模型行为差异**：不同模型有明显的工具偏好。例如，Grok-4.1 调用工具最积极，而 GPT-5 则相对保守。
*   **开源差距**：开源模型在工具规划能力上显著落后于顶级闭源模型，常出现格式错误或无效参数。

### 7. 优点（亮点）
*   **范式创新**：首次提出“智能体工具规划”概念，统一了交错生成中的检索与生成路径。
*   **高质量数据**：ATP-Bench 经过严格的人工验证，确保了查询的“视觉关键性”和标准答案的专业性。
*   **评测框架解耦**：MAM 系统实现了规划能力与执行环境的解耦，解决了工具后端多变导致的评测不稳定性问题。

### 8. 不足与局限
*   **模态局限**：目前仅关注文本和图像的交错，未涉及音频、视频等多模态输出。
*   **工具集规模**：定义的工具箱仅包含 5 种工具，可能无法完全覆盖更广泛的智能体应用场景。
*   **法官偏见风险**：虽然经过验证，但使用 MLLM 作为裁判仍可能存在潜在的自我偏好或对特定模型风格的偏见。
*   **端到端验证**：虽然 MAM 评估了规划，但规划成功并不等同于最终生成的图像质量（如美观度）一定完美。

（完）
