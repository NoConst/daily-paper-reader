---
title: "ViLL-E: Video LLM Embeddings for Retrieval"
title_zh: ViLL-E：用于检索的视频大语言模型嵌入
authors: "Rohit Gupta, Jayakrishnan Unnikrishnan, Fan Fei, Sheng Liu, Son Tran, Mubarak Shah"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.12148v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 用于检索任务的视频大模型嵌入
tldr: 针对VideoLLM在检索任务中表现不佳的问题，本文提出ViLL-E架构。它引入了动态嵌入生成机制，允许模型根据视频复杂度调整计算量。通过结合生成式与对比学习的三阶段训练（大规模预训练、详细描述训练及多任务微调），ViLL-E在保持VideoQA能力的同时，显著提升了视频检索和时序定位性能，并在零样本组合检索等任务上达到SOTA。
selection_source: fresh_fetch
motivation: 现有的VideoLLM在生成任务上表现优异，但在文本-视频检索和时序定位等嵌入驱动的任务中逊于专用模型。
method: 提出一种具备动态嵌入生成机制的统一架构，并采用结合生成式与对比学习的三阶段训练策略（预训练、详细描述训练、多任务微调）。
result: "在时序定位上平均提升7%，视频检索提升4%，并在零样本组合检索和长文本检索任务中刷新了SOTA记录。"
conclusion: ViLL-E成功统一了生成与检索能力，证明了通过联合训练和动态嵌入机制可以使VideoLLM在理解与检索任务中均达到顶尖水平。
---

## 摘要
视频大语言模型（VideoLLMs）在输出为文本的视频理解任务中表现出色，例如视频问答（Video Question Answering）和视频描述（Video Captioning）。然而，在检索任务（如文本-视频检索和时刻检索）中，它们的表现逊于专门的基于嵌入的模型。我们提出了 ViLL-E（Video-LLM-Embed），这是一种统一的 VideoLLM 架构，配备了一种新颖的嵌入生成机制，允许模型对复杂视频进行“更长时间的思考”，而对简单视频则提前停止。我们采用结合了生成式学习和对比学习的三阶段训练方法来训练该模型：首先是使用视频-描述对进行大规模预训练；随后在较小的详细描述数据集上进行持续训练；最后在涵盖视频问答、时序定位、视频检索和视频-文本匹配的新型多任务数据集上进行特定任务的微调。我们的模型显著提升了时序定位（比其他 VideoLLM 平均提高 7%）和视频检索（比双编码器模型最高提高 4%）的性能，在保持视频问答任务竞争力的同时，达到了与最先进的专用嵌入模型相当的水平。此外，我们的联合对比-生成训练开启了新的零样本能力，在组合视频检索（比现有最先进方法提高 5%）和长文本检索（比现有最先进方法提高 2%）方面显著优于现有技术。

## Abstract
Video Large Language Models (VideoLLMs) excel at video understanding tasks where outputs are textual, such as Video Question Answering and Video Captioning. However, they underperform specialized embedding-based models in Retrieval tasks, such as Text-toVideo Retrieval and Moment Retrieval. We introduce ViLL-E (Video-LLM-Embed), a unified VideoLLM architecture endowed with a novel embedding generation mechanism that allows the model to "think longer" for complex videos and stop early for easy ones. We train this model with a three-stage training methodology combining generative and contrastive learning: initial large-scale pre-training with video-caption pairs; followed by continual training on a smaller, detailed-caption dataset; and concluding with task-specific fine-tuning on a novel multi-task dataset covering Video QA, Temporal Localization, Video Retrieval, and Video-Text Matching. Our model significantly improves temporal localization (on avg. 7% over other VideoLLMs) and video retrieval (up to 4% over dual encoder models), achieving performance comparable to state-of-the-art specialized embedding models while remaining competitive on VideoQA tasks. Furthermore, our joint contrastive-generative training unlocks new zero-shot capabilities, significantly outperforming state-of-the-art methods in composed video retrieval (+5% over SotA) and retrieval from long text (+2% over SotA).

---

## 论文详细总结（自动生成）

### ViLL-E：用于检索的视频大语言模型嵌入（论文总结）

#### 1. 核心问题与整体含义（研究动机和背景）
目前的视频大语言模型（VideoLLMs）在生成式任务（如视频问答 VideoQA、视频描述 Captioning）中表现优异，但在**检索类任务**（如文本-视频检索、时序时刻定位）中显著逊于专门的基于嵌入（Embedding-based）的专家模型（如 CLIP 变体或专用检测器）。
*   **研究动机**：VideoLLM 通常将视频特征投影到文本空间进行自回归生成，缺乏判别性的嵌入表示；而专家模型虽然检索强，却无法进行复杂的文本生成和推理。
*   **核心目标**：开发一种统一的架构，使单个模型既能像 LLM 一样“说话”（生成文本），又能像专家模型一样“思考”（生成高质量的视频/文本嵌入），从而填补 VideoLLM 在检索和定位任务上的性能鸿沟。

#### 2. 方法论：核心思想与关键技术
ViLL-E 基于 **PaliGemma-3B** 架构，通过以下核心技术实现统一：
*   **自适应嵌入头（Embedding Head）设计**：
    *   引入了一个名为 **KV-Former** 的可学习池化机制。它将 LLM 最后一层的输出 token 作为 Query，与一组可学习的“池化 token”（Pooling Tokens）作为 Key 和 Value 进行交叉注意力运算。
    *   **动态“思考”机制**：模型通过自回归生成 `<EOS>` 标记来触发嵌入生成。对于复杂的视频，模型可以生成更多的 token 后再进行池化；对于简单视频则早停。这种变长生成机制允许模型根据内容复杂度分配计算资源。
*   **三阶段训练策略**：
    1.  **大规模联合预训练**：在 10M 规模的视频-描述对上，同时使用**下文预测损失（Next-token Prediction）**和**对比损失（Contrastive Loss）**。
    2.  **高质量持续预训练**：利用 Claude-3-Sonnet 生成的 200k 条详细、长篇描述进行训练，增强模型对视频细节的捕捉能力。
    3.  **多任务指令微调（IFT）**：在包含 QA、检索、匹配和时序定位的混合数据集上进行微调，使用 LoRA 技术保证参数效率。

#### 3. 实验设计：数据集、Benchmark 与对比方法
*   **数据集**：
    *   **时序定位**：ActivityNet-Captions, Charades-STA, QVHighlights。
    *   **视频检索**：MSR-VTT, DiDeMo, VATEX。
    *   **视频问答**：MVBench, VideoMME, MSVD-QA。
*   **Benchmark**：涵盖了 R@1 (Recall at 1), mAP (mean Average Precision) 等检索指标，以及准确率等 QA 指标。
*   **对比方法**：
    *   **专家模型**：VidLA, SigLIP, QD-DETR, InternVideo2。
    *   **VideoLLMs**：Qwen2.5-VL, LLaVA-ST, TimeChat, VTimeLLM 等。

#### 4. 资源与算力
论文明确列出了训练配置：
*   **GPU 数量与型号**：
    *   **第一、二阶段**：使用 32 张 GPU（未指明具体型号，但提到在 AWS 环境下）。
    *   **第三阶段**：使用 8 张 GPU。
*   **训练细节**：
    *   第一阶段 Batch Size 为 12/GPU，共 25,000 步。
    *   推理延迟测试在 **NVIDIA L40S GPU** (AWS g6e.8xlarge) 上进行，结果显示自适应嵌入机制仅增加极小的开销（约 262ms）。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 8 个主流 Benchmark 上进行了测试，涵盖了从短视频检索到长视频定位的多种场景。
*   **消融实验**：非常充分。作者对比了：
    1.  不同的嵌入头设计（Linear, MLP, Q-Former, KV-Former 等）。
    2.  监督信号的影响（生成 vs 对比 vs 匹配）。
    3.  预训练阶段的贡献（证明了高质量长描述预训练对定位任务至关重要）。
    4.  Token 数量的影响（证明了动态变长 token 优于固定长度）。
*   **客观性**：通过与当前最强的开源模型（如 Qwen2.5-VL）对比，客观展示了 ViLL-E 在检索上的领先地位及在 QA 上的竞争水平。

#### 6. 主要结论与发现
*   **检索性能突破**：ViLL-E 在时序定位任务上比现有 VideoLLM 平均提升了 **7%**，在视频检索上比双编码器模型提升了 **4%**。
*   **统一性**：ViLL-E 是首个在保持 VideoQA 竞争力的同时，在检索任务上达到专家模型水平的 VideoLLM。
*   **零样本新能力**：
    *   **组合视频检索**：在“原视频+修改指令”检索目标视频的任务中，比 SOTA 提升了 5%。
    *   **长文本检索**：由于 LLM 的长文本处理能力，其在 500 词以上的长描述检索中表现远超 CLIP 等模型。

#### 7. 优点：亮点总结
*   **架构优雅**：通过一个简单的嵌入头和 `<EOS>` 触发机制，巧妙解决了生成与检索的矛盾。
*   **动态计算**：提出的“自适应思考”机制（Adaptive Embedding Tokens）符合直觉，即复杂视频需要更多表征空间。
*   **训练流程完备**：三阶段训练法（从噪声数据到高质量数据再到多任务微调）为多模态模型训练提供了成熟范式。
*   **多功能性**：单模型支持检索、定位、QA、描述及两阶段重排序（Re-ranking）。

#### 8. 不足与局限
*   **基础模型限制**：继承了 PaliGemma 的局限性，例如缺乏多轮对话能力。
*   **语言偏差**：训练数据主要为英文，模型在多语言场景下的表现可能大幅下降。
*   **推理成本**：虽然嵌入生成很快，但相比于极轻量级的双编码器模型（如 CLIP），其作为检索后端时的计算成本（LLM 前向传播）仍然较高。
*   **时序粒度**：虽然在定位上提升显著，但与最顶尖的非 LLM 专家模型（如 QD-DETR）在某些特定指标上仍有微小差距。

（完）
