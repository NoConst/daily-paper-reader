---
title: "Few Shots Text to Image Retrieval: New Benchmarking Dataset and Optimization Methods"
title_zh: 少样本图文检索：新基准数据集与优化方法
authors: "Ofer Idan, Vladi Vexler, Gil Lederman, Dima Sivov, Aviad Cohen Zada, Shir Niego Komforti"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25891v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 明确针对带有参考示例的文本图像检索，处理组合查询
tldr: 针对预训练视觉语言模型在处理组合查询和分布外数据时的局限性，本文提出了少样本图文检索（FSIR）任务及首个基准数据集FSIR-BD。该数据集包含3.8万余张图像，专注于复杂的组合场景。同时，作者提出了两种利用少量参考样本优化检索性能的新方法，这些方法兼容现有图像编码器，在实验中显著提升了平均精度均值（mAP），有效缩小了机器与人类在少样本推理方面的差距。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在处理复杂的组合查询和分布外数据时表现不佳，且缺乏利用少量示例进行检索优化的基准。
method: 提出了FSIR-BD基准数据集，并开发了两种利用单样本或少样本参考示例（包含正样本和硬负样本）来优化检索结果的新方法。
result: 实验证明FSIR-BD是一个极具挑战性的基准，且所提优化方法在mAP指标上显著优于现有基准模型。
conclusion: 通过少样本学习优化图文检索是提升模型组合推理能力的有效途径，有助于缩小机器与人类理解水平的差距。
---

## 摘要
预训练视觉语言模型（VLM）在多模态任务中表现出色，通常将图像编码为嵌入向量存储在数据库中，并通过近似最近邻搜索（ANNS）进行检索。然而，这些模型在处理组合式查询和分布外（OOD）图文对时面临挑战。受人类认知从极少数示例中学习的能力启发，我们通过专门为图像检索设计的少样本学习方法来弥补这一性能差距。我们提出了少样本图文检索（FSIR）任务及其配套的基准数据集 FSIR-BD。这是首个明确针对带有参考示例的文本进行图像检索的数据集，重点关注具有挑战性的组合式和分布外查询。组合部分分为城市场景和自然物种，两者均处于特定情境或具有独特特征。FSIR-BD 包含 38,353 张图像和 303 个查询，其中 82% 构成测试语料库（每个查询平均包含 37 个正样本、地面真值匹配项以及大量的难负样本），18% 构成由示例正样本和难负样本组成的少样本参考语料库（FSR）。此外，我们提出了两种新型检索优化方法，利用 FSR 中的单样本或少样本参考示例来提升性能。这两种方法都与任何预训练图像编码器兼容，使其适用于现有的规模化环境。我们的实验表明：(1) FSIR-BD 为图像检索提供了一个具有挑战性的基准；(2) 以平均精度均值（mAP）衡量，我们的优化方法优于现有的基准模型。对 FSIR 优化方法的进一步研究将有助于缩小机器与人类水平理解之间的差距，特别是在基于有限示例的组合推理方面。

## Abstract
Pre-trained vision-language models (VLMs) excel in multimodal tasks, commonly encoding images as embedding vectors for storage in databases and retrieval via approximate nearest neighbor search (ANNS). However, these models struggle with compositional queries and out-of-distribution (OOD) image-text pairs. Inspired by human cognition's ability to learn from minimal examples, we address this performance gap through few-shot learning approaches specifically designed for image retrieval. We introduce the Few-Shot Text-to-Image Retrieval (FSIR) task and its accompanying benchmark dataset, FSIR-BD - the first to explicitly target image retrieval by text accompanied by reference examples, focusing on the challenging compositional and OOD queries. The compositional part is divided to urban scenes and nature species, both in specific situations or with distinctive features. FSIR-BD contains 38,353 images and 303 queries, with 82% comprising the test corpus (averaging per query 37 positives, ground truth matches, and significant number of hard negatives) and 18% forming the few-shot reference corpus (FSR) of exemplar positive and hard negative images. Additionally, we propose two novel retrieval optimization methods leveraging single shot or few shot reference examples in the FSR to improve performance. Both methods are compatible with any pre-trained image encoder, making them applicable to existing large-scale environments. Our experiments demonstrate that: (1) FSIR-BD provides a challenging benchmark for image retrieval; and (2) our optimization methods outperform existing baselines as measured by mean Average Precision (mAP). Further research into FSIR optimization methods will help narrow the gap between machine and human-level understanding, particularly for compositional reasoning from limited examples.

---

## 论文详细总结（自动生成）

### 论文总结：少样本图文检索（FSIR）的新基准与优化方法

#### 1. 论文的核心问题与整体含义
*   **研究背景**：预训练视觉语言模型（VLM，如 CLIP、BLIP）虽然在通用图文匹配上表现优异，但在处理**组合性查询**（需要理解对象、属性及空间关系的复杂组合）和**分布外（OOD）数据**（如专业医疗、卫星或特定物种图像）时效果较差。
*   **核心问题**：当用户发现初始检索结果不准确时，往往能提供几个正向或负向示例。论文旨在研究如何利用这些“少样本（Few-Shot）”参考示例来即时优化检索性能，从而弥补机器与人类在复杂逻辑推理上的差距。
*   **主要贡献**：定义了少样本图文检索（FSIR）任务，发布了首个专门的基准数据集 **FSIR-BD**，并提出了两种优化检索的方法。

#### 2. 论文提出的方法论
论文提出了两种互补的优化策略，均兼容现有的预训练图像编码器：

*   **FSIR-PL（少样本提示学习）**：
    *   **核心思想**：针对特定查询，通过少量的正负样本在线训练一组可学习的“上下文向量（Prompt）”。
    *   **技术细节**：将 16 个可学习向量与用户查询文本拼接。使用**二元交叉熵（BCE）损失**来优化这些向量，通过一个可学习的 Sigmoid 函数将余弦相似度映射到 [0, 1] 概率区间。
    *   **防止遗忘**：引入 **KL 散度损失**和 **ProGrad 因子**，确保在学习新概念的同时，不丢失 VLM 原有的通用知识。
*   **FSIR-CTR（组合文本加参考样本模型）**：
    *   **核心思想**：利用多模态大语言模型（MLLM）作为编码器，将文本和参考图像融合为一个统一的嵌入向量，并对齐到外部图像编码器的特征空间。
    *   **技术细节**：采用 Phi-3.5-V 作为骨干，通过 **LoRA** 进行微调。训练分为三阶段：检索能力微调、特征空间对齐、任务特定微调。使用 **InfoNCE 损失**进行对比学习，使 MLLM 生成的查询向量与目标图像向量在空间中靠近。

#### 3. 实验设计
*   **数据集（FSIR-BD）**：包含 38,353 张图像和 303 个查询，分为三个子集：
    1.  **Compositional-VG**：基于视觉基因组，侧重城市场景的复杂组合（如“穿着黑衬衫在公园里的男人”）。
    2.  **Compositional-INQUIRE**：侧重自然界物种、行为和环境的专家级查询。
    3.  **OOD**：包含飞机型号、纹理、卫星图像等分布外数据。
*   **Benchmark 与对比方法**：
    *   **基准模型**：CLIP-Large 和 BLIP-Large 的零样本（Zero-Shot）表现。
    *   **对比指标**：平均精度均值（mAP），相比 Recall@K，该指标更能反映检索结果的排序质量。
    *   **对比方法**：包括 SOTA 的组合图像检索（CIR）模型，如 Phi-3.5-E5V。

#### 4. 资源与算力
*   **FSIR-PL**：在单张 **NVIDIA V100 GPU** 上完成训练，优化过程针对每个查询独立进行。
*   **FSIR-CTR**：在 **4 张 NVIDIA A100 GPU** 上进行训练。
    *   **训练参数**：迭代 20,000 次，Batch Size 为 32，LoRA Rank 为 8。
    *   **数据生成**：使用自动化流水线从 COCO-Captions 生成了训练所需的三元组数据（FSIR-TD）。

#### 5. 实验数量与充分性
*   **实验规模**：论文在三个不同领域的子集上进行了详尽测试，涵盖了从日常场景到专业领域的跨度。
*   **消融实验**：针对 FSIR-PL 进行了“样本数量（Shot）”的消融实验（从 1 到 16 个样本），证明了性能随样本增加而稳步提升。
*   **客观性**：实验对比了不同架构的骨干网络（CLIP vs BLIP），并与最新的 MLLM 检索方法进行了公平竞争。数据集设计中包含了大量的“硬负样本（Hard Negatives）”，增加了实验的挑战性和说服力。

#### 6. 论文的主要结论与发现
*   **性能提升**：FSIR-PL 在所有基准上均显著优于零样本基准，BLIP-L PL 平均提升了 9.3%，在 OOD 任务上提升高达 17.9%。
*   **模型通用性**：FSIR-CTR 作为一个通用模型，在未见过特定负样本的情况下，依然能通过参考图像提升检索精度，尤其在自然科学和 OOD 领域表现突出。
*   **基准价值**：FSIR-BD 被证明是一个极具挑战性的基准，现有的强力 VLM 在没有少样本辅助时表现较差。

#### 7. 优点
*   **数据集创新**：FSIR-BD 是首个同时强调组合性、多地面真值（Ground Truth）和硬负样本的少样本检索数据集，更贴近真实应用。
*   **架构兼容性**：提出的方法不需要重新编码现有的海量图像数据库，只需优化查询端的向量，极具工程落地价值。
*   **方法多样性**：既提供了针对特定查询的高精度在线优化（PL），也提供了通用的端到端融合模型（CTR）。

#### 8. 不足与局限
*   **计算开销**：FSIR-PL 需要针对每个新查询进行微调，虽然样本少，但在高并发实时检索场景下可能存在延迟压力。
*   **参考样本敏感性**：FSIR-CTR 的效果高度依赖于所选参考图像的质量，不同参考图可能导致结果波动。
*   **负样本利用**：目前 FSIR-CTR 主要利用正向参考图，如何有效利用用户提供的“负向示例”来排除干扰项仍是待解决的难题。

（完）
