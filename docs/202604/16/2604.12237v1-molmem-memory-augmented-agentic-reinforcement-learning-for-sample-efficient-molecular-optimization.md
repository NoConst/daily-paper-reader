---
title: "MolMem: Memory-Augmented Agentic Reinforcement Learning for Sample-Efficient Molecular Optimization"
title_zh: MolMem：用于样本高效分子优化的记忆增强智能体强化学习
authors: "Ziqing Wang, Yibo Wen, Abhishek Pandy, Han Liu, Kaize Ding"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12237v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 记忆增强的智能体强化学习
tldr: 本研究针对药物发现中分子优化评估成本高昂的挑战，提出了MolMem框架。该框架采用增强记忆的智能体强化学习方法，通过双重记忆系统（静态示例记忆和演化技能记忆）来解决样本效率低的问题。静态记忆用于冷启动引导，演化记忆则将成功经验提炼为可重用策略。实验表明，在极低评估预算下，MolMem在单属性和多属性优化任务上均显著优于现有基准，为高效分子设计提供了新思路。
selection_source: fresh_fetch
motivation: 现有分子优化方法在有限的评估预算下样本效率较低，且缺乏能够提供长期指导和可重用策略的记忆机制。
method: 提出一种双重记忆增强的强化学习框架，利用静态示例记忆进行冷启动引导，并通过演化技能记忆将成功轨迹转化为可重用的优化策略。
result: "在仅500次评估预算下，MolMem在单属性任务上达到90%的成功率（优于基线1.5倍），在多属性任务上达到52%的成功率。"
conclusion: 通过引入长短期记忆机制，MolMem显著提升了分子优化的样本效率，证明了智能体架构在受限资源药物发现中的巨大潜力。
---

## 摘要
在药物发现中，分子优化旨在通过迭代改进先导化合物来提升其分子性质，同时保持与原始分子的结构相似性。然而，每次 oracle 评估都十分昂贵，这使得在有限的评估预算下，样本效率成为现有方法面临的关键挑战。试错法需要大量的 oracle 调用，而利用外部知识的方法往往倾向于重用熟悉的模板，在应对挑战性目标时显得力不从心。一个关键的缺失环节是能够为决策提供依据并为未来优化提供可重用见解的长期记忆。为了解决这一问题，我们

## Abstract
In drug discovery, molecular optimization aims to iteratively refine a lead compound to improve molecular properties while preserving structural similarity to the original molecule. However, each oracle evaluation is expensive, making sample efficiency a key challenge for existing methods under a limited oracle budget. Trial-and-error approaches require many oracle calls, while methods that leverage external knowledge tend to reuse familiar templates and struggle on challenging objectives. A key missing piece is long-term memory that can ground decisions and provide reusable insights for future optimizations. To address this, we present MolMem (\textbf{Mol}ecular optimization with \textbf{Mem}ory), a multi-turn agentic reinforcement learning (RL) framework with a dual-memory system. Specifically, MolMem uses Static Exemplar Memory to retrieve relevant exemplars for cold-start grounding, and Evolving Skill Memory to distill successful trajectories into reusable strategies. Built on this memory-augmented formulation, we train the policy with dense step-wise rewards, turning costly rollouts into long-term knowledge that improves future optimization. Extensive experiments show that MolMem achieves 90\% success on single-property tasks (1.5$\times$ over the best baseline) and 52\% on multi-property tasks using only 500 oracle calls. Our code is available at https://github.com/REAL-Lab-NU/MolMem.

---

## 论文详细总结（自动生成）

以下是对论文《MolMem: Memory-Augmented Agentic Reinforcement Learning for Sample-Efficient Molecular Optimization》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：在药物发现中，分子优化是一个迭代改进先导化合物性质（如药效、溶解度）同时保持结构相似性的过程。
*   **核心痛点**：现有的优化方法（如遗传算法或单步强化学习）样本效率低下，因为每次分子性质评估（Oracle 调用）通常涉及昂贵的湿实验或高保真模拟。
*   **研究动机**：现有方法要么缺乏先验知识（试错法），要么仅能模仿外部模板（学徒法），缺乏像人类专家那样从过去成功经验中提炼长期、可重用策略的能力。

### 2. 方法论
MolMem 提出了一个**多轮智能体强化学习框架**，其核心是**双重记忆系统**：
*   **静态示例记忆 (Static Exemplar Memory)**：从包含 280 万个分子的 ChEMBL 数据库中，检索结构相似且目标属性得分高的分子作为参考，解决优化初期的“冷启动”问题。
*   **演化技能记忆 (Evolving Skill Memory)**：这是本文的创新点。系统会自动分析高奖励的优化轨迹，利用 LLM（如 GPT-4o）将成功的结构修改提炼为文本形式的“技能卡”（例如：“将芳香环上的甲氧基替换为氟以降低分子量”），并存储在技能库中供后续调用。
*   **算法流程**：
    1.  **SFT 阶段**：在高质量分子编辑对上进行监督微调，使模型掌握基本的分子编辑能力。
    2.  **RL 阶段**：采用 PPO（近端策略优化）算法，配合**密集步进奖励 (Dense Step-wise Rewards)**。当优化陷入停滞时，触发记忆检索（示例或技能）来引导智能体。
    3.  **推理阶段**：智能体根据当前分子状态、历史轨迹及检索到的记忆，迭代提出修改建议。

### 3. 实验设计
*   **数据集**：从 ZINC-250k 数据库中随机抽取 200 个先导分子作为测试集。
*   **任务设置**：
    *   **单属性优化**：QED（药效性）、plogP（亲脂性）、SA（合成难易度）、DRD2 和 JNK3（生物活性）。
    *   **多属性优化**：上述属性的多种组合（如 QED+plogP+DRD2）。
*   **Benchmark 与对比方法**：
    *   **新手类 (Novice)**：Graph-GA, QMO, Reinvent 4。
    *   **学徒类 (Apprentice)**：直接检索法、直接 Prompt 法、SFT 模型，以及多种化学专用大模型（MOLLEO, LlaSMol, ChemLLM, GeLLM 3O, PEIT-LLM）。
*   **约束条件**：Oracle 调用预算严格限制在 500 次以内，Tanimoto 相似度需 $\ge 0.4$。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 张 NVIDIA H100 GPU**。
*   **训练时长**：
    *   **SFT 阶段**：约 10 小时。
    *   **任务特定 RL 阶段**：每个任务约 4 小时。
    *   **推理评估**：每个任务约 3 小时。
*   **模型规模**：主干网络采用 **Qwen2.5-1.5B-Instruct**，技能提炼使用了 GPT-4o API。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10 个不同的优化任务（5 单 5 多），每个任务针对 200 个分子进行测试，总计进行了数千次优化实验。
*   **消融实验**：非常充分。作者分别验证了多轮机制、RL 训练、SFT 初始化以及两种记忆模块（静态与演化）的贡献。
*   **客观性**：通过统一的 Oracle 预算和相似度阈值，确保了与基线方法在同一标准下对比，且使用了较小规模的 1.5B 模型挑战 7B/8B 模型，证明了架构的优越性而非单纯堆砌参数。

### 6. 主要结论与发现
*   **性能突破**：在仅 500 次 Oracle 调用下，MolMem 在单属性任务上达到 **90% 的成功率**（比最强基线提升 1.5 倍），多属性任务达到 **52%**。
*   **记忆的价值**：演化技能记忆能显著提升在复杂生物活性目标（如 DRD2）上的表现，因为它能捕捉到跨分子的通用结构-活性关系（SAR）。
*   **化学合理性**：分析表明，技能库学到的策略（如去环化、卤素取代）与药物化学家的实际经验高度一致。

### 7. 优点
*   **样本效率极高**：通过记忆机制大幅减少了昂贵的 Oracle 调用次数。
*   **自我进化能力**：能够将训练过程中的成功经验转化为可理解、可检索的文本策略。
*   **轻量化高效**：证明了小参数模型（1.5B）配合良好的记忆管理和 RL 框架，可以超越参数量大数倍的专用模型。

### 8. 不足与局限
*   **Oracle 偏差风险**：实验依赖于计算模拟 Oracle，其结果与真实湿实验之间可能存在 gap。
*   **提炼成本**：技能总结阶段依赖 GPT-4o 等闭源强模型，增加了额外的 API 开销。
*   **应用范围**：目前主要针对小分子编辑，对于更复杂的生物大分子或涉及多步合成路径的优化尚待验证。

（完）
