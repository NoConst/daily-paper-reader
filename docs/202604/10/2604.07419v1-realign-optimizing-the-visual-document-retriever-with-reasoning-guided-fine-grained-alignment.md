---
title: "ReAlign: Optimizing the Visual Document Retriever with Reasoning-Guided Fine-Grained Alignment"
title_zh: ReAlign：通过推理引导的细粒度对齐优化视觉文档检索器
authors: "Hao Yang, Yifan Ji, Zhipeng Xu, Zhenghao Liu, Yukun Yan, Zulong Chen, Shuo Wang, Yu Gu, Ge Yu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07419v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 通过视觉语言模型引导的对齐优化视觉文档检索器
tldr: 针对视觉文档检索中复杂布局导致关键信息难以捕捉的问题，本文提出ReAlign方法。该方法利用高性能视觉语言模型（VLM）的推理能力，识别页面中与查询相关的局部区域并生成细粒度描述。通过将这些描述作为监督信号，引导检索器对齐查询与文档语义，从而提升模型对关键视觉线索的关注度。实验证明ReAlign在多个基准测试中均取得显著提升。
selection_source: fresh_fetch
motivation: 视觉文档布局复杂且关键证据分散，导致现有检索模型难以有效捕捉用于嵌入学习的关键视觉线索。
method: 利用高性能VLM识别查询相关区域并生成细粒度描述，通过蒸馏这些描述产生的排序分布来优化检索器的语义对齐。
result: "在多个视觉文档检索基准上实现了一致的性能提升，在域内和跨域数据集上最高获得2%的相对改进。"
conclusion: ReAlign通过推理引导的细粒度对齐，有效增强了检索器对关键视觉信息的捕捉能力，具有良好的通用性和鲁棒性。
---

## 摘要
视觉文档检索旨在从视觉丰富的集合中检索出与查询相关的文档页面。现有方法通常采用视觉语言模型（VLM）将查询和视觉页面编码到共享的嵌入空间中，并通过对比训练进行优化。然而，在视觉文档表示过程中，局部证据通常分散在复杂的文档布局中，使得检索模型难以捕捉到有效嵌入学习的关键线索。在本文中，我们提出了推理引导对齐（ReAlign），该方法利用 VLM 的推理能力提供细粒度视觉文档描述作为训练监督信号，从而增强视觉文档检索。具体而言，ReAlign 采用性能更优的 VLM 来识别页面上与查询相关的区域，并生成基于裁剪视觉区域的查询感知描述。随后，利用这些聚焦区域的描述来训练检索器，通过促使由聚焦区域描述引导的文档排名分布与由原始查询引导的分布相匹配，从而实现查询与视觉文档之间的语义对齐。在多种视觉丰富文档检索基准上的实验表明，ReAlign 在域内和跨域数据集上均能持续提升视觉文档检索性能，实现高达 2% 的相对提升。此外，ReAlign 的优势可推广至不同的 VLM 骨干网络，引导模型更好地关注文档表示中的关键视觉线索。所有代码和数据集均可在 https://github.com/NEUIR/ReAlign 获取。

## Abstract
Visual document retrieval aims to retrieve a set of document pages relevant to a query from visually rich collections. Existing methods often employ Vision-Language Models (VLMs) to encode queries and visual pages into a shared embedding space, which is then optimized via contrastive training. However, during visual document representation, localized evidence is usually scattered across complex document layouts, making it difficult for retrieval models to capture crucial cues for effective embedding learning. In this paper, we propose Reasoning-Guided Alignment (ReAlign), a method that enhances visual document retrieval by leveraging the reasoning capability of VLMs to provide fine-grained visual document descriptions as supervision signals for training. Specifically, ReAlign employs a superior VLM to identify query-related regions on a page and then generates a query-aware description grounding the cropped visual regions. The retriever is then trained using these region-focused descriptions to align the semantics between queries and visual documents by encouraging the document ranking distribution induced by the region-focused descriptions to match that induced by the original query. Experiments on diverse visually rich document retrieval benchmarks demonstrate that ReAlign consistently improves visual document retrieval performance on both in-domain and out-of-domain datasets, achieving up to 2% relative improvements. Moreover, the advantages of ReAlign generalize across different VLM backbones by guiding models to better focus their attention on critical visual cues for document representation. All code and datasets are available at https://github.com/NEUIR/ReAlign.

---

## 论文详细总结（自动生成）

以下是对论文《ReAlign: Optimizing the Visual Document Retriever with Reasoning-Guided Fine-Grained Alignment》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉文档检索（VDR）旨在从大量视觉丰富的文档（如报告、图表、网页）中检索与查询相关的页面。
*   **研究背景**：现有基于视觉语言模型（VLM）的检索器通常采用对比学习（如 InfoNCE）将查询和图像映射到统一空间。
*   **研究动机**：
    *   **布局复杂性**：文档中的关键证据往往分散在复杂的布局中，而大部分页面内容是无关的。
    *   **监督信号粗糙**：传统的对比学习仅提供页面级的全局对齐信号，导致模型注意力涣散，难以捕捉细粒度的视觉线索（如特定的数值或标题）。
    *   **推理能力缺失**：现有检索器缺乏对查询与文档之间逻辑关联的深度理解。

### 2. 方法论（ReAlign）
ReAlign 的核心思想是利用高性能 VLM 的推理能力生成“细粒度证据描述”，并以此作为辅助监督信号。
*   **区域引导的监督合成**：
    1.  使用高性能 VLM（如 Qwen2.5-VL-72B）针对查询识别文档中的 $K$ 个相关区域（Bounding Boxes）。
    2.  基于这些裁剪区域生成“查询感知”的证据描述（Evidence Description），将视觉线索转化为精确的文本表达。
*   **推理引导的分布对齐**：
    1.  **双分布构建**：计算“查询-文档”之间的排序分布 $P(d|q)$ 和“证据描述-文档”之间的排序分布 $P(d|t)$。
    2.  **KL 散度正则化**：通过最小化两个分布之间的 KL 散度，强制检索器在处理原始查询时，能够像处理精确的证据描述一样，关注到相同的关键视觉区域。
*   **联合损失函数**：$L = L_{Contrast} + \lambda L_{KL}$。其中 $L_{Contrast}$ 是标准的对比损失，$\lambda$ 是平衡系数（实验设定为 0.2）。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：整合了 DocVQA、InfoVQA、VisualMRC 等 6 个主流数据集，约 3.8 万个查询-文档对。
    *   **测试集**：包含域内（DocVQA, InfoVQA）和零样本跨域（ChartQA, SlideVQA, PlotQA, ArXivQA）共 6 个基准。
*   **对比方法**：
    *   **基于 OCR 的文本检索器**：BM25, BGE, E5-Mistral, NV-Embed。
    *   **多模态/视觉检索器**：CLIP, SigLIP 2, VLM2Vec, E5-V, DSE, ColPali, VDocRetriever。
*   **评价指标**：NDCG@5 和 NDCG@10。

### 4. 资源与算力
*   **数据生成**：使用 **4 台 NVIDIA A800 (40GB) GPU** 部署 Qwen2.5-VL-72B-Instruct 进行推理引导的证据合成，耗时约 **100 小时**。
*   **模型训练**：基于 Phi3V-4B 和 Qwen2.5-VL-7B 骨干网络，使用 **LoRA** 技术进行高效微调。
*   **优化器**：AdamW，有效 Batch Size 为 256，学习率 1e-4。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从工业文档、信息图表到学术论文、科学图表等多种场景，实验设置全面。
*   **消融实验**：对比了仅使用 InfoNCE、使用全局描述（无推理引导）以及完整 ReAlign 的效果，验证了“推理引导”和“区域聚焦”的必要性。
*   **深入分析**：
    *   **超参数敏感性**：测试了不同 $\lambda$ 值的影响。
    *   **嵌入空间分析**：通过对齐性（Alignment）和均匀性（Uniformity）量化分析了嵌入空间的质量。
    *   **注意力可视化**：通过 IoU 分数证明了 ReAlign 确实让模型注意力更集中于关键证据区域。
*   **客观性**：使用了统计显著性检验（p < 0.05），确保改进并非随机波动。

### 6. 主要结论与发现
*   **性能提升**：ReAlign 在所有基准上均取得显著提升，平均性能改进超过 2%，在复杂布局（如 DocVQA）上表现尤为突出。
*   **通用性强**：该方法在不同的 VLM 骨干网络（Phi3V, Qwen2.5-VL）上均有效，证明了其作为一种训练范式的普适性。
*   **机制验证**：ReAlign 成功引导模型将注意力从无关背景转移到关键视觉线索（如数值、特定文本块），显著增强了检索的精确度。
*   **空间优化**：相比基准模型，ReAlign 学习到的嵌入空间具有更好的区分度和结构化特征。

### 7. 优点与亮点
*   **创新的对齐范式**：不只是简单的三元组训练，而是通过“分布对齐”将高性能模型的推理知识蒸馏到检索器中。
*   **细粒度监督**：解决了视觉文档检索中“大海捞针”的问题，通过坐标引导让模型“看哪里”和“怎么看”。
*   **端到端无需 OCR**：避免了 OCR 识别错误带来的级联影响，同时保留了布局和空间信息。

### 8. 不足与局限
*   **合成成本高**：依赖极高参数量的教师模型（72B）生成训练信号，数据准备阶段的算力和时间成本较高。
*   **教师模型依赖**：训练质量高度受限于教师模型识别区域和生成描述的准确性，若教师模型在某些极端生僻领域推理出错，可能引入噪声。
*   **推理延迟**：虽然训练时引入了复杂对齐，但论文未详细讨论在超大规模索引（如亿级页面）下的在线检索延迟表现（尽管其架构仍是双编码器）。

（完）
