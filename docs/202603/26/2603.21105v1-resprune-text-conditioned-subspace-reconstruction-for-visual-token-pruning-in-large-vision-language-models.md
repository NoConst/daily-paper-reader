---
title: "ResPrune: Text-Conditioned Subspace Reconstruction for Visual Token Pruning in Large Vision-Language Models"
title_zh: ResPrune：大语言视觉模型中基于文本条件的子空间重构视觉 Token 剪枝
authors: "Xu Li, Yi Zheng, Yuxuan Liang, Zhe Liu, Xiaolei Chen, Haotian Chen, Rui Zhu, Xiangyang Xue"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21105v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大模型中用于高效推理的视觉标记剪枝
tldr: 针对大型视觉语言模型（LVLM）推理时视觉Token过多导致的计算和内存开销问题，本文提出了ResPrune。这是一种无需训练的视觉Token剪枝框架，通过将剪枝建模为子空间重构问题，利用残差能量引导的贪婪策略保留原始特征的几何结构，并结合文本相关性进行筛选。该方法具有轻量级、模型无关且无需重训的特点，在多个主流模型上显著降低了推理延迟并保持了优异性能。
selection_source: fresh_fetch
motivation: 旨在解决大型视觉语言模型在处理密集视觉Token时面临的高昂计算成本和内存开销问题。
method: 将视觉Token剪枝建模为子空间重构问题，采用残差能量引导的贪婪扩展策略并结合文本相关性进行Token筛选。
result: 在LLaVA和Qwen2.5-VL等多个模型上的实验表明，ResPrune在显著降低计算量和延迟的同时，性能优于现有剪枝方法。
conclusion: ResPrune提供了一种高效、通用且无需训练的视觉Token剪枝方案，能有效提升LVLM的推理效率。
---

## 摘要
大语言视觉模型（LVLMs）依赖密集的视觉 Token 来捕捉细粒度的视觉信息，但在推理过程中处理所有这些 Token 会产生巨大的计算和内存开销。为了解决这一问题，我们提出了 ResPrune，这是一个无需训练的视觉 Token 剪枝框架，通过选择一个紧凑且信息丰富的视觉 Token 子集来实现高效的 LVLM 推理。ResPrune 将视觉 Token 剪枝建模为一个子空间重构问题，并采用一种由残差能量引导的贪婪子空间扩展策略，从而能够保留原始视觉 Token 空间的几何结构。为了进一步结合跨模态对齐，选择过程以文本相关性为条件，鼓励保留既具有信息量又与指令相关的 Token。该方法轻量且与模型无关，可以无缝集成到现有的 LVLM 流线中，无需重新训练或修改架构。在包括 LLaVA-1.5、LLaVA-NeXT 和 Qwen2.5-VL 在内的多个 LVLM 骨干网络上进行的广泛实验表明，ResPrune 在各种基准测试中始终优于现有的剪枝方法，同时有效降低了计算量、内存消耗和推理延迟。

## Abstract
Large Vision-Language Models (LVLMs) rely on dense visual tokens to capture fine-grained visual information, but processing all these tokens incurs substantial computational and memory overhead during inference. To address this issue, we propose ResPrune, a training-free visual token pruning framework that enables efficient LVLM inference by selecting a compact yet informative subset of visual tokens. ResPrune formulates visual token pruning as a subspace reconstruction problem and employs a greedy subspace expansion strategy guided by residual energy, allowing it to preserve the geometric structure of the original visual token space. To further incorporate cross modal alignment, the selection process is conditioned on textual relevance, encouraging the retention of tokens that are both informative and instruction-relevant. The proposed method is lightweight and model-agnostic, and can be seamlessly integrated into existing LVLM pipelines without retraining or architectural modifications. Extensive experiments on multiple LVLM backbones, including LLaVA-1.5, LLaVA-NeXT, and Qwen2.5-VL, demonstrate that ResPrune consistently outperforms existing pruning approaches across a wide range of benchmarks, while achieving effective reductions in computation, memory consumption, and inference latency.

---

## 论文详细总结（自动生成）

### 论文总结：ResPrune - 基于文本条件的子空间重构视觉 Token 剪枝

#### 1. 核心问题与整体含义（研究动机和背景）
现代大型视觉语言模型（LVLMs）为了捕捉细粒度信息，通常将图像编码为成百上千个视觉 Token。然而，LLM 的自注意力机制计算复杂度随序列长度呈平方级增长，密集的视觉 Token 导致了巨大的计算开销和内存占用，限制了其在低延迟或资源受限场景下的应用。
现有的剪枝方法存在局限：
*   **视觉注意力法**：易导致语义冗余，且缺乏指令引导。
*   **跨模态注意力法**：存在位置偏差，且提取注意力权重会破坏 FlashAttention 等优化内核的效率。
*   **多样性法**：通常与文本指令无关，无法针对特定问题保留关键区域。
**ResPrune** 旨在提出一种无需训练、模型无关且能兼顾“视觉覆盖度”与“指令相关性”的通用剪枝框架。

#### 2. 方法论
ResPrune 将视觉 Token 剪枝重新定义为 **LLM 输入嵌入空间中的子空间重构问题**。
*   **核心思想**：选择一个视觉 Token 子集，使其张成的子空间能够最大限度地重构原始全量 Token 空间。
*   **关键技术细节**：
    1.  **残差能量（Residual Energy）**：定义为候选 Token 在当前已选 Token 子空间正交补上的投影长度。残差能量越高，代表该 Token 包含的未被解释的新信息越多。
    2.  **贪婪子空间扩展**：采用迭代策略，每次选择残差能量最大的 Token 加入子集，从而逐步构建能代表全局信息的正交基。
    3.  **文本相关性引导**：利用视觉与文本 Token 在 LVLM 中共享嵌入空间的特性，计算视觉 Token 与文本中名词 Token 的最大余弦相似度，作为权重（Gating Function）调制残差能量。
*   **算法流程**：提取文本名词 -> 计算文本相关性权重 -> 初始化种子 Token -> 迭代选择“残差能量 × 文本权重”最大的 Token -> 通过施密特正交化更新子空间基向量 -> 重复直至达到预设预算。

#### 3. 实验设计
*   **骨干模型**：LLaVA-1.5-7B（固定分辨率）、LLaVA-NeXT-7B（多图块拼接）、Qwen2.5-VL-7B（动态分辨率）。
*   **数据集/基准测试**：涵盖 9 个主流 Benchmark，包括 VQAv2, VizWiz, GQA（通用问答）、SQA-I（科学推理）、TextVQA（场景文本）、POPE（物体幻觉评估）、以及 MME, MMB-en, MM-Vet（综合能力评估）。
*   **对比方法**：对比了包括 ToMe, FastV, PruMerge, SparseVLM, DART, VisionZIP, DivPrune, SCOPE 等在内的十余种近年发表于顶级会议（CVPR, ICCV, NeurIPS 等）的剪枝算法。

#### 4. 资源与算力
*   **训练算力**：该方法为 **Training-free（无需训练）**，因此不涉及模型训练时长和训练算力。
*   **推理测试环境**：实验在 **4 台 NVIDIA L20 GPU** 上进行。
*   **效率提升**：在 LLaVA-NeXT-7B 上，剪枝 66.7% 的 Token 可使吞吐量提升 2.08 倍，评估时间缩短约 49%。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 3 种不同架构的模型上进行了测试，涵盖了从 66.7% 到 88.9% 的多种剪枝比例。
*   **消融实验**：非常充分。分别验证了文本预处理、文本引导机制、子空间重构机制的贡献；探讨了引导强度参数 $\alpha$、文本相关性计算公式、以及初始种子 Token 选择策略的影响。
*   **客观性**：通过在多个异构模型和多样化任务上取得一致的 SOTA 结果，证明了该方法的鲁棒性和公平性。

#### 6. 主要结论与发现
*   **性能保持极佳**：在 LLaVA-1.5 上剪枝 77.8% 的 Token 后，仍能保留 99.3% 的原始性能。
*   **缓解幻觉**：在 POPE 基准上，ResPrune 甚至在某些高剪枝率下超过了全量 Token 的表现，说明剪枝掉无关 Token 有助于减少模型产生误导性关联。
*   **高效性**：ResPrune 的计算复杂度远低于 LLM 的自注意力计算，通过将计算压力从 LLM 转移到轻量级的预处理阶段，实现了显著的端到端加速。

#### 7. 优点
*   **无需训练（Plug-and-Play）**：无需微调模型或修改架构，可直接集成到现有推理管线。
*   **理论支撑强**：相比简单的相似度计算，子空间重构能更本质地捕捉特征空间的几何结构，减少冗余。
*   **指令感知**：通过文本引导确保了剪枝过程是“任务相关”的，避免了纯视觉剪枝丢失关键细节的问题。
*   **兼容性好**：不依赖 LLM 内部注意力权重，完全兼容 FlashAttention 等高性能算子。

#### 8. 不足与局限
*   **超参数敏感性**：文本引导强度 $\alpha$ 在不同模型家族（如 LLaVA vs Qwen）间需要手动微调以达到最优效果。
*   **文本处理依赖**：依赖外部 NLP 工具（如 spaCy）进行名词提取，这增加了推理流程的复杂性，且 NLP 工具的准确性可能影响剪枝效果。
*   **静态权重**：$\alpha$ 在推理过程中是全局固定的，无法根据图像的复杂程度或指令的特异性进行动态自适应调整。

（完）
