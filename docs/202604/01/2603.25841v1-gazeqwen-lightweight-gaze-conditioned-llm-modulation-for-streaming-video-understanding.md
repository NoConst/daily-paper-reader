---
title: "GazeQwen: Lightweight Gaze-Conditioned LLM Modulation for Streaming Video Understanding"
authors: "Trong Thang Pham, Hien Nguyen, Ngan Le"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25841v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大模型的视线条件调制
tldr: "针对多模态大模型在视频理解中难以有效利用视线信息的问题，本文提出 GazeQwen。该方法通过轻量级的视线重采样器（1-5M参数）将视线特征编码为残差，并注入到 LLM 解码器层中。实验表明，GazeQwen 在 StreamGaze 基准测试上达到 63.9% 的准确率，显著优于 GPT-4o 和基于视觉提示的基准模型，证明了隐层调制在视线感知视频理解中的高效性。"
selection_source: fresh_fetch
motivation: 现有的多模态大模型即使通过视觉覆盖或文本描述也无法有效利用视线信息来增强视频理解。
method: 提出一种轻量级的视线重采样器，将视频特征与注视点位置编码结合，通过前向钩子将生成的残差注入 LLM 解码器层。
result: "在 StreamGaze 的 10 项任务中，GazeQwen 取得了 63.9% 的准确率，比 GPT-4o 高出 10.5 个百分点，刷新了开源和闭源模型的纪录。"
conclusion: 实验结果表明，学习在 LLM 内部何处注入视线信息比单纯增加模型规模或优化提示词更为有效。
---

## Abstract
Current multimodal large language models (MLLMs) cannot effectively utilize eye-gaze information for video understanding, even when gaze cues are supplied via visual overlays or text descriptions. We introduce GazeQwen, a parameter efficient approach that equips an open-source MLLM with gaze awareness through hidden-state modulation. At its core is a compact gaze resampler (~1-5 M trainable parameters) that encodes V-JEPA 2.1 video features together with fixation-derived positional encodings and produces additive residuals injected into selected LLM decoder layers via forward hooks. An optional second training stage adds low-rank adapters (LoRA) to the LLM for tighter integration. Evaluated on all 10 tasks of the StreamGaze benchmark, GazeQwen reaches 63.9% accuracy, a +16.1 point gain over the same Qwen2.5-VL-7B backbone with gaze as visual prompts and +10.5 points over GPT-4o, the highest score among all open-source and proprietary models tested. These results suggest that learning where to inject gaze within an LLM is more effective than scaling model size or engineering better prompts. All code and checkpoints are available at https://github.com/phamtrongthang123/gazeqwen .

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **GazeQwen** 的轻量级方法，旨在通过视线（Gaze）引导的隐层调制，提升多模态大语言模型（MLLM）在流式视频理解中的表现。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在增强现实（AR）和具身智能应用中，用户的视线是揭示关注焦点和意图的关键信号。
*   **现有瓶颈**：尽管现有的多模态大模型（如 GPT-4o, Qwen2.5-VL）非常强大，但即使通过文本坐标、视觉覆盖或显著性图提供视线信息，它们在处理视线相关的视频问答（如 StreamGaze 基准测试）时仍远逊于人类。
*   **核心目标**：探索一种比“提示词工程”更有效的方法，让模型在内部表征层面真正“理解”视线信息。

### 2. 方法论
GazeQwen 的核心思想是**隐层状态调制（Hidden-state Modulation）**，即不改变输入格式，而是直接干预 LLM 的内部计算过程。
*   **输入处理**：
    *   **视频特征**：使用预训练且冻结的 **V-JEPA 2.1** 编码器提取时空特征，因其比传统视觉编码器保留了更多细粒度的空间细节。
    *   **视线编码**：将注视点坐标通过 DETR 风格的正弦位置编码（Sinusoidal Positional Encoding）转换为高维向量。
*   **视线条件重采样器（Gaze-Conditioned Resampler）**：
    *   这是一个极小的模块（1-5M 参数），利用交叉注意力机制将视频特征与视线编码融合。
    *   **关键细节**：视线编码被添加到注意力的 **Keys** 中，从而引导模型关注视线附近的视觉 Token，而不改变其原始语义（Values）。
*   **钩子注入（Hook-based Injection）**：
    *   利用 PyTorch 的前向钩子（Forward Hooks），将重采样器生成的残差直接加到 LLM 解码器的特定层（第 6, 13, 20, 27 层）。
*   **两阶段训练**：
    1.  **第一阶段**：仅训练重采样器，冻结 LLM，学习视线对齐。
    2.  **第二阶段**：引入 LoRA 适配器（约 3.5M 参数）与重采样器联合微调，使 LLM 更好地整合注入的信号。

### 3. 实验设计
*   **数据集/基准测试**：**StreamGaze**，包含 285 个第一视角视频、8,521 个视线条件问答对。
*   **任务分类**：涵盖过去（如物体跟踪）、现在（如属性识别）和前瞻性（如物体出现预警）三大类共 10 项任务。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Claude 4 (Sonnet/Opus)。
    *   **开源模型**：Qwen2.5-VL, InternVL3.5, VITA 1.5 等。
    *   **流式模型**：ViSpeak, VideoLLM-online 等。
    *   **专用模型**：AssistGaze。

### 4. 资源与算力
*   **算力需求**：论文强调了该方法的低成本特性，明确提到支持**单 GPU 训练**。
*   **参数量**：可训练参数极少，重采样器约 1-5M，LoRA 约 3.5M，总计约 5-9M 参数。
*   **具体型号**：文中未明确给出具体的 GPU 型号（如 A100 或 H100）及总训练时长，但从参数量和 Epoch 数（20 次）来看，计算开销远低于全参数微调。

### 5. 实验数量与充分性
*   **实验规模**：在 10 个不同的任务维度上进行了全面评估，并与超过 12 种主流模型进行了对比。
*   **消融实验**：针对视线编码方式（坐标 vs 热力图）、视觉主干（V-JEPA vs DINOv2）、层共享策略、以及 LoRA 的必要性进行了 4 组核心消融实验。
*   **客观性**：采用了严格的视频级数据划分（70/15/15），防止了数据泄露，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **性能卓越**：GazeQwen 达到了 **63.9%** 的准确率，比 GPT-4o 高出 **10.5%**，比同底座的视觉提示方法高出 **16.1%**。
*   **空间解歧**：视线信息在“物体属性识别”和“物体出现预警”等需要空间定位的任务中提升最显著（最高提升 34.1%）。
*   **内部调制胜过提示词**：实验证明，在 LLM 内部注入视线信号比在输入端进行复杂的提示词工程或视觉覆盖更有效。

### 7. 优点
*   **轻量高效**：仅需极少量参数即可实现大幅性能提升，适合资源受限场景。
*   **模块化设计**：视线模块可以像插件一样挂载或卸载，无需修改 LLM 核心架构或重新加载权重。
*   **高精度**：通过正弦位置编码保留了亚像素级的视线精度，优于粗糙的视觉热力图。

### 8. 不足与局限
*   **依赖外部视线数据**：该模型的前提是必须有准确的眼动追踪数据输入，在缺乏此类传感器的通用场景下无法使用。
*   **高层逻辑提升有限**：在涉及复杂未来动作预测（FAP）的任务中提升较小（仅 1.7%），说明视线对高层时间推理的帮助有限。
*   **基准单一**：主要在 StreamGaze 一个基准上进行验证，其在更多样化的视频理解任务中的泛化能力有待进一步观察。

（完）
