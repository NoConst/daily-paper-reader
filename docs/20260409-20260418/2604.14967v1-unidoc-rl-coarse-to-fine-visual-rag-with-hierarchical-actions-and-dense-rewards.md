---
title: "UniDoc-RL: Coarse-to-Fine Visual RAG with Hierarchical Actions and Dense Rewards"
title_zh: UniDoc-RL：具有层级化动作和稠密奖励的从粗到细视觉 RAG
authors: "Jun Wang, Shuo Tan, Zelong Sun, Tiancheng Gu, Yongle Zhao, Ziyong Feng, Kaicheng Yang, Cewu Lu"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14967v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 基于分层动作的LVLM智能体视觉RAG
tldr: 针对现有视觉检索增强生成（RAG）系统在处理复杂推理时缺乏细粒度语义的问题，本文提出了UniDoc-RL框架。该框架将视觉信息获取建模为分层决策过程，通过强化学习使LVLM智能体能够执行从文档检索、图像选择到区域裁剪的由粗到精的操作。结合密集多奖励机制和GRPO算法，UniDoc-RL显著提升了模型在复杂视觉推理任务中的表现，在多个基准测试中超越了现有SOTA方法。
selection_source: fresh_fetch
motivation: 现有的视觉RAG系统主要依赖通用检索信号，难以捕捉复杂推理所需的细粒度视觉语义信息。
method: 提出一种统一的强化学习框架，利用分层动作空间实现从文档到区域的由粗到精视觉证据提取，并采用密集多奖励方案进行端到端训练。
result: "在三个基准测试中一致超越现有SOTA模型，相比之前的强化学习方法取得了高达17.7%的性能提升。"
conclusion: UniDoc-RL证明了通过分层决策和密集奖励对LVLM进行对齐，能有效增强其在视觉RAG任务中的细粒度感知与推理能力。
---

## 摘要
检索增强生成 (RAG) 通过外部视觉知识扩展了大视觉语言模型 (LVLMs)。然而，现有的视觉 RAG 系统通常依赖于通用的检索信号，忽略了复杂推理所必需的细粒度视觉语义。为了解决这一局限性，我们提出了 UniDoc-RL，这是一个统一的强化学习框架，其中 LVLM 智能体共同执行检索、重排序、主动视觉感知和推理。UniDoc-RL 将视觉信息获取建模为具有层级化动作空间的序列决策问题。具体而言，它将视觉证据从粗粒度的文档检索逐步细化为细粒度的图像选择和主动区域裁剪，使模型能够抑制无关内容并关注信息密集区域。为了实现有效的端到端训练，我们引入了一种稠密多奖励方案，为每个动作提供任务感知的监督。基于群体相对策略优化 (GRPO)，UniDoc-RL 在不依赖独立价值网络的情况下，使智能体行为与多个目标保持一致。为了支持这种训练范式，我们构建了一个包含细粒度动作标注的高质量推理轨迹综合数据集。在三个基准测试上的实验表明，UniDoc-RL 持续超越了最先进的基准模型，相比之前的基于强化学习的方法，性能提升高达 17.7%。

## Abstract
Retrieval-Augmented Generation (RAG) extends Large Vision-Language Models (LVLMs) with external visual knowledge. However, existing visual RAG systems typically rely on generic retrieval signals that overlook the fine-grained visual semantics essential for complex reasoning. To address this limitation, we propose UniDoc-RL, a unified reinforcement learning framework in which an LVLM agent jointly performs retrieval, reranking, active visual perception, and reasoning. UniDoc-RL formulates visual information acquisition as a sequential decision-making problem with a hierarchical action space. Specifically, it progressively refines visual evidence from coarse-grained document retrieval to fine-grained image selection and active region cropping, allowing the model to suppress irrelevant content and attend to information-dense regions. For effective end-to-end training, we introduce a dense multi-reward scheme that provides task-aware supervision for each action. Based on Group Relative Policy Optimization (GRPO), UniDoc-RL aligns agent behavior with multiple objectives without relying on a separate value network. To support this training paradigm, we curate a comprehensive dataset of high-quality reasoning trajectories with fine-grained action annotations. Experiments on three benchmarks demonstrate that UniDoc-RL consistently surpasses state-of-the-art baselines, yielding up to 17.7% gains over prior RL-based methods.

---

## 论文详细总结（自动生成）

### 论文总结：UniDoc-RL —— 具有层级化动作和稠密奖励的从粗到细视觉 RAG

#### 1. 核心问题与整体含义（研究动机和背景）
传统的视觉检索增强生成（Visual RAG）系统主要面临三个挑战：
*   **检索精度不足**：现有的检索器多依赖通用的相似度评分，难以捕捉复杂推理所需的细粒度语义，导致引入无关背景或产生幻觉。
*   **视觉利用率低**：模型通常被动接收整张图像，而视觉文档（如报表、图表）包含大量冗余信息，模型难以聚焦于关键局部区域。
*   **优化困难（信用分配问题）**：现有的强化学习（RL）方法多采用稀疏的最终结果奖励，无法有效指导检索、筛选和裁剪等中间决策步骤。
**UniDoc-RL** 旨在通过一个统一的 RL 框架，将视觉 RAG 建模为“搜索-筛选-感知-推理”的序列决策过程，实现从粗到精的证据提取。

#### 2. 方法论：核心思想与关键技术
UniDoc-RL 将 LVLM 智能体置于一个“思考-动作-观察”的循环中，核心技术包括：
*   **层级化动作空间（Hierarchical Action Space）**：
    1.  **Image Search**：调用外部工具进行粗粒度文档检索。
    2.  **Precise Selection**：由 LVLM 对候选图进行语义重排，过滤无关文档。
    3.  **Visual Perception**：通过主动裁剪（Crop）和缩放（Zoom）操作，聚焦于高价值的局部区域。
*   **稠密多奖励机制（Dense Multi-Reward Scheme）**：
    *   **检索奖励 ($r_{ir}$)**：基于 NDCG 评估搜索结果的质量。
    *   **筛选奖励 ($r_{sel}$)**：评估 LVLM 选择正确证据图的准确性。
    *   **感知奖励 ($r_{crop}$)**：基于预测框与真实区域的交并比（IoU）给予反馈。
    *   **模式奖励 ($r_{pat}$)**：确保模型遵循预定义的 XML 标签格式。
    *   **结果奖励 ($rans$)**：由奖励模型（RM）评估最终答案的正确性。
*   **优化算法**：采用 **GRPO（群体相对策略优化）**，在不使用独立价值网络的情况下，通过组内相对优势流对齐多个目标。
*   **数据合成**：利用 Qwen3-VL-235B 作为教师模型，结合 Mineru 布局分析工具，自动合成高质量的推理轨迹数据集。

#### 3. 实验设计
*   **数据集/场景**：
    *   **SlideVQA**：多图幻灯片问答，侧重多跳推理。
    *   **ViDoSeek**：视觉文档搜索与推理。
    *   **MMLongBench**：长上下文文档理解，涵盖图表、表格等多种视觉类型。
*   **基准测试（Baselines）**：
    *   **Vanilla RAG**：直接检索并推理。
    *   **ReAct**：迭代式的“思考-行动”框架。
    *   **Search-R1(-VL)**：基于 RL 的搜索增强基准。
    *   **VRAG-RL**：之前最先进的具备视觉感知能力的 RL RAG 方法。
*   **对比维度**：区分了基于 OCR 的文本 RAG 和纯视觉 RAG。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 80G GPU**。
*   **训练细节**：
    *   **SFT 阶段**：全参数微调，余弦学习率调度，Warmup 比例 0.1。
    *   **RL 阶段**：使用 verl 框架，组大小（Group Size）设为 5，Actor 学习率为 1.0e-6。
    *   **时长**：文中未明确给出具体的训练总时长，但提到了 SFT 训练 3 个 Epoch，RL 训练 1 个 Epoch。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流基准上进行了测试，涵盖了 3B 和 7B 两种参数规模的模型（基于 Qwen2.5-VL）。
*   **消融实验**：
    *   针对**动作空间**进行了消融，验证了“筛选”和“感知”动作的独立贡献。
    *   针对**奖励函数**进行了增量消融，证明了稠密奖励优于单一的结果奖励。
*   **定性分析**：提供了案例研究（Case Study）和动作频率分析（如 RL 如何提升裁剪动作的频率和精度）。
*   **评价**：实验设计较为全面，对比了多种 SOTA 方法，且通过消融实验清晰地展示了各模块的有效性，实验结果具有较强的说服力。

#### 6. 主要结论与发现
*   **性能领先**：UniDoc-RL 在所有基准上均显著优于现有方法，相比之前的 RL 方法（VRAG-RL），在 3B 和 7B 模型上分别取得了 **17.5% 和 17.7%** 的大幅提升。
*   **RL 的作用**：RL 不仅提升了最终答案的准确率，还显著改善了中间行为。例如，RL 训练后的模型更倾向于执行“精确裁剪”而非“平庸裁剪”。
*   **筛选的重要性**：LVLM 驱动的筛选动作能有效弥补粗粒度检索器的不足，显著提升证据的召回率。

#### 7. 优点
*   **系统性强**：将检索、筛选、感知和推理整合进一个统一的决策流，而非零散的模块堆叠。
*   **奖励设计精巧**：通过 NDCG 和 IoU 等指标将中间步骤显式监督化，解决了 RL 在长链条任务中的信用分配难题。
*   **高质量数据贡献**：发布了包含细粒度动作标注的推理轨迹数据集，对社区具有参考价值。

#### 8. 不足与局限
*   **推理成本**：由于引入了多轮“思考-动作”循环（如搜索、筛选、裁剪），系统的推理延迟（Latency）和 Token 消耗会高于单次推理的 RAG。
*   **教师模型依赖**：训练数据的质量高度依赖于强大的教师模型（如 Qwen3-VL-235B），对于缺乏此类资源的研究者来说，复现或扩展数据具有挑战性。
*   **场景局限**：目前主要集中在文档类图像（Slides, Reports），对于自然场景下的视觉 RAG 效果尚待验证。

（完）
