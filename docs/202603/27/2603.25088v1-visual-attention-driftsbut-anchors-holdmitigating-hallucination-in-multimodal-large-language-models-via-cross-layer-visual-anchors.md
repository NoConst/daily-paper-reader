---
title: "Visual Attention Drifts,but Anchors Hold:Mitigating Hallucination in Multimodal Large Language Models via Cross-Layer Visual Anchors"
title_zh: 视觉注意力在漂移，但锚点依然稳固：通过跨层视觉锚点缓解多模态大语言模型中的幻觉
authors: "Chengxu Yang, Jingling Yuan, Chuang Hu, Jiawei Jiang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25088v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 缓解多模态大模型中的幻觉问题
tldr: 针对多模态大语言模型中的物体幻觉问题，本文研究发现幻觉源于深层注意力向早期视觉噪声的回归。为此提出了CLVA（跨层视觉锚点），这是一种无需训练的方法，通过强化中间层的关键视觉锚点并抑制深层回归噪声，有效纠正注意力偏移。实验证明，该方法在多种架构和基准测试中均能显著减少幻觉，且不增加计算负担。
selection_source: fresh_fetch
motivation: 现有研究缺乏对模型后期注意力偏移的解释，且深层注意力往往会退化回初始视觉噪声导致幻觉产生。
method: 提出无需训练的CLVA方法，利用中间层捕获的视觉锚点来强化关键特征并抑制深层的回归噪声。
result: 在多种模型架构和基准测试上均取得了优异表现，且几乎不增加额外的计算时间和显存消耗。
conclusion: 通过跨层视觉锚点纠正深层注意力偏移是缓解多模态模型幻觉的一种高效、通用且具有可解释性的方案。
---

## 摘要
多模态大语言模型（MLLMs）经常遭受物体幻觉的困扰。虽然现有研究利用注意力增强和视觉回溯，但我们发现这些工作在模型最终阶段的注意力漂移方面缺乏足够的解释性。在本文中，我们研究了视觉特征的逐层演变，并发现幻觉源于深层注意力向早期层的初始视觉噪声回归。我们观察到，输出的可靠性取决于在中间层而非最终层获取视觉锚点。基于这些见解，我们提出了 CLVA（Cross-Layer Visual Anchors，跨层视觉锚点），这是一种无需训练的方法，它在抑制回归噪声的同时增强了关键的中间层特征。该方法通过利用从注意力动态中捕获的关键锚点，有效地将深层注意力拉回到正确的视觉区域。我们在多种架构和基准测试上评估了我们的方法，证明了其在不显著增加计算时间和 GPU 显存的情况下具有出色的性能。

## Abstract
Multimodal Large Language Models often suffer from object hallucination. While existing research utilizes attention enhancement and visual retracing, we find these works lack sufficient interpretability regarding attention drift in final model stages. In this paper, we investigate the layer wise evolution of visual features and discover that hallucination stems from deep layer attention regressing toward initial visual noise from early layers. We observe that output reliability depends on acquiring visual anchors at intermediate layers rather than final layers. Based on these insights, we propose CLVA, which stands for Cross-Layer Visual Anchors, a training free method that reinforces critical mid layer features while suppressing regressive noise. This approach effectively pulls deep layer attention back to correct visual regions by utilizing essential anchors captured from attention dynamics. We evaluate our method across diverse architectures and benchmarks, demonstrating outstanding performance without significant increase in computational time and GPU memory.

---

## 论文详细总结（自动生成）

这是一份关于论文《Visual Attention Drifts, but Anchors Hold: Mitigating Hallucination in Multimodal Large Language Models via Cross-Layer Visual Anchors》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：多模态大语言模型（MLLM）在生成描述时经常出现“物体幻觉”（即生成图像中不存在的物体）。
*   **核心问题**：现有研究虽尝试通过注意力增强或对比解码来缓解幻觉，但缺乏对模型内部注意力演变机制的深入解释。
*   **研究动机**：作者通过可视化发现，模型在中间层（Mid-layers）其实能精准锁定目标区域，但在深层（Deep layers）注意力会发生“漂移”，退化回早期层（Early layers）的初始视觉噪声。本文旨在利用中间层的“视觉锚点”来纠正深层的注意力偏差。

### 2. 方法论
*   **核心思想**：提出 **CLVA（跨层视觉锚点）** 方法。这是一种**无需训练（Training-free）**的即插即用模块，通过强化中间层的正向视觉特征并抑制早期层的负向噪声，引导深层注意力回归正确区域。
*   **关键技术细节**：
    1.  **功能头分类**：根据注意力强度，将注意力头分为“视觉敏感头”（关注图像信息）和“视觉不敏感头”（关注语言偏见或噪声）。
    2.  **双锚点提取**：
        *   **正向锚点（Positive Anchor）**：从中间层的敏感头中提取，代表准确的语义区域。
        *   **负向锚点（Negative Anchor）**：从初始层的不敏感头中提取，代表固有的空间噪声。
    3.  **注意力重锚定（Attention Re-anchoring）**：在深层推理阶段，利用双锚点生成的掩码对注意力矩阵进行修正：
        $$\tilde{A} = A \cdot (1 + \alpha Z_{pos} - \beta Z_{neg})$$
        其中 $\alpha$ 为增强因子，$\beta$ 为惩罚因子。修正后进行重新归一化，以确保概率分布的有效性。

### 3. 实验设计
*   **数据集/场景**：
    *   **POPE**：评估物体存在性的判别式基准（包含 MSCOCO, AOKVQA, GQA 数据集）。
    *   **CHAIR**：评估图像标题生成中的物体幻觉率。
    *   **MME**：综合评估模型的感知和认知能力。
    *   **LLaVA-Bench**：评估复杂开放场景下的表现。
*   **对比方法**：对比了 **IMCCD**（对比解码）、**MemVR**（视觉回溯）、**ClearSight**（视觉增强）等当前最先进的无需训练的方法。
*   **测试模型**：LLaVA-1.5-7B、Qwen-VL-7B、InstructBLIP-7B。

### 4. 资源与算力
*   **硬件环境**：实验在单张 **NVIDIA H20 GPU** 上完成。
*   **效率表现**：论文明确提到 CLVA 相比 IMCCD 具有显著优势。在相同测试下，CLVA 的显存占用仅为 IMCCD 的约 **1/2**，推理速度快约 **2 倍**。
*   **训练时长**：由于是无需训练的方法，该方案不涉及训练时长，仅增加极微小的推理延迟（Latency 增加约 0.002s-0.02s）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同的模型架构（线性投影、Q-Former 等），在 3 个主流基准测试的多个子集上进行了详尽测试。
*   **消融实验**：作者针对正向锚点、负向锚点的有效性，以及超参数 $\alpha$ 和 $\beta$ 的敏感度进行了专门的消融研究。
*   **充分性评价**：实验设计较为全面，不仅验证了幻觉的缓解，还通过 MME 全集验证了模型通用能力的保持，实验结果具有较强的客观性和说服力。

### 6. 主要结论与发现
*   **注意力漂移规律**：证实了 MLLM 的幻觉源于深层注意力向早期层噪声的系统性回归。
*   **中间层的重要性**：中间层是模型获取可靠视觉证据的关键阶段，而非最终层。
*   **CLVA 的有效性**：CLVA 在不改变模型参数的情况下，显著提升了 POPE 的 F1 分数（最高提升 7.13%）并降低了 CHAIR 的幻觉率，同时保持了良好的推理效率。

### 7. 优点
*   **高解释性**：通过层级注意力分析，为幻觉产生提供了清晰的机械论解释。
*   **极低开销**：无需多轮前向计算（不同于对比解码），显存和计算量增加几乎可以忽略不计。
*   **通用性强**：支持多种 MLLM 架构（如 LLaVA 和 InstructBLIP），具有良好的适配性。

### 8. 不足与局限
*   **超参数敏感性**：虽然提供了默认值，但 $\alpha$ 和 $\beta$ 的最优取值可能随模型架构的变化而波动，需要一定的调优。
*   **任务偏差**：在某些高度依赖语言逻辑而非视觉引导的任务（如数值计算、文本翻译）中，强制引入视觉锚点可能会导致性能轻微下降。
*   **模型规模覆盖**：实验主要集中在 7B 级别的模型，对于更大规模（如 70B+）或更小规模模型的泛化效果有待进一步验证。

（完）
