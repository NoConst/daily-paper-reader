---
title: "BALM: A Model-Agnostic Framework for Balanced Multimodal Learning under Imbalanced Missing Rates"
title_zh: BALM：一种针对不平衡缺失率下平衡多模态学习的模型无关框架
authors: "Phuong-Anh Nguyen, Tien Anh Pham, Duc-Trong Le, Cam-Van Thi Nguyen"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19718v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 用于跨模态表示与融合的平衡多模态学习框架
tldr: 针对多模态学习中因各模态缺失率不均衡（IMR）导致的优化失衡问题，本文提出了一种模型无关的插件式框架BALM。该框架包含特征校准模块（FCM）以统一异构缺失模式下的特征表示，以及梯度重平衡模块（GRM）从分布和空间维度调节学习动态。实验表明，BALM能显著增强多模态情感识别模型在复杂缺失场景下的鲁棒性，且无需修改原有架构即可无缝集成。
selection_source: fresh_fetch
motivation: 解决多模态学习中由于不同模态缺失概率不一致导致的表示学习扭曲和梯度优化主导权失衡问题。
method: 提出由特征校准模块（FCM）和梯度重平衡模块（GRM）组成的通用框架，分别实现特征基准统一与梯度动态调节。
result: 在多个多模态情感识别基准上，BALM在多种缺失和不平衡配置下均显著提升了模型的性能与鲁棒性。
conclusion: BALM提供了一种有效的模型无关方案，能够平衡不均衡缺失率下的多模态学习，具有极强的通用性和实用价值。
---

## 摘要
从多个模态中学习通常会面临不平衡问题，即信息丰富的模态主导优化过程，而较弱或部分缺失的模态贡献较少。在具有不平衡缺失率（IMR）的现实场景中，这种不平衡变得更加严重，其中每个模态以不同的概率缺失，从而扭曲了表示学习和梯度动力学。我们从训练过程的角度重新审视这一问题，并提出了 BALM，这是一个模型无关的插件式框架，旨在实现 IMR 下的平衡多模态学习。该框架包含两个互补模块：特征校准模块（FCM），利用全局上下文重新校准单模态特征，以在异构缺失模式之间建立共享的表示基础；梯度重平衡模块（GRM），通过从分布和空间两个维度调节梯度的大小和方向，来平衡各模态之间的学习动力学。BALM 可以无缝集成到包括多模态情感识别（MER）模型在内的各种骨干网络中，而无需改变其架构。在多个 MER 基准测试上的实验结果证实，BALM 在各种缺失和不平衡设置下都能持续增强鲁棒性并提高性能。代码可在以下网址获取：https://github.com/np4s/BALM_CVPR2026.git。

## Abstract
Learning from multiple modalities often suffers from imbalance, where information-rich modalities dominate optimization while weaker or partially missing modalities contribute less. This imbalance becomes severe in realistic settings with imbalanced missing rates (IMR), where each modality is absent with different probabilities, distorting representation learning and gradient dynamics. We revisit this issue from a training-process perspective and propose BALM, a model-agnostic plug-in framework to achieve balanced multimodal learning under IMR. The framework comprises two complementary modules: the Feature Calibration Module (FCM), which recalibrates unimodal features using global context to establish a shared representation basis across heterogeneous missing patterns; the Gradient Rebalancing Module (GRM), which balances learning dynamics across modalities by modulating gradient magnitudes and directions from both distributional and spatial perspectives. BALM can be seamlessly integrated into diverse backbones, including multimodal emotion recognition (MER) models, without altering their architectures. Experimental results across multiple MER benchmarks confirm that BALM consistently enhances robustness and improves performance under diverse missing and imbalance settings. Code available at: https://github.com/np4s/BALM_CVPR2026.git

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **BALM**（Balanced Agnostic Learning under Imbalanced Missing Rates）的框架，旨在解决多模态学习在现实场景中面临的模态缺失率不均衡（IMR）问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统多模态学习假设所有模态在训练中完整可用，或者假设各模态缺失概率相同（Shared Missing Rate, SMR）。但在现实中，由于传感器质量、环境噪声或采集成本，不同模态的缺失率往往是不均衡的（IMR）。
*   **研究动机**：IMR 会导致两个层面的失衡：
    1.  **表示失衡**：不同缺失模式扭曲了单模态特征分布，阻碍了跨模态融合。
    2.  **学习失衡**：缺失率低的模态会主导梯度更新，导致缺失率高的模态学习不足，模型陷入偏见平衡，鲁棒性差。
*   **整体含义**：BALM 作为一个“模型无关”的插件框架，旨在不改变原有模型架构的前提下，通过校准特征和重平衡梯度来提升模型在复杂缺失环境下的性能。

### 2. 方法论：核心思想与关键技术
BALM 由两个互补模块组成，分别从表示和优化层面进行干预：
*   **特征校准模块 (FCM)**：
    *   **核心思想**：利用全局上下文信息来补偿缺失模态造成的表示偏差。
    *   **技术细节**：计算所有可用模态的全局描述符（Global Descriptor），通过全连接层提取跨模态上下文，生成模态特定的校准权重（类似 Squeeze-and-Excitation 机制），对单模态特征进行门控缩放，从而在异构缺失模式间建立统一的表示基准。
*   **梯度重平衡模块 (GRM)**：
    *   **核心思想**：动态调节各模态编码器的梯度，确保弱势模态（缺失率高）获得足够的监督。
    *   **分布驱动调节**：计算单模态预测与多模态预测之间的 KL 散度，衡量各模态的学习进度。学习慢的模态（散度大且变化慢）会被赋予更大的梯度增益系数 $\mu_m$。
    *   **空间驱动调节**：计算单模态梯度与全局梯度之间的余弦相似度，通过辅助损失函数 $L_{mod}$ 强制各模态的优化方向向平衡的多模态方向对齐。
*   **算法流程**：在训练每一步中，先通过 FCM 校准特征，再进行前向传播计算任务损失，随后 GRM 根据预测分布和梯度方向动态调整各模态编码器的参数更新。

### 3. 实验设计
*   **数据集**：使用了两个标准的多模态情感识别（MER）数据集：**IEMOCAP**（6类情感）和 **CMU-MOSEI**（情感倾向回归/分类）。
*   **Benchmark 设置**：模拟了多种 IMR 配置，例如缺失率组合 $(r_A, r_L, r_V) \in \{0.3, 0.5, 0.7\}$ 的各种排列。
*   **对比方法**：
    *   **缺失/不平衡处理模型**：MMIN, SDR-GNN, Mi-CGA, MoMKE, GCNet, Ada2I, RedCore, MCE。
    *   **典型 MER 骨干网络**：MMGCN, MMDFN。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用 PyTorch 实现，通过 Comet.ml 跟踪实验，提供了 GitHub 代码库。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个数据集上分别测试了 6 种不同的 IMR 组合。
    *   进行了 SMR（共享缺失率）下的鲁棒性对比实验。
    *   针对极端缺失情况（如 90% 缺失）进行了压力测试。
    *   **消融实验**：分别验证了 FCM 和 GRM（及其子组件）的有效性，并分析了不同模态组合下的表现。
*   **充分性评价**：实验设计非常充分。通过将 BALM 集成到多种不同类型的基准模型中，证明了其“模型无关”的通用性。对比实验涵盖了从传统模型到最新专门针对 IMR 设计的模型，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：BALM 持续提升了所有骨干网络在 IMR 场景下的表现，在某些配置下（如 IEMOCAP）相比原模型提升了超过 3% 的准确率。
*   **模态敏感性**：实验发现文本（Lexical）模态虽然判别力最强，但也最容易受到缺失的影响；BALM 能有效缓解这种敏感性。
*   **优化稳定性**：GRM 成功提高了单模态梯度与全局梯度之间的余弦相似度，使训练过程更加协调。
*   **极端鲁棒性**：即使在某模态缺失率高达 90% 的情况下，BALM 依然能保持模型性能的相对稳定。

### 7. 优点（亮点）
*   **模型无关性**：作为插件使用，不需要重新设计复杂的生成模型或对齐架构，易于部署到现有系统。
*   **双重平衡机制**：同时从特征表示（FCM）和梯度优化（GRM）两个维度解决问题，比单一的重采样或损失加权更全面。
*   **理论支撑**：论文对 IMR 导致的梯度偏差进行了数学推导（Lemma 1），为 GRM 的设计提供了理论依据。

### 8. 不足与局限
*   **领域局限**：实验主要集中在情感识别（MER）任务上，虽然该任务具有代表性，但在自动驾驶、医疗影像等其他多模态领域的泛化能力尚待验证。
*   **超参数敏感性**：GRM 引入了调节强度 $\rho$ 和权衡系数 $\tau$ 等超参数，实验显示在不同缺失配置下，最优超参数的选择存在差异，可能需要针对特定场景进行调优。
*   **计算开销**：虽然框架轻量，但引入了额外的单模态预测头和梯度相似度计算，在超大规模模态或极深网络中可能会带来一定的训练开销。

（完）
