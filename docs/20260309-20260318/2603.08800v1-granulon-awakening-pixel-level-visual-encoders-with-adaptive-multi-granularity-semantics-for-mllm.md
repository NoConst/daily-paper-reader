---
title: "Granulon: Awakening Pixel-Level Visual Encoders with Adaptive Multi-Granularity Semantics for MLLM"
title_zh: Granulon：通过自适应多粒度语义唤醒多模态大语言模型的像素级视觉编码器
authors: "Junyuan Mao, Qiankun Li, Linghao Meng, Zhicheng He, Xinliang Zhou, Kun Wang, Yang Liu, Yueming Jin"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08800v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 基于DINOv3的具有自适应粒度增强的多模态大模型
tldr: 针对多模态大模型中CLIP编码器缺乏细粒度理解和DINOv3缺乏高层语义抽象的问题，本文提出Granulon。该模型基于DINOv3，引入文本驱动的粒度控制器和自适应Token聚合模块，实现了根据文本需求动态调整视觉抽象级别。Granulon支持从像素到粗粒度的统一推理，显著提升了模型在复杂视觉任务中的准确性并减少了幻觉。
selection_source: fresh_fetch
motivation: 现有的视觉编码器在全局语义对齐与像素级感知之间存在断层，难以兼顾多粒度的视觉推理需求。
method: 提出Granulon架构，通过文本驱动的粒度控制器和自适应Token聚合模块，实现动态的视觉抽象与语义增强。
result: "实验表明Granulon在相同设置下将准确率提升了约30%，并减少了约20%的幻觉现象。"
conclusion: Granulon成功唤醒了像素级编码器的多粒度语义能力，为构建更精准、低幻觉的多模态大模型提供了有效方案。
---

## 摘要
最近多模态大语言模型（MLLM）的进展在很大程度上依赖于基于 CLIP 的视觉编码器，这类编码器强调全局语义对齐，但在细粒度视觉理解方面表现欠佳。相比之下，DINOv3 提供了强大的像素级感知能力，但缺乏粗粒度语义抽象，导致多粒度推理能力有限。为了填补这一空白，我们提出了 Granulon，一种基于 DINOv3 且具有自适应粒度增强的新型 MLLM。Granulon 引入了一个文本条件的粒度控制器，可根据文本输入的语义范围动态调整视觉抽象级别；此外还引入了一个自适应 Token 聚合模块，通过粒度引导的池化和关系感知聚类来生成紧凑且语义丰富的视觉 Token。这种设计使得在单次前向传播中实现统一的“像素到细粒度到粗粒度”推理成为可能。广泛且可解释的实验表明，Granulon 将准确率提高了约 30%，并将幻觉减少了约 20%，在相同设置下优于所有视觉编码器。

## Abstract
Recent advances in multimodal large language models largely rely on CLIP-based visual encoders, which emphasize global semantic alignment but struggle with fine-grained visual understanding. In contrast, DINOv3 provides strong pixel-level perception yet lacks coarse-grained semantic abstraction, leading to limited multi-granularity reasoning. To address this gap, we propose Granulon, a novel DINOv3-based MLLM with adaptive granularity augmentation. Granulon introduces a text-conditioned granularity Controller that dynamically adjusts the visual abstraction level according to the semantic scope of the textual input, and an Adaptive Token Aggregation module that performs granularity-guided pooling and relation-aware clustering to produce compact, semantically rich visual tokens. This design enables unified "pixel-to-fine-to-coarse" reasoning within a single forward pass. Extensive and interpretable experiments demonstrate that Granulon improves accuracy by ~30% and reduces hallucination by ~20%, outperforming all visual encoders under identical settings.

---

## 论文详细总结（自动生成）

这是一份关于论文《Granulon: Awakening Pixel-Level Visual Encoders with Adaptive Multi-Granularity Semantics for MLLM》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）目前主要依赖 **CLIP** 类视觉编码器，其优势在于全局语义对齐，但在处理需要细粒度理解的任务（如局部纹理、几何细节）时表现较差。相反，**DINOv3** 等像素级自监督编码器虽擅长捕捉细节，却缺乏高层语义抽象能力。
论文的核心问题在于：**如何让像素级编码器（如 DINOv3）具备任务自适应的语义粒度，从而在单一模型中统一“像素-细粒度-粗粒度”的推理能力？** 

### 2. 方法论：核心思想与关键技术
论文提出了 **Granulon** 架构，其核心思想是通过文本指令动态控制视觉特征的抽象程度。
*   **文本驱动的粒度控制器（Granularity Controller）：** 利用 LLM 的前几层提取问题的语义特征，预测最适合该问题的视觉粒度参数（如空间下采样比例 $\alpha$ 和聚类基数 $\beta$）。例如，宏观问题触发粗粒度模式，细节问题触发细粒度模式。
*   **自适应 Token 聚合（AdaTA）：** 
    1.  **粒度引导池化：** 根据控制器输出调整特征图分辨率。
    2.  **关系感知聚类：** 使用 mini-k-means 算法，结合空间位置和注意力权重，将相似的像素 Token 聚类为语义原型。
    3.  **特征精炼与选择：** 基于空间支持度、语义同质性和分布离散度对聚类进行评分，保留最富信息的 Top-K 个语义 Token。
*   **多粒度融合：** 将原始像素级 Token 与 AdaTA 生成的语义 Token 拼接，共同输入 LLM 进行推理。

### 3. 实验设计
*   **数据集与场景：**
    *   **VQA（视觉问答）：** SEED-Bench, A-OKVQA。
    *   **图像描述：** CC12M, Imagenet21K Recap。
    *   **复杂推理：** FLUX-Reason（百万级文本-图像推理数据集）。
    *   **医疗领域：** SurgVLM（手术阶段与器械识别），用于验证跨域泛化性。
*   **Benchmark 与对比方法：** 在 LLaVA 框架下，保持 LLM 后端（Qwen-2.5-1.5B 和 Llama-3.2-3B）一致，对比了 **CLIP (ViT-L/14)**、**SigLIP**、**DINOv2** 和 **DINOv3**。

### 4. 资源与算力
*   **硬件：** 使用了 **8 × NVIDIA H200 GPU**。
*   **训练细节：** 训练 2 个 epoch，全局 Batch Size 为 128，学习率为 $2 \times 10^{-5}$。
*   **策略：** 冻结视觉编码器和 LLM 主干，仅训练多模态投影器（Projector）和粒度控制器。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 5 个主流基准测试和 1 个特定领域（医疗）测试。
*   **消融实验：** 详细探讨了语义 Token 粒度对不同任务的影响、粒度控制器的效率与准确率权衡。
*   **公平性：** 实验在“相同设置（Identical Settings）”下进行，即模型参数量（视觉端约 0.4B）、训练数据和优化参数完全一致，确保了对比的客观性。
*   **可解释性分析：** 进行了层级对齐分析（Layer-wise Alignment），通过余弦相似度证明了 Granulon 在深层推理中比 CLIP 更能保持语义一致性。

### 6. 主要结论与发现
*   **性能飞跃：** Granulon 在各项指标上均优于基准模型，准确率提升约 **30%**。
*   **幻觉抑制：** 相比 CLIP 和 DINOv3，Granulon 的幻觉率降低了约 **20%**。
*   **粒度相关性：** 实验发现，粗粒度抽象有利于全局理解（如 A-OKVQA），而细粒度抽象对复杂推理（如 FLUX-Reason）至关重要。
*   **对齐能力：** 像素级编码器通过自适应粒度增强后，能更好地支持 LLM 的层级计算需求。

### 7. 优点（亮点）
*   **动态自适应：** 改变了以往固定视觉 Token 的做法，实现了“按需分配”视觉抽象级别。
*   **单次前向传播：** 无需多编码器融合或多次推理，计算效率较高。
*   **鲁棒性强：** 在医疗等专业领域表现出极佳的泛化能力，证明了像素级特征的底蕴。
*   **解释性工具：** 引入了层级状态对齐分析，直观展示了视觉特征如何随 LLM 深度增加而演化。

### 8. 不足与局限
*   **控制器依赖：** 粒度控制器的训练依赖于 GPT-4o 标注的“文本-粒度”语料，这可能引入教师模型的偏见。
*   **Token 开销：** 虽然 AdaTA 进行了聚合，但相比纯像素 Token，额外的语义 Token 仍增加了约 10% 的序列长度。
*   **细粒度瓶颈：** 尽管减少了幻觉，但在极少数极其微小的视觉概念上，若编码器本身未学习到相关特征，模型仍会失效。

（完）
