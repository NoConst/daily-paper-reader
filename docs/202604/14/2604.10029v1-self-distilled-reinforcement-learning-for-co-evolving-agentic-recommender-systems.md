---
title: Self-Distilled Reinforcement Learning for Co-Evolving Agentic Recommender Systems
title_zh: 面向协同演化智能体推荐系统的自蒸馏强化学习
authors: "Zongwei Wang, Min Gao, Hongzhi Yin, Junliang Yu, Tong Chen, Shazia Sadiq, Tianrui Li"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10029v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 具有文本记忆的智能体推荐系统
tldr: 针对现有智能体推荐系统（ARS）依赖外部记忆而缺乏参数化学习能力，以及强化学习中监督信号稀疏的问题，本文提出CoARS框架。该框架通过协同演化机制让推荐与用户智能体在交互中共同进化，并引入自蒸馏信用分配机制，将历史轨迹转化为Token级细粒度信号，显著提升了推荐性能与用户对齐度。
selection_source: fresh_fetch
motivation: 现有ARS主要依赖外部文本记忆而非参数化学习，且传统强化学习难以捕捉智能体间的交互演化及多轮对话中的密集监督信号。
method: 提出CoARS框架，包含为双智能体提供耦合任务级监督的交互奖励机制，以及基于教师-学生模式将历史轨迹转化为Token级信用信号的自蒸馏机制。
result: 在多个数据集上的实验证明，CoARS在推荐准确性及用户意图对齐方面均优于现有的代表性智能体推荐基准模型。
conclusion: 通过自蒸馏强化学习实现智能体的协同演化，能有效将交互经验内化为模型能力，是提升多轮交互推荐系统性能的有效途径。
---

## 摘要
大语言模型赋能的智能体推荐系统（ARS）将推荐重新定义为推荐智能体与用户智能体之间的多轮交互，从而实现了超越传统单次预测的迭代式偏好诱导与细化。然而，现有的 ARS 主要在 Reflexion 式范式下进行优化，即通过将过去的交互轨迹存储为文本记忆，并将其检索为提示词上下文以进行后续推理。尽管这种设计允许智能体召回先前的反馈和观察结果，但积累的经验仍处于模型参数之外，导致智能体依赖于通用推理，而无法通过学习逐步获得特定于推荐的决策能力。因此，强化学习（RL）提供了一种将此类交互经验内化到参数中的自然方式。然而，现有的 ARS 强化学习方法仍面临两个关键局限：首先，它们未能捕捉到 ARS 的交互本质，即推荐智能体和用户智能体持续相互影响，并能通过交互反馈自然地产生内生监督；其次，它们将丰富的多轮交互过程简化为最终结果，忽略了嵌入在整个轨迹中的密集监督信号。为此，我们提出了 CoARS，一种用于协同演化智能体推荐系统的自蒸馏强化学习框架。CoARS 引入了两种互补的学习方案：交互奖励（interaction reward），从同一交互轨迹中为推荐智能体和用户智能体推导出耦合的任务级监督；以及自蒸馏信用分配（self-distilled credit assignment），在教师-学生条件化下将历史轨迹转化为标记级（token-level）信用信号。在多个数据集上的实验表明，CoARS 在推荐性能和用户对齐方面均优于代表性的 ARS 基准模型。

## Abstract
Large language model-empowered agentic recommender systems (ARS) reformulate recommendation as a multi-turn interaction between a recommender agent and a user agent, enabling iterative preference elicitation and refinement beyond conventional one-shot prediction. However, existing ARS are mainly optimized in a Reflexion-style paradigm, where past interaction trajectories are stored as textual memory and retrieved as prompt context for later reasoning. Although this design allows agents to recall prior feedback and observations, the accumulated experience remains external to model parameters, leaving agents reliant on generic reasoning rather than progressively acquiring recommendation-specific decision-making ability through learning. Reinforcement learning (RL) therefore provides a natural way to internalize such interaction experience into parameters. Yet existing RL methods for ARS still suffer from two key limitations. First, they fail to capture the interactive nature of ARS, in which the recommender agent and the user agent continuously influence each other and can naturally generate endogenous supervision through interaction feedback. Second, they reduce a rich multi-turn interaction process to final outcomes, overlooking the dense supervision embedded throughout the trajectory. To this end, we propose CoARS, a self-distilled reinforcement learning framework for co-evolving agentic recommender systems. CoARS introduces two complementary learning schemes: interaction reward, which derives coupled task-level supervision for the recommender agent and the user agent from the same interaction trajectory, and self-distilled credit assignment, which converts historical trajectories into token-level credit signals under teacher-student conditioning. Experiments on multiple datasets show that CoARS outperforms representative ARS baselines in recommendation performance and user alignment.

---

## 论文详细总结（自动生成）

以下是对论文《Self-Distilled Reinforcement Learning for Co-Evolving Agentic Recommender Systems》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的智能体推荐系统（ARS）主要依赖“Reflexion”范式，即通过外部文本记忆（Memory）存储交互轨迹。这种方式导致经验仅停留在提示词（Prompt）层面，无法内化为模型参数。
*   **研究动机**：
    *   **缺乏参数化学习**：智能体依赖通用推理，而非通过学习获得特定领域的决策能力。
    *   **奖励机制局限**：现有的强化学习（RL）方法多采用预定义规则或“智能体即法官”模式，忽略了推荐智能体与用户智能体之间持续相互影响的**交互本质**。
    *   **监督信号稀疏**：传统方法将多轮交互简化为最终结果（如是否点击），忽略了交互轨迹中蕴含的**密集监督信号**。

### 2. 方法论：CoARS 框架
CoARS 提出了一种自蒸馏强化学习框架，旨在实现推荐智能体（RecAgent）与用户智能体（UserAgent）的协同演化。
*   **核心思想**：将交互历史转化为内生的双向监督信号，并通过教师-学生模式实现 Token 级的细粒度信用分配。
*   **关键技术细节**：
    1.  **交互奖励（Interaction Reward）**：
        *   **RecAgent 奖励**：结合推荐准确性、用户接受强度（Acceptance Score）和交互阶段敏感性。对后期轮次的错误施加更大惩罚。
        *   **UserAgent 奖励**：基于推荐准确性、用户反应方向及强度，并引入“同伴相似性”（Peer Similarity）作为辅助信号，鼓励用户智能体做出符合真实偏好的反馈。
    2.  **自蒸馏信用分配（Self-distilled Credit Assignment）**：
        *   **诊断与重写**：将历史轨迹重写为“参考轨迹”，包含正确决策的追溯性解释。
        *   **教师-学生模式**：同一智能体在不同上下文下扮演两个角色。**教师模式**可观察参考轨迹（上帝视角），**学生模式**仅观察原始上下文。
        *   **Token 级优势计算**：通过对比教师和学生在每个 Token 上的对数概率差，计算诊断优势（Diagnostic Advantage），引导学生模型吸收教师的推理路径。
    3.  **协同演化训练**：RecAgent 和 UserAgent 在同一个交互循环中根据各自的奖励和自蒸馏信号同步优化参数。

### 3. 实验设计
*   **数据集**：LastFM（音乐）、MovieLens（电影）、Instruments（乐器）。
*   **实验场景**：多轮交互式推荐重排序（Reranking）。
*   **Benchmark（基准方法）**：
    *   **Reflexion**：基础的基于记忆的单智能体模型。
    *   **AFL**：建模双智能体反馈循环的基准。
    *   **iAgent**：指令感知的用户侧智能体框架。
    *   **RecoWorld**：基于 RL 优化 RecAgent 的模拟环境。
*   **评估指标**：Hit@1（推荐准确性）、F1（用户模拟对齐度）。

### 4. 资源与算力
*   **模型骨干**：使用了 Qwen3-8B、Qwen3-4B 以及 GPT-5.4 mini（注：文中提及版本，可能为未来版本或特定命名）。
*   **微调技术**：采用了基于 **LoRA** 的强化学习微调（Reinforcement Fine-tuning）。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长，但提到在训练过程中教师模型是冻结的（Frozen-teacher setting），这在一定程度上节省了显存开销。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同领域的标准数据集上进行了测试，每个数据集包含约 1000 个测试样本。
*   **实验维度**：
    *   **主实验**：对比了 4 种基准方法在 3 种不同参数规模骨干模型下的表现。
    *   **用户模拟实验**：专门验证了 UserAgent 的对齐能力。
    *   **消融实验**：分别针对 RecAgent 和 UserAgent 验证了协同训练、交互奖励和自蒸馏机制的贡献。
    *   **案例研究**：详细展示了教师模式如何通过诊断重写引导模型。
*   **评价**：实验设计较为全面，涵盖了从宏观性能到微观机制的验证，对比实验考虑了模型规模的影响，具有较好的客观性和公平性。

### 6. 主要结论与发现
*   **性能领先**：CoARS 在所有数据集和骨干模型上均显著优于基于记忆的方法和传统的 RL 方法。
*   **协同效应**：优化推荐智能体的同时，用户智能体的模拟准确性也得到了显著提升，证明了双向协同进化的必要性。
*   **参数化优势**：实验证明，将交互经验通过 RL 内化到参数中，比单纯依赖文本记忆（In-context learning）更有效。
*   **细粒度监督的重要性**：消融实验显示，自蒸馏提供的 Token 级信号对于提升推理质量至关重要。

### 7. 优点（亮点）
*   **内生监督**：无需外部昂贵的标注或复杂的法官模型，直接从交互反馈中提取监督信号。
*   **协同演化范式**：打破了“固定用户模拟器”的限制，让推荐系统和用户模型在对抗与协作中共同成长。
*   **细粒度信用分配**：通过自蒸馏解决了 RL 在长文本生成中奖励稀疏的问题，使模型能学习到具体的推理路径。

### 8. 不足与局限
*   **计算开销**：虽然使用了 LoRA，但双智能体同时进行 RL 训练的计算成本仍高于传统的单次微调。
*   **诊断重写的依赖性**：自蒸馏的效果高度依赖于“参考轨迹”的生成质量，如果重写过程引入偏差，可能导致错误累积。
*   **数据集规模限制**：测试集采样为 1000 条，虽然符合智能体研究的常规，但在超大规模工业场景下的泛化能力仍有待验证。
*   **实时性挑战**：多轮交互推理和参数更新在实时推荐系统中的部署难度较大。

（完）
