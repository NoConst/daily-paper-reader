---
title: "No Hard Negatives Required: Concept Centric Learning Leads to Compositionality without Degrading Zero-shot Capabilities of Contrastive Models"
title_zh: 无需难负样本：以概念为中心的学习在不降低对比模型零样本能力的情况下实现组合性
authors: "Hai X. Pham, David T. Hoffmann, Ricardo Guerrero, Brais Martinez"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25722v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 视觉语言模型中组合表示的概念中心学习
tldr: 针对对比视觉语言模型在组合性表示方面的局限，本文提出了一种无需硬负样本的概念中心学习方法。研究发现长文本描述和全局池化是限制组合性的主因。通过提取短概念标题并引入无参数的跨模态注意力池化，该方法在提升组合性基准测试表现的同时，保持甚至增强了模型的零样本和检索能力，且不增加推理成本。
selection_source: fresh_fetch
motivation: 现有的提升组合性方法依赖硬负样本，容易导致零样本能力下降且泛化性差，且模型架构中的全局池化丢失了关键的绑定信息。
method: 提取短的概念中心标题片段进行对齐，并引入无参数的跨模态注意力池化来获取概念中心视觉嵌入。
result: 在标准组合性基准测试上达到SOTA水平，同时维持或提升了零样本分类和检索性能。
conclusion: 概念中心学习是实现组合性且不牺牲通用能力的有效途径，证明了无需复杂负采样也能解决组合性问题。
---

## 摘要
对比式视觉语言（V&L）模型在各种应用中仍然是热门选择。然而，一些局限性也随之显现，最显著的是 V&L 模型学习组合式表示的能力有限。先前的方法通常通过生成自定义训练数据以获取难负样本（hard negative samples）来解决这一局限。难负样本已被证明可以提高组合性任务的性能，但通常仅针对单一基准测试，缺乏泛化能力，并可能导致零样本或检索性能等基础 V&L 能力的大幅下降，从而使其变得不切实际。在这项工作中，我们采用了一种不同的方法。我们确定了限制 V&L 组合性性能的两个根本原因：1) 长的训练描述（captions）不需要组合式表示；2) 文本和图像编码器中的最终全局池化导致从根本上完全丢失了学习绑定（binding）所需的必要信息。作为补救措施，我们提出了两个简单的解决方案：1) 我们使用标准的 NLP 软件获取以概念为中心的短描述片段，并将其与图像对齐；2) 我们引入了一种无参数的跨模态注意力池化（cross-modal attention-pooling），以从图像编码器中获取以概念为中心的视觉嵌入。通过这两项改进和简单的辅助对比损失，我们在标准组合性基准测试上获得了 SOTA 性能，同时保持或提升了强大的零样本和检索能力。这是在不增加推理成本的情况下实现的。我们在 https://github.com/SamsungLabs/concept_centric_clip 发布了本工作的代码。

## Abstract
Contrastive vision-language (V&L) models remain a popular choice for various applications. However, several limitations have emerged, most notably the limited ability of V&L models to learn compositional representations. Prior methods often addressed this limitation by generating custom training data to obtain hard negative samples. Hard negatives have been shown to improve performance on compositionality tasks, but are often specific to a single benchmark, do not generalize, and can cause substantial degradation of basic V&L capabilities such as zero-shot or retrieval performance, rendering them impractical. In this work we follow a different approach. We identify two root causes that limit compositionality performance of V&Ls: 1) Long training captions do not require a compositional representation; and 2) The final global pooling in the text and image encoders lead to a complete loss of the necessary information to learn binding in the first place. As a remedy, we propose two simple solutions: 1) We obtain short concept centric caption parts using standard NLP software and align those with the image; and 2) We introduce a parameter-free cross-modal attention-pooling to obtain concept centric visual embeddings from the image encoder. With these two changes and simple auxiliary contrastive losses, we obtain SOTA performance on standard compositionality benchmarks, while maintaining or improving strong zero-shot and retrieval capabilities. This is achieved without increasing inference cost. We release the code for this work at https://github.com/SamsungLabs/concept_centric_clip.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **C²LIP (Concept-centric CLIP)** 的方法，旨在解决对比式视觉语言模型（如 CLIP、SigLIP）在组合性表示（Compositionality）方面的缺陷，同时避免传统方法导致的通用能力下降。

### 1. 核心问题与研究动机
*   **核心问题**：对比模型（V&L）在处理物体、属性与关系之间的“绑定”（Binding）时表现较差（例如，容易混淆“红色的沙发”和“蓝色的椅子”）。
*   **研究背景**：
    *   **词袋效应（BoW）**：由于训练描述通常较长且详细，模型只需识别出关键词即可匹配图像，无需理解词序或复杂的组合逻辑。
    *   **全局池化的信息损失**：编码器最后的全局池化操作将所有视觉/文本特征混合，导致属性与物体之间的空间或语义关联在进入对比损失函数前就已丢失。
    *   **现有方案的局限**：先前方法多依赖“硬负样本”（Hard Negatives）微调，但这往往导致模型过拟合于特定基准测试，且会显著损害零样本分类和检索等通用性能。

### 2. 方法论
论文提出了两个核心解决方案，旨在不使用合成负样本的情况下增强组合性：
*   **以概念为中心的短描述（Concept-centric Captions）**：
    *   利用 NLP 工具（如 spaCy）从原始长描述中提取**名词短语（Noun-phrases）**作为“概念”（如“条纹帽子”）。
    *   **核心思想**：短描述迫使模型学习更具判别力的表示，因为简单的词袋策略在短语级别更容易失效。
*   **跨模态注意力池化（Cross-modal Attention-pooling）**：
    *   **技术细节**：在训练期间，引入一个**无参数**的注意力机制。使用文本编码器输出的概念嵌入（Concept Embeddings）作为 Query，去查询图像编码器的视觉 Token（Key 和 Value）。
    *   **辅助损失函数**：
        1.  **$L_{npc}$ (名词短语对比损失)**：将全局图像嵌入与该图像对应的所有短概念嵌入进行对齐（多正样本对比）。
        2.  **$L_{xac}$ (跨模态注意力对比损失)**：将通过注意力池化提取的“概念特定视觉嵌入”与其对应的文本概念嵌入进行对齐。
*   **推理阶段**：该方法在推理时**不增加任何额外计算开销**，保持与原始 SigLIP/CLIP 完全一致的架构。

### 3. 实验设计
*   **数据集**：在 CC3M（使用 DreamLIP 增强版描述）上进行微调。
*   **基准测试（Benchmarks）**：
    *   **组合性**：SugarCrepe, SugarCrepe++（评估物体、属性、关系的识别）。
    *   **零样本分类**：ImageNet-1K。
    *   **检索**：Flickr30K, MSCOCO, DOCCI, ImageInWords (IIW)。
*   **对比方法**：包括原始 SigLIP/CLIP、组合感知模型（NegCLIP, DAC, CE-CLIP, CLIC 等）、细粒度训练模型（DreamLIP, FG-CLIP）以及大型基础模型（SigLIP-2, BLIP, FLAVA）。

### 4. 资源与算力
*   **算力设备**：使用了 **8 台 NVIDIA A40 GPU**。
*   **训练细节**：有效 Batch Size 为 768，微调 5 个 Epoch。
*   **优化器**：Adam 优化器，基础学习率为 1e-5。
*   **预处理**：名词短语提取是离线完成的。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从组合性到通用检索、再到零样本分类的全面评估。
*   **消融实验**：对 $L_{npc}$ 和 $L_{xac}$ 两个组件分别做了消融，验证了各自的贡献。
*   **鲁棒性分析**：对超参数（损失权重）进行了敏感性分析，证明方法对参数选择不敏感。
*   **下游应用**：还进行了 LLaVA 指令微调实验，证明 C²LIP 作为视觉编码器能提升多模态大模型（VLM）的组合理解能力。
*   **评价**：实验设计非常充分且客观，特别是引入了 SugarCrepe++（更难、更难被“刷榜”的基准）来验证泛化性。

### 6. 主要结论与发现
*   **SOTA 性能**：C²LIP 在 SugarCrepe++ 等组合性基准上达到了 SOTA，且在检索任务上优于或持平原始模型。
*   **无需硬负样本**：证明了仅通过优化正样本的对齐方式（概念级对齐）即可获得极强的组合性，且泛化能力优于基于负样本的方法。
*   **保持通用性**：与先前方法动辄在 ImageNet 上掉分 10% 以上不同，C²LIP 的性能下降非常轻微（约 2.9%），且在检索任务上反而有所提升。

### 7. 优点
*   **简洁高效**：不需要复杂的硬负样本生成流水线（如 LLM 生成或扩散模型生成图像）。
*   **无推理负担**：不改变模型架构，不增加推理延迟。
*   **泛化性强**：在 SugarCrepe++ 上的表现证明其学习到了真正的语义绑定，而非简单的模板匹配。
*   **即插即用**：理论上可以应用于任何基于对比学习的视觉语言模型。

### 8. 不足与局限
*   **ImageNet 微跌**：虽然跌幅较小，但仍存在性能权衡。作者认为这是因为组合性训练使模型更关注“场景中心”而非 ImageNet 偏好的“单一物体中心”。
*   **依赖 NLP 工具**：预处理阶段依赖 spaCy 等工具的解析准确性，对于极低资源语言或非规范文本可能存在限制。
*   **池化层仍有改进空间**：尽管引入了训练时的注意力池化，但推理时仍使用全局池化，这可能限制了模型处理极复杂场景的上限。

（完）
