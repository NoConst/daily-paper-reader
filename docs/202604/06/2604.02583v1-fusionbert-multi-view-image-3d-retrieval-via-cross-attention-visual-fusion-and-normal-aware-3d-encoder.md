---
title: "FusionBERT: Multi-View Image-3D Retrieval via Cross-Attention Visual Fusion and Normal-Aware 3D Encoder"
title_zh: FusionBERT：通过交叉注意力视觉融合与法向感知 3D 编码器实现的多视图图像-3D 检索
authors: "Wei Li, Yufan Ren, Hanqing Jiang, Jianhui Ding, Zhen Peng, Leman Feng, Yichun Shentu, Guoqiang Xu, Baigui Sun"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02583v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多视图图像-3D多模态检索框架
tldr: 本文提出FusionBERT，一种针对图像-3D检索的多视图视觉融合框架。针对现有方法多关注单视图对齐而忽略多视图互补信息的局限，该框架通过基于交叉注意力的视觉聚合器自适应整合多视图特征，并引入法线感知3D编码器，结合点云法线与位置增强几何表示。实验证明，FusionBERT在单视图和多视图检索任务中均显著优于现有SOTA模型，为多视图多模态检索建立了强有力的基准。
selection_source: fresh_fetch
motivation: 现有的图像-3D检索方法大多局限于单视图对齐，难以有效利用现实场景中多视图观测所提供的互补几何与外观信息。
method: 创新性地采用基于交叉注意力的多视图视觉聚合器进行特征融合，并结合法线感知3D编码器来增强对无纹理或颜色退化模型的表征。
result: 在单视图和多视图设置下的图像-3D检索实验中，FusionBERT的准确率均显著超过了现有的SOTA多模态大模型。
conclusion: FusionBERT通过有效融合多视图视觉线索和增强3D几何特征，为多视图多模态检索领域提供了高效且鲁棒的解决方案。
---

## 摘要
我们提出了 FusionBERT，这是一种用于图像-3D 多模态检索的新型多视图视觉融合框架。现有的图像-3D 表示学习方法主要集中在单个物体图像与其 3D 模型之间的特征对齐，这限制了它们在现实场景中的适用性，因为在现实中，物体通常是从多个视角被观察和捕捉的。尽管多视图观测自然地提供了互补的几何和外观线索，但现有的多模态大模型很少探索如何有效融合此类多视图视觉信息以实现更好的跨模态检索。为了解决这一局限性，我们引入了一个名为 FusionBERT 的多视图图像-3D 检索框架，该框架创新性地利用基于交叉注意力的多视图视觉聚合器，自适应地整合来自物体多视图图像的特征。所提出的多视图视觉编码器融合了视图间的互补关系，并选择性地强调跨多个视角的关键视觉线索，从而获得更稳健的融合视觉特征，以实现更好的 3D 模型匹配。此外，FusionBERT 提出了一种法向感知的 3D 模型编码器，通过联合编码点法向和 3D 位置，进一步增强物体模型的 3D 几何特征，从而为无纹理或颜色退化的 3D 模型实现更稳健的表示学习。广泛的图像-3D 检索实验表明，在单视图和多视图设置下，FusionBERT 的检索准确率均显著高于现有的 SOTA 多模态大模型，为多视图多模态检索建立了一个强大的基准。

## Abstract
We propose FusionBERT, a novel multi-view visual fusion framework for image-3D multimodal retrieval. Existing image-3D representation learning methods predominantly focus on feature alignment of a single object image and its 3D model, limiting their applicability in realistic scenarios where an object is typically observed and captured from multiple viewpoints. Although multi-view observations naturally provide complementary geometric and appearance cues, existing multimodal large models rarely explore how to effectively fuse such multi-view visual information for better cross-modal retrieval. To address this limitation, we introduce a multi-view image-3D retrieval framework named FusionBERT, which innovatively utilizes a cross-attention-based multi-view visual aggregator to adaptively integrate features from multi-view images of an object. The proposed multi-view visual encoder fuses inter-view complementary relationships and selectively emphasizes informative visual cues across multiple views to get a more robustly fused visual feature for better 3D model matching. Furthermore, FusionBERT proposes a normal-aware 3D model encoder that can further enhance the 3D geometric feature of an object model by jointly encoding point normals and 3D positions, enabling a more robust representation learning for textureless or color-degraded 3D models. Extensive image-3D retrieval experiments demonstrate that FusionBERT achieves significantly higher retrieval accuracy than SOTA multimodal large models under both single-view and multi-view settings, establishing a strong baseline for multi-view multimodal retrieval.

---

## 论文详细总结（自动生成）

这是一份关于论文《FusionBERT: Multi-View Image-3D Retrieval via Cross-Attention Visual Fusion and Normal-Aware 3D Encoder》的结构化深入总结：

### 1. 核心问题与研究背景
*   **核心问题**：现有的图像-3D 检索模型（如 OpenShape, TAMM）主要针对“单张图像对齐单个 3D 模型”的场景。但在现实应用（如机器人、电商、工业零件检索）中，物体通常具有多个视角的观测。
*   **研究动机**：
    1.  **多视图融合不足**：现有方法缺乏有效的多视图特征融合机制，简单的平均池化或拼接无法捕捉不同视角间的互补几何与外观信息。
    2.  **3D 编码器对纹理过度依赖**：目前的 3D 编码器（如 Point-BERT）高度依赖点云的颜色信息，在处理无纹理的 CAD 模型或 LiDAR 扫描数据时性能会大幅下降。

### 2. 方法论
FusionBERT 提出了一个双支路对齐框架，核心包含两个创新模块：
*   **基于两阶段注意力的多视图视觉聚合器 (MVVA)**：
    *   **第一阶段（自注意力）**：利用 Transformer 编码器对 $V$ 个视角的 CLIP 特征进行建模，捕捉视角间的依赖关系，强化一致性特征并抑制噪声。
    *   **第二阶段（交叉注意力）**：计算所有视角的均值作为“全局共识嵌入”（Consensus Embedding），以此为 Query，对第一阶段的特征进行交叉注意力加权融合，从而自适应地强调信息量最大的视角。
*   **法向感知 3D 模型编码器 (NA3DE)**：
    *   在传统的点云输入 $[x, y, z, R, G, B]$ 基础上，增加了表面法向量 $[N_x, N_y, N_z]$，形成 9 维输入向量。
    *   通过法向信息增强模型对局部表面曲率和几何结构的感知，使其在无颜色（Color-degraded）的情况下依然保持鲁棒。
*   **两阶段训练策略**：
    *   **阶段一**：将法向感知 3D 编码器与预训练的 CLIP 图像特征空间对齐。
    *   **阶段二**：冻结 3D 编码器，专门训练多视图聚合器，使其输出的融合特征与 3D 特征空间对齐。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用包含 Objaverse、ShapeNet、ABO 和 3D-FUTURE 的联合数据集（约 140 万个三模态样本）。
    *   **评估**：Objaverse-LVIS（高质量 3D 模型）、LVIS no-RGB（去除颜色的鲁棒性测试）、ModelNet40（标准基准）以及 **IMP（作者自采的工业机械零件数据集，包含真实照片和 CAD 模型）**。
*   **对比方法 (Baselines)**：OpenShape、TAMM、ULIP-2。
*   **评价指标**：Recall@1, Recall@3, Recall@5。

### 4. 资源与算力
*   **硬件**：论文提到在单张 **NVIDIA H20 Tensor Core GPU** 上评估了运行效率。
*   **耗时统计**：
    *   **视觉端**：3 视图输入时，CLIP 编码耗时 313.85ms，多视图融合模块耗时 193.14ms，总计约 507ms。
    *   **3D 端**：10,000 个点的法向感知编码耗时 15.55ms。
*   **训练细节**：文中未明确说明完整的预训练总时长和使用的 GPU 总数量，但从数据集规模（1.4M）推测需要大规模计算集群。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个不同性质的数据集上进行了全面对比。
    *   **消融实验**：分别验证了多视图聚合器（MVVA）和法向感知编码器（NA3DE）的独立贡献。
    *   **视角数量实验**：测试了输入视角从 1 个增加到 12 个时性能的变化趋势（Recall@5 随视角增加稳步提升）。
*   **客观性**：对比实验中，基准模型（如 TAMM）在多视图设置下统一使用平均池化，确保了对比的公平性。引入“无颜色版 LVIS”和“真实工业数据集”增强了实验的说服力。

### 6. 主要结论与发现
*   **多视图优势**：FusionBERT 在 3 视图配置下的检索准确率显著高于单视图，且在所有视角数量下均优于 SOTA 模型。
*   **几何鲁棒性**：在 LVIS no-RGB 数据集上，FusionBERT 的领先优势扩大，证明了法向信息在缺失纹理时的关键作用。
*   **工业应用潜力**：在 IMP 真实工业零件数据集上，FusionBERT 表现出良好的跨域泛化能力（从合成数据到真实照片）。

### 7. 优点
*   **结构化融合**：相比简单的池化，两阶段注意力机制能更智能地处理视角遮挡和冗余。
*   **法向增强**：通过简单的维度扩展（法向）解决了 3D 编码器对颜色过度依赖的痛点，具有很强的实用价值。
*   **端到端对齐**：两阶段训练策略有效地利用了 CLIP 的强泛化能力，同时实现了多视图的特化对齐。

### 8. 不足与局限
*   **视角选择策略**：目前是固定视角输入，尚未探索如何主动选择“最佳视角”以进一步降低计算开销。
*   **计算延迟**：虽然单次推理在 500ms 左右，但在实时性要求极高的机器人感知任务中，多视图融合带来的额外延迟仍需优化。
*   **训练资源未完全透明**：缺乏具体的训练超参数和总算力消耗说明，不利于完全复现。

（完）
