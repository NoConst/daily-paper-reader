---
title: "Experience Compression Spectrum: Unifying Memory, Skills, and Rules in LLM Agents"
title_zh: 经验压缩光谱：统一大语言模型智能体中的记忆、技能与规则
authors: "Xing Zhang, Guanghui Wang, Yanwei Cui, Wei Qiu, Ziyuan Li, Bing Zhu, Peiyang He"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15877v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 统一LLM智能体中的记忆、技能和规则，以管理累积的经验。
tldr: 针对LLM智能体在长期任务中经验管理效率低下的问题，本文提出了“经验压缩光谱”统一框架。该框架将记忆、技能和规则视为不同压缩程度的知识形态（5x到1000x+），揭示了现有系统在跨层级自适应压缩上的缺失（即“缺失的对角线”）。研究通过分析22篇核心论文，指出了记忆与技能发现社区间的隔阂，并为构建可扩展的全光谱智能体学习系统提供了设计原则。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15877v1/fig-001.webp\", \"caption\": \"Figure 1. The Experience Compression Spectrum. Existing agent learning systems map onto a single axis from raw traces to abstract rules. Memory systems cluster at Level 1, skill systems at Level 2, with Level 3 largely empty. A small number of cross-level systems (dashed) bridge Levels 1–2 but none support adaptive level selection. Compression ratios are approximate.\", \"page\": 2, \"index\": 1, \"width\": 952, \"height\": 428}, {\"url\": \"assets/figures/arxiv/2604.15877v1/fig-002.webp\", \"caption\": \"Table 2. Representative systems mapped onto the Experience Compression Spectrum. Level indicates primary output granularity. Mechanism indicates the core compression method. Lifecycle indicates explicit support for knowledge maintenance (versioning, conflict detection, deprecation). Notably, no system supports adaptive level selection—all operate at a predetermined compression level.\", \"page\": 4, \"index\": 2, \"width\": 492, \"height\": 471}]"
motivation: 现有的智能体记忆系统与技能发现研究缺乏交流，且缺乏统一框架来高效管理长期部署中积累的海量交互经验。
method: 提出经验压缩光谱（ECS）框架，将经验按压缩率划分为情节记忆、程序化技能和陈述性规则，并对20多个现有系统进行映射分析。
result: 发现现有系统均运行在固定压缩级别，缺乏跨层级自适应能力，且不同社区在独立解决相似子问题时缺乏方案交换。
conclusion: 智能体学习系统应向全光谱、自适应压缩方向发展，并需重点关注知识生命周期管理及跨层级的知识迁移。
---

## 摘要
随着大语言模型（LLM）智能体扩展到长周期、多会话的部署场景，高效管理积累的经验已成为关键瓶颈。智能体记忆系统和智能体技能发现都在应对这一挑战——即从交互轨迹中提取可重用的知识——然而，对22篇核心论文中1,136条参考文献的引用分析显示，跨社区引用率低于1%。我们提出了“经验压缩光谱”（Experience Compression Spectrum），这是一个统一的框架，将记忆、技能和规则定位在压缩率不断增加的单一轴线上（情景记忆为5-20倍，程序性技能为50-500倍，陈述性规则为1,000倍以上），从而直接降低上下文消耗、检索延迟和计算开销。将20多个系统映射到该光谱上发现，每个系统都在固定的、预设的压缩级别上运行，没有一个系统支持自适应的跨级别压缩，我们将这一空白称为“缺失的对角线”（missing diagonal）。我们进一步表明，仅靠专业化是不够的——两个社区都在不交换解决方案的情况下独立解决共同的子问题——评估方法与压缩级别紧密耦合，可迁移性随压缩程度的增加而提高，但以牺牲特异性为代价，且知识生命周期管理在很大程度上仍被忽视。我们阐述了构建可扩展、全光谱智能体学习系统的开放性问题和设计原则。

## Abstract
As LLM agents scale to long-horizon, multi-session deployments, efficiently managing accumulated experience becomes a critical bottleneck. Agent memory systems and agent skill discovery both address this challenge -- extracting reusable knowledge from interaction traces -- yet a citation analysis of 1,136 references across 22 primary papers reveals a cross-community citation rate below 1%. We propose the \emph{Experience Compression Spectrum}, a unifying framework that positions memory, skills, and rules as points along a single axis of increasing compression (5--20$\times$ for episodic memory, 50--500$\times$ for procedural skills, 1,000$\times$+ for declarative rules), directly reducing context consumption, retrieval latency, and compute overhead. Mapping 20+ systems onto this spectrum reveals that every system operates at a fixed, predetermined compression level -- none supports adaptive cross-level compression, a gap we term the \emph{missing diagonal}. We further show that specialization alone is insufficient -- both communities independently solve shared sub-problems without exchanging solutions -- that evaluation methods are tightly coupled to compression levels, that transferability increases with compression at the cost of specificity, and that knowledge lifecycle management remains largely neglected. We articulate open problems and design principles for scalable, full-spectrum agent learning systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Experience Compression Spectrum: Unifying Memory, Skills, and Rules in LLM Agents》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着大语言模型（LLM）智能体进入长期、多会话的部署阶段，它们积累了海量的交互经验。这些原始轨迹（Traces）会迅速撑爆上下文窗口，导致检索成本高昂且效率低下。
*   **核心问题**：目前研究界分裂为“智能体记忆”和“技能发现”两个独立社区，两者引用率互引不足1%。论文旨在通过**经验压缩光谱（ECS）**框架统一这两个领域，将记忆、技能和规则视为对经验进行不同程度压缩的产物，以解决智能体长期运行的扩展性瓶颈。

### 2. 方法论
*   **核心思想**：将经验管理定义为一个压缩函数 $C_L: T \to K_L$，将交互轨迹 $T$ 映射为不同粒度的知识构件 $K_L$。
*   **四个压缩层级**：
    *   **Level 0 (原始轨迹)**：1:1 压缩，包含完整日志，重用性极低。
    *   **Level 1 (情节记忆)**：5–20倍压缩，提取“发生了什么”（如键值对、事件摘要），重用性中等。
    *   **Level 2 (程序化技能)**：50–500倍压缩，提取“如何行动”（如代码片段、工作流模板），重用性高。
    *   **Level 3 (陈述性规则)**：1000倍以上压缩，提取“决策原则”（如领域无关的约束、策略），重用性最高。
*   **关键技术细节**：
    *   **向上压缩（Promotion）**：在空闲时间将多个记忆合并为技能，或将技能抽象为规则（类似生物学的睡眠巩固）。
    *   **向下回退（Demotion）**：当抽象规则在特定语境失效时，降级为具体的记忆证据。
    *   **缺失的对角线**：论文指出当前系统均固定在某一压缩层级，缺乏自适应选择压缩层级的能力。

### 3. 实验设计
*   **研究性质**：本文属于综述与框架定义类论文，而非单一算法实验。
*   **分析对象**：对22篇核心论文的1,136条参考文献进行了引用分析；对20多个代表性系统（如 Mem0, Voyager, ExpeL, Trace2Skill 等）进行了光谱映射。
*   **基准测试（Benchmark）引用**：通过汇总已有研究的数据（如 ALFWorld, SpreadsheetBench, BrowseComp, SWE-bench 等）来证明高压缩率（L2/L3）在跨任务迁移和性能提升上的优势。
*   **对比方法**：对比了“仅使用记忆检索”与“使用抽象技能/规则”在下游任务中的成功率差异。

### 4. 资源与算力
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因**：该研究侧重于系统架构分析和元分析（Meta-analysis），而非从头训练大型基础模型。其讨论的“压缩”操作通常由 LLM 作为“压缩器”在推理侧完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   引用分析覆盖了千余条文献，具有统计学意义。
    *   系统映射涵盖了2023-2026年间的主流智能体框架，覆盖面广。
    *   汇总了多项独立研究的实验结果（如 Table 1），显示 L2 技能相比 L1 检索在不同基准下有 16% 到 68% 的性能提升。
*   **充分性评价**：作为框架论文，其通过跨学科（认知科学、软件工程）的对比和现有数据的整合，充分证明了“压缩光谱”存在的客观性及现有研究的局限性。

### 6. 主要结论与发现
*   **社区割裂**：记忆与技能研究社区几乎互不交流（互引率 < 1%）。
*   **压缩效益**：更高的压缩级别（L2/L3）能显著降低存储和检索开销（100-1000倍），并提升跨模型、跨任务的迁移能力。
*   **缺失的环节**：目前没有任何系统支持“自适应层级选择”，且“陈述性规则（L3）”的自动化提取几乎是空白。
*   **生命周期管理**：现有系统普遍忽视了知识的冲突检测、版本控制和过时清理。

### 7. 优点
*   **统一性**：首次将零散的记忆、技能、规则研究整合进单一的压缩维度，消除了术语混乱。
*   **前瞻性**：提出了“缺失的对角线”和“空闲时间巩固”等设计原则，为下一代智能体架构指明了方向。
*   **跨学科借鉴**：引入了认知科学（CLS理论）和软件工程（版本控制）的成熟思想。

### 8. 不足与局限
*   **缺乏端到端验证**：论文提出了全光谱系统的设计原则，但尚未实现并测试一个能同时自适应处理 L1-L3 的完整原型系统。
*   **层级离散化**：将压缩划分为四个离散层级是一种简化，实际应用中知识的抽象程度可能是连续的。
*   **L3 评估难题**：论文承认目前缺乏评估自动化提取规则质量的有效方法，容易陷入元评估的无限递归。

（完）
