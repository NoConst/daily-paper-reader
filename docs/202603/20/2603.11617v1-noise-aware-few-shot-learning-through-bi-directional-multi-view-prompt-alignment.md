---
title: Noise-aware few-shot learning through bi-directional multi-view prompt alignment
title_zh: 通过双向多视图提示对齐的噪声感知少样本学习
authors: "Lu Niu, Cheng Xue"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11617v1"
tags: ["query:cir-ml"]
score: 8.0
evidence: 视觉语言模型中的提示微调与多视图提示对齐
tldr: 针对视觉语言模型在少样本学习中易受噪声标签干扰的问题，本文提出NA-MVP框架。该框架通过双向多视图提示对齐，利用不平衡最优传输实现细粒度区域对齐并抑制噪声，结合双向提示捕捉稳定语义，并通过对齐引导的策略修正错误标签。实验证明，该方法在处理噪声监督时具有显著的鲁棒性和优越性能。
selection_source: fresh_fetch
motivation: 现有的少样本提示微调方法在存在噪声标签时，难以有效建模细粒度语义并自适应地分离干净信号与噪声信号。
method: 提出NA-MVP框架，通过多视图提示结合不平衡最优传输实现区域对齐，并利用双向提示设计与选择性精炼策略来捕捉稳定语义并修正误标样本。
result: 在多个合成及真实世界的噪声基准测试中，NA-MVP的性能均显著优于目前最先进的基准模型。
conclusion: 研究证明，从全局匹配转向区域感知的双向对齐是实现噪声监督下鲁棒少样本学习的有效途径。
---

## 摘要
视觉语言模型通过提示微调（prompt tuning）提供了强大的少样本学习能力，但仍容易受到噪声标签的影响，这可能会破坏提示并降低跨模态对齐的效果。现有方法面临挑战，因为它们通常缺乏建模细粒度语义线索以及自适应分离干净信号与噪声信号的能力。为了应对这些挑战，我们提出了 NA-MVP，这是一个通过双向多视图提示对齐实现噪声感知少样本学习的框架。NA-MVP 基于一个关键的概念转变：鲁棒的提示学习需要从全局匹配转向区域感知对齐，从而明确区分干净线索和噪声线索。为实现这一目标，NA-MVP 采用了：(1) 多视图提示结合非平衡最优传输（unbalanced optimal transport），以实现细粒度的补丁到提示（patch-to-prompt）对应关系，同时抑制不可靠区域；(2) 双向提示设计，捕捉互补的面向干净信号和感知噪声的线索，使模型能够专注于稳定的语义；以及 (3) 一种对齐引导的选择性细化策略，利用最优传输仅纠正错误标记的样本，同时保留可靠数据。在合成和真实世界噪声基准上的实验表明，NA-MVP 一致优于最先进的基准模型，证实了其在噪声监督下实现鲁棒少样本学习的有效性。

## Abstract
Vision-language models offer strong few-shot capability through prompt tuning but remain vulnerable to noisy labels, which can corrupt prompts and degrade cross-modal alignment. Existing approaches struggle because they often lack the ability to model fine-grained semantic cues and to adaptively separate clean from noisy signals. To address these challenges, we propose NA-MVP, a framework for Noise-Aware few-shot learning through bi-directional Multi-View Prompt alignment. NA-MVP is built upon a key conceptual shift: robust prompt learning requires moving from global matching to region-aware alignment that explicitly distinguishes clean cues from noisy ones. To realize this, NA-MVP employs (1) multi-view prompts combined with unbalanced optimal transport to achieve fine-grained patch-to-prompt correspondence while suppressing unreliable regions; (2) a bi-directional prompt design that captures complementary clean-oriented and noise-aware cues, enabling the model to focus on stable semantics; and (3) an alignment-guided selective refinement strategy that uses optimal transport to correct only mislabeled samples while retaining reliable data. Experiments on synthetic and real-world noisy benchmarks demonstrate that NA-MVP consistently outperforms state-of-the-art baselines, confirming its effectiveness in enabling robust few-shot learning under noisy supervision.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：NA-MVP 框架

#### 1. 核心问题与研究动机
*   **核心问题**：视觉语言模型（VLM，如 CLIP）在少样本（Few-shot）场景下通过提示微调（Prompt Tuning）表现优异，但极易受到**噪声标签**的干扰。少量的错误标签会严重偏差梯度更新，导致跨模态对齐失效。
*   **研究动机**：现有方法（如负向学习、噪声选择）存在三大局限：
    1.  **单视图依赖**：通常仅使用一两个提示，无法捕捉复杂的语义线索。
    2.  **硬负样本限制**：强制分配显式负标签过于僵化，在噪声环境下不够可靠。
    3.  **粗粒度去噪**：依赖固定阈值或全局伪标签，容易导致错误传播。
*   **整体含义**：论文提出从“全局匹配”转向“区域感知的双向对齐”，通过区分图像内部的干净区域与噪声区域来增强鲁棒性。

#### 2. 方法论：NA-MVP 框架
核心思想是利用多视图提示和最优传输（OT）理论，实现细粒度的噪声感知对齐与标签修正。
*   **双向多视图提示设计**：为每个类别构建两组可学习提示：**面向干净的提示（Clean-oriented）**和**感知噪声的提示（Noise-aware）**。每组包含多个视图（默认 $N=4$），以捕捉类内多样性。
*   **基于不平衡最优传输（UOT）的局部对齐**：
    *   将图像的局部特征补丁（Patches）与多视图提示进行匹配。
    *   使用 **UOT** 替代标准 OT，通过放宽质量守恒约束，允许部分匹配，从而自动抑制与噪声相关的不可靠区域，增强干净语义的权重。
*   **双向提示损失（ITBP Loss）**：通过对比学习，鼓励图像特征靠近干净提示并远离对应的噪声提示。
*   **选择性标签精炼策略**：
    *   **自适应识别**：根据干净提示与噪声提示的相对对齐强度，计算自适应阈值 $\phi$，识别出潜在的误标样本。
    *   **经典 OT 修正**：仅对识别出的噪声样本，利用经典 OT 将全局图像特征与类别文本特征进行对齐，生成更准确的伪标签进行修正。

#### 3. 实验设计
*   **数据集**：
    *   **合成噪声数据集**：Caltech101, DTD, Flowers102, OxfordPets, UCF101。
    *   **真实世界噪声数据集**：Food101N。
*   **噪声设置**：对称噪声（Symmetric）和非对称噪声（Asymmetric），噪声比例从 12.5% 到 75% 不等。
*   **基准测试（Benchmark）**：16-shot 学习设置。
*   **对比方法**：CoOp (基础提示学习), CoOp+GCE (鲁棒损失), JoAPR, NLPrompt (SOTA 噪声提示学习), DEFT 等。

#### 4. 资源与算力
*   **硬件**：单张 **NVIDIA GeForce RTX 4090 GPU**。
*   **软件/参数**：使用 SGD 优化器，初始学习率 0.002，动量 0.9，权重衰减 $5 \times 10^{-4}$。
*   **训练时长**：总计训练 50 个 Epoch，在 $T_{sup}$ Epoch 后开启标签精炼。

#### 5. 实验数量与充分性
*   **实验规模**：在 6 个数据集上进行了广泛测试，涵盖了极高噪声比例（75%）的极端情况。
*   **消融实验**：详细验证了双向提示、多视图数量、UOT 局部对齐、以及自适应阈值 $\phi$ 的有效性。
*   **客观性**：所有结果均为三次随机种子运行的平均值，并对比了最新的顶级会议基准（如 CVPR 2025 的 NLPrompt），实验设计严谨、对比公平。

#### 6. 主要结论与发现
*   **性能领先**：NA-MVP 在所有数据集和噪声比例下均优于现有 SOTA 方法，尤其在**高噪声比例**下优势更明显（例如在 DTD 75% 噪声下比 NLPrompt 高出约 9%）。
*   **多视图价值**：增加提示视图数量（从 1 到 4）能显著提升性能，但超过 4 个后收益递减。
*   **UOT 的优越性**：相比于标准 OT 或 KL 散度，UOT 的柔性匹配机制在处理局部噪声特征时更为有效。
*   **选择性修正**：通过自适应阈值仅修正部分样本，比全局重标注更能防止模型在低噪声环境下被误导。

#### 7. 优点（亮点）
*   **概念创新**：将噪声处理从全局标签层面深入到图像局部区域与多视图语义的对齐层面。
*   **机制灵活**：引入“感知噪声提示”作为隐式负样本，避免了硬负标签带来的优化困难。
*   **鲁棒性强**：在真实世界噪声（Food101N）和极少样本（4-shot）下依然保持稳定，具有很强的实用潜力。

#### 8. 不足与局限
*   **计算开销**：虽然使用了 Sinkhorn 算法加速，但 UOT 和多视图提示的引入相比简单提示微调增加了计算复杂度和内存占用。
*   **超参数敏感性**：框架涉及多个超参数（如 UOT 的正则化系数 $\epsilon$、温度参数 $\tau$、精炼开启时间 $T_{sup}$），在不同分布的数据集上可能需要精细调优。
*   **应用限制**：目前主要针对分类任务，在更复杂的视觉任务（如检测或分割）中的适用性尚待验证。

（完）
