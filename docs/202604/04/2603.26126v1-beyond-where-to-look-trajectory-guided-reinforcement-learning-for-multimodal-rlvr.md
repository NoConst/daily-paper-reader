---
title: "Beyond Where to Look: Trajectory-Guided Reinforcement Learning for Multimodal RLVR"
title_zh: 超越“看哪里”：面向多模态可验证奖励强化学习的轨迹引导强化学习
authors: "Jinda Lu, Junkang Wu, Jinghan Li, Kexin Huang, Shuo Yang, Mingzhu Chen, Jiancan Wu, Kuien Liu, Xiang Wang"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26126v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大语言模型中多模态推理的强化学习
tldr: 本研究针对多模态大语言模型在强化学习中存在的“视觉感知与逻辑推理脱节”问题，提出了轨迹引导强化学习（TGRL）。该方法利用强模型的专家推理轨迹，引导策略模型将视觉证据融入细粒度的推理链条中。通过引入标记级重权和轨迹过滤机制，TGRL 在多个基准测试上显著提升了推理性能，有效增强了模型将视觉事实转化为逻辑结论的能力。
selection_source: fresh_fetch
motivation: 尽管模型能关注到相关视觉区域，但往往无法将视觉证据有效整合进后续推理过程，导致推理链条与视觉事实脱节。
method: 提出轨迹引导强化学习（TGRL），利用专家轨迹引导细粒度推理，并结合标记级重权和轨迹过滤来确保策略优化。
result: 在多个多模态推理基准测试上的实验证明，TGRL 持续提升了推理表现，并有效弥合了视觉感知与逻辑推理之间的鸿沟。
conclusion: TGRL 通过引导模型整合视觉证据，显著增强了多模态大语言模型在复杂任务中的逻辑推理能力和视觉接地性。
---

## 摘要
多模态大语言模型（MLLM）中具有可验证奖励的强化学习（RLVR）的最新进展主要集中在提高最终答案的正确性和增强视觉定位上。然而，一个关键瓶颈仍然存在：尽管模型能够关注相关的视觉区域，但它们往往无法有效地将视觉证据整合到后续的推理中，导致推理链与视觉事实的关联较弱。为了解决这一问题，我们提出了轨迹引导强化学习（TGRL），该方法利用来自更强模型的专家推理轨迹，引导策略模型将视觉证据整合到细粒度的推理过程中。我们进一步引入了标记级（token-level）重加权和轨迹过滤，以确保稳定且有效的策略优化。在多个多模态推理基准测试上的广泛实验表明，TGRL 持续提升了推理性能，并有效地弥合了视觉感知与逻辑推理之间的差距。

## Abstract
Recent advances in Reinforcement Learning with Verifiable Rewards (RLVR) for multimodal large language models (MLLMs) have mainly focused on improving final answer correctness and strengthening visual grounding. However, a critical bottleneck remains: although models can attend to relevant visual regions, they often fail to effectively incorporate visual evidence into subsequent reasoning, leading to reasoning chains that are weakly grounded in visual facts. To address this issue, we propose Trajectory-Guided Reinforcement Learning (TGRL), which guides the policy model to integrate visual evidence into fine-grained reasoning processes using expert reasoning trajectories from stronger models. We further introduce token-level reweighting and trajectory filtering to ensure stable and effective policy optimization. Extensive experiments on multiple multimodal reasoning benchmarks demonstrate that TGRL consistently improves reasoning performance and effectively bridges the gap between visual perception and logical reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《Beyond Where to Look: Trajectory-Guided Reinforcement Learning for Multimodal RLVR》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在多模态大语言模型（MLLM）的强化学习（RLVR）中，现有的方法主要关注“结果正确性”或“视觉定位（看哪里）”。然而，研究发现模型存在**感知与推理脱节**的瓶颈：即使模型关注到了正确的视觉区域，也往往无法将这些视觉事实准确地转化为后续的逻辑推理步骤（例如：数对了图片中的物体数量，但在计算总价时却使用了错误的数字）。
*   **核心目标**：提出一种能够引导模型将视觉证据有效整合进细粒度推理链条的强化学习框架，弥合感知与推理之间的鸿沟。

### 2. 方法论：核心思想与关键技术
论文提出了**轨迹引导强化学习（TGRL）**，其核心思想是利用强模型的专家推理轨迹来规范学生模型的推理路径。
*   **轨迹集成（Trajectory Integration）**：在采样阶段，将策略模型生成的在策（on-policy）轨迹与少量专家模型（如 Qwen2.5-VL-32B）生成的专家轨迹混合，构建联合 Rollout 组。
*   **标记级重权（Token-level Reweighting）**：
    *   **重要性采样修正**：针对专家轨迹属于离策（off-policy）数据的问题，引入重要性比率修正分布偏移。
    *   **自适应权重**：基于当前模型与专家模型在 Token 预测概率上的“差异度”计算权重。差异越大，说明当前模型在该推理步骤上偏离专家逻辑越远，赋予更高的梯度权重以强化学习。
*   **轨迹过滤（Trajectory Filtering）**：引入正确性掩码，仅保留最终答案正确的专家轨迹，防止错误的推理路径干扰策略优化。
*   **算法兼容性**：该方法可无缝集成到 GRPO 和 DAPO 等主流 RLVR 优化算法中。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：Geo3K（2.1K 几何题目）、ViRL39K（39K 大规模多模态推理数据）。
    *   **评估集**：MathVista、WeMath、MathVision、MathVerse（侧重数学与逻辑推理）；HallusionBench（侧重视觉幻觉与感知）。
*   **对比方法**：
    *   基础模型：Qwen2.5-VL-7B。
    *   基线算法：标准 GRPO、DAPO。
    *   SOTA 方法：Perception-R1、NoisyRollout、ThinkLite、OpenVLThinker 等。
*   **模型设置**：使用 Qwen2.5-VL-3B/7B 作为学生策略模型，Qwen2.5-VL-32B 作为专家模型。

### 4. 资源与算力
*   **框架**：基于 `EasyR1` 训练框架实现。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100/H100）、显卡数量或具体的训练总时长。
*   **训练参数**：在 Geo3K 上训练 15 epoch，在 ViRL39K 上训练 2 epoch；Rollout 组大小设置为 5 或 8。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个主流多模态推理基准上进行了广泛测试，涵盖了从几何到通用数学推理的多种场景。
*   **消融实验**：针对“轨迹过滤”、“标记级重权”以及超参数 $\beta$（控制权重敏感度）进行了详细的消融研究，验证了各组件的必要性。
*   **充分性评价**：实验设计较为客观、公平。通过在不同规模的训练集（2.1K vs 39K）和不同基础 RL 算法（GRPO vs DAPO）上的一致性提升，证明了该方法的鲁棒性和普适性。

### 6. 主要结论与发现
*   **轨迹引导的必要性**：单纯引入专家轨迹而不进行重权和过滤，性能提升有限甚至会下降，说明处理分布偏移和噪声至关重要。
*   **性能提升**：TGRL 在所有基准测试中均显著优于基线。在 39K 数据规模下，TGRL-DAPO 达到了 60.68 的平均分，超越了现有的 SOTA 方法。
*   **弥合鸿沟**：实验证明 TGRL 能有效增强模型在推理过程中对视觉事实的“忠实度”，减少了即便看对也做错的情况。

### 7. 优点
*   **理论支撑**：通过对多模态推理梯度的系统分析，精准定位了“感知-推理转换”这一关键瓶颈。
*   **即插即用**：作为一种通用的引导机制，可以轻松增强现有的 RLVR 算法，具有很强的工程实用性。
*   **高效性**：仅需极少量的专家轨迹（如 1/8 的比例）即可实现显著的性能增益。

### 8. 不足与局限
*   **专家依赖**：性能上限在一定程度上受限于专家模型的质量。
*   **算力细节缺失**：未披露具体的算力消耗，不便于其他研究者评估复现成本。
*   **任务覆盖**：主要集中在数学和逻辑推理任务，对于更感性的多模态任务（如艺术理解、复杂视频推理）的适用性尚待验证。
*   **离策风险**：尽管有修正机制，但在极端分布差异下，离策轨迹仍可能带来训练不稳定的风险。

（完）
