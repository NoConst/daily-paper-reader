---
title: "Chain of Modality: From Static Fusion to Dynamic Orchestration in Omni-MLLMs"
title_zh: 模态链：全模态大语言模型中从静态融合到动态编排的演进
authors: "Ziyang Luo, Nian Liu, Junwei Han"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14520v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 识别 MLLM 中的位置和模态偏差
tldr: 针对全模态大语言模型中多模态推理性能常低于单模态的“性能悖论”，本文揭示了静态融合拓扑导致的结构性偏差，并提出了Chain of Modality (CoM) 框架。该框架将多模态融合从被动拼接转变为动态编排，通过自适应调整输入拓扑并引入双路径认知执行逻辑，在无需大量训练的情况下显著提升了模型在各类基准测试中的鲁棒性和泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14520v1/fig-001.webp\", \"caption\": \"Figure 1: Comparison of Omni-modal inference paradigms. (a) Sequential formats suffer frommodality bias, where dominant visual features often hijack the model’s final prediction. (b) Interleaved formats fall into the alignment trap, coercing the model into hallucinating semantic consistency between discordant signals. (c) Our CoM framework enables dynamic orchestration, adaptively configuring modality sets, topologies, and cognitive depth based on task complexity.\", \"page\": 1, \"index\": 1, \"width\": 502, \"height\": 331}, {\"url\": \"assets/figures/arxiv/2604.14520v1/fig-002.webp\", \"caption\": \"Table 4: Comprehensive performance comparison on intuitive tasks. Comparison of CoM framework against representative models across five diverse benchmarks with three architectures in training-free setting. Models marked with * are evaluated using our own evaluation scripts. All results are reported in accuracy (%).\", \"page\": 6, \"index\": 2, \"width\": 1022, \"height\": 415}, {\"url\": \"assets/figures/arxiv/2604.14520v1/fig-003.webp\", \"caption\": \"Figure 4: An example of the CoM agentic workflow: from task decomposition (Planner) and evidence-based auditing (Reasoner) to grounded decision-making (Decider), enabling systematic cross-modal reasoning.\", \"page\": 8, \"index\": 3, \"width\": 1052, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2604.14520v1/fig-004.webp\", \"caption\": \"Figure 2: Empirical analysis of modality bias and functional rigidity in Qwen-Omni. (a,b) Layer-wise modality attention trajectories for visual-centric and audio-centric tasks, respectively. (c) Cross-task comparison of average attention scores aggregated across shallow, middle, and deep layers. (d) Sequence sensitivity analysis showing shifts in attention ratios when the modality input order is permuted. (e) Impact of input topology on accuracy and “Yes” response rates across varying interleaved densities against a 15-frame sequential baseline. Here, audio-, visual-, and cross-modal tasks represent queries that rely on auditory cues, visual cues, or joint audio-visual evidence, respectively.\", \"page\": 4, \"index\": 4, \"width\": 1056, \"height\": 412}, {\"url\": \"assets/figures/arxiv/2604.14520v1/fig-005.webp\", \"caption\": \"Figure 3: Architecture of the CoM framework. Our model reconfigures a single Omni-MLLM backbone into three distinct cognitive roles: Planner, Reasoner, and Decider. For each query, the Planner first determines the optimal modality topology and cognitive pathway. For intuitive tasks, the model routes the organized modalities directly to the Decider for response generation. For complex analytical tasks, the Reasoner executes modality-specific evidence auditing, followed by the Decider to synthesize logical rationales into a grounded final response.\", \"page\": 5, \"index\": 5, \"width\": 1052, \"height\": 508}]"
motivation: 现有全模态模型采用的静态融合拓扑存在位置偏差和对齐陷阱，导致多模态联合推理性能往往逊于单模态基准。
method: 提出CoM框架，通过动态编排并行、顺序和交错等输入拓扑，并结合“直接决策”与“推理决策”双路径进行任务执行。
result: 在免训练或数据高效的微调设置下，CoM在多个多样化基准测试中均取得了稳健且一致的性能提升。
conclusion: 将多模态融合从静态连接转向动态编排，能有效消除结构性偏差，是实现高效全模态智能的关键路径。
---

## 摘要
全模态大语言模型（Omni-MLLMs）有望实现多种感官流的统一集成。然而，最近的评估揭示了一个关键的性能悖论：单模态基准往往优于联合多模态推理。我们将这种感知脆弱性归因于当前模型普遍采用的静态融合拓扑结构，并识别出两种结构性病理：顺序输入中的位置偏差和交错格式中的对齐陷阱，无论任务语义如何，这些问题都会系统性地扭曲注意力。为了解决这种功能僵化，我们提出了模态链（Chain of Modality, CoM），这是一个智能体框架，将多模态融合从被动拼接转变为动态编排。CoM 自适应地编排输入拓扑，在并行、顺序和交错路径之间切换，以消除结构偏差。此外，CoM 将认知执行分叉为两条与任务对齐的路径：用于直接感知的精简“直接-决策”路径，以及用于分析审计的结构化“推理-决策”路径。在无需训练或数据高效的微调（SFT）设置下，CoM 在多种基准测试中实现了稳健且一致的泛化。

## Abstract
Omni-modal Large Language Models (Omni-MLLMs) promise a unified integration of diverse sensory streams. However, recent evaluations reveal a critical performance paradox: unimodal baselines frequently outperform joint multimodal inference. We trace this perceptual fragility to the static fusion topologies universally employed by current models, identifying two structural pathologies: positional bias in sequential inputs and alignment traps in interleaved formats, which systematically distort attention regardless of task semantics. To resolve this functional rigidity, we propose Chain of Modality (CoM), an agentic framework that transitions multimodal fusion from passive concatenation to dynamic orchestration. CoM adaptively orchestrates input topologies, switching among parallel, sequential, and interleaved pathways to neutralize structural biases. Furthermore, CoM bifurcates cognitive execution into two task-aligned pathways: a streamlined ``Direct-Decide'' path for direct perception and a structured ``Reason-Decide'' path for analytical auditing. Operating in either a training-free or a data-efficient SFT setting, CoM achieves robust and consistent generalization across diverse benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Chain of Modality: From Static Fusion to Dynamic Orchestration in Omni-MLLMs》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：全模态大语言模型（Omni-MLLMs）在处理多模态任务时存在“性能悖论”——即联合多模态推理的性能往往不如单模态基准。
*   **研究动机**：作者发现这种脆弱性源于当前模型普遍采用的**静态融合拓扑结构**。具体表现为两种病理特征：
    *   **位置偏差（Positional Bias）**：在顺序输入（如先音频后视频）中，模型倾向于关注靠近文本查询的模态，而非语义相关的模态。
    *   **对齐陷阱（Alignment Trap）**：在交错输入格式中，物理上的邻近性会诱导模型虚构（幻觉）出并不存在的跨模态语义一致性。
*   **整体含义**：论文旨在将多模态处理从“被动拼接”转变为“动态编排”，通过自适应调整模态的组织方式来消除结构性偏差。

### 2. 论文提出的方法论：Chain of Modality (CoM)
CoM 是一个将模型重新配置为三个认知角色的智能体框架：
*   **核心思想**：根据任务复杂度动态构建“模态链”，自适应选择模态集合、输入拓扑和认知路径。
*   **关键技术细节**：
    *   **规划器（Planner）**：分析查询意图，确定任务类型（音频中心、视频中心、跨模态等），并选择最佳拓扑：
        *   **并行（Parallel）**：独立审计各模态，防止对齐陷阱。
        *   **顺序（Sequential）**：建立因果推理链。
        *   **交错（Interleaved）**：用于精细的时间同步任务。
    *   **双路径执行**：
        *   **Plan-Decide (PD)**：针对直觉型任务，直接映射结构化模态到答案，保持感知忠实度。
        *   **Plan-Reason-Decide (PRD)**：针对分析型任务，先由**推理器（Reasoner）**进行模态审计生成文本证据，再由**决策器（Decider）**进行逻辑综合。
*   **算法流程**：Planner 生成执行蓝图 -> 根据任务复杂度选择 PD 或 PRD 路径 -> Decider 输出最终答案。

### 3. 实验设计
*   **数据集/场景**：涵盖了 7 个代表性基准测试，包括：
    *   音乐感知：Music-AVQA
    *   开放域场景：AV-Odyssey, DailyOmni, OmniBench, WorldSense
    *   计数与幻觉检测：AV-Counting, AVHBench
*   **对比方法**：
    *   **通用模型**：One-LLM, VideoLLaMA 2
    *   **专用/强化学习模型**：EchoInk, HumanOmniV2, Omni-R1
    *   **训练前沿模型**：ThinkOmni, Qwen2.5-Omni (3B/7B), Ola-7B
*   **实验设置**：分为“免训练（Training-free）”模式和“数据高效微调（SFT）”模式。

### 4. 资源与算力
*   **微调细节**：使用了 Gemini-2.5-Pro 生成的 4,745 条高质量推理轨迹进行 SFT（仅占 Music-AVQA 训练集的 15%）。
*   **硬件与参数**：采用 LoRA 微调（Rank=16），训练 1 个 epoch。
*   **推理算力**：在单张 vGPU 上进行延迟测量。论文对比了预填充（Prefill）和生成（Generate）时间，指出 CoM 通过模态剪枝在某些场景下比原始模型更快。
*   **未明确说明**：未具体列出训练所用的 GPU 型号和具体总时长。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个不同维度的基准测试上进行了验证，涵盖了从 3B 到 7B 不同规模的底座模型（Qwen, Ola）。
*   **消融实验**：
    *   验证了不同拓扑结构（并行 vs 顺序 vs 交错）对不同任务的影响。
    *   测试了对不同提示词（Prompt）模板的鲁棒性。
    *   分析了视觉采样频率（帧数）对性能的影响。
*   **充分性评价**：实验设计较为全面，通过跨模型、跨规模、跨任务的对比，客观地证明了动态编排的有效性。特别是针对“位置偏差”和“对齐陷阱”的实证分析非常深入。

### 6. 论文的主要结论与发现
*   **动态优于静态**：没有一种固定的输入拓扑能胜任所有任务，动态编排是解决多模态干扰的关键。
*   **消除幻觉**：并行拓扑能有效减少因物理邻近导致的跨模态幻觉。
*   **效率提升**：通过 Planner 剔除冗余模态，CoM 可以在减少 66% 视觉 Token 的情况下依然保持甚至超越原始性能。
*   **能力激活**：Omni-MLLMs 内部潜藏着强大的分析能力，PRD 结构能像催化剂一样激活这些能力，而无需大规模全量微调。

### 7. 优点
*   **结构性洞察**：深刻揭示了现有模型在融合层面的底层缺陷（位置偏差和对齐陷阱）。
*   **即插即用**：提出的免训练路径具有极强的通用性，能直接提升现有开源模型的性能。
*   **数据高效**：仅需极少量（约 4.7k 条）数据微调即可在复杂推理任务上媲美经过大规模强化学习的模型。

### 8. 不足与局限
*   **Planner 依赖性**：如果规划器（Planner）解析错误，系统会退回到默认序列，性能增益消失。
*   **底座限制**：在较小规模模型（如 3B）上，由于指令遵循能力较弱，动态编排的成功率受限。
*   **模态扩展**：虽然称为“全模态”，但实验主要集中在音-视-文，对于触觉、热成像等更广泛模态的编排尚未涉及。
*   **推理开销**：虽然有模态剪枝，但 Planner 的额外推理步骤在处理极短查询时可能会增加相对延迟。

（完）
