---
title: "WikiSeeker: Rethinking the Role of Vision-Language Models in Knowledge-Based Visual Question Answering"
title_zh: WikiSeeker：重新思考视觉语言模型在基于知识的视觉问答中的作用
authors: "Yingjian Zhu, Xinming Wang, Kun Ding, Ying Wang, Bin Fan, Shiming Xiang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05818v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 重新定义视觉语言模型在多模态RAG中的角色
tldr: 本研究针对基于知识的视觉问答（KB-VQA）提出WikiSeeker框架。针对现有方法过度依赖图像检索且未充分利用视觉语言模型（VLM）的问题，WikiSeeker重新定义了VLM的角色：作为“精炼器”根据图像重写文本查询以优化检索，作为“检查员”判断检索可靠性并路由生成策略。该框架在多个基准测试上达到SOTA，显著提升了检索精度和回答质量。
selection_source: fresh_fetch
motivation: 现有KB-VQA方法主要依赖图像作为检索键，且未能充分发挥视觉语言模型在检索增强生成中的潜力。
method: 提出WikiSeeker框架，利用VLM作为精炼器优化查询，并作为检查员在检索上下文与模型内部知识间进行智能路由。
result: 在EVQA、InfoSeek和M2KR等多个数据集上取得了最先进的性能，显著提高了检索准确率和最终答案的质量。
conclusion: 通过重新定义VLM在多模态RAG中的角色，可以有效弥合检索与生成之间的差距，提升知识密集型视觉任务的处理能力。
---

## 摘要
多模态检索增强生成 (RAG) 已成为基于知识的视觉问答 (KB-VQA) 的一种高效范式。尽管近期取得了进展，但主流方法仍主要依赖图像作为检索键，且往往忽视或错置了视觉语言模型 (VLMs) 的作用，从而未能充分发挥其潜力。在本文中，我们提出了 WikiSeeker，这是一个新颖的多模态 RAG 框架，通过提出多模态检索器并重新定义 VLM 的角色来弥补这些差距。VLM 不再仅仅充当答案生成器，我们为其分配了两个专门的代理：优化器 (Refiner) 和检查器 (Inspector)。优化器利用 VLM 的能力，根据输入图像重写文本查询，从而显著提高多模态检索器的性能。检查器通过选择性地将可靠的检索上下文路由到另一个大语言模型 (LLM) 进行答案生成，而在检索不可靠时则依赖 VLM 的内部知识，从而实现了一种解耦生成策略。在 EVQA、InfoSeek 和 M2KR 上的广泛实验表明，WikiSeeker 达到了最先进的性能，在检索准确性和答案质量方面均有显著提升。我们的代码将发布在 https://github.com/zhuyjan/WikiSeeker。

## Abstract
Multi-modal Retrieval-Augmented Generation (RAG) has emerged as a highly effective paradigm for Knowledge-Based Visual Question Answering (KB-VQA). Despite recent advancements, prevailing methods still primarily depend on images as the retrieval key, and often overlook or misplace the role of Vision-Language Models (VLMs), thereby failing to leverage their potential fully. In this paper, we introduce WikiSeeker, a novel multi-modal RAG framework that bridges these gaps by proposing a multi-modal retriever and redefining the role of VLMs. Rather than serving merely as answer generators, we assign VLMs two specialized agents: a Refiner and an Inspector. The Refiner utilizes the capability of VLMs to rewrite the textual query according to the input image, significantly improving the performance of the multimodal retriever. The Inspector facilitates a decoupled generation strategy by selectively routing reliable retrieved context to another LLM for answer generation, while relying on the VLM's internal knowledge when retrieval is unreliable. Extensive experiments on EVQA, InfoSeek, and M2KR demonstrate that WikiSeeker achieves state-of-the-art performance, with substantial improvements in both retrieval accuracy and answer quality. Our code will be released on https://github.com/zhuyjan/WikiSeeker.

---

## 论文详细总结（自动生成）

### WikiSeeker：重新思考视觉语言模型在基于知识的视觉问答中的作用

#### 1. 核心问题与整体含义
论文针对**基于知识的视觉问答（KB-VQA）**任务中的现有挑战提出了改进方案。KB-VQA 要求模型结合图像内容与外部百科知识进行推理。
*   **研究动机**：作者发现现有的多模态检索增强生成（RAG）框架存在两个主要缺陷：
    1.  **检索方式单一**：大多仅依赖图像作为检索键，忽略了用户文本查询中的语义信息，导致检索结果不准确。
    2.  **VLM 角色错置**：通常仅将视觉语言模型（VLM）作为答案生成器。实验表明，在处理检索到的文本上下文时，VLM 的信息提取能力往往不如纯文本大语言模型（LLM）。

#### 2. 方法论：WikiSeeker 框架
WikiSeeker 重新定义了 VLM 的角色，将其从单一的生成器转变为两个专门的代理：**Refiner（优化器）**和 **Inspector（检查器）**。
*   **Refiner（优化器）**：
    *   **核心思想**：利用 VLM 的视觉感知能力，根据图像内容重写并扩展原始问题，使其更具描述性（例如将“这个建筑在哪？”扩展为“巴黎拉维莱特港的巴黎之轮（Roue de Paris）的街道地址是什么？”）。
    *   **技术细节**：采用**强化学习（GRPO 算法）**进行优化。奖励函数结合了“格式奖励”（XML 标签正确性）和“检索奖励”（基于检索结果中目标实体的排名）。
*   **多模态密集检索**：
    *   构建包含 `<图像, 文本段落>` 对的知识库。
    *   使用加权拼接策略融合视觉特征（EVA-CLIP）和文本特征（Qwen-Embedding），通过超参数 $\alpha$ 平衡两种模态的贡献。
*   **Inspector（检查器）与解耦生成**：
    *   **Inspector**：VLM 评估检索到的上下文是否足以回答问题且与图像一致。
    *   **路由策略**：若检查通过（PASS），则将上下文交给**纯文本 LLM（Qwen2.5-7B）**提取答案；若不通过（FAIL），则由 VLM 利用其内部参数化知识直接回答。

#### 3. 实验设计
*   **数据集**：
    *   **E-VQA**：包含 100 万样本，侧重细粒度实体。
    *   **InfoSeek**：大规模视觉信息寻求基准，包含 1.3M 训练样本和 8.9k 人工标注测试样本。
    *   **M2KR**：多模态检索综合基准，包含 OKVQA 和 OVEN 等子集。
*   **Benchmark 与对比方法**：
    *   对比了 Wiki-LLaVA, EchoSight, OMGM, ReflectiVA, CoRe-MMRAG 等当前最先进的（SOTA）多模态 RAG 模型。
    *   评估指标包括检索精度（Recall@K）和问答准确率（BEM score, VQA Acc）。

#### 4. 资源与算力
*   **硬件资源**：实验在 **4 台 NVIDIA A800 40GB SXM4 GPU** 上完成。
*   **软件框架**：使用 **LlamaFactory** 进行高效微调，使用 **HybridFlow (Verl)** 框架运行 GRPO 强化学习算法。
*   **训练细节**：Refiner 训练了 600 个 step，global batch size 为 32。

#### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流大基准（EVQA, InfoSeek, M2KR）上进行了全面测试。
*   **消融实验**：详细分析了 Refiner 和 Inspector 的独立贡献，验证了强化学习对 Query 优化的有效性。
*   **参数搜索**：针对模态权重 $\alpha$、重排权重 $\beta_1$ 和文本重排权重 $\beta_2$ 进行了详尽的网格搜索。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了最终精度，还通过“Oracle 分析”探讨了 VLM 与 LLM 协作的理论上限，并提供了推理效率分析。

#### 6. 主要结论与发现
*   **SOTA 性能**：WikiSeeker 在所有基准测试中均达到领先水平，尤其在 EVQA 上比之前最好的方法提升了 5.45%。
*   **Refiner 的价值**：通过 VLM 重写查询，检索精度（Recall@1）在多个数据集上实现了大幅跃升。
*   **解耦生成的优越性**：证实了“VLM 负责视觉验证 + LLM 负责文本理解”的解耦策略优于单一 VLM 生成。

#### 7. 优点
*   **角色创新**：打破了 VLM 仅作为生成器的思维定式，将其引入检索优化和质量监控环节。
*   **RL 驱动的优化**：利用强化学习让模型自主学习如何生成更有利于检索的 Query，无需昂贵的人工标注对。
*   **多模态融合**：提出的加权密集检索策略灵活且高效。

#### 8. 不足与局限
*   **硬路由限制**：目前的 Inspector 采用“全有或全无”的硬路由策略，可能在某些边缘案例中丢失信息。
*   **单跳限制**：目前主要针对单跳检索任务，尚不支持需要多次检索的复杂多跳（Multi-hop）问题。
*   **计算开销**：虽然推理效率尚可，但引入 Refiner 和 Inspector 增加了推理链路的复杂度和模型调用的次数。

（完）
