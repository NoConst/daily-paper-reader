---
title: Uncertainty-guided Compositional Alignment with Part-to-Whole Semantic Representativeness in Hyperbolic Vision-Language Models
title_zh: 双曲视觉语言模型中具有局部到整体语义代表性的不确定性引导组合对齐
authors: "Hayeon Kim, Ji Ha Jang, Junghun James Kim, Se Young Chun"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22042v2"
tags: ["query:cir-ml"]
score: 9.0
evidence: 双曲视觉语言模型中的组合对齐与局部-整体关系
tldr: 针对视觉语言模型在处理多对象组合场景及层级关系时的局限性，本文提出了UNCHA框架。该方法利用双曲空间建模层级结构，并引入不确定性引导的组合对齐机制。通过为不同代表性的局部图像分配双曲不确定性，并结合不确定性加权的对比损失和蕴含损失，UNCHA显著提升了模型对复杂场景的理解能力，在多项基准测试中达到SOTA水平。
selection_source: fresh_fetch
motivation: 现有的双曲视觉语言模型未能考虑到图像局部对整体语义代表性的差异，导致难以精确建模复杂的组合结构。
method: 提出UNCHA框架，利用双曲不确定性衡量局部对整体的代表性，并将其应用于不确定性引导的对比损失和带熵正则化的蕴含损失中。
result: 在零样本分类、检索和多标签分类等多个基准测试中均取得了当前最优（SOTA）的性能表现。
conclusion: 通过引入不确定性引导的层级对齐，双曲视觉语言模型能更准确地捕捉图像内部的组合结构和局部-整体关系。
---

## 摘要
尽管视觉语言模型（VLM）取得了显著的性能，但其欧几里得嵌入在捕捉局部到整体或父子结构等层级关系方面仍然存在局限性，并且在多对象组合场景中经常面临挑战。双曲 VLM 通过更好地保留层级结构并通过蕴含关系（即整个场景及其局部图像）建模局部-整体关系来缓解这一问题。然而，现有方法并未建模每个局部对整体具有不同程度的语义代表性。我们提出了不确定性引导的组合双曲对齐（UNCHA）来增强双曲 VLM。UNCHA 利用双曲不确定性建模局部到整体的语义代表性，通过为对整个场景更具代表性的局部分配较低的不确定性，而为代表性较差的局部分配较高的不确定性。随后，这种代表性通过不确定性引导的权重被纳入对比目标函数中。最后，利用基于熵项正则化的蕴含损失对不确定性进行进一步校准。通过所提出的损失函数，UNCHA 学习到了具有更准确局部-整体排序的双曲嵌入，捕捉了图像中潜在的组合结构，并提高了对复杂多对象场景的理解。UNCHA 在零样本分类、检索和多标签分类基准测试中达到了最先进的性能。我们的代码和模型可在以下地址获取：https://github.com/jeeit17/UNCHA.git。

## Abstract
While Vision-Language Models (VLMs) have achieved remarkable performance, their Euclidean embeddings remain limited in capturing hierarchical relationships such as part-to-whole or parent-child structures, and often face challenges in multi-object compositional scenarios. Hyperbolic VLMs mitigate this issue by better preserving hierarchical structures and modeling part-whole relations (i.e., whole scene and its part images) through entailment. However, existing approaches do not model that each part has a different level of semantic representativeness to the whole. We propose UNcertainty-guided Compositional Hyperbolic Alignment (UNCHA) for enhancing hyperbolic VLMs. UNCHA models part-to-whole semantic representativeness with hyperbolic uncertainty, by assigning lower uncertainty to more representative parts and higher uncertainty to less representative ones for the whole scene. This representativeness is then incorporated into the contrastive objective with uncertainty-guided weights. Finally, the uncertainty is further calibrated with an entailment loss regularized by entropy-based term. With the proposed losses, UNCHA learns hyperbolic embeddings with more accurate part-whole ordering, capturing the underlying compositional structure in an image and improving its understanding of complex multi-object scenes. UNCHA achieves state-of-the-art performance on zero-shot classification, retrieval, and multi-label classification benchmarks. Our code and models are available at: https://github.com/jeeit17/UNCHA.git.

---

## 论文详细总结（自动生成）

以下是对论文《Uncertainty-guided Compositional Alignment with Part-to-Whole Semantic Representativeness in Hyperbolic Vision-Language Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的视觉语言模型（VLM，如 CLIP）主要在欧几里得空间中进行对齐，这限制了其捕捉复杂层级关系（如“局部-整体”或“父子”结构）的能力。虽然双曲空间（Hyperbolic Space）因其指数级体积增长特性被引入以建模层级结构，但现有双曲 VLM 存在一个关键缺陷：**它们假设图像的所有局部区域对整体场景的语义贡献是均等的**。
实际上，图像的不同局部（Part）对整体（Whole）的代表性差异巨大（例如，交通标志比路面更能代表“街道场景”）。如果忽略这种代表性差异，会导致模型在处理多对象组合场景时出现对齐模糊，甚至引起嵌入空间的结构坍缩。

### 2. 方法论：UNCHA 框架
论文提出了 **UNCHA**（不确定性引导的组合双曲对齐），核心思想是利用双曲几何特性来量化并引导局部与整体的对齐。

*   **双曲不确定性建模**：利用双曲半径（点到原点的测地距离）作为语义抽象度或不确定性的代理。定义不确定性函数 $u(x) = \log(1 + \exp(-\|x\|_2))$。代表性强的局部被推向远离原点的位置（低不确定性），代表性弱的局部靠近原点（高不确定性）。
*   **不确定性引导的对比损失**：在对比学习中引入自适应温度系数。对于不确定性高的局部，分配更大的温度值，从而降低其在对比损失中的权重，避免噪声局部干扰全局对齐。
*   **不确定性校准的蕴含损失**：
    *   **分段连续蕴含损失**：改进了传统的硬阈值蕴含锥（Entailment Cone）损失，引入类似 Leaky-ReLU 的项，确保即使在局部进入蕴含锥后仍能进行细粒度优化。
    *   **熵正则化**：引入基于熵的项来稳定不确定性估计，防止所有嵌入坍缩到狭窄区域，促进嵌入空间的高效利用。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用 GRIT 数据集（20.5M 图像文本对，包含 35.9M 局部标注）。
    *   **下游任务**：16 个零样本分类数据集（ImageNet, CIFAR 等）、检索数据集（MS-COCO, Flickr30K）、多标签分类（VOC, COCO）、组合性基准（ComCo, SimCo）、局部对齐基准（sDCI）。
*   **对比方法**：包括欧几里得模型 **CLIP**，以及最先进的双曲模型 **MERU**、**ATMG** 和 **HyCoCLIP**。
*   **评估指标**：Top-1 准确率、Recall@K、mAP、层级指标（TIE, LCA 等）以及语义分割的 mIoU。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA A100 GPU**。
*   **训练配置**：Batch Size 为 768，总训练步数为 **500,000 步**。
*   **优化器**：AdamW，学习率 $5 \times 10^{-4}$，采用余弦退火调度。

### 5. 实验数量与充分性
论文进行了极其详尽的实验验证：
*   **多维度基准**：涵盖了从基础分类到复杂组合理解、从全局检索到局部定位的各类任务。
*   **消融实验**：分别验证了不确定性引导、对比损失改进、熵正则化对性能的贡献。
*   **可视化分析**：通过 HoroPCA 观察嵌入空间分布，验证了 UNCHA 解决了双曲空间常见的“窄区域坍缩”问题。
*   **公平性**：所有基准模型均在相同的 GRIT 数据集和训练步数下重新实现，确保了对比的客观性。

### 6. 主要结论与发现
*   **性能领先**：UNCHA 在几乎所有下游任务中均优于现有的双曲和欧几里得 VLM，尤其在多对象场景（ComCo/SimCo）和局部对齐任务中提升显著。
*   **空间利用率**：相比于 HyCoCLIP，UNCHA 的嵌入分布更广且更具结构化，局部与整体在空间上有明显的径向分离。
*   **语义相关性**：实验证实，模型估计的不确定性与局部-整体的语义相似度呈强负相关（$r = -0.739$），证明了不确定性确实捕捉到了语义代表性。

### 7. 优点（亮点）
*   **理论与几何的结合**：巧妙地将双曲半径的物理含义（抽象度）转化为深度学习中的不确定性权重。
*   **解决坍缩问题**：通过熵正则化和校准损失，克服了双曲模型在处理大规模数据时容易出现的表征退化问题。
*   **组合理解能力强**：在处理包含多个不同大小、位置物体的复杂场景时，表现出比 CLIP 更强的鲁棒性。

### 8. 不足与局限
*   **计算复杂度**：虽然双曲运算在现代框架下已优化，但相比纯欧几里得模型，其对数/指数映射及测地距离计算仍增加了额外的计算开销。
*   **数据依赖**：模型高度依赖于带有局部框（Bounding Box）标注的预训练数据（如 GRIT），在缺乏局部标注的大规模弱监督数据上的表现尚未验证。
*   **超参数敏感性**：尽管文中做了敏感性分析，但引入了多个加权系数（$\lambda_1, \lambda_2, \alpha$ 等），在不同规模的模型或数据集上可能需要精细调优。

（完）
