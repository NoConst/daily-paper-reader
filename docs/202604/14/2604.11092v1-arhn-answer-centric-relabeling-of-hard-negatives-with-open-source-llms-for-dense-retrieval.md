---
title: "ARHN: Answer-Centric Relabeling of Hard Negatives with Open-Source LLMs for Dense Retrieval"
title_zh: ARHN：利用开源大语言模型对稠密检索中的难负样本进行以答案为中心的重新标注
authors: "Hyewon Choi, Jooyoung Choi, Hansol Jang, Hyun Kim, Chulmin Yun, ChangWook Jun, Stanley Jungkyu Choi"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11092v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 利用 LLM 为密集检索重新标注困难负样本
tldr: 针对神经检索模型训练中硬负采样带来的标签噪声问题，本文提出ARHN框架。该框架利用开源大模型通过两阶段过程优化负样本：首先提取段落中的答案片段，随后进行列表排序。通过将高相关性负样本重标记为正样本，并过滤掉含答案的模糊负样本，ARHN显著提升了检索性能，为大规模训练提供了低成本且高效的样本精炼方案。
selection_source: fresh_fetch
motivation: 传统的硬负采样容易引入包含正确答案或高度相关的伪负样本，导致训练监督信号不一致并损害检索效果。
method: 利用开源LLM提取段落答案片段并进行列表排序，将高质量负样本重标记为正样本，同时过滤掉含有答案的模糊负样本。
result: 在BEIR基准测试中，ARHN的重标记与过滤结合策略在多个数据集上一致优于单一策略，显著提升了检索器的准确性。
conclusion: ARHN证明了利用开源大模型进行以答案为中心的样本精炼是解决检索标签噪声、提升模型质量的有效且经济的途径。
---

## 摘要
神经检索器通常在包含查询、正向段落和一组难负样本的大规模三元组数据上进行训练。在实践中，难负样本挖掘可能会引入假负样本和其他模糊负样本，包括与查询相关或包含部分答案的段落。这种标签噪声会导致监督信号不一致，并可能降低检索效果。我们提出了 ARHN（以答案为中心的难负样本重新标注），这是一个利用开源大语言模型（LLM）并根据以答案为中心的关联信号来优化难负样本的两阶段框架。在第一阶段，针对每个查询-段落对，ARHN 提示 LLM 生成基于段落的答案片段，或者指出该段落不支持回答。在第二阶段，ARHN 对候选集应用基于 LLM 的列表式排序（listwise ranking），根据对查询的直接可回答性对段落进行排序。排名高于原始正样本的段落被重新标注为额外的正样本。在排名低于正样本的段落中，ARHN 从负样本集中剔除任何包含答案片段的段落，以避免模糊监督。我们在 BEIR 基准测试上评估了 ARHN 的三种配置：仅重新标注、仅过滤以及两者的结合。在不同数据集上，结合策略的效果始终优于任何单一步骤，这表明同时对假负样本进行重新标注和对模糊负样本进行过滤，能为训练神经检索模型提供更清晰的监督信号。通过严格依赖开源模型，ARHN 建立了一个具有成本效益且可扩展的优化流水线，适用于大规模训练。

## Abstract
Neural retrievers are often trained on large-scale triplet data comprising a query, a positive passage, and a set of hard negatives. In practice, hard-negative mining can introduce false negatives and other ambiguous negatives, including passages that are relevant or contain partial answers to the query. Such label noise yields inconsistent supervision and can degrade retrieval effectiveness.   We propose ARHN (Answer-centric Relabeling of Hard Negatives), a two-stage framework that leverages open-source LLMs to refine hard negative samples using answer-centric relevance signals. In the first stage, for each query-passage pair, ARHN prompts the LLM to generate a passage-grounded answer snippet or to indicate that the passage does not support an answer. In the second stage, ARHN applies an LLM-based listwise ranking over the candidate set to order passages by direct answerability to the query. Passages ranked above the original positive are relabeled to additional positives. Among passages ranked below the positive, ARHN excludes any that contain an answer snippet from the negative set to avoid ambiguous supervision.   We evaluated ARHN on the BEIR benchmark under three configurations: relabeling only, filtering only, and their combination. Across datasets, the combined strategy consistently improves over either step in isolation, indicating that jointly relabeling false negatives and filtering ambiguous negatives yields cleaner supervision for training neural retrieval models. By relying strictly on open-source models, ARHN establishes a cost-effective and scalable refinement pipeline suitable for large-scale training.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ARHN (Answer-Centric Relabeling of Hard Negatives)** 的框架，旨在通过开源大语言模型（LLM）优化稠密检索模型的训练数据质量。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在稠密检索（Dense Retrieval）的对比学习训练中，通常使用“硬负样本”（Hard Negatives）来提升模型的区分能力。然而，现有的挖掘方法（如 BM25 或检索器自采样）经常会引入**假负样本（False Negatives）**——即那些实际上包含答案或与查询高度相关的文档，却被错误地标注为负样本。
*   **研究动机**：这种标签噪声会导致监督信号冲突，扭曲嵌入空间的几何结构，从而降低检索器的性能，尤其是在处理域外（OOD）数据时。作者希望利用 LLM 的语义理解能力，从“是否包含答案”的角度重新审视并修正这些负样本的标签。

### 2. 方法论：ARHN 框架
ARHN 采用两阶段流程对训练数据进行精炼：
*   **第一阶段：答案片段生成（Answer Snippet Generation）**
    *   对于每个查询-文档对，提示 LLM 从文档中**逐字提取**一段能够回答查询的片段（Snippet）。
    *   如果文档不包含答案，LLM 必须输出 `NO_ANSWER`。这一步将长文档简化为核心证据，减少了后续排序的噪声。
*   **第二阶段：以答案为中心的列表式重排序（Answer-Centric Reranking）**
    *   将原始正样本片段与所有候选负样本片段放入一个列表中，提示 LLM 根据“回答问题的直接程度和准确性”进行**列表式排序（Listwise Ranking）**。
*   **标签重构策略**：
    *   **重新标注（Relabeling）**：如果某个负样本的片段排名高于原始正样本，则将其提升为“正样本”。
    *   **过滤（Filtering）**：如果负样本片段排名低于正样本但并非 `NO_ANSWER`（即包含部分证据但不够直接），则将其从负样本集中剔除，以消除模糊性。
    *   **保留（Retention）**：只有被判定为 `NO_ANSWER` 的文档才作为真正的硬负样本保留。

### 3. 实验设计
*   **数据集**：使用 BGE 训练集中的 7 个核心数据集进行微调，并在 **BEIR 基准测试**（包含 16 个数据集，涵盖生物医学、新闻、金融等多个领域）上进行零样本（Zero-shot）评估。
*   **基准模型（Backbones）**：
    *   **E5-base**：基于 BERT 的中型双编码器。
    *   **LG-ANNA-Embedding**：基于 Mistral-7B 的大型 LLM 嵌入模型。
*   **对比方法**：
    *   **No Refinement**：原始未处理数据。
    *   **TopK-PercPos**：NV-Retriever 使用的硬负样本挖掘策略。
    *   **RLHN**：使用 GPT-4o/GPT-4o-mini 进行重标注的现有先进方法。

### 4. 资源与算力
*   **算力投入**：
    *   **LLM 推理**：使用 **16 张 NVIDIA A100 GPU** 进行第一阶段和第二阶段的标签生成。
    *   **模型训练**：同样在 A100 GPU 上进行。E5-base 微调 5 个 epoch；Mistral-7B 模型使用 LoRA 技术微调 2 个 epoch。
*   **使用的开源 LLM**：Qwen3-8B、Qwen3-14B 和 **Qwen3-32B**（最终结果基于 32B 版本）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 16 个 BEIR 数据集，进行了详尽的对比。
*   **消融实验**：分别测试了“仅过滤”、“仅重标注”以及“两者结合（R+F）”的效果，验证了各组件的贡献。
*   **模型规模分析**：对比了不同参数量（8B, 14B, 32B）的 LLM 对重标注质量的影响。
*   **人工验证**：随机抽取 500 个样本进行人工标注，计算 Cohen’s Kappa 系数，证明了 LLM 标注与人类判断的高度一致性（32B 模型表现最佳）。
*   **评价**：实验设计客观、公平，通过多模型、多数据集和人工校验充分证明了方法的有效性。

### 6. 主要结论与发现
*   **协同效应**：同时进行“重标注”和“过滤”的效果最好，在 E5-base 上比原始数据提升了 1.3 个 nDCG@10 点。
*   **开源胜过闭源**：使用开源的 Qwen3-32B 表现优于使用 GPT-4o-mini 的 RLHN 方法，证明了该方案的成本效益和可扩展性。
*   **规模效应**：LLM 的规模至关重要。**8B 模型可能会引入更多噪声反而降低性能**，而 32B 模型则能显著提升质量。
*   **泛化能力**：ARHN 在域外（OOD）数据集上的提升尤为明显，说明减少标签噪声能显著增强模型的鲁棒性。

### 7. 优点
*   **以答案为中心**：相比于传统的“段落级”相关性判断，提取“答案片段”后再排序能更精准地识别假负样本。
*   **成本低廉**：完全依赖开源模型，无需支付昂贵的商业 API 费用，适合大规模工业化应用。
*   **逻辑严密**：通过将负样本分为“真负样本”、“假负样本”和“模糊样本”进行分类处理，提供了更清晰的监督信号。

### 8. 不足与局限
*   **推理开销**：虽然训练成本降低，但两阶段的 LLM 推理（尤其是 32B 模型）在处理数百万量级的数据时，计算开销依然巨大。
*   **模型依赖性**：实验表明该方法高度依赖于所选 LLM 的能力，若 LLM 性能不足，可能会适得其反。
*   **语言限制**：论文主要在英文基准（BEIR）上进行测试，尚未验证其在多语言环境下的表现。

（完）
