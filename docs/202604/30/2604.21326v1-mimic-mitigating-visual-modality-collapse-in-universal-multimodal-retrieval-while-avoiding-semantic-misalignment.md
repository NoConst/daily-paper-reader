---
title: "MiMIC: Mitigating Visual Modality Collapse in Universal Multimodal Retrieval While Avoiding Semantic Misalignment"
title_zh: MiMIC：缓解通用多模态检索中的视觉模态崩溃并避免语义错配
authors: "Juan Li, Chuanghao Ding, Xujie Zhang, Cam-Tu Nguyen"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21326v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 缓解多模态检索中的视觉模态坍缩
tldr: 本研究针对通用多模态检索（UMR）中早期融合导致的视觉模态坍缩和后期融合导致的语义失配问题，提出了MiMIC模型。该模型采用解码器融合架构，并通过单模态混合及随机标题丢弃技术增强训练鲁棒性。在WebQA+和EVQA+数据集上的实验表明，MiMIC在处理缺乏标题的图像或查询时，性能显著优于现有基线方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21326v1/fig-001.webp\", \"caption\": \"Figure 2: Different Fusion Strategies. (a) Late Fusion. Use separate image and text encoder (e.g., UniVL-DR). (b) Early Fusion. Concatenates image and text embeddings, which are then fed into the LM. (e.g., Marvel)\", \"page\": 2, \"index\": 1, \"width\": 456, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2604.21326v1/fig-002.webp\", \"caption\": \"Table 3: Retrieval Performance on WebQA+ (based on a mixed-modal corpus D{T,IV ,IV C}) and EVQA+.\", \"page\": 7, \"index\": 2, \"width\": 941, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2604.21326v1/fig-003.webp\", \"caption\": \"Figure 3: Embeddings of T2I Queries, Image Documents (IV C or IV ) and Text Documents (T ).\", \"page\": 3, \"index\": 3, \"width\": 456, \"height\": 528}, {\"url\": \"assets/figures/arxiv/2604.21326v1/fig-004.webp\", \"caption\": \"Figure 7: (left) The encoding of different modalities under our architecture. (right) The multi-modal representation in training and inference phase.\", \"page\": 5, \"index\": 4, \"width\": 456, \"height\": 282}]"
motivation: 旨在解决现有UMR方法中存在的视觉特征被忽略（模态坍缩）或语义关联性不足（语义失配）的缺陷。
method: 提出了MiMIC架构，结合了解码器内融合机制，并引入单模态混合与随机标题丢弃的训练策略。
result: 在WebQA+和EVQA+等多个数据集上，MiMIC的检索性能一致优于早期融合和后期融合的基线模型。
conclusion: MiMIC通过创新的架构和训练策略，有效平衡了多模态融合的深度与语义对齐的准确性，提升了通用多模态检索的鲁棒性。
---

## 摘要
通用多模态检索（UMR）旨在将不同模态（如视觉和文本）映射到一个共享的嵌入空间中，以进行多模态检索。现有的 UMR 方法大致可分为两类：早期融合方法（如 Marvel），它将视觉特征投影到语言模型（LM）空间中以与文本模态集成；以及后期融合方法（如 UniVL-DR），它使用独立的编码器对视觉和文本输入进行编码，并通过加法获得融合后的嵌入。我们的初步研究发现，Marvel 表现出视觉模态崩溃，其特征是模型倾向于忽略视觉特征，而过度依赖文本线索。相比之下，虽然 UniVL-DR 受此问题影响较小，但它更容易受到语义错配的影响，即语义相关的内容在嵌入空间中距离较远。为了应对这些挑战，我们提出了 MiMIC，它引入了两项关键创新：（1）一种用于有效多模态集成的解码器内融合（fusion-in-decoder）架构，以及（2）通过单模态混入（single modality mixin）和随机标题丢弃（random caption dropout）进行的鲁棒训练。在 WebQA+ 和 EVQA+ 数据集上的实验表明（在这些数据集中，文档或查询中的图像可能缺少标题），MiMIC 的表现始终优于早期融合和后期融合的基准模型。

## Abstract
Universal Multimodal Retrieval (UMR) aims to map different modalities (e.g., visual and textual) into a shared embedding space for multi-modal retrieval. Existing UMR methods can be broadly divided into two categories: early-fusion approaches, such as Marvel, which projects visual features into the language model (LM) space for integrating with text modality, and late-fusion approaches, such as UniVL-DR, which encode visual and textual inputs using separate encoders and obtain fused embeddings through addition. Our pilot study reveals that Marvel exhibits visual modality collapse, which is characterized by the model's tendency to disregard visual features while depending excessively on textual cues. In contrast, although UniVL-DR is less affected by this issue, it is more susceptible to semantic misalignment, where semantically related content is positioned far apart in the embedding space. To address these challenges, we propose MiMIC, which introduces two key innovations: (1) a fusion-in-decoder architecture for effective multimodal integration, and (2) robust training through single modality mixin and random caption dropout. Experiments on the WebQA+ and EVQA+ datasets, where image in documents or queries might lack captions, indicate that MiMIC consistently outperforms both early- and late-fusion baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《MiMIC: Mitigating Visual Modality Collapse in Universal Multimodal Retrieval While Avoiding Semantic Misalignment》的结构化深度总结：

### 1. 核心问题与研究动机
通用多模态检索（UMR）旨在将图像和文本映射到统一的共享嵌入空间。论文指出，现有的两类主流融合策略均存在严重缺陷：
*   **早期融合（Early-fusion，如 Marvel）：** 容易出现**视觉模态坍缩（Visual Modality Collapse）**。模型在处理图文混合输入时，倾向于忽略视觉特征而过度依赖文本线索（文本主导现象）。
*   **后期融合（Late-fusion，如 UniVL-DR）：** 容易出现**语义错配（Semantic Misalignment）**。虽然保留了视觉特征，但不同模态表达同一语义时，在嵌入空间中的距离过远，缺乏深度交互。
*   **研究目标：** 提出一种既能防止视觉特征被忽略，又能确保跨模态语义对齐的新型 UMR 架构。

### 2. 方法论：MiMIC 架构与训练策略
MiMIC（Mitigating Modality Collapse）的核心思想是结合独立编码的灵活性与解码器交叉注意力的交互能力。

*   **核心架构：Fusion-in-Decoder (FiD)**
    *   使用独立的编码器（如 CLIP 视觉编码器和 T5 文本编码器）提取特征。
    *   **融合机制：** 并不直接相加或拼接，而是将视觉和文本特征作为 Key-Value 向量，输入到语言模型（LM）的 **Decoder** 中。通过 Decoder 的**交叉注意力（Cross-Attention）**机制动态聚合多模态信息，生成最终的融合嵌入。
*   **关键训练策略：**
    1.  **单模态混合（Single-Modality Mix-in）：** 在训练阶段，将单模态的表示（仅视觉或仅文本）以一定比例 $\alpha$ 随机混入到融合表示中。这确保了模型在优化过程中必须保留单模态的特有信号，防止视觉特征被淹没。
    2.  **随机标题丢弃（Random Caption Dropout）：** 强制模型在没有文本辅助的情况下处理图像，迫使模型优化视觉嵌入，从而缓解“文本主导”问题。
*   **训练流程：** 采用两阶段训练。第一阶段使用对比学习（In-batch Negatives）；第二阶段引入 ANCE（硬负采样）进行精调。

### 3. 实验设计
*   **数据集：** 扩展了两个主流基准，构建了 **WebQA+** 和 **EVQA+**。这些“+”版本特意引入了缺失模态的场景（如 50% 的图像不带标题），以模拟现实中不完美的数据环境。
*   **Benchmark 与对比方法：**
    *   **早期融合基线：** Marvel, VISTA。
    *   **后期融合基线：** UniVL-DR。
    *   **评估指标：** Recall@k (1/5/20/100), MRR, NDCG。

### 4. 资源与算力
*   **硬件环境：** 论文明确提到实验是在**单张 NVIDIA A100 GPU** 上完成的。
*   **训练细节：** 使用了 AdamW 优化器，学习率在 $5 \times 10^{-6}$ 到 $1 \times 10^{-5}$ 之间，最大训练轮数为 20 轮，并采用了早停（Early Stopping）策略。

### 5. 实验数量与充分性
*   **实验规模：** 论文进行了多维度的实验验证，包括：
    *   在两个大规模数据集（WebQA+ 和 EVQA+）上的整体性能对比。
    *   针对 T2T（文搜文）和 T2I（文搜图）子任务的独立评估。
    *   **消融实验：** 分别验证了 FiD 架构、标题丢弃、单模态混合策略的贡献。
    *   **可视化分析：** 使用 t-SNE 观察嵌入空间的分布，直观证明了 MiMIC 解决了坍缩和错配问题。
*   **充分性评价：** 实验设计较为充分且客观。通过对比“完美数据”与“缺失模态数据”，证明了该方法在复杂现实场景下的鲁棒性。

### 6. 主要结论与发现
*   **性能领先：** MiMIC 在所有任务中均优于基线。在 WebQA+ 上，MiMIC-ANCE 的 Recall@100 比最强基线 Marvel 提升了 6.77%。
*   **解决坍缩：** 可视化结果显示，在无标题情况下，Marvel 的嵌入会发生聚类坍缩，而 MiMIC 保持了良好的多样性分布。
*   **语义对齐：** 通过计算跨模态邻域重叠度（Neighborhood Overlap），证明 MiMIC 在不同模态间实现了更好的语义一致性。

### 7. 优点与亮点
*   **架构创新：** 巧妙利用 FiD 结构平衡了“特征独立性”与“交互深度”。
*   **策略实用：** 提出的 Mix-in 和 Dropout 策略简单有效，不增加推理负担，但显著提升了训练质量。
*   **现实意义：** 针对“不完美数据”进行优化，更符合工业界多模态数据质量参差不齐的现状。

### 8. 不足与局限
*   **模型规模限制：** 实验主要基于 T5-base 和 CLIP-B/32，尚未在超大规模多模态模型（如 7B 以上的 MLLM）上验证其扩展性。
*   **超参数敏感性：** 混合比例 $\alpha$ 的最优值在不同数据集上表现不一（WebQA+ 为 0.1，EVQA+ 为 0.5），缺乏一种动态自适应调整 $\alpha$ 的机制。
*   **模态局限：** 目前仅关注图像和文本，尚未涉及音频、视频等更多模态的融合。

（完）
