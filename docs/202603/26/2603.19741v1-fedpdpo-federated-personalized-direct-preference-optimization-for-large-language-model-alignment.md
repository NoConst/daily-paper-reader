---
title: "FedPDPO: Federated Personalized Direct Preference Optimization for Large Language Model Alignment"
title_zh: FedPDPO：面向大语言模型对齐的联邦个性化直接偏好优化
authors: "Kewen Zhu, Liping Yi, Zhiming Zhao, Zhuang Qi, Han Yu, Qinghua Hu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19741v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 大语言模型对齐的个性化联邦框架
tldr: 针对联邦学习中非独立同分布（non-IID）偏好数据导致的大模型对齐性能下降问题，本文提出FedPDPO框架。该框架采用LoRA参数高效微调架构，通过全局共享LoRA与个性化客户端头部、引入显式奖励头以及瓶颈适配器，有效解决了数据异构性并增强了隐式奖励的泛化能力。实验证明其在多项任务中达到SOTA水平，显著提升了对齐准确率。
selection_source: fresh_fetch
motivation: 传统的直接偏好优化（DPO）在联邦学习环境下因数据高度非独立同分布和隐式奖励泛化受限，导致模型对齐性能严重退化。
method: 提出一种结合全局共享LoRA、个性化客户端头部、显式奖励头及瓶颈适配器的个性化联邦学习框架，以平衡全局知识与本地特征。
result: "在多个偏好数据集上的实验显示，该方法在域内和跨域联邦设置下均取得SOTA性能，平均准确率最高提升了4.80%。"
conclusion: FedPDPO通过多维度的个性化策略有效克服了联邦偏好对齐中的异构性挑战，为大规模语言模型的隐私保护对齐提供了坚实的理论与实践基础。
---

## 摘要
在联邦学习（FL）中，由于偏好数据具有去中心化、隐私敏感且高度非独立同分布（non-IID）的特性，将大语言模型（LLMs）与人类偏好对齐面临巨大挑战。直接偏好优化（DPO）为基于人类反馈的强化学习（RLHF）提供了一种高效的替代方案，但其在联邦学习中的直接应用在非独立同分布数据下会遭受严重的性能退化，且隐式奖励的泛化能力有限。为了弥补这一差距，我们提出了 FedPDPO（联邦个性化直接偏好优化），这是一种用于大语言模型偏好对齐的个性化联邦框架。该框架采用参数高效微调架构，每个客户端维护一个冻结的预训练大语言模型主干，并辅以低秩自适应（LoRA）适配器，从而实现通信高效的聚合。为了应对非独立同分布的异构性，我们设计了：（1）结合个性化客户端特定 LLM 头的全局共享 LoRA 适配器；（2）一种带有客户端特定显式奖励头的个性化 DPO 训练策略，以补充隐式奖励并进一步缓解非独立同分布异构性；以及（3）一个瓶颈适配器来平衡全局和局部特征。我们提供了理论分析，确立了其概率基础和合理性。在多个偏好数据集上的广泛实验证明了该方法具有当前最先进的性能，在联邦域内和跨域设置中平均准确率提升高达 4.80%。

## Abstract
Aligning large language models (LLMs) with human preferences in federated learning (FL) is challenging due to decentralized, privacy-sensitive, and highly non-IID preference data. Direct Preference Optimization (DPO) offers an efficient alternative to reinforcement learning with human feedback (RLHF), but its direct application in FL suffers from severe performance degradation under non-IID data and limited generalization of implicit rewards. To bridge this gap, we propose FedPDPO (Federated Personalized Direct Preference Optimization), a personalized federated framework for preference alignment of LLMs. It adopts a parameter-efficient fine-tuning architecture where each client maintains a frozen pretrained LLM backbone augmented with a Low-Rank Adaptation (LoRA) adapter, enabling communication-efficient aggregation. To address non-IID heterogeneity, we devise (1) the globally shared LoRA adapter with the personalized client-specific LLM head. Moreover, we introduce (2) a personalized DPO training strategy with a client-specific explicit reward head to complement implicit rewards and further alleviate non-IID heterogeneity, and (3) a bottleneck adapter to balance global and local features. We provide theoretical analysis establishing the probabilistic foundation and soundness. Extensive experiments on multiple preference datasets demonstrate state-of-the-art performance, achieving up to 4.80% average accuracy improvements in federated intra-domain and cross-domain settings.

---

## 论文详细总结（自动生成）

### 论文总结：FedPDPO —— 面向大语言模型对齐的联邦个性化直接偏好优化

#### 1. 核心问题与整体含义（研究动机和背景）
在实际应用中，大语言模型（LLM）的偏好对齐数据（如医疗、金融数据）通常分散在不同机构且受隐私保护。联邦学习（FL）是解决这一问题的天然方案。然而，直接将现有的**直接偏好优化（DPO）**应用于联邦环境面临两大挑战：
*   **非独立同分布（non-IID）数据异构性**：不同客户端的偏好分布差异巨大，导致全局模型难以兼顾所有用户的个性化需求，性能严重退化。
*   **隐式奖励的泛化受限**：DPO 依赖于从策略模型中推导出的隐式奖励，在面对高度多样化或分布外（OOD）的偏好数据时，其泛化能力较弱。

#### 2. 方法论：核心思想与关键技术
论文提出了 **FedPDPO** 框架，旨在通过个性化联邦学习实现高效的 LLM 偏好对齐。其核心思想是“全局共享知识，本地保留个性”。
*   **参数高效的架构分解**：
    *   **冻结主干（Backbone）**：保持预训练 LLM 参数不变。
    *   **全局 LoRA 适配器**：在主干中注入 LoRA 模块，仅聚合和共享 LoRA 参数，以降低通信开销并捕获全局偏好知识。
    *   **个性化头部（Heads）**：每个客户端维护独立的 **LLM 生成头**和**显式奖励头**，不参与聚合，以适应本地特定的偏好分布。
*   **个性化 DPO 训练策略（PDPO）**：
    *   引入一个**显式奖励头**（MLP 结构），计算显式奖励差值（$\Delta_{er}$）。
    *   将 DPO 的隐式奖励差值（$\Delta_{ir}$）与显式奖励差值结合，形成修正后的偏好信号。
    *   通过**自适应缩放因子**平衡两者的量级，增强模型在异构数据下的泛化性。
*   **瓶颈适配器（Bottleneck Adapter）**：
    *   在主干网络与头部之间插入轻量级的瓶颈结构，用于融合全局 LoRA 特征与本地主干特征，平衡全局与局部信号。
*   **交替优化算法**：
    *   本地训练分为两步：先冻结 LoRA 训练个性化模块（头部和适配器），再冻结个性化模块训练 LoRA。

#### 3. 实验设计
*   **数据集与场景**：
    *   **域内（Intra-domain）**：IMDB（情感分析）、Code-Vulnerability-Security（代码安全）。通过奖励边界和编程语言进行 non-IID 划分。
    *   **跨域（Cross-domain）**：WebGPT（问答）、PyDPO（Python代码）、UltraFeedback（指令遵循）。每个客户端分配完全不同的领域任务。
*   **Benchmark 与对比方法**：
    *   对比了 5 种代表性联邦学习方法：**FedAvg (即 FedDPO)**、**Per-FedAvg**、**FedAMP**、**FedPer**、**FedRep**。
    *   每种基线方法均分别结合了 **PPO**（基于强化学习）和 **DPO**（直接优化）两种对齐目标。

#### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA GeForce RTX 3090 GPU (24 GB)**。
*   **实现方式**：通过顺序执行多个客户端来模拟联邦学习环境。
*   **训练时长**：文中未明确给出总训练时长，但提到通信轮数在域内设置为 20 轮，跨域设置为 10 轮。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 客户端到 11 客户端的不同规模设置。
*   **消融实验**：针对“瓶颈适配器”和“显式奖励头”进行了消融分析，验证了各组件的必要性。
*   **超参数敏感性分析**：对 LoRA 秩（Rank）、奖励头维度、适配器维度以及 DPO 温度系数 $\beta$ 进行了详尽的网格搜索实验。
*   **充分性评价**：实验设计较为全面，覆盖了从单域异构到极端跨域异构的多种场景，并使用了 DistilGPT2 和 TinyLlama-1.1B 两种不同规模的模型，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能领先**：FedPDPO 在所有实验设置下均显著优于现有 SOTA 方法，平均准确率提升高达 **4.80%**。
*   **DPO 优于 PPO**：在联邦环境下，基于 DPO 的方法普遍比基于 PPO 的方法更稳定且高效。
*   **个性化的必要性**：实验证明，仅靠全局共享模型无法处理 non-IID 偏好，个性化头部和显式奖励修正对对齐质量至关重要。
*   **理论支撑**：通过数学证明（Gumbel-Bradley-Terry 等价性）确立了 PDPO 目标的理论合理性。

#### 7. 优点
*   **首创性**：这是首个将个性化联邦学习与 DPO 结合用于 LLM 对齐的工作。
*   **通信高效**：仅传输 LoRA 参数（约占总参数的 1% 以下），极大地降低了带宽需求。
*   **鲁棒性强**：通过显式奖励头修正了 DPO 在数据分布不均时的偏差问题。
*   **理论与实践结合**：不仅有显著的实验提升，还提供了严谨的概率基础证明。

#### 8. 不足与局限
*   **模型规模限制**：实验主要集中在小型 LLM（DistilGPT2, TinyLlama-1.1B），尚未在 7B 或更大规模的参数模型上验证其扩展性。
*   **计算开销**：虽然通信高效，但每个客户端需要额外训练奖励头和瓶颈适配器，本地计算负担略高于标准 DPO。
*   **超参数依赖**：PDPO 引入了奖励权重 $w_r$ 和缩放因子等超参数，可能需要针对不同任务进行精细调优。

（完）
