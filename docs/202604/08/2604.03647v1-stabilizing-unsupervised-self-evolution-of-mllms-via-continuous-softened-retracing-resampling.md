---
title: Stabilizing Unsupervised Self-Evolution of MLLMs via Continuous Softened Retracing reSampling
title_zh: 通过连续平滑回溯重采样稳定多模态大语言模型的无监督自我进化
authors: "Yunyao Yu, Zhengxian Wu, Zhuohong Chen, Hangrui Xu, Zirui Liao, Xiangwen Deng, Zhifang Liu, Senyuan Shi, Haoqian Wang"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03647v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大语言模型的无监督自我进化
tldr: 本研究针对多模态大模型（MLLM）无监督自进化中多数投票机制易引入偏见的问题，提出了CSRS框架。该框架引入回溯推理机制（RRM）以探索长尾推理路径，并利用软化频率奖励（SFR）提供连续反馈信号，同时结合视觉语义扰动（VSP）确保模型优先关注数学逻辑而非视觉表象。实验表明，CSRS显著提升了模型在MathVision等基准上的推理性能，在几何任务中达到了SOTA水平，为稳定MLLM自进化提供了新方案。
selection_source: fresh_fetch
motivation: 现有的无监督自进化方法过度依赖多数投票来确定伪标签，这可能导致模型强化自身偏见而非客观正确的推理路径。
method: 提出CSRS框架，包含回溯推理机制以扩展路径探索、软化频率奖励以提供连续信号，以及视觉语义扰动以强化逻辑推理。
result: 在Qwen2.5-VL-7B模型上，CSRS在MathVision等基准测试中显著增强了推理性能，并在几何任务的无监督自进化中取得SOTA结果。
conclusion: CSRS通过优化反馈信号的质量和多样性，有效稳定了MLLM的自进化过程，提升了模型处理复杂多模态推理任务的能力。
---

## 摘要
在多模态大语言模型（MLLMs）的无监督自我进化中，训练后阶段反馈信号的质量对于稳定且有效的学习至关重要。然而，现有的自我进化方法主要依赖多数投票来选择出现频率最高的输出作为伪黄金答案，这可能源于模型的内在偏见，而无法保证推理路径的客观正确性。为了应对这种性能退化，我们提出了多模态大语言模型自我进化中的连续平滑回溯重采样（CSRS）。具体而言，我们引入了一种回溯重推理机制（RRM），使模型能够从锚点开始重新推理，以扩大对长尾推理路径的探索。同时，我们提出了平滑频率奖励（SFR），用连续信号取代二元奖励，并根据采样推理集中答案的频率来校准奖励。此外，结合视觉语义扰动（VSP），CSRS 确保模型优先考虑数学逻辑而非视觉表象。实验结果表明，CSRS 显著提升了 Qwen2.5-VL-7B 在 MathVision 等基准测试上的推理性能。我们在几何任务的无监督自我进化方面取得了当前最先进（SOTA）的结果。我们的代码可在 https://github.com/yyy195/CSRS 获取。

## Abstract
In the unsupervised self-evolution of Multimodal Large Language Models, the quality of feedback signals during post-training is pivotal for stable and effective learning. However, existing self-evolution methods predominantly rely on majority voting to select the most frequent output as the pseudo-golden answer, which may stem from the model's intrinsic biases rather than guaranteeing the objective correctness of the reasoning paths. To counteract the degradation, we propose \textbf{C}ontinuous \textbf{S}oftened \textbf{R}etracing re\textbf{S}ampling (\textbf{CSRS}) in MLLM self-evolution. Specifically, we introduce a Retracing Re-inference Mechanism (\textbf{RRM}) that the model re-inferences from anchor points to expand the exploration of long-tail reasoning paths. Simultaneously, we propose Softened Frequency Reward (\textbf{SFR}), which replaces binary rewards with continuous signals, calibrating reward based on the answers' frequency across sampled reasoning sets. Furthermore, incorporated with Visual Semantic Perturbation (\textbf{VSP}), CSRS ensures the model prioritizes mathematical logic over visual superficiality. Experimental results demonstrate that CSRS significantly enhances the reasoning performance of Qwen2.5-VL-7B on benchmarks such as MathVision. We achieve state-of-the-art (SOTA) results in unsupervised self-evolution on geometric tasks. Our code is avaible at https://github.com/yyy195/CSRS.

---

## 论文详细总结（自动生成）

这是一份关于论文《Stabilizing Unsupervised Self-Evolution of MLLMs via Continuous Softened Retracing reSampling》的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：多模态大语言模型（MLLM）在进行无监督自我进化（Self-Evolution）时，通常依赖“多数投票”（Majority Voting）机制来生成伪标签。这种机制存在严重的**确认偏差**：模型会不断强化其固有的偏见，导致推理路径失去多样性，最终引发“模型崩溃”（Model Collapse），即模型退化为确定性映射，无法探索真正的正确解。
*   **研究背景**：为了摆脱对昂贵人工标注的依赖，无监督后训练（Post-training）成为提升模型推理能力的关键。但在几何推理等复杂任务中，稀疏且粗粒度的二元奖励（0/1）不足以引导模型跨越关键逻辑节点。

### 2. 方法论：CSRS 框架
论文提出了**连续平滑回溯重采样（CSRS）**框架，包含三个核心组件：
*   **回溯重推理机制（RRM）**：
    *   **核心思想**：不再仅仅进行全局随机采样，而是在初始推理路径（母轨迹）中设置“回溯锚点”。
    *   **技术细节**：将推理路径在特定比例（如 0.7 处）截断，从该点开始进行多次局部重采样。这强制模型在关键逻辑决策点进行深度探索，打破初始偏见的累积。
*   **平滑频率奖励（SFR）**：
    *   **核心思想**：用连续的频率信号取代离散的二元奖励。
    *   **算法逻辑**：计算答案在“母轨迹集”与“重推理集”中的频率差异。利用 `tanh` 函数对奖励进行平滑处理，动态放大那些初始频率低但重推理后一致性增强的“长尾”路径奖励，同时抑制高频错误路径的过度强化。
*   **视觉语义扰动（VSP）**：
    *   **核心思想**：在重推理阶段向输入图像添加高斯噪声。
    *   **目的**：迫使模型依赖内在的数学逻辑结构，而非表层的视觉特征或启发式捷径，从而增强推理的鲁棒性。

### 3. 实验设计
*   **基座模型**：主要基于 **Qwen2.5-VL-7B**，同时也验证了 InternVL3-8B 和 ThinkLite-VL-7B。
*   **训练数据集**：Geometry3K、GeoQA、MMR1（作为无监督进化的原始数据源）。
*   **评估基准（Benchmarks）**：
    1.  **MathVision**：衡量多模态数学推理。
    2.  **MathVerse**：评估模型是否真正“看懂”了图表。
    3.  **MathVista**：综合数学推理基准。
    4.  **We-Math**：人类水平的数学推理测试。
*   **对比方法**：主要对比了基座模型（Vanilla）和当前最先进的无监督后训练方法 **MM-UPT**。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A800-80G GPU**。
*   **训练时长**：在 Qwen2.5-VL-7B 上进行 15 个 epoch 的训练，耗时约 **36 小时**。
*   **软件框架**：基于 veRL 框架构建，采用 GRPO（群体相对策略优化）算法。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流基准上进行了详尽测试，并针对 RRM、SFR、VSP 三个模块进行了**消融实验**。
*   **参数敏感性分析**：对回溯率（$\omega$）、重采样次数（$m$）、奖励超参数（$\gamma, \beta$）进行了多组对比实验。
*   **多样性验证**：除了主模型外，还跨越了不同的模型家族（InternVL, ThinkLite）进行验证，证明了方法的普适性。
*   **客观性**：通过 t-SNE 语义空间可视化和熵值变化曲线，从理论和实证两个维度客观展示了 CSRS 如何缓解模型崩溃。实验设计严谨，对比基准统一。

### 6. 主要结论与发现
*   **性能提升**：CSRS 在所有基准上均优于 MM-UPT。在 MathVista 上达到 68.25% 的准确率，相比基座模型提升显著。
*   **缓解崩溃**：理论证明并实验观察到，SFR 机制能有效减缓策略熵的下降速度，保持训练过程中的逻辑多样性。
*   **长尾挖掘**：回溯机制能够发现那些在全局采样中被忽略、但在局部逻辑上更稳健的正确路径。
*   **逻辑优先**：视觉扰动实验证明，模型在干扰下仍能保持推理能力，说明其进化方向是真正的数学逻辑而非视觉作弊。

### 7. 优点与亮点
*   **理论支撑**：论文不仅给出了实验结果，还通过拉格朗日乘数法推导了 SFR 缓解分布极化的数学证明。
*   **局部探索创新**：RRM 机制通过锚点重推理，解决了长程推理中“一步错步步错”导致的采样稀疏问题。
*   **连续奖励设计**：SFR 巧妙地利用频率方差作为奖励校准器，比传统的多数投票更精细。

### 8. 不足与局限
*   **领域局限性**：目前的研究主要集中在数学和几何推理任务上，对于更广泛、开放式的通用场景理解（如常识推理、视频理解）的适用性尚待验证。
*   **计算开销**：虽然比全量采样高效，但重推理机制（RRM）相比简单的多数投票仍增加了额外的推理计算负担。
*   **动态锚点缺失**：目前的锚点选择策略（固定比例）相对静态，未来若能实现基于逻辑不确定性的动态锚点选择，效果可能更佳。

（完）
