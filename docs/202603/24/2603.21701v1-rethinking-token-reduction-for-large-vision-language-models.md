---
title: Rethinking Token Reduction for Large Vision-Language Models
title_zh: 重新思考大型视觉语言模型的 Token 削减
authors: "Yi Wang, Haofei Zhang, Qihan Huang, Anda Cao, Gongfan Fang, Wei Wang, Xuan Jin, Jie Song, Mingli Song, Xinchao Wang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21701v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多轮 VQA 中大型视觉语言模型的标记减少
tldr: 针对大型视觉语言模型（LVLM）在多轮对话中视觉标记过多导致推理成本高的问题，本文提出了一种名为 MetaCompress 的学习型提示无关标记缩减方法。现有方法在多轮对话中往往因过度依赖初始提示或使用启发式指标而表现不佳。MetaCompress 将标记缩减建模为可学习的压缩映射，统一了剪枝与合并操作，并通过高效的训练范式学习最优映射。实验证明，该方法在多轮对话基准测试中实现了优异的效率与准确率平衡，具有良好的泛化能力。
selection_source: fresh_fetch
motivation: 现有的视觉标记缩减方法在多轮对话场景下，由于无法预知后续问题或依赖启发式指标，导致信息丢失或性能受限。
method: 提出 MetaCompress 方法，将标记缩减建模为统一剪枝与合并的可学习压缩映射，并采用数据高效的训练范式进行优化。
result: 在多个多轮对话基准测试和 LVLM 架构上，MetaCompress 均实现了比现有启发式方法更优的效率与准确率权衡。
conclusion: 通过将标记缩减转化为可学习的压缩任务，可以有效解决多轮视觉对话中的推理效率问题，并保持跨轮次的泛化性能。
---

## 摘要
大型视觉语言模型 (LVLMs) 在视觉理解和推理方面表现出色，但过多的视觉 Token 导致了高昂的推理成本。尽管最近的 Token 削减方法缓解了这一问题，但它们主要针对单轮视觉问答 (VQA)，使得更具实际意义的多轮视觉问答 (MT-VQA) 场景在很大程度上尚未得到探索。MT-VQA 引入了额外的挑战，因为后续问题预先未知，且可能涉及图像的任意区域，这使得现有的削减策略失效。具体而言，当前的方法分为两类：提示依赖型 (prompt-dependent) 方法，它们偏向于初始文本提示，并丢弃了对后续轮次有用的信息；提示无关型 (prompt-agnostic) 方法，虽然在技术上适用于多轮设置，但依赖于注意力分数等启发式削减指标，导致性能欠佳。在本文中，我们提出了一种基于学习的提示无关方法，称为 MetaCompress，克服了启发式设计的局限性。我们首先将 Token 削减公式化为一种可学习的压缩映射，将剪枝 (pruning) 和合并 (merging) 等现有形式统一到一个单一的学习目标中。基于这一公式，我们引入了一种数据高效的训练范式，能够在有限的计算成本下学习最优压缩映射。在 MT-VQA 基准测试和多种 LVLM 架构上的广泛实验表明，MetaCompress 在保持跨对话轮次的强大泛化能力的同时，实现了卓越的效率-准确度权衡。我们的代码可在 https://github.com/MArSha1147/MetaCompress 获取。

## Abstract
Large Vision-Language Models (LVLMs) excel in visual understanding and reasoning, but the excessive visual tokens lead to high inference costs. Although recent token reduction methods mitigate this issue, they mainly target single-turn Visual Question Answering (VQA), leaving the more practical multi-turn VQA (MT-VQA) scenario largely unexplored. MT-VQA introduces additional challenges, as subsequent questions are unknown beforehand and may refer to arbitrary image regions, making existing reduction strategies ineffective. Specifically, current approaches fall into two categories: prompt-dependent methods, which bias toward the initial text prompt and discard information useful for subsequent turns; prompt-agnostic ones, which, though technically applicable to multi-turn settings, rely on heuristic reduction metrics such as attention scores, leading to suboptimal performance. In this paper, we propose a learning-based prompt-agnostic method, termed MetaCompress, overcoming the limitations of heuristic designs. We begin by formulating token reduction as a learnable compression mapping, unifying existing formats such as pruning and merging into a single learning objective. Upon this formulation, we introduce a data-efficient training paradigm capable of learning optimal compression mappings with limited computational costs. Extensive experiments on MT-VQA benchmarks and across multiple LVLM architectures demonstrate that MetaCompress achieves superior efficiency-accuracy trade-offs while maintaining strong generalization across dialogue turns. Our code is available at https://github.com/MArSha1147/MetaCompress.

---

## 论文详细总结（自动生成）

以下是对论文《Rethinking Token Reduction for Large Vision-Language Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大型视觉语言模型（LVLM）在处理高分辨率图像时会产生数千个视觉 Token，导致推理时的计算开销（尤其是注意力机制的 $O(n^2)$ 复杂度）和内存占用巨大。
*   **研究背景**：现有的 Token 削减方法主要针对单轮 VQA。
    *   **提示依赖型方法**（如 FastV）：依赖当前问题来决定保留哪些 Token，但在多轮对话（MT-VQA）中，后续问题不可预知，初始轮次丢弃的信息可能对后续轮次至关重要。
    *   **提示无关型方法**（如 PruMerge）：虽然适用于多轮对话，但大多依赖于注意力分数等“启发式指标”，作者通过实验证明这些人工设计的指标并非最优。
*   **整体含义**：本文旨在开发一种**基于学习的、提示无关的** Token 缩减方法，使其在多轮对话场景下既能大幅提升效率，又能保持极高的准确度。

### 2. 方法论：MetaCompress
*   **核心思想**：将 Token 削减建模为一个可学习的压缩映射矩阵 $P$，通过数据驱动的方式学习如何将原始 Token 序列压缩为更短的表示，而不是依赖人工定义的启发式规则。
*   **关键技术细节**：
    *   **统一公式**：将剪枝（Pruning）和合并（Merging）统一为线性投影形式 $\tilde{X}_{IMG} = P X_{IMG}$，其中 $P$ 是稀疏压缩矩阵。
    *   **MetaCompress 模块**：包含位置编码、查询下采样投影（$\tilde{D}_q$）、键投影（$D_k$）和加权内积层。它根据图像序列 $X_{IMG}$ 动态生成适配不同分辨率的压缩矩阵 $P$。
    *   **训练范式**：采用数据高效（Data-efficient）的训练。
    *   **损失函数**：由三部分组成：
        1.  **预测差异损失 ($L_{pred}$)**：最小化压缩前后模型输出分布的 KL 散度。
        2.  **稀疏正则化 ($L_{entropy}$)**：鼓励压缩矩阵的稀疏性。
        3.  **崩溃正则化 ($L_{collapse}$)**：防止所有压缩后的 Token 都指向同一个输入源。

### 3. 实验设计
*   **数据集/场景**：
    *   **MT-VQA-v2**：基于 VQA-v2 构建的 2.5 万组三轮对话。
    *   **MT-GQA**：基于 GQA 构建的 4061 组三轮对话。
    *   **ConvBench**：原生多轮对话评估基准，具有层次化能力评估。
*   **对比方法**：
    *   **Base**：无缩减的原始模型。
    *   **Random/Sample**：随机剪枝或等间距下采样。
    *   **FastV**：典型的提示依赖型剪枝方法。
    *   **PruMerge**：典型的基于启发式注意力的提示无关方法。
*   **适配模型**：LLaVA-1.5 (7B/13B)、LLaVA-NeXT (7B/13B)、InternLM-XComposer-2.5-7B。

### 4. 资源与算力
*   **算力设备**：使用了 **4 台 NVIDIA RTX A6000 GPU**。
*   **训练时长**：以 LLaVA-NeXT-7B 为例，在 90% 缩减率下，总训练时间约为 30 个 GPU 小时（在 4 卡机器上约 **9 小时**）。
*   **训练数据量**：仅使用了约 **2 万条** 样本进行轻量化训练，体现了方法的“数据高效”性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 5 种主流 LVLM 架构。
    *   测试了从 50% 到 95% 的多种 Token 缩减率。
    *   进行了跨数据集的迁移实验（如 MT-GQA 训练模型在 MT-VQA-v2 上测试）。
    *   进行了消融实验验证三个损失项的有效性。
    *   扩展到了视频问答（MT-Video-MME）的迁移测试。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了准确率，还详细对比了首字延迟（TTFT）、端到端生成时间、显存占用和 TFLOPs，提供了全方位的效率评估。

### 6. 主要结论与发现
*   **启发式指标的局限性**：实验发现，学习到的最优保留 Token 与传统的注意力分数（如 [CLS] 注意力）之间没有明显的线性关系，证明了启发式设计的子优化性。
*   **性能优越性**：MetaCompress 在 90% 的 Token 缩减率下，准确率显著高于 FastV 和 PruMerge，且在多轮对话的后续轮次中表现出更强的鲁棒性。
*   **高效性**：在大幅减少计算量（如 TFLOPs 从 95.3 降至 12.7）的同时，保持了与简单下采样（Sample）相当的推理速度。

### 7. 优点
*   **提示无关性**：非常适合实际的多轮对话场景，不需要为每一轮新问题重新计算压缩。
*   **学习型设计**：摆脱了对人工先验的依赖，通过最小化模型预测差异来保留最关键的信息。
*   **通用性与兼容性**：能够无缝集成到具有动态分辨率和多尺度视觉特征的现代 LVLM（如 LLaVA-NeXT）中。
*   **轻量且易迁移**：训练成本极低，且表现出强大的跨任务（如视频）迁移能力。

### 8. 不足与局限
*   **训练依赖**：虽然是轻量化的，但相比于 FastV 等“即插即用”的非学习方法，MetaCompress 仍需要一个简短的训练过程。
*   **插入位置限制**：目前主要在 LLM 解码器之前进行一次性压缩，尚未探索在 LLM 内部多层进行逐层深度压缩的效果。
*   **视频适配**：虽然在视频任务上做了迁移测试，但尚未针对视频的长序列特性进行专门的结构优化。

（完）
