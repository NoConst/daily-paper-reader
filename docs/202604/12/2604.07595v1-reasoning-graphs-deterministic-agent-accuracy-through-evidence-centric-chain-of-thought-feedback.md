---
title: "Reasoning Graphs: Deterministic Agent Accuracy through Evidence-Centric Chain-of-Thought Feedback"
title_zh: 推理图：通过以证据为中心的思维链反馈实现确定性的智能体准确率
authors: Matthew Penaroza
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07595v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 作为智能体持久记忆机制的推理图
tldr: 针对大语言模型智能体在处理相似查询时重复推理且结果不稳定的问题，本文提出“推理图”结构。该方法将针对特定证据的思维链持久化为图中的结构化边，通过证据中心化的反馈机制，使智能体在处理新任务时能追溯历史评价。结合检索图优化，该系统实现了无需重训的自我进化，在多跳问答任务中显著提升了准确性并降低了结果波动。
selection_source: fresh_fetch
motivation: 现有智能体在处理相似查询时无法复用先前的推理经验，导致推理效率低下且结果存在高度不确定性。
method: 提出推理图和检索图结构，通过证据中心化的反向遍历提供历史反馈，并利用流水线规划器优化候选证据筛选。
result: 在多跳问答基准测试中，该方法在不调整模型参数的情况下，实现了准确率的持续提升和结果方差的显著下降。
conclusion: 推理图通过将历史推理经验与具体证据挂钩，为构建确定性强、可追溯且具备自我改进能力的智能体提供了有效路径。
---

## 摘要
语言模型智能体在处理每个查询时都从零开始推理：每当智能体检索证据并进行深思熟虑时，思维链都会被丢弃，而下一个类似的查询在开始时没有任何先前的见解。这导致了较低的准确率和较高的方差，因为同类型的查询可能会不可预测地成功或失败。我们引入了推理图（reasoning graphs），这是一种将智能体针对每个证据的思维链持久化为结构化边的图结构，这些边连接到它们所评估的证据项上。与以往将提炼后的策略存储

## Abstract
Language model agents reason from scratch on every query: each time an agent retrieves evidence and deliberates, the chain of thought is discarded and the next similar query starts with no prior insight. This produces lower accuracy and high variance, as the same type of query can succeed or fail unpredictably. We introduce reasoning graphs, a graph structure that persists an agent's per-evidence chain of thought as structured edges connected to the evidence items they evaluate. Unlike prior memory mechanisms that store distilled strategies as flat records indexed by query similarity or appended by recency, reasoning graphs enable evidence-centric feedback: given a new candidate set, the system traverses all incoming evaluation edges for each evidence item across all prior runs, surfacing how that specific item has been judged before. This backward traversal from evidence inward is a structurally different capability from query-similarity retrieval, because the feedback is tied to the specific evidence the agent is currently examining, not to the query. We further introduce retrieval graphs, a complementary structure that feeds a pipeline planner to tighten the candidate funnel over successive runs. Together, both graphs form a self-improving feedback loop: accuracy rises and variance collapses over successive runs, with every decision fully traceable through the graph. This improvement requires no retraining; the base model remains frozen and all gains come from context engineering via graph traversal. We formalize the graph structure, traversal algorithms, and feedback mechanisms, and describe a sequential cluster evaluation protocol for measuring accuracy convergence and variance collapse on multi-hop question answering benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Reasoning Graphs: Deterministic Agent Accuracy through Evidence-Centric Chain-of-Thought Feedback》的结构化分析报告：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的语言模型（LLM）智能体在处理查询时存在“冷启动”问题，即每次检索和推理都是从零开始，即便面对相似的查询，之前的思维链（CoT）也会被丢弃。
*   **核心问题**：这种“无记忆”状态导致了两个主要缺陷：
    1.  **准确率受限**：智能体无法从过去的正确评价中获益，重复犯错。
    2.  **高方差（不确定性）**：同一类型的查询在不同运行中表现不稳定。研究表明，ReAct 智能体在相同输入下可能产生多种不同的行动序列，导致成功率波动巨大。
*   **整体含义**：本文提出通过“推理图”将智能体对特定证据的评价持久化，实现**以证据为中心（Evidence-Centric）**的反馈，从而让智能体在不进行模型微调的情况下，实现准确率的收敛和方差的消除（即“确定性准确率”）。

### 2. 论文提出的方法论
核心思想是构建两个互补的图结构，形成自我改进的反馈闭环：

*   **推理图 (Reasoning Graphs, $G_R$)**：
    *   **结构**：包含智能体节点、决策节点和证据项节点。
    *   **边属性**：记录“已决策”（结果、置信度）和“已评估”（评价结论、理由、步骤）。
    *   **核心技术：反向遍历（Backward Traversal）**。当新查询检索到某证据时，系统反向遍历该证据的所有历史评价边，构建“证据画像”（Evidence Profile），包含该证据的历史采纳率、典型拒绝理由等，并将其注入当前 Prompt。
*   **检索图 (Retrieval Graphs, $G_P$)**：
    *   **功能**：记录检索元数据（排名、相似度）。
    *   **流水线规划器**：根据历史拒绝率，自动从候选集中剔除那些在同类任务中被频繁拒绝的无效证据，从而收窄候选范围（Candidate Funnel）。
*   **反馈闭环**：
    *   **推理时**：注入证据画像，引导智能体参考历史经验。
    *   **运行后**：原子化写入新的决策和评价边。
    *   **过滤机制**：仅提取“结果正确”的决策路径进行反馈，防止错误逻辑的自我强化。

### 3. 实验设计
论文提出了一个详尽的评估协议（Evaluation Protocol），旨在验证准确率收敛和方差坍缩：

*   **数据集**：
    *   **HotpotQA**：多跳问答，侧重于在干扰项中寻找证据。
    *   **MuSiQue**：更复杂的多跳推理（2-4 步）。
*   **基准测试协议（Sequential Cluster Protocol）**：
    *   将查询按类型聚类，在每个簇内进行顺序处理，模拟智能体在特定领域经验积累的过程。
*   **对比方法（Baselines）**：
    *   **Vanilla RAG**：无反馈的基准。
    *   **Reflexion**：基于最近经验的文本自我反思。
    *   **ReasoningBank**：基于查询相似度检索的提炼策略。
    *   **Ours-QC**：基于查询中心化检索的图结构（用于消融实验）。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **原因**：该方法属于**非训练型（Zero-retraining）**改进。其核心开销在于图数据库的查询遍历和 Prompt 增加的 Token 成本。论文提到，通过检索图剔除无效证据所节省的 Token 往往能抵消注入证据画像带来的成本。

### 5. 实验数量与充分性
*   **实验状态**：需要注意的是，这篇论文（标注日期为 2026 年）更像是一个**框架定义与实验协议论文**。作者在第 5 章节明确表示“本文介绍了框架、定义和协议，但不包含实验结果”，并承诺将发布实验工具链。
*   **充分性评估**：虽然缺乏实测数据，但其设计的实验维度非常全面，包括：
    *   8 个核心假设的验证。
    *   针对正确结果过滤器的消融实验。
    *   噪声鲁棒性实验（模拟错误标签的影响）。
    *   跨模型迁移实验（用强模型积累图，弱模型使用图）。

### 6. 论文的主要结论与发现
*   **准确率收敛**：随着运行次数增加，证据画像越发丰富，任务准确率在期望上单调递增。
*   **方差坍缩**：智能体的决策逐渐与证据画像中的多数信号对齐，表现出确定性的行为。
*   **全量可审计性**：每一项决策都可以通过图遍历追溯到具体的证据评价逻辑，解决了 LLM 决策的“黑盒”问题。
*   **零重训进化**：无需梯度更新，仅通过上下文工程即可实现智能体能力的持续进化。

### 7. 优点
*   **粒度更细**：相比于传统的“查询相似度”检索，该方法深入到“证据项”级别，反馈更精准。
*   **确定性提升**：直接针对智能体行为不一致的痛点，提出了方差坍缩的概念。
*   **可解释性强**：图结构天然支持审计和故障诊断。
*   **成本优化**：通过收窄候选集，在提升性能的同时有望降低长期推理成本。

### 8. 不足与局限
*   **冷启动问题**：在没有任何历史评价的新任务或新证据面前，系统退化为普通 RAG。
*   **存储膨胀**：评价边随运行次数线性增长，大规模部署时需要考虑图压缩或过期清理策略。
*   **证据识别依赖**：高度依赖于证据的去重（如内容哈希），如果同一事实以不同表述出现，系统无法关联其评价。
*   **错误强化风险**：如果初始的正确性信号（Outcome Signal）存在偏差，可能会导致智能体在错误的逻辑上达成“确定性”。
*   **实证缺失**：目前仅有理论框架和协议，缺乏实际运行的大规模数据支撑。

（完）
