---
title: "IE as Cache: Information Extraction Enhanced Agentic Reasoning"
title_zh: IE 即缓存：信息抽取增强的智能体推理
authors: "Hang Lv, Sheng Liang, Hongchao Gu, Wei Guo, Defu Lian, Yong Liu, Hao Wang, Enhong Chen"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14930v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 将信息抽取作为认知缓存以增强智能体推理
tldr: 本文提出IE-as-Cache框架，将信息抽取（IE）从传统的终点任务转变为增强智能体推理的认知缓存。受计算机分级存储启发，该方法结合查询驱动抽取与缓存感知推理，动态维护中间信息并过滤噪声。在多个大模型和基准测试上的实验证明，该框架显著提升了推理准确率，证明了IE作为可重用认知资源在复杂推理中的巨大潜力。
selection_source: fresh_fetch
motivation: 传统信息抽取通常被视为独立且终结性的任务，未能充分发挥其在多步复杂推理中作为中间结构化信息的复用价值。
method: 提出一种受计算机内存层次结构启发的框架，通过查询驱动的抽取和缓存感知推理，动态维护紧凑的中间信息并过滤无关噪声。
result: 在多个挑战性基准测试和不同大模型上的实验表明，该方法显著提高了智能体的推理准确性。
conclusion: 将信息抽取重新定义为可重用的认知资源，为提升大模型智能体在下游任务中的推理能力提供了有效的新方向。
---

## 摘要
信息抽取旨在从非结构化文本中提取结构化的、与决策相关的关键信息，作为下游理解和推理的基础。然而，传统上它仅被视为一个终点目标：一旦完成抽取，生成的结构化信息通常被孤立地使用，而非在多步推理过程中进行维护和复用。超越这一局限，我们提出了 IE-as-Cache 框架，将信息抽取重新定位为一种认知缓存，以增强智能体推理。受计算机分级存储器的启发，我们的方法将查询驱动的抽取与缓存感知推理相结合，动态维护精简的中间信息并过滤噪声。在多种大语言模型（LLM）上进行的挑战性基准测试实验表明，该方法显著提升了推理准确率，这表明信息抽取可以被有效地重新利用为一种可复用的认知资源，并为未来信息抽取下游应用的研究提供了极具前景的方向。

## Abstract
Information Extraction aims to distill structured, decision-relevant information from unstructured text, serving as a foundation for downstream understanding and reasoning. However, it is traditionally treated merely as a terminal objective: once extracted, the resulting structure is often consumed in isolation rather than maintained and reused during multi-step inference. Moving beyond this, we propose \textit{IE-as-Cache}, a framework that repurposes IE as a cognitive cache to enhance agentic reasoning. Drawing inspiration from hierarchical computer memory, our approach combines query-driven extraction with cache-aware reasoning to dynamically maintain compact intermediate information and filter noise. Experiments on challenging benchmarks across diverse LLMs demonstrate significant improvements in reasoning accuracy, indicating that IE can be effectively repurposed as a reusable cognitive resource and offering a promising direction for future research on downstream uses of IE.

---

## 论文详细总结（自动生成）

这篇论文由中国科学技术大学和华为技术有限公司的研究团队合作完成，提出了一种将信息抽取（IE）作为“认知缓存”来增强大模型智能体推理能力的创新框架。

### 1. 核心问题与整体含义
*   **研究动机**：传统的信息抽取（IE）通常被视为任务的“终点”，即提取出结构化数据后流程就结束了。但在处理长文本或噪声丰富的输入时，大语言模型（LLM）常面临“信息迷失”和无关干扰的问题。
*   **核心思想**：借鉴计算机系统的分级存储架构（如 CPU 缓存），将 IE 重新定位为一种**动态认知缓存**。它作为原始文本（外部存储）与推理智能体（处理器）之间的桥梁，通过维护一个紧凑、结构化且可读写的中间表示，过滤噪声并保留关键证据，从而辅助智能体进行多步复杂推理。

### 2. 方法论
该框架名为 **IE-as-Cache**，主要包含两个协同组件：
*   **查询驱动的信息抽取（初始化缓存）**：
    *   采用“模式解耦抽取”（Schema-Decoupled Extraction）策略。
    *   首先根据用户查询 $Q$ 生成特定的抽取模式 $S$（如表格列名或槽位），然后基于 $S$ 从原始文本 $T$ 中提取初始信息 $E$。
*   **缓存感知智能体推理（动态维护）**：
    *   **推理-行动接口**：智能体不再反复扫描全文本，而是主要在缓存 $C$ 上进行推理。
    *   **按需抽取（内存访问）**：当智能体遇到不确定性时，触发 `seek_information` 动作，针对性地从原始文本中提取新证据。
    *   **缓存更新（写回机制）**：通过一个 LLM 调用实现缓存维护，包括合并新信息、删除冗余或无关内容、剪枝低效细节，确保缓存始终紧凑且与查询高度相关。

### 3. 实验设计
*   **数据集与场景**：
    1.  **TACT (逻辑问答)**：要求在含有大量干扰项的长文本中进行逻辑演绎。
    2.  **Calendar Scheduling (智能规划)**：处理自然语言描述中的日程冲突和约束满足。
    3.  **QMSUM (查询聚焦摘要)**：从高噪声的会议记录对话中提取并总结特定信息。
*   **对比方法（Baselines）**：
    *   **Generic**：标准提示词，直接输入全文本。
    *   **CoT (思维链)**：逐步推理。
    *   **ReAct**：标准的智能体推理-行动循环。
    *   **IE-as-Tool**：将文本转为表格并执行 Pandas 查询（仅用于 TACT 数据集）。
*   **测试模型**：涵盖了 GPT-4o、GPT-4o-mini 以及开源模型 Llama 3.1-8B、Qwen 3 系列（8B 和 0.6B）。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件算力（如 GPU 型号、数量或训练时长）。
*   由于该框架主要基于 Prompting（提示工程）和 Agentic Loop（智能体循环），不涉及大规模模型微调，其资源消耗主要体现在推理阶段的 API 调用次数或本地推理的 Token 消耗上。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同类型的任务族、5 种不同规模的模型上进行了广泛测试。
*   **消融实验**：专门设计了“无更新（w/o update）”实验，验证了动态维护缓存的必要性。
*   **分析深度**：进行了抽取质量与推理准确率的相关性分析，并对比了使用“金标准模式（Gold Schema）”的上限性能。
*   **评价**：实验设计较为全面，涵盖了从 0.6B 到 GPT-4o 的多种参数量级，证明了该方法在不同能力水平的模型上均具有普适性和客观性。

### 6. 主要结论与发现
*   **性能提升**：IE-as-Cache 在所有任务和模型上均显著优于基准方法，尤其在长文本和高噪声环境下表现突出。
*   **小模型受益更大**：对于 0.6B 或 8B 的小模型，显式缓存能显著弥补其长文本处理能力的不足。
*   **动态更新是关键**：静态的一次性抽取不足以支撑多步推理，动态的“读-写”缓存机制对复杂任务至关重要。
*   **高效性**：即使没有完美的抽取模式，该框架生成的“足够好”的缓存也能支持接近最优的推理性能。

### 7. 优点
*   **视角新颖**：将 IE 从“任务终点”转变为“推理基础设施”，为 IE 的下游应用开辟了新思路。
*   **抗噪性强**：通过结构化缓存有效地解决了 LLM 在长文本中间位置信息遗忘（Lost in the Middle）的问题。
*   **架构兼容**：可以无缝集成到现有的 ReAct 等智能体框架中，无需对模型进行微调。

### 8. 不足与局限
*   **成本与延迟**：由于涉及多步智能体循环和多次 IE 抽取/更新调用，相比单次 Prompting，其推理延迟和 Token 成本会更高。
*   **依赖性**：框架的效果高度依赖于模型遵循 Schema 和执行更新指令的能力，在极小规模模型上可能仍存在稳定性风险。
*   **任务覆盖**：目前主要集中在文本推理任务，尚未在多模态或超大规模数据库检索场景下进行验证。

（完）
