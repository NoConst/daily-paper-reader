---
title: "CLEAR: Unlocking Generative Potential for Degraded Image Understanding in Unified Multimodal Models"
title_zh: CLEAR：在统一多模态模型中释放退化图像理解的生成潜力
authors: "Xiangzhao Hao, Zefeng Zhang, Zhenyu Zhang, Linhao Yu, Yao Chen, Yiqian Zhang, Haiyun Guo, Shuohuan Wang, Yu Sun"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04780v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 用于图像理解的统一多模态模型
tldr: 针对现实场景中图像退化（如模糊、噪声）损害多模态理解的问题，本文提出 CLEAR 框架。该框架通过监督微调建立“先生成后回答”的推理模式，利用潜空间表示桥接生成与推理路径，并采用交替 GRPO 强化学习算法进行联合优化。实验表明，CLEAR 在 MMD-Bench 基准测试中显著提升了模型对退化图像的理解鲁棒性，同时保持了在清晰图像上的优异性能。
selection_source: fresh_fetch
motivation: 现有多模态模型在处理退化图像时未能有效利用其生成能力辅助推理，且传统的解码-重编码路径限制了生成与推理的联合优化。
method: 提出包含退化感知微调、潜空间表示桥接以及交替 GRPO 强化学习的三阶段框架，实现生成与推理能力的深度融合。
result: 在 MMD-Bench 上的实验证明 CLEAR 显著增强了模型对退化输入的鲁棒性，且任务驱动的优化能自发提升中间视觉状态的感知质量。
conclusion: 通过将生成能力有机整合进推理流程，可以有效解决统一多模态模型在复杂现实环境下的视觉理解瓶颈。
---

## 摘要
模糊、噪声、压缩和光照不足导致的图像退化严重损害了现实场景中的多模态理解。在单一架构中结合理解与生成的统一多模态模型是应对这一挑战的天然选择，因为它们的生成路径可以对被退化破坏的细粒度视觉结构进行建模。然而，这些模型在处理退化输入时未能利用自身的生成能力。我们将这种脱节归因于两个复合因素：现有的训练方案从未要求模型在推理过程中调用生成，且标准的“解码-再编码”路径不支持有效的联合优化。我们提出了 CLEAR 框架，通过三个递进步骤连接这两种能力：(1) 在退化感知数据集上进行有监督微调，以建立“先生成后回答”的推理模式；(2) 引入潜表征桥接（Latent Representation Bridge），用生成与推理之间的直接、可优化连接取代“解码-再编码”的绕路；(3) 采用交替 GRPO（Interleaved GRPO），这是一种在答案正确性奖励下联合优化文本推理和视觉生成的强化学习方法。我们构建了 MMD-Bench，涵盖了六个标准多模态基准测试中的三个退化严重程度等级。实验表明，CLEAR 在保持清晰图像性能的同时，显著提高了对退化输入的鲁棒性。我们的分析进一步揭示，移除像素级重建监督会产生具有更高感知质量的中间视觉状态，这表明任务驱动的优化与视觉质量是自然对齐的。

## Abstract
Image degradation from blur, noise, compression, and poor illumination severely undermines multimodal understanding in real-world settings. Unified multimodal models that combine understanding and generation within a single architecture are a natural fit for this challenge, as their generative pathway can model the fine-grained visual structure that degradation destroys. Yet these models fail to leverage their own generative capacity on degraded inputs. We trace this disconnect to two compounding factors: existing training regimes never ask the model to invoke generation during reasoning, and the standard decode-reencode pathway does not support effective joint optimization. We present CLEAR, a framework that connects the two capabilities through three progressive steps: (1) supervised fine-tuning on a degradation-aware dataset to establish the generate-then-answer reasoning pattern; (2) a Latent Representation Bridge that replaces the decode-reencode detour with a direct, optimizable connection between generation and reasoning; (3) Interleaved GRPO, a reinforcement learning method that jointly optimizes text reasoning and visual generation under answer-correctness rewards. We construct MMD-Bench, covering three degradation severity levels across six standard multimodal benchmarks. Experiments show that CLEAR substantially improves robustness on degraded inputs while preserving clean-image performance. Our analysis further reveals that removing pixel-level reconstruction supervision leads to intermediate visual states with higher perceptual quality, suggesting that task-driven optimization and visual quality are naturally aligned.

---

## 论文详细总结（自动生成）

这是一份关于论文《CLEAR: Unlocking Generative Potential for Degraded Image Understanding in Unified Multimodal Models》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在现实场景中，图像常受模糊、噪声、压缩和光照不足等“退化”影响，严重削弱了多模态大模型（MLLM）的理解能力。
*   **核心矛盾**：现有的“统一多模态模型”（同时具备理解和生成能力）虽然理论上能通过生成路径修复图像细节，但在实际推理时，生成与理解模块是脱节的。模型不会主动调用生成能力来辅助理解，且传统的“解码-重编码”路径（生成像素再重新编码）存在计算冗余且不可导，限制了联合优化。
*   **整体含义**：本文旨在打破这种脱节，让模型学会“为了更好地理解而进行生成”，通过内部修复受损的视觉特征来提升鲁棒性。

### 2. 方法论
CLEAR 框架通过三个递进步骤实现生成与推理的深度融合：
*   **行为初始化（SFT）**：构建退化感知数据集，训练模型形成“先生成后回答”的思维链（CoT）。对于严重退化的图像，模型需先输出 `<image restore>` 触发生成，再基于修复后的状态回答。
*   **潜表征桥接（Latent Representation Bridge）**：
    *   **技术细节**：取消了将生成的 VAE 潜变量（Latent）解码为像素再用 ViT 重新编码的繁琐过程。
    *   **核心思想**：直接将生成的 VAE 潜表征注入 LLM 的推理上下文。这不仅保留了细粒度的视觉结构，还建立了一条从最终答案到生成模块的可微路径。
*   **交替 GRPO（Interleaved GRPO）**：
    *   **算法流程**：结合了针对文本推理的 GRPO 和针对图像生成的 Flow-GRPO。
    *   **联合优化**：在一次前向传播中同时优化文本 Logits 和生成流的预测速度场。奖励函数（Reward）主要基于**答案的正确性**，而非像素级的重建误差。
    *   **自适应策略**：模型通过强化学习自主决定何时需要生成（针对退化图像）以及何时跳过生成（针对清晰图像）。

### 3. 实验设计
*   **数据集与基准**：
    *   **MMD-Bench（自建）**：基于 MMBench, MM-Vet, MMVP, CV-Bench, MMStar, RealWorldQA 六个主流基准，引入 16 种现实世界退化类型（如运动模糊、高斯噪声、JPEG 压缩等），分为 Low、Mid、Hard 三个等级。
    *   **R-Bench-Dis**：现有的退化图像理解基准，用于验证泛化性。
*   **对比方法**：
    *   **闭源模型**：GPT-4o-mini, Gemini-2.5-Flash 等。
    *   **开源统一模型**：Emu3, Janus-Pro, Bagel（本文的基座模型）。
    *   **变体对比**：纯文本 CoT、外部修复模型（Restoration model + MLLM）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 80GB GPU**。
*   **训练细节**：
    *   **SFT 阶段**：3 个 Epoch，学习率 2e-5。
    *   **RL 阶段（GRPO）**：学习率 1e-6，组大小（Group Size）G=4。
    *   **推理**：去噪步数设置为 30 步。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 7 个大型基准测试，对 16 种退化类型进行了逐一分析。
*   **消融实验**：非常充分。对比了“有无桥接器”、“有无 RL 优化”、“不同奖励权重”以及“潜变量替换实验”，验证了生成内容确实提供了退化图像缺失的信息。
*   **客观性**：使用了 CircularEval（循环评估）减少选项偏见，并采用 LLM-as-judge 评分，实验设计严谨且对比公平。

### 6. 主要结论与发现
*   **性能提升**：CLEAR-RL 在 Hard 退化等级下的平均得分显著超过基座模型 Bagel（提升约 8.5%），在所有开源统一模型中表现最优。
*   **鲁棒性增强**：CLEAR 缩小了清晰图像与退化图像之间的性能差距，证明了生成路径对鲁棒性的贡献。
*   **视觉质量与任务对齐**：**最重要的发现**是，当仅以“答案正确性”为奖励时，模型生成的中间图像在感知质量（如清晰度、纹理）上反而优于传统的像素级 MSE 监督。这说明任务驱动的优化与视觉美学是自然统一的。

### 7. 优点
*   **架构创新**：潜表征桥接器解决了多模态模型中生成与理解路径的“断连”问题，实现了端到端可微。
*   **效率平衡**：通过自适应策略，模型在处理清晰图像时不会增加额外生成开销，仅在必要时调用生成。
*   **理论贡献**：挑战了“必须进行像素级重建监督”的传统观念，证明了强化学习可以引导模型生成“对推理有用”的视觉特征。

### 8. 不足与局限
*   **局部细节修复限制**：对于极小区域的严重损坏（如远处的微小文字），当前的全局去噪生成可能仍无法完全恢复细节。
*   **推理延迟**：虽然是自适应的，但在触发生成时，30 步去噪仍会显著增加推理时间（相比纯理解模型）。
*   **基座依赖**：性能高度依赖于 VAE 的表征能力和 LLM 的推理能力，在更小规模的模型上效果可能受限。

（完）
