---
title: "MG$^2$-RAG: Multi-Granularity Graph for Multimodal Retrieval-Augmented Generation"
title_zh: MG$^2$-RAG：用于多模态检索增强生成的多粒度图
authors: "Sijun Dai, Qiang Huang, Xiaoxing You, Jun Yu"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.04969v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 用于多模态检索的多粒度图
tldr: 针对多模态大模型在跨模态推理中的幻觉和结构缺失问题，本文提出MG$^2$-RAG框架。该框架通过轻量化文本解析与视觉定位构建多粒度知识图谱，将文本实体与视觉区域融合为统一节点。结合多粒度检索机制支持多跳推理，在多项任务中达到SOTA，且大幅降低了建图成本与耗时。
selection_source: fresh_fetch
motivation: 现有的多模态RAG系统在处理复杂推理时，往往忽略结构化依赖或因昂贵的“图转文”处理导致视觉细节丢失。
method: 提出一种轻量级多粒度图框架，通过融合文本实体与视觉区域构建分层知识图谱，并利用多粒度检索机制进行相关性传播。
result: 在检索、问答、推理和分类等任务中均取得领先性能，且建图速度提升43.3倍，成本降低23.9倍。
conclusion: MG$^2$-RAG通过高效的多粒度图表示与检索，为多模态检索增强生成提供了一种兼顾性能与成本的优选方案。
---

## 摘要
检索增强生成 (RAG) 缓解了多模态大语言模型 (MLLM) 中的幻觉问题，但现有系统在处理复杂的跨模态推理时仍面临挑战。扁平向量检索通常忽略结构依赖性，而目前的基于图的方法依赖于昂贵的“翻译为文本”流水线，这会丢弃细粒度的视觉信息。为了解决这些局限性，我们提出了 MG$^2$-RAG，这是一个轻量级的多粒度图 RAG 框架，共同改进了图构建、模态融合和跨模态检索。MG$^2$-RAG 通过将轻量级文本解析与实体驱动的视觉定位相结合，构建了一个分层多模态知识图谱，使文本实体和视觉区域能够融合为统一的多模态节点，从而保留原子证据。基于这种表示，我们引入了一种多粒度图检索机制，该机制聚合密集相似度并在图中传播相关性，以支持结构化的多跳推理。在四个代表性多模态任务（即检索、基于知识的 VQA、推理和分类）上的广泛实验表明，MG$^2$-RAG 始终如一地实现了最先进的性能，同时与先进的基于图的框架相比，显著减少了图构建开销，平均提速 43.3 倍，成本降低 23.9 倍。

## Abstract
Retrieval-Augmented Generation (RAG) mitigates hallucinations in Multimodal Large Language Models (MLLMs), yet existing systems struggle with complex cross-modal reasoning. Flat vector retrieval often ignores structural dependencies, while current graph-based methods rely on costly ``translation-to-text'' pipelines that discard fine-grained visual information. To address these limitations, we propose \textbf{MG$^2$-RAG}, a lightweight \textbf{M}ulti-\textbf{G}ranularity \textbf{G}raph \textbf{RAG} framework that jointly improves graph construction, modality fusion, and cross-modal retrieval. MG$^2$-RAG constructs a hierarchical multimodal knowledge graph by combining lightweight textual parsing with entity-driven visual grounding, enabling textual entities and visual regions to be fused into unified multimodal nodes that preserve atomic evidence. Building on this representation, we introduce a multi-granularity graph retrieval mechanism that aggregates dense similarities and propagates relevance across the graph to support structured multi-hop reasoning. Extensive experiments across four representative multimodal tasks (i.e., retrieval, knowledge-based VQA, reasoning, and classification) demonstrate that MG$^2$-RAG consistently achieves state-of-the-art performance while reducing graph construction overhead with an average 43.3$\times$ speedup and 23.9$\times$ cost reduction compared with advanced graph-based frameworks.

---

## 论文详细总结（自动生成）

以下是对论文 **《MG$^2$-RAG: Multi-Granularity Graph for Multimodal Retrieval-Augmented Generation》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在知识密集型任务中常面临**幻觉**和**缺乏私有知识**的问题。现有的多模态检索增强生成（MM-RAG）主要存在两大瓶颈：
*   **结构化依赖缺失**：传统的扁平向量检索（Flat Vector Retrieval）将模态孤立处理，忽略了证据间的逻辑关联，难以支持复杂的多步推理。
*   **建图成本与信息丢失**：现有的图 RAG 方法通常依赖昂贵的 MLLM 提取三元组（Triplet Extraction），且往往将图像预先“翻译”为文本描述，导致细粒度的视觉特征（如特定区域、物体属性）在抽象过程中丢失。

**MG$^2$-RAG** 的提出旨在通过构建轻量级的、保留原始模态特征的多粒度知识图谱，实现高效且精准的跨模态推理。

### 2. 方法论：核心思想与关键技术
MG$^2$-RAG 包含两个核心模块：**分层多模态知识图谱构建**与**多粒度图检索**。

#### A. 分层多模态知识图谱构建
1.  **轻量级文本解析**：放弃昂贵的 MLLM 提取，改用基于规则和依赖解析（spaCy）的轻量级流水线，从文本块中提取实体及其语法关系。
2.  **实体驱动的视觉定位**：利用开放词汇分割模型（如 SAM 3），以提取的文本实体作为提示（Prompt），在关联图像中直接定位并分割出对应的视觉对象（Object）。
3.  **模态保留的节点融合**：将文本实体与对应的视觉对象融合为**统一多模态节点**。这种设计保留了原子级的视觉区域，避免了“图转文”的信息损失。
4.  **多粒度图集成**：构建包含文档块（Chunk）、图像（Image）、多模态节点（Multimodal Node）的异构图，建立上下文边、语义边和定位边。

#### B. 多粒度图检索机制
1.  **多模态种子节点检索**：计算查询（文本或图像）与图中各粒度元素（句子、块、图像、对象）的密集相似度，通过关联矩阵将得分聚合到多模态节点上，激活初始“种子节点”。
2.  **图传播（Graph Propagation）**：采用个性化 PageRank（PPR）算法，在异构图拓扑结构中传播相关性得分。
    *   **公式逻辑**：$r_{\ell+1} = \alpha W r_\ell + (1-\alpha)r_0$，其中 $\alpha$ 控制传播强度，$r_0$ 为初始分布。
3.  **证据提取**：根据收敛后的得分提取 Top-K 文档块，作为 MLLM 生成的上下文。

### 3. 实验设计
*   **任务与数据集**：
    *   **多模态检索与知识问答**：E-VQA（包含单跳和两跳查询）、InfoSeek（侧重未见实体）。
    *   **多模态推理**：ScienceQA（科学常识推理）。
    *   **多模态分类**：CrisisMMD（灾难响应社交媒体数据，含噪声）。
*   **Benchmark 与对比方法**：
    *   **图 RAG 基准**：VaLiK, mKG-RAG, MMGraphRAG, LightRAG。
    *   **检索基准**：EVA-CLIP-8B, Nomic-Embed, CLIP 等。
    *   **生成基准**：GPT-5.2, Qwen3.5-27B, Gemini-3.1-Pro, LLaVA-v1.5 等。

### 4. 资源与算力
*   **硬件环境**：实验在两张 **NVIDIA RTX 6000 Ada (48GB)** GPU 上完成。
*   **算力表现**：论文强调了极高的效率。相比于基于 MLLM 的建图方法，MG$^2$-RAG 实现了平均 **43.3倍** 的加速和 **23.9倍** 的成本降低。
*   **训练/推理时长**：文中未详细列出总训练时长，但明确指出检索阶段的相似度计算和 PPR 传播均在 GPU 上加速，支持大规模推理。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了四个大类任务、五个主流数据集，实验组数非常丰富。
*   **效率对比**：专门针对建图的时间和金钱成本进行了量化对比（图 3），证明了其轻量化特性。
*   **消融与案例分析**：通过可视化案例（图 4, 6, 7）展示了模型在纠正幻觉、处理噪声数据和多步推理中的具体表现。
*   **客观性**：对比了从 7B 到 72B 以及闭源的最强模型（如 GPT-5.2），且在相同硬件和受限数据集（5k 子集）下与基准模型进行了公平对比。

### 6. 主要结论与发现
*   **性能领先**：MG$^2$-RAG 在所有四个任务中均达到了 SOTA 性能。例如在 InfoSeek 检索任务中，R@1 比强基准 mKG-RAG 提升了 9.9%。
*   **效率突破**：通过绕过 MLLM 驱动的三元组提取，大幅降低了工业化部署的门槛。
*   **推理增强**：多粒度图结构能有效捕捉跨模态的逻辑链条，显著提升了 MLLM 在科学推理和灾难分类中的准确性，减少了幻觉。

### 7. 优点（亮点）
*   **轻量化建图**：利用传统的 NLP 工具（spaCy）结合先进的分割模型（SAM 3），在保证结构化信息的同时极大降低了开销。
*   **原生多模态融合**：不同于以往将图像转化为文本的做法，该方法在图节点内部保留了视觉原子证据，实现了真正的“模态保留”。
*   **多粒度检索策略**：结合了密集向量检索的灵活性和图传播的结构化推理能力，能够处理复杂的跨模态依赖。

### 8. 不足与局限
*   **超参数敏感性**：PPR 算法中的阻尼因子 $\alpha$ 和不同粒度节点的权重（$\omega_C, \omega_I$）在不同数据集上差异较大（见附录表 5），可能需要针对新场景进行微调。
*   **依赖预训练模型质量**：虽然建图轻量，但其性能高度依赖于底层视觉定位模型（SAM 3）和嵌入模型（EVA-CLIP）的对齐质量。
*   **动态更新挑战**：论文主要讨论了静态知识库的建图，对于实时流式数据的图动态更新机制探讨较少。

（完）
