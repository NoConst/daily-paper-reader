---
title: "SLQ: Bridging Modalities via Shared Latent Queries for Retrieval with Frozen MLLMs"
title_zh: SLQ：通过共享潜查询桥接模态以实现基于冻结 MLLM 的检索
authors: "Haoran Lou, Ziyan Liu, Chunxiao Fan, Yuexin Wu, Yue Ming"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13710v2"
tags: ["query:cir-ml"]
score: 9.0
evidence: 使用共享潜查询将冻结的多模态大模型适配为检索器
tldr: 针对多模态大模型（MLLM）在检索任务中微调易破坏预训练知识的问题，本文提出SLQ框架。该方法通过在冻结的MLLM末端添加共享潜查询（Shared Latent Queries），利用原生因果注意力机制聚合图文特征，实现高效的跨模态检索。此外，研究者构建了KARR-Bench基准以评估知识感知推理检索能力。实验证明SLQ在多个数据集上优于全量微调和LoRA，有效保留了模型的推理能力。
selection_source: fresh_fetch
motivation: 现有的全量微调或LoRA等侵入式更新方法会破坏MLLM预训练的语义空间和结构化知识，限制了其在复杂检索任务中的表现。
method: 提出SLQ框架，在保持MLLM骨干网络冻结的前提下，通过在文本和图像序列末尾添加共享潜查询来聚合全局特征并生成统一空间的嵌入。
result: SLQ在COCO和Flickr30K上优于全量微调和LoRA，并在新构建的知识感知推理检索基准KARR-Bench上取得了显著的性能提升。
conclusion: 研究表明，通过提取而非重写预训练表示，SLQ为将冻结的MLLM适配为高效检索器提供了一种极具竞争力的方案。
---

## 摘要
多模态大语言模型（MLLM）展现出强大的推理能力和世界知识，但将其适配于检索任务仍然具有挑战性。现有方法依赖于侵入性的参数更新（如全量微调和 LoRA），这可能会破坏预训练的语义空间，并损害对推理至关重要的结构化知识。在这项工作中，我们认为将 MLLM 适配于检索应侧重于诱导预训练表示，而非重写它们。为此，我们提出了 SLQ，这是一个高效且有效的框架，通过一小组共享潜查询（Shared Latent Queries）将冻结的 MLLM 适配为检索器。这些查询被附加在文本和图像标记序列的末尾，利用模型原生的因果注意力机制作为全局聚合接口，在保持主干网络不变的情况下，在统一空间中生成紧凑的嵌入。此外，为了更好地评估超越表层模式匹配的检索能力，我们构建了 KARR-Bench，这是一个专为知识感知推理检索设计的基准测试。广泛的实验表明，SLQ 在 COCO 和 Flickr30K 上的表现优于全量微调和 LoRA，同时在 MMEB 上取得了具有竞争力的性能，并在 KARR-Bench 上获得了显著提升。结果表明，保留了预训练表示的 SLQ 为将 MLLM 适配于检索提供了一个高效且有效的框架。

## Abstract
Multimodal Large Language Models (MLLMs) exhibit strong reasoning and world knowledge, yet adapting them for retrieval remains challenging. Existing approaches rely on invasive parameter updates, such as full fine-tuning and LoRA, which may disrupt the pre-trained semantic space and impair the structured knowledge essential for reasoning. In this work, we argue that adapting MLLMs for retrieval should focus on eliciting pre-trained representations rather than overwriting them. To this end, we propose SLQ, an effective and efficient framework that adapts a frozen MLLM into a retriever through a small set of Shared Latent Queries. Appended to the end of both text and image token sequences, these queries leverage the model's native causal attention to serve as global aggregation interfaces, producing compact embeddings in a unified space while keeping the backbone unchanged. Furthermore, to better evaluate retrieval beyond superficial pattern matching, we construct KARR-Bench, a benchmark designed for knowledge-aware reasoning retrieval. Extensive experiments show that SLQ outperforms full fine-tuning and LoRA on COCO and Flickr30K, while achieving competitive performance on MMEB and yielding substantial gains on KARR-Bench. The results demonstrate that SLQ, which preserves pre-trained representations, provides an effective and efficient framework for adapting MLLMs to retrieval.

---

## 论文详细总结（自动生成）

这是一份关于论文《SLQ: Bridging Modalities via Shared Latent Queries for Retrieval with Frozen MLLMs》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）虽然在推理和世界知识方面表现卓越，但将其转化为高效的**检索器（Retriever）**仍面临挑战。
*   **核心矛盾**：现有的适配方法（如全量微调或 LoRA）属于“侵入式”更新，会破坏 MLLM 预训练的语义空间，导致“灾难性遗忘”，且在大规模对比学习中计算成本极高。
*   **研究动机**：作者认为适配检索不应是“重写”模型参数，而应是“诱导（Elicit）”其内部已有的对齐表示。论文旨在探索能否在不改变 MLLM 主干参数的前提下，将其转化为高质量的嵌入模型。

### 2. 论文提出的方法论：SLQ 框架
**核心思想**：引入一小组**共享潜查询（Shared Latent Queries, SLQ）**作为全局聚合接口，利用 MLLM 原生的因果注意力机制提取特征。

*   **关键技术细节**：
    *   **共享查询**：定义 $N$ 个可学习的潜查询向量（如 $N=20$），这些向量在图像和文本模态间共享，旨在将不同模态投影到统一的嵌入空间。
    *   **因果聚合**：将这些查询向量附加在输入序列（文本 Token 或图像特征 Token）的末尾。由于 Decoder-only 模型采用因果掩码，末尾的查询向量可以“看到”并聚合前面所有的多模态上下文信息。
    *   **嵌入提取**：提取最后 $N$ 个位置对应的隐藏状态，通过**均值池化（Mean Pooling）**和 L2 归一化生成最终的全局表示向量。
    *   **训练流程**：完全冻结 MLLM 主干网络，仅优化这 $N$ 个查询向量。使用对称的 **InfoNCE 损失函数**进行对比学习训练。

### 3. 实验设计
*   **数据集与基准**：
    *   **标准检索**：COCO (5K 测试集) 和 Flickr30K (1K 测试集)。
    *   **通用多模态嵌入**：MMEB 基准（包含 36 个任务，涵盖分类、VQA、检索和定位）。
    *   **新基准 KARR-Bench**：作者自建的“知识感知推理检索基准”，包含 2915 个图像-文本对，分为工具用途、空间关系、文化象征、百科知识等 6 个推理维度，旨在评估模型超越表面模式匹配的深层推理能力。
*   **对比方法**：
    *   **双塔模型**：CLIP, BLIP, SigLIP, FLAME。
    *   **基于 MLLM 的方法**：E5-V, VLM2VEC, MM-Embed, MMRet 等。
    *   **微调策略对比**：全量微调（Full FT）和 LoRA。
*   **基座模型**：InternVL3 (1B, 8B) 和 Qwen3-VL (2B, 4B)。

### 4. 资源与算力
论文在附录中详细列出了算力消耗：
*   **硬件**：使用 NVIDIA **H800 GPU**。
*   **算力规模**：根据模型大小使用 8 到 32 块 GPU。
*   **训练时长与效率**：
    *   **SLQ-1B**：在 COCO 上训练 5 个 epoch 仅需 **3.9 GPU 小时**（比 LoRA 快约 65%）。
    *   **SLQ-8B**：仅需 **38.9 GPU 小时**（比 LoRA 快约 70%）。
    *   **参数量**：可训练参数极少，1B 模型仅 36K 参数，8B 模型仅 144K 参数。

### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多维度验证**：不仅在标准检索任务上对比，还深入分析了模型在 VQA 任务上的能力保留情况（MMMU, OCRBench 等）。
*   **消融实验**：详细探讨了查询数量 $N$ 的影响（发现 $N=20$ 最优）、不同池化策略（Mean vs Max vs Last）、以及与其他 PEFT 方法（Prompt Tuning, Adapter）的对比。
*   **公平性**：在相同的基座模型下对比了不同微调策略，并可视化了模态间隙（Modality Gap）和嵌入分布（PCA），证明了 SLQ 在对齐质量上的优越性。

### 6. 论文的主要结论与发现
*   **性能领先**：SLQ 在 COCO 和 Flickr30K 上优于全量微调和 LoRA；SLQ-8B 在 MMEB 上刷新了 SOTA 记录。
*   **推理能力保留**：在 KARR-Bench 上，SLQ 显著优于侵入式微调方法，证明了冻结主干能有效保留 MLLM 的世界知识和逻辑推理能力。
*   **能力不退化**：全量微调和 LoRA 会显著损害模型的原始 VQA 能力（灾难性遗忘），而 SLQ 几乎没有副作用。
*   **高效对齐**：SLQ 生成的嵌入空间更加紧凑，模态间隙更小，分布更均匀。

### 7. 优点：亮点总结
*   **非侵入式适配**：提出了一种极简的“诱导式”学习方案，解决了生成式模型转判别式任务时的目标冲突问题。
*   **极致的高效性**：以万分之一级别的参数量和极低的算力成本，达到了超越全量微调的效果。
*   **高质量新基准**：KARR-Bench 的提出填补了多模态检索领域缺乏“深层推理”评估的空白。
*   **通用性强**：在不同家族（InternVL, Qwen）和不同规模（1B 到 8B）的模型上均表现出良好的可扩展性。

### 8. 不足与局限
*   **领域自适应限制**：由于主干完全冻结，对于与预训练数据分布差异极大的领域（如医学影像、遥感卫星图），SLQ 可能无法学习到必要的底层视觉特征。
*   **训练目标单一**：目前仅在 InfoNCE 损失下验证，尚未探索三元组损失（Triplet Loss）或蒸馏等其他训练目标。
*   **长序列压力**：虽然查询向量仅 20 个，但在处理极长上下文时，因果注意力的计算开销仍受限于 Transformer 的平方复杂度。

（完）
