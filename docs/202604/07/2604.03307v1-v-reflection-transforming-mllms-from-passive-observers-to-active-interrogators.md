---
title: "V-Reflection: Transforming MLLMs from Passive Observers to Active Interrogators"
title_zh: V-Reflection：将多模态大语言模型从被动观察者转变为主动质询者
authors: "Jiazhou Zhou, Yucheng Chen, Hongyang Li, Qing Jiang, Hu Zhou, Ying-Cong Chen, Lei Zhang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.03307v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 将多模态大模型转变为针对细粒度任务的主动询问者
tldr: 针对多模态大模型在细粒度任务中易产生感知幻觉的问题，本文提出V-Reflection框架。该框架通过“先思后看”机制，将模型从被动观察者转变为主动询问者，利用推理过程中的隐藏状态作为动态探针来检索视觉证据。通过两阶段蒸馏策略，模型在保持高效端到端推理的同时，获得了自主定位任务关键视觉细节的能力。实验表明，该方法在多个感知基准上显著提升了性能。
selection_source: fresh_fetch
motivation: 现有模型将视觉输入视为静态背景，无法在推理过程中动态地重新审视视觉细节，导致在细粒度任务中频繁出现感知幻觉。
method: 采用两阶段蒸馏策略，利用框引导压缩模块建立空间基准，并通过动态自回归压缩模块将隐藏状态映射为主动探测视觉特征的探针。
result: 在六个感知密集型基准测试中显著提升了模型表现，可视化结果证实模型能自主定位任务关键的视觉证据。
conclusion: V-Reflection通过将视觉反射机制内化到潜空间推理中，有效增强了多模态大模型的细粒度感知能力与推理可靠性。
---

## 摘要
多模态大语言模型（MLLMs）已取得显著成功，但在细粒度任务中仍容易产生与感知相关的幻觉。这种脆弱性源于一个根本限制：它们的推理很大程度上局限于语言领域，将视觉输入视为静态的、与推理无关的前导信息，而非动态参与者。因此，目前的模型表现得像被动观察者，无法重新审视视觉细节以锚定其不断演进的推理状态。为了克服这一问题，我们提出了 V-Reflection 框架，通过一种“先思后看”的视觉反思机制，将 MLLM 转变为主动质询者。在推理过程中，潜在状态充当动态探针，主动质询视觉特征空间，为每个推理步骤寻找关键任务证据。我们的方法采用两阶段蒸馏策略：首先，框引导压缩（BCM）模块通过显式的空间锚定建立稳定的像素到潜在目标的映射；接着，动态自回归压缩（DAC）模块将模型的隐藏状态映射为动态探针，以质询全局视觉特征图。通过将 BCM 教师模型的空间专业知识蒸馏到 DAC 学生模型中，V-Reflection 内化了定位关键任务证据的能力。在推理阶段，这两个模块均保持完全停用状态，以最优效率在潜在空间中维持纯粹的端到端自回归解码。广泛的实验证明了 V-Reflection 在六个感知密集型基准测试中的有效性，显著缩小了细粒度感知差距。可视化结果证实，潜在推理能够自主定位关键任务的视觉证据。

## Abstract
Multimodal Large Language Models (MLLMs) have achieved remarkable success, yet they remain prone to perception-related hallucinations in fine-grained tasks. This vulnerability arises from a fundamental limitation: their reasoning is largely restricted to the language domain, treating visual input as a static, reasoning-agnostic preamble rather than a dynamic participant. Consequently, current models act as passive observers, unable to re-examine visual details to ground their evolving reasoning states. To overcome this, we propose V-Reflection, a framework that transforms the MLLM into an active interrogator through a "think-then-look" visual reflection mechanism. During reasoning, latent states function as dynamic probes that actively interrogate the visual feature space, grounding each reasoning step for task-critical evidence. Our approach employs a two-stage distillation strategy. First, the Box-Guided Compression (BCM) module establishes stable pixel-to-latent targets through explicit spatial grounding. Next, a Dynamic Autoregressive Compression (DAC) module maps the model's hidden states into dynamic probes that interrogate the global visual feature map. By distilling the spatial expertise of the BCM teacher into the DAC student, V-Reflection internalizes the ability to localize task-critical evidence. During inference, both modules remain entirely inactive, maintaining a purely end-to-end autoregressive decoding in the latent space with optimal efficiency. Extensive experiments demonstrate the effectiveness of our V-Reflection across six perception-intensive benchmarks, significantly narrowing the fine-grained perception gap. Visualizations confirm that latent reasoning autonomously localizes task-critical visual evidence.

---

## 论文详细总结（自动生成）

以下是对论文《V-Reflection: Transforming MLLMs from Passive Observers to Active Interrogators》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
目前的多模态大语言模型（MLLM）在处理细粒度任务时极易产生**感知幻觉**。其根本原因在于：
*   **被动观察者模式**：模型将视觉输入视为静态的、推理无关的前导信息。一旦自回归推理开始，模型就无法重新审视视觉细节，导致推理过程脱离实际视觉证据，转而依赖语言先验（例如：看到手套就预测是“棉质”，即便视觉特征显示是“橡胶”）。
*   **语言中心化瓶颈**：推理主要在语言域进行，视觉特征在初始编码后即被固定，缺乏动态反馈机制。

本研究旨在将 MLLM 从“被动观察者”转变为**“主动质询者”**，通过引入“先思后看”的视觉反思机制，让模型在推理过程中能根据当前的思维状态主动检索视觉证据。

### 2. 方法论：核心思想与技术细节
V-Reflection 框架通过**两阶段蒸馏策略**实现视觉反思能力的内化：

*   **核心思想**：利用 LLM 的隐藏状态（Hidden States）作为“动态探针”，主动去质询（Interrogate）视觉特征空间，从而在推理的每一步锚定关键证据。
*   **关键模块**：
    1.  **框引导压缩模块 (BCM - 教师)**：在第一阶段，利用目标检测框（Bounding Box）作为先验，通过 RoI-Align 提取局部特征，并使用可学习查询（Queries）将其压缩为高质量的“教师潜在标记”。
    2.  **动态自回归压缩模块 (DAC - 学生)**：在第二阶段，将 LLM 的隐藏状态投影为动态探针，质询全局视觉特征图。
*   **算法流程与损失函数**：
    *   **随机解耦对齐 (Stochastic Decoupled Alignment)**：在第一阶段，通过 Bernoulli 分布随机切换梯度流，防止 BCM 和 LLM 之间的表示坍缩。
    *   **视觉潜在蒸馏 (VLD)**：在第二阶段，通过最小化 MSE 损失（特征对齐）和 KL 散度（空间注意力图对齐），将 BCM 的空间定位能力蒸馏给 DAC。
*   **推理机制**：推理时，BCM 和 DAC 模块均**不激活**。模型通过已内化的潜在推理路径（类似 Coconut 机制），直接在潜在空间中进行端到端的自回归解码，效率极高。

### 3. 实验设计
*   **数据集与场景**：使用了 Visual CoT 数据集进行训练。
*   **评估基准 (Benchmarks)**：涵盖了 6 个感知密集型基准：
    *   **感知鲁棒性**：MMVP（衡量 CLIP 盲点）。
    *   **视觉搜索**：V* Bench。
    *   **认知任务**：BLINK（包含计数、IQ 测试、拼图、空间关系等）。
    *   **高分辨率理解**：HRBench-4K/8K、MME-Real-Lite。
*   **对比方法**：包括 GPT-4o、Qwen2.5-VL-7B（基座模型）、InternVL3、Deepeyes、LVR、Vision-R1 等主流开源及闭源模型。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 × NVIDIA A800 GPU** 集群。
*   **训练时长**：
    *   第一阶段（显式接地预热）：约 24 小时（2500 步）。
    *   第二阶段（视觉潜在蒸馏）：约 12 小时（1250 步）。
*   **模型规模**：以 Qwen2.5-VL-7B 为主干网络。

### 5. 实验数量与充分性
*   **实验规模**：论文在 6 个主流基准上进行了详尽测试，涵盖了从基础感知到高阶逻辑推理的多个维度。
*   **消融实验**：非常充分，分别验证了 BCM/DAC 模块的必要性、损失函数的有效性、两阶段训练的必要性、动态探针来源的影响以及潜在推理步数（S）的最优取值。
*   **客观性**：通过可视化注意力图（Attention Maps）直观展示了模型在推理时如何自主聚焦于任务关键区域，证明了性能提升并非来自简单的过拟合，而是源于感知能力的增强。

### 6. 主要结论与发现
*   **性能显著提升**：在 MMVP 上达到 72.3%，超过 GPT-4o (58.33%) 和基座模型 (66.7%)。在 HRBench-4K 的跨实例感知（FCP）子集上提升了 6.7%。
*   **有效缓解幻觉**：通过“先思后看”机制，模型能够纠正因语言先验导致的错误判断。
*   **推理效率高**：由于推理时无需额外架构，仅增加了极少量的潜在推理步数，参数开销仅增加 1.45%，保持了纯 LLM 的解码速度。

### 7. 优点（亮点）
*   **主动性**：打破了传统 MLLM 被动接收视觉信息的模式，实现了视觉与推理的动态交互。
*   **内化机制**：通过蒸馏将复杂的空间定位能力内化到模型参数中，推理时无需外部工具或复杂的辅助模块。
*   **端到端效率**：在不增加推理 FLOPs 的前提下，显著提升了细粒度感知精度。

### 8. 不足与局限
*   **模态限制**：目前主要集中在静态图像，尚未扩展到视频或具身智能（Embodied AI）等动态场景。
*   **依赖高质量标注**：第一阶段的训练仍依赖于带框标注的数据集（如 Visual CoT），这在一定程度上限制了其在无标注数据上的扩展。
*   **潜在步数平衡**：虽然实验确定了 S=8 为最优，但对于极度复杂的任务，固定的潜在步数可能不足或冗余，缺乏自适应调整机制。

（完）
