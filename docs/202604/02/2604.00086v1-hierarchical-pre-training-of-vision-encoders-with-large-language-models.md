---
title: Hierarchical Pre-Training of Vision Encoders with Large Language Models
title_zh: 利用大语言模型进行视觉编码器的分层预训练
authors: "Eugene Lee, Ting-Yu Chang, Jui-Huang Tsai, Jiajie Diao, Chen-Yi Lee"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00086v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 视觉编码器与大语言模型的分层预训练
tldr: 针对现有视觉-语言模型中视觉编码器与大语言模型集成度低、忽略层级特征的问题，本文提出HIVE框架。通过引入层级交叉注意力机制，实现视觉编码器多层特征与LLM的结构化融合，并配合三阶段训练策略优化对齐。实验证明，HIVE在图像分类及MME、ScienceQA等多个多模态基准测试中均优于传统方法，显著提升了视觉表征学习效率。
selection_source: fresh_fetch
motivation: 现有方法通常将视觉编码器与LLM视为独立模块，限制了层级视觉特征的深度集成与对齐。
method: 提出HIVE框架，利用层级交叉注意力实现多层视觉特征融合，并采用三阶段训练策略进行渐进式对齐。
result: HIVE在MME、GQA、OK-VQA和ScienceQA等多个视觉问答基准上均取得了超越传统自注意力方法的优异表现。
conclusion: 层级特征集成能有效增强视觉-语言对齐，为构建更高效、表达力更强的多模态模型提供了新路径。
---

## 摘要
计算机视觉领域通过可扩展的视觉编码器和多模态预训练框架取得了显著进展。然而，现有方法通常将视觉编码器和大语言模型（LLMs）视为独立的模块，限制了分层视觉特征的整合。在这项工作中，我们提出了 HIVE（视觉编码器分层预训练），这是一个通过在视觉编码器和 LLM 之间引入分层交叉注意力来增强视觉-语言对齐的新型框架。与将图像嵌入扁平化的传统方法不同，HIVE 实现了跨多层的结构化特征融合，改善了梯度流和表示学习。为了优化这种交互，我们引入了一种三阶段训练策略，逐步将视觉编码器与 LLM 对齐，确保了稳定的优化和有效的多模态融合。实证评估表明，HIVE 不仅在图像分类方面表现优异，在各种视觉-语言任务中也取得了卓越性能，在 MME、GQA、OK-VQA 和 ScienceQA 等基准测试中超越了基于自注意力的方法。我们的结果强调了分层特征整合的优势，为开发更高效、更具表达力的视觉-语言模型铺平了道路。

## Abstract
The field of computer vision has experienced significant advancements through scalable vision encoders and multimodal pre-training frameworks. However, existing approaches often treat vision encoders and large language models (LLMs) as independent modules, limiting the integration of hierarchical visual features. In this work, we propose HIVE (Hierarchical Pre-Training of Vision Encoders), a novel framework that enhances vision-language alignment by introducing hierarchical cross-attention between the vision encoder and LLM. Unlike conventional methods that flatten image embeddings, HIVE enables structured feature fusion across multiple layers, improving gradient flow and representation learning. To optimize this interaction, we introduce a three-stage training strategy that progressively aligns the vision encoder with the LLM, ensuring stable optimization and effective multimodal fusion. Empirical evaluations demonstrate that HIVE achieves superior performance not only in image classification but also on various vision-language tasks, outperforming self-attention-based methods in benchmarks such as MME, GQA, OK-VQA, and ScienceQA. Our results highlight the benefits of hierarchical feature integration, paving the way for more efficient and expressive vision-language models.

---

## 论文详细总结（自动生成）

以下是对论文《Hierarchical Pre-Training of Vision Encoders with Large Language Models》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言模型（VLM）通常将视觉编码器（Vision Encoder）和语言模型（LLM）视为独立的模块。常见的做法是将视觉编码器的最后一层输出扁平化后直接输入LLM。
*   **研究动机**：
    *   **特征利用不足**：这种“浅层集成”忽略了视觉编码器内部丰富的层级特征（从底层的纹理到高层的语义）。
    *   **梯度流受阻**：独立模块化的设计限制了梯度在模型层间的有效流动，削弱了表示学习的能力。
    *   **效率瓶颈**：传统的自注意力机制在处理大量视觉Token时计算开销巨大。

### 2. 方法论：HIVE 框架
HIVE（Hierarchical Pre-Training of Vision Encoders）的核心思想是通过**层级交叉注意力**实现视觉与语言的深度融合。
*   **层级特征集成**：
    *   从视觉编码器中均匀采样约 25% 的层（Subset $S$）。
    *   使用轻量级投影器 $g_l$（2层MLP + GELU）将这些中间层特征映射到LLM的特征空间。
*   **层级交叉注意力机制**：
    *   在LLM的对应深度注入交叉注意力层。
    *   **公式逻辑**：Query ($Q_l$) 来自LLM的中间状态，Key ($K_l$) 和 Value ($V_l$) 由视觉编码器的层级特征投影而来。这迫使编码器在早期层保留结构信息，在深层保留抽象语义。
*   **三阶段训练策略**：
    1.  **投影器预训练**：冻结编码器和LLM，仅训练投影器，实现初步对齐。
    2.  **联合训练（LLM + Projector）**：冻结编码器，优化LLM处理多层视觉特征的能力。
    3.  **全量端到端微调**：解锁所有参数，使视觉编码器能够根据LLM的反馈优化其层级表征。

### 3. 实验设计
*   **数据集与场景**：
    *   **图像分类**：CIFAR-10/100、ImageNet-1K、Tiny-ImageNet、Food-101、Stanford Cars、Oxford-IIIT Pets、Caltech-256。
    *   **视觉-语言任务（VLM）**：MME（综合感知与推理）、GQA（视觉问答）、OK-VQA（常识问答）、ScienceQA（科学问答）。
*   **Benchmark 与对比方法**：
    *   **Base**：原始的 CLIP 或 SigLIP 基础模型。
    *   **SA (Self-Attention)**：采用相同三阶段策略但使用传统自注意力机制的基准模型。

### 4. 资源与算力
*   **硬件环境**：实验在**单张 NVIDIA RTX 3090 GPU (24GB VRAM)** 上完成。
*   **训练细节**：
    *   由于显存限制，最大 Batch Size 为 256，并使用了梯度累积技术。
    *   语言模型骨干采用 MobileLLM-350M（预训练阶段）和 Llama-3.2-1B（VLM微调阶段）。
    *   优化器使用 AdamW，配合余弦退火学习率调度。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 8 个分类数据集和 4 个主流 VLM 基准测试，并针对 CLIP 和 SigLIP 两种主流编码器进行了验证。
*   **分析维度**：除了性能指标，还包括梯度流可视化（Gradient Map）、注意力图分析（Attention Map）以及详细的计算复杂度与运行时间分析。
*   **充分性评价**：实验设计较为全面，对比了同等训练条件下的自注意力模型，保证了公平性。但受限于算力（单卡），未能在更大规模的 LLM（如 7B 以上）上进行验证。

### 6. 主要结论与发现
*   **性能提升**：HIVE 在细粒度分类（如 Food-101）和复杂推理任务（如 OK-VQA, ScienceQA）上显著优于自注意力基准。
*   **效率优势**：相比全自注意力机制，HIVE 实现了 **3.43 倍的训练加速**，并减少了约 **59.3% 的峰值显存占用**。
*   **表征优化**：可视化证明 HIVE 能使编码器早期层捕获更锐利、更具结构性的梯度，从而学习到更高质量的视觉特征。
*   **推理友好**：预训练后的编码器在下游任务中可回归标准架构，无需保留复杂的交叉注意力层，兼顾了性能与推理效率。

### 7. 优点与亮点
*   **结构化融合**：打破了“视觉特征扁平化”的固有模式，利用层级对齐提升了多模态理解的深度。
*   **极高的计算效率**：通过选择性层级连接（25% 密度）和绕过 LLM 的部分 MLP 计算，大幅降低了训练门槛。
*   **正则化效应**：层级交叉注意力在预训练中起到了强有力的正则化作用，使编码器保留了多尺度的语义信息。

### 8. 不足与局限
*   **算力规模限制**：实验主要在小参数模型（350M/1B）和单卡环境下进行，其在超大规模模型上的扩展性（Scaling Law）尚待验证。
*   **消融实验深度**：对于“25% 连接密度”这一超参数，论文仅基于初步观察选择，缺乏更详尽的密度对比实验。
*   **应用范围**：目前仅验证了静态图像，尚未扩展到视频等时序多模态数据。

（完）
