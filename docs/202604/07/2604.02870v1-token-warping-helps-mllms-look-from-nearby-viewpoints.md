---
title: Token Warping Helps MLLMs Look from Nearby Viewpoints
title_zh: Token 扭曲助力多模态大语言模型从邻近视角进行观察
authors: "Phillip Y. Lee, Chanho Park, Mingue Park, Seungwoo Yoo, Juil Koo, Minhyuk Sung"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02870v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 通过Token扭曲改进多模态大模型的视觉推理
tldr: 针对多模态大模型（MLLM）在视角变换下表现脆弱的问题，本文提出了一种基于图像 Token 的变形方法（Token Warping）。受人类心理表象理论启发，研究者发现相比于传统的像素级变形，在 ViT 的 Token 空间进行反向变形能更好地保持语义一致性并减少几何畸变。通过在 ViewBench 基准测试上的实验，该方法在近距离视角推理任务中显著优于像素变形和生成式方法，提升了模型的空间推理能力。
selection_source: fresh_fetch
motivation: 传统的像素级变形对深度误差高度敏感且易产生几何畸变，导致 MLLM 在处理视角变化时的视觉推理能力不足。
method: 提出在 ViT 的 Token 空间进行反向变形，通过在目标视图定义密集网格并检索对应的源视图 Token 来实现视角转换。
result: 实验证明 Token 级变形在 ViewBench 基准上一致优于像素级变形、空间微调模型及生成式变形方法。
conclusion: 图像 Token 是进行视角变换的有效表征基础，Token 级变形能显著增强 MLLM 从邻近视角进行可靠推理的能力。
---

## 摘要
扭曲 Token 而非像素，能否帮助多模态大语言模型（MLLMs）理解场景在邻近视角下的呈现方式？尽管 MLLMs 在视觉推理方面表现出色，但它们对视角变化仍显脆弱，因为像素级扭曲对微小的深度误差高度敏感，且往往会引入几何畸变。借鉴心理表象理论（该理论认为局部层级的结构化表征是人类视角转换的基础），我们探讨了基于 ViT 的 MLLMs 中的图像 Token 是否可以作为视角变化的有效基质。我们对比了前向和后向扭曲，发现后向 Token 扭曲（即在目标视图上定义密集网格，并为每个网格点检索对应的源视图 Token）在视角偏移下具有更高的稳定性，并能更好地保持语义连贯性。在我们提出的 ViewBench 基准测试上的实验表明，Token 级扭曲使 MLLMs 能够从邻近视角进行可靠推理，其表现一致优于包括像素级扭曲方法、空间微调的 MLLMs 以及生成式扭曲方法在内的所有基准模型。

## Abstract
Can warping tokens, rather than pixels, help multimodal large language models (MLLMs) understand how a scene appears from a nearby viewpoint? While MLLMs perform well on visual reasoning, they remain fragile to viewpoint changes, as pixel-wise warping is highly sensitive to small depth errors and often introduces geometric distortions. Drawing on theories of mental imagery that posit part-level structural representations as the basis for human perspective transformation, we examine whether image tokens in ViT-based MLLMs serve as an effective substrate for viewpoint changes. We compare forward and backward warping, finding that backward token warping, which defines a dense grid on the target view and retrieves a corresponding source-view token for each grid point, achieves greater stability and better preserves semantic coherence under viewpoint shifts. Experiments on our proposed ViewBench benchmark demonstrate that token-level warping enables MLLMs to reason reliably from nearby viewpoints, consistently outperforming all baselines including pixel-wise warping approaches, spatially fine-tuned MLLMs, and a generative warping method.

---

## 论文详细总结（自动生成）

这篇论文《Token Warping Helps MLLMs Look from Nearby Viewpoints》探讨了如何通过在 Token 空间进行几何变换来增强多模态大语言模型（MLLM）的空间推理能力。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管 MLLM 在视觉问答上表现优异，但在处理视角变化（Viewpoint Changes）时非常脆弱。传统的像素级变形（Pixel-wise Warping）对深度估计误差极其敏感，微小的深度偏差会导致严重的图像畸变和空洞，从而干扰模型的语义理解。
*   **核心思想**：受认知科学中“心理表象（Mental Imagery）”理论启发，人类通过局部结构的表征进行视角转换。论文提出将 ViT（Vision Transformer）中的图像 Token 视为“感知原子”，通过在 Token 级别而非像素级别进行扭曲，利用 Token 对局部空间噪声的鲁棒性，实现更稳定的视角变换模拟。

### 2. 方法论
*   **核心技术：Token 扭曲（Token Warping）**：
    *   **后向扭曲（Backward Warping）**：这是本文推荐的核心策略。首先在目标视图（Target View）上定义一个规则的密集网格，然后利用深度图和相对相机位姿，通过射线投射（Ray Casting）将目标网格点映射回源视图（Source View）的坐标。
    *   **Token 检索策略**：
        *   **最近邻获取（Nearest Fetching）**：直接从源视图中选取距离映射坐标最近的现有 Token。这种方法计算开销极小，且实验证明效果良好。
        *   **自适应获取（Adaptive Fetching）**：以映射坐标为中心，在源图像上重新进行切片（Re-patchify）并编码生成新的 Token。
*   **算法流程**：
    1.  利用源视图深度图构建 3D 代理网格。
    2.  根据目标位姿，计算目标视图网格点到源视图的映射函数 $f_{T \to S}$。
    3.  在源视图提取对应的 Token 或 Patch。
    4.  将这些带有目标视图位置编码的 Token 输入 MLLM 进行推理。
*   **优势**：相比前向扭曲（容易产生空洞和不规则分布），后向扭曲生成的 Token 分布是规则且密集的，符合 MLLM 的预训练分布（In-distribution）。

### 3. 实验设计
*   **数据集与场景**：
    *   主要使用 **ScanNet**（真实室内场景扫描）构建源-目标视图对。
    *   使用 **ProcTHOR**（程序化生成场景）进行遮挡（Occlusion）压力测试。
*   **Benchmark：ViewBench**：
    *   **View-Conditioned Spatial Reasoning**：评估模型在视角切换后，判断物体左右空间关系翻转的能力（分为 Text 和 Shape 两个子集）。
    *   **Target-View Object Description**：评估模型在变换视角后，描述物体细粒度属性的准确性。
*   **对比方法**：
    *   **像素级变形**：前向/后向像素扭曲。
    *   **专用空间 MLLM**：如 SpatialReasoner, VLM-3R, ViLaSR 等经过空间微调的模型。
    *   **生成式方法**：如 GenWarp（基于扩散模型的视角合成）。
    *   **通用模型**：Qwen2.5-VL, InternVL3, Qwen3-VL 等。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的训练时长或 GPU 数量。
*   **原因**：该方法本质上是一种**推理时（Inference-time）的即插即用策略**，不需要对基础 MLLM 进行重新训练或大规模微调。实验主要基于 Qwen2.5-VL-7B/14B 模型进行评估。文中提到该方法引入的推理额外开销极小。

### 5. 实验数量与充分性
*   **实验规模**：
    *   ViewBench 包含 571 个文本问题、744 个形状问题和 300 个物体描述样本。
    *   涵盖了不同的视角重叠率（5-15%, 15-25%, 25-35%）以及极端视角偏移（2-5%）。
*   **充分性**：实验设计非常全面，包含了：
    *   **噪声敏感度测试**：验证了 Token 对位置偏移的鲁棒性。
    *   **消融实验**：对比了前向 vs 后向、最近邻 vs 自适应获取。
    *   **几何鲁棒性测试**：使用了真实深度图（GT）和预测深度图（Depth Anything v2, Depth Pro）进行对比。
    *   **客观性**：通过引入“几何先验 Oracle”作为上限参考，证明了性能提升确实源于视角变换的改进。

### 6. 主要结论与发现
*   **Token 级变形优于像素级**：Token 能够容忍几何扭曲带来的局部噪声，保持语义连贯性。
*   **后向扭曲是关键**：保持目标视图 Token 的规则密集分布对 MLLM 的性能至关重要。
*   **无需复杂微调**：简单的 Token 扭曲配合现有的通用 MLLM，其表现优于专门针对空间数据微调的模型。
*   **优于生成式模型**：相比扩散模型合成新视角（容易产生幻觉或丢失小物体），Token 扭曲能更忠实地保留源视图的视觉细节。

### 7. 优点
*   **轻量高效**：无需重新训练模型，计算成本低，易于集成到现有 MLLM 架构中。
*   **鲁棒性强**：对深度估计误差不敏感，解决了传统几何视觉方法在 MLLM 上的适配难题。
*   **理论支撑**：结合了认知科学的心理表象理论，为 MLLM 的空间表征提供了新的视角。

### 8. 不足与局限
*   **依赖几何输入**：虽然可以使用预测值，但该方法仍高度依赖深度图和相机位姿的准确性。
*   **视角范围限制**：主要适用于“邻近视角”。对于视角差异极大、遮挡严重的区域，由于源视图缺乏信息，模型仍难以处理（虽然优于基准，但距离 Oracle 仍有差距）。
*   **感知边界**：该方法提升的是“观察”能力，但最终的推理质量仍受限于底层 LLM 的逻辑推理水平。

（完）
