---
title: "CLIP-AUTT: Test-Time Personalization with Action Unit Prompting for Fine-Grained Video Emotion Recognition"
title_zh: CLIP-AUTT：基于动作单元提示的测试时个性化细粒度视频情感识别
authors: "Muhammad Osama Zeeshan, Masoumeh Sharafi, Benoît Savary, Alessandro Lameiras Koerich, Marco Pedersoli, Eric Granger"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27999v2"
tags: ["query:cir-ml"]
score: 9.0
evidence: 使用视觉语言模型进行视频情感识别的测试时个性化
tldr: 针对细粒度视频情感识别中个性化不足及现有CLIP方法依赖噪声文本的问题，本文提出CLIP-AU和CLIP-AUTT。CLIP-AU利用动作单位（AU）作为结构化文本提示来捕捉细微表情，而CLIP-AUTT通过熵引导的时间窗口选择和提示微调实现测试时个性化适配。实验证明该方法在多个数据集上优于现有SOTA方法，实现了鲁棒且个性化的情感识别。
selection_source: fresh_fetch
motivation: 现有的CLIP情感识别方法依赖噪声较大的LLM生成文本，且难以捕捉细粒度及受试者特有的表情变化。
method: 提出将动作单位（AU）作为结构化提示，并结合熵引导的时间窗口选择进行测试时提示微调，以实现受试者相关的动态适配。
result: 在BioVid、StressID和BAH三个挑战性视频数据集上，该方法显著优于现有的CLIP基础模型和测试时自适应（TTA）方法。
conclusion: 通过结合AU语义提示与测试时个性化技术，本研究为细粒度视频情感识别提供了一种高效、可解释且具备强泛化能力的解决方案。
---

## 摘要
情感识别（ER）中的个性化对于准确解释细微且特定于受试者的表达模式至关重要。视觉语言模型（VLMs）如 CLIP 的最新进展展示了在 ER 中利用图像-文本联合表示的巨大潜力。然而，基于 CLIP 的方法要么依赖于 CLIP 的对比预训练，要么依赖于大语言模型（LLMs）生成描述性文本提示，这些提示存在噪声、计算成本高，且无法捕捉细粒度表情，导致性能下降。在这项工作中，我们利用动作单元（AUs）作为 CLIP 中的结构化文本提示来建模细粒度面部表情。AUs 编码了表情背后的细微肌肉激活，为更鲁棒的 ER 提供了局部化且可解释的语义线索。我们引入了 CLIP-AU，这是一种轻量级的 AU 引导时序学习方法，将可解释的 AU 语义集成到 CLIP 中。它通过将 AU 提示与面部动态对齐来学习通用的、与受试者无关的表示，从而在无需 CLIP 微调或 LLM 生成文本监督的情况下实现细粒度 ER。尽管 CLIP-AU 建模了细粒度的 AU 语义，但它无法适应细微表情中特定于受试者的变异性。为了解决这一局限性，我们提出了 CLIP-AUTT，这是一种基于视频的测试时个性化方法，可动态地将 AU 提示调整为来自未知受试者的视频。通过将熵引导的时序窗口选择与提示微调相结合，CLIP-AUTT 实现了特定于受试者的自适应，同时保持了时序一致性。我们在三个具有挑战性的基于视频的细微 ER 数据集（BioVid、StressID 和 BAH）上进行了广泛实验，结果表明 CLIP-AU 和 CLIP-AUTT 优于最先进的基于 CLIP 的面部表情识别（FER）和测试时自适应（TTA）方法，实现了鲁棒且个性化的细微 ER。我们的代码已公开：https://github.com/osamazeeshan/CLIP-AUTT。

## Abstract
Personalization in emotion recognition (ER) is essential for an accurate interpretation of subtle and subject-specific expressive patterns. Recent advances in vision-language models (VLMs) such as CLIP demonstrate strong potential for leveraging joint image-text representations in ER. However, CLIP-based methods either depend on CLIP's contrastive pretraining or on LLMs to generate descriptive text prompts, which are noisy, computationally expensive, and fail to capture fine-grained expressions, leading to degraded performance. In this work, we leverage Action Units (AUs) as structured textual prompts within CLIP to model fine-grained facial expressions. AUs encode the subtle muscle activations underlying expressions, providing localized and interpretable semantic cues for more robust ER. We introduce CLIP-AU, a lightweight AU-guided temporal learning method that integrates interpretable AU semantics into CLIP. It learns generic, subject-agnostic representations by aligning AU prompts with facial dynamics, enabling fine-grained ER without CLIP fine-tuning or LLM-generated text supervision. Although CLIP-AU models fine-grained AU semantics, it does not adapt to subject-specific variability in subtle expressions. To address this limitation, we propose CLIP-AUTT, a video-based test-time personalization method that dynamically adapts AU prompts to videos from unseen subjects. By combining entropy-guided temporal window selection with prompt tuning, CLIP-AUTT enables subject-specific adaptation while preserving temporal consistency. Our extensive experiments on three challenging video-based subtle ER datasets, BioVid, StressID, and BAH, indicate that CLIP-AU and CLIP-AUTT outperform state-of-the-art CLIP-based FER and TTA methods, achieving robust and personalized subtle ER. Our code is publicly available at: https://github.com/osamazeeshan/CLIP-AUTT.

---

## 论文详细总结（自动生成）

以下是对论文《CLIP-AUTT: Test-Time Personalization with Action Unit Prompting for Fine-Grained Video Emotion Recognition》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：细粒度面部表情（如疼痛、压力）通常强度低且具有高度的**受试者差异性**（同一动作在不同人身上含义不同）。
*   **现有局限**：目前的 CLIP 情感识别方法多依赖通用的类别提示（如“一个痛苦的人”）或 LLM 生成的描述。这些提示噪声大、计算贵，且难以捕捉肌肉层面的细微变化。
*   **核心目标**：利用**面部动作单元（Action Units, AU）**作为结构化文本提示，结合**测试时自适应（TTA）**技术，实现无需标注的受试者个性化情感识别。

### 2. 方法论
论文提出了两个阶段的方法：
*   **CLIP-AU（细粒度视频对齐）**：
    *   **AU 提示词**：将 46 个 AU 的解剖学描述（如“内眉提升”、“鼻部皱缩”）作为 CLIP 文本编码器的输入。
    *   **时序模块**：采用 **1D-CNN + 门控线性单元（GLU）** 提取视频帧间的动态特征，相比 Transformer 在处理细微表情时更稳定。
    *   **AU 适配器**：引入轻量级适配器（At）微调文本嵌入，计算视频特征与 AU 提示词的余弦相似度，生成 AU 激活向量。
    *   **分类器**：在相似度向量上训练 MLP 分类器，无需显式的 AU 标签监督。
*   **CLIP-AUTT（测试时个性化）**：
    *   **熵引导时序窗口选择**：通过滑动窗口计算各片段的预测熵值，自动识别视频中表达最强烈、最自信的“峰值”片段，避免噪声干扰。
    *   **AU 提示微调（Prompt Tuning）**：在推理阶段，针对特定受试者，通过最小化预测熵来动态优化 AU 文本嵌入，使模型适配该受试者独特的表情风格。

### 3. 实验设计
*   **数据集**：
    *   **BioVid**：热诱导疼痛数据集（受控环境）。
    *   **StressID**：多模态压力识别数据集。
    *   **BAH**：行为矛盾/犹豫数据集（真实世界、非受控环境）。
*   **Benchmark**：加权平均召回率（WAR）和 F1 分数。
*   **对比方法**：
    *   **CLIP 基础模型**：Zero-shot CLIP, Fine-tuned CLIP, EmoCLIP, X-CLIP, Exp-CLIP。
    *   **TTA 方法**：TPT, TDA, DPE, PromptAlign, ReTA, T3AL。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A100 GPU (48 GB)**。
*   **训练细节**：
    *   CLIP-AU 预训练阶段为 10 个 epoch，Batch Size 为 8。
    *   CLIP-AUTT 在测试时针对每个视频进行 10 次迭代优化，Batch Size 为 1。
    *   所有实验均基于冻结的 CLIP-ViT-B/32 骨干网络。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个具有挑战性的数据集上进行了测试，每个数据集选取了 10 个目标受试者进行个性化评估，并进行了扩展到 30 个受试者的鲁棒性分析。
*   **消融实验**：涵盖了时序模块设计（CNN vs Transformer）、提示词模板选择、AU 提示词 vs 类别提示词、窗口长度影响等多个维度。
*   **充分性评价**：实验设计客观、公平，通过受试者独立协议（Subject-independent）验证了泛化性，并利用 OpenFace 提取的 AU 进行了定性对比，验证了 AU 预测的解释性。

### 6. 主要结论与发现
*   **AU 提示的优越性**：相比通用的类别提示，AU 提示能提供更细粒度的语义线索，显著提升细微表情的识别率。
*   **个性化的必要性**：CLIP-AUTT 通过测试时微调，在 BioVid 和 StressID 上分别取得了 81.5% 和 80.8% 的 WAR，大幅领先于非个性化模型。
*   **时序窗口的重要性**：仅在最自信的窗口进行自适应，能有效防止模型在非表达片段（如中性表情）上发生漂移。

### 7. 优点
*   **可解释性强**：通过 AU 权重可视化，可以清晰看到模型是基于哪些肌肉动作（如“眉毛下压”）做出判断的。
*   **高效轻量**：无需全参数微调 CLIP，仅优化极少量的提示词嵌入和适配器参数。
*   **无监督适配**：在推理阶段无需任何受试者标签即可实现个性化，具有极强的实际应用潜力（如医疗监控）。

### 8. 不足与局限
*   **极端复杂场景的挑战**：在 BAH 数据集（高度矛盾和非受控环境）上，虽然优于 SOTA，但提升幅度有限，说明在极度模糊的表情下，AU 模式可能过于杂乱。
*   **计算延迟**：虽然是轻量级微调，但测试时的迭代优化（10 次 iteration）相比直接推理仍会增加一定的计算延迟。
*   **依赖预训练质量**：模型性能仍受限于 CLIP 原始视觉编码器对低强度面部变化的捕捉能力。

（完）
