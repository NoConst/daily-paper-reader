---
title: "LinguDistill: Recovering Linguistic Ability in Vision- Language Models via Selective Cross-Modal Distillation"
title_zh: LinguDistill：通过选择性跨模态蒸馏恢复视觉语言模型中的语言能力
authors: "Patrick Amadeus Irawan, Erland Hilman Fuadi, Shanu Kumar, Alham Fikri Aji, Yova Kementchedjhieva"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00829v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 将预训练语言模型适配为视觉语言模型
tldr: "针对预训练语言模型在转化为视觉语言模型过程中出现的语言能力退化问题，本文提出了 LinguDistill。这是一种无需额外适配器的蒸馏方法，通过创新的层级 KV-cache 共享机制，利用冻结的原始语言模型作为教师来指导学生模型。该方法在不增加推理复杂度的前提下，通过在语言密集型数据上进行选择性蒸馏，成功恢复了约 10% 的语言能力损失，同时保持了视觉任务性能，为多模态模型优化提供了高效方案。"
selection_source: fresh_fetch
motivation: 多模态适配过程中的表示偏移会导致语言模型原有的语言能力退化，而现有恢复方法通常需要引入复杂的额外模块。
method: 通过层级 KV-cache 共享使教师模型能处理学生的视觉表示，并在语言密集型数据上进行选择性跨模态蒸馏。
result: "该方法在语言和知识基准测试中恢复了约 10% 的性能损失，同时在视觉重度任务上保持了与原模型相当的性能。"
conclusion: 研究证明无需额外模块即可有效恢复多模态模型的语言能力，为解决模态退化问题提供了一种简洁且高效的实践路径。
---

## 摘要
将预训练语言模型（LMs）适配为视觉语言模型（VLMs）时，由于多模态适配过程中引入的表示偏移和跨模态干扰，可能会降低其原有的语言能力。即使使用标准目标进行针对性的特定任务微调，这种损失也很难恢复。先前的恢复方法通常引入额外的模块作为中间对齐层，以维持或隔离特定模态的子空间，这增加了架构复杂度，增加了推理时的参数量，并限制了在不同模型和设置下的灵活性。我们提出了 LinguDistill，这是一种无适配器（adapter-free）的蒸馏方法，通过利用原始冻结的语言模型作为教师模型来恢复语言能力。我们通过引入逐层 KV 缓存共享（layer-wise KV-cache sharing），克服了实现视觉条件教师监督的关键挑战，这使得教师模型能够在不修改任何一个模型架构的情况下接触到学生的多模态表示。然后，我们在语言密集型数据上选择性地蒸馏教师模型强大的语言信号以恢复语言能力，同时保留学生模型在多模态任务上的视觉接地能力。结果表明，LinguDistill 在语言和知识基准测试中恢复了约 10% 的性能损失，同时在视觉密集型任务上保持了相当的性能。我们的研究结果表明，无需额外模块即可恢复语言能力，为多模态模型中特定模态的退化问题提供了一种高效且实用的解决方案。

## Abstract
Adapting pretrained language models (LMs) into vision-language models (VLMs) can degrade their native linguistic capability due to representation shift and cross-modal interference introduced during multimodal adaptation. Such loss is difficult to recover, even with targeted task-specific fine-tuning using standard objectives. Prior recovery approaches typically introduce additional modules that act as intermediate alignment layers to maintain or isolate modality-specific subspaces, which increases architectural complexity, adds parameters at inference time, and limits flexibility across models and settings. We propose LinguDistill, an adapter-free distillation method that restores linguistic capability by utilizing the original frozen LM as a teacher. We overcome the key challenge of enabling vision-conditioned teacher supervision by introducing layer-wise KV-cache sharing, which exposes the teacher to the student's multimodal representations without modifying the architecture of either model. We then selectively distill the teacher's strong linguistic signal on language-intensive data to recover language capability, while preserving the student's visual grounding on multimodal tasks. As a result, LinguDistill recovers $\sim$10% of the performance lost on language and knowledge benchmarks, while maintaining comparable performance on vision-heavy tasks. Our findings demonstrate that linguistic capability can be recovered without additional modules, providing an efficient and practical solution to modality-specific degradation in multimodal models.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **LinguDistill**，一种旨在恢复视觉语言模型（VLM）在多模态适配过程中损失的语言能力的新方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：将预训练语言模型（LM）适配为视觉语言模型时，由于表示偏移（Representation Shift）和跨模态干扰，模型原有的语言能力和知识储备会发生显著退化（即“灾难性遗忘”）。
*   **研究动机**：现有的恢复方法通常需要引入额外的适配器或对齐层，这增加了推理时的计算开销和架构复杂度。作者希望找到一种**无需额外参数**、在推理阶段完全透明的方法，通过蒸馏原始冻结的 LM 教师来找回丢失的“灵魂”。

### 2. 方法论
*   **核心思想**：利用原始的、未经过视觉适配的冻结 LM 作为教师模型，通过跨模态蒸馏将语言先验重新注入到 VLM 学生模型中。
*   **关键技术细节**：
    *   **层级 KV 缓存共享（Layer-wise KV-cache Sharing）**：这是本文最核心的创新。由于教师（纯文本 LM）无法处理图像，作者让教师模型在计算时直接复用学生模型（VLM）生成的 KV 缓存。这意味着教师模型虽然参数冻结且只处理文本 Query，但它能通过学生的 KV 矩阵“看到”多模态上下文。
    *   **选择性蒸馏（Selective Distillation）**：并非所有数据都适合蒸馏。作者将数据分为“语言密集型”和“视觉/OCR 密集型”。
    *   **公式化目标**：损失函数结合了软蒸馏损失（KL 散度）和硬标签损失（交叉熵）。通过权重因子 $\alpha$ 控制：在语言任务上 $\alpha > 0$（强化教师指导），在 OCR/文档任务上 $\alpha = 0$（仅依赖真实标签，防止教师的纯文本偏见干扰视觉接地能力）。

### 3. 实验设计
*   **数据集**：使用 **The Cauldron**（包含 50 个视觉指令微调数据集），筛选出 17 个核心来源（约 60 万样本），并将其划分为语言密集型（8 个）和 OCR/文档密集型（9 个）。
*   **Benchmark**：
    *   **语言/知识类**：ARC (Easy/Challenge), HellaSwag, ScienceQA, COCO 标题生成。
    *   **OCR/文档类**：DocVQA, InfographicVQA, OCRBench。
    *   **综合类**：MMMU, MMStar, MME, AI2D, RealWorldQA。
*   **对比方法**：原始预训练模型（Baseline）、标准全量微调（nanoVLM-full）、仅语言数据微调（nanoVLM-lang）、全局统一蒸馏（distill-full）。

### 4. 资源与算力
*   **算力设备**：使用了单张 **NVIDIA RTX A6000 PRO (96 GB)** 显卡。
*   **训练细节**：训练步数为 4000 步，有效 Batch Size 为 128，总计处理约 3.8 亿至 4 亿个 Token。采用 BF16 混合精度训练。

### 5. 实验数量与充分性
*   **实验规模**：设计了 10 组主要的实验变体，涵盖了基准线、不同数据子集、统一蒸馏与选择性蒸馏的对比。
*   **消融实验**：对蒸馏强度（$\alpha$）、温度（$T$）以及数据配比进行了详细的消融研究。
*   **充分性评价**：实验设计较为客观、公平。作者不仅报告了最终分数，还深入分析了 OCRBench 的 10 个子任务，解释了为何蒸馏会导致某些视觉任务退化，体现了研究的深度。

### 6. 主要结论与发现
*   **语言能力恢复显著**：LinguDistill 成功恢复了约 **10%** 的语言/知识性能损失。在 HellaSwag 和 ARC 等纯文本推理任务上，表现远优于标准微调。
*   **选择性蒸馏的必要性**：统一蒸馏虽然能提升语言能力，但会严重破坏 OCR 能力（OCRBench 下降 40%）。而选择性蒸馏在提升语言能力的同时，将视觉任务的损失控制在极小范围内。
*   **教师模型的价值**：冻结的教师模型在处理需要常识推理和科学知识的视觉任务（如 ScienceQA）时，能提供比真实标签更丰富的监督信号。

### 7. 优点
*   **推理零成本**：训练完成后教师模型被移除，不增加任何推理参数或延迟。
*   **架构通用性**：KV 共享机制适用于任何共享相同 Backbone 架构的 LM 和 VLM。
*   **解决实际痛点**：针对 VLM 变得“越来越笨（语言能力下降）”的问题提供了一个简洁高效的工业级方案。

### 8. 不足与局限
*   **模型规模限制**：实验主要在小型模型（460M 参数）上进行，尚未在大规模模型（如 7B 或 70B）上验证其扩展性。
*   **视觉接地权衡**：尽管采用了选择性蒸馏，但在极高精度的 OCR 任务上仍有轻微退化，因为强大的语言先验有时会诱导模型进行“过度纠错”（例如将非语义字符串纠正为单词）。
*   **序列长度限制**：受限于单卡算力，训练序列长度限制在 1024 Token，这可能影响了在长文档理解任务（如 MMMU）上的表现。

（完）
