---
title: Hierarchical Contrastive Learning for Multimodal Data
title_zh: 多模态数据的层级对比学习
authors: "Huichao Li, Junhan Yu, Doudou Zhou"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05462v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态表示学习中的分层对比学习
tldr: 针对多模态学习中传统“共享-私有”二分法的局限性，本文提出层级对比学习（HCL）框架。该框架通过层级潜变量模型和结构稀疏性，能够同时捕获全局共享、部分共享及模态特有的表示。HCL利用结构感知对比目标避免过度对齐，并在理论上证明了分解的可识别性。实验证明其在模拟数据和电子健康记录任务中均优于现有方法。
selection_source: fresh_fetch
motivation: 传统的共享-私有分解忽略了仅在部分模态间共享的信息，导致过度对齐或信息丢失。
method: 提出HCL框架，结合层级潜变量、结构稀疏性和结构感知对比损失，实现对不同共享程度特征的精准建模。
result: 理论上证明了层级分解的可识别性，实验中在结构恢复和下游预测任务上均表现优异。
conclusion: HCL通过精细化建模多模态间的复杂共享关系，显著提升了表示学习的质量和预测性能。
---

## 摘要
多模态表示学习通常建立在共享-私有分解的基础上，将潜在信息视为所有模态共有或仅为某一模态特有。这种二元观点往往是不充分的：许多因素仅由模态的子集共享，而忽略这种部分共享可能会导致无关信号的过度对齐，并掩盖互补信息。我们提出了层级对比学习（HCL），这是一个在统一模型中学习全局共享、部分共享和模态特有表示的框架。HCL 将层级潜在变量建模与结构稀疏性以及结构感知对比目标相结合，仅对齐真正共享潜在因子的模态。在潜在变量不相关的情况下，我们证明了层级分解的可识别性，确立了载荷矩阵的恢复保证，并推导了下游预测的参数估计和超额风险界限。仿真实验表明，该方法能够准确恢复层级结构并有效选择任务相关组件。在多模态电子健康记录上，HCL 产生了更具信息量的表示，并持续提升了预测性能。

## Abstract
Multimodal representation learning is commonly built on a shared-private decomposition, treating latent information as either common to all modalities or specific to one. This binary view is often inadequate: many factors are shared by only subsets of modalities, and ignoring such partial sharing can over-align unrelated signals and obscure complementary information. We propose Hierarchical Contrastive Learning (HCL), a framework that learns globally shared, partially shared, and modality-specific representations within a unified model. HCL combines a hierarchical latent-variable formulation with structural sparsity and a structure-aware contrastive objective that aligns only modalities that genuinely share a latent factor. Under uncorrelated latent variables, we prove identifiability of the hierarchical decomposition, establish recovery guarantees for the loading matrices, and derive parameter estimation and excess-risk bounds for downstream prediction. Simulations show accurate recovery of hierarchical structure and effective selection of task-relevant components. On multimodal electronic health records, HCL yields more informative representations and consistently improves predictive performance.

---

## 论文详细总结（自动生成）

这篇论文由中国科学院大学和新加坡国立大学的研究人员合作完成，提出了一种名为**层级对比学习（Hierarchical Contrastive Learning, HCL）**的新框架，旨在解决多模态数据中复杂的共享结构建模问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的多模态表示学习通常采用“共享-私有（Shared-Private）”的二分法，即假设信息要么是所有模态共有的，要么是某个模态特有的。
*   **研究动机**：在现实场景（如包含文本、图像、音频的三模态数据）中，这种二元论过于粗糙。许多潜在因子可能仅由**部分模态子集**共享（例如仅由视觉和文本共享，而与音频无关）。
*   **后果**：忽略这种“部分共享”结构会导致无关信号的过度对齐（Over-alignment）、互补信息的利用不足，以及模型对缺失模态的鲁棒性下降。

### 2. 论文提出的方法论
HCL 框架通过统一的概率模型和对比学习目标来捕获全局共享、部分共享和模态特有的表示。
*   **层级潜变量模型**：将观测数据 $x^{(m)}$ 建模为不同层级潜在变量 $z_s$ 的组合。例如，三模态下包含：Level 1（三者共有）、Level 2（两两共有）、Level 3（模态特有）。
*   **可识别性理论**：论文在理论上证明了在潜在变量不相关（Uncorrelated）的温和条件下，这种层级分解在正交变换意义下是可识别的。
*   **层级对比损失（HCL Loss）**：
    *   **结构感知对齐**：仅对齐真正共享同一潜在因子的模态表示。
    *   **正负采样**：同一结构在同一样本跨模态的表示为正样本对，不同样本间的表示为负样本对。
    *   **结构稀疏正则化**：引入正则项鼓励不同结构在同一模态内部保持分离，防止过拟合。
*   **下游预测**：提出了一种**去偏（Debiased）且带组正则化（Group-regularized）**的线性回归方法，能够自动识别哪些层级结构对特定下游任务是有贡献的。

### 3. 实验设计
*   **数据集与场景**：
    1.  **仿真实验**：生成具有已知层级结构的合成数据，验证载荷矩阵的恢复准确性。
    2.  **真实应用**：使用 **MIMIC-IV** 电子健康记录（EHR）数据库。包含三种模态：离散临床代码（Codes）、连续生理测量（Lab results）和非结构化临床文本（Notes）。
*   **下游任务**：30天再入院预测、下次住院时长预测、一年死亡率预测。
*   **对比方法（Benchmark）**：
    *   **统计矩阵分解类**：JIVE, SLIDE, sJIVE, MMFL。
    *   **深度多模态学习类**：MISA, ConVIRT, DLF, TSD（其中 TSD 是最新的三子空间解耦方法）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了 Adam 优化器，并利用 **Optuna** 框架进行超参数自动搜索（如各层级的潜在维度 $r$）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验进行了 **100 次**重复实验，涵盖了不同的样本量（5k 到 30k）和模态维度。
    *   真实数据实验在 MIMIC-IV 的 108,927 名患者队列上进行，采用了 **50 次**随机划分（70%训练, 10%验证, 20%测试）。
*   **充分性评价**：实验设计较为充分。不仅对比了预测性能（AUC, F1, RMSE 等），还通过**消融实验**分析了不同层级结构对不同临床任务的贡献度（权重可视化），证明了层级建模的必要性。

### 6. 论文的主要结论与发现
*   **结构恢复**：HCL 在恢复全局和块状载荷矩阵方面显著优于传统的 SVD 方法，验证了其理论保证。
*   **预测性能**：在所有 EHR 预测任务中，HCL 持续优于现有的共享-私有分解方法和深度学习基准。
*   **任务特异性**：发现不同任务依赖的结构不同。例如，一年死亡率预测高度依赖“全局共享”信息，而住院时长预测则主要受“临床代码特有”信息驱动。
*   **无监督潜力**：预训练+微调的效果与端到端训练接近，说明 HCL 能在无监督情况下有效捕获数据内在的层级结构。

### 7. 优点
*   **理论与实证结合**：不仅提出了算法，还给出了严谨的可识别性证明和非渐近误差界限。
*   **精细化建模**：突破了二分法的局限，填补了多模态表示学习中“部分共享”结构的理论空白。
*   **可解释性**：通过组稀疏性，模型可以明确告知用户哪些模态组合对预测最重要。

### 8. 不足与局限
*   **理论假设限制**：理论分析假设链接函数（Link functions）是已知且可逆的，但在全非线性深度学习场景下，这一假设难以完全满足。
*   **计算复杂度**：随着模态数量 $M$ 的增加，部分共享结构的组合数量会呈指数级增长（$2^M$），在大规模多模态场景下的扩展性可能面临挑战。
*   **动态性缺失**：目前模型主要处理静态或对齐后的特征，对于异步、非平稳的时序多模态数据处理尚需进一步扩展。

（完）
