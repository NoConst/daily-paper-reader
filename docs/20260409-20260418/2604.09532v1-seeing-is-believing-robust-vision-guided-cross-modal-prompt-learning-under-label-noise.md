---
title: "Seeing is Believing: Robust Vision-Guided Cross-Modal Prompt Learning under Label Noise"
title_zh: 眼见为实：标签噪声下鲁棒的视觉引导跨模态提示学习
authors: "Zibin Geng, Xuefeng Jiang, Jia Li, Zheng Li, Tian Wen, Lvhua Wu, Sheng Sun, Yuwei Wang, Min Liu"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09532v1"
tags: ["query:cir-ml"]
score: 8.0
evidence: 视觉引导的视觉语言模型跨模态提示学习
tldr: 针对视觉语言模型在标签噪声环境下提示学习鲁棒性不足的问题，本文提出了VisPrompt框架。该方法利用视觉信息比文本标签更稳定的特性，通过跨模态注意力机制将视觉语义反向注入提示表示中，并结合条件调制机制动态平衡文本先验与图像证据。VisPrompt在保持预训练模型冻结的同时，仅引入少量参数，有效抑制了噪声干扰并缓解了对错误标签的记忆，在多个数据集上显著提升了鲁棒性。
selection_source: carryover_cache
motivation: 提示学习在标签噪声下极易受干扰，而视觉内容包含更丰富可靠的语义，可作为锚点提升学习的鲁棒性。
method: 提出VisPrompt框架，利用跨模态注意力将视觉语义注入提示，并引入轻量级条件调制机制自适应控制注入强度。
result: 在7个合成及真实噪声基准数据集上的实验表明，VisPrompt在保持极低参数量的情况下，性能显著优于现有基线方法。
conclusion: 通过视觉引导的跨模态提示学习，可以有效增强视觉语言模型在噪声环境下的鲁棒性，实现更稳定的参数高效微调。
---

## 摘要
提示学习是视觉语言模型的一种参数高效方法，但其在标签噪声下的鲁棒性研究较少。视觉内容包含更丰富且更可靠的语义信息，在标签噪声下表现得更为鲁棒。然而，提示本身极易受到标签噪声的影响。基于这一直觉，我们提出了 VisPrompt，这是一个针对噪声标签场景的轻量级且鲁棒的视觉引导提示学习框架。具体而言，我们利用跨模态注意力机制，反向地将视觉语义注入到提示表示中。这使得提示标记能够选择性地聚合与当前样本相关的视觉信息，从而通过将提示学习锚定在稳定的实例级视觉证据上并减少噪声监督的影响，来提高鲁棒性。为了解决因对所有样本采用相同的视觉信息注入方式（尽管其视觉线索质量存在差异）而导致的不稳定性，我们进一步引入了一种轻量级的条件调制机制，以自适应地控制视觉信息注入的强度，从而在文本侧语义先验与图像侧实例证据之间取得更鲁棒的平衡。所提出的框架有效地抑制了噪声引起的干扰，减少了提示更新中的不稳定性，并缓解了对错误标注样本的记忆。VisPrompt 在保持预训练视觉语言模型（VLM）骨干网络冻结且仅引入少量额外可训练参数的情况下，显著提高了鲁棒性。在合成和真实世界标签噪声下的广泛实验表明，VisPrompt 在七个基准数据集上普遍优于现有基准方法，并表现出更强的鲁棒性。我们的代码已公开发布于 https://github.com/gezbww/Vis_Prompt。

## Abstract
Prompt learning is a parameter-efficient approach for vision-language models, yet its robustness under label noise is less investigated. Visual content contains richer and more reliable semantic information, which remains more robust under label noise. However, the prompt itself is highly susceptible to label noise. Motivated by this intuition, we propose VisPrompt, a lightweight and robust vision-guided prompt learning framework for noisy-label settings. Specifically, we exploit a cross-modal attention mechanism to reversely inject visual semantics into prompt representations. This enables the prompt tokens to selectively aggregate visual information relevant to the current sample, thereby improving robustness by anchoring prompt learning to stable instance-level visual evidence and reducing the influence of noisy supervision. To address the instability caused by using the same way of injecting visual information for all samples, despite differences in the quality of their visual cues, we further introduce a lightweight conditional modulation mechanism to adaptively control the strength of visual information injection, which strikes a more robust balance between text-side semantic priors and image-side instance evidence. The proposed framework effectively suppresses the noise-induced disturbances, reduce instability in prompt updates, and alleviate memorization of mislabeled samples. VisPrompt significantly improves robustness while keeping the pretrained VLM backbone frozen and introducing only a small amount of additional trainable parameters. Extensive experiments under synthetic and real-world label noise demonstrate that VisPrompt generally outperforms existing baselines on seven benchmark datasets and achieves stronger robustness. Our code is publicly available at https://github.com/gezbww/Vis_Prompt.

---

## 论文详细总结（自动生成）

这是一份关于论文《Seeing is Believing: Robust Vision-Guided Cross-Modal Prompt Learning under Label Noise》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉语言模型（VLM）的提示学习（Prompt Learning）虽然参数高效，但在面对**标签噪声（Label Noise）**时非常脆弱。由于预训练骨干网络被冻结，少量的可学习提示词（Soft Prompts）会直接吸收来自错误标签的噪声梯度，导致“提示漂移”和对错误样本的过拟合。
*   **研究动机**：作者提出“眼见为实”的直觉——即便标签是错误的，图像本身的视觉内容依然包含稳定且可靠的语义。因此，研究如何利用视觉信息作为锚点来引导提示词的优化，从而抵御标签噪声的干扰。

### 2. 方法论：VisPrompt 框架
VisPrompt 包含四个关键阶段，旨在将视觉语义反向注入提示表示中：
1.  **视觉特征投影**：利用线性层将图像编码器提取的局部视觉特征投影到提示词的嵌入空间。
2.  **跨模态视觉提示调节（核心）**：采用**跨模态注意力机制（MHA）**，以提示词为 Query，视觉特征为 Key 和 Value。这使得提示词能根据图像内容选择性地聚合相关的视觉证据，形成实例级的调节信号。
3.  **基于 FiLM 的鲁棒调制**：引入**特征线性调制（FiLM）**和**残差门控机制**。通过学习缩放和偏移参数，自适应地控制视觉信息注入提示词的强度。这能根据视觉线索的质量，在文本先验和图像证据之间取得平衡。
4.  **鲁棒损失选择**：利用**最优传输（OT）**理论估计样本的可靠性。将样本分为可靠集（使用标准交叉熵损失 CE）和不可靠集（使用广义交叉熵损失 GCE），以进一步减少噪声标签的负面影响。

### 3. 实验设计
*   **数据集**：
    *   **合成噪声场景**：EuroSAT（卫星图）、Flowers102（花卉）、OxfordPets（宠物）、DTD（纹理）、UCF101（动作）、Caltech101（物体）。
    *   **真实噪声场景**：Food101N（来自网络的真实噪声食品数据集）。
*   **噪声设置**：涵盖对称噪声和非对称噪声，噪声率从 12.5% 到 75% 不等。
*   **对比基准（Baselines）**：CoOp（经典提示学习）、GCE（鲁棒损失）、JoAPR（基于损失分布的清洗）、NLPrompt（基于最优传输的最新方法）。

### 4. 资源与算力
*   **硬件**：实验在 **Nvidia RTX 4090 GPU** 上运行。
*   **软件框架**：PyTorch。
*   **训练细节**：使用 SGD 优化器，学习率为 0.002，采用余弦退火调度，训练 200 个 epoch。
*   **参数量**：VisPrompt 引入的可训练参数极少，不到总参数量的 1%（FiLM 模块仅占约 0.3%）。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个数据集上进行了广泛测试，涵盖了从低噪声到极高噪声（75%）的多种情况。
*   **充分性**：
    *   **消融实验**：验证了视觉引导、FiLM 门控机制各自的贡献。
    *   **骨干网络扩展性**：测试了 ViT-B/16、ViT-B/32、ResNet 系列等多种 CLIP 视觉骨干，证明了方法的通用性。
    *   **样本效率**：测试了不同 Shot（1 到 16）下的表现。
*   **客观性**：所有实验均取 3 次随机种子的平均值，对比方法均在相同超参数设置下进行，确保了公平性。

### 6. 主要结论与发现
*   **性能领先**：VisPrompt 在所有数据集和几乎所有噪声率下均优于现有最强基线（如 NLPrompt）。
*   **高噪声鲁棒性**：在 75% 的极端噪声下，VisPrompt 的领先优势扩大（如在 EuroSAT 上比基线高出 15% 以上），证明视觉引导能有效防止模型崩溃。
*   **理论支撑**：通过理论分析证明了跨模态注意力能提供去噪的语义近似，且 FiLM 调制具有 Lipschitz 稳定性，限制了噪声扰动的传播。

### 7. 优点与亮点
*   **视角独特**：不同于以往仅在标签侧或损失函数侧做文章的方法，该方法从“视觉引导”的角度重新思考了提示词的生成。
*   **轻量高效**：在不冻结 VLM 骨干的前提下，仅通过极少量的额外参数实现了显著的鲁棒性提升。
*   **自适应性**：FiLM 门控机制能根据图像质量动态调整干预强度，避免了盲目引入视觉噪声。

### 8. 不足与局限
*   **任务覆盖**：目前主要集中在图像分类任务，尚未在目标检测、语义分割或视频理解等更复杂的任务中验证。
*   **计算开销**：虽然参数量小，但引入跨模态注意力机制会带来一定的额外计算延迟，在资源极度受限的实时推理场景中可能存在挑战。
*   **噪声类型**：虽然测试了真实噪声，但大部分实验仍基于合成噪声，对于更复杂的实例相关噪声（Instance-dependent noise）探讨较少。

（完）
