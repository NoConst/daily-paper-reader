---
title: "Le MuMo JEPA: Multi-Modal Self-Supervised Representation Learning with Learnable Fusion Tokens"
title_zh: Le MuMo JEPA：具有可学习融合令牌的多模态自监督表示学习
authors: "Ciem Cornelissen, Sam Leroux, Pieter Simoens"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24327v2"
tags: ["query:cir-ml"]
score: 9.0
evidence: 带有融合Token的多模态自监督表示学习
tldr: 本研究针对单模态自监督学习无法利用异构传感器互补信息的局限，提出了Le MuMo JEPA框架。该方法通过在共享Transformer中引入可学习的融合令牌作为模态间的潜在瓶颈，并采用剪枝融合策略，实现了RGB与LiDAR或热成像数据的高效集成。实验表明，该模型在Waymo、nuScenes和FLIR等自动驾驶数据集上的检测与深度估计任务中，展现出卓越的性能与效率平衡，显著降低了计算与内存开销。
selection_source: fresh_fetch
motivation: 现有的自监督学习方法多局限于单模态，难以捕捉异构传感器（如RGB与LiDAR）之间的互补结构信息。
method: 提出一种基于可学习融合令牌的架构，通过剪枝融合策略将多模态信息压缩至共享的潜在瓶颈中，并结合SIGReg正则化。
result: 在Waymo和nuScenes等数据集上，该模型在下游检测和深度任务中取得了最佳的性能与效率权衡。
conclusion: Le MuMo JEPA证明了通过轻量化融合机制进行多模态自监督学习，能在显著降低计算成本的同时提升表征质量。
---

## 摘要
自监督学习已成为一种无需手动标注即可学习视觉表示的强大范式，但大多数方法仍运行在单一模态上，因此错失了异构传感器提供的互补结构。我们提出了 Le MuMo JEPA，这是一个从 RGB 图像及其对齐的伴随模态中学习统一表示的自监督框架。在我们的驾驶实验中，第二种模态是与相机对齐的 LiDAR 深度；我们还在 Teledyne FLIR ADAS 基准测试中评估了 RGB-热成像训练和迁移。我们的方法通过学习融合令牌将 LeJEPA 扩展到多模态设置，这些令牌在共享 Transformer 内部的模态特定补丁主干（patch stems）之间充当潜瓶颈（latent bottleneck）。我们的默认模型采用了一种剪枝融合策略：在初始跨模态注意力层之后，丢弃模态特定令牌，迫使跨模态信息进入共享的融合令牌网格，作为一种高效的潜瓶颈，随后对联合多模态 CLS 嵌入应用草图各向同性高斯正则化（SIGReg）。在 Waymo 数据集上，Le MuMo JEPA 在从零开始训练的多模态基准模型中，在下游补丁探测（patch probes）任务上表现出最强的性能-效率权衡，改进了 CenterNet 检测和稠密深度，同时在分割任务上保持了竞争力。在 nuScenes 的从零开始训练中，Le MuMo JEPA 依然是最强的模型，并且在 FLIR 上也取得了最佳结果，尤其是在经过 Waymo 初始化的微调之后。在我们的研究中，它还以显著更低的计算量、内存占用和预估训练时间，保持了最佳的整体准确率-效率平衡。

## Abstract
Self-supervised learning has emerged as a powerful paradigm for learning visual representations without manual annotations, yet most methods still operate on a single modality and therefore miss the complementary structure available from heterogeneous sensors. We present Le MuMo JEPA, a self-supervised framework that learns unified representations from RGB images and aligned companion modalities. In our driving experiments, the second modality is camera-aligned LiDAR depth; we also evaluate RGB-thermal training and transfer on the Teledyne FLIR ADAS benchmark. Our approach extends LeJEPA to the multi-modal setting by learning fusion tokens that act as a latent bottleneck between modality-specific patch stems inside a shared transformer. Our default model employs a pruned fusion strategy: after an initial cross-modal attention layer, modality-specific tokens are dropped, forcing cross-modal information into the shared fusion-token grid as an efficient latent bottleneck before Sketched Isotropic Gaussian Regularization (SIGReg) is applied to the joint multimodal CLS embedding. On Waymo, Le MuMo JEPA gives the strongest performance-efficiency trade-off on downstream patch probes among the from-scratch multimodal baselines, improving CenterNet detection and dense depth while remaining competitive on segmentation. Under from-scratch training on nuScenes, Le MuMo JEPA remains the strongest model, and it also gives the best FLIR results, especially after Waymo-initialized fine-tuning. It also retains the best overall accuracy-efficiency balance in our study at substantially lower compute, memory, and estimated training time.

---

## 论文详细总结（自动生成）

以下是对论文《Le MuMo JEPA: Multi-Modal Self-Supervised Representation Learning with Learnable Fusion Tokens》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的自监督学习（SSL）方法大多集中在单模态（如纯图像），忽略了自动驾驶等场景中异构传感器（如LiDAR、热成像）提供的互补信息。
*   **核心挑战**：如何高效地融合结构差异巨大的模态（如稠密的RGB与稀疏的LiDAR），同时避免全注意力机制带来的高昂计算成本，并摆脱对有监督标签或复杂对比学习启发式算法的依赖。
*   **整体含义**：本文提出 **Le MuMo JEPA**，通过在共享的Transformer架构中引入“可学习融合令牌（Fusion Tokens）”，将多模态信息压缩至一个潜在瓶颈（Latent Bottleneck）中，利用正则化手段实现高效的多模态表征学习。

### 2. 方法论
*   **核心思想**：将所有模态映射到统一的2D空间，利用一组专门的融合令牌作为中介，在Transformer的早期阶段捕获跨模态关联，随后通过剪枝减少后续计算量。
*   **关键技术细节**：
    *   **输入处理**：RGB图像和伴随模态（如投影后的LiDAR深度图或热成像图）均被切分为14x14的补丁（Patches）。
    *   **可学习融合令牌（FT）**：引入与空间补丁数量相等的融合令牌。在Transformer的第0层，融合令牌通过交叉注意力同时观察对应的RGB补丁和深度补丁。
    *   **剪枝融合策略（Pruned Fusion）**：在第0层之后，直接丢弃原始的RGB和深度令牌，仅保留融合令牌和CLS令牌进入后续层。这形成了一个“潜瓶颈”，将计算复杂度从 $O((1+3N)^2)$ 降低到 $O((1+N)^2)$。
    *   **SIGReg正则化**：继承自LeJEPA，通过“草图各向同性高斯正则化”强制联合多模态的CLS嵌入服从各向同性高斯分布，防止表征崩溃，且无需负采样或动量教师网络。
    *   **训练目标**：结合不变性损失（Invariance Loss）和SIGReg损失，对联合嵌入进行优化。

### 3. 实验设计
*   **数据集与场景**：
    *   **Waymo Open Dataset**：主要驾驶基准，用于从零训练和评估。
    *   **nuScenes**：用于验证从零训练的泛化能力。
    *   **Teledyne FLIR ADAS**：用于RGB-热成像场景的迁移学习和微调评估。
*   **评估基准（Benchmark）**：采用**冻结补丁探测（Frozen Patch Probes）**，即固定编码器权重，仅训练轻量级下游头，测试其在3D物体检测（CenterNet风格）、稠密深度估计和语义分割（mIoU）上的表现。
*   **对比方法**：
    *   单模态基准：LeJEPA (RGB)、LiDAR-only JEPA、DINOv3。
    *   多模态基准：MultiMAE（基于掩码重建）、ImageBind（基于对比学习）。
    *   融合消融：早期融合（Early Fusion）、后期融合（Late Fusion）。

### 4. 资源与算力
*   **硬件环境**：实验在配备 **NVIDIA H200 GPU** 和 AMD EPYC 9275F 24核处理器的系统上运行。
*   **训练时长与开销**：
    *   默认模型（Le MuMo JEPA）在Waymo子集上训练5个Epoch约需 **55.09分钟**。
    *   峰值显存占用约为 **12.11 GB**。
    *   相比于不剪枝的“持久路由（Persistent Routing）”方案，训练时间缩短了约3倍，显存占用降低了约66%。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个主流数据集，进行了从零训练、跨数据集迁移、端到端微调等多维度测试。
*   **消融实验**：对比了剪枝融合与持久融合、SIGReg与VICReg损失函数、单次前向与三次前向（3-pass）等变体。
*   **公平性**：所有对比方法均在相同的计算预算、数据子集和下游探测器架构下重新训练，确保了“苹果对苹果”的客观对比。

### 6. 主要结论与发现
*   **性能领先**：在Waymo和nuScenes上，Le MuMo JEPA在3D检测和深度估计任务中显著优于所有从零训练的多模态基准（如MultiMAE和ImageBind）。
*   **效率极高**：通过令牌剪枝，该模型在保持高精度的同时，计算量（FLOPs）和内存需求远低于传统的全注意力多模态模型。
*   **正则化优势**：SIGReg在处理多模态联合嵌入时表现出比对比学习更强的稳定性，能有效对齐不同模态的几何结构。
*   **迁移能力**：在Waymo上预训练的模型在FLIR热成像数据集上表现出极强的零样本迁移和微调潜力。

### 7. 优点
*   **架构优雅**：通过融合令牌巧妙解决了异构模态的对齐问题，且计算开销随模态增加的增长非常缓慢。
*   **无需3D主干**：将LiDAR投影为2D深度图，利用成熟的ViT架构即可处理，降低了系统复杂性。
*   **实用性强**：在有限的算力和显存下即可实现高性能，适合资源受限的自动驾驶感知系统。

### 8. 不足与局限
*   **信息损失**：将LiDAR点云投影为2D深度图虽然高效，但不可避免地丢失了部分原生3D空间结构信息。
*   **对齐依赖**：模型高度依赖于传感器之间的精确空间校准，对校准误差或视野不重叠情况的鲁棒性尚未深入探讨。
*   **模型规模限制**：实验主要集中在ViT-Small规模，尚未在大规模模型（如ViT-Huge）上验证其扩展性。
*   **评估局限**：主要依赖冻结探测器（Frozen Probes），虽然能反映表征质量，但与全参数微调的SOTA感知模型相比，绝对精度仍有差距。

（完）
