---
title: Interpretable Zero-shot Referring Expression Comprehension with Query-driven Scene Graphs
title_zh: 基于查询驱动场景图的可解释零样本指代性表达理解
authors: "Yike Wu, Necva Bolucu, Stephen Wan, Dadong Wang, Jiahao Xia, Jian Zhang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25004v1"
tags: ["query:cir-ml"]
score: 8.0
evidence: 利用视觉语言模型和大模型进行零样本指代理解
tldr: 针对零样本指代理解（REC）中视觉语言模型难以捕捉细粒度细节和复杂关系的问题，本文提出SGREC方法。该方法利用查询驱动的场景图作为结构化中介，通过视觉语言模型构建包含空间关系和物体交互的场景图，再由大语言模型基于此图进行推理并给出详细解释。SGREC在RefCOCO等多个基准测试上取得了领先的准确率，显著提升了视觉场景理解的性能与可解释性。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在零样本REC任务中难以处理复杂的物体关系，而大语言模型又缺乏直接处理视觉特征的能力。
method: 提出SGREC框架，利用视觉语言模型构建查询驱动的场景图，并结合大语言模型的语义推理能力来定位目标物体。
result: 在RefCOCO、RefCOCO+和RefCOCOg等多个主流基准测试中，SGREC均取得了Top-1的准确率表现。
conclusion: 通过引入场景图作为视觉与语义间的桥梁，SGREC有效提升了零样本指代理解的准确性与推理过程的可解释性。
---

## 摘要
零样本指代性表达理解（REC）旨在根据自然语言查询在图像中定位目标对象，而不依赖于特定任务的训练数据，这要求强大的视觉理解能力。现有的视觉语言模型（VLM），如 CLIP，通常通过直接测量文本查询与图像区域之间的特征相似度来解决零样本 REC 问题。然而，这些方法难以捕捉细粒度的视觉细节并理解复杂的对象关系。同时，大语言模型（LLM）擅长高层语义推理，但它们无法直接将视觉特征抽象为文本语义，这限制了它们在 REC 任务中的应用。为了克服这些局限性，我们提出了 SGREC，这是一种利用查询驱动的场景图作为结构化中间介质的可解释零样本 REC 方法。具体而言，我们首先利用 VLM 构建一个查询驱动的场景图，该图显式地编码了与给定查询相关的空间关系、描述性说明和对象交互。通过利用该场景图，我们弥合了底层图像区域与 LLM 所需的高层语义理解之间的鸿沟。最后，LLM 从场景图提供的结构化文本表示中推断出目标对象，并为其决策提供详细解释，从而确保了推理过程的可解释性。大量实验表明，SGREC 在大多数零样本 REC 基准测试中均取得了 Top-1 准确率，包括 RefCOCO val (66.78%)、RefCOCO+ testB (53.43%) 和 RefCOCOg val (73.28%)，彰显了其强大的视觉场景理解能力。

## Abstract
Zero-shot referring expression comprehension (REC) aims to locate target objects in images given natural language queries without relying on task-specific training data, demanding strong visual understanding capabilities. Existing Vision-Language Models~(VLMs), such as CLIP, commonly address zero-shot REC by directly measuring feature similarities between textual queries and image regions. However, these methods struggle to capture fine-grained visual details and understand complex object relationships. Meanwhile, Large Language Models~(LLMs) excel at high-level semantic reasoning, their inability to directly abstract visual features into textual semantics limits their application in REC tasks. To overcome these limitations, we propose \textbf{SGREC}, an interpretable zero-shot REC method leveraging query-driven scene graphs as structured intermediaries. Specifically, we first employ a VLM to construct a query-driven scene graph that explicitly encodes spatial relationships, descriptive captions, and object interactions relevant to the given query. By leveraging this scene graph, we bridge the gap between low-level image regions and higher-level semantic understanding required by LLMs. Finally, an LLM infers the target object from the structured textual representation provided by the scene graph, responding with detailed explanations for its decisions that ensure interpretability in the inference process. Extensive experiments show that SGREC achieves top-1 accuracy on most zero-shot REC benchmarks, including RefCOCO val (66.78\%), RefCOCO+ testB (53.43\%), and RefCOCOg val (73.28\%), highlighting its strong visual scene understanding.

---

## 论文详细总结（自动生成）

这是一份关于论文《Interpretable Zero-shot Referring Expression Comprehension with Query-driven Scene Graphs》的结构化深度分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不使用特定任务训练数据的情况下（零样本），准确且可解释地在图像中定位由自然语言描述的目标物体（REC 任务）。
*   **研究动机**：
    *   **现有 VLM 的局限**：如 CLIP 等模型在处理零样本 REC 时，往往像“词袋模型”，难以捕捉细粒度的视觉细节，且缺乏对物体间复杂空间和逻辑关系的建模能力。
    *   **LLM 的视觉缺失**：大语言模型（LLM）虽具备极强的语义推理能力，但无法直接理解底层视觉特征。
    *   **语义鸿沟**：需要一种结构化的中介表示，将复杂的视觉场景转化为 LLM 能够理解并推理的文本形式。

### 2. 方法论（SGREC 框架）
SGREC 的核心思想是利用**查询驱动的场景图（Query-driven Scene Graphs）**作为视觉与语言之间的桥梁。其流程分为三个关键步骤：

*   **步骤 1：物体定位（Object Grounding）**
    *   使用预训练检测器（如 VinVL）提取候选框。
    *   通过 SpaCy 提取查询中的名词，并利用 VLM（如 LLaVA）推断查询的“主体”（Subject），以解决“左边那个”这类模糊表达的指代问题。
    *   基于词向量相似度过滤出与查询相关的物体。
*   **步骤 2：场景图生成（Scene Graph Generation）**
    *   **节点信息**：包含物体的边界框坐标（空间信息）、检测器提供的属性，以及利用 LLaVA 为每个区域生成的详细描述（Caption）。
    *   **边信息（交互）**：计算物体间的重叠率，对潜在交互对使用 LLaVA 预测关系三元组（如 `[人, 拿着, 伞]`），并使用视觉提示（红/蓝框）辅助模型区分相似物体。
*   **步骤 3：LLM 推理（LLM Inference）**
    *   将生成的场景图序列化为 JSON 格式，连同原始查询和指令输入给 LLM（如 Qwen-72B）。
    *   LLM 通过数值计算（坐标）和语义匹配，选出目标物体 ID 并给出推理逻辑的文本解释。

### 3. 实验设计
*   **数据集**：在三个主流 REC 基准上进行评估：**RefCOCO**（侧重空间）、**RefCOCO+**（侧重外观）、**RefCOCOg**（侧重复杂长句）。
*   **对比方法**：
    *   **零样本方法**：ReCLIP, RedCircle, FGVP, ViperGPT, GroundVLP, MCCE-REC, EAGR 等。
    *   **弱监督/全监督方法**：CPL, Grounding-DINO, LGRAN 等。
*   **评价指标**：Top-1 Accuracy（预测框与真值 IoU > 0.5）。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA H100 GPU** 上完成。
*   **推理效率**：
    *   主体推断：约 0.16 秒/样本。
    *   场景图生成：约 1.11 秒/样本（描述 0.59s + 关系推理 0.52s）。
    *   LLM 推理（Qwen-72B）：约 7.67 秒/样本。
*   **模型选择**：使用了 LLaVA-v1.6 (7B/72B) 作为视觉提取器，Qwen2.5-72B 和 Llama-3.1 作为推理后端。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个数据集的所有验证集和测试集（val, testA, testB），实验数据量达数万组。
*   **消融实验**：非常充分。分别验证了：
    1.  物体定位中名词、类别、主体推断的贡献。
    2.  场景图中坐标、描述、交互信息的各自作用。
    3.  不同 LLM 模型规模对结果的影响。
    4.  相似度阈值 $\tau$ 和重叠阈值 $\theta$ 的敏感度分析。
*   **客观性**：论文不仅对比了零样本方法，还对比了全监督模型，并详细分析了长尾分布下的泛化能力和密集场景下的鲁棒性，实验设计客观且公平。

### 6. 主要结论与发现
*   **性能领先**：SGREC 在 RefCOCOg val 上达到 73.28%，显著超过了之前的 SOTA 方法（如 MCCE-REC 的 62.56%）。
*   **结构化优势**：实验证明，将视觉信息转化为 JSON 格式的场景图比纯自然语言描述更能提升 LLM 的推理稳定性。
*   **复杂关系建模**：在长句子和复杂关系的 RefCOCOg 数据集上提升最明显，证明了场景图在处理物体间交互方面的卓越能力。
*   **模型规模正相关**：推理用的 LLM 规模越大，REC 的准确率越高，表明该任务高度依赖语言模型的逻辑推理能力。

### 7. 优点与亮点
*   **高可解释性**：不同于黑盒模型，SGREC 能输出选择目标的理由，增强了用户信任。
*   **查询驱动**：通过查询引导场景图生成，有效过滤了图像中的无关噪声，降低了 LLM 的处理负担。
*   **无需微调**：完全基于现有的预训练模型组合，具有极强的通用性和迁移能力。
*   **空间推理**：巧妙地利用 LLM 的数值计算能力处理坐标，解决了传统方法难以处理的复杂方位关系（如“左起第二个”）。

### 8. 不足与局限
*   **计算开销**：推理过程涉及多次大模型调用（VLM 生成描述 + LLM 最终推理），单样本耗时接近 9 秒，难以满足实时性要求。
*   **流水线误差累积**：性能高度依赖于第一步物体检测器的质量。如果检测器漏掉目标，后续推理无法挽回（约 10% 的错误源于此）。
*   **歧义处理**：对于语义本身极度模糊的查询（如“桌上的食物”但桌上有多种食物），模型仍存在随机猜测的情况。
*   **应用限制**：由于依赖大型 LLM，该方法在边缘设备或算力受限的环境下难以部署。

（完）
