---
title: "HeBA: Heterogeneous Bottleneck Adapters for Robust Vision-Language Models"
title_zh: HeBA：用于鲁棒视觉-语言模型的异构瓶颈适配器
authors: Md Jahidul Islam
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16653v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 利用模态特定结构偏差适配类CLIP视觉语言模型
tldr: 针对视觉语言模型（VLM）微调中忽略模态差异的问题，本文提出HeBA框架。该方法通过引入异构瓶颈适配器，为图像和文本分别设计2D深度可分离卷积和稠密线性投影，并结合瓶颈压缩结构与Kaiming初始化策略。HeBA在保持预训练知识的同时增强了特征鲁棒性，在11个少样本基准测试中取得了SOTA性能。
selection_source: fresh_fetch
motivation: 传统适配器对视觉和文本模态采用统一架构，忽略了图像的空间局部性与文本的语义密度差异。
method: 采用异构设计（视觉用2D卷积，文本用线性投影）、瓶颈压缩结构（D/4）以及Kaiming初始化来优化特征学习。
result: 在11个少样本学习基准测试中，HeBA显著提升了模型的稳定性和准确性，达到了新的领先水平。
conclusion: 引入模态特定的结构归纳偏置和瓶颈正则化是提升大规模视觉语言模型下游任务适配能力的有效途径。
---

## 摘要
将 CLIP 等大规模视觉-语言模型 (VLMs) 适配到下游任务时，通常受困于“一刀切”的架构方法，即视觉和文本标记 (tokens) 被宽泛的通用适配器统一处理。我们认为这种同质化忽略了模态之间截然不同的结构性质——图像中的空间局部性与文本中的语义密度。为了解决这一问题，我们提出了 HeBA (Heterogeneous Bottleneck Adapter)，这是一个引入了模态特定结构归纳偏置的统一架构框架。HeBA 通过三项关键架构创新区别于传统设计：(1) 异构性：它通过 2D 深度可分离卷积处理视觉标记以保留空间相关性，同时通过稠密线性投影独特地处理文本标记以捕获语义关系；(2) 瓶颈正则化：与标准的扩展型适配器不同，HeBA 采用压缩瓶颈 (D -> D/4)，显式地迫使模型学习紧凑、鲁棒的特征，并充当结构正则化器；(3) 主动梯度初始化：我们挑战了限制性的零初始化范式，利用 Kaiming 初始化策略确保充足的初始梯度流，从而在不损害冻结主干网络预训练知识的情况下加速收敛。大量实验表明，HeBA 的架构专业化设计实现了卓越的稳定性和准确性，在 11 个少样本基准测试中达到了当前最先进水平 (SOTA)。代码可在 https://github.com/Jahid12012021/VLM-HeBA 获取。

## Abstract
Adapting large-scale Vision-Language Models (VLMs) like CLIP to downstream tasks often suffers from a "one-size-fits-all" architectural approach, where visual and textual tokens are processed uniformly by wide, generic adapters. We argue that this homogeneity ignores the distinct structural nature of the modalities -- spatial locality in images versus semantic density in text. To address this, we propose HeBA (Heterogeneous Bottleneck Adapter), a unified architectural framework that introduces modality-specific structural inductive biases. HeBA departs from conventional designs through three key architectural innovations: (1) Heterogeneity: It processes visual tokens via 2D depthwise-separable convolutions to preserve spatial correlations, while distinctively processing text tokens via dense linear projections to capture semantic relationships; (2) Bottleneck Regularization: Unlike standard expanding adapters, HeBA employs a compression bottleneck (D -> D/4) that explicitly forces the model to learn compact, robust features and acts as a structural regularizer; and (3) Active Gradient Initialization: We challenge the restrictive zero-initialization paradigm, utilizing a Kaiming initialization strategy that ensures sufficient initial gradient flow to accelerate convergence without compromising the frozen backbone's pre-trained knowledge. Extensive experiments demonstrate that HeBA's architecturally specialized design achieves superior stability and accuracy, establishing a new state-of-the-art on 11 few-shot benchmarks. Code is available at https://github.com/Jahid12012021/VLM-HeBA.

---

## 论文详细总结（自动生成）

这是一份关于论文《HeBA: Heterogeneous Bottleneck Adapters for Robust Vision-Language Models》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何高效且鲁棒地将大规模预训练视觉-语言模型（如 CLIP）适配到下游任务。
*   **研究背景**：现有的参数高效微调（PEFT）方法（如 Prompt Learning 或 Adapter）通常采用“一刀切”的架构，将视觉和文本标记（tokens）视为统一的 1D 向量处理。
*   **动机**：这种同质化设计忽略了模态间的结构差异——图像具有**空间局部性**，而文本具有**语义密度**。现有适配器往往存在“空间健忘症”，且过度参数化的设计在小样本场景下容易过拟合。

### 2. 方法论
HeBA 框架引入了三种关键的架构创新：
*   **异构归纳偏置（Heterogeneity）**：
    *   **视觉流**：将 1D 标记还原为 2D 网格，利用 **3×3 深度可分离卷积（DW-Conv）** 显式建模空间相关性（如纹理、形状）。
    *   **文本流**：采用**稠密线性投影**，以保持全局语义的完整性。
*   **瓶颈正则化（Bottleneck Regularization）**：不同于以往将特征维度扩展（如 4倍）的适配器，HeBA 采用**压缩瓶颈结构（$D \to D/4$）**。这种物理约束迫使模型学习低秩、紧凑的特征表示，起到天然的正则化作用，过滤任务无关的噪声。
*   **主动梯度初始化（Active Gradient Initialization）**：挑战了主流的“零初始化”范式。HeBA 使用 **Kaiming 初始化**策略，确保训练初期有充足的梯度流，加速收敛，并通过动态缩放因子（Dynamic Scaling）和标签平滑（Label Smoothing）来维持主干网络的稳定性。

### 3. 实验设计
*   **数据集**：涵盖 11 个多样化的图像分类基准，包括通用物体（ImageNet, Caltech101）、细粒度分类（OxfordPets, StanfordCars, Flowers102, Food101, FGVCAircraft）、场景（SUN397）、纹理（DTD）、卫星图像（EuroSAT）和动作识别（UCF101）。
*   **评估协议**：
    1.  **从基类到新类的泛化（Base-to-Novel）**：在基类训练，评估对未见类别的泛化能力。
    2.  **跨数据集评估**：在 ImageNet 训练，直接在其他 10 个数据集上测试。
    3.  **领域泛化**：在 ImageNet 变体（V2, Sketch, A, R）上测试鲁棒性。
*   **对比方法**：包括 CLIP 原型、CoOp、CoCoOp、MaPLe、PromptSRC、MMA 以及直接前作 LwEIB 等 SOTA 方法。

### 4. 资源与算力
*   **硬件**：实验在单块 **NVIDIA Tesla P100 GPU** 上完成（通过 Kaggle Kernels 环境）。
*   **训练细节**：Base-to-Novel 任务训练 30 个 epoch，批大小为 16；跨数据集任务训练 10 个 epoch，批大小为 64。使用了 AdamW 或 SGD 优化器。

### 5. 实验数量与充分性
*   **实验规模**：在 11 个数据集上进行了详尽测试，涵盖了少样本学习的主要评价维度。
*   **消融实验**：针对初始化策略（Kaiming vs. Zero）、空间偏置（2D vs. 1D）、卷积层的作用以及推理时的缩放因子进行了专门的消融研究。
*   **客观性**：所有结果均取 3 次独立运行（不同随机种子）的平均值，确保了实验的公平性和结果的可重复性。

### 6. 主要结论与发现
*   **性能领先**：HeBA 在 11 个基准测试中达到了 **81.35% 的调和平均值（HM）**，刷新了 SOTA 纪录。
*   **结构敏感任务表现优异**：在 EuroSAT（卫星图）和 DTD（纹理）等极度依赖空间几何特征的任务上，HeBA 相比 LwEIB 等方法有显著提升（如 EuroSAT 的 HM 提升了 1.3%）。
*   **初始化策略的有效性**：证明了主动梯度初始化在配合瓶颈约束时，能比零初始化更快、更好地捕捉领域特征，且不会导致灾难性遗忘。

### 7. 优点与亮点
*   **模态感知设计**：通过异构架构尊重图像和文本的本质属性，解决了适配器中的“空间健忘”问题。
*   **参数高效且稳健**：压缩瓶颈设计不仅减少了参数量，还通过结构限制提升了模型在数据稀缺环境下的鲁棒性。
*   **即插即用**：作为轻量级模块，易于集成到现有的 Transformer 架构中。

### 8. 不足与局限
*   **推理缩放依赖**：实验表明，在跨数据集任务中需要手动调整推理时的适配器缩放因子（$\alpha_{novel}$）以获得最佳效果，这在完全自动化的部署中可能存在挑战。
*   **提示词依赖**：HeBA 结合了 LLM 生成的丰富描述（CuPL），虽然增强了性能，但也增加了对外部语言模型生成质量的依赖。
*   **计算开销**：虽然是轻量级的，但在每一层 Transformer 中插入多个适配器仍会带来微小的推理延迟增加。

（完）
