---
title: "SlotVTG: Object-Centric Adapter for Generalizable Video Temporal Grounding"
title_zh: SlotVTG：用于可泛化视频时序定位的以对象为中心适配器
authors: "Jiwook Han, Geo Ahn, Youngrae Kim, Jinwoo Choi"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25733v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 用于视频时序定位和以物体为中心推理的多模态大模型
tldr: 针对多模态大模型在视频时序定位（VTG）中因微调导致的泛化性差问题，本文提出SlotVTG框架。该框架引入轻量级槽位适配器，利用槽位注意力机制将视觉特征分解为以物体为中心的抽象表示，并结合自监督视觉模型的先验知识。实验表明，SlotVTG在保持域内性能的同时，显著提升了跨域泛化能力，且计算开销极低。
selection_source: fresh_fetch
motivation: 现有的VTG模型在微调过程中容易学习数据集特定的捷径而非真实的视觉内容，导致其在分布外场景下的泛化能力较差。
method: 引入轻量级槽位适配器，通过槽位注意力机制将视觉标记分解为语义连贯的物体中心表示，并利用自监督模型提供物体性先验。
result: 在标准VTG基准测试上的评估显示，该方法在极小开销下显著增强了模型的跨域鲁棒性，并保持了竞争力的域内性能。
conclusion: SlotVTG证明了通过引入以物体为中心的归纳偏置，可以有效缓解多模态大模型在视频理解任务中的过拟合问题并提升泛化性。
---

## 摘要
多模态大语言模型 (MLLM) 在视频时序定位 (VTG) 任务上表现出了强大的性能。然而，它们粗粒度的识别能力不足以进行细粒度的时序理解，使得特定任务的微调变得不可或缺。这种微调会导致模型记忆特定于数据集的捷径，而不是忠实地定位到实际的视觉内容，从而导致较差的域外 (OOD) 泛化能力。以对象为中心的学习通过将场景分解为实体级表示提供了一种有前景的补救方案，但现有方法需要从头开始重新运行整个多阶段训练流水线。我们提出了 SlotVTG，这是一个以极小成本引导 MLLM 进行以对象为中心、基于输入的视觉推理的框架。SlotVTG 引入了一个轻量级的槽位适配器 (slot adapter)，通过槽位注意力 (slot attention) 将视觉标记分解为抽象槽位并重构原始序列，其中来自自监督视觉模型的对象性先验 (objectness priors) 促进了语义连贯的槽位形成。在标准 VTG 基准测试上的跨域评估表明，我们的方法在保持具有竞争力的域内 (ID) 性能的同时，以极小的开销显著提高了 OOD 鲁棒性。

## Abstract
Multimodal Large Language Models (MLLMs) have shown strong performance on Video Temporal Grounding (VTG). However, their coarse recognition capabilities are insufficient for fine-grained temporal understanding, making task-specific fine-tuning indispensable. This fine-tuning causes models to memorize dataset-specific shortcuts rather than faithfully grounding in the actual visual content, leading to poor Out-of-Domain (OOD) generalization. Object-centric learning offers a promising remedy by decomposing scenes into entity-level representations, but existing approaches require re-running the entire multi-stage training pipeline from scratch. We propose SlotVTG, a framework that steers MLLMs toward object-centric, input-grounded visual reasoning at minimal cost. SlotVTG introduces a lightweight slot adapter that decomposes visual tokens into abstract slots via slot attention and reconstructs the original sequence, where objectness priors from a self-supervised vision model encourage semantically coherent slot formation. Cross-domain evaluation on standard VTG benchmarks demonstrates that our approach significantly improves OOD robustness while maintaining competitive In-Domain (ID) performance with minimal overhead.

---

## 论文详细总结（自动生成）

以下是对论文《SlotVTG: Object-Centric Adapter for Generalizable Video Temporal Grounding》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：多模态大语言模型（MLLM）在视频时序定位（VTG）任务中，虽然通过微调能获得较好的性能，但往往会产生“捷径学习”（Shortcut Learning）现象。模型倾向于记忆数据集特定的偏置（如时间位置偏置、文本查询偏置），而非真正理解视觉内容。
*   **核心问题**：当模型遇到分布外（OOD）的视频时，性能会剧烈下降。现有的以对象为中心的学习方法虽然能提升泛化性，但通常需要从头训练整个视觉-语言对齐流水线，成本极高。
*   **研究目标**：开发一种轻量级、低成本的方案，引导 MLLM 关注视频中的实体级表示，从而提升跨域泛化能力。

### 2. 论文提出的方法论
*   **核心思想**：引入一个轻量级的**槽位适配器（Slot Adapter）**，通过瓶颈结构强迫视觉特征分解为离散的、以物体为中心的“槽位”（Slots），并利用自监督视觉先验进行引导。
*   **关键技术细节**：
    *   **槽位适配器（Slot Adapter）**：插入在 LLM 解码器的早期层（1-7层）。它包含：
        1.  **下采样投影**：降低视觉标记维度。
        2.  **槽位注意力（Slot Attention）**：通过迭代竞争机制，让一组可学习的槽位去“竞争”解释输入的视觉标记，从而捕捉不同的语义实体。
        3.  **标记重构**：利用交叉注意力机制，从槽位中检索信息并重构回原始序列长度，通过残差连接反馈给 LLM。
    *   **槽位对齐损失（Slot Alignment Loss, $L_{SA}$）**：利用预训练的 DINOv2 模型提取特征相似度矩阵作为“教师”，引导槽位注意力图在语义上更加连贯（即相似的物体部分应归于同一槽位）。
    *   **训练策略**：冻结视觉编码器和 LLM 主体，仅微调 Slot Adapter 和后深层的 LoRA 参数。

### 3. 实验设计
*   **数据集**：使用了三个主流 VTG 基准数据集：**Charades-STA**、**QVHighlights** 和 **ActivityNet Captions**。
*   **Benchmark 设置**：采用跨域评估协议（Cross-domain evaluation），即在数据集 A 上训练，在数据集 B 和 C 上测试，以衡量 OOD 泛化性。
*   **对比方法**：
    *   **零样本 MLLM**：如 HawkEye, TimeSuite, UniTime, VideoMind。
    *   **DETR 类专用模型**：如 EaTR, CG-DETR。
    *   **微调类 MLLM**：如 Chrono-Qwen (基于 Qwen2.5-VL)。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA RTX 3090 或 4090 GPU**。
*   **训练参数**：
    *   训练 5 个 epoch，使用 AdamW 优化器。
    *   可训练参数量极小：3B 模型约为 7.6M（占总参数 0.25%），7B 模型约为 23.3M（占总参数 0.33%）。
*   **输入配置**：Charades-STA 采样 20 帧，QVHighlights 采样 60 帧。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的跨域对比实验，涵盖了 3 个数据集之间的多种组合（如 Cha.→ANet, QVH→Cha. 等）。
*   **消融实验**：非常详尽，包括适配器类型对比、损失函数权重 $\lambda$、插入层数位置、槽位数量 $N_s$、瓶颈维度 $d$ 以及重构方式的对比。
*   **诊断性分析**：通过噪声扰动实验（验证模型是否关注真实视觉内容）、视觉相似度分析和 MMD 距离测量（验证域间差距是否缩小），从多维度证明了方法的有效性。
*   **评价**：实验设计客观、公平，对比基准涵盖了从传统模型到最新大模型的多种路径，论据充分。

### 6. 论文的主要结论与发现
*   **泛化性显著提升**：SlotVTG 在 OOD 场景下显著优于基准模型（如在 ANet 上 R1@0.5 提升了约 2.4%~4.0%），同时保持了竞争力的域内（ID）性能。
*   **减少域间差距**：通过 MMD 测量发现，以物体为中心的表示将源域和目标域之间的特征分布差距缩小了约 49.6%。
*   **早期层的重要性**：实验发现将适配器插入 LLM 的早期层效果最好，因为这些层主要负责视觉-时序交互。
*   **视觉内容关注度**：噪声扰动实验证明，SlotVTG 相比普通微调模型，更加依赖目标时刻的实际视觉内容而非统计捷径。

### 7. 优点
*   **参数高效**：作为适配器插件，无需重训整个模型，计算开销极低。
*   **即插即用**：可以轻松集成到现有的预训练 MLLM（如 Qwen2.5-VL）中。
*   **可解释性强**：通过槽位可视化，清晰展示了模型如何将场景分解为人物、物体和背景等实体。
*   **理论支撑扎实**：结合了以对象为中心的学习理论与大语言模型的时序推理能力。

### 8. 不足与局限
*   **槽位数量固定**：实验中固定槽位数为 4，对于极度复杂的场景（包含大量细碎物体），固定的槽位数可能限制表达能力。
*   **视频长度限制**：实验主要针对 20-60 帧的短/中等长度视频，在超长视频（如数小时）上的表现尚未验证。
*   **依赖外部先验**：Slot Alignment 损失依赖于 DINOv2 的特征质量，如果基础视觉模型在某些特殊领域（如医学、红外视频）表现不佳，可能会影响效果。

（完）
