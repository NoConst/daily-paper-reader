---
title: "Time is Not a Label: Continuous Phase Rotation for Temporal Knowledge Graphs and Agentic Memory"
title_zh: 时间不是标签：用于时序知识图谱与智能体记忆的连续相位旋转
authors: "Weixian Waylon Li, Jiaxin Zhang, Xianan Jim Yang, Tiejun Ma, Yiwen Guo"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11544v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 用于智能体记忆的时序知识图谱模块
tldr: 针对现有知识图谱将时间视为离散标签导致难以区分持久与演化事实的问题，本文提出RoMem模块。它通过语义速度门学习关系的波动性，并利用连续相位旋转在复向量空间中实现几何遮蔽。该方法使过时事实自然降权而非删除，在时序知识图谱补全和智能体记忆任务中均达到SOTA性能，并具备出色的零样本泛化能力。
selection_source: fresh_fetch
motivation: 现有系统将时间视为离散元数据，无法有效区分持久事实与动态演化事实，导致记忆更新成本高且易丢失长期知识。
method: 提出RoMem模块，利用语义速度门预测关系波动性，并通过连续相位旋转使过时事实在复向量空间中自然被正确事实遮蔽。
result: 在ICEWS05-15数据集上取得SOTA，并在智能体记忆推理任务中实现2-3倍的性能提升，同时保持静态记忆零退化。
conclusion: 将时间建模为连续旋转相位而非离散标签，能显著提升结构化记忆系统的时序推理能力和跨领域泛化性。
---

## 摘要
结构化记忆表示（如知识图谱）是自主智能体和其他长效系统的核心。然而，大多数现有方法将时间建模为离散元数据，要么按新近度排序（从而掩埋了陈旧但永久的知识），要么简单地覆盖过时事实，或者在每个摄取步骤都需要昂贵的大语言模型（LLM）调用，导致其无法区分持久性事实与演化性事实。为此，我们引入了 RoMem，这是一个用于结构化记忆系统的即插即用式时序知识图谱模块，适用于智能体记忆及更多场景。预训练的语义速度门（Semantic Speed Gate）将每个关系的文本嵌入映射为波动性得分，通过数据学习到演化关系（如“...的总统”）应当快速旋转，而持久关系（如“出生于”）应当保持稳定。结合连续相位旋转，该方法实现了几何遮蔽（geometric shadowing）：过时的事实在复向量空间中被旋转出相位，使得时序正确的事实在不被删除的情况下自然地优于矛盾事实。在时序知识图谱补全任务中，RoMem 在 ICEWS05-15 上取得了最先进的结果（72.6 MRR）。在应用于智能体记忆时，它在时序推理（MultiTQ）上的 MRR 和回答准确率提升了 2-3 倍，在混合基准（LoCoMo）中表现优异，在静态记忆保持上实现零退化（DMR-MSC），并能零样本泛化至未见的金融领域（FinTMMBench）。

## Abstract
Structured memory representations such as knowledge graphs are central to autonomous agents and other long-lived systems. However, most existing approaches model time as discrete metadata, either sorting by recency (burying old-yet-permanent knowledge), simply overwriting outdated facts, or requiring an expensive LLM call at every ingestion step, leaving them unable to distinguish persistent facts from evolving ones. To address this, we introduce RoMem, a drop-in temporal knowledge graph module for structured memory systems, applicable to agentic memory and beyond. A pretrained Semantic Speed Gate maps each relation's text embedding to a volatility score, learning from data that evolving relations (e.g., "president of") should rotate fast while persistent ones (e.g., "born in") should remain stable. Combined with continuous phase rotation, this enables geometric shadowing: obsolete facts are rotated out of phase in complex vector space, so temporally correct facts naturally outrank contradictions without deletion. On temporal knowledge graph completion, RoMem achieves state-of-the-art results on ICEWS05-15 (72.6 MRR). Applied to agentic memory, it delivers 2-3x MRR and answer accuracy on temporal reasoning (MultiTQ), dominates hybrid benchmark (LoCoMo), preserves static memory with zero degradation (DMR-MSC), and generalises zero-shot to unseen financial domains (FinTMMBench).

---

## 论文详细总结（自动生成）

这是一份关于论文《Time is Not a Label: Continuous Phase Rotation for Temporal Knowledge Graphs and Agentic Memory》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对当前智能体（Agent）记忆系统在处理“时间”维度时的**静态-动态困境（Static-Dynamic Dilemma）**提出了挑战。
*   **研究动机**：现有的基于知识图谱（KG）的记忆系统通常将时间视为离散的元数据（如时间戳列）。这种做法存在三大弊端：
    1.  **破坏性覆盖**：为了更新事实而删除旧数据，导致历史上下文丢失。
    2.  **LLM 仲裁开销**：每次摄取新信息都需要调用 LLM 来判断是更新还是删除，成本极高且难以扩展。
    3.  **新近度偏见**：简单按时间排序会掩埋“旧但永久”的知识（如“出生地”），导致系统在检索时混淆持久事实与瞬时事实。
*   **核心含义**：论文主张“时间不是标签，而是一种连续的几何算子”。通过将时间融入向量空间的物理规律，使系统能够自动区分哪些关系是稳定的，哪些是随时间演化的。

### 2. 方法论：RoMem 架构
RoMem 引入了一种**仅追加（Append-only）**的结构化记忆模块，核心技术包括：

*   **核心思想：几何遮蔽（Geometric Shadowing）**
    在复向量空间中，将时间建模为连续的相位旋转。当查询当前时间时，过时的事实会因为相位偏移而自然地在几何距离上远离查询向量，从而被“遮蔽”，而最新的正确事实则保持对齐。
*   **关键技术细节**：
    1.  **语义速度门（Semantic Speed Gate, $\alpha_r$）**：这是一个预训练的 MLP 模块，输入关系的文本嵌入（Embedding），输出一个波动性得分 $\alpha_r \in (0, 1)$。
        *   静态关系（如 "born in"）：$\alpha_r \approx 0$，几乎不旋转，永久保持相位对齐。
        *   动态关系（如 "president of"）：$\alpha_r \approx 1$，随时间快速旋转，使旧事实迅速失效。
    2.  **连续旋转公式**：旋转角度定义为 $\theta_r(\tau) = s \cdot \alpha_r \cdot \tau \cdot \omega$。其中 $s$ 是全局时间缩放参数，$\tau$ 是连续时间戳，$\omega$ 是学习到的频率向量。
    3.  **两阶段训练**：
        *   **阶段 1**：离线预训练速度门，通过观察事实的变化频率学习关系的波动性。
        *   **阶段 2**：在线学习全局频谱和实体/关系嵌入，引入时间对比损失（Time-Contrastive Loss）来增强时间分辨力。

### 3. 实验设计
论文设计了多维度的实验来验证 RoMem 的有效性：
*   **数据集与场景**：
    1.  **ICEWS05-15**：标准时序知识图谱补全任务（验证基础建模能力）。
    2.  **MultiTQ**：重度时序推理任务，包含大量时间冲突。
    3.  **LoCoMo**：混合任务基准，测试长程对话记忆。
    4.  **DMR-MSC**：静态对话记忆，验证是否会损害非时序检索。
    5.  **FinTMMBench**：金融领域基准，测试零样本（Zero-shot）跨领域泛化能力。
*   **对比方法**：包括 Mem0、Zep、HippoRAG、A-Mem、LicoMemory 等主流智能体记忆框架，以及多种 SOTA 时序 KG 嵌入方法（如 ChronoR、TeRo 等）。

### 4. 资源与算力
*   **模型配置**：实验使用了 GPT-5-mini 和 LLaMA-3.1-70B 作为基础 LLM，嵌入模型使用了 OpenAI 的 `text-embedding-3-small` 和 BAAI 的 `BGE-M3`。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。但提到其架构是“即插即用”的，且避免了每步摄取时的 LLM 调用，暗示了其在推理阶段的计算效率优于基于 LLM 仲裁的方法。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个不同性质的数据集上进行了测试，涵盖了从基础图嵌入到下游 QA 任务的全流程。
*   **充分性评估**：
    *   **多模型验证**：在闭源 API（OpenAI）和开源模型（LLaMA）两种环境下均进行了实验，证明了方法的鲁棒性。
    *   **消融与定性分析**：附录中提供了详细的数学证明（如连续插值证明）和定性分析（如对“奥巴马”相关事实的评分轨迹追踪）。
    *   **客观性**：通过引入静态记忆基准（DMR-MSC）来证明该方法不会产生“副作用”，实验设计较为严谨、客观。

### 6. 主要结论与发现
1.  **性能突破**：在 MultiTQ 时序推理任务中，RoMem 将检索 MRR 和下游回答准确率提升了 **2-3 倍**。
2.  **SOTA 达成**：在 ICEWS05-15 上达到了 72.6 MRR，刷新了时序 KG 补全的记录。
3.  **零退化**：在处理纯静态记忆时，RoMem 保持了与基准模型持平甚至略优的性能，证明了“语义速度门”作为时间离合器的有效性。
4.  **零样本泛化**：语义速度门能够根据关系的语义（而非 ID）判断波动性，使其能直接应用于未见的金融领域关系。

### 7. 优点与亮点
*   **架构优雅**：采用 Append-only 架构，通过几何规律而非数据库删除操作解决冲突，保留了完整的历史轨迹。
*   **无需人工标注**：语义速度门自动从数据中学习关系的波动性，无需手动指定哪些关系是动态的。
*   **连续性建模**：解决了离散时间步无法进行时间插值（Interpolation）的问题，支持对任意未见日期的查询。
*   **低延迟**：将时间逻辑下沉到嵌入层，减少了对昂贵 LLM 调用的依赖。

### 8. 不足与局限性
*   **高频振荡风险**：虽然论文给出了单调交叉的数学证明，但在实际多维空间中，若频率参数学习不当，可能会在非观测时间段产生评分振荡。
*   **依赖 OpenIE 质量**：系统的表现高度依赖于前端 LLM 提取三元组和时间信息的准确性，若提取阶段出错，几何旋转也无法纠正。
*   **复杂推理限制**：目前的几何遮蔽主要解决直接事实冲突，对于需要跨越极长时序链条的复杂逻辑推理，可能仍需结合更高层的推理机制。

（完）
