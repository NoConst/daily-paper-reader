---
title: "Reflect to Inform: Boosting Multimodal Reasoning via Information-Gain-Driven Verification"
title_zh: 反思以告知：通过信息增益驱动的验证提升多模态推理
authors: "Shuai Lv, Chang Liu, Feng Tang, Yujie Yuan, Aojun Zhou, Kui Zhang, Xi Yang, Yangqiu Song"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26348v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 通过视觉内省增强多模态大模型的推理能力
tldr: 针对多模态大模型在长文本推理中容易脱离图像证据并产生幻觉的问题，本文提出了一种名为视觉重新检查（VRE）的自进化训练框架。该框架利用模型内在的视觉验证潜力，通过信息增益驱动的反射轨迹进行自我改进，无需额外视觉输入即可增强推理的准确性和感知可靠性，显著减少了长链推理中的幻觉现象。
selection_source: fresh_fetch
motivation: 研究发现多模态模型在长生成过程中会逐渐偏离图像信息，但其内部仍保留着未被充分激活的视觉验证能力。
method: 提出VRE框架，通过信息增益引导模型生成反射轨迹，实现无需外部教师模型的迭代式自我进化。
result: 在多个多模态基准测试中，该方法显著提升了推理准确性，并大幅降低了长文本生成中的幻觉。
conclusion: VRE证明了通过激活模型内在的视觉内省能力，可以有效提升多模态推理的可靠性与一致性。
---

## 摘要
多模态大语言模型（MLLMs）在多模态推理方面表现出色，但我们发现在长文本生成中存在一种反复出现的失败模式：随着输出变长，模型逐渐偏离图像证据，转而依赖文本先验，导致推理缺乏依据并产生幻觉。有趣的是，基于注意力分析，我们发现 MLLMs 具备一种潜在的后期视觉验证能力，这种能力虽然存在但并未被持续激活。受此启发，我们提出了视觉再检查（Visual Re-Examination, VRE），这是一个自我进化的训练框架，使 MLLMs 能够在推理过程中自主进行视觉内省，而无需额外的视觉输入。VRE 并非从更强大的教师模型中蒸馏视觉能力，而是通过利用模型自身生成反思轨迹来促进迭代式自我提升，通过信息增益使视觉信息变得可操作。在多种多模态基准测试上的广泛实验表明，VRE 持续提高了推理准确性和感知可靠性，同时显著减少了幻觉，尤其是在长链推理场景下。代码可在 https://github.com/Xiaobu-USTC/VRE 获取。

## Abstract
Multimodal Large Language Models (MLLMs) achieve strong multimodal reasoning performance, yet we identify a recurring failure mode in long-form generation: as outputs grow longer, models progressively drift away from image evidence and fall back on textual priors, resulting in ungrounded reasoning and hallucinations. Interestingly, Based on attention analysis, we find that MLLMs have a latent capability for late-stage visual verification that is present but not consistently activated. Motivated by this observation, we propose Visual Re-Examination (VRE), a self-evolving training framework that enables MLLMs to autonomously perform visual introspection during reasoning without additional visual inputs. Rather than distilling visual capabilities from a stronger teacher, VRE promotes iterative self-improvement by leveraging the model itself to generate reflection traces, making visual information actionable through information gain. Extensive experiments across diverse multimodal benchmarks demonstrate that VRE consistently improves reasoning accuracy and perceptual reliability, while substantially reducing hallucinations, especially in long-chain settings. Code is available at https://github.com/Xiaobu-USTC/VRE.

---

## 论文详细总结（自动生成）

以下是对论文《Reflect to Inform: Boosting Multimodal Reasoning via Information-Gain-Driven Verification》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：多模态大语言模型（MLLM）在进行长链推理（Long-form reasoning）时，存在明显的**“视觉漂移”（Visual Drift）**现象。即随着生成文本的增长，模型会逐渐忽略原始图像证据，转而依赖语言模型的文本先验知识，导致推理过程脱离实际图像，产生严重的幻觉。
*   **研究动机**：作者通过注意力机制分析发现，MLLM 内部其实潜藏着一种“后期视觉验证”的能力，只是这种能力在常规推理中处于休眠状态。现有的解决方法通常依赖外部工具（如切图、放大）或强教师模型蒸馏，这会带来额外的计算开销或感知偏差。因此，本文旨在探索如何让模型实现**自主的、无需外部干预的视觉内省**。

### 2. 方法论：核心思想与关键技术
论文提出了 **VRE（Visual Re-Examination，视觉再检查）** 框架，这是一个闭环的自进化训练体系，核心思想是让模型学会“在推理中停下来，重新看一眼图像”。

*   **核心流程**：
    1.  **反射数据构建（冷启动 SFT）**：
        *   **难度感知划分**：根据模型对样本的通过率（Pass Rate）将其分为稳定（Stable）、棘手（Intractable）和不稳定（Unstable）三类。重点利用“不稳定”样本作为训练信号。
        *   **信息增益驱动的合成**：模型基于自身生成的初始推理轨迹，再次观察图像，生成包含新视觉证据的`<reflection>`块。分为三种模式：**阐述性反射**（补充细节）、**纠正性反射**（修复错误）、**复核性反射**（验证精度）。
    2.  **强化学习与可验证奖励（RLVR）**：
        *   采用 **GRPO（群组相对策略优化）** 算法。
        *   **多维奖励函数**：$R = \lambda_{form}R_{form} + \lambda_{acc}R_{acc} + \lambda_{refl}R_{refl}$。其中 $R_{refl}$（反射奖励）最为关键，利用 LLM 评分器判断反射内容是否提供了真实、非冗余的视觉信息增益。
    3.  **Post-RL 自蒸馏**：利用 RL 优化后的模型重新解决之前失败的难题，收集高质量轨迹进行新一轮 SFT，从而稳定策略并拓宽能力边界。
*   **同源重构（Homologous Reconstruction）**：坚持使用模型自身的视觉编码器进行反思，确保感知与推理的严格对齐，避免了外部知识引入的感知冲突。

### 3. 实验设计
*   **数据集与 Benchmark**：
    *   **数学与逻辑推理**：MathVista, MathVerse, MathVision, WeMath, LogicVista。
    *   **高分辨率感知**：V*-Bench（侧重细微物体定位）。
    *   **文档与图表理解**：ChartQA, OCRBench_v2。
    *   **多学科综合**：MMMU。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Claude 3.7。
    *   **开源通用模型**：Qwen2.5-VL-7B/32B, InternVL2.5/3。
    *   **开源推理增强模型**：DeepEyesV2, Thyme, Semantic-back, R1-VL 等。

### 4. 资源与算力
*   **硬件设备**：
    *   **SFT 阶段**：使用 8 台华为昇腾（Ascend）910B (64G) NPU。
    *   **RLVR 阶段**：扩展至 64 台 Ascend 910B NPU。
*   **并行策略**：采用了 3D 并行策略（张量并行、流水线并行、数据并行度均为 4）。
*   **训练时长**：文中未明确给出总训练小时数，但提到 RL 阶段在 100 步左右达到最优性能，随后会出现过拟合。

### 5. 实验数量与充分性
*   **实验规模**：在 10 个主流多模态基准测试上进行了验证，涵盖了从基础感知到高阶逻辑推理的各个维度。
*   **消融实验**：系统地对比了“仅 SFT”、“SFT + RL”以及“完整 VRE（含迭代 SFT）”的效果，证明了各模块的增益。
*   **深度分析**：
    *   **注意力可视化**：通过热力图证明了模型在`<reflection>`阶段确实出现了视觉注意力的“回流”。
    *   **信息论验证**：利用 KL 散度和条件熵（Conditional Entropy）证明了 VRE 生成的反射具有高信息密度，而非简单的文本重复。
*   **评价**：实验设计非常充分且客观，通过统计学指标（如通过率分布变化）深入揭示了模型能力的进化过程。

### 6. 主要结论与发现
*   **性能突破**：VRE 在 7B 参数规模下，在多个榜单上超越了需要外部工具（Tool-use）的推理模型（如 Thyme, DeepEyesV2），并显著缩小了与 GPT-4o 等巨型闭源模型的差距。
*   **解决视觉漂移**：通过强制性的反射机制，模型在长文本生成中能保持对图像的持续关注，V*-Bench 上的感知准确率提升了 7.4%。
*   **自进化可行性**：证明了 MLLM 可以通过“自我博弈”和“信息增益驱动的奖励”实现能力的闭环提升，无需依赖更强的教师模型。

### 7. 优点
*   **架构简洁**：无需修改模型架构，无需引入高分辨率切图插件，完全基于原生注意力机制。
*   **数据高效**：通过“信息增益”标准筛选数据，避免了强化学习中常见的“奖励作弊”（Reward Hacking，即模型通过刷字数来骗取奖励）。
*   **感知对齐**：同源重构设计确保了模型是在用“自己的眼睛”看世界，推理更加稳健。

### 8. 不足与局限
*   **模型规模局限**：实验主要集中在 7B 规模，虽然效果显著，但在更大规模（如 72B+）模型上的扩展性有待进一步验证。
*   **判别器依赖**：RL 阶段的反射奖励依赖于另一个大模型（Qwen2.5-72B）作为裁判，这可能引入裁判模型的偏见或评分不一致性。
*   **计算成本**：虽然推理时无需额外输入，但训练阶段的多次采样（Rollout）和迭代 SFT 对算力资源仍有较高要求。

（完）
