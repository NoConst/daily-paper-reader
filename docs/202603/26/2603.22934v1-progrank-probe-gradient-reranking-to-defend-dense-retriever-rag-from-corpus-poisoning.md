---
title: "ProGRank: Probe-Gradient Reranking to Defend Dense-Retriever RAG from Corpus Poisoning"
title_zh: ProGRank：通过探针梯度重排序防御稠密检索 RAG 中的语料库投毒
authors: "Xiangyu Yin, Yi Qi, Chih-hong Cheng"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22934v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 针对密集检索RAG的防御
tldr: 针对检索增强生成（RAG）面临的语料库投毒攻击，本文提出了一种名为 ProGRank 的防御方法。该方法是一种事后、无需训练的检索端重排序防御机制，通过对查询-文档对进行随机扰动并提取探针梯度，利用表示一致性和分散风险两个不稳定性信号来识别恶意文档。实验证明，ProGRank 在多种数据集和检索器上均表现出优异的防御性能和鲁棒性，且无需修改文档内容或重新训练模型。
selection_source: fresh_fetch
motivation: 现有的 RAG 语料库投毒防御方法往往依赖内容过滤或辅助模型，导致部署困难且可能损害正常检索性能。
method: ProGRank 通过对查询-文档对施加随机扰动并提取检索器特定参数子集的探针梯度，结合表示一致性和分散风险信号进行重排序。
result: 在三个数据集和三种检索器上的实验表明，ProGRank 在检索阶段和端到端设置下均提供了更强的防御能力和良好的效用平衡。
conclusion: ProGRank 是一种高效、灵活且无需训练的防御方案，能有效抵御针对稠密检索器的语料库投毒攻击。
---

## 摘要
检索增强生成 (RAG) 通过将生成内容锚定在检索到的证据上，提升了大语言模型应用的可靠性，但同时也引入了一个新的攻击面：语料库投毒。在这种设定下，攻击者通过注入或编辑段落，使其在针对目标查询的检索结果中排名前 K 位，从而影响下游生成。现有的语料库投毒防御手段通常依赖于内容过滤、辅助模型或生成器端推理，这会增加部署难度。我们提出了 ProGRank，这是一种针对稠密检索 RAG 的事后、无需训练的检索端防御方案。ProGRank 在轻微的随机扰动下对每个“查询-段落”对进行压力测试，并从检索器的一小部分固定参数子集中提取探针梯度。基于这些信号，它推导出两种不稳定性信号：表示一致性和离散风险，并在重排序步骤中将它们与评分门控相结合。ProGRank 保留了原始段落内容，无需重新训练，且在部署的检索器不可用时支持基于代理模型的变体。在三个数据集、三个稠密检索器骨干网络、代表性语料库投毒攻击以及检索阶段和端到端设置下的广泛实验表明，ProGRank 提供了更强的防御性能以及良好的鲁棒性与效用权衡。此外，它在自适应规避攻击下也保持了竞争力。

## Abstract
Retrieval-Augmented Generation (RAG) improves the reliability of large language model applications by grounding generation in retrieved evidence, but it also introduces a new attack surface: corpus poisoning. In this setting, an adversary injects or edits passages so that they are ranked into the Top-$K$ results for target queries and then affect downstream generation. Existing defences against corpus poisoning often rely on content filtering, auxiliary models, or generator-side reasoning, which can make deployment more difficult. We propose ProGRank, a post hoc, training-free retriever-side defence for dense-retriever RAG. ProGRank stress-tests each query--passage pair under mild randomized perturbations and extracts probe gradients from a small fixed parameter subset of the retriever. From these signals, it derives two instability signals, representational consistency and dispersion risk, and combines them with a score gate in a reranking step. ProGRank preserves the original passage content, requires no retraining, and also supports a surrogate-based variant when the deployed retriever is unavailable. Extensive experiments across three datasets, three dense retriever backbones, representative corpus poisoning attacks, and both retrieval-stage and end-to-end settings show that ProGRank provides stronger defence performance and a favorable robustness--utility trade-off. It also remains competitive under adaptive evasive attacks.

---

## 论文详细总结（自动生成）

### 论文总结：ProGRank —— 通过探针梯度重排序防御稠密检索 RAG 中的语料库投毒

#### 1. 论文的核心问题与整体含义
*   **研究背景**：检索增强生成（RAG）通过引入外部知识提升了 LLM 的可靠性，但也引入了“语料库投毒（Corpus Poisoning）”这一攻击面。攻击者通过注入精心构造的文档，使其在检索阶段进入 Top-K，从而误导下游生成器产生错误或有害信息。
*   **核心问题**：现有的防御方法（如内容过滤、辅助检测模型或生成器端推理）往往部署复杂、计算开销大，或者需要重新训练检索器。
*   **研究动机**：开发一种**事后（Post hoc）、无需训练、仅在检索端运行**的轻量级防御机制，在不修改原始文档内容的前提下，有效识别并抑制经过优化后的投毒文档。

#### 2. 论文提出的方法论
ProGRank 的核心思想是利用投毒文档在随机扰动下的“不稳定性”进行识别。
*   **随机压力测试（Randomized Probing）**：对每个“查询-文档”对施加轻微的随机扰动（如 Token Dropout 或 Encoder Dropout），重复 $R$ 次。
*   **探针梯度提取（Probe Gradients）**：固定检索器参数，仅计算检索相似度得分相对于一小部分固定参数子集（如第 $L$ 层的 LayerNorm 模块）的梯度。
*   **两个不稳定性信号**：
    1.  **表示一致性（Representational Consistency, Rep）**：衡量不同扰动下梯度方向的一致性。投毒文档通常依赖特定的脆弱特征，一致性较低。
    2.  **分散风险（Dispersion Risk, DR）**：通过指数核函数和分位数统计，捕捉梯度偏离均值的下界风险，识别极端不稳定的情况。
*   **得分门控惩罚融合（Score-Gated Penalty Fusion）**：
    *   计算一个基于原始得分分布的门控值（Sigmoid 函数），确保惩罚主要作用于高分区域（即最有潜力进入 Top-K 的候选文档）。
    *   最终得分 = 原始得分 - 门控值 × (一致性惩罚 + 分散风险惩罚)。

#### 3. 实验设计
*   **数据集**：MS MARCO、Natural Questions (NQ)、HotpotQA。
*   **投毒攻击方法**：PoisonedRAG、LIAR-RAG、Joint-GCG（涵盖了针对检索、生成及联合优化的多种攻击手段）。
*   **检索器骨干**：Contriever、DPR、BGE。
*   **对比基准（Benchmarks）**：
    *   **Baseline**：无防御的原始检索。
    *   **对比方法**：GRADA（基于图的重排序）、GMTP（基于梯度的掩码概率）、RAGuard（基于异常得分的防御）。
*   **评估指标**：检索阶段的投毒命中率（Poison Hit Rate）、投毒召回率（Poison Recall Rate）；端到端的攻击成功率（ASR）和准确率（ACC）。

#### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA DGX Spark** 服务器上进行。
*   **算力细节**：论文未明确给出具体的 GPU 数量和总训练时长，但强调了该方法是**无需训练（Training-free）**的。
*   **推理延迟**：文中提供了端到端延迟测试（表 4）。ProGRank 的平均延迟约为 4.73 秒/查询，远快于 RAGuard（118.17 秒/查询），但慢于 GRADA（0.90 秒/查询）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个数据集 × 3 种检索器 × 3 种攻击方式，并针对生成器（Qwen2.5、Llama-3、Mistral）进行了交叉验证。
*   **消融实验**：详细分析了扰动次数 $R$、探针层数 $L$、扰动类型（Token/Encoder/Mixed）以及惩罚项构成对性能的影响。
*   **自适应攻击测试**：专门针对“已知防御机制”的自适应规避攻击进行了鲁棒性评估。
*   **评价**：实验设计全面，覆盖了从检索指标到端到端生成的全链路，对比了当前主流的开源防御方案，实验结果具有较高的客观性和说服力。

#### 6. 论文的主要结论与发现
*   **防御有效性**：ProGRank 在所有数据集上均显著降低了投毒文档的 Top-K 曝光率，在端到端评估中达到了最低的平均攻击成功率（ASR 接近 0）。
*   **性能权衡**：在大幅提升鲁棒性的同时，ProGRank 对正常查询的准确率（Clean Utility）虽有小幅波动，但在 NQ 等数据集上甚至提升了准确率。
*   **鲁棒性**：即使面对针对性的自适应规避攻击，ProGRank 依然能保持较强的防御能力。
*   **参数敏感性**：实验发现 $R=20$ 且探测浅层（如第 3 层）LayerNorm 时效果最佳。

#### 7. 优点
*   **部署友好**：无需重新训练模型，不依赖辅助的大语言模型（LLM）进行实时推理，适合集成到现有 RAG 流程中。
*   **内容保真**：不像某些方法需要对检索内容进行掩码或分块，ProGRank 保留了文档的完整性。
*   **原理创新**：将参数空间的敏感性分析（探针梯度）引入检索防御，比单纯的表示空间分析更深入。
*   **效率平衡**：在防御强度和计算开销之间取得了较好的平衡。

#### 8. 不足与局限
*   **攻击假设限制**：该方法主要针对“基于优化”的投毒攻击（这类攻击往往导致模型响应不稳定）。对于某些非优化、纯语义的误导性文档，其梯度信号可能不够显著。
*   **计算开销**：虽然比 LLM 验证快，但相比原始检索，$R$ 次随机扰动和梯度计算仍增加了数秒的延迟，在极高并发场景下可能存在压力。
*   **效用损失风险**：在某些数据集（如 MS MARCO）上，防御机制可能会误伤部分高质量的正常文档，导致清洁任务的 F1 或 ROUGE 分数略微下降。

（完）
