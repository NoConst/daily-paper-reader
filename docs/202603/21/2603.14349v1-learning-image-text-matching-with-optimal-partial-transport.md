---
title: Learning Image-Text Matching with Optimal Partial Transport
title_zh: 基于最优部分传输的图像-文本匹配学习
authors: "Zhengxin Pan, Haishuai Wang, Fangyu Wu, Bailing Zhang, Jiajun Bu, Hongyang Chen"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14349v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 跨模态匹配以及视觉和文本片段之间的相似性
tldr: 跨模态匹配是连接视觉与语言的关键任务，但现有方法难以兼顾性能与效率。本文提出OMIT网络，基于最优传输理论，利用Sinkhorn迭代高效计算细粒度图文片段间的相似度。通过引入局部到全局的部分匹配机制，有效消除了冗余对齐的干扰。在Flickr30K和MS-COCO数据集上的实验证明了该方法在提升匹配精度的同时保持了高效性。
selection_source: fresh_fetch
motivation: 旨在解决现有跨模态匹配方法在衡量语义相关性时难以同时实现高性能和高效率的问题。
method: 提出OMIT网络，结合Sinkhorn迭代的最优传输理论与部分匹配机制，实现细粒度片段的高效精准对齐。
result: 在Flickr30K和MS-COCO基准数据集上均取得了优越的检索性能，验证了方法的有效性。
conclusion: OMIT通过聚焦匹配和消除冗余干扰，为高效且精准的跨模态图文匹配提供了一种有效的解决方案。
---

## 摘要
跨模态匹配是连接视觉与语言的一项基础任务，近年来引起了广泛的研究兴趣。尽管已经开发了许多旨在量化图像-文本对之间语义相关性的方法，但这些方法往往难以同时实现卓越的性能和高效率。在本文中，我们提出了跨模态 Sinkhorn 匹配（OMIT）网络，作为在保持效率的同时有效提升性能的有效解决方案。基于最优传输（Optimal Transport）的理论基础，OMIT 利用跨模态搬运距离（Cross-modal Mover's Distance）的能力来精确计算细粒度视觉和文本片段之间的相似度，并利用 Sinkhorn 迭代进行高效近似。为了进一步缓解冗余对齐问题，我们将部分匹配（partial matching）无缝集成到 OMIT 中，利用局部到全局的相似性来消除无关片段的干扰。我们在两个基准图像-文本检索数据集（即 Flickr30K 和 MS-COCO）上对 OMIT 进行了广泛的评估。OMIT 在这两个数据集上取得的优异性能明确证明了其在跨模态匹配中的有效性。此外，通过全面的可视化分析，我们阐明了 OMIT 固有的聚焦匹配倾向，从而揭示了其有效性的原因。我们的代码已在 https://github.com/ppanzx/OMIT 公开发布。

## Abstract
Cross-modal matching, a fundamental task in bridging vision and language, has recently garnered substantial research interest. Despite the development of numerous methods aimed at quantifying the semantic relatedness between image-text pairs, these methods often fall short of achieving both outstanding performance and high efficiency. In this paper, we propose the crOss-Modal sInkhorn maTching (OMIT) network as an effective solution to effectively improving performance while maintaining efficiency. Rooted in the theoretical foundations of Optimal Transport, OMIT harnesses the capabilities of Cross-modal Mover's Distance to precisely compute the similarity between fine-grained visual and textual fragments, utilizing Sinkhorn iterations for efficient approximation. To further alleviate the issue of redundant alignments, we seamlessly integrate partial matching into OMIT, leveraging local-to-global similarities to eliminate the interference of irrelevant fragments. We conduct extensive evaluations of OMIT on two benchmark image-text retrieval datasets, namely Flickr30K and MS-COCO. The superior performance achieved by OMIT on both datasets unequivocally demonstrates its effectiveness in cross-modal matching. Furthermore, through comprehensive visualization analysis, we elucidate OMIT's inherent tendency towards focal matching, thereby shedding light on its efficacy. Our code is publicly available at https://github.com/ppanzx/OMIT.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Image-Text Matching with Optimal Partial Transport》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证**高效率**的同时，实现**高精度**的细粒度图像-文本匹配。
*   **研究背景**：
    *   传统的**全局嵌入方法（VSE）**效率高但缺乏细粒度语义表达。
    *   **交叉注意力方法（CAM）**虽然精度高，但计算开销大，且容易受到图像/文本中无关冗余片段（Redundant Alignments）的干扰。
    *   **概率嵌入方法（PEM）**试图通过分布建模解决歧义，但受限于分布假设的简单性。
*   **动机**：利用**最优传输（Optimal Transport, OT）**理论来衡量跨模态片段间的距离，并通过近似算法和部分匹配机制解决效率与冗余对齐问题。

### 2. 方法论
*   **核心思想**：将图像和文本分别视为离散的特征片段集合，通过计算“跨模态搬运距离”（Cross-modal Mover’s Distance）来衡量相似度。
*   **关键技术细节**：
    *   **Sinkhorn 迭代**：为了解决传统最优传输（EMD）计算复杂度高的问题，引入熵正则化项，利用 Sinkhorn-Knopp 算法进行快速数值近似，显著提升了匹配效率。
    *   **最优部分传输（Optimal Partial Transport, OPT）**：针对图文对中存在不相关片段（如背景物体或虚词）的问题，引入“垃圾桶”（Dustbins）机制。将全局嵌入作为辅助节点，吸收无法匹配的局部片段，从而放宽边际约束，实现局部到全局的部分匹配。
    *   **OMIT 网络架构**：
        1.  **特征提取**：视觉端支持 Grid（ResNet/ResNeXT）和 Region（Faster R-CNN）特征；文本端支持 BiGRU 和 BERT。
        2.  **部分 Sinkhorn 匹配模块**：计算片段间的成本矩阵，通过迭代得到最优传输方案。
        3.  **目标函数**：采用带有在线难负样本挖掘（Hardest Negative Mining）的 Hinge-based 三元组排序损失。

### 3. 实验设计
*   **数据集**：
    *   **MS-COCO**：包含 12.3 万张图像，每张图配 5 句描述。测试采用 5K 全量测试和 5-fold 1K 测试。
    *   **Flickr30K**：包含 3.1 万张图像。
*   **Benchmark 与对比方法**：
    *   对比了包括 VSE++、SCAN、VSRN、IMRAM、SGRAF、NAAF、CHAN、HREM 以及 CLIP 等在内的数十种 SOTA 方法。
    *   实验涵盖了不同的视觉骨干网络（ResNet-152, ResNeXT-101, BUTD）和文本编码器（BiGRU, BERT）。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号和数量。
*   **训练细节**：
    *   优化器：AdamW，权重衰减 5e-4。
    *   Batch Size：区域特征为 384，网格特征为 128。
    *   训练周期：共 15 个 epoch（前 10 个 epoch 学习率为 5e-4，后 5 个为 5e-5）。
    *   效率评估：在 Table 3 中对比了匹配耗时，OMIT 的耗时（15.03s）远低于 CAM 方法（42.55s），接近 VSE 方法（9.48s）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集上进行了全方位的性能对比。
    *   **消融实验**：对比了不同匹配机制（VSE, PEM, CAM vs OMIT）、超参数 $\lambda$ 的敏感性、部分匹配的有效性、以及四种不同的边际初始化策略（Uni, Intra, Inter, Norm）。
*   **充分性评价**：实验设计非常充分且客观。通过控制变量法（仅更换匹配模块）确保了对比的公平性，并提供了丰富的可视化分析（传输方案的稀疏性演变）来验证理论的合理性。

### 6. 主要结论与发现
*   **性能领先**：OMIT 在所有基准测试中均取得了 SOTA 结果，尤其在 R@1 指标上通常比现有方法高出约 2%。
*   **效率优势**：通过 Sinkhorn 迭代，OMIT 成功弥补了全局嵌入方法与局部匹配方法之间的效率鸿沟。
*   **部分匹配的必要性**：实验证明，引入“垃圾桶”机制处理冗余片段能显著提升检索精度，且该机制具有通用性（也可提升 CAM 的性能）。
*   **聚焦匹配**：可视化显示 OMIT 具有天然的“聚焦”特性，能够自动关注关键的语义对齐对。

### 7. 优点（亮点）
*   **理论结合紧密**：将数学上的最优传输理论优雅地应用于跨模态对齐，解决了离散分布匹配的难题。
*   **兼顾性能与效率**：打破了“细粒度匹配必低效”的固有印象。
*   **鲁棒性强**：部分传输机制有效解决了现实场景中图文信息不对等（冗余信息多）的痛点。
*   **即插即用**：该匹配模块可以适配多种主流的视觉和文本骨干网络。

### 8. 不足与局限
*   **认知推理能力有限**：可视化分析显示，OMIT 在处理抽象概念、功能词（如 "of", "back"）以及复杂的逻辑关系（如“谁背着谁”）时仍存在困难，主要依赖共现频率而非深层逻辑推理。
*   **超参数依赖**：虽然实验做了分析，但 Sinkhorn 迭代中的正则化参数 $\lambda$ 和部分匹配的阈值仍需根据不同数据集进行微调。
*   **骨干网络依赖**：性能提升在很大程度上仍受限于底层特征提取器的质量。

（完）
