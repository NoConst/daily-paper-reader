---
title: "Memory Transfer Learning: How Memories are Transferred Across Domains in Coding Agents"
title_zh: 记忆迁移学习：编程智能体中记忆如何跨领域迁移
authors: "Kangsan Kim, Minki Kang, Taeil Kim, Yanlai Yang, Mengye Ren, Sung Ju Hwang"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14004v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 代码智能体跨领域的记忆迁移学习
tldr: "本研究探讨了编程智能体中的“记忆迁移学习”（MTL），旨在解决现有智能体仅在单一领域利用记忆的局限。通过构建跨异构领域的统一记忆池，研究评估了从具体追踪到抽象见解的四种记忆表示。实验表明，跨领域记忆能提升3.7%的平均性能，且迁移的核心是验证程序等元知识而非具体代码。研究揭示了抽象程度对迁移性的关键影响，并证明了记忆在不同模型间也具有可迁移性，为构建跨领域自进化智能体提供了重要指导。"
selection_source: fresh_fetch
motivation: 现有的编程智能体通常将记忆局限于同类任务，忽略了不同编程领域间共享的运行环境和语言基础。
method: 提出记忆迁移学习框架，通过在6个基准测试上评估从具体追踪到抽象见解的四种记忆表示，研究跨领域记忆的利用效率。
result: "跨领域记忆使平均性能提升3.7%，且高层抽象见解比底层具体追踪更具通用性，后者甚至可能导致负迁移。"
conclusion: 记忆的迁移效果取决于其抽象程度并随记忆库规模增长，为打破领域孤岛、实现跨领域记忆共享提供了实证设计原则。
---

## 摘要
基于记忆的自我演化已成为编程智能体（coding agents）的一种极具前景的范式。然而，现有方法通常将记忆利用局限于同质任务领域，未能利用跨越不同现实世界编程问题的共享基础设施基础，如运行时环境和编程语言。为解决这一局限性，我们通过利用来自异质领域的统一记忆池，研究了记忆迁移学习（MTL）。我们使用从具体追踪（traces）到抽象见解（insights）的四种记忆表示，在 6 个编程基准测试上评估了性能。我们的实验表明，跨领域记忆使平均性能提升了 3.7%，这主要是通过迁移元知识（如验证例程）而非特定任务的代码实现的。重要的是，我们发现抽象程度决定了可迁移性；高层见解具有良好的泛化性，而底层追踪往往因过度特异性而导致负迁移。此外，我们展示了迁移效果随记忆池规模的扩大而提升，且记忆甚至可以在不同模型之间进行迁移。我们的工作为将记忆利用扩展到单领域孤岛之外建立了经验性设计原则。项目主页：https://memorytransfer.github.io/

## Abstract
Memory-based self-evolution has emerged as a promising paradigm for coding agents. However, existing approaches typically restrict memory utilization to homogeneous task domains, failing to leverage the shared infrastructural foundations, such as runtime environments and programming languages, that exist across diverse real-world coding problems. To address this limitation, we investigate \textbf{Memory Transfer Learning} (MTL) by harnessing a unified memory pool from heterogeneous domains. We evaluate performance across 6 coding benchmarks using four memory representations, ranging from concrete traces to abstract insights. Our experiments demonstrate that cross-domain memory improves average performance by 3.7\%, primarily by transferring meta-knowledge, such as validation routines, rather than task-specific code. Importantly, we find that abstraction dictates transferability; high-level insights generalize well, whereas low-level traces often induce negative transfer due to excessive specificity. Furthermore, we show that transfer effectiveness scales with the size of the memory pool, and memory can be transferred even between different models. Our work establishes empirical design principles for expanding memory utilization beyond single-domain silos. Project page: https://memorytransfer.github.io/

---

## 论文详细总结（自动生成）

这篇论文《Memory Transfer Learning: How Memories are Transferred Across Domains in Coding Agents》对编程智能体如何跨越不同任务领域利用记忆进行了深入研究。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的编程智能体（Coding Agents）主要依赖基于记忆的自我演化（Self-evolution），但现有的方法通常局限于“同质领域”（如在同一个基准测试内生成和使用记忆）。
*   **核心问题**：论文探讨了**记忆迁移学习（MTL）**的可能性。既然不同的编程任务（如算法竞赛、软件工程、机器学习开发）共享底层基础设施（如 Linux Shell、编程语言语法、调试逻辑），那么来自异构领域的记忆是否能提升智能体的表现？如果能，这种迁移是如何发生的？

### 2. 方法论
*   **核心思想**：构建一个跨越多个异构编程领域的统一记忆池，在面对新任务时，检索并利用来自其他领域的经验。
*   **关键技术细节**：
    *   **四种记忆表示**：为了研究抽象程度的影响，设计了四种格式：
        1.  **Trajectory (追踪)**：原始的命令和执行结果（低抽象）。
        2.  **Workflow (工作流)**：从追踪中提取的可重用动作序列。
        3.  **Summary (摘要)**：对任务、环境、动作及成败原因的段落式总结。
        4.  **Insight (见解)**：高度抽象、脱离具体任务细节的通用编程原则（高抽象）。
    *   **记忆流程**：
        1.  **离线生成**：在各基准测试上运行智能体，利用 LLM 判别器对成功/失败案例生成上述四种记忆。
        2.  **在线检索**：推理时，根据当前任务的嵌入向量（Embedding）与记忆池进行余弦相似度匹配，检索 Top-N 条记忆放入系统提示词中。

### 3. 实验设计
*   **数据集/基准测试**：涵盖了 6 个具有代表性的编程基准测试：
    *   函数/算法级：`LiveCodeBenchv6`, `Aider Polyglot`。
    *   仓库级软件工程：`SWE-Bench Verified`, `TerminalBench2`。
    *   领域特定：`ReplicationBench` (科学计算), `MLGym-Bench` (机器学习)。
*   **对比方法**：
    *   **Zero-shot**：无记忆基准。
    *   **现有自进化方法**：`ReasoningBank`（仅利用同领域记忆）和 `AgentKB`（利用通用推理记忆）。
*   **测试模型**：主要使用 `gpt-5-mini`，并验证了 `DeepSeek V3.2` 和 `Qwen3-Coder-480B` 的泛化性。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **资源使用**：实验主要基于 API 调用（OpenAI 的 `gpt-5-mini` 和 `text-embedding-3-small`）以及开源模型推理。评估使用了 `harbor` 容器化评估框架。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个基准测试上进行了广泛测试，每个任务采用 Pass@3 或 Pass@1 进行评估。
*   **充分性**：实验设计非常全面，不仅对比了不同记忆格式，还进行了：
    *   **消融实验**：研究记忆池大小、来源领域数量对性能的影响。
    *   **跨模型迁移**：验证 A 模型生成的记忆能否被 B 模型使用。
    *   **定性分析**：通过 LLM 标注和人工案例研究，分析了记忆贡献的类别（如元知识 vs 任务代码）。
*   **客观性**：论文诚实地讨论了“负迁移”现象，并对比了多种检索策略，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **MTL 有效性**：跨领域记忆迁移使平均性能提升了 **3.7%**，证明了异构领域记忆的价值。
*   **元知识是核心**：迁移的收益主要来自**元知识**（如验证流程、环境适应、避坑指南），而非具体的任务代码。
*   **抽象决定迁移性**：**抽象程度越高，迁移效果越好**。`Insight` 格式表现最佳，而低抽象的 `Trajectory` 容易因包含过多特定任务细节而导致智能体“生搬硬套”，引发负迁移。
*   **规模效应**：性能随记忆池规模和来源领域多样性的增加而提升。
*   **跨模型兼容**：记忆可以在不同模型间迁移，尽管使用模型自身生成的记忆效果最佳。

### 7. 优点
*   **视角新颖**：打破了“记忆必须来自同领域”的固有思维，填补了编程智能体跨领域记忆研究的空白。
*   **理论与实证结合**：不仅给出了实验数据，还通过数学建模（附录 C）解释了抽象程度与迁移收益之间的权衡。
*   **设计原则明确**：为未来开发自进化智能体提供了清晰的指导——应优先存储高抽象的元知识。

### 8. 不足与局限
*   **检索瓶颈**：简单的嵌入检索在处理复杂、多步骤的智能体任务时仍显不足，LLM 重排序等高级方法在实验中并未表现出明显优势。
*   **负迁移风险**：在某些特定任务中，错误的记忆引导会导致智能体产生“虚假信心”或误用不相关的技术模式。
*   **动态性缺失**：目前的记忆利用是静态检索，缺乏在任务执行过程中动态更新或精炼记忆的机制。

（完）
