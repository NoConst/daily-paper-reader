---
title: "HIVE: Query, Hypothesize, Verify An LLM Framework for Multimodal Reasoning-Intensive Retrieval"
title_zh: HIVE：查询、假设、验证——一种用于多模态推理密集型检索的大语言模型框架
authors: "Mahmoud Abdalla, Mahmoud SalahEldin Kasem, Mohamed Mahmoud, Mostafa Farouk Senussi, Abdelrahman Abdallah, Hyun-Soo Kang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07220v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 用于多模态推理密集型检索的大语言模型框架
tldr: 针对多模态检索中复杂推理能力不足的问题，本文提出HIVE框架。该框架通过LLM显式整合视觉与文本推理，采用“检索-假设-验证”的迭代流程，包括初始检索、补偿性查询合成、二次检索及重排序。在MM-BRIGHT基准测试中，HIVE显著提升了检索性能，超越了现有最强多模态和纯文本模型，为解决技术领域复杂多模态检索提供了新方案。
selection_source: fresh_fetch
motivation: 现有多模态检索模型在处理需要深度整合图表、截图等视觉信息的复杂推理查询时表现不佳，甚至逊于纯文本模型。
method: 提出HIVE框架，通过LLM分析初始检索结果的视觉与逻辑差距，合成补偿性查询并进行二次检索，最后由LLM进行验证与重排序。
result: "在MM-BRIGHT数据集上达到41.7 nDCG@10的新SOTA，相比最强多模态模型提升了14.1个百分点，在游戏、化学等视觉密集领域表现尤为突出。"
conclusion: HIVE证明了通过LLM介导的视觉假设生成与验证可以有效弥补多模态检索中的推理鸿沟，且具有良好的插件式兼容性。
---

## 摘要
多模态检索模型在处理推理密集型查询时往往表现不佳，这类查询需要将图像（如示意图、图表、屏幕截图）与文本深度整合以识别相关文档。在 MM-BRIGHT 数据集上，表现最好的多模态模型仅达到 27.6 的 nDCG@10，甚至逊于强力的纯文本检索器（32.2）。我们提出了 HIVE（假设驱动的迭代视觉证据检索），这是一种即插即用的框架，通过大语言模型（LLM）将显式的视觉-文本推理注入检索器。HIVE 分四个阶段运行：(1) 在语料库上进行初始检索；(2) 基于 LLM 的补偿性查询合成，显式阐述在 top-k 候选文档中观察到的视觉和逻辑差距；(3) 使用优化后的查询进行二次检索；(4) 对候选文档并集进行 LLM 验证和重排序。在 MM-BRIGHT 的多模态转文本赛道（涵盖 29 个技术领域的 2,803 个真实查询）上的评估结果显示，HIVE 实现了 41.7 的新最先进（SOTA）聚合 nDCG@10，相比最佳纯文本模型（DiVeR: 32.2）提升了 9.5 个点，相比最佳多模态模型（Nomic-Vision: 27.6）提升了 14.1 个点。其中，我们经过推理增强的基础检索器贡献了 33.2，而 HIVE 框架进一步增加了 8.5 个点。在视觉要求较高的领域（如游戏：68.2，化学：42.5，可持续性：49.4），HIVE 的表现尤为强劲。HIVE 兼容标准检索器和推理增强型检索器，证明了由 LLM 介导的视觉假设生成与验证可以显著弥合检索中的多模态推理差距。

## Abstract
Multimodal retrieval models fail on reasoning-intensive queries where images (diagrams, charts, screenshots) must be deeply integrated with text to identify relevant documents -- the best multimodal model achieves only 27.6 nDCG@10 on MM-BRIGHT, underperforming even strong text-only retrievers (32.2). We introduce \textbf{HIVE} (\textbf{H}ypothesis-driven \textbf{I}terative \textbf{V}isual \textbf{E}vidence Retrieval), a plug-and-play framework that injects explicit visual-text reasoning into a retriever via LLMs. HIVE operates in four stages: (1) initial retrieval over the corpus, (2) LLM-based compensatory query synthesis that explicitly articulates visual and logical gaps observed in top-$k$ candidates, (3) secondary retrieval with the refined query, and (4) LLM verification and reranking over the union of candidates. Evaluated on the multimodal-to-text track of MM-BRIGHT (2,803 real-world queries across 29 technical domains), HIVE achieves a new state-of-the-art aggregated nDCG@10 of \textbf{41.7} -- a \textbf{+9.5} point gain over the best text-only model (DiVeR: 32.2) and \textbf{+14.1} over the best multimodal model (Nomic-Vision: 27.6), where our reasoning-enhanced base retriever contributes 33.2 and the HIVE framework adds a further \textbf{+8.5} points -- with particularly strong results in visually demanding domains (Gaming: 68.2, Chemistry: 42.5, Sustainability: 49.4). Compatible with both standard and reasoning-enhanced retrievers, HIVE demonstrates that LLM-mediated visual hypothesis generation and verification can substantially close the multimodal reasoning gap in retrieval. https://github.com/mm-bright/multimodal-reasoning-retrieval

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **HIVE**（Hypothesis-driven Iterative Visual Evidence Retrieval，假设驱动的迭代视觉证据检索）的框架，旨在解决多模态检索中复杂的推理难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多模态检索模型在处理“推理密集型”查询时表现极差。这类查询要求模型深度整合图像（如电路图、统计图表、技术截图）与文本。
*   **研究背景**：在 MM-BRIGHT 等最新基准测试中，最强的多模态模型（nDCG@10 为 27.6）甚至不如纯文本检索模型（32.2）。这表明目前的模型虽然能对齐图文嵌入，但缺乏理解图像隐含逻辑并据此检索相关文档的能力。
*   **整体含义**：论文提出了一种“即插即用”的框架，通过引入大语言模型（LLM）作为显式推理器，弥补视觉信息与文档相关性之间的推理鸿沟。

### 2. 核心方法论
HIVE 采用“查询-假设-验证”的四阶段迭代流程，无需对基础模型进行额外训练：
1.  **初始检索 (Initial Retrieval)**：使用基础检索器根据原始查询（文本+图像描述）从语料库中检索出前 $k_1$ 个候选文档（探测集）。
2.  **补偿性查询合成 (Compensatory Query Synthesis)**：这是核心创新点。LLM 同时分析图像描述和初始检索到的文档，识别出哪些视觉或逻辑信息是现有结果中缺失的，从而生成一个针对性的“补偿性查询” $\hat{q}$。
3.  **二次检索 (Secondary Retrieval)**：使用生成的 $\hat{q}$ 进行第二次检索，获取更广泛的候选集 $D^{(2)}$（通常 $k_2 \gg k_1$）。
4.  **验证与重排序 (Verification and Reranking)**：LLM 对两次检索结果的并集进行逐一推理验证，排除噪声，输出最终的排名列表。

### 3. 实验设计
*   **数据集**：主要在 **MM-BRIGHT** 基准上进行评估。该数据集包含 2,803 个真实世界查询，涵盖 29 个技术领域（如游戏、化学、法律、可持续发展等）。
*   **任务类型**：多模态到文本（Multimodal-to-Text）检索。
*   **对比方法**：
    *   **多模态模型**：CLIP, SigLIP, Jina-CLIP, Nomic Embed Vision, BGE-VL, GME-Qwen2-VL 等。
    *   **纯文本模型**：BM25, E5-Mistral, GTE-Qwen2, 以及当前的 SOTA 模型 DiVeR。

### 4. 资源与算力
*   **训练阶段**：论文训练了一个名为 **HIVE-Retriever** 的基础检索器（基于 Qwen3-Embedding-4B），使用了 **4 张 NVIDIA H100 80GB GPU** 进行分布式训练。
*   **推理阶段**：HIVE 框架本身是推理时增强，不涉及梯度更新。LLM 调用（用于合成查询和验证）使用的是 **GPT-4o**。
*   **算力说明**：文中明确提到了 GPU 型号和数量，但未详细说明具体的训练时长。

### 5. 实验数量与充分性
*   **实验规模**：在 29 个细分领域分别进行了测试，并计算了聚合得分。
*   **消融实验**：非常充分。作者分别验证了“仅补偿性查询”、“仅重排序”以及“完整 HIVE 流程”的效果，并测试了不同探测集大小（$k_1, k_2$）对性能的影响。
*   **兼容性测试**：将 HIVE 框架应用于 5 种不同的基础检索器（从弱到强），均观察到了显著提升，证明了框架的普适性和公平性。

### 6. 主要结论与发现
*   **性能突破**：HIVE 达到了 **41.7 nDCG@10** 的新 SOTA，比之前的最佳多模态模型提升了 **14.1** 个点，比最佳纯文本模型提升了 **9.5** 个点。
*   **推理价值**：实验证明，基础检索器的提升贡献了 33.2 分，而 HIVE 框架通过推理机制额外贡献了 8.5 分。
*   **领域差异**：在视觉信息密集的领域（如游戏、经济、可持续性）提升最显著；但在极其抽象的领域（如量子计算、密码学），由于图像本身信号微弱，提升相对较小。

### 7. 优点
*   **即插即用**：不需要重新训练现有的嵌入模型，可以作为任何检索系统的增强插件。
*   **显式推理**：通过 LLM 合成补偿性查询，模拟了人类专家“根据搜索结果调整搜索词”的行为，极大地提高了检索的查全率。
*   **效果显著**：在极具挑战性的推理密集型基准上取得了大幅度的领先。

### 8. 不足与局限
*   **成本与延迟**：由于在推理过程中需要多次调用 GPT-4o（图像描述、查询合成、重排序），其计算成本和响应延迟会显著高于传统的单次嵌入检索。
*   **依赖图像描述**：框架的效果高度依赖于第一步生成的图像描述（Caption）的质量，如果描述丢失了关键细节，后续推理将受限。
*   **模型偏差**：目前主要依赖闭源模型（GPT-4o），尚未验证使用较小的开源模型是否能达到同等水平的推理能力。

（完）
