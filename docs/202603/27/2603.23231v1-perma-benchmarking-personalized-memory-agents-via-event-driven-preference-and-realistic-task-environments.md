---
title: "PERMA: Benchmarking Personalized Memory Agents via Event-Driven Preference and Realistic Task Environments"
title_zh: PERMA：通过事件驱动偏好和真实任务环境评估个性化记忆智能体
authors: "Shuochen Liu, Junyi Zhu, Long Shu, Junda Lin, Yuhao Chen, Haotian Zhang, Chao Zhang, Derong Xu, Jia Li, Bo Tang, Zhiyu Li, Feiyu Xiong, Enhong Chen, Tong Xu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23231v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 个性化记忆智能体基准测试
tldr: 针对大语言模型在长期记忆和个性化适配方面的不足，本文提出了PERMA基准。不同于以往仅关注简单信息检索的评估，PERMA强调用户偏好在真实噪声环境下的动态演变。该基准通过模拟跨领域、跨会话的交互事件，并引入文本变异性和语言风格对齐，全面考察智能体在时间维度上的角色一致性。实验表明，尽管现有记忆系统有所进步，但在应对长时跨度和跨领域干扰时仍显乏力。
selection_source: fresh_fetch
motivation: 现有评估方法将个性化简化为简单的信息检索，忽视了用户偏好随时间演变及在噪声环境下的动态积累过程。
method: 提出PERMA基准，通过模拟具有文本变异性和语言对齐特征的跨领域、跨会话交互事件，评估模型在动态时间线上的角色一致性。
result: 实验显示先进记忆系统能更精准提取偏好并降低成本，但在处理长跨度时间深度和跨领域干扰时仍面临巨大挑战。
conclusion: PERMA揭示了当前智能体在维护长期连贯人格方面的局限性，强调了开发更鲁棒的个性化记忆管理系统的必要性。
---

## 摘要
为大语言模型赋予长期记忆能力，对于构建能够适应用户不断变化需求的智能体至关重要。然而，先前的评估通常将偏好相关的对话与无关对话交织在一起，将任务简化为“大海捞针”式的检索，而忽略了驱动用户偏好演变的事件之间的关系。这种设置忽略了现实世界个性化的一个基本特征：偏好是在噪声背景下的多次交互中逐渐显现并累积的。为了弥补这一差距，我们推出了 PERMA，这是一个旨在评估随时间推移的人格一致性（而非仅仅是静态偏好召回）的基准测试。此外，我们结合了（1）文本变异性和（2）语言对齐，以模拟现实数据中不稳定的用户输入和个人方言。PERMA 由跨越多个会话和领域的按时间排序的交互事件组成，并随时间插入偏好相关的查询。我们设计了多项选择和交互式任务，以探测模型在交互时间线上对人格的理解。实验表明，通过链接相关的交互，先进的记忆系统可以提取更精确的偏好并减少 Token 消耗，优于传统的原始对话语义检索。尽管如此，它们在跨越时间深度和跨域干扰时仍难以维持连贯的人格，这凸显了智能体中需要更稳健的个性化记忆管理。我们的代码和数据已在 https://github.com/PolarisLiu1/PERMA 开源。

## Abstract
Empowering large language models with long-term memory is crucial for building agents that adapt to users' evolving needs. However, prior evaluations typically interleave preference-related dialogues with irrelevant conversations, reducing the task to needle-in-a-haystack retrieval while ignoring relationships between events that drive the evolution of user preferences. Such settings overlook a fundamental characteristic of real-world personalization: preferences emerge gradually and accumulate across interactions within noisy contexts. To bridge this gap, we introduce PERMA, a benchmark designed to evaluate persona consistency over time beyond static preference recall. Additionally, we incorporate (1) text variability and (2) linguistic alignment to simulate erratic user inputs and individual idiolects in real-world data. PERMA consists of temporally ordered interaction events spanning multiple sessions and domains, with preference-related queries inserted over time. We design both multiple-choice and interactive tasks to probe the model's understanding of persona along the interaction timeline. Experiments demonstrate that by linking related interactions, advanced memory systems can extract more precise preferences and reduce token consumption, outperforming traditional semantic retrieval of raw dialogues. Nevertheless, they still struggle to maintain a coherent persona across temporal depth and cross-domain interference, highlighting the need for more robust personalized memory management in agents. Our code and data are open-sourced at https://github.com/PolarisLiu1/PERMA.

---

## 论文详细总结（自动生成）

### 论文总结：PERMA — 评估个性化记忆智能体的事件驱动型基准测试

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）在构建个性化智能体时，长期记忆能力至关重要。然而，现有的评估基准（如“大海捞针”测试）存在三大局限：
*   **偏好静态化**：将用户偏好视为预定义的静态陈述，忽略了偏好在真实交互中通过反馈逐渐演变和累积的过程。
*   **缺乏上下文噪声**：测试环境过于理想化，未能模拟现实中用户输入的不确定性、意图跳转及个人语言风格。
*   **评估维度单一**：往往只关注最终输出的准确性，而未能将记忆检索质量与生成推理能力解耦，无法衡量记忆系统的效率和鲁棒性。

**PERMA** 的提出旨在通过**事件驱动**的范式，模拟用户偏好的动态演化，并在包含真实噪声的环境中评估智能体维护“人格一致性”（Persona Consistency）的能力。

#### 2. 方法论：核心思想与关键技术
PERMA 采用两阶段生成流程来构建高质量、长时序的对话数据集：
*   **事件驱动的对话重构**：
    *   **时间线生成**：利用 LLM 规划器将用户画像转化为按时间排序的事件流，区分“偏好涌现”（首次出现）和“偏好补充”（后续深化/修改）。
    *   **对话生成与验证**：根据事件目标生成多轮对话，并通过 LLM 验证确保偏好细节隐式或显式地融入对话中。
*   **真实交互模拟（噪声注入）**：
    *   **文本变异性**：注入 5 种人类提示偏差（信息缺失、上下文切换、偏好不一致、多语言、口语化表达）。
    *   **语言风格对齐**：利用 WildChat 真实数据对齐用户查询的语言风格，避免模型利用合成文本的模板化特征。
*   **动态人格建模评估**：
    *   定义了三种评估检查点：**Type 1 (零记忆)**、**Type 2 (即时记忆)**、**Type 3 (干预后记忆)**，以测试模型在不同时间深度下的记忆保持能力。

#### 3. 实验设计
*   **数据集规模**：涵盖 10 个代表性用户画像、20 个不同领域（如旅游、金融、电影等）、808 个事件、2166 个偏好细节，总计约 180 万 Token。
*   **对比方法**：
    *   **独立 LLM**：包括推理模型（MiniMax-M2.5, GLM-5, Kimi-K2.5）和聊天模型（Qwen2.5, Llama3.3, Gemini2.5, GPT-4o-mini）。
    *   **记忆系统/RAG**：基于 GPT-4o-mini 作为底座，对比了 vanilla RAG 以及先进记忆系统（MemOS, Mem0, LightMem, Memobase, EverMemOS, Supermemory）。
*   **评估任务**：
    *   **多选题 (MCQ)**：从任务完成度、偏好一致性、信息置信度三个维度设计选项。
    *   **交互式评估**：使用 LLM 模拟器作为用户，根据模型回复提供反馈，直到任务圆满完成或达到轮次上限。

#### 4. 资源与算力
论文未详细列出具体的 GPU 训练时长或集群规模。实验主要依赖于调用主流 LLM 的 API（如 GPT-4o, Gemini, Qwen 等）进行数据生成、系统推理和评估。对于本地部署的模型（如 EverMemOS, LightMem），提到了使用 MongoDB 和 Milvus 向量数据库进行存储管理。

#### 5. 实验数量与充分性
实验设计非常详尽且具有多维性：
*   **多场景覆盖**：对比了“纯净（Clean）”、“噪声（Noise）”和“风格对齐长文本（Long-context）”三种环境。
*   **多维度指标**：不仅评估准确率（MCQ Acc），还衡量了记忆保真度（BERT-f1, Memory Score）、搜索效率（Token 消耗、检索耗时）以及交互成本（User Token）。
*   **消融与探测**：通过改变检索深度（Top-k）和位置探测（Positional Probing），深入分析了记忆系统的性能边界。
*   **客观性**：引入了人工校验环节，确保合成数据的逻辑严密性和偏好覆盖率。

#### 6. 主要结论与发现
*   **记忆系统的优越性**：相比于 vanilla RAG，先进记忆系统（如 MemOS）能更好地链接相关交互，在降低 50x-300x Token 消耗的同时，提供更精准的偏好提取。
*   **推理能力是核心**：Kimi-K2.5 等强推理模型在处理长上下文和复杂偏好合成时表现最佳。
*   **跨域干扰挑战**：模型在单领域任务中表现稳健，但在需要跨多个领域合成信息的任务中，性能显著下降，且容易受到无关对话的干扰。
*   **噪声的“催化”作用**：有趣的是，适度的噪声有时能促使记忆系统提取更详细的偏好描述，从而在某些指标上反而有所提升。

#### 7. 优点与亮点
*   **范式创新**：从静态的“事实检索”转向动态的“人格状态维护”，更符合真实个性化助手的应用场景。
*   **高度仿真**：通过注入 5 种真实噪声和 WildChat 风格对齐，极大地提高了基准测试的难度和现实意义。
*   **解耦评估**：通过 MCQ 和交互式模拟器的结合，能够区分模型是“检索不到”还是“推理不对”。

#### 8. 不足与局限
*   **模型依赖性**：数据生成和评估高度依赖 GPT-4o 等顶级模型，可能存在模型偏见（Model Bias）。
*   **计算成本**：交互式评估虽然真实，但对于大规模基准测试来说，API 调用成本和时间成本较高。
*   **应用限制**：目前主要关注文本交互，尚未扩展到多模态个性化记忆（如视觉偏好）。

（完）
