---
title: "flexvec: SQL Vector Retrieval with Programmatic Embedding Modulation"
title_zh: flexvec：支持程序化嵌入调制的 SQL 向量检索
authors: Damian Delmas
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22587v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 带嵌入调制的SQL向量检索
tldr: 随着AI智能体成为检索API的主要使用者，flexvec提出了一种将嵌入矩阵和评分数组作为可编程表面的检索内核。通过“程序化嵌入调制（PEM）”，用户可以在查询时对向量进行算术运算。该系统通过SQL接口集成，支持可组合的查询原语。实验表明，在不使用近似索引的情况下，flexvec在桌面CPU上处理百万级数据仅需82毫秒，实现了高效且灵活的向量检索。
selection_source: fresh_fetch
motivation: 旨在为AI智能体提供对检索流水线更深层的控制权，使其能灵活干预检索过程。
method: 引入程序化嵌入调制（PEM）技术，允许在SQL接口中对嵌入矩阵和评分数组进行实时算术操作。
result: 在桌面CPU上，处理24万个数据块的端到端耗时仅19毫秒，百万级数据耗时82毫秒。
conclusion: flexvec证明了通过SQL集成可编程调制操作，可以在不牺牲性能的前提下显著提升向量检索的灵活性。
---

## 摘要
随着 AI 智能体成为检索 API 的主要消费者，有机会向调用者暴露更多的检索流水线。flexvec 是一个检索内核，它将嵌入矩阵和评分数组暴露为可编程表面，允许在选择之前对两者进行算术运算。我们将查询时在该表面上组合操作称为程序化嵌入调制（PEM）。本文描述了一组此类操作，并通过一个支持可组合查询原语的查询物化器将其集成到 SQL 接口中。在包含 240,000 个数据块的生产语料库上，在不使用近似索引的情况下，三种组合调制在台式机 CPU 上端到端执行仅需 19 毫秒。在一百万个数据块的规模下，相同的操作执行时间为 82 毫秒。

## Abstract
As AI agents become the primary consumers of retrieval APIs, there is an opportunity to expose more of the retrieval pipeline to the caller. flexvec is a retrieval kernel that exposes the embedding matrix and score array as a programmable surface, allowing arithmetic operations on both before selection. We refer to composing operations on this surface at query time as Programmatic Embedding Modulation (PEM). This paper describes a set of such operations and integrates them into a SQL interface via a query materializer that facilitates composable query primitives. On a production corpus of 240,000 chunks, three composed modulations execute in 19 ms end-to-end on a desktop CPU without approximate indexing. At one million chunks, the same operations execute in 82 ms.

---

## 论文详细总结（自动生成）

### flexvec: 支持程序化嵌入调制的 SQL 向量检索论文总结

#### 1. 论文的核心问题与整体含义
随着 AI 智能体（AI Agents）逐渐取代人类成为检索 API 的主要使用者，传统的“黑盒式”检索接口（固定端点、特定 DSL、固定结果格式）限制了智能体的灵活性。论文提出了 **flexvec**，一个将嵌入矩阵（Embedding Matrix）和评分数组（Score Array）暴露为可编程表面的检索内核。其核心动机是让 AI 智能体能够利用其擅长的 SQL 语言，在查询时通过算术运算动态干预检索逻辑（如抑制特定主题、增加多样性、引入时间衰减等），实现更精准的上下文获取。

#### 2. 论文提出的方法论
论文引入了**程序化嵌入调制（Programmatic Embedding Modulation, PEM）**技术，通过一个**查询物化器（Query Materializer）**将向量操作集成到标准 SQL 中。

*   **核心思想：三阶段流水线**
    1.  **阶段 1：SQL 预过滤（Pre-filter）**：利用 SQL 子查询根据元数据（如项目、时间、类型）缩小候选集。这解决了传统向量数据库在后过滤时常见的“池枯竭（Pool Starvation）”问题。
    2.  **阶段 2：Numpy 调制（Modulation）**：在内存中对候选矩阵进行暴力点积计算，随后应用一系列可组合的算术调制算子。
    3.  **阶段 3：SQL 组合（Compose）**：将评分后的 Top-K 结果写回临时表，由外部 SQL 进行最终的 Join、分组或重排序。
*   **关键调制算子：**
    *   `suppress:X`：减去与概念 X 的方向相似度（负向查询扩展）。
    *   `decay:N`：根据时间戳进行倒数衰减。
    *   `centroid:ids`：将查询向量移向已知相关文档的质心（Rocchio 反馈）。
    *   `from:A to:B`：沿特定轨迹（如从“原型”到“生产”）增强相似度。
    *   `diverse`：使用 MMR 算法迭代选择，平衡相关性与多样性。

#### 3. 实验设计
*   **数据集/场景：**
    *   **生产语料库**：包含 240,000 个数据块（AI 编码会话历史），涉及 4,000 个会话及丰富的结构化元数据。
    *   **行为验证**：使用 BEIR 基准测试中的四个数据集（SciFact, NFCorpus, SCIDOCS, FiQA）验证调制操作对排名的实际影响。
*   **Benchmark 与对比：**
    *   **性能基准**：测量不同规模（25万到100万数据块）下的端到端延迟。
    *   **行为指标**：使用 RBO（排名偏差重叠）衡量排名变化，ILS（列表内相似度）衡量多样性，以及 nDCG@10 的保留率。
    *   **对比对象**：以标准的暴力余弦相似度检索作为 Baseline。

#### 4. 资源与算力
*   **硬件环境**：台式机 CPU（Intel i9-13900KF, 24 核），32GB RAM。
*   **软件栈**：Python, numpy (OpenBLAS), SQLite。
*   **嵌入模型**：Nomic Embed v1.5（128 维，利用 Matryoshka 截断技术）。
*   **算力说明**：论文强调无需 GPU，完全在 CPU 上通过内存矩阵运算实现高性能检索。文中未提及模型训练时长，因为该系统专注于推理阶段的检索内核。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了从 250K 到 1M 数据规模的扩展性实验。
    *   在 4 个 BEIR 数据集上对 30 个查询进行了行为验证。
    *   执行了 1,840 次代数正确性对比测试，确保调制公式与代码实现一致。
*   **充分性评价**：实验设计较为客观，涵盖了延迟分析、预过滤效果、缩放曲线及具体案例研究。虽然 BEIR 测试的查询样本量（每数据集 30 个）不算巨大，但对于验证“系统行为改变”而非“刷榜”目的而言是充分的。

#### 6. 论文的主要结论与发现
*   **高性能**：在百万级语料库上，包含三种组合调制的端到端检索仅需 **82 毫秒**，证明了在一定规模内，暴力矩阵运算配合 SQL 预过滤优于复杂的近似索引（ANN）。
*   **灵活性**：通过 SQL 物化器，AI 智能体可以像编写普通程序一样组合检索原语，显著提升了处理复杂语义冲突（如区分“文档描述”与“代码实现”）的能力。
*   **预过滤的重要性**：SQL 预过滤不仅降低了计算开销，还彻底消除了向量检索中常见的因元数据过滤导致的召回率损失。

#### 7. 优点
*   **架构优雅**：将向量检索无缝集成进 SQL 生态，无需学习新的 DSL。
*   **可编程性强**：PEM 允许在评分阶段进行细粒度的算术干预，这是目前主流向量数据库（如 Pinecone, Milvus）难以直接提供的。
*   **低门槛**：依赖极简（numpy + SQLite），适合本地部署和边缘计算场景。

#### 8. 不足与局限
*   **扩展性限制**：由于需要将嵌入矩阵完全加载至内存，该方法不适用于超大规模（如十亿级）的 Web 检索。
*   **维度敏感**：实验主要基于 128 维向量，若使用更高维度（如 1536 维）的模型，计算开销将显著增加。
*   **元数据依赖**：预过滤的效率高度依赖于数据的结构化程度，对于纯非结构化语料库，性能优势会减弱。
*   **检索质量评估**：论文侧重于系统架构和延迟，缺乏与最先进重排序模型（Rerankers）在检索准确率上的深度对比。

（完）
