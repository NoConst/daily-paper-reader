---
title: "Escaping the Context Bottleneck: Active Context Curation for LLM Agents via Reinforcement Learning"
title_zh: 摆脱上下文瓶颈：通过强化学习为大语言模型智能体实现主动上下文策展
authors: "Xiaozhe Li, Tianyi Lyu, Yizhao Yang, Liang Shan, Siyi Yang, Ligao Zhang, Zhuoyi Huang, Qingwen Liu, Yang Li"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11462v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: LLM 智能体工作记忆的主动上下文管理
tldr: 针对大语言模型在长程任务中面临的“上下文瓶颈”和“信息迷失”问题，本文提出了一种将上下文管理与任务执行解耦的共生框架。该框架引入轻量级策略模型 ContextCurator，通过强化学习主动修剪环境噪声并保留关键推理锚点。实验表明，该方法在 WebArena 和 DeepSearch 任务中显著提升了成功率并大幅降低了 Token 消耗，7B 规模的模型即可达到与 GPT-4o 相当的上下文管理能力。
selection_source: fresh_fetch
motivation: 解决大语言模型在长程多轮交互中因上下文冗余和噪声累积导致的推理性能下降及“信息迷失”问题。
method: 提出一种解耦架构，利用强化学习训练轻量级 ContextCurator 模型主动精简上下文，仅保留关键推理锚点供执行模型使用。
result: "在 WebArena 和 DeepSearch 任务中，该框架在提升任务成功率的同时，分别减少了 8.8% 和 8 倍的 Token 消耗。"
conclusion: 主动上下文管理为构建高效、可扩展的长程自主智能体提供了一种优于传统全量上下文处理的新范式。
---

## 摘要
大语言模型（LLMs）在处理长程任务时面临挑战，这是由于“上下文瓶颈”和“迷失中间（lost-in-the-middle）”现象，即来自冗长环境的累积噪声会降低多轮交互中的推理能力。为了解决这一问题，我们引入了一种共生框架，将上下文管理与任务执行解耦。我们的架构将一个轻量级、专门化的策略模型 ContextCurator 与一个强大的冻结基础模型 TaskExecutor 配对。通过强化学习训练，ContextCurator 主动降低工作记忆中的信息熵。它在积极剪枝环境噪声的同时，保留推理锚点（reasoning anchors），即对未来推导至关重要的稀疏数据点。在 WebArena 上，我们的框架将 Gemini-3.0-flash 的成功率从 36.4% 提高到 41.2%，同时将 Token 消耗降低了 8.8%（从 47.4K 降至 43.3K）。在 DeepSearch 上，它实现了 57.1% 的成功率（对比 53.9%），同时将 Token 消耗降低了 8 倍。值得注意的是，一个 7B 参数的 ContextCurator 达到了与 GPT-4o 相当的上下文管理性能，为自主长程智能体提供了一种可扩展且计算高效的范式。

## Abstract
Large Language Models (LLMs) struggle with long-horizon tasks due to the "context bottleneck" and the "lost-in-the-middle" phenomenon, where accumulated noise from verbose environments degrades reasoning over multi-turn interactions. To address this issue, we introduce a symbiotic framework that decouples context management from task execution. Our architecture pairs a lightweight, specialized policy model, ContextCurator, with a powerful frozen foundation model, TaskExecutor. Trained via reinforcement learning, ContextCurator actively reduces information entropy in the working memory. It aggressively prunes environmental noise while preserving reasoning anchors, that is, sparse data points that are critical for future deductions. On WebArena, our framework improves the success rate of Gemini-3.0-flash from 36.4% to 41.2% while reducing token consumption by 8.8% (from 47.4K to 43.3K). On DeepSearch, it achieves a 57.1% success rate, compared with 53.9%, while reducing token consumption by a factor of 8. Remarkably, a 7B ContextCurator matches the context management performance of GPT-4o, providing a scalable and computationally efficient paradigm for autonomous long-horizon agents.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）智能体在长程任务中的性能瓶颈提出了创新的解决方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：LLM 智能体在处理长程交互任务（如复杂的网页导航或多步搜索）时，面临**“上下文瓶颈（Context Bottleneck）”**和**“信息迷失（Lost-in-the-Middle）”**现象。
*   **研究背景**：
    *   **高熵环境噪声**：真实环境（如网页 DOM 树）包含超过 90% 的结构噪声（广告、脚本等），直接输入会导致注意力稀释。
    *   **信噪比（SNR）低下**：简单扩大上下文窗口并不能解决推理失败，关键在于如何从海量数据中提取“推理锚点（Reasoning Anchors）”。
    *   **现有方案不足**：被动存储系统（如 RAG）存在检索偏差；单体模型（同时负责记忆和执行）在强化学习训练时存在计算昂贵且目标冲突的问题。

### 2. 方法论：ActiveContext 框架
*   **核心思想**：采用**“共生架构（Symbiotic Architecture）”**，将上下文管理与任务执行解耦。
    *   **ContextCurator（上下文策展人）**：一个轻量级的策略模型（如 7B 参数），负责主动将原始交互日志压缩、提炼为高保真的工作记忆。
    *   **TaskExecutor（任务执行者）**：一个强大的、冻结的基座模型（如 GPT-4o 或 Gemini），负责基于策展后的记忆进行逻辑推理和行动。
*   **关键技术细节**：
    *   **主动信息熵还原**：Curator 不仅仅是摘要，而是通过生成式重写，剔除噪声并保留对未来决策至关重要的“推理锚点”。
    *   **多轮 GRPO（MT-GRPO）**：使用强化学习算法训练 Curator。其奖励信号（Reward）来自于 TaskExecutor 最终任务的成功与否（远端奖励），从而让 Curator 学习哪些信息对执行者是有用的。
    *   **因果依赖链**：Curator 决策 $\rightarrow$ Executor 行动 $\rightarrow$ 环境反馈 $\rightarrow$ 远端奖励。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **WebArena**：模拟真实网页环境（购物、论坛、Gitlab 等），测试智能体处理高噪声 DOM 树的能力。
    *   **DeepSearch**：基于 RAG 的复杂搜索环境，要求智能体在多轮检索中合成答案。
*   **对比方法（Baselines）**：
    *   **No Memory**：仅基于当前观察。
    *   **Full Context**：拼接所有历史记录（基准线）。
    *   **先进记忆架构**：Synapse（轨迹示例）、AWM（工作流记忆）、Mem0、Training-Free GRPO 等。
*   **测试模型**：TaskExecutor 采用了 Gemini-3.0-flash、Gemini-2.5-flash、GPT-4o 和 GPT-4o-mini 等多种闭源模型。

### 4. 资源与算力
*   **算力配置**：使用了 **1 个节点，配备 8 张 NVIDIA A100 GPU (80GB)**。
*   **训练细节**：
    *   对 7B 规模的 ContextCurator 进行全参数微调。
    *   学习率为 1e-6，Batch Size 为 8。
    *   使用了 vLLM 推理加速框架。
*   **训练时长**：文中未明确给出总训练小时数，但提到了针对 WebArena-Lite 和 DeepSearch 的具体 Rollout 采样设置。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两个主流的长程任务 Benchmark。
    *   进行了跨模型的泛化性测试（使用不同的 TaskExecutor）。
    *   **消融实验**：对比了不同规模模型（如 7B vs 70B 或闭源模型）作为 Curator 的效果，以及 RL 训练前后的性能差异。
*   **充分性评价**：实验设计较为充分，通过对比 Token 消耗和成功率（SR）两个维度，客观展示了框架在性能和效率上的双重优势。

### 6. 主要结论与发现
*   **性能提升**：在 WebArena 上，将 Gemini-3.0-flash 的成功率从 36.4% 提升至 **41.2%**。
*   **效率飞跃**：在 DeepSearch 任务中，实现了 **8 倍** 的 Token 消耗削减（从 46.7K 降至 6.6K）。
*   **以小博大**：经过 RL 微调的 **7B 模型** 在上下文管理能力上，可以媲美甚至超越未经针对性训练的 **GPT-4o**。
*   **推理锚点的重要性**：实验证明，主动保留关键数据点（如坐标、特定参数）比保留全量原始数据更能防止推理过程中的“幻觉”。

### 7. 优点
*   **解耦设计**：巧妙地解决了小模型推理弱、大模型微调难的矛盾，让小模型专精于“过滤”，大模型专精于“思考”。
*   **端到端优化**：通过远端奖励信号，解决了“什么信息才是重要的”这一主观定义难题，实现了跨模型的策略对齐。
*   **显著降本**：大幅减少了输入给昂贵 API 的 Token 数量，具有极高的商业应用价值。

### 8. 不足与局限
*   **依赖闭源模型反馈**：Curator 的训练高度依赖于 TaskExecutor 的反馈，如果执行者模型发生变化，Curator 可能需要重新对齐。
*   **训练成本**：虽然推理时省钱，但训练一个专门的 Curator 仍需要 A100 等高性能算力支持。
*   **应用范围限制**：目前主要集中在文本/代码类环境（网页、搜索），对于多模态原生环境（如视频流、实时物理交互）的适用性尚待验证。
*   **潜在的过度剪枝**：在极端复杂的任务中，Curator 可能会误删一些看似无用但后续步骤需要的“冷门”信息。

（完）
