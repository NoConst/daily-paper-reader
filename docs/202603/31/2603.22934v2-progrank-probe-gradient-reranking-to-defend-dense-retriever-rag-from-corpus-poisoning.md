---
title: "ProGRank: Probe-Gradient Reranking to Defend Dense-Retriever RAG from Corpus Poisoning"
title_zh: ProGRank：利用探针梯度重排序防御稠密检索 RAG 中的语料库投毒
authors: "Xiangyu Yin, Yi Qi, Chih-Hong Cheng"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22934v2"
tags: ["query:cir-ml"]
score: 9.0
evidence: 针对密集检索RAG语料库投毒的防御方法
tldr: 针对检索增强生成（RAG）面临的语料库投毒攻击，本文提出了一种名为 ProGRank 的后验、无需训练的检索端防御方法。该方法通过对查询-文档对进行随机扰动，并从检索器的特定参数子集中提取探测梯度，以此计算表示一致性和离散风险，进而在重排序阶段识别并过滤恶意文档。实验证明，ProGRank 在多种数据集和攻击场景下均表现出优异的防御性能，且在鲁棒性与效用之间取得了良好平衡。
selection_source: fresh_fetch
motivation: 现有的 RAG 语料库投毒防御方法往往依赖内容过滤或辅助模型，增加了部署难度且难以有效应对针对密集检索器的对抗性攻击。
method: 通过对查询-文档对施加随机扰动并提取探测梯度，利用表示一致性和离散风险两个不稳定性信号进行重排序防御。
result: 在多个数据集和检索器上的实验表明，ProGRank 在检索阶段和端到端设置下均优于现有防御手段，并能有效对抗自适应攻击。
conclusion: ProGRank 是一种无需训练、不改变文档内容的检索端防御方案，为提升 RAG 系统的安全性提供了一种高效且实用的新思路。
---

## 摘要
检索增强生成（RAG）通过将生成内容锚定在检索到的证据上，提升了大语言模型应用的可靠性，但同时也引入了新的攻击面：语料库投毒。在这种攻击场景下，对手通过注入或编辑段落，使其进入目标查询的前 K 个检索结果，从而影响下游生成。现有的语料库投毒防御手段往往依赖于内容过滤、辅助模型或生成侧推理，这增加了部署难度。我们提出了 ProGRank，一种针对稠密检索 RAG 的事后、无需训练的检索侧防御方法。ProGRank 在轻微的随机扰动下对每个“查询-段落”对进行压力测试，并从检索器的一小部分固定参数子集中提取探针梯度。基于这些信号，它推导出两种不稳定性信号——表示一致性和离散风险，并在重排序步骤中将其与评分门控相结合。ProGRank 保留了原始段落内容，无需重新训练，且在部署的检索器不可用时支持基于代理的变体。在三个数据集、三个稠密检索器骨干网络、代表性语料库投毒攻击以及检索阶段和端到端设置下的广泛实验表明，ProGRank 提供了更强的防御性能以及更优的鲁棒性与效用权衡。此外，它在自适应规避攻击下也保持了竞争力。

## Abstract
Retrieval-Augmented Generation (RAG) improves the reliability of large language model applications by grounding generation in retrieved evidence, but it also introduces a new attack surface: corpus poisoning. In this setting, an adversary injects or edits passages so that they are ranked into the Top-$K$ results for target queries and then affect downstream generation. Existing defences against corpus poisoning often rely on content filtering, auxiliary models, or generator-side reasoning, which can make deployment more difficult. We propose ProGRank, a post hoc, training-free retriever-side defence for dense-retriever RAG. ProGRank stress-tests each query--passage pair under mild randomized perturbations and extracts probe gradients from a small fixed parameter subset of the retriever. From these signals, it derives two instability signals, representational consistency and dispersion risk, and combines them with a score gate in a reranking step. ProGRank preserves the original passage content, requires no retraining, and also supports a surrogate-based variant when the deployed retriever is unavailable. Extensive experiments across three datasets, three dense retriever backbones, representative corpus poisoning attacks, and both retrieval-stage and end-to-end settings show that ProGRank provides stronger defence performance and a favorable robustness--utility trade-off. It also remains competitive under adaptive evasive attacks.

---

## 论文详细总结（自动生成）

以下是对论文《ProGRank: Probe-Gradient Reranking to Defend Dense-Retriever RAG from Corpus Poisoning》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：检索增强生成（RAG）通过引入外部知识提升了 LLM 的可靠性，但也带来了**语料库投毒（Corpus Poisoning）**风险。攻击者通过优化手段注入恶意文档，使其在检索阶段进入 Top-K，从而误导 LLM 生成错误或有害信息。
*   **核心问题**：现有的防御方法要么依赖昂贵的辅助模型（如额外的分类器），要么需要修改文档内容或重新训练检索器，且难以应对针对稠密检索器（Dense Retriever）精心设计的对抗性攻击。
*   **研究动机**：开发一种**事后（Post hoc）、无需训练、不改变文档内容**的检索端防御机制，通过识别投毒文档在模型内部信号上的“不稳定性”来过滤攻击。

### 2. 方法论：ProGRank
ProGRank 的核心思想是利用**随机扰动下的探针梯度（Probe-Gradient）**作为不稳定性信号进行重排序。
*   **随机压力测试**：对每个“查询-文档”对施加多次随机扰动（如 Token Dropout 或 Encoder Dropout）。
*   **探针梯度提取**：在固定检索器参数的情况下，计算检索相似度得分相对于一小部分特定参数（论文中选取第 L 层的 LayerNorm 模块）的梯度。
*   **双重不稳定性信号**：
    1.  **表示一致性（Representational Consistency, Rep）**：衡量多次扰动下梯度方向的一致程度。投毒文档通常依赖脆弱的匹配特征，一致性较低。
    2.  **离散风险（Dispersion Risk, DR）**：通过指数核函数和分位数统计，捕捉梯度偏离均值的程度，识别潜在的尾部风险。
*   **评分门控融合（Score-Gated Fusion）**：设计了一个基于原始得分分布的 Sigmoid 门控函数，确保防御惩罚主要作用于高分候选区（即最有威胁的文档），避免干扰低分区的正常排名。

### 3. 实验设计
*   **数据集**：MS MARCO、Natural Questions (NQ)、HotpotQA。
*   **检索器骨干**：Contriever、DPR、BGE。
*   **攻击方法（Benchmark）**：PoisonedRAG、LIAR-RAG、Joint-GCG（涵盖了直接注入、端到端优化及触发词攻击）。
*   **对比方法**：
    *   **GRADA**：基于图的重排序方法。
    *   **GMTP**：基于梯度掩码 Token 概率的方法。
    *   **RAGuard**：基于异常得分的层级防御框架。
*   **生成器**：Qwen2.5-7B、Llama-3-8B、Mistral-7B。

### 4. 资源与算力
*   **硬件平台**：实验在 **NVIDIA DGX Spark** 服务器上进行。
*   **算力细节**：文中未明确给出总训练时长（因为该方法无需训练），但提供了**推理延迟**数据。ProGRank 的平均处理速度约为 **4.73 秒/查询**，虽然慢于轻量级的 GRADA（0.9s），但比目前最强的防御基准 RAGuard（118s）快了约 25 倍。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个数据集、3 种检索器、3 种主流攻击方式以及 3 个不同的生成器模型，构成了多维度的交叉验证。
*   **消融实验**：对扰动次数 $R$、探针层位置 $L$、扰动类型（Token/Encoder/Mixed）以及评分门控的有效性进行了详尽的消融分析。
*   **自适应攻击测试**：专门设计了针对 ProGRank 的规避攻击实验，验证了防御机制在攻击者知情情况下的鲁棒性。
*   **评价**：实验设计全面、客观，通过检索阶段指标（Hit Rate, Recall）和端到端指标（ASR, Accuracy）双重验证，具有较高的说服力。

### 6. 主要结论与发现
*   **防御效能**：ProGRank 在所有测试场景下均显著降低了投毒文档的 Top-K 暴露率。在端到端生成测试中，其 LLM 判定的攻击成功率（ASR）在多个数据集上降至 **0%**。
*   **鲁棒性权衡**：在大幅提升安全性的同时，ProGRank 较好地保留了原始任务的准确度（Accuracy），在鲁棒性与效用（Utility）之间取得了平衡。
*   **机制发现**：实验证明，优化驱动的投毒文档在参数空间表现出明显高于良性文档的梯度不稳定性，这验证了探针梯度作为防御信号的有效性。

### 7. 优点
*   **部署友好**：无需重新训练模型，不改变原始语料库，适合现有 RAG 系统的快速集成。
*   **轻量化探针**：仅计算一小部分参数的梯度，相比全参数梯度计算极大降低了开销。
*   **自适应能力**：通过评分门控机制，能够精准打击高风险文档而不误伤长尾良性文档。
*   **黑盒支持**：提供了基于代理模型（Surrogate）的变体，即使无法获取部署模型的梯度也能工作。

### 8. 不足与局限
*   **计算开销**：尽管比 RAGuard 快，但多次随机扰动（如 $R=20$）和梯度回传仍显著增加了检索阶段的延迟，对于实时性要求极高的场景可能存在压力。
*   **超参数敏感性**：防御效果依赖于扰动次数、探针层选择等超参数，不同检索器可能需要微调。
*   **任务效用损失**：在某些特定数据集（如 MS MARCO）上，防御机制会导致正常任务的 F1 或 ROUGE 指标出现小幅下降。

（完）
