---
title: Bottleneck Tokens for Unified Multimodal Retrieval
title_zh: 用于统一多模态检索的瓶颈标记
authors: "Siyu Sun, Jing Ren, Zhaohe Liao, Dongxiao Mao, Xiangyuan Ren, Yiyi Zhang, Haohua Zhao, Weixiong Lin, Jiang Shaohua, Liqing Zhang, Yuchao Zheng"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11095v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 使用瓶颈令牌的统一多模态检索
tldr: "本文针对解码器架构的多模态大模型在检索任务中的局限性，提出了瓶颈令牌（BToks）和生成式信息压缩（GIC）方法。BToks作为显式池化机制，解决了传统隐式池化（如<EOS>）信息聚合能力不足的问题；GIC通过特殊的掩码机制强制模型将语义信息压缩至BToks中。该方法在MMEB-V2基准测试中取得了2B规模模型的SOTA性能，显著提升了视频问答等复杂任务的检索效果。"
selection_source: fresh_fetch
motivation: 现有方法依赖隐式池化且缺乏令牌级压缩指导，导致解码器模型难以高效聚合多模态语义信息进行检索。
method: 引入可学习的瓶颈令牌作为显式池化机制，并配合生成式信息压缩目标，通过掩码强制预测信号流经BToks。
result: 在MMEB-V2基准上取得59.0的高分，在2B规模模型中达到SOTA，且在视频问答等语义密集型任务上表现优异。
conclusion: 通过显式瓶颈令牌和生成式压缩任务，能够有效提升多模态大模型在统一检索任务中的表征能力和效率。
---

## 摘要
将仅解码器（decoder-only）多模态大语言模型（MLLMs）适配于统一多模态检索面临两个结构性差距。首先，现有方法依赖于隐式池化，即将标准词表标记（如 <EOS>）的隐藏状态重载为序列级表示，而这种机制并非为信息聚合而设计。其次，对比微调（contrastive fine-tuning）规定了嵌入向量应匹配的对象，但未就如何将信息压缩至其中提供标记级的指导。我们通过两个互补的组件解决了这两个差距。在架构上，我们引入了瓶颈标记（Bottleneck Tokens, BToks），这是一组作为固定容量显式池化机制的可学习标记。在训练方面，我们提出了生成式信息冷凝（Generative Information Condensation）：这是一种结合了冷凝掩码（Condensation Mask）的下一标记预测目标，该掩码切断了从目标标记到查询标记的直接注意力路径。因此，所有预测信号都被迫通过 BToks，从而将生成损失转化为用于语义压缩的密集标记级监督。在推理时，仅需通过单次前向传播处理输入和 BToks，与传统的末尾标记池化（last-token pooling）相比，开销几乎可以忽略不计。在 MMEB-V2（包含 78 个数据集、3 种模态、9 个元任务）上，我们的方法在同等数据条件下，在 2B 规模的方法中达到了最先进水平，总分达到 59.0（比 VLM2Vec-V2 高出 3.6 分），并在语义要求较高的任务上取得了显著提升（例如在 Video-QA 上提升了 12.6 分）。

## Abstract
Adapting decoder-only multimodal large language models (MLLMs) for unified multimodal retrieval faces two structural gaps. First, existing methods rely on implicit pooling, which overloads the hidden state of a standard vocabulary token (e.g., <EOS>) as the sequence-level representation, a mechanism never designed for information aggregation. Second, contrastive fine-tuning specifies what the embedding should match but provides no token-level guidance on how information should be compressed into it. We address both gaps with two complementary components. Architecturally, we introduce Bottleneck Tokens (BToks), a small set of learnable tokens that serve as a fixed-capacity explicit pooling mechanism. For training, we propose Generative Information Condensation: a next-token prediction objective coupled with a Condensation Mask that severs the direct attention path from target tokens to query tokens. All predictive signals are thereby forced through the BToks, converting the generative loss into dense, token-level supervision for semantic compression. At inference time, only the input and BToks are processed in a single forward pass with negligible overhead over conventional last-token pooling. On MMEB-V2 (78 datasets, 3 modalities, 9 meta-tasks), our approach achieves state-of-the-art among 2B-scale methods under comparable data conditions, attaining an Overall score of 59.0 (+3.6 over VLM2Vec-V2) with substantial gains on semantically demanding tasks (e.g., +12.6 on Video-QA).

---

## 论文详细总结（自动生成）

### 论文总结：用于统一多模态检索的瓶颈标记（BToks）

#### 1. 核心问题与整体含义（研究动机和背景）
本文针对将**仅解码器（Decoder-only）架构的多模态大语言模型（MLLM）**转化为统一检索模型时存在的两个核心结构性缺陷：
*   **隐式池化的局限性：** 现有方法通常直接提取序列末尾标记（如 `<EOS>`）的隐藏状态作为全局嵌入向量。然而，`<EOS>` 本质上是词表中的普通标记，并非为信息聚合而设计，难以承载复杂的多模态语义。
*   **缺乏压缩指导：** 传统的对比学习（Contrastive Learning）只告诉模型“哪些样本应该接近”，但没有在标记（Token）级别指导模型“如何将长序列信息压缩进有限维度的嵌入向量中”。
这导致模型在处理语义密集型任务（如视频问答检索、文档检索）时表现不佳。

#### 2. 方法论：核心思想与关键技术
论文提出了 **BToks** 框架，包含两个互补组件：
*   **瓶颈标记（Bottleneck Tokens, BToks）：** 在输入序列末尾添加一小组（默认 $K=4$）可学习的专用标记。这些标记作为显式池化机制，专门负责聚合全序列信息。最终的检索嵌入向量通过对这 $K$ 个标记的隐藏状态进行均值池化获得。
*   **生成式信息冷凝（Generative Information Condensation, GIC）：**
    *   **核心思想：** 利用“下一标记预测（NTP）”任务作为辅助监督信号，强制模型将查询信息压缩进 BToks。
    *   **冷凝掩码（Condensation Mask）：** 这是关键技术细节。在训练时，通过特殊的注意力掩码切断目标文本（Target）直接访问查询输入（Query）的路径。目标文本必须通过 BToks 才能获取查询信息。
    *   **算法流程：** 1. 输入查询 + BToks + 目标文本；2. 应用冷凝掩码；3. 计算对比损失（基于 BToks 嵌入）和 NTP 损失（基于目标文本预测）；4. 联合优化。

#### 3. 实验设计
*   **数据集/场景：** 使用了大规模多模态嵌入基准 **MMEB-V2**，涵盖 78 个数据集、3 种模态（图像、视频、视觉文档）和 9 个元任务（分类、问答、检索、定位等）。
*   **对比方法：**
    *   **同规模（2B 级别）：** VLM2Vec-V1/V2、ColPali、Gme-2.2B。
    *   **大规模参考（7B-8B 级别）：** UniME-V2、CAFe、IFM-TTE、RzenEmbed 等。
*   **基准测试：** 严格遵循 MMEB-V2 的官方评估协议，计算宏平均得分。

#### 4. 资源与算力
*   **硬件：** 使用 NVIDIA **A100-80GB** GPU。
*   **算力消耗：** 
    *   BToks 训练总耗时约为 **695 GPU·小时**。
    *   作为对比，基准模型 VLM2Vec-V2 耗时 504 GPU·小时。BToks 增加的开销主要源于生成式头部的计算。
*   **推理效率：** 推理时仅需单次前向传播，相比末尾池化方案，延迟仅增加约 1.2%（约 0.97ms），吞吐量下降仅 1.2%，几乎可以忽略不计。

#### 5. 实验数量与充分性
实验设计非常充分且客观：
*   **广泛覆盖：** 在 78 个子数据集上进行了全面测试。
*   **消融实验：** 分别验证了 BToks 架构、冷凝掩码、生成式目标对性能的影响，证明了各组件的协同作用。
*   **敏感性分析：** 探讨了瓶颈标记数量 $K$（从 1 到 32）以及 LoRA 秩的大小对结果的影响。
*   **跨模态泛化：** 进行了单模态训练、全模态测试的实验，验证了 BToks 学习到的表征具有模态无关的语义特性。
*   **公平性：** 在与 VLM2Vec-V2 对比时，使用了完全相同的训练数据和基础模型（Qwen2VL-2B）。

#### 6. 主要结论与发现
*   **性能领先：** 在 2B 规模模型中达到 SOTA，总分 59.0，比强基线 VLM2Vec-V2 提升 3.6 分。
*   **复杂任务突破：** 在语义要求极高的任务上提升巨大，如 **Video-QA 检索提升了 12.6 分**，视觉文档检索提升了 4.2 分。
*   **显式池化优势：** 实验证明，专用 BToks 比重用 `<EOS>` 标记能更有效地捕获细粒度对应关系。
*   **零样本迁移：** 仅在图像数据上训练的 BToks 在视觉文档任务上表现出极强的迁移能力（提升 15.88 分）。

#### 7. 优点
*   **架构优雅：** 引入 BToks 的改动极小，不改变 MLLM 的核心架构，易于集成。
*   **训练高效：** 通过冷凝掩码巧妙地将生成任务转化为压缩任务，提供了比对比学习更密集的监督信号。
*   **推理友好：** 几乎没有增加推理成本，适合大规模检索系统部署。
*   **鲁棒性：** 在长序列（视频）和复杂布局（文档）任务中表现出极强的稳定性。

#### 8. 不足与局限
*   **模型规模限制：** 目前仅在 2.2B 规模的模型上进行了验证，尚未在 7B 或更大规模的模型上展示其扩展性。
*   **超参数依赖：** 瓶颈标记的数量 $K$ 需要手动设定（实验发现 $K=4$ 最优），未来可能需要自适应的 $K$ 值。
*   **训练成本：** 虽然推理很快，但训练阶段由于引入了生成任务，计算开销比纯对比学习高出约 38%。
*   **任务覆盖：** 尚未在音频、3D 等其他模态上进行测试。

（完）
