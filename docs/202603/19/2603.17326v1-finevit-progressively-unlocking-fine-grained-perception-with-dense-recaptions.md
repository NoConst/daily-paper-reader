---
title: "FineViT: Progressively Unlocking Fine-Grained Perception with Dense Recaptions"
title_zh: FineViT：通过密集重描述逐步解锁细粒度感知
authors: "Peisen Zhao, Xiaopeng Zhang, Mingxing Xu, Ruoyu Sun, Zewei Du, Dunzheng Wang, Guanghao Zheng, Haohang Xu, Zhibo Zhang, Yuhang Zhang, Yi Ai, Lin Liu, Qi Tian"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17326v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大模型的视觉编码器
tldr: 针对多模态大模型视觉编码器在处理细粒度空间任务时因低分辨率和噪声数据导致的性能瓶颈，本文提出FineViT。该方法通过渐进式训练范式，首先在高分辨率下利用数十亿全局重标注数据建立语义基础，随后利用包含4.5亿高质量局部描述的FineCap-450M数据集进行LLM对齐，显著提升了局部感知能力。FineViT在零样本识别、检索及多模态任务中均达到SOTA水平，为细粒度视觉感知提供了强力基准。
selection_source: fresh_fetch
motivation: 传统的CLIP视觉编码器受限于低分辨率预训练和粗糙的互联网图文对，难以应对需要精细空间信息的视觉任务。
method: 采用渐进式训练策略，先在大规模全局重标注数据上进行高分辨率预训练，再利用4.5亿局部描述数据进行对齐以增强局部感知。
result: FineViT在零样本识别和长上下文检索中表现卓越，且在集成至MLLM时性能优于SigLIP2和Qwen-ViT等主流模型。
conclusion: FineViT通过密集重标注和渐进式训练成功解锁了细粒度视觉感知能力，成为多模态视觉编码器的新基准。
---

## 摘要
尽管多模态大语言模型（MLLMs）取得了飞速进展，但其视觉编码器往往仍是性能瓶颈。传统的基于 CLIP 的编码器在处理密集空间任务时表现不佳，这是由于低分辨率预训练导致的视觉细节丢失，以及对噪声大、粗粒度的网络爬取图文对的依赖。为了克服这些局限性，我们推出了 FineViT，这是一种专门为解锁细粒度感知而设计的新型视觉编码器。通过用密集的重描述（recaptions）取代粗糙的网络数据，我们通过一种渐进式训练范式系统地减轻了信息损失：首先，编码器在数十亿个全局重描述图文对上以高原生分辨率从头开始训练，建立起稳健且细节丰富的语义基础。随后，我们利用精心策划的 FineCap-450M 数据集（包含超过 4.5 亿条高质量局部描述），通过大语言模型（LLM）对齐进一步增强其局部感知能力。广泛的实验验证了该渐进式策略的有效性。FineViT 实现了最先进的零样本识别和检索性能，特别是在长上下文检索方面；当集成到 MLLM 中时，其表现始终优于 SigLIP2 和 Qwen-ViT 等多模态视觉编码器。我们希望 FineViT 能够成为细粒度视觉感知的一个强大的新基准。

## Abstract
While Multimodal Large Language Models (MLLMs) have experienced rapid advancements, their visual encoders frequently remain a performance bottleneck. Conventional CLIP-based encoders struggle with dense spatial tasks due to the loss of visual details caused by low-resolution pretraining and the reliance on noisy, coarse web-crawled image-text pairs. To overcome these limitations, we introduce FineViT, a novel vision encoder specifically designed to unlock fine-grained perception. By replacing coarse web data with dense recaptions, we systematically mitigate information loss through a progressive training paradigm.: first, the encoder is trained from scratch at a high native resolution on billions of global recaptioned image-text pairs, establishing a robust, detail rich semantic foundation. Subsequently, we further enhance its local perception through LLM alignment, utilizing our curated FineCap-450M dataset that comprises over $450$ million high quality local captions. Extensive experiments validate the effectiveness of the progressive strategy. FineViT achieves state-of-the-art zero-shot recognition and retrieval performance, especially in long-context retrieval, and consistently outperforms multimodal visual encoders such as SigLIP2 and Qwen-ViT when integrated into MLLMs. We hope FineViT could serve as a powerful new baseline for fine-grained visual perception.

---

## 论文详细总结（自动生成）

这是一份关于论文《FineViT: Progressively Unlocking Fine-Grained Perception with Dense Recaptions》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文指出，当前的**多模态大语言模型（MLLM）的性能瓶颈在于视觉编码器**。传统的视觉编码器（如基于 CLIP 的 ViT）存在三大缺陷：
*   **信息丢失**：预训练分辨率低（如 224px 或 336px），导致 OCR 和小物体识别所需的细节在输入阶段就被压缩。
*   **数据质量差**：依赖于从网络爬取的噪声大、描述简短的图文对（如 LAION-400M），缺乏细粒度语义。
*   **模态对齐鸿沟**：对比学习（全局对齐）与 LLM 的自回归目标（局部/序列对齐）之间存在训练目标的不一致。
**FineViT 的核心目标**是通过“密集重描述（Dense Recaptions）”和“渐进式训练”来解锁视觉编码器的细粒度感知能力。

### 2. 论文提出的方法论
FineViT 采用了**三阶段渐进式训练范式**，从头开始训练一个拥有 8.6 亿参数的视觉编码器：

*   **核心思想**：通过不断提升分辨率和文本描述的粒度，逐步引导模型从理解全局语义转向掌握局部细节。
*   **关键技术细节**：
    *   **架构设计**：采用 28 层 Transformer，使用 **2D RoPE（旋转位置嵌入）** 以支持原生高分辨率输入，Patch Size 为 14x14。
    *   **Stage I：MIM 初始化**。使用掩码图像建模（Masked Image Modeling），以 DINOv3 作为教师模型进行特征重建，建立基础的空间几何感知。
    *   **Stage II：大规模对比学习**。在 15.6 亿个**全局重描述（Global Recaptions）**图文对上训练。采用 SigLIP 损失函数，分辨率从 336 提升至 448，文本上下文长度从 64 扩展至 256。
    *   **Stage III：LLM 辅助的细粒度对齐**。利用自研的 **FineCap-450M 数据集**，在 1K 分辨率下进行自回归训练。任务包括框到文本（bbox2str）、文本到框（str2bbox）以及 OCR 相关任务。
*   **数据构建**：构建了 FineCap-450M，包含 4.5 亿个高质量局部描述，涵盖自然场景、富文本图像和文档。

### 3. 实验设计
*   **数据集与场景**：
    *   **判别式任务**：ImageNet-1k/v2/R（分类）；COCO、Flickr30k（检索）；DCI、IIW、Urban-1k（长文本检索）。
    *   **生成式任务（MLLM）**：MMBench、MMMU、MathVista（推理）；OCRBench、DocVQA、ChartQA（OCR/图表）；RefCOCO 系列（定位）。
*   **Benchmark**：使用了 VLMEvalKit 标准化框架。
*   **对比方法**：
    *   **视觉编码器**：SigLIP、SigLIP2、Seed-ViT。
    *   **多模态模型**：Qwen3-VL、Intern3.5-VL、Aquila-VL。

### 4. 资源与算力
*   **算力说明**：论文**未明确给出具体的 GPU 型号、数量及总训练时长**。
*   **技术栈**：提到使用了 DeepSpeed ZeRO-2 优化器，采用 BF16 精度训练，使用 AdamW 优化器和余弦学习率调度。
*   **训练规模**：Stage II 处理了约 93 亿个样本（Samples Seen），Stage III 处理了 5 亿个样本，这暗示了极大的算力消耗（通常为数千张昇腾或 H100 级别显卡）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了非常广泛的实验，涵盖了从基础视觉能力到复杂多模态推理的数十个数据集。
*   **消融实验**：
    *   验证了三个训练阶段的增量贡献（Stage I -> II -> III 性能稳步提升）。
    *   对比了视觉骨干网络“冻结”与“非冻结”对性能的影响。
    *   测试了 FineViT 在不同规模 LLM（1.7B 和 8B）下的泛化能力。
*   **客观性**：通过在长文本检索和细粒度定位任务上的大幅领先，证明了其方法的有效性。对比实验均在相同 API 和框架下进行，具有较高的公平性。

### 6. 主要结论与发现
*   **细粒度感知突破**：FineViT 在 OCR 和定位任务上表现卓越，例如在 DocVQA 上比 SigLIP2 提升了 18.52%。
*   **长文本检索优势**：得益于密集的重描述数据，FineViT 在处理复杂、长篇幅的图像描述检索时显著优于 LongCLIP 和 SigLIP2。
*   **MLLM 适配性**：作为视觉插件，FineViT 在集成到不同 LLM 后，其多模态理解能力（尤其是空间感知）一致优于现有的 SOTA 编码器。

### 7. 优点（亮点）
*   **高质量数据集**：FineCap-450M 是目前已知最大的细粒度标注数据集，对社区具有重要价值。
*   **渐进式策略**：系统性地解决了从自监督几何学习到语义对齐，再到细粒度指令遵循的演进过程。
*   **原生高分辨率**：不依赖简单的插值，而是通过从头训练和 2D RoPE 实现了对高分辨率（1K）的真正支持。

### 8. 不足与局限
*   **视频能力缺失**：目前 FineViT 仅限于静态图像处理，尚未扩展到视频时序建模。
*   **算力门槛高**：从头训练（From Scratch）数十亿参数模型并处理百亿级样本，对于普通研究机构而言成本极高，难以复现。
*   **推理开销**：1K 分辨率和 28 层 ViT 虽然带来了精度提升，但在实时推理场景下的计算成本（Token 数量）会显著增加。

（完）
