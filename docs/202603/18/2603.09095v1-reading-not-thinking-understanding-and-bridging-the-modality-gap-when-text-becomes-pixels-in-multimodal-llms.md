---
title: "Reading, Not Thinking: Understanding and Bridging the Modality Gap When Text Becomes Pixels in Multimodal LLMs"
title_zh: 是阅读而非思考：理解并弥合多模态大语言模型中文字转像素时的模态鸿沟
authors: "Kaiser Sun, Xiaochuang Yuan, Hongjun Liu, Chen Zhao, Cheng Zhang, Mark Dredze, Fan Bai"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09095v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 评估多模态大模型（MLLM）中的模态间隙问题
tldr: "本研究系统分析了多模态大模型（MLLM）在处理图像化文本时的“模态差距”。通过对7个模型在多种渲染模式下的评估，发现该差距受任务和渲染方式（如字体、分辨率）显著影响。错误分析显示，图像输入主要引发阅读和格式错误，而非推理能力缺失。为此，研究者提出一种自我蒸馏方法，利用模型自身的纯文本推理轨迹指导图像输入训练，使GSM8K准确率从30.71%大幅提升至92.72%，为增强视觉文本理解提供了有效路径。"
selection_source: fresh_fetch
motivation: 旨在诊断并解决多模态大模型在处理以像素形式呈现的文本时，性能显著低于处理纯文本标记的问题。
method: 系统评估了多种模型在不同渲染条件下的表现，并提出一种基于纯文本推理轨迹的自我蒸馏训练方案。
result: 发现图像模式主要放大阅读错误而非推理错误，且新方法在GSM8K等任务上实现了性能的跨越式提升。
conclusion: 模态差距主要源于视觉感知层面的阅读障碍，通过自我蒸馏可以有效弥合这一差距并保持模型的通用能力。
---

## 摘要
多模态大语言模型（MLLMs）能够处理以图像形式呈现的文本，但其表现往往逊于以文本标记（tokens）形式提供相同内容时的表现。我们通过在五种输入模式下的七个基准测试中评估七种 MLLM，系统地诊断了这种“模态鸿沟”（modality gap），涵盖了从合成渲染文本到来自 arXiv PDF 和维基百科页面的真实文档图像。我们发现模态鸿沟具有任务和数据依赖性。例如，数学任务在合成渲染上的表现下降了超过 60 分，而自然文档图像的表现往往与文本模式持平甚至更高。字体和分辨率等渲染选择是强干扰因素，仅字体一项就能使准确率波动高达 47 个百分点。为了理解这一点，我们对 4,000 多个样本进行了扎根理论（grounded-theory）错误分析，揭示了图像模式会选择性地放大阅读错误（计算和格式失败），而知识和推理错误基本保持不变，且部分模型在视觉输入下会出现思维链（CoT）推理崩溃。受这些发现的启发，我们提出了一种自蒸馏方法，利用模型自身的纯文本推理轨迹与图像输入配对进行训练，将 GSM8K 上的图像模式准确率从 30.71% 提升至 92.72%，并能迁移到未见过的基准测试中且不发生灾难性遗忘。总之，我们的研究提供了对模态鸿沟的系统性理解，并为提高多模态语言模型中的视觉文本理解能力提供了一条切实可行的路径。

## Abstract
Multimodal large language models (MLLMs) can process text presented as images, yet they often perform worse than when the same content is provided as textual tokens. We systematically diagnose this "modality gap" by evaluating seven MLLMs across seven benchmarks in five input modes, spanning both synthetically rendered text and realistic document images from arXiv PDFs to Wikipedia pages. We find that the modality gap is task- and data-dependent. For example, math tasks degrade by over 60 points on synthetic renderings, while natural document images often match or exceed text-mode performance. Rendering choices such as font and resolution are strong confounds, with font alone swinging accuracy by up to 47 percentage points. To understand this, we conduct a grounded-theory error analysis of over 4,000 examples, revealing that image mode selectively amplifies reading errors (calculation and formatting failures) while leaving knowledge and reasoning errors largely unchanged, and that some models exhibit a chain-of-thought reasoning collapse under visual input. Motivated by these findings, we propose a self-distillation method that trains the model on its own pure text reasoning traces paired with image inputs, raising image-mode accuracy on GSM8K from 30.71% to 92.72% and transferring to unseen benchmarks without catastrophic forgetting. Overall, our study provides a systematic understanding of the modality gap and suggests a practical path toward improving visual text understanding in multimodal language models.

---

## 论文详细总结（自动生成）

### 论文总结：是阅读而非思考（Reading, Not Thinking）

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）虽然具备处理图像中文字的能力，但在面对相同内容的“像素化文本”（图像）与“标记化文本”（Tokens）时，表现出显著的性能差异，即**“模态鸿沟”（Modality Gap）**。
*   **研究动机**：人类通过视觉阅读文字，而 MLLM 将视觉和文本通过分离的路径处理。现有研究多关注于记录这种差距或构建新系统，但缺乏对差距根源的系统性诊断。
*   **核心问题**：这种性能下降是因为模型“不会读”（感知缺陷）还是“不会想”（推理能力丧失）？

#### 2. 方法论：核心思想与技术细节
论文通过“诊断-分析-修复”的流程展开：
*   **五种输入模式诊断**：
    1.  **Pure Text**：基准参考。
    2.  **Pure Image**：仅提供渲染后的文本图像。
    3.  **Instr.+Image**：文本指令 + 图像内容。
    4.  **OCR-1P (One Pass)**：指令要求模型先提取文字再解决问题。
    5.  **OCR-2P (Two Passes)**：先由模型提取文字，再将提取出的文字作为纯文本输入。
*   **扎根理论错误分析**：利用 GPT-5.2 辅助，对 4,000 多个错误样本进行归纳式编码，构建错误分类体系。
*   **自我蒸馏（Self-Distillation）**：
    *   **核心思想**：让模型“向自己学习”。利用模型在纯文本模式下生成的正确思维链（CoT）推理轨迹作为监督信号，训练模型在面对图像输入时产生相同的推理过程。
    *   **技术细节**：使用 LoRA 微调，对比了仅微调视觉编码器（ViT-only）、仅微调语言模型（LM-only）以及两者同时微调（ViT+LM）的效果。

#### 3. 实验设计
*   **数据集**：
    *   **合成图像**：MMLU（知识）、ARC（科学）、GPQA（专家级问答）、GSM8K（数学）、HumanEval（代码）。
    *   **自然图像**：QASPER（arXiv PDF 页面）、SQuAD v2（维基百科截图）。
*   **评估模型**：Qwen2.5-VL (7B/32B), Qwen3-VL-8B, InternVL3/3.5, Pixtral-12B, GPT-5.2。
*   **对比维度**：渲染参数（字体、分辨率、颜色反转）、输入模态、OCR 准确率与任务准确率的相关性。

#### 4. 资源与算力
*   **微调配置**：使用 LoRA (rank=64)，学习率 2e-4，训练 2 个 epoch，有效 Batch Size 为 16。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长，但考虑到模型规模（最高 32B）和 LoRA 的使用，属于中等规模的微调实验。

#### 5. 实验数量与充分性
*   **实验规模**：评估了 7 个主流模型在 7 个基准测试上的表现，涵盖了从合成到真实的多种场景。
*   **分析深度**：手动/AI 协同标注了超过 4,000 个错误，并进行了详尽的消融实验（如分辨率、字体对性能的影响）。
*   **客观性**：通过引入 OCR-2P 模式，成功分离了“感知”与“推理”变量，实验设计逻辑严密，对比公平。

#### 6. 主要结论与发现
*   **模态鸿沟是“阅读”问题而非“思考”问题**：图像模式显著增加了计算错误和格式错误（感知层面），但对知识检索和逻辑推理错误的影响较小。
*   **思维链（CoT）崩溃**：在视觉输入下，模型倾向于直接给出简短答案，跳过推理步骤，导致准确率大幅下降。
*   **渲染敏感性**：字体选择可导致准确率波动高达 47%；模型对自然文档图像的适应性优于合成渲染图像。
*   **自我蒸馏极其有效**：仅通过 LM-only 的 LoRA 微调，GSM8K 的图像模式准确率从 30.71% 飙升至 92.72%，且不会导致其他任务的灾难性遗忘。

#### 7. 优点与亮点
*   **诊断深入**：不仅发现了差距，还通过错误分类学定性地解释了“为什么失败”。
*   **现实意义强**：引入了真实的 PDF 和网页截图，揭示了合成数据评估的局限性。
*   **方案简洁高效**：自我蒸馏方法不需要额外的标注数据，仅靠模型自身能力即可大幅弥合模态鸿沟。

#### 8. 不足与局限
*   **语言覆盖**：实验主要集中在英文文本，未探讨多语言或复杂排版（如多栏、艺术字）下的表现。
*   **算力细节缺失**：未提供具体的训练资源消耗，不便于复现成本评估。
*   **OCR 局限**：虽然证明了 OCR 质量不是唯一因素，但对于极低分辨率或极端模糊情况下的感知极限探讨较少。

（完）
