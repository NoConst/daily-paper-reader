---
title: "CLAY: Conditional Visual Similarity Modulation in Vision-Language Embedding Space"
title_zh: CLAY：视觉语言嵌入空间中的条件视觉相似度调制
authors: "Sohwi Lim, Lee Hyoseok, Jungjoon Park, Tae-Hyun Oh"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11539v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 利用视觉语言模型构建文本条件相似性空间进行自适应图像检索
tldr: 人类对视觉相似性的感知具有主观性，但传统检索系统多依赖固定度量。本文提出 CLAY，一种无需训练的自适应相似性计算方法，通过将预训练视觉语言模型（VLM）的嵌入空间重构为文本条件相似性空间，实现了视觉特征提取与文本条件的解耦。该方法支持高效的多条件检索，并配套推出了评估数据集 CLAY-EVAL。实验表明，CLAY 在保持高检索精度的同时，显著提升了计算效率。
selection_source: fresh_fetch
motivation: 针对传统图像检索系统难以根据用户特定兴趣或多重文本条件灵活调整相似性度量的问题。
method: 将预训练 VLM 的嵌入空间重构为文本条件相似性空间，在不改变视觉特征的前提下通过文本引导实现动态相似性计算。
result: 在标准基准和新构建的 CLAY-EVAL 数据集上，该方法在检索准确性和处理速度上均表现出色。
conclusion: CLAY 为实现灵活、高效且无需重新训练的条件视觉检索提供了一种创新的空间调制方案。
---

## 摘要
人类对视觉相似性的感知本质上是自适应且主观的，取决于用户的兴趣和关注点。然而，大多数图像检索系统未能体现这种灵活性，往往依赖于固定的、单一的度量标准，无法同时整合多个条件。为解决这一问题，我们提出了 CLAY，这是一种自适应相似度计算方法，它在无需额外训练的情况下，将预训练视觉语言模型（VLM）的嵌入空间重新构建为文本条件相似度空间。该设计将文本条件处理过程与视觉特征提取分离，从而在视觉嵌入固定的情况下实现高效的多条件检索。我们还构建了一个合成评估数据集 CLAY-EVAL，用于在多种条件检索设置下进行全面评估。在标准数据集和我们提出的数据集上的实验表明，与以往的工作相比，CLAY 实现了较高的检索准确率和显著的计算效率。

## Abstract
Human perception of visual similarity is inherently adaptive and subjective, depending on the users' interests and focus. However, most image retrieval systems fail to reflect this flexibility, relying on a fixed, monolithic metric that cannot incorporate multiple conditions simultaneously. To address this, we propose CLAY, an adaptive similarity computation method that reframes the embedding space of pretrained Vision-Language Models (VLMs) as a text-conditional similarity space without additional training. This design separates the textual conditioning process and visual feature extraction, allowing highly efficient and multi-conditioned retrieval with fixed visual embeddings. We also construct a synthetic evaluation dataset CLAY-EVAL, for comprehensive assessment under diverse conditioned retrieval settings. Experiments on standard datasets and our proposed dataset show that CLAY achieves high retrieval accuracy and notable computational efficiency compared to previous works.

---

## 论文详细总结（自动生成）

### 论文总结：CLAY —— 视觉语言嵌入空间中的条件视觉相似度调制

#### 1. 核心问题与整体含义（研究动机和背景）
人类对视觉相似性的感知是**主观且自适应**的。例如，在检索图像时，用户可能有时关注物体的“种类”，有时关注“颜色”或“动作”。然而，现有的图像检索系统大多依赖固定的、静态的相似度度量标准。
*   **现有挑战**：现有的条件检索方法通常需要针对特定属性进行微调（Training-based），这不仅消耗大量计算资源，且在用户更改检索条件时，往往需要重新计算整个数据库的特征向量，导致推理效率极低。
*   **核心目标**：提出一种**无需训练（Training-free）**的方法，在不改变预训练视觉语言模型（VLM）原始图像嵌入的前提下，根据文本条件动态调制相似度空间。

#### 2. 提出方法论
CLAY 的核心思想是将 VLM 的嵌入空间重构为一个**文本条件相似性空间**，实现视觉特征提取与条件处理的解耦。
*   **核心思想**：利用文本条件构建一个子空间，并将固定的图像特征投影到该子空间中进行相似度计算。
*   **关键技术细节**：
    1.  **文本子空间构建**：利用 LLM 生成与条件相关的多样化提示词（如“一张{颜色}的照片”），通过 VLM 文本编码器获取嵌入。
    2.  **流形感知调制（Manifold-aware）**：考虑到 VLM 嵌入分布在超球面上，CLAY 不直接使用欧氏几何投影，而是通过**对数映射（Logarithm Map）**将嵌入映射到切空间（Tangent Space），再进行奇异值分解（SVD）提取投影矩阵 $P_c$。
    3.  **正交旋转对齐（Orthonormal Rotation）**：为了消除视觉与文本模态间的间隙（Conic Effect），在投影前通过 Householder 变换旋转视觉特征，使其均值与文本特征均值对齐。
    4.  **推理流程**：输入查询图和条件文本 $\rightarrow$ 获取预计算的投影矩阵 $\rightarrow$ 旋转并投影查询图与数据库特征 $\rightarrow$ 计算余弦相似度。

#### 3. 实验设计
*   **数据集**：
    *   **现实世界数据集**：Stanford40（动作）、细粒度分类数据集（OxfordPets, Flowers102, StanfordCars, FGVC-Aircraft 等）。
    *   **合成数据集**：CLEVR4（形状、颜色、纹理、计数）。
    *   **新基准 CLAY-EVAL**：论文自建的合成数据集，包含 7,325 张物体图像和 6,745 张人物图像，具有多标签标注（如年龄、动作、背景、子类别等），专门用于评估多条件检索。
*   **对比方法**：包括基准 VLM（CLIP, SigLIP）、条件检索方法（GeneCIS, FocalLens）、组合图像检索方法（SEARLE, MagicLens）以及多模态大模型（InstructBLIP, Qwen3-VL-Embedding, VLM2Vec）。

#### 4. 资源与算力
*   **训练算力**：该方法是**无需训练（Training-free）**的，因此不需要大规模 GPU 集群进行模型微调。
*   **推理算力**：论文对比了推理延迟（表 4）。在 100 张图像的数据库规模下，CLAY 的后续条件检索耗时极短（约 0.09s-0.12s），远低于需要重新编码特征的方法（如 Qwen3-VL 需要 12s 以上）。
*   **具体硬件**：文中未详细列出具体的 GPU 型号，但强调了其在 CPU/通用硬件上的部署潜力。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10 多个现实世界数据集和 3 个合成数据集，进行了单条件和多条件检索的定量评估。
*   **消融实验**：针对“旋转操作”和“流形建模”进行了消融研究，验证了各组件对提升 mAP 的贡献。
*   **可视化分析**：通过 t-SNE 降维展示了调制前后特征空间的变化，直观证明了条件聚类的有效性。
*   **客观性**：对比了对称与非对称两种相似度计算架构，证明了 CLAY 在性能与效率之间的平衡点（Pareto-front）上优于现有 SOTA 方法。

#### 6. 主要结论与发现
*   **高效性**：CLAY 实现了“对称式”检索的精度，却只需“非对称式”检索的计算成本，无需在条件改变时重新扫描数据库。
*   **多条件支持**：CLAY 能够自然扩展到多个条件叠加的场景，而以往方法大多仅支持单一条件。
*   **性能领先**：在多个基准测试中，CLAY 的检索准确率（mAP）显著超过了经过微调的专用模型和参数量更大的多模态大模型。
*   **流形重要性**：实验证明，考虑嵌入空间的超球面几何特性（切空间投影）比简单的欧氏空间投影更有效。

#### 7. 优点
*   **即插即用**：无需任何参数更新，可直接应用于现有的预训练 VLM（如 CLIP, SigLIP）。
*   **计算友好**：极大地降低了大规模数据库在动态条件下的检索延迟。
*   **灵活性高**：支持开放域的文本条件，且能处理复杂的多条件组合。
*   **理论严谨**：引入流形几何和切空间近似，解决了 VLM 嵌入空间的非欧氏特性问题。

#### 8. 不足与局限
*   **依赖基础模型**：检索性能受限于底层 VLM 的表征能力。如果原始 VLM 未能捕捉到某些细微属性（如极小物体），CLAY 也无法通过空间调制找回这些信息。
*   **LLM 依赖**：构建投影矩阵需要 LLM 生成提示词，这在离线或受限环境下可能增加预处理步骤。
*   **属性覆盖**：目前主要针对图像中的核心属性（Core Attributes），对于极其复杂的场景理解或多目标交互的精细调制仍有提升空间。

（完）
