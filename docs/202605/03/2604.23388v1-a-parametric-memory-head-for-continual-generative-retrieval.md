---
title: A Parametric Memory Head for Continual Generative Retrieval
title_zh: 用于持续生成式检索的参数化记忆头
authors: "Kidist Amde Mekonnen, Yubao Tang, Maarten de Rijke"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23388v1"
tags: ["query:cir-ml"]
score: 7.5
evidence: 生成式信息检索与用于文档检索的参数化记忆微调
tldr: 生成式检索（GenIR）将索引参数化，但在处理动态文档集时面临灾难性遗忘。本文提出后自适应记忆微调（PAMT），通过引入参数化记忆头（PMH）在不改变主干模型的情况下，利用乘积键记忆提供残差修正。该方法通过稀疏更新记忆值，有效平衡了新文档的学习与旧文档的保留，在多个数据集上显著提升了持续检索性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23388v1/fig-001.webp\", \"caption\": \"Figure 2: Stage 1 vs. Stage 2 across temporal slices. Hit@10 (%) over slices 𝐷0–𝐷5 for NQ and MS MARCO under Expanded and Fixed protocols. Dashed lines denote Stage 1 adaptation before PAMT; solid lines denote Stage 2 after PAMT. The 𝐷0 point corresponds to the initially trained model. Colors indicate method/docid combinations.\", \"page\": 7, \"index\": 1, \"width\": 494, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2604.23388v1/fig-002.webp\", \"caption\": \"Figure 3: Stage 1 vs. Stage 2 aggregate continual-learning metrics. AP, FWTdiag, and signed BWT ± are computed from MRR@10 and reported in percentage points for NQ and MS MARCO under Expanded and Fixed protocols. Hatched bars denote Stage 1 adaptation before PAMT; dotted bars denote Stage 2 after PAMT.\", \"page\": 8, \"index\": 2, \"width\": 1033, \"height\": 562}, {\"url\": \"assets/figures/arxiv/2604.23388v1/fig-003.webp\", \"caption\": \"Figure 1: Post-adaptation memory tuning (PAMT) for continual GenIR. (a) Adapt-then-stabilize pipeline: The parametric memory head (PMH) is attached and co-trained with the backbone onD0 to establish the initial addressing mechanism. At each session 𝑡 ≥1, the backbone is adapted on the new sliceD𝑡 (Stage 1), then frozen and refined via a memory-only stabilization stage (Stage 2). Stage 2 updates a sparse subset of PMH value rows to improve retention on legacy slices D0:𝑡−1 without gradient-based training on legacy relevance supervision. (b) Parametric memory head: During prefix-trie constrained docid decoding, decoder hidden states are projected by 𝑓𝜙 to perform product-key top-𝐾mem retrieval from the key tables (𝐾 (1) , 𝐾 (2) ) and latent value table 𝑉 hid. Retrieved values are aggregated into a latent correction 𝑏hid 𝑘 in decoder hidden space; its effect on token scores is computed via the frozen output embedding matrix over trie-valid tokens 𝐴𝑘 (𝜋) only. (c) Sparse PMH updates: Decoding-time access logs from the current session X𝑡 are contrasted with a historical access sketch to score rows using access frequency and inverse historical frequency (AF×IHF), forming an update set Ω𝑡 . Sparsity is enforced by updating only 𝑉 hid [𝑛] for 𝑛 ∈Ω𝑡 while blocking gradients on protected rows P𝑡 ; the adapted backbone, 𝑓𝜙 , and keys remain fixed during Stage 2, keeping the Stage 2 routing function stable.\", \"page\": 4, \"index\": 3, \"width\": 1051, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2604.23388v1/fig-004.webp\", \"caption\": \"Table 2: RQ4 (Expanded): Comparison to prior approaches. Diagonal entries report per-slice Hit@10 (%) after each session under the Expanded protocol, using constrained decoding over the cumulative corpus D0:𝑡 . AP, FWTdiag, and signed BWT ± are computed from the lower-triangular MRR@10 matrix (Section 5.3). Best values in each column are underlined.\", \"page\": 9, \"index\": 4, \"width\": 1027, \"height\": 613}]"
motivation: 传统的生成式检索模型在动态更新文档库时，由于知识高度参数化，容易出现严重的灾难性遗忘和稳定性-塑性失衡。
method: 提出PAMT方法，在冻结主干模型的基础上增加一个参数化记忆头，通过稀疏更新特定记忆值来修正解码过程中的隐藏状态。
result: 在MS MARCO和Natural Questions数据集上的实验表明，PAMT在保持新文档检索效果的同时，大幅提升了对旧文档的记忆保留能力。
conclusion: 该研究证明了通过模块化记忆扩展可以有效解决生成式检索的持续学习难题，实现了高效且低干扰的动态索引更新。
---

## 摘要
生成式信息检索（GenIR）将检索整合到单个神经模型中，该模型直接从查询中解码文档标识符（docids）。虽然这种“模型即索引”的范式提供了架构上的简洁性，但它并不适用于动态文档集合。与索引易于更新的模块化系统不同，GenIR 的知识以参数形式编码在其权重中；因此，全量微调和参数高效微调等标准适配方法可能会导致灾难性遗忘。我们表明，顺序适配提高了对新添加文档的检索性能，但会显著降低在早期切片上的表现，暴露出明显的稳定性-可塑性权衡。为了解决这个问题，我们提出了适配后记忆微调（PAMT），这是一种仅记忆的稳定阶段，它通过模块化参数化记忆头（PMH）来增强适配后的模型。PAMT 冻结主干网络并附加一个具有固定寻址的乘积键记忆（product-key memory）。在基于前缀树约束的解码过程中，解码器隐藏状态稀疏地查询 PMH，以在隐藏空间中产生残差修正；这些修正通过冻结的输出嵌入矩阵映射为得分调整，且仅在树内有效标记上进行计算。这在保持路由和主干参数固定的同时引导 docid 的生成。为了限制跨切片干扰，PAMT 仅更新固定预算的记忆值，这些值是根据解码时的访问统计数据选择的，优先考虑当前切片频繁激活且在先前会话中很少使用的条目。在 MS MARCO 和 Natural Questions 上进行的顺序、不相交语料库增量实验表明，PAMT 显著提高了对早期切片的保留能力，同时对新添加文档的检索性能影响极小，且每个会话仅修改记忆值的稀疏子集。

## Abstract
Generative information retrieval (GenIR) consolidates retrieval into a single neural model that decodes document identifiers (docids) directly from queries. While this model-as-index paradigm offers architectural simplicity, it is poorly suited to dynamic document collections. Unlike modular systems, where indexes are easily updated, GenIR's knowledge is parametrically encoded in its weights; consequently, standard adaptation methods such as full and parameter-efficient fine-tuning can induce catastrophic forgetting. We show that sequential adaptation improves retrieval on newly added documents but substantially degrades performance on earlier slices, exposing a pronounced stability-plasticity trade-off. To address this, we propose post-adaptation memory tuning (PAMT), a memory-only stabilization stage that augments an adapted model with a modular parametric memory head (PMH). PAMT freezes the backbone and attaches a product-key memory with fixed addressing. During prefix-trie constrained decoding, decoder hidden states sparsely query PMH to produce residual corrections in hidden space; these corrections are mapped to score adjustments via the frozen output embedding matrix, computed only over trie-valid tokens. This guides docid generation while keeping routing and backbone parameters fixed. To limit cross-slice interference, PAMT updates only a fixed budget of memory values selected using decoding-time access statistics, prioritizing entries frequently activated by the current slice and rarely used in prior sessions. Experiments on MS MARCO and Natural Questions under sequential, disjoint corpus increments show that PAMT substantially improves retention on earlier slices with minimal impact on retrieval performance for newly added documents, while modifying only a sparse subset of memory values per session.