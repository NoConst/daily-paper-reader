---
title: Do VLMs Need Vision Transformers? Evaluating State Space Models as Vision Encoders
title_zh: VLM 是否需要 Vision Transformer？评估状态空间模型作为视觉编码器的性能
authors: "Shang-Jui Ray Kuo, Paola Cascante-Bonilla"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19209v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 评估用于多模态大模型架构的SSM视觉编码器
tldr: 本研究探讨了在视觉语言模型（VLM）中，状态空间模型（SSM）作为视觉编码器替代传统 Transformer 的潜力。通过在受控环境下系统评估 SSM 与 ViT 骨干网络，发现 SSM 在视觉问答和定位任务中表现优异，且在较小模型规模下仍具竞争力。研究还揭示了 ImageNet 准确率与 VLM 性能的不一致性，并提出了提升定位稳定性的策略，证明了 SSM 是构建高效 VLM 的强力选择。
selection_source: fresh_fetch
motivation: 探究状态空间模型（SSM）是否能作为视觉语言模型中视觉 Transformer（ViT）的高效替代方案。
method: 在统一的实验条件下，对比评估了 SSM 和 ViT 骨干网络在 VQA 和定位任务中的表现，并分析了密集任务预训练的影响。
result: SSM 骨干网络在多项任务中表现出最强性能，尤其在小规模模型下极具竞争力，同时发现视觉模型在定位任务中存在不稳定性。
conclusion: SSM 是视觉编码器的强力替代者，结合所提的稳定化策略可显著提升 VLM 的鲁棒性与性能。
---

## 摘要
大规模视觉-语言模型（VLM）通常使用冻结的视觉主干网络，其图像特征通过轻量级连接器映射到大语言模型中。虽然基于 Transformer 的编码器是标准的视觉主干，但我们探究状态空间模型（SSM）视觉主干是否可以成为一种强有力的替代方案。我们在受控设置下系统地评估了用于 VLM 的 SSM 视觉主干。在匹配的 ImageNet-1K 初始化下，SSM 主干在视觉问答（VQA）和定位/接地（grounding/localization）方面均实现了最强的综合性能。我们进一步通过检测或分割训练对 SSM 和 ViT 系列主干进行适配，发现密集任务微调通常能提升各系列模型的性能；在适配后，SSM 主干在模型规模显著减小的情况下仍保持竞争力。我们还观察到：(i) 更高的 ImageNet 准确率或更大的主干网络并不一定能可靠地转化为更好的 VLM 性能；(ii) 某些视觉主干在定位任务中表现不稳定。基于这些发现，我们提出了稳定化策略，提高了这两个主干系列的鲁棒性，并强调 SSM 主干是 VLM 中基于 Transformer 的视觉编码器的强力替代方案。

## Abstract
Large vision--language models (VLMs) often use a frozen vision backbone, whose image features are mapped into a large language model through a lightweight connector. While transformer-based encoders are the standard visual backbone, we ask whether state space model (SSM) vision backbones can be a strong alternative. We systematically evaluate SSM vision backbones for VLMs in a controlled setting. Under matched ImageNet-1K initialization, the SSM backbone achieves the strongest overall performance across both VQA and grounding/localization. We further adapt both SSM and ViT-family backbones with detection or segmentation training and find that dense-task tuning generally improves performance across families; after this adaptation, the SSM backbone remains competitive while operating at a substantially smaller model scale. We further observe that (i) higher ImageNet accuracy or larger backbones do not reliably translate into better VLM performance, and (ii) some visual backbones are unstable in localization. Based on these findings, we propose stabilization strategies that improve robustness for both backbone families and highlight SSM backbones as a strong alternative to transformer-based vision encoders in VLMs.

---

## 论文详细总结（自动生成）

### 论文总结：VLM 是否需要 Vision Transformer？评估状态空间模型作为视觉编码器的性能

#### 1. 核心问题与整体含义（研究动机和背景）
当前的大规模视觉-语言模型（VLM）几乎普遍采用基于 Transformer 的视觉编码器（如 ViT）。然而，Transformer 在处理高分辨率图像时计算复杂度较高，且其全局注意力机制在缺乏特定预训练时可能忽略精细的空间结构。
本研究的**核心问题**是：**状态空间模型（SSM，如 Mamba 系列）能否作为 ViT 的强力替代方案，成为 VLM 的视觉编码器？**
研究动机在于利用 SSM 线性复杂度的优势及其在密集预测任务（如检测、分割）中表现出的强大空间归纳偏置，探索其在视觉问答（VQA）和空间定位（Localization）任务中的潜力。

#### 2. 论文提出的方法论
论文采用了**受控骨干网络替换（Controlled Backbone Swaps）**的实验框架，以消除训练策略、数据和连接器设计等混淆变量的影响。
*   **核心思想**：将不同架构（Transformer, SSM, Hybrid）的视觉编码器接入统一的 VLM 框架（类 LLaVA 架构），保持语言模型（Vicuna-7B）和训练数据一致，仅改变视觉编码器及其预训练目标。
*   **关键技术细节**：
    *   **架构组件**：视觉编码器提取特征 $\rightarrow$ 轻量级连接器（MLP）映射 $\rightarrow$ 语言模型生成。
    *   **SSM 骨干选择**：主要使用 **VMamba**，其核心是 2D 选择性扫描（SS2D）机制，通过四个方向的扫描保留 2D 空间结构。
    *   **适配策略**：研究了 ImageNet-1K 监督预训练、检测（COCO）和分割（ADE20K）任务适配对 VLM 性能的影响。
    *   **稳定化策略**：针对发现的“定位崩溃”现象，提出了增加连接器容量（从 2 层 MLP 增至 3 层）和调整输入几何形状（改用正方形输入）的稳定化方法。

#### 3. 实验设计
*   **数据集与 Benchmark**：
    *   **VQA 任务**：VQA-v2, GQA, VizWiz, TextVQA, POPE, TallyQA。
    *   **定位任务**：RefCOCO, RefCOCO+, RefCOCOg, OCID-Ref。
*   **对比方法**：
    *   **Transformer 系列**：ViT, DeiT, ViTDet。
    *   **SSM 系列**：VMamba, Vim。
    *   **混合架构**：MaxViT（卷积+注意力）, MambaVision（Mamba+注意力）。
*   **实验设置**：分为“严格匹配设置”（相同预训练数据、分辨率和 Token 数量）和“密集任务适配设置”。

#### 4. 资源与算力
*   **硬件**：使用了 **4 × NVIDIA H200 GPUs**。
*   **训练细节**：采用完全分片数据并行（FSDP）技术，混合精度（BF16）训练。
*   **时长与规模**：在 665K 条指令微调数据上训练 1 个 epoch，全局 Batch Size 为 128。具体的总训练时长未在文中明确标注，但给出了详细的优化参数（如学习率 $2 \times 10^{-5}$，AdamW 优化器）。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，正文和附录共包含超过 30 个详细的实验结果表。
*   **覆盖面**：涵盖了从模型架构对比、预训练目标分析、特征提取阶段消融、连接器容量实验到输入分辨率/长宽比的影响分析。
*   **公平性与客观性**：通过固定 LLM 和训练脚本，实现了高度受控的“苹果对苹果”对比。此外，还进行了推理效率分析（Latency vs. Memory），确保了评估的全面性。

#### 6. 主要结论与发现
*   **SSM 表现优异**：在相同条件下，VMamba 在定位任务上显著优于 ViT，且在 VQA 任务中保持领先或持平。
*   **指标不一致性**：ImageNet 的分类准确率并不是 VLM 性能的可靠预测指标。大型模型可能在分类任务上过拟合，反而丢失了对 VLM 至关重要的空间细节。
*   **定位崩溃（Localization Collapse）**：发现某些检测预训练的高分辨率模型在 VLM 中会出现定位能力骤降。
*   **稳定化有效性**：通过增加连接器层数和保持输入图像为正方形，可以有效修复上述崩溃现象，显著提升鲁棒性。
*   **空间偏置**：SSM 架构由于其扫描机制，天然比 Transformer 更能保留空间结构信息。

#### 7. 优点
*   **严谨的受控实验**：填补了 VLM 领域对视觉编码器架构（尤其是 SSM）系统评估的空白。
*   **深入的故障诊断**：不仅发现了性能差异，还深入分析了“定位崩溃”的原因，并给出了切实可行的工程建议。
*   **效率与性能并重**：证明了 SSM 在较小参数规模下即可达到甚至超过大型 ViT 的性能，具有极高的算力性价比。

#### 8. 不足与局限
*   **预训练数据限制**：目前 SSM 缺乏像 CLIP 或 SigLIP 那样在大规模互联网级数据上进行对比学习预训练的权重，这限制了其在极大规模 VLM 中的直接应用对比。
*   **LLM 侧单一**：实验主要基于 Vicuna-7B，虽然具有代表性，但未验证在更先进或更小规模 LLM 上的泛化性。
*   **应用范围**：主要关注生成式 VLM，未深入探讨 SSM 在视频 VLM 或纯检索式多模态模型中的表现。

（完）
