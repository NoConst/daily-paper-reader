---
title: Response-Aware User Memory Selection for LLM Personalization
title_zh: 大语言模型个性化中的响应感知用户记忆选择
authors: "Jillian Fisher, Jennifer Neville, Chan Young Park"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14473v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 用于LLM个性化的用户记忆选择
tldr: "针对大语言模型个性化生成中，传统基于语义相似度的记忆检索方法忽略了记忆项对模型输出分布实际影响的问题，本文提出了RUMS方法。该方法基于信息论，通过衡量记忆子集与模型输出之间的互信息，筛选出能显著降低预测不确定性并使预测更精准的记忆项。实验表明，RUMS在选择质量上更接近人类偏好，且在提升生成质量的同时，相比现有方法降低了高达95%的计算成本。"
selection_source: fresh_fetch
motivation: 现有的个性化记忆选择方法主要依赖语义相似度，未能考虑记忆内容如何实际影响模型的响应分布。
method: 提出RUMS方法，利用互信息衡量记忆项对模型输出不确定性的降低程度，从而筛选出最具响应效用的记忆。
result: RUMS在记忆选择上比现有SOTA方法及超大规模模型更符合人类直觉，且显著提升了响应质量并大幅降低了计算开销。
conclusion: 结合响应感知的记忆选择机制比单纯的语义匹配更有效，为高效、精准的LLM个性化提供了新路径。
---

## 摘要
大语言模型（LLM）个性化的一种常用方法是在推理时将用户记忆的子集整合到提示词中，以引导模型的生成。现有方法主要根据用户记忆项与输入查询之间的相似性来选择这些子集，忽略了特征实际上如何影响模型的响应分布。我们提出了响应效用优化记忆选择（RUMS），这是一种通过衡量记忆子集与模型输出之间的互信息来选择用户记忆项的新方法，旨在识别出能够降低响应不确定性并使预测比语义相似性更精准的项。我们证明，这种信息论基础能够实现更具原则性的用户记忆选择，与现有最先进的方法以及规模大 400 倍的模型相比，其选择结果与人类的选择更加一致。此外，我们还表明，与现有方法相比，使用 RUMS 选择的记忆项能带来更好的响应质量，同时计算成本降低了高达 95%。

## Abstract
A common approach to personalization in large language models (LLMs) is to incorporate a subset of the user memory into the prompt at inference time to guide the model's generation. Existing methods select these subsets primarily using similarity between user memory items and input queries, ignoring how features actually affect the model's response distribution. We propose Response-Utility optimization for Memory Selection (RUMS), a novel method that selects user memory items by measuring the mutual information between a subset of memory and the model's outputs, identifying items that reduce response uncertainty and sharpen predictions beyond semantic similarity. We demonstrate that this information-theoretic foundation enables more principled user memory selection that aligns more closely with human selection compared to state-of-the-art methods, and models $400\times$ larger. Additionally, we show that memory items selected using RUMS result in better response quality compared to existing approaches, while having up to $95\%$ reduction in computational cost.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **RUMS**（Response-Utility optimization for Memory Selection）的新方法，旨在解决大语言模型（LLM）个性化过程中的记忆筛选问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在进行 LLM 个性化时，如何从海量的用户记忆（如个人偏好、历史记录）中挑选出最有助于生成高质量响应的子集？
*   **研究动机**：
    *   **现有方法的缺陷**：目前的检索方法（如 RAG）主要依赖“语义相似度”（查询与记忆是否相似），但相似的内容不一定对生成有帮助，甚至可能引入噪声或导致模型分心。
    *   **性能与成本矛盾**：将所有记忆放入提示词（Prompt）会增加计算成本、延迟，并可能降低生成质量。
    *   **核心假设**：衡量记忆项对模型“响应分布”的影响（即能否降低输出的不确定性），比单纯衡量其与“输入查询”的相似性更有效。

### 2. 方法论
RUMS 的核心思想是**将记忆选择视为一个信息获取问题**，通过最大化互信息来识别最具“响应效用”的记忆。
*   **RUMS-Utility（效用函数）**：
    *   利用信息论中的**条件互信息**来衡量记忆子集对响应不确定性的降低程度。
    *   **公式逻辑**：计算“仅给定查询时的响应熵”与“给定查询和记忆子集时的响应熵”之差。熵减越大，说明该记忆对确定答案的贡献越大。
    *   **技术实现**：由于序列熵计算困难，论文采用了**标记级分解（Token-level Decomposition）**和**蒙特卡洛采样（Monte Carlo Sampling）**来近似估计。
*   **RUMS-Models（轻量化模型）**：
    *   直接计算熵减在推理时太慢，因此作者使用 RUMS-Utility 在离线阶段生成高质量标注数据。
    *   训练轻量级的编码器-分类器（基于 **DeBERTa-v3-large**），使其在推理时能快速预测哪些记忆项具有高信息增益。
    *   提供了两种变体：**RUMS-Binary**（独立评估每个记忆项）和 **RUMS-Multi**（联合评估多个项）。

### 3. 实验设计
*   **数据集**：
    *   **PersonaFeedback**（合成数据）：包含明确用户画像的个性化查询。
    *   **FreebaseQA**（非个性化数据）：常识性问答，用于测试模型是否能识别“无需个性化”的场景。
    *   **WildChat**（真实世界数据）：真实的 ChatGPT 交互记录，由人类标注是否需要个性化。
*   **对比方法（Baselines）**：
    *   **语义相似度**：基于 Sentence Transformer 的向量检索。
    *   **SOTA 检索方法**：BM25、ReContriever。
    *   **大模型提示法**：GPT-4（零样本和少样本提示）。
    *   **朴素基准**：None（不加记忆）、All（加入所有记忆）、Random（随机选 5 个）。
*   **评估指标**：与人类标注的对齐度（P/R/F1）、LLM-as-Judge 胜率、推理成本。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA H100 GPU** 和 **1 张 NVIDIA A100 GPU**（80GB 显存）。
*   **训练时长**：RUMS-Models 的监督微调（SFT）在单卡上运行 12 个 epoch 约需 **2 小时**。
*   **软件框架**：Python 3.13, PyTorch, HuggingFace Transformers。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 H1/H1' 阶段分析了 500+ 样本的效用分布。
    *   在 H2 阶段进行了详细的人类对齐实验（60 个样本，每样本 4 名标注员）。
    *   在 H3 阶段进行了大规模生成质量评估（数百个样本，跨越不同数据集和响应模型）。
*   **充分性评价**：实验设计非常全面。不仅对比了多种基线，还进行了**鲁棒性分析**（探讨模型大小、解码方式、采样数对熵计算的影响）和**成本分析**。通过合成数据与真实数据的交叉验证，证明了方法的泛化性。

### 6. 主要结论与发现
*   **响应感知更精准**：RUMS 在识别“何时需要个性化”以及“选择哪些记忆”方面显著优于基于相似度的方法，甚至超过了比它大 400 倍的 GPT-4。
*   **提升生成质量**：使用 RUMS 选择的记忆能显著提升 LLaMA-70B 和 GPT-4 的响应质量，尤其是在减少“过度个性化”和“噪声干扰”方面。
*   **大幅降本增效**：相比于全量输入记忆，RUMS 减少了冗余信息，使个性化相关的计算成本降低了 **95%**。
*   **理论验证**：论文从理论上证明了最小化预测熵等价于最大化期望用户效用。

### 7. 优点与亮点
*   **视角独特**：跳出了传统的“查询-文档”匹配范式，转向“记忆-响应分布”的影响分析。
*   **理论与实践结合**：既有严谨的信息论证明，又通过知识蒸馏手段解决了推理延迟问题，具有很强的工程落地价值。
*   **自适应性**：RUMS 能够自动决定何时“保持沉默”（不调用任何记忆），这在处理混合任务时非常重要。

### 8. 不足与局限
*   **静态假设**：理论证明假设用户偏好是静态的，但在长程对话中，用户意图可能会演变。
*   **初始筛选依赖**：当用户记忆库极其庞大（如数万条）时，RUMS 仍需先依赖一个粗略的检索器（如 GPT-4 或向量检索）将候选集缩小到 50 条左右，无法完全脱离传统检索。
*   **语言限制**：实验主要集中在英文数据上，多语言环境下的表现尚待验证。

（完）
