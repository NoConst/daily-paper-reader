---
title: "Stop Fixating on Prompts: Reasoning Hijacking and Constraint Tightening for Red-Teaming LLM Agents"
title_zh: 摆脱对提示词的执着：针对大语言模型智能体红队测试的推理劫持与约束收紧
authors: "Yanxu Mao, Peipei Liu, Tiehan Cui, Congying Liu, Mingzhe Xing, Datao You"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05549v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 操纵智能体的推理轨迹和记忆检索
tldr: 本研究针对LLM智能体日益严峻的安全威胁，提出了一种名为JailAgent的红队测试框架。该框架摒弃了传统的提示词修改方法，转而通过操纵智能体的推理轨迹和记忆检索来实现攻击。JailAgent包含触发器提取、推理劫持和约束收紧三个阶段，利用实时自适应机制和优化目标函数，在不改变用户原始输入的情况下，成功在多种模型和场景中实现了高效的越狱攻击，揭示了智能体特有的安全漏洞。
selection_source: fresh_fetch
motivation: 现有的红队测试方法主要依赖于修改用户提示词，这种方式缺乏对新数据的适应性，且可能损害智能体的正常功能。
method: 提出JailAgent框架，通过触发器提取、推理劫持和约束收紧三个核心阶段，隐式地操纵智能体的推理路径和记忆检索。
result: 实验证明JailAgent在跨模型和跨场景环境下表现优异，无需修改提示词即可实现高效的攻击。
conclusion: 该研究表明LLM智能体的安全防范不能仅局限于提示词层面，必须关注其内部推理逻辑和记忆机制的潜在风险。
---

## 摘要
随着基于大语言模型（LLM）的智能体在各个领域的广泛应用，其复杂性引入了新的安全威胁。现有的红队测试方法大多依赖于修改用户提示词，这不仅缺乏对新数据的适应性，还可能影响智能体的性能。为了应对这一挑战，本文提出了 JailAgent 框架，该框架完全避免了对用户提示词的修改。具体而言，它通过三个关键阶段隐式地操纵智能体的推理轨迹和记忆检索：触发器提取（Trigger Extraction）、推理劫持（Reasoning Hijacking）和约束收紧（Constraint Tightening）。通过精确的触发器识别、实时自适应机制以及优化的目标函数，JailAgent 在跨模型和跨场景环境中展现出了卓越的性能。

## Abstract
With the widespread application of LLM-based agents across various domains, their complexity has introduced new security threats. Existing red-team methods mostly rely on modifying user prompts, which lack adaptability to new data and may impact the agent's performance. To address the challenge, this paper proposes the JailAgent framework, which completely avoids modifying the user prompt. Specifically, it implicitly manipulates the agent's reasoning trajectory and memory retrieval with three key stages: Trigger Extraction, Reasoning Hijacking, and Constraint Tightening. Through precise trigger identification, real-time adaptive mechanisms, and an optimized objective function, JailAgent demonstrates outstanding performance in cross-model and cross-scenario environments.

---

## 论文详细总结（自动生成）

以下是对论文《Stop Fixating on Prompts: Reasoning Hijacking and Constraint Tightening for Red-Teaming LLM Agents》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：随着基于大语言模型（LLM）的智能体（Agents）在临床决策、视频分析等高价值领域的广泛应用，其安全性变得至关重要。
*   **核心问题**：现有的红队测试（越狱攻击）方法过度依赖于**显式修改用户提示词（Prompt）**。这种方式存在三大缺陷：
    1.  **易被检测**：修改后的提示词往往带有明显的对抗性特征，容易被防御系统拦截。
    2.  **泛化性差**：针对特定模型或数据集设计的提示词难以迁移。
    3.  **性能损耗**：攻击往往会破坏智能体完成正常任务的能力，导致攻击行为不隐蔽。
*   **研究动机**：探索一种**无需修改原始用户输入**，而是通过操纵智能体内部推理轨迹和记忆检索过程来实现越狱的新型攻击框架。

### 2. 方法论：JailAgent 框架
JailAgent 采用三阶段流水线实现“隐式”劫持：

*   **阶段一：触发器提取（Trigger Extraction）**
    *   **核心思想**：识别输入中对模型输出贡献最大的关键语义单元。
    *   **技术细节**：利用 BERT 的 WordPiece 分词与 spaCy 的语法解析进行短语级对齐。通过计算掩码前后的 **Log-probability 变化**和**步进式 KL 散度（Step-wise KL Divergence）**，从粗到细地定位高贡献 Token。
*   **阶段二：推理劫持（Reasoning Hijacking）**
    *   **核心思想**：通过实时自适应的重排序机制（Reranker）引导模型选择错误的推理路径。
    *   **技术细节**：
        1.  **数据工厂**：基于提取的触发器自动合成正负样本对（毒数据 vs 正常数据）。
        2.  **Reranker 训练**：使用冻结的 SentenceTransformer 作为编码器，仅微调一个轻量级的 MLP 评分头，通过成对排序损失（Pairwise Ranking Loss）学习触发器偏差。
        3.  **候选生成与重排**：在推理时生成多个候选答案，由 Reranker 筛选出符合攻击意图的选项。
*   **阶段三：约束收紧（Constraint Tightening）**
    *   **核心思想**：在语义空间中优化触发器的嵌入表示，增强攻击的稳定性。
    *   **联合优化公式**：通过四个损失函数共同约束：
        1.  **Particularity Loss**：使触发器远离正常数据簇。
        2.  **Clustering Loss**：确保触发器内部语义紧凑。
        3.  **Separability Loss**：提高在向量检索中的命中率。
        4.  **Margin Loss**：扩大毒数据与正常数据之间的决策边界。

### 3. 实验设计
*   **实验场景与智能体**：
    *   **VideoAgent**：长视频理解任务（数据集：EgoSchema, NExT-QA）。
    *   **ReAct-UALA**：协同推理与行动任务（数据集：HotpotQA, StrategyQA, MMLU）。
    *   **EHRAgent**：医疗电子病历复杂推理任务（数据集：MIMIC-III, eICU, TREQS）。
*   **模型内核（Backbones）**：涵盖 GPT-3.5-turbo, GPT-4o, GPT-5 (预研版), Llama-3.1-70B, Claude-3.5-haiku, Gemini-3.0-pro, ERNIE-3.5。
*   **对比基线（Baselines）**：
    *   **PAIR**：基于提示词迭代优化的黑盒攻击。
    *   **AgentPoison**：针对 RAG 智能体的知识库投毒攻击。
    *   **BadChain**：在思维链（CoT）中注入恶意推理步骤。
*   **评估指标**：ASR-R（检索成功率）、ASR-L（日志分析成功率）、ASR-H（人工专家验证成功率）、任务准确率（ACC/EM/SR）及任务完成率（CR）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **效率数据**：论文提供了一项“单次成功攻击耗时（TCPS）”的分析。结果显示 JailAgent 在 StrategyQA 任务上仅需 52.29 秒，比 PAIR（342.20 秒）快了约 84.7%，证明了其轻量级微调机制的高效性。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。实验跨越了 **3 种智能体架构、7 种主流 LLM 内核、8 个数据集**，并采用了 **5 种评估指标**。
*   **消融实验**：针对触发器提取的指标（ΔL, KL）、四个约束损失函数、以及数据工厂的规模均做了详细的消融测试。
*   **客观性**：引入了 ASR-H 指标，由至少 5 名人类专家独立审核执行日志并投票决定攻击是否成功，有效弥补了自动化评估可能存在的偏差。

### 6. 主要结论与发现
*   **攻击效能**：JailAgent 在所有模型和场景下均显著优于基线。例如在 GPT-5 上，其 ASR 指标比 AgentPoison 高出约 20% 以上。
*   **隐蔽性与无损性**：JailAgent 对智能体原始性能的影响极小（ACC/CR 指标几乎与未受攻击时持平），而基线方法往往导致智能体任务完成率大幅下降。
*   **防御抗性**：在困惑度过滤器（PPL Filter）和 RA-LLM 等防御机制下，JailAgent 依然保持了较高的攻击成功率，因为它没有破坏提示词的语义流畅度。

### 7. 优点
*   **范式创新**：从“修改输入”转向“操纵推理轨迹”，为智能体安全研究提供了新视角。
*   **实时自适应**：Reranker 能够根据当前 Prompt 动态生成数据并快速微调，具备极强的环境适应能力。
*   **高隐蔽性**：不改变用户原始意图，使得攻击行为极难通过传统的输入审计发现。

### 8. 不足与局限
*   **白盒依赖风险**：触发器提取阶段依赖于影子模型（Shadow Model）的 logprob 访问权限，在完全黑盒（无概率输出）的环境下效果可能受限。
*   **计算延迟**：虽然比基线快，但实时合成数据和微调 Reranker 仍会给大规模实时系统带来额外的推理延迟。
*   **静态环境局限**：实验主要在标准 Benchmark 上完成，对于具有高度动态交互和多智能体协作的复杂现实场景，其鲁棒性仍需进一步验证。

（完）
