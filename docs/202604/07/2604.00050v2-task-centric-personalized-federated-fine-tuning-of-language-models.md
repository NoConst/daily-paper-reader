---
title: Task-Centric Personalized Federated Fine-Tuning of Language Models
title_zh: 以任务为中心的语言模型个性化联邦微调
authors: "Gabriel U. Talasso, Meghdad Kurmanji, Allan M. de Souza, Nicholas D. Lane, Leandro A. Villas"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2604.00050v2"
tags: ["query:cir-ml"]
score: 9.0
evidence: 以任务为中心的语言模型个性化联邦微调
tldr: 本研究针对联邦学习中语言模型微调面临的任务异构性问题，提出了FedRouter框架。传统个性化联邦学习在处理客户端内多任务干扰及未知任务泛化时表现不佳。FedRouter通过适配器（Adapters）和双重聚类机制（本地与全局），将模型个性化从“客户端中心”转向“任务中心”，并引入评估路由机制。实验证明，该方法在处理任务干扰和提升泛化能力方面显著优于现有方案。
selection_source: fresh_fetch
motivation: 现有个性化联邦学习难以应对客户端内部多任务分布的相互干扰，且在面对分布变化或未知任务时泛化能力不足。
method: 提出FedRouter框架，利用适配器结合本地与全局双重聚类机制构建任务专用模型，并通过路由机制将测试样本分配至最佳适配器。
result: "在多任务数据集上，FedRouter在任务干扰场景下性能提升达6.1%，在泛化评估中相对提升高达136%。"
conclusion: 采用以任务为中心的聚类适配器策略，能有效增强联邦语言模型在复杂多任务环境下的鲁棒性与泛化性能。
---

## 摘要
联邦学习 (FL) 已成为在分布式的、包含多样化任务的私有数据集上训练语言模型的一种极具前景的技术。然而，聚合在异构任务上训练的模型往往会降低单个客户端的整体性能。为了解决这个问题，个性化联邦学习 (pFL) 旨在为每个客户端的数据分布创建定制化模型。尽管这些方法提高了本地性能，但它们通常在两个方面缺乏鲁棒性：(i) 泛化性：当客户端必须对未见过的任务进行预测，或面临数据分布变化时；(ii) 客户端内任务干扰：当单个客户端的数据包含多个分布，且在本地训练期间可能相互干扰时。为了应对这两个挑战，我们提出了 FedRouter，这是一种基于聚类的 pFL 方法，它为每个任务而非每个客户端构建专用模型。FedRouter 使用适配器 (adapters) 来实现模型个性化，通过采用两种聚类机制将适配器与特定任务关联起来：一种是本地聚类，将适配器与任务数据样本关联；另一种是全局聚类，将来自不同客户端的相似适配器关联起来，以构建以任务为中心的个性化模型。此外，我们提出了一种评估路由机制，根据创建的聚类将测试样本路由到最佳适配器。在多任务数据集上将我们的方法与现有方法进行对比实验，FedRouter 在这些挑战性场景中表现出强大的韧性，在任务干扰下的相对性能提升高达 6.1%，在泛化性评估下的相对提升高达 136%。

## Abstract
Federated Learning (FL) has emerged as a promising technique for training language models on distributed and private datasets of diverse tasks. However, aggregating models trained on heterogeneous tasks often degrades the overall performance of individual clients. To address this issue, Personalized FL (pFL) aims to create models tailored for each client's data distribution. Although these approaches improve local performance, they usually lack robustness in two aspects: (i) generalization: when clients must make predictions on unseen tasks, or face changes in their data distributions, and (ii) intra-client tasks interference: when a single client's data contains multiple distributions that may interfere with each other during local training. To tackle these two challenges, we propose FedRouter, a clustering-based pFL that builds specialized models for each task rather than for each client. FedRouter uses adapters to personalize models by employing two clustering mechanisms to associate adapters with specific tasks. A local clustering that associate adapters with task data samples and a global one that associates similar adapters from different clients to construct task-centric personalized models. Additionally, we propose an evaluation router mechanism that routes test samples to the best adapter based on the created clusters. Experiments comparing our method with existing approaches across a multitask dataset, FedRouter demonstrate strong resilience in these challenging scenarios performing up to 6.1% relatively better under tasks interference and up to 136% relative improvement under generalization evaluation.

---

## 论文详细总结（自动生成）

### 论文总结：以任务为中心的语言模型个性化联邦微调 (FedRouter)

#### 1. 核心问题与研究动机
在联邦学习（FL）场景下微调大语言模型（LLM）时，数据异构性是主要障碍。现有的个性化联邦学习（pFL）方法通常以“客户端”为中心，为每个客户端生成一个定制模型。然而，这种方法面临两大挑战：
*   **客户端内任务干扰（Intra-client tasks interference）：** 单个客户端的数据可能包含多种不同任务（如摘要、翻译、问答），这些任务的目标函数可能冲突，导致单一适配器难以兼顾。
*   **泛化性不足（Generalization）：** 当客户端在测试阶段遇到训练时未见过的任务或分布变化时，高度个性化的模型性能会剧烈下降。

#### 2. 方法论：FedRouter 核心思想与流程
FedRouter 将个性化视角从“以客户端为中心”转向**“以任务为中心”**，通过适配器（LoRA）和双重聚类机制构建任务专用模型。其核心流程如下：
*   **本地聚类（Local Clustering）：** 客户端利用预训练基座模型提取本地数据的嵌入（Embeddings），使用 K-Means 算法将数据划分为 $n_l$ 个任务簇，并为每个簇训练一个专用的 LoRA 适配器。
*   **全局聚类（Global Clustering）：** 客户端将各任务簇的质心（Centroids）和适配器参数发送至服务器。服务器对所有客户端提交的质心进行二次聚类，识别出跨客户端的相似任务，并对属于同一全局簇的适配器进行加权平均聚合。
*   **评估路由机制（Evaluation Router）：** 
    *   **本地模式：** 测试样本根据与本地质心的距离，路由至最匹配的本地适配器。
    *   **全局模式：** 测试样本可路由至联邦网络中所有的全局任务适配器，从而处理本地未曾见过的任务，增强泛化性。
*   **通信优化：** 采用轮询（Round-robin）机制，每轮仅训练和传输一个适配器，以控制计算和通信开销。

#### 3. 实验设计
*   **数据集：** 选取 FLAN 数据集的子集，涵盖四种任务：QQP（语义等价判定）、WebNLG（结构化文本生成）、Samsum（对话摘要）和 GigaWord（文本摘要）。
*   **实验场景：**
    *   **Single：** 每个客户端仅 1 个任务（无干扰）。
    *   **Dual：** 每个客户端 2 个任务（中度干扰）。
    *   **All：** 每个客户端包含所有 4 个任务（高度干扰）。
*   **基准方法（Benchmarks）：** 
    *   FedIT（标准联邦指令微调）、Local（独立本地训练）。
    *   个性化方法：FedCluster（基于聚类的 FL）、FedSA（选择性聚合）、FedDPA（双个性化适配器）。
*   **评价指标：** ROUGE-1 分数。

#### 4. 资源与算力
*   **硬件：** 使用了 NVIDIA A100 80GB GPU。
*   **模型：** 主要使用 Llama 3.2 (1B)，消融实验中扩展至 3B 和 8B 版本。
*   **超参数：** 联邦训练进行 25 轮，每轮本地训练 10 步，Batch Size 为 16，LoRA Rank 为 8。
*   **框架：** 基于 Flower 联邦学习框架和 OpenFedLLM 代码库实现。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了 5 次独立运行并取平均值（含标准差），确保了结果的统计显著性。
*   **消融与扩展实验：** 
    *   验证了模型规模（1B 到 8B）的可扩展性。
    *   验证了客户端数量（4 到 16 个）的扩展性。
    *   利用轮廓系数（Silhouette Score）分析了聚类数量选择的科学性。
*   **客观性：** 实验涵盖了从无干扰到全干扰的多种异构场景，对比了多种最新的 SOTA 个性化联邦学习算法，实验设计较为全面、客观。

#### 6. 主要结论与发现
*   **抗干扰能力强：** 在任务干扰最严重的“All”场景下，FedRouter 比现有方法性能提升约 6.1%。
*   **泛化性能卓越：** 在面对未见任务的泛化性评估中，FedRouter 相比传统个性化方法实现了高达 136% 的相对性能提升，甚至优于非个性化的 FedIT。
*   **聚类精准：** 实验显示本地聚类准确率极高（Single/Dual 场景 100%，All 场景 95.4%），证明了基于 Embedding 的任务划分是有效的。
*   **扩展性良好：** 性能随模型参数量和客户端数量的增加而稳步提升。

#### 7. 优点与亮点
*   **范式创新：** 突破了“一客户端一模型”的局限，通过“一任务一适配器”解决了多任务干扰问题。
*   **路由灵活性：** 引入的全局路由机制巧妙地解决了个性化模型在面对新分布时“用力过猛”导致的泛化失效问题。
*   **实用性：** 结合了 PEFT（LoRA）和轮询机制，考虑到了实际联邦学习环境中的带宽和计算限制。

#### 8. 不足与局限
*   **聚类依赖性：** 方案高度依赖 K-Means 聚类效果，若不同任务在 Embedding 空间中重叠严重，路由准确性可能会下降。
*   **计算开销：** 虽然有轮询机制，但客户端仍需存储多个适配器和计算 Embedding，对于极度资源受限的设备仍有压力。
*   **任务多样性有限：** 实验仅在 4 种 NLP 任务上进行，尚未在更极端的多任务（如数十种任务）或跨模态任务中验证。
*   **冷启动问题：** 论文未详细讨论新客户端加入且无本地数据进行初始聚类时的处理机制。

（完）
