---
title: Stabilizing Unsupervised Self-Evolution of MLLMs via Continuous Softened Retracing reSampling
title_zh: 通过连续平滑回溯重采样稳定多模态大语言模型的无监督自我进化
authors: "Yunyao Yu, Zhengxian Wu, Zhuohong Chen, Hangrui Xu, Zirui Liao, Xiangwen Deng, Zhifang Liu, Senyuan Shi, Haoqian Wang"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03647v2"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大语言模型的无监督自我演化
tldr: 针对多模态大模型（MLLM）无监督自进化中多数投票法易受模型偏见影响的问题，本文提出CSRS方法。该方法通过回溯重推理机制（RRM）探索长尾推理路径，利用软化频率奖励（SFR）提供连续反馈信号，并结合视觉语义扰动（VSP）强化数学逻辑。实验表明，CSRS在MathVision等基准测试上显著提升了Qwen2.5-VL-7B的推理能力，在几何任务的无监督自进化中达到SOTA水平。
selection_source: fresh_fetch
motivation: 现有的无监督自进化方法依赖多数投票选择伪标签，容易受到模型内在偏见的影响，导致推理路径的客观正确性无法保证。
method: 提出了包含回溯重推理机制（RRM）、软化频率奖励（SFR）和视觉语义扰动（VSP）的连续软化回溯重采样框架。
result: 在MathVision等基准测试中显著提升了Qwen2.5-VL-7B的性能，并在几何任务的无监督自进化中取得了当前最佳结果。
conclusion: CSRS通过优化反馈信号质量和探索深度，有效稳定了MLLM的无监督自进化过程，增强了模型的逻辑推理能力。
---

## 摘要
在多模态大语言模型（MLLMs）的无监督自我进化中，训练后阶段反馈信号的质量对于稳定且有效的学习至关重要。然而，现有的自我进化方法主要依赖多数投票（majority voting）来选择出现频率最高的输出作为伪金标准答案（pseudo-golden answer），这可能源于模型的内在偏置，而无法保证推理路径的客观正确性。为了应对这种性能退化，我们提出了 MLLM 自我进化中的连续平滑回溯重采样（Continuous Softened Retracing reSampling, CSRS）。具体而言，我们引入了一种回溯重推理机制（Retracing Re-inference Mechanism, RRM），使模型能够从锚点开始重新推理，从而扩大对长尾推理路径的探索。同时，我们提出了平滑频率奖励（Softened Frequency Reward, SFR），用连续信号取代二值奖励，并根据采样推理集中答案的频率来校准奖励。此外，结合视觉语义扰动（Visual Semantic Perturbation, VSP），CSRS 确保模型优先考虑数学逻辑而非视觉表象。实验结果表明，CSRS 显著提升了 Qwen2.5-VL-7B 在 MathVision 等基准测试上的推理性能。我们在几何任务的无监督自我进化中取得了当前最先进（SOTA）的结果。我们的代码可在 https://github.com/yyy195/CSRS 获取。

## Abstract
In the unsupervised self-evolution of Multimodal Large Language Models, the quality of feedback signals during post-training is pivotal for stable and effective learning. However, existing self-evolution methods predominantly rely on majority voting to select the most frequent output as the pseudo-golden answer, which may stem from the model's intrinsic biases rather than guaranteeing the objective correctness of the reasoning paths. To counteract the degradation, we propose Continuous Softened Retracing reSampling (CSRS) in MLLM self-evolution. Specifically, we introduce a Retracing Re-inference Mechanism (RRM) that the model re-inferences from anchor points to expand the exploration of long-tail reasoning paths. Simultaneously, we propose Softened Frequency Reward (SFR), which replaces binary rewards with continuous signals, calibrating reward based on the answers' frequency across sampled reasoning sets. Furthermore, incorporated with Visual Semantic Perturbation (VSP), CSRS ensures the model prioritizes mathematical logic over visual superficiality. Experimental results demonstrate that CSRS significantly enhances the reasoning performance of Qwen2.5-VL-7B on benchmarks such as MathVision. We achieve state-of-the-art (SOTA) results in unsupervised self-evolution on geometric tasks. Our code is avaible at https://github.com/yyy195/CSRS.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CSRS（Continuous Softened Retracing reSampling，连续平滑回溯重采样）** 的新框架，旨在解决多模态大语言模型（MLLM）在无监督自我进化过程中的稳定性问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：MLLM 的无监督自我进化（Self-Evolution）通常依赖“多数投票”（Majority Voting）来生成伪标签。
*   **核心问题**：多数投票机制存在严重的**确认偏差**。模型倾向于强化其初始分布中高频但未必正确的路径，导致模型在训练中陷入“自我强化陷阱”，最终引发**模型崩溃（Model Collapse）**，即模型丧失逻辑多样性，退化为确定性的错误映射。
*   **整体含义**：论文旨在通过优化反馈信号的质量，使模型在没有人类标注的情况下，能够自主、稳定地提升复杂的几何和数学推理能力。

### 2. 方法论：核心思想与关键技术
CSRS 框架由三个协同组件组成，旨在打破初始偏见并提供更细粒度的奖励信号：
*   **回溯重推理机制 (RRM)**：
    *   **核心思想**：在初始推理路径（母轨迹）的中间位置设置“回溯锚点”。
    *   **技术细节**：将推理路径在锚点处截断，强迫模型从该逻辑节点重新进行多次局部采样（重推理）。这增加了对“长尾”（低概率但可能正确）推理路径的探索，解决了长程推理中的采样稀疏性问题。
*   **平滑频率奖励 (SFR)**：
    *   **核心思想**：用连续的频率信号取代传统的 0/1 二值奖励。
    *   **技术细节**：计算答案在初始集和重推理集中的频率方差。如果一个答案在局部重推理中频率上升，说明该路径具有潜在的逻辑稳健性。通过 `tanh` 函数平滑处理，动态放大低频正确路径的奖励，同时抑制对高频错误路径的过度拟合。
*   **视觉语义扰动 (VSP)**：
    *   **核心思想**：在重推理阶段对输入图像加入高斯噪声。
    *   **技术细节**：通过扰动低级视觉特征，迫使模型依赖不变的数学逻辑结构而非视觉表象（如“看起来像某个答案”）来完成推理。

### 3. 实验设计
*   **训练数据集**：Geometry3K、GeoQA、MMR1（均为几何/数学相关）。
*   **评估基准 (Benchmarks)**：MathVision、MathVerse、MathVista、We-Math。
*   **对比方法**：
    *   **Base Models**：Qwen2.5-VL-7B 原型。
    *   **SOTA Baseline**：MM-UPT（目前领先的基于多数投票的无监督训练方法）。

### 4. 资源与算力
*   **硬件设备**：使用了 **8 张 NVIDIA A800-80G GPU**。
*   **训练时长**：在上述配置下，针对 Qwen2.5-VL-7B 的训练大约耗时 **36 小时**。
*   **软件框架**：基于 veRL 框架和 GRPO（群体相对策略优化）算法实现。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流数学推理基准上进行了全面测试，并针对 Qwen2.5-VL-7B、InternVL3-8B 和 ThinkLite-VL-7B 等多个模型家族验证了泛化性。
*   **消融实验**：非常充分。分别验证了 RRM、SFR、VSP 三个模块的独立贡献，并对超参数（如回溯率 $\omega$、重采样次数 $m$、奖励系数 $\gamma$ 和 $\beta$）进行了敏感性分析。
*   **客观性**：通过 t-SNE 语义空间可视化和熵值变化曲线，从理论和实证两个维度证明了方法对模型崩溃的抑制作用，实验设计严谨、公平。

### 6. 主要结论与发现
*   **性能提升**：CSRS 在所有基准测试中均显著优于 MM-UPT。在 MathVista 上达到 68.25% 的准确率，相比基准提升了约 2.41%。
*   **抑制崩溃**：理论证明并实验观察到，CSRS 能减缓策略熵的下降速度，保持推理路径的多样性，有效防止了模型在无监督训练中的退化。
*   **逻辑优先**：视觉扰动实验证明，该方法确实增强了模型对数学逻辑的理解，而非仅仅记住了图像特征。

### 7. 优点与亮点
*   **理论深度**：不仅提出了算法，还通过拉格朗日乘数法推导了自进化闭式解，从数学上证明了连续奖励比二值奖励更具稳定性。
*   **机制巧妙**：回溯机制（RRM）模拟了人类做题时“在关键步骤重新思考”的过程，非常符合数学推理的特性。
*   **无监督潜力**：在完全不依赖人类标注的情况下，达到了接近甚至超过部分监督学习模型的水平。

### 8. 不足与局限
*   **任务覆盖面**：目前仅专注于数学和几何推理任务，在通用视觉问答或自然语言理解任务上的有效性尚待验证。
*   **计算开销**：由于引入了重推理机制（每个样本需要生成 $n$ 个母轨迹和 $m \times n$ 个重推理轨迹），推理成本和训练前的采样成本较高。
*   **频率依赖**：SFR 仍然在一定程度上依赖“频率”作为正确性的代理指标，如果模型在某个问题上初始偏见极强（所有采样全错且一致），该方法仍可能失效。

（完）
