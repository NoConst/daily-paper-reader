---
title: Unbiased Dynamic Multimodal Fusion
title_zh: 无偏动态多模态融合
authors: "Shicai Wei, Kaijie Zhang, Luyi Chen, Tao He, Guiduo Duan"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19681v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 无偏动态多模态融合框架
tldr: 针对动态多模态融合中噪声评估不准和模态依赖偏差导致难学模态受双重抑制的问题，本文提出无偏动态多模态学习（UDML）框架。该框架引入噪声感知不确定性估计器，通过预测受控噪声强度实现精准的质量评估，并利用模态丢弃量化固有依赖偏差以优化权重分配。实验证明，UDML在多种基准任务上均优于现有动态和静态融合方法，具有极强的通用性。
selection_source: fresh_fetch
motivation: 现有动态融合方法在极端噪声下质量评估失效，且忽视了模态依赖偏差对难学模态的过度抑制。
method: 提出UDML框架，结合噪声感知不确定性估计器和基于模态丢弃的偏差量化机制，实现更公平、准确的动态加权。
result: 在多个多模态基准任务上的广泛实验验证了该方法在提升性能、通用性和泛化能力方面的有效性。
conclusion: UDML通过解决噪声评估和模态偏差问题，显著提升了动态多模态融合的鲁棒性与公平性。
---

## 摘要
传统的多模态方法通常假设模态质量是静态的，这限制了它们在动态现实场景中的适应性。因此，研究者提出了动态多模态方法来评估模态质量并相应地调整其贡献。然而，这些方法通常依赖于经验指标，在噪声水平极低或极高时无法准确衡量模态质量。此外，现有方法通常假设每个模态的初始贡献是相同的，忽略了内在的模态依赖偏差。结果，难以学习的模态会受到双重惩罚，动态融合的性能可能劣于静态融合。为了应对这些挑战，我们提出了无偏动态多模态学习（UDML）框架。具体而言，我们引入了一种噪声感知不确定性估计器，它向模态数据添加受控噪声，并根据模态特征预测其强度。这迫使模型学习特征损坏与噪声水平之间的清晰对应关系，从而在低噪声和高噪声条件下都能实现准确的不确定性衡量。此外，我们通过模态丢弃（modality dropout）量化了多模态网络中固有的模态依赖偏差，并将其纳入加权机制。这消除了对难学模态的双重抑制效应。在多种多模态基准任务上的广泛实验验证了所提 UDML 的有效性、通用性和泛化能力。代码可在 https://github.com/shicaiwei123/UDML 获取。

## Abstract
Traditional multimodal methods often assume static modality quality, which limits their adaptability in dynamic real-world scenarios. Thus, dynamical multimodal methods are proposed to assess modality quality and adjust their contribution accordingly. However, they typically rely on empirical metrics, failing to measure the modality quality when noise levels are extremely low or high. Moreover, existing methods usually assume that the initial contribution of each modality is the same, neglecting the intrinsic modality dependency bias. As a result, the modality hard to learn would be doubly penalized, and the performance of dynamical fusion could be inferior to that of static fusion. To address these challenges, we propose the Unbiased Dynamic Multimodal Learning (UDML) framework. Specifically, we introduce a noise-aware uncertainty estimator that adds controlled noise to the modality data and predicts its intensity from the modality feature. This forces the model to learn a clear correspondence between feature corruption and noise level, allowing accurate uncertainty measure across both low- and high-noise conditions. Furthermore, we quantify the inherent modality reliance bias within multimodal networks via modality dropout and incorporate it into the weighting mechanism. This eliminates the dual suppression effect on the hard-to-learn modality. Extensive experiments across diverse multimodal benchmark tasks validate the effectiveness, versatility, and generalizability of the proposed UDML. The code is available at https://github.com/shicaiwei123/UDML.

---

## 论文详细总结（自动生成）

这是一份关于论文《Unbiased Dynamic Multimodal Fusion》（无偏动态多模态融合）的结构化深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：在现实场景中，多模态数据的质量是动态变化的（如自动驾驶中，RGB相机在白天强、红外在夜晚强）。现有的动态融合方法旨在评估模态可靠性并调整权重。
*   **核心问题**：
    1.  **不确定性估计偏差**：现有基于经验指标（如能量分数或概率嵌入）的方法在噪声极低或极高时失效，无法精准刻画模态质量。
    2.  **双重抑制效应**：模型天然倾向于依赖“易学”模态。现有方法假设初始贡献相等，导致“难学”模态在面临高不确定性时，既受优化偏差抑制，又受权重下调抑制，性能甚至不如静态融合。
*   **研究目标**：提出一个无偏的动态多模态学习框架（UDML），实现精准的质量评估并消除模态依赖偏差。

### 2. 方法论
UDML 框架由两个核心组件和一个优化策略组成：
*   **噪声感知不确定性估计器 (NUE)**：
    *   **核心思想**：通过主动注入受控噪声并训练模型预测噪声强度，建立特征损坏与噪声水平的明确映射。
    *   **技术细节**：采用概率表示技术，将模态特征映射为高斯分布（均值表示语义，方差表示噪声特性）。估计器从方差中推导噪声强度，从而对未见过的噪声类型具有更强的泛化力。
*   **模态依赖计算器 (MC)**：
    *   **核心思想**：利用“模态丢弃”（Modality Dropout）量化模型对各模态的固有依赖度。
    *   **技术细节**：计算全模态输出与缺失某一模态后输出之间的差异（L1 范数）。差异越大，表示模型对该模态的依赖（α）越高。最终权重由“1/不确定性”与“依赖系数”共同决定，从而补偿难学模态。
*   **渐进式优化策略 (POS)**：
    *   **流程**：分为两个阶段。第一阶段在干净数据上预训练以稳定多模态表示；第二阶段引入受控扰动训练噪声估计器，同时通过梯度阻断防止噪声任务干扰主任务。

### 3. 实验设计
*   **数据集与场景**：
    *   **情感分析**：CMU-MOSI、CMU-MOSEI（三模态：文本、视觉、音频）、MVSA-Single（双模态：图像、文本）。
    *   **情感/动作识别**：CREMA-D、Kinetics-Sounds（双模态：音频、视觉）。
*   **Benchmark 与对比方法**：
    *   对比了静态融合（Late Fusion）、多模态 Transformer（MMBT）、可信多视图分类（TMC）以及最新的 SOTA 动态融合方法（如 QMF、EAU、LDDU）。
    *   设置了不同强度的椒盐噪声和高斯噪声实验，验证鲁棒性。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **超参数信息**：提到了使用 Adam 和 SGD 优化器，学习率分别为 $1 \times 10^{-5}$ 和 $1 \times 10^{-3}$，Batch Size 分别为 16 和 64。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个主流基准数据集上进行了测试，涵盖了分类和回归两种任务类型。
*   **充分性评价**：
    *   **消融实验**：分别验证了 NUE、MC 和 POS 三个组件的必要性，证明了各模块对性能的贡献。
    *   **架构泛化性**：将 UDML 集成到 CNN 架构（MMTM）和 Transformer 架构（mmFormer）中，均取得了显著提升，证明了方法的通用性。
    *   **客观性**：通过可视化权重曲线，直观展示了 UDML 如何在不同噪声水平下调整权重，实验设计较为客观、严谨。

### 6. 主要结论与发现
*   **性能领先**：UDML 在所有数据集上均优于现有 SOTA 方法，尤其在噪声环境下，性能下降幅度远小于对比方法。
*   **解决偏差**：实验证实了“双重抑制”现象的存在，而 UDML 通过引入依赖系数 α，成功纠正了模型对易学模态的过度依赖。
*   **噪声敏感度**：NUE 能够精准识别极低和极高强度的噪声，克服了传统方法在极端情况下的权重分配僵化问题。

### 7. 优点与亮点
*   **架构无关性**：UDML 仅作用于模态表示层，可以轻松插入任何现有的多模态模型中。
*   **理论与直觉结合**：不仅从概率角度解决不确定性，还敏锐捕捉到了深度学习模型在优化过程中的“模态竞争”偏差。
*   **鲁棒性强**：虽然仅用高斯噪声训练，但在椒盐噪声下也表现优异，展现了极佳的跨噪声泛化能力。

### 8. 不足与局限
*   **样本级偏差未覆盖**：作者在文中提到，UDML 主要关注模态级偏差，尚未显式处理样本级偏差（例如长尾分布中的样本可能因数据稀缺而被误判为高噪声）。
*   **计算开销**：虽然模态丢弃不增加参数，但两阶段训练和多次前向传播（计算依赖度时）会增加一定的训练和推理延迟。
*   **噪声类型假设**：虽然具有泛化性，但 NUE 的核心仍依赖于预定义的噪声扰动训练，对于极其复杂的非线性失真可能仍有局限。

（完）
