---
title: "MiMIC: Mitigating Visual Modality Collapse in Universal Multimodal Retrieval While Avoiding Semantic Misalignment"
title_zh: MiMIC：在缓解通用多模态检索中视觉模态崩溃的同时避免语义失配
authors: "Juan Li, Chuanghao Ding, Xujie Zhang, Cam-Tu Nguyen"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21326v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 缓解多模态检索中的视觉模态坍缩
tldr: 本研究针对通用多模态检索（UMR）中存在的视觉模态崩溃和语义失配问题，提出了MiMIC模型。现有方法中，早融合模型易忽略视觉特征，而晚融合模型易导致语义关联内容在嵌入空间中距离过远。MiMIC通过引入解码器融合架构，并结合单模态混入及随机标题丢弃的鲁棒训练策略，有效提升了多模态整合能力。在WebQA+和EVQA+数据集上的实验证明，MiMIC在处理缺乏标题的图像数据时，性能显著优于现有的早融合和晚融合基准模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21326v1/fig-001.webp\", \"caption\": \"Figure 2: Different Fusion Strategies. (a) Late Fusion. Use separate image and text encoder (e.g., UniVL-DR). (b) Early Fusion. Concatenates image and text embeddings, which are then fed into the LM. (e.g., Marvel)\", \"page\": 2, \"index\": 1, \"width\": 456, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2604.21326v1/fig-002.webp\", \"caption\": \"Table 3: Retrieval Performance on WebQA+ (based on a mixed-modal corpus D{T,IV ,IV C}) and EVQA+.\", \"page\": 7, \"index\": 2, \"width\": 941, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2604.21326v1/fig-003.webp\", \"caption\": \"Figure 3: Embeddings of T2I Queries, Image Documents (IV C or IV ) and Text Documents (T ).\", \"page\": 3, \"index\": 3, \"width\": 456, \"height\": 528}, {\"url\": \"assets/figures/arxiv/2604.21326v1/fig-004.webp\", \"caption\": \"Figure 7: (left) The encoding of different modalities under our architecture. (right) The multi-modal representation in training and inference phase.\", \"page\": 5, \"index\": 4, \"width\": 456, \"height\": 282}]"
motivation: 旨在解决通用多模态检索中早融合导致的视觉模态崩溃以及晚融合引起的语义失配问题。
method: 提出了MiMIC模型，采用解码器融合架构，并结合单模态混入和随机标题丢弃策略进行鲁棒训练。
result: 在WebQA+和EVQA+数据集上的实验结果显示，MiMIC在多种检索场景下均优于现有的早融合和晚融合基准方法。
conclusion: MiMIC通过创新的架构和训练策略，有效平衡了多模态信息的整合与语义对齐，提升了多模态检索的性能。
---

## 摘要
通用多模态检索（UMR）旨在将不同模态（如视觉和文本）映射到一个共享的嵌入空间中，以进行多模态检索。现有的 UMR 方法大致可分为两类：早期融合方法（如 Marvel），它将视觉特征投影到语言模型（LM）空间中以与文本模态集成；以及后期融合方法（如 UniVL-DR），它使用独立的编码器对视觉和文本输入进行编码，并通过加法获得融合后的嵌入。我们的初步研究表明，Marvel 表现出视觉模态崩溃，其特征是模型倾向于忽略视觉特征，而过度依赖文本线索。相比之下，虽然 UniVL-DR 受此问题影响较小，但它更容易受到语义失配的影响，即语义相关的内容在嵌入空间中相距甚远。为了应对这些挑战，我们提出了 MiMIC，它引入了两项关键创新：（1）一种用于有效多模态集成的解码器内融合（fusion-in-decoder）架构；（2）通过单模态混入（single modality mixin）和随机标题丢弃（random caption dropout）进行的鲁棒训练。在 WebQA+ 和 EVQA+ 数据集上的实验表明（这些数据集中的文档或查询图像可能缺少标题），MiMIC 的表现始终优于早期融合和后期融合的基准模型。

## Abstract
Universal Multimodal Retrieval (UMR) aims to map different modalities (e.g., visual and textual) into a shared embedding space for multi-modal retrieval. Existing UMR methods can be broadly divided into two categories: early-fusion approaches, such as Marvel, which projects visual features into the language model (LM) space for integrating with text modality, and late-fusion approaches, such as UniVL-DR, which encode visual and textual inputs using separate encoders and obtain fused embeddings through addition. Our pilot study reveals that Marvel exhibits visual modality collapse, which is characterized by the model's tendency to disregard visual features while depending excessively on textual cues. In contrast, although UniVL-DR is less affected by this issue, it is more susceptible to semantic misalignment, where semantically related content is positioned far apart in the embedding space. To address these challenges, we propose MiMIC, which introduces two key innovations: (1) a fusion-in-decoder architecture for effective multimodal integration, and (2) robust training through single modality mixin and random caption dropout. Experiments on the WebQA+ and EVQA+ datasets, where image in documents or queries might lack captions, indicate that MiMIC consistently outperforms both early- and late-fusion baselines.

---

## 论文详细总结（自动生成）

这篇论文针对通用多模态检索（Universal Multimodal Retrieval, UMR）中的关键瓶颈提出了创新性的解决方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：论文指出当前的 UMR 方法存在两大失效模式：
    *   **视觉模态崩溃（Visual Modality Collapse）**：在“早融合”模型（如 Marvel）中，模型过度依赖文本线索而忽略视觉特征，导致在无标题图像检索中表现极差。
    *   **语义失配（Semantic Misalignment）**：在“晚融合”模型（如 UniVL-DR）中，虽然保留了视觉信息，但不同模态的语义表示在空间中分布疏离，难以有效对齐。
*   **研究动机**：旨在设计一种既能深度整合多模态信息（避免失配），又能强制模型利用视觉特征（避免崩溃）的检索框架，特别是在现实中常见的“缺失标题”或“不完美数据”场景下。

### 2. 提出的方法论：MiMIC
MiMIC 模型结合了架构创新与鲁棒性训练策略：
*   **Fusion-in-Decoder (FiD) 架构**：
    *   使用独立的编码器（CLIP 视觉编码器和 T5 文本编码器）提取特征。
    *   引入 **LM 解码器（T5 Decoder）**，通过**交叉注意力（Cross-Attention）**机制动态聚合来自不同编码器的信息，生成融合嵌入。这种方式比简单的加法（晚融合）更具表达力，比全自注意力（早融合）更稳定。
*   **单模态混入策略（Single-Modality Mix-in）**：
    *   在训练阶段，将单模态的表示（仅视觉或仅文本）以一定比例 $\alpha$ 混合到融合表示中。这确保了模型在学习融合特征的同时，不会丢失特定模态的判别性信号。
*   **随机标题丢弃（Random Caption Dropout）**：
    *   在训练中随机去掉图像的文本描述，强制模型必须通过视觉编码器提取语义，从而有效缓解“文本主导”现象。
*   **两阶段训练**：首先进行基于批内负样本的对比学习，随后采用 **ANCE（近似最近邻负对比学习）** 进行硬负样本挖掘训练。

### 3. 实验设计
*   **数据集**：
    *   **WebQA+**：基于 WebQA 扩展，设置 50% 的图像不含标题，模拟不完美场景。
    *   **EVQA+**：基于 Encyclopedic-VQA 采样并混合 WebQA 的文本检索任务，增加任务复杂性。
*   **检索场景**：涵盖了 T2T（文本查文本）、T2I（文本查图）、TI2T（图文查文本）等多种组合。
*   **基准方法（Benchmarks）**：对比了 **Marvel**（早融合代表）、**UniVL-DR**（晚融合代表）和 **VISTA** 等最先进的 UMR 模型。
*   **评价指标**：Recall@k (1/5/20/100), MRR, NDCG。

### 4. 资源与算力
*   **硬件环境**：论文明确提到实验是在 **单张 NVIDIA A100 GPU** 上完成的。
*   **训练细节**：使用了 AdamW 优化器，学习率在 $5 \times 10^{-6}$ 到 $1 \times 10^{-5}$ 之间，采用了余弦退火策略，最大训练轮数为 20 轮。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的基准测试（Table 3），涵盖两个数据集和多种模型变体。
    *   针对不同子任务（T2T, T2I）进行了细分性能分析（Figure 8, 12）。
    *   **消融实验**（Table 4）：分别验证了标题丢弃、单模态混入以及 FiD 架构本身的贡献。
    *   **超参数分析**：探讨了标题丢弃比例和混入比例 $\alpha$ 对结果的影响。
*   **充分性评价**：实验设计非常充分且客观。通过 t-SNE 可视化和邻域重叠度（Neighborhood Overlap）量化分析，直观地证明了模型在缓解崩溃和对齐语义方面的有效性。

### 6. 主要结论与发现
*   **性能领先**：MiMIC-ANCE 在所有指标上均显著优于基准模型。在 WebQA+ 上，Recall@100 比最强基准 Marvel-ANCE 提升了 **6.77%**。
*   **鲁棒性强**：在图像缺失标题的情况下，MiMIC 的表现远比早融合模型稳定，证明了其视觉特征提取的有效性。
*   **语义对齐度高**：量化指标显示，MiMIC 空间中图像与对应标题的邻域重叠度远高于 UniVL-DR，解决了语义失配问题。

### 7. 优点
*   **架构优雅**：FiD 架构巧妙地平衡了计算效率与模态交互深度。
*   **训练策略实用**：单模态混入和标题丢弃是解决“模态偏见”的简单且高效的手段，具有很强的通用性。
*   **问题诊断清晰**：论文对 UMR 现有问题的诊断（崩溃 vs 失配）非常精准，为后续研究提供了清晰的视角。

### 8. 不足与局限
*   **模型规模限制**：目前主要基于 T5-base 和 CLIP-ViT-B/32，尚未在超大规模多模态预训练模型（如 LLama-3-V 等）上验证其扩展性。
*   **超参数敏感性**：单模态混入比例 $\alpha$ 在不同数据集上的最优值不同（WebQA+ 为 0.1，EVQA+ 为 0.5），缺乏一种动态自适应调整该参数的机制。
*   **模态多样性**：目前主要集中在图像和文本，尚未涉及音频、视频等更多模态的通用检索。

（完）
