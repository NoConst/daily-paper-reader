---
title: "MiMIC: Mitigating Visual Modality Collapse in Universal Multimodal Retrieval While Avoiding Semantic Misalignment"
title_zh: MiMIC：在缓解通用多模态检索中视觉模态崩溃的同时避免语义失配
authors: "Juan Li, Chuanghao Ding, Xujie Zhang, Cam-Tu Nguyen"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21326v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 缓解通用多模态检索中的视觉模态崩溃和偏差
tldr: 本研究针对通用多模态检索（UMR）中存在的视觉模态崩溃和语义失配问题，提出了MiMIC模型。现有早期融合方法易导致模型过度依赖文本而忽略视觉信息，而后期融合则难以保证语义对齐。MiMIC通过引入Decoder融合架构，并结合单模态混合与随机标题丢弃的鲁棒训练策略，有效提升了多模态集成效果。在WebQA+和EVQA+数据集上的实验证明，MiMIC在处理缺乏标题的图像检索任务时，性能显著优于现有的基准模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21326v1/fig-001.webp\", \"caption\": \"Figure 2: Different Fusion Strategies. (a) Late Fusion. Use separate image and text encoder (e.g., UniVL-DR). (b) Early Fusion. Concatenates image and text embeddings, which are then fed into the LM. (e.g., Marvel)\", \"page\": 2, \"index\": 1, \"width\": 456, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2604.21326v1/fig-002.webp\", \"caption\": \"Table 3: Retrieval Performance on WebQA+ (based on a mixed-modal corpus D{T,IV ,IV C}) and EVQA+.\", \"page\": 7, \"index\": 2, \"width\": 941, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2604.21326v1/fig-003.webp\", \"caption\": \"Figure 3: Embeddings of T2I Queries, Image Documents (IV C or IV ) and Text Documents (T ).\", \"page\": 3, \"index\": 3, \"width\": 456, \"height\": 528}, {\"url\": \"assets/figures/arxiv/2604.21326v1/fig-004.webp\", \"caption\": \"Figure 7: (left) The encoding of different modalities under our architecture. (right) The multi-modal representation in training and inference phase.\", \"page\": 5, \"index\": 4, \"width\": 456, \"height\": 282}]"
motivation: 旨在解决通用多模态检索中早期融合导致的视觉模态崩溃以及后期融合引发的语义失配问题。
method: 提出了MiMIC模型，采用Decoder融合架构，并结合单模态混合与随机标题丢弃策略进行鲁棒训练。
result: 在WebQA+和EVQA+数据集上的实验结果显示，MiMIC在多模态检索性能上全面超越了现有的早期和后期融合基准。
conclusion: MiMIC通过创新的架构和训练策略，成功平衡了多模态信息的集成与对齐，为通用多模态检索提供了更有效的解决方案。
---

## 摘要
通用多模态检索（UMR）旨在将不同模态（如视觉和文本）映射到一个共享的嵌入空间中，以实现多模态检索。现有的 UMR 方法大致可分为两类：早融合方法（如 Marvel），将视觉特征投影到语言模型（LM）空间中以与文本模态集成；以及晚融合方法（如 UniVL-DR），使用独立的编码器对视觉和文本输入进行编码，并通过加法获得融合后的嵌入。我们的初步研究表明，Marvel 表现出视觉模态崩溃，其特征是模型倾向于忽略视觉特征，而过度依赖文本线索。相比之下，尽管 UniVL-DR 受此问题影响较小，但它更容易受到语义失配的影响，即语义相关的内容在嵌入空间中距离较远。为了应对这些挑战，我们提出了 MiMIC，它引入了两项关键创新：(1) 用于有效多模态集成的解码器内融合（fusion-in-decoder）架构，以及 (2) 通过单模态混入（single modality mixin）和随机标题丢弃（random caption dropout）实现的鲁棒训练。在 WebQA+ 和 EVQA+ 数据集（其中文档或查询中的图像可能缺少标题）上的实验表明，MiMIC 的性能始终优于早融合和晚融合基准模型。

## Abstract
Universal Multimodal Retrieval (UMR) aims to map different modalities (e.g., visual and textual) into a shared embedding space for multi-modal retrieval. Existing UMR methods can be broadly divided into two categories: early-fusion approaches, such as Marvel, which projects visual features into the language model (LM) space for integrating with text modality, and late-fusion approaches, such as UniVL-DR, which encode visual and textual inputs using separate encoders and obtain fused embeddings through addition. Our pilot study reveals that Marvel exhibits visual modality collapse, which is characterized by the model's tendency to disregard visual features while depending excessively on textual cues. In contrast, although UniVL-DR is less affected by this issue, it is more susceptible to semantic misalignment, where semantically related content is positioned far apart in the embedding space. To address these challenges, we propose MiMIC, which introduces two key innovations: (1) a fusion-in-decoder architecture for effective multimodal integration, and (2) robust training through single modality mixin and random caption dropout. Experiments on the WebQA+ and EVQA+ datasets, where image in documents or queries might lack captions, indicate that MiMIC consistently outperforms both early- and late-fusion baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《MiMIC: Mitigating Visual Modality Collapse in Universal Multimodal Retrieval While Avoiding Semantic Misalignment》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
通用多模态检索（UMR）的目标是将文本、图像等不同模态映射到统一的共享嵌入空间。论文指出当前主流方法存在两大缺陷：
*   **视觉模态崩溃（Visual Modality Collapse）：** 以 Marvel 为代表的“早融合”方法将视觉特征投影到语言模型空间。研究发现，这类模型会产生“文本霸权”，即过度依赖文本线索而忽略视觉特征，导致在图像缺失标题时检索性能剧降。
*   **语义失配（Semantic Misalignment）：** 以 UniVL-DR 为代表的“晚融合”方法使用独立编码器并通过简单加法融合。这导致相同语义的不同模态在空间中分布疏离，难以实现深层的跨模态理解。
**动机：** 开发一种既能防止视觉信号被忽略，又能保证不同模态语义高度对齐的新型检索架构。

### 2. 论文提出的方法论
论文提出了 **MiMIC** 模型，其核心思想结合了灵活的架构设计与鲁棒的训练策略：
*   **Fusion-in-Decoder (FiD) 架构：** 
    *   使用独立的视觉编码器（CLIP）和文本编码器（T5 Encoder）提取特征。
    *   引入 **LM Decoder（T5 Decoder）**，利用其**交叉注意力机制（Cross-Attention）**动态地从视觉和文本特征中聚合信息，生成最终的融合嵌入。这种方式比简单加法更深入，比早融合更具模态独立性。
*   **单模态混入（Single-Modality Mix-in）：** 
    *   在训练阶段，将单模态的表示（纯视觉或纯文本）按比例 $\alpha$ 随机混入到融合表示中。公式表达为：$\hat{x}_d = (1 - \alpha)x_d + \alpha(\text{单模态表示})$。这确保了模型在融合时不会丢失特定模态的判别信号。
*   **随机标题丢弃（Random Caption Dropout）：** 
    *   在训练中主动丢弃图像的标题，强迫模型必须通过视觉特征来完成检索任务，从而有效缓解“文本霸权”问题。
*   **两阶段训练：** 先进行基于批内负样本的对比学习，再利用 ANCE（硬负采样）进行微调。

### 3. 实验设计
*   **数据集：** 扩展了两个主流基准，构建了 **WebQA+** 和 **EVQA+**。这两个“+”版本特意引入了 50% 缺失标题的图像场景，以模拟现实中不完美的数据环境。
*   **任务场景：** 涵盖了文本检索文本（T2T）、文本检索图像（T2I）以及多模态查询检索文本（TI2T）。
*   **对比方法（Benchmark）：** 
    *   早融合代表：**Marvel**、**VISTA**。
    *   晚融合代表：**UniVL-DR**。
    *   基础模型：CLIP-DPR、Marvel-DPR 等。

### 4. 资源与算力
*   **硬件：** 论文明确提到实验是在 **单张 NVIDIA A100 GPU** 上完成的。
*   **训练细节：** 使用了 AdamW 优化器，学习率在 $5 \times 10^{-6}$ 到 $1 \times 10^{-5}$ 之间，采用了余弦退火策略。具体训练时长未详细标明，但给出了最大 Epoch 为 20，且每 500 步进行一次评估。

### 5. 实验数量与充分性
*   **实验规模：** 论文进行了多维度的实验，包括：
    1.  **主实验：** 在两个数据集上对比了 6-7 种基准模型。
    2.  **消融实验：** 分别验证了 FiD 架构、标题丢弃、单模态混入的有效性。
    3.  **超参数分析：** 探讨了标题丢弃比例和混入比例 $\alpha$ 对性能的影响。
    4.  **可视化分析：** 使用 t-SNE 观察嵌入空间的分布，直观证明了 MiMIC 解决了模态崩溃问题。
*   **客观性与公平性：** 实验通过控制变量（如统一使用 CLIP-B-32 骨干网络）确保了对比的公平性，且针对“缺失模态”这一痛点设计的 WebQA+ 数据集具有很强的说服力。

### 6. 论文的主要结论与发现
*   **MiMIC 性能卓越：** 在 WebQA+ 和 EVQA+ 上，MiMIC-ANCE 在所有指标（Recall, MRR, NDCG）上均达到 SOTA，尤其在 R@100 上比强基准 Marvel 提升了约 7%。
*   **解决崩溃问题：** 可视化证明 MiMIC 的图像嵌入分布更均匀，不再像早融合模型那样坍缩成密集的簇。
*   **语义对齐增强：** 通过跨模态邻域重叠度测量，MiMIC 的对齐程度显著优于 UniVL-DR 和 Marvel。
*   **鲁棒性：** 证明了标题丢弃是训练多模态检索模型的必要手段，能显著提升模型对视觉信息的利用率。

### 7. 优点
*   **架构创新：** 巧妙地将 FiD 思想从生成任务引入到检索嵌入任务中，利用交叉注意力平衡了融合深度与模态独立性。
*   **策略实用：** 提出的 Mix-in 和 Dropout 策略简单易行，但直接击中了多模态学习中常见的“模态偏见”痛点。
*   **现实意义：** 针对“不完美数据”（缺失标题）的研究更符合互联网真实场景。

### 8. 不足与局限
*   **模型规模限制：** 实验主要基于 T5-base 和 CLIP-B-32，尚未在超大规模多模态模型（如 7B 以上的 MLLM）上验证其扩展性。
*   **超参数敏感性：** 实验显示混入比例 $\alpha$ 在不同数据集上的最优值不同（WebQA+ 为 0.1，EVQA+ 为 0.5），这意味着在实际应用中需要针对新数据进行调优。
*   **计算开销：** 虽然使用了单卡，但 FiD 架构涉及 Decoder 的交叉注意力计算，其推理延迟可能略高于简单的晚融合加法模型。

（完）
