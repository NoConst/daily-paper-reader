---
title: "QMoP: Query Guided Mixture-of-Projector for Efficient Visual Token Compression"
title_zh: QMoP：用于高效视觉 Token 压缩的查询引导混合投影器
authors: "Zhongyang Li, Yaqian Li, Faming Fang, Rinyoichi Takezoe, Zi-Hao Bo, Cheng Qian, Mo Guang, Guixu Zhang, Kaiwen Long"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21232v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大模型的高效视觉标记压缩
tldr: 针对多模态大模型中视觉标记过多导致的计算与内存瓶颈，本文提出QMoP框架。该框架通过池化、重采样和剪枝三个协作分支，结合查询引导路由（QGR）根据视觉输入和文本查询动态压缩视觉标记。此外，研究者还推出了评估压缩损失的基准VTCBench。实验证明，QMoP在显著降低资源开销的同时，性能优于现有基准。
selection_source: fresh_fetch
motivation: 现有的视觉标记压缩方法通常依赖固定的启发式策略，难以在不同场景下兼顾全局语义与局部细节的自适应平衡。
method: 提出一种混合投影器架构，利用查询引导路由动态协调池化、重采样和剪枝三个分支，并采用类MoE机制融合输出。
result: QMoP在显著减少内存占用、计算量和推理时间的情况下，在多个基准测试中表现优于强基线模型。
conclusion: QMoP通过自适应的多策略融合实现了高效的视觉标记压缩，为提升多模态大模型的推理效率提供了有效方案。
---

## 摘要
多模态大语言模型面临严重的计算和内存瓶颈，因为视觉 Token 的数量远超文本 Token。虽然近期的方法采用投影器（projector）模块将视觉 Token 对齐并压缩为文本对齐的特征，但它们通常依赖于固定的启发式方法，限制了在不同场景下的适应性。在本文中，我们首次提出了查询引导混合投影器（QMoP），这是一个新颖且灵活的框架，通过三个协作分支自适应地压缩视觉 Token：(1) 基于池化的分支，用于提取粗粒度的全局语义；(2) 重采样器（resampler）分支，用于提取高层语义表示；(3) 基于剪枝的分支，用于细粒度的 Token 选择以保留关键视觉细节。为了自适应地协调这些分支，我们引入了查询引导路由（QGR），它根据视觉输入和文本查询动态地选择不同分支的输出并分配权重。设计了一种混合专家（MoE）风格的融合机制来聚合输出，在抑制噪声的同时利用每种策略的优势。为了系统地评估视觉 Token 压缩的效果，我们还开发了 VTCBench，这是一个专门用于评估视觉 Token 压缩引起的信息损失的基准测试。大量实验表明，尽管依赖于基础压缩模块，QMoP 的性能仍优于强基准模型，并在内存、计算和推理时间方面实现了显著节省。

## 速览
**TLDR**：针对多模态大模型中视觉标记过多导致的计算与内存瓶颈，本文提出QMoP框架。该框架通过池化、重采样和剪枝三个协作分支，结合查询引导路由（QGR）根据视觉输入和文本查询动态压缩视觉标记。此外，研究者还推出了评估压缩损失的基准VTCBench。实验证明，QMoP在显著降低资源开销的同时，性能优于现有基准。 \
**Motivation**：现有的视觉标记压缩方法通常依赖固定的启发式策略，难以在不同场景下自适应地平衡信息保留与压缩效率。 \
**Method**：提出一种混合投影器架构，利用查询引导路由动态协调池化、重采样和剪枝三个分支，并采用类MoE机制融合输出。 \
**Result**：QMoP在显著减少内存占用、计算量和推理时间的情况下，在多个任务上展现出优于强基线模型的性能。 \
**Conclusion**：QMoP通过自适应的多策略融合实现了高效的视觉标记压缩，为提升多模态大模型的推理效率提供了有效方案。

---

## Abstract
Multimodal large language models suffer from severe computational and memory bottlenecks, as the number of visual tokens far exceeds that of textual tokens. While recent methods employ projector modules to align and compress visual tokens into text-aligned features, they typically depend on fixed heuristics that limit adaptability across diverse scenarios. In this paper, we first propose Query Guided Mixture-of-Projector (QMoP), a novel and flexible framework that adaptively compresses visual tokens via three collaborative branches: (1) a pooling-based branch for coarse-grained global semantics, (2) a resampler branch for extracting high-level semantic representations, and (3) a pruning-based branch for fine-grained token selection to preserve critical visual detail. To adaptively coordinate these branches, we introduce the Query Guided Router (QGR), which dynamically selects and weights the outputs from different branches based on both visual input and textual queries. A Mixture-of-Experts-style fusion mechanism is designed to aggregate the outputs, harnessing the strengths of each strategy while suppressing noise. To systematically evaluate the effects of Visual Token Compression, we also develop VTCBench, a dedicated benchmark for evaluating the information loss induced by visual token compression. Extensive experiments demonstrate that despite relying on fundamental compression modules, QMoP outperforms strong baselines and delivers significant savings in memory, computation, and inference time.

---

## 论文详细总结（自动生成）

以下是对论文《QMoP: Query Guided Mixture-of-Projector for Efficient Visual Token Compression》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态大语言模型（MLLM）中，视觉 Token 数量远超文本 Token（如 LLaVA 默认 576 个），导致巨大的计算开销和内存瓶颈。
*   **研究动机**：现有的视觉 Token 压缩方法（如池化、重采样、剪枝）通常采用固定的启发式规则。
    *   **池化（Pooling）**：擅长全局语义，但易丢失局部细节。
    *   **重采样（Resampler）**：擅长高层语义抽象，但易产生幻觉。
    *   **剪枝（Pruning）**：擅长保留关键细节，但会破坏空间结构。
*   **目标**：开发一种能够根据图像内容和文本查询（Query）自适应选择压缩策略的灵活框架。

### 2. 方法论
QMoP 框架的核心思想是利用“混合专家（MoE）”逻辑，动态协调三种互补的压缩分支。
*   **三个协作分支**：
    1.  **池化分支**：利用局部注意力机制聚合相邻 Token，保留全局场景结构。
    2.  **重采样分支**：通过可学习的查询 Token 和交叉注意力提取高层语义。
    3.  **剪枝分支**：基于重要性（视觉注意力）和相关性（与文本的匹配度）筛选 Token，保留细粒度细节。
*   **关键技术：查询引导路由（QGR）**：
    *   输入：视觉编码器的 `CLS` Token 和文本编码器的 `EOS` Token。
    *   处理：将两者拼接后通过两层 MLP 生成路由权重 $\alpha$。
    *   融合：采用类 MoE 机制，选择权重最高的两个分支进行加权融合，抑制不相关分支的噪声。
*   **训练策略**：
    *   **第一阶段**：投影器预训练，三个分支共同学习对齐。
    *   **第二阶段**：联合微调，激活 QGR 路由，并引入 Gumbel 噪声和温度退火策略以防止路由塌陷。

### 3. 实验设计
*   **数据集/场景**：涵盖了自然图像、文档、艺术、科学图表等多种场景。
*   **标准基准（10个）**：GQA, VQAv2, TextVQA, MM-Vet, POPE, MME, MMBench, SEED-Image, MMMU, MMStar。
*   **专用基准（VTCBench）**：论文自建的诊断性基准，从全局理解（GU）、空间计数（SC）、推理常识（RC）、风格情感（SE）、局部细节（LD）五个维度评估压缩引起的信息损失。
*   **对比方法**：
    *   **Pre-LLM 压缩**：Pixel-Shuffle, FasterVLM, C-Abstractor, TokenPacker, LDP-v2 等。
    *   **Intra-LLM 压缩**：FastV, HiRED, SparseVLM, MustDrop 等。

### 4. 资源与算力
*   **训练时长**：文中提供了详细的训练时间对比。例如，在 144 Token 配置下，训练耗时约 **75.6 小时**；在 576 Token（原始 LLaVA）下耗时 **102.3 小时**。
*   **算力设备**：文中未明确指出具体的 GPU 型号和数量（通常此类实验基于 A100 或 H100 集群），但给出了计算量（TFLOPs）和 KV Cache 占用情况。
*   **效率提升**：相比原始模型，QMoP 在 144 Token 下仅需 **25% 的 FLOPs 和内存**，推理延迟显著降低。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的跨模型（7B 和 13B）、跨压缩率（4 到 144 Token）的对比实验。
*   **消融实验**：详细探讨了不同投影器组合（A+B, B+C 等）的效果、Token 数量对性能的影响、以及路由器的计算开销。
*   **客观性与公平性**：实验在相同的训练数据（LAION-558K, 665K 指令微调数据）和相同的 LLM 后端下进行，确保了对比的公平性。VTCBench 的引入增强了对压缩副作用的深入诊断。

### 6. 主要结论与发现
*   **自适应性至关重要**：没有一种单一的压缩策略能胜任所有任务，QMoP 通过动态路由实现了各策略的取长补短。
*   **性能保持**：将视觉 Token 从 576 压缩至 144（4倍压缩）时，QMoP 的平均性能（100.73%）甚至略高于未压缩的基线模型。
*   **压缩极限**：当 Token 减少到 36 个以下时，性能开始出现显著下降，表明存在信息丢失的临界点。

### 7. 优点
*   **多策略融合**：首次将池化、重采样和剪枝整合进一个自适应框架，解决了单一策略的局限性。
*   **查询感知**：路由决策不仅考虑图像，还考虑了文本查询，使压缩过程具有“任务导向性”。
*   **诊断工具**：提出的 VTCBench 为视觉压缩领域提供了更细致的评估工具，具有较高的学术价值。
*   **高效性**：在大幅降低计算资源需求的同时保持了极高的模型精度。

### 8. 不足与局限
*   **架构复杂性**：相比单一投影器，QMoP 维护三个分支增加了模型参数量和实现复杂度。
*   **路由依赖**：路由器的效果高度依赖于预训练的 CLIP 文本编码器，对于极长或极复杂的文本查询，路由准确性可能受限。
*   **极端压缩挑战**：在极低 Token 数量（如 4 或 16 个）下，虽然比同类方法好，但仍无法避免严重的语义丢失，难以处理极细粒度的任务。

（完）
