---
title: "Time is Not a Label: Continuous Phase Rotation for Temporal Knowledge Graphs and Agentic Memory"
title_zh: 时间并非标签：用于时序知识图谱与智能体记忆的连续相位旋转
authors: "Weixian Waylon Li, Jiaxin Zhang, Xianan Jim Yang, Tiejun Ma, Yiwen Guo"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11544v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 用于智能体记忆的时序知识图谱模块
tldr: 针对现有知识图谱将时间视为离散标签导致难以区分持久与演化事实的问题，本文提出RoMem。该方法利用语义速度门学习关系波动性，结合连续相位旋转在复向量空间实现几何遮蔽，使过时事实自然降权。RoMem在时序知识图谱补全和智能体记忆任务中均达SOTA，具备极强的时序推理与零样本泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11544v1/fig-001.webp\", \"caption\": \"Table 1: Results on ICEWS05-15. Baseline results are taken from Li et al. (2025) and Shen et al. (2025). Best results\", \"page\": 6, \"index\": 1, \"width\": 452, \"height\": 521}, {\"url\": \"assets/figures/arxiv/2604.11544v1/fig-002.webp\", \"caption\": \"Table 5: Pretrained semantic speed gate values. Higher αr = faster rotation. Seen: appeared in ICEWS05-15 during pretraining; Unseen: zero-shot via text embedding similarity.\", \"page\": 21, \"index\": 2, \"width\": 366, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2604.11544v1/fig-003.webp\", \"caption\": \"Figure 3: Scoring trace for the competing slot (Obama, Consult, ?). Bold curves are smoothed (5-quarter rolling average); light traces show raw quarterly scores. The blue and yellow shaded regions mark the observation windows for Blair (2007–2008) and Xi (2013–2015) respectively. The crossover point τ∗ (red dot) marks where the temporally outdated fact is geometrically shadowed by the newer one. The gate value αr = 0.87 confirms the model treats “Consult” as a highly dynamic relation.\", \"page\": 21, \"index\": 3, \"width\": 693, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2604.11544v1/fig-004.webp\", \"caption\": \"Figure 1: Performance Overview.\", \"page\": 2, \"index\": 4, \"width\": 447, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2604.11544v1/fig-005.webp\", \"caption\": \"Table 4: TKGE hyperparameters. Parameters above the mid-rule are core KGE settings; below are temporal extension parameters.\", \"page\": 18, \"index\": 5, \"width\": 658, \"height\": 456}, {\"url\": \"assets/figures/arxiv/2604.11544v1/fig-006.webp\", \"caption\": \"Table 2: Comprehensive evaluation of ROMEM. (a) MultiTQ: Heavy temporal reasoning. (b) LoCoMo: Hybrid reasoning (Recall@10). (c) DMR-MSC: Static memory preservation. (d) FinTMMBench: Zero-shot domain generalisation. Implementation = LLM for graph construction (named entity recognition and triple extraction)\", \"page\": 8, \"index\": 6, \"width\": 950, \"height\": 934}, {\"url\": \"assets/figures/arxiv/2604.11544v1/fig-007.webp\", \"caption\": \"Figure 2: Overview of the ROMEM Architecture. The framework consists of four stages: (A) Functional Rotation (§3.3) applies geometric phase shifts to obsolete facts; (B) Semantic Speed Gate (§3.4) determines relational volatility from text embeddings; (C) Two-Phase Training (§3.5) pretrains the gate and learns the temporal spectrum; (D) Inference-Time Retrieval (§3.6) resolves contradictions via geometric shadowing.\", \"page\": 4, \"index\": 7, \"width\": 950, \"height\": 523}]"
motivation: 现有系统将时间视为离散元数据，无法有效区分持久事实与演化事实，导致知识更新效率低下或旧知识丢失。
method: 引入语义速度门预测关系波动性，并利用连续相位旋转在复向量空间中对过时事实进行几何遮蔽。
result: 在ICEWS05-15上取得SOTA，并在智能体记忆基准测试中实现2-3倍的性能提升及优秀的零样本泛化表现。
conclusion: RoMem通过将时间融入几何表示，为构建能区分事实时效性且具备高效推理能力的智能体记忆系统提供了新方案。
---

## 摘要
诸如知识图谱之类的结构化记忆表示对于自主智能体和其他长效系统至关重要。然而，大多数现有方法将时间建模为离散元数据，要么按新近度排序（掩埋了陈旧但永久的知识），要么简单地覆盖过时事实，或者在每个摄取步骤都需要昂贵的大语言模型（LLM）调用，导致它们无法区分持久性事实与演化性事实。为解决这一问题，我们推出了 RoMem，这是一个用于结构化记忆系统的即插即用式时序知识图谱模块，适用于智能体记忆及其他领域。预训练的语义速度门（Semantic Speed Gate）将每个关系的文本嵌入映射到波动率分数，从数据中学习到演化关系（例如“...的总统”）应当快速旋转，而持久关系（例如“出生于”）应当保持稳定。结合连续相位旋转，这实现了几何遮蔽（geometric shadowing）：过时的事实在复向量空间中被旋转出相位，从而使时间上正确的事实在无需删除的情况下自然地优于矛盾事实。在时序知识图谱补全任务中，RoMem 在 ICEWS05-15 数据集上达到了最先进的结果（72.6 MRR）。应用于智能体记忆时，它在时序推理（MultiTQ）上的 MRR 和回答准确率提升了 2-3 倍，在混合基准测试（LoCoMo）中占据主导地位，在静态记忆保持方面实现零退化（DMR-MSC），并能零样本泛化至未见的金融领域（FinTMMBench）。

## Abstract
Structured memory representations such as knowledge graphs are central to autonomous agents and other long-lived systems. However, most existing approaches model time as discrete metadata, either sorting by recency (burying old-yet-permanent knowledge), simply overwriting outdated facts, or requiring an expensive LLM call at every ingestion step, leaving them unable to distinguish persistent facts from evolving ones. To address this, we introduce RoMem, a drop-in temporal knowledge graph module for structured memory systems, applicable to agentic memory and beyond. A pretrained Semantic Speed Gate maps each relation's text embedding to a volatility score, learning from data that evolving relations (e.g., "president of") should rotate fast while persistent ones (e.g., "born in") should remain stable. Combined with continuous phase rotation, this enables geometric shadowing: obsolete facts are rotated out of phase in complex vector space, so temporally correct facts naturally outrank contradictions without deletion. On temporal knowledge graph completion, RoMem achieves state-of-the-art results on ICEWS05-15 (72.6 MRR). Applied to agentic memory, it delivers 2-3x MRR and answer accuracy on temporal reasoning (MultiTQ), dominates hybrid benchmark (LoCoMo), preserves static memory with zero degradation (DMR-MSC), and generalises zero-shot to unseen financial domains (FinTMMBench).

---

## 论文详细总结（自动生成）

这是一份关于论文《Time is Not a Label: Continuous Phase Rotation for Temporal Knowledge Graphs and Agentic Memory》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的智能体记忆系统（Agentic Memory）在处理时间信息时存在“静态-动态困境”。
*   **研究背景**：
    *   大多数系统将时间视为离散的元数据（如时间戳列），导致无法区分**持久性事实**（如“出生于”）和**演化性事实**（如“...的总统”）。
    *   现有方案的缺陷：**破坏性覆盖**会丢失历史；**LLM 仲裁**成本高且难以扩展；**新近度排序**会掩埋陈旧但永久正确的知识。
*   **整体含义**：论文提出时间不应仅仅是一个标签，而应作为嵌入空间中的连续几何算子，通过物理规律而非数据库操作来解决时序冲突。

### 2. 方法论：RoMem 架构
RoMem 是一个即插即用的时序知识图谱模块，其核心思想是**连续相位旋转**和**几何遮蔽**。

*   **核心思想**：在复向量空间中，将时间建模为旋转算子。动态事实随时间快速旋转出相位（降权），而静态事实保持稳定。
*   **关键技术细节**：
    1.  **连续相位旋转（Continuous Phase Rotation）**：定义旋转角 $\theta_r(\tau) = s \cdot \alpha_r \cdot \tau \cdot \omega$。其中 $\tau$ 是连续时间戳，$s$ 是全局缩放因子，$\omega$ 是学习的频率向量。
    2.  **语义速度门（Semantic Speed Gate）**：这是一个预训练的 MLP，将关系的文本嵌入映射为波动率分数 $\alpha_r \in (0, 1)$。静态关系（如“born in”）的 $\alpha_r \approx 0$，动态关系（如“president of”）的 $\alpha_r \approx 1$。
    3.  **几何遮蔽（Geometric Shadowing）**：系统采用“仅追加（Append-only）”模式。当查询当前时间时，最新的事实因相位对齐而获得最高分，旧事实因旋转而自然降权，实现“遮蔽”效果。
    4.  **两阶段训练**：第一阶段离线预训练速度门（学习关系的波动性）；第二阶段在线学习全局时间频谱和实体/关系嵌入。

### 3. 实验设计
*   **数据集与场景**：
    *   **RQ1（时序补全）**：使用 ICEWS05-15（地缘政治事件）。
    *   **RQ2（智能体记忆）**：
        *   **MultiTQ**：重度时序推理任务。
        *   **LoCoMo**：混合任务（单跳、多跳、时序、开放域）。
        *   **DMR-MSC**：静态对话记忆保持。
    *   **RQ3（领域泛化）**：使用 FinTMMBench（金融领域）。
*   **对比方法**：
    *   **TKG 基准**：ChronoR, TeRo, 3DG-TE 等。
    *   **智能体记忆基准**：Mem0, Zep, HippoRAG, A-Mem, LicoMemory。
*   **配置**：对比了 OpenAI API（GPT-5-mini）和开源服务器（LLaMA-3.1-70B + BGE-M3）两种实现。

### 4. 资源与算力
*   **模型使用**：文中明确提到了使用 GPT-5-mini、GPT-5.2、LLaMA-3.1-70B 以及 text-embedding-3-small 和 BGE-M3 嵌入模型。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到了训练参数（如 500 steps per update，100 epochs for gate pretraining），表明其训练成本主要集中在 TKG 嵌入学习上，而推理阶段通过“逆旋转技巧”实现了高效的 1-vs-N 检索。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从标准知识图谱补全到复杂智能体对话记忆的 5 个主流基准测试。
*   **充分性**：实验设计非常全面。不仅验证了时序推理能力的提升（MultiTQ 上 MRR 提升 2-3 倍），还通过 DMR-MSC 证明了该机制不会损害静态记忆的检索效果。
*   **客观性**：采用了两种不同的 LLM/Embedding 组合进行测试，证明了方法的鲁棒性；同时提供了数学证明（附录 F）来解释连续插值和检索效率。

### 6. 主要结论与发现
*   **SOTA 性能**：RoMem 在 ICEWS05-15 上达到了 72.6 的 MRR，刷新了时序知识图谱补全的纪录。
*   **智能体记忆突破**：在 MultiTQ 任务中，将下游 LLM 的回答准确率从 11.2% 提升至 36.6%。
*   **零样本泛化**：语义速度门能够根据关系的文本语义自动推断未见过的金融关系（如“季度营收”）的波动性，具备极强的跨领域能力。
*   **静态保持**：在纯静态任务中表现与基准持平或略优，解决了“新近度偏见”掩埋旧知识的问题。

### 7. 优点
*   **优雅的几何解法**：将复杂的时间逻辑转化为向量空间中的旋转，避免了昂贵的 LLM 实时仲裁。
*   **“时间离合器”机制**：语义速度门巧妙地解决了静态与动态事实的共存问题。
*   **即插即用**：作为 KG 组件的增强模块，可以轻松集成到现有的 Graph-RAG 系统中。
*   **连续性**：支持对任意未见时间点的零样本插值推理。

### 8. 不足与局限
*   **依赖提取质量**：RoMem 的效果高度依赖于前期 OpenIE（开放信息抽取）提取三元组和时间信息的准确性。
*   **震荡风险**：虽然论文提到了平滑处理，但在数据极度稀疏的情况下，高频旋转分量可能导致评分出现局部震荡。
*   **数学复杂度**：相比于简单的向量检索，复数空间的旋转和频谱学习增加了系统的实现门槛和理解难度。
*   **应用限制**：主要针对结构化事实记忆，对于非结构化的感性对话或纯文本流的建模能力尚未在文中深入探讨。

（完）
