---
title: "MELT: Improve Composed Image Retrieval via the Modification Frequentation-Rarity Balance Network"
authors: "Guozhi Qiu, Zhiwei Chen, Zixu Li, Qinlei Huang, Zhiheng Fu, Xuemeng Song, Yupeng Hu"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29291v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 通过 MELT 网络改进组合图像检索
tldr: 本文针对组合图像检索（CIR）中频率偏差导致的稀有样本忽视及硬负样本干扰问题，提出了MELT网络。该网络通过在多模态上下文中加强对稀有修改语义的关注，并引入基于扩散的去噪机制来处理高相似度负样本，从而提升了多模态融合与匹配的鲁棒性。实验表明，MELT在多个基准测试中表现优异。
selection_source: fresh_fetch
motivation: 现有CIR方法面临稀有样本被忽视以及相似度评分易受硬负样本和噪声干扰的局限性。
method: 提出MELT网络，利用频率-稀有度平衡机制关注稀有语义，并结合扩散去噪技术优化相似度估计。
result: 在两个主流CIR基准数据集上的实验结果证明了MELT具有领先的检索性能。
conclusion: MELT通过有效应对稀有样本忽视和噪声干扰挑战，显著增强了组合图像检索的性能。
---

## Abstract
Composed Image Retrieval (CIR) uses a reference image and a modification text as a query to retrieve a target image satisfying the requirement of ``modifying the reference image according to the text instructions''. However, existing CIR methods face two limitations: (1) frequency bias leading to ``Rare Sample Neglect'', and (2) susceptibility of similarity scores to interference from hard negative samples and noise. To address these limitations, we confront two key challenges: asymmetric rare semantic localization and robust similarity estimation under hard negative samples. To solve these challenges, we propose the Modification frEquentation-rarity baLance neTwork MELT. MELT assigns increased attention to rare modification semantics in multimodal contexts while applying diffusion-based denoising to hard negative samples with high similarity scores, enhancing multimodal fusion and matching. Extensive experiments on two CIR benchmarks validate the superior performance of MELT. Codes are available at https://github.com/luckylittlezhi/MELT.

---

## 论文详细总结（自动生成）

这篇论文提出了 **MELT**（Modification frEquentation-rarity baLance neTwork），旨在解决组合图像检索（Composed Image Retrieval, CIR）中的频率偏差和噪声干扰问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
组合图像检索（CIR）要求系统根据“参考图像 + 修改文本”检索目标图像。现有方法存在两个主要局限：
*   **频率偏差导致“稀有样本忽视”（L1）：** 数据集中物体和指令分布极不平衡，模型倾向于学习高频模式（如“狗”、“坐着”），而忽视稀有语义（如“在人手中”），导致检索结果偏向常见物体。
*   **易受硬负样本和噪声干扰（L2）：** 特别是在时尚领域，存在大量外观相似的硬负样本，单次相似度评分容易受到噪声干扰，导致排名错误。
*   **核心挑战：** 如何在多模态交互中精确捕捉稀有语义，以及如何在存在硬负样本的情况下实现鲁棒的相似度估计。

### 2. 论文提出的方法论
MELT 网络由两个核心模块组成，旨在平衡高频与稀有语义，并对相似度矩阵进行去噪：

*   **稀有度感知令牌细化（RATR）：**
    *   **语义定位：** 利用交叉注意力机制（以文本为 Query，图像为 Key/Value）定位与修改指令最相关的图像区域。
    *   **稀有度度量：** 引入“拟合分数”（Fitting Score），结合了基于马氏距离的**残差能量**（衡量修改幅度）和**余弦相似度**（衡量修改方向）。
    *   **令牌修正：** 当拟合分数超过设定阈值时，判定为稀有样本，通过可学习的强度参数对关键图像令牌进行补偿和重构，生成更精确的组合特征。
*   **基于扩散的相似度去噪（DSD）：**
    *   **噪声建模：** 将硬负样本引起的高相似度视为相似度矩阵中的“噪声”。
    *   **扩散去噪：** 采用轻量级的 DDIM（扩散概率模型）架构。在训练阶段，将真实标签分布作为目标，通过前向过程加入高斯噪声，再利用训练好的去噪器逐步剔除硬负样本的干扰。
    *   **知识蒸馏：** 引入知识蒸馏损失（$L_{KD}$），使原始相似度分布向去噪后的分布对齐，增强匹配的鲁棒性。

### 3. 实验设计
*   **数据集：** 
    *   **FashionIQ：** 专注于时尚领域（Dresses, Shirts, Tops&Tees），包含大量细微修改和硬负样本。
    *   **CIRR：** 开放领域数据集，涵盖更广泛的现实场景。
*   **Benchmark 与对比方法：** 对比了包括 TG-CIR (2023)、SPRC (2024)、ENCODER (2025) 等在内的 10 余种最先进（SOTA）的 CIR 模型。
*   **评价指标：** Recall@k (k=1, 5, 10, 50) 以及针对 CIRR 的 Recall subset@k。

### 4. 资源与算力
*   **硬件：** 使用了 **1 张 NVIDIA V100 GPU (32GB)**。
*   **软件/参数：** 基于预训练的 **BLIP-2** 提取特征；优化器为 AdamW；学习率 1e-5；Batch Size 为 128。
*   **训练时长：** 文中未明确给出具体的训练总时长，但提到使用了单卡 V100，说明该模型在算力需求上相对高效。

### 5. 实验数量与充分性
*   **实验规模：** 论文在两个主流基准数据集上进行了全面的性能对比，并针对 FashionIQ 的三个子类分别给出了结果。
*   **消融实验：** 设计了 6 组消融实验（D#1-D#6），分别验证了注意力机制、稀有度阈值、令牌修正、高斯噪声、扩散损失和知识蒸馏损失的有效性。
*   **充分性评价：** 实验设计较为充分，涵盖了从整体性能到单个模块贡献的验证。通过在时尚领域（高噪声）和开放领域（分布均匀）的对比，证明了模型在不同场景下的适应性。

### 6. 主要结论与发现
*   **性能领先：** MELT 在 FashionIQ 和 CIRR 上均取得了 SOTA 结果。在 FashionIQ 上，平均 R@10 提升了 3.07%，R@50 提升了 2.27%。
*   **领域差异：** MELT 在 FashionIQ 上的提升比 CIRR 更显著，这证实了该方法在处理硬负样本和稀有类别（时尚领域典型特征）方面的卓越能力。
*   **去噪有效性：** 扩散去噪模块能有效纠正因硬负样本导致的排名错误，提高检索精度。

### 7. 优点
*   **创新性度量：** 提出利用残差能量和方向相似度来量化“修改稀有度”，为解决 CIR 中的频率偏差提供了新视角。
*   **去噪思路新颖：** 将硬负样本匹配问题转化为相似度矩阵的信号去噪问题，并引入扩散模型处理，这在 CIR 领域具有前瞻性。
*   **即插即用潜力：** 模块设计逻辑清晰，易于集成到其他多模态检索框架中。

### 8. 不足与局限
*   **超参数敏感性：** 稀有度阈值 $\gamma$ 和修改强度 $\eta$ 需要通过网格搜索确定，不同数据集可能需要重新调优。
*   **骨干网络依赖：** 性能在很大程度上依赖于预训练的 BLIP-2 特征质量，对于更轻量级或不同架构的特征提取器的兼容性有待验证。
*   **推理开销：** 虽然扩散模块是轻量级的，但多步 DDIM 去噪过程相比单次评分会增加一定的推理延迟。

（完）
