---
title: "Symbiotic-MoE: Unlocking the Synergy between Generation and Understanding"
title_zh: Symbiotic-MoE：解锁生成与理解之间的协同效应
authors: "Xiangyue Liu, Zijian Zhang, Miles Yang, Zhao Zhong, Liefeng Bo, Ping Tan"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07753v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 用于多模态大模型生成与理解的多模态混合专家架构
tldr: 本研究针对多模态大模型在集成图像生成时出现的理解能力退化问题，提出了Symbiotic-MoE框架。该框架采用原生MoE架构，通过模态感知专家解耦（将专家分为任务特定型和共享型）以及渐进式训练策略（差分学习率与梯度屏蔽），有效解决了生成与理解任务间的梯度冲突。实验证明，该方法不仅实现了生成的快速收敛，还通过跨模态协同显著提升了模型在MMLU和OCRBench等理解任务上的表现。
selection_source: fresh_fetch
motivation: 旨在解决多模态模型在同时学习生成与理解任务时，因梯度冲突导致的理解能力灾难性遗忘及现有隔离方案导致的协同性丧失。
method: 提出模态感知专家解耦机制以建立语义桥梁，并结合包含差分学习率和梯度屏蔽的渐进式训练策略来优化专家利用。
result: 该框架在实现生成任务快速收敛的同时，显著提升了模型在MMLU和OCRBench等理解基准测试中的性能。
conclusion: Symbiotic-MoE成功打破了生成与理解任务间的竞争关系，实现了零参数开销下的跨模态能力协同增强。
---

## 摘要
为大型多模态模型（LMMs）赋予图像生成能力，往往会因严重的梯度冲突导致理解任务中的灾难性遗忘。虽然现有的 Mixture-of-Transformers (MoT) 等范式通过结构隔离缓解了这种冲突，但它们从根本上切断了跨模态协同，并面临容量碎片化的问题。在这项工作中，我们提出了 Symbiotic-MoE，这是一个统一的预训练框架，在原生多模态混合专家（MoE）Transformer 架构中以零参数开销解决了任务干扰问题。我们首先发现，标准的 MoE 微调会导致路由崩溃，即生成梯度主导了专家的利用。为了解决这一问题，我们引入了模态感知专家解耦，将专家划分为特定任务组，同时利用共享专家作为多模态语义桥梁。至关重要的是，这种设计允许共享专家从生成任务中吸收细粒度的视觉语义，从而丰富文本表示。为了优化这一过程，我们提出了一种渐进式训练策略，其特点是差异化学习率和早期梯度屏蔽。该机制不仅保护了预训练知识免受早期波动的影响，而且最终将生成信号转化为对理解任务有益的建设性反馈。大量实验表明，Symbiotic-MoE 在实现快速生成收敛的同时，解锁了跨模态协同效应，在 MMLU 和 OCRBench 上显著提升了固有的理解能力。

## Abstract
Empowering Large Multimodal Models (LMMs) with image generation often leads to catastrophic forgetting in understanding tasks due to severe gradient conflicts. While existing paradigms like Mixture-of-Transformers (MoT) mitigate this conflict through structural isolation, they fundamentally sever cross-modal synergy and suffer from capacity fragmentation. In this work, we present Symbiotic-MoE, a unified pre-training framework that resolves task interference within a native multimodal Mixture-of-Experts (MoE) Transformers architecture with zero-parameter overhead. We first identify that standard MoE tuning leads to routing collapse, where generative gradients dominate expert utilization. To address this, we introduce Modality-Aware Expert Disentanglement, which partitions experts into task-specific groups while utilizing shared experts as a multimodal semantic bridge. Crucially, this design allows shared experts to absorb fine-grained visual semantics from generative tasks to enrich textual representations. To optimize this, we propose a Progressive Training Strategy featuring differential learning rates and early-stage gradient shielding. This mechanism not only shields pre-trained knowledge from early volatility but eventually transforms generative signals into constructive feedback for understanding. Extensive experiments demonstrate that Symbiotic-MoE achieves rapid generative convergence while unlocking cross-modal synergy, boosting inherent understanding with remarkable gains on MMLU and OCRBench.

---

## 论文详细总结（自动生成）

这是一份关于论文《Symbiotic-MoE: Unlocking the Synergy between Generation and Understanding》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在大型多模态模型（LMM）中，同时实现“理解”与“生成”能力一直面临**灾难性遗忘**的挑战。
*   **核心冲突**：理解任务（多对一映射）与生成任务（一对多映射）在梯度上存在严重冲突。
*   **现有方案局限**：
    *   **标准 MoE**：会导致“路由崩溃”，生成任务的高方差梯度会主导专家利用，冲刷掉预训练的理解知识。
    *   **结构隔离（如 MoT）**：虽然缓解了冲突，但导致了“裂脑”效应，切断了模态间的语义协同，且存在参数效率低和容量碎片化问题。
*   **论文目标**：在零参数开销的前提下，通过原生 MoE 架构实现生成与理解的**共生协同**。

### 2. 论文提出的方法论
Symbiotic-MoE 框架由架构创新和训练策略两部分组成：
*   **模态感知专家解耦 (Modality-Aware Expert Disentanglement)**：
    *   **专家分组**：基于预训练激活模式分析，将专家分为“理解组”（处理文本和 ViT 标记）和“生成组”（处理 VAE 标记）。
    *   **共享专家桥梁 (Shared Experts)**：保留一部分专家由所有模态共享，作为语义桥梁，促进跨模态对齐。
    *   **知识继承初始化**：通过“路由权重切片 (Router Weight Slicing)”，将原路由器的权重直接分配给解耦后的子路由器，实现零冷启动。
*   **渐进式训练策略 (Progressive Training Strategy)**：
    *   **差分学习率 (Differential Learning Rates)**：为生成组件分配较高学习率（1e-4）以快速学习，为理解组件分配极低学习率（1e-6）以保持稳定性。
    *   **预热梯度屏蔽 (Warmup Gradient Shielding)**：在训练初期屏蔽生成任务对共享专家的梯度更新，防止未成熟的生成信号破坏预训练语义，待稳定后再开启双向流动。

### 3. 实验设计
*   **数据集**：使用大规模私有语料库，涵盖文本生成图像（T2I）、长文本生成图像（T2I-Long）、语言模型（LM）和多模态理解（MMU）。
*   **评测基准 (Benchmarks)**：
    *   **理解能力**：MMLU（推理）、OCRBench（感知）、POPE（幻觉检测）、GQA、ChartQA 等。
    *   **生成能力**：COCO-30K（FID、CLIPScore、HPSv2）、T2I-CompBench（属性绑定）。
*   **对比方法**：
    *   **Standard MoE**：原生统一微调。
    *   **MoT (Mixture-of-Transformers)**：物理隔离专家的架构。
    *   **Only_LM_MMU**：仅在理解任务上训练的对照组。

### 4. 资源与算力
*   **硬件**：使用了 **256 台 NVIDIA H20 GPU**。
*   **训练规模**：
    *   **迭代次数**：30,000 次迭代（部分实验展示了扩展至 100k 步的效果）。
    *   **数据量**：总计消耗约 **600 亿 (60B) tokens**。
    *   **批大小**：全局 Batch Size 约为 2500 个样本（约 200 万 tokens/iter）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模预训练实验，而非小规模微调，具有较高的可信度。
*   **消融实验**：非常充分。涵盖了专家分组策略（三模态 vs 二模态）、专家分配比例（96/32 等）、共享专家的必要性、差分学习率的影响以及梯度屏蔽的效果。
*   **客观性**：通过固定随机种子确保确定性，并使用开源工具包（VLMEvalKit）进行标准化评测，对比基准设置公平（相同数据、相同参数量）。

### 6. 论文的主要结论与发现
*   **打破“零和博弈”**：生成训练不仅不会破坏理解能力，反而能作为**视觉正则化器**增强理解。Symbiotic-MoE 在 MMLU 和 OCRBench 上的表现甚至超过了纯理解训练的基准。
*   **路由稳定性**：通过解耦，专家利用率（Capacity Rate）保持在 0.95 以上，远高于标准 MoE，有效解决了路由崩溃。
*   **协同效应**：共享专家捕获了生成任务所需的细粒度空间关系，从而提升了模型在复杂推理和感知任务中的表现。

### 7. 优点
*   **零参数开销**：在不增加推理延迟和参数量的情况下，实现了多任务统一。
*   **架构优雅**：通过逻辑解耦而非物理隔离，保留了模型内部的语义连通性。
*   **训练稳健**：提出的差分学习率和梯度屏蔽策略为解决多模态训练中的“稳定性-塑性”困境提供了有效范式。

### 8. 不足与局限
*   **数据依赖**：实验主要基于私有大规模语料库，社区难以完全复现其数据分布。
*   **生成分辨率限制**：预训练阶段图像分辨率限制在 256x256 级别，尚未展示在高分辨率（如 1024x1024）下的扩展表现。
*   **架构特定性**：该方法高度依赖 MoE 架构，对于稠密（Dense）模型的普适性有待验证。
*   **计算成本**：虽然参数效率高，但 256 台 H20 的预训练成本对中小型研究机构而言依然巨大。

（完）
