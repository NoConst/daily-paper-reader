---
title: Negative Data Mining for Contrastive Learning in Dense Retrieval at IKEA.com
title_zh: IKEA.com 稠密检索中对比学习的负样本数据挖掘
authors: "Eva Agapaki, Amritpal Singh Gill"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00353v1"
tags: ["query:cir-ml"]
score: 9.5
evidence: 通过结构化负采样和对比学习改进密集检索
tldr: "本研究针对宜家电商检索系统，提出了一种改进对比学习效果的负样本挖掘方法。通过利用产品层级分类和属性构建结构化负采样策略，并引入LLM作为评判者进行大规模相关性评估。实验显示该方法在离线测试中提升了2.6%的准确率，但在A/B测试中未见显著线上提升。研究揭示了高零点击率等用户行为对检索效果转化的影响，强调了训练数据需贴合真实搜索意图的重要性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00353v1/fig-001.webp\", \"caption\": \"Figure 1: (a) Training data generation pipeline consisting of three stages and (b) data mixtures visualization.\", \"page\": 2, \"index\": 1, \"width\": 537, \"height\": 314}]"
motivation: 旨在解决对比学习在密集检索中对高质量负样本的依赖问题，以提升宜家电商平台的搜索相关性。
method: 结合产品分类学与属性进行结构化负采样，并利用大语言模型（LLM）对候选产品进行自动化相关性评分以生成训练数据。
result: "离线实验在加拿大市场实现了2.6%的平均类别准确率提升，但线上A/B测试在长尾查询上未表现出显著的参与度差异。"
conclusion: 尽管硬负样本挖掘能提升检索质量，但必须结合真实用户搜索行为（如零点击模式）来弥合离线评估与线上业务指标之间的差距。
---

## 摘要
对比学习是现代检索系统的核心组件，但其有效性在很大程度上取决于训练过程中使用的负样本质量。在这项工作中，我们提出了一种系统化的方法，通过结构化负采样策略和可扩展的 LLM-as-a-judge 相关性评估，来改进宜家（IKEA）产品搜索的稠密检索。基于宜家搜索引擎的延迟交互（late-interaction）检索架构，我们引入了两个关键贡献：（1）利用产品层级分类和产品属性生成具有语义挑战性的负样本的结构化负采样策略；（2）一种用于生成训练数据的综合性基于 LLM 的评估方法。我们的基于 LLM 的评估系统不是依赖稀疏的人工标注或随机采样，而是为所有候选产品针对每个查询分配评分。我们的方法在加拿大市场的线下真实用户查询实验中实现了 +2.6% 的平均类别准确率提升。然而，我们在长尾查询上的 A/B 测试显示，改进模型与基准模型在用户参与度指标上没有统计学显著差异（p > 0.05）。我们将这一差距归因于用户搜索行为：67% 的热门搜索显示零点击率超过 50%，这表明无论结果排名如何，很大比例的搜索会话都没有产生产品互动。这些发现强调了难负样本挖掘的重要性，但也表明需要将训练数据和线下评估植根于真实用户搜索行为（包括查询意图分布和零点击模式），以弥合线下检索质量与线上用户参与度之间的差距。

## Abstract
Contrastive learning is a core component of modern retrieval systems, but its effectiveness heavily relies on the quality of negative examples used during training. In this work, we present a systematic approach to improving dense retrieval for IKEA product search through structured negative sampling strategies and scalable LLM-as-a-judge relevance evaluation.   Building on IKEA Search Engine's late-interaction retrieval architectures, we introduce two key contributions: (1) structured negative sampling strategies that leverage product hierarchical taxonomy and product attributes to generate semantically challenging negatives, and (2) a comprehensive LLM-based evaluation methodology for generating training data. Rather than relying on sparse human annotations or random sampling, our LLM-based evaluation system allocates a score for all candidate products against each query.   Our methodology achieves +2.6\% average category accuracy on offline real user query experiments on the Canada market. However, our A/B test on long-tail queries showed no statistically significant differences in user engagement metrics between the improved and baseline models ($p > 0.05$). We trace this gap to user search behavior: 67\% of popular searches exhibit zero-click rates above 50\%, indicating that a substantial proportion of search sessions result in no product engagement regardless of result ranking. These findings underscore the importance of hard negative mining but also the need for grounding training data and offline evals in real user search behavior -- including query intent distribution and zero-click patterns -- to bridge the gap between offline retrieval quality and online user engagement.

---

## 论文详细总结（自动生成）

这篇论文针对宜家（IKEA）电商平台的搜索系统，深入探讨了稠密检索（Dense Retrieval）中对比学习的负样本挖掘策略，并分析了离线评估指标与在线业务表现之间的差距。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在电商搜索场景下，如何通过挖掘高质量的“硬负样本”（Hard Negatives）来提升稠密检索模型的性能，以及为什么离线指标的显著提升往往难以转化为在线用户参与度的实际增长。
*   **研究动机**：对比学习的效果高度依赖负样本质量。传统的随机采样或简单的硬负样本挖掘无法捕捉复杂的语义差异（如“白色沙发”与“黑色沙发”的区别）。此外，工业界普遍存在离线评估与在线 A/B 测试结果不一致的“泛化鸿沟”问题。

### 2. 核心方法论
论文提出了一套结合大语言模型（LLM）和产品分类学（Taxonomy）的自动化训练数据生成流水线：
*   **核心思想**：利用 LLM 作为“评判者”（Judge）和“生成器”，结合宜家现有的产品层级和属性数据，构建具有语义挑战性的三元组（查询、正样本、负样本）。
*   **关键技术细节**：
    1.  **类别特征提取**：使用 LLM 从 373 个叶子类别中提取特定属性（如材质、颜色、尺寸、生活场景），确保属性与类别逻辑匹配。
    2.  **合成查询生成**：根据提取的属性生成不同复杂度（简单、中等、复杂）和意图（活动、场景、产品名）的合成查询。
    3.  **结构化负采样策略**：
        *   **仅属性差异**：同类别但属性不同（如：查询“白色沙发”，负样本为“黑色沙发”）。
        *   **跨类别匹配**：属性相同但类别错误（如：查询“白色沙发”，负样本为“白色桌子”）。
        *   **多属性违背**：针对复杂查询，负样本仅在某一个关键属性上不匹配。
    4.  **LLM 自动化标注**：使用 LLM 对“查询-产品”对进行 1-5 分的相关性评分，分值 $\ge 4$ 为正样本，$\le 2$ 为负样本。
*   **检索架构**：采用延迟交互（Late-interaction）模型，通过计算查询和文档 Token 嵌入的最大相似度之和来衡量相关性。

### 3. 实验设计
*   **数据集**：宜家加拿大市场数据（24,350 个产品，373 个叶子类别）。
*   **评估场景**：
    1.  **合成查询基准（Offline）**：70 个与训练分布一致的合成查询。
    2.  **真实查询基准（Offline）**：从搜索日志中抽取的 100 个真实用户查询（涵盖单属性、多属性和产品名）。
    3.  **在线 A/B 测试**：在生产环境针对长尾查询（$\ge 3$ 个单词）进行为期 2 周的 50/50 流量测试。
*   **对比方法（Baseline）**：
    *   **Baseline**：随机负采样。
    *   **HNM**：基础硬负样本挖掘。
    *   **HNM + QE / Full**：引入不同规模的 LLM 查询扩展数据。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA A100 GPU**。
*   **训练时长**：每个模型配置大约需要 **18 小时** 完成训练。
*   **超参数**：学习率 $3 \times 10^{-6}$，Batch Size 为 32，最大文档长度 512 Token，训练步数上限 50 万步。

### 5. 实验数量与充分性
*   **实验规模**：论文对比了 4 种主要的数据混合方案。
*   **充分性与公平性**：
    *   每个配置都使用了 **5 个不同的随机种子** 进行训练，并报告了均值和标准差，具有较好的统计鲁棒性。
    *   通过人工评估验证了 LLM 标注的准确性（与人类一致性达 93%）。
    *   实验涵盖了从合成数据到真实日志，再到在线生产环境的全链路验证，评估维度较为全面。

### 6. 主要结论与发现
*   **硬负样本有效性**：HNM 策略在离线测试中表现最好，相比基准模型在类别准确率（Cat@10）上提升了 **+2.6% 到 +4.3%**。
*   **数据质量 > 数量**：简单增加查询扩展（QE）的数据量（从 7.9M 增加到 8.8M）反而导致性能下降，说明合成数据的分布偏差会损害模型泛化力。
*   **离线-在线鸿沟**：尽管离线指标提升显著，但在线 A/B 测试在点击率、加购率等指标上**无统计学显著差异**（$p > 0.05$）。
*   **原因分析**：
    1.  **意图宽度差异**：真实查询涉及的产品类别平均是合成查询的 7 倍，用户意图更宽泛。
    2.  **零点击行为**：67% 的热门搜索存在超过 50% 的零点击率，说明用户行为受排名之外的因素影响巨大。

### 7. 优点与亮点
*   **工业级落地**：展示了在大型电商平台部署稠密检索的真实挑战，而非仅停留在实验室数据集。
*   **结构化采样**：将产品分类学与 LLM 结合，解决了传统负采样中“逻辑不通”的问题（如给纺织品分配木材属性）。
*   **深度分析**：不仅报告了结果，还深入分析了用户搜索行为（如零点击模式）对实验结论的影响，具有很高的参考价值。

### 8. 不足与局限
*   **评估样本量较小**：离线评估仅使用了 70 和 100 个查询，样本量相对较小，可能存在选择偏差。
*   **A/B 测试限制**：在线测试仅针对长尾查询，可能掩盖了模型在头部或短查询上的表现。
*   **LLM 偏见风险**：训练和评估高度依赖 LLM 生成的合成数据，可能引入 LLM 特有的系统性偏见（如评分过于宽松或意图过于单一）。

（完）
