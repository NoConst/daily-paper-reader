---
title: "MEG-RAG: Quantifying Multi-modal Evidence Grounding for Evidence Selection in RAG"
title_zh: MEG-RAG：量化多模态证据落地以优化 RAG 中的证据选择
authors: "Xihang Wang, Zihan Wang, Chengkai Huang, Quan Z. Sheng, Lina Yao"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24564v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 量化多模态证据落地以进行RAG中的选择
tldr: 针对多模态检索增强生成中难以区分证据真实支持度的问题，本文提出MEG指标，通过语义确定性锚点量化证据贡献。基于此构建MEG-RAG框架，训练多模态重排序器以对齐高价值语义内容。实验证明该方法在M2RAG基准上显著提升了生成准确性和多模态一致性，展现了强大的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24564v1/fig-001.webp\", \"caption\": \"Figure 2: Ablation study on Teacher Model Selection. We compare the performance (CIDEr) of MEG-RAG trained with different teacher models against the Jina Reranker baseline.\", \"page\": 5, \"index\": 1, \"width\": 506, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2604.24564v1/fig-002.webp\", \"caption\": \"Table 1: Main comparison of MEG-RAG against baselines on the𝑀2𝑅𝐴𝐺 benchmark tasks across multiple MLLMs. (B-N = Bleu-N, R-N = Rouge-N, R-L = Rouge-L). Bold indicates the best performance. Naive@𝑘 directly uses the top-𝑘 retrieved documents without reranking, while Jina@𝑘 and MEG-RAG@𝑘 select the top-𝑘 documents after reranking.\", \"page\": 4, \"index\": 2, \"width\": 1056, \"height\": 688}, {\"url\": \"assets/figures/arxiv/2604.24564v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of the MEG-RAG framework. (1) Dataset Construction computes MEG scores to quantify the utility of multimodal documents. (2) Reranker Training learns a multimodal reranker using the constructed MEG dataset with a hybrid objective combining document relevance classification and pairwise ranking. (3) MEG-RAG Inference retrieves candidate documents, reranks them to select the top-𝐾 results, and generates the final answer based on the selected evidence.\", \"page\": 3, \"index\": 3, \"width\": 1056, \"height\": 661}]"
motivation: 现有的多模态检索评估指标依赖启发式位置置信度，无法准确衡量检索证据对答案核心语义的实际贡献。
method: 提出MEG指标并利用语义确定性锚点关注高IDF信息令牌，以此训练多模态重排序器来优化证据选择。
result: 在M2RAG基准测试中，MEG-RAG在多个指标上均优于强基准模型，并展现出良好的跨模型泛化能力。
conclusion: 通过语义接地而非简单的概率分布来优先处理高价值内容，能有效提升多模态大模型生成的准确性与一致性。
---

## 摘要
多模态检索增强生成（MRAG）解决了多模态大语言模型（MLLM）的关键局限性，如幻觉和知识陈旧。然而，当前的 MRAG 系统难以区分检索到的多模态数据是真正支持答案的语义核心，还是仅提供表面相关性。现有指标通常依赖于基于位置的启发式置信度，无法捕捉多模态实体的核心信息密度。为此，我们提出了多模态证据落地（MEG），这是一种量化检索证据贡献的语义感知指标。与标准的置信度度量不同，MEG 利用语义确定性锚定（Semantic Certainty Anchoring），专注于能更好捕捉答案语义核心的高 IDF 信息承载标记。基于 MEG，我们引入了 MEG-RAG 框架，该框架训练一个多模态重排序器，使检索到的证据与真实答案的语义锚点对齐。通过基于语义落地而非标记概率分布来优先处理高价值内容，MEG-RAG 提高了生成输出的准确性和多模态一致性。在 M$^2$RAG 基准测试上的广泛实验表明，MEG-RAG 始终优于强基准模型，并在不同的教师模型中表现出鲁棒的泛化能力。

## Abstract
Multimodal Retrieval-Augmented Generation (MRAG) addresses key limitations of Multimodal Large Language Models (MLLMs), such as hallucination and outdated knowledge. However, current MRAG systems struggle to distinguish whether retrieved multimodal data truly supports the semantic core of an answer or merely provides superficial relevance. Existing metrics often rely on heuristic position-based confidence, which fails to capture the informational density of multimodal entities. To address this, we propose Multi-modal Evidence Grounding (MEG), a semantic-aware metric that quantifies the contribution of retrieved evidence. Unlike standard confidence measures, MEG utilizes Semantic Certainty Anchoring, focusing on high-IDF information-bearing tokens that better capture the semantic core of the answer. Building on MEG, we introduce MEG-RAG, a framework that trains a multimodal reranker to align retrieved evidence with the semantic anchors of the ground truth. By prioritizing high-value content based on semantic grounding rather than token probability distributions, MEG-RAG improves the accuracy and multimodal consistency of generated outputs. Extensive experiments on the M$^2$RAG benchmark show that MEG-RAG consistently outperforms strong baselines and demonstrates robust generalization across different teacher models.

---

## 论文详细总结（自动生成）

### MEG-RAG：量化多模态证据落地以优化 RAG 中的证据选择——论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
多模态检索增强生成（MRAG）旨在通过引入外部多模态知识（文本、图像、音频）来缓解多模态大模型（MLLM）的幻觉和知识滞后问题。然而，现有的 MRAG 系统面临一个核心挑战：**难以区分检索到的证据是真正提供了事实支撑，还是仅仅具有表面上的语义相关性**。
传统的重排序（Rerank）指标往往依赖于启发式的概率分布或位置置信度，无法准确衡量多模态实体对答案核心语义的实际贡献。因此，研究者需要一种能够量化“证据落地（Evidence Grounding）”程度的指标，以确保模型选择的是对生成正确答案最有价值的信息。

#### 2. 方法论：核心思想与关键技术
论文提出了 **MEG-RAG** 框架，其核心在于通过量化证据对模型生成信心的边际贡献来筛选文档。

*   **核心指标：多模态证据落地（MEG）**
    MEG 被定义为：在给定查询 $x$ 和候选文档 $d_i$ 的情况下，教师模型 $\phi$ 生成正确答案 $y$ 的“语义置信度”变化量。
    公式表达为：$MEG(d_i|x) = S_\phi(y|x, d_i) - S_\phi(y|x)$。
    *   正值表示文档提供了关键证据；
    *   负值表示文档引入了干扰或矛盾信息。

*   **关键技术：语义确定性锚定（Semantic Certainty Anchoring）**
    为了避免停用词（如 "the", "is"）干扰置信度计算，该方法利用 **逆文档频率（IDF）** 动态识别信息量大的 Token 作为“锚点”。只有高 IDF 的 Token 会被纳入置信度计算（取其对数概率的平均值），从而使指标聚焦于答案的语义核心。

*   **重排序器训练：混合损失优化**
    基于 MEG 分数构建数据集，训练一个多模态重排序器 $f_\theta$，采用双重目标：
    1.  **分类损失（CE Loss）**：学习文档与查询之间的绝对相关性（二分类）。
    2.  **排序损失（RankNet Loss）**：通过成对比较（Pairwise），学习文档之间的相对效用排序。
    最终损失函数为两者的加权融合：$L_{hybrid} = \alpha \cdot L_{CE} + (1-\alpha) \cdot L_{RankNet}$。

#### 3. 实验设计
*   **数据集与场景**：使用了 **$M^2RAG$** 综合基准，涵盖三个典型任务：
    1.  **多模态问答（MMQA）**：合成多模态信息回答问题。
    2.  **图像描述（Image Captioning）**：结合检索背景生成描述。
    3.  **事实核查（Fact Verification）**：验证多模态主张的真实性。
*   **对比基准（Baselines）**：
    *   **Naive-MRAG**：直接使用检索到的 Top-K 文档，不进行重排。
    *   **Jina Reranker**：目前业界领先的多模态重排序模型。
*   **评估模型**：在 LLaVA-NeXT、InternVL-3.5、Qwen2.5-VL 等多个主流 MLLM 上进行了验证。

#### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA A100 (80GB)** GPU。
*   **训练细节**：
    *   训练时长：3 个 Epoch。
    *   优化器：AdamW，学习率 2e-5。
    *   数据规模：构建了包含 **62,000 个三元组**（1:1 正负样本比例）的平衡训练集。

#### 5. 实验数量与充分性
论文进行了多维度的实验，整体非常充分且客观：
*   **主实验**：在 4 种不同的 MLLM 和 3 个跨维度任务上进行了对比，证明了方法的普适性。
*   **消融实验**：验证了混合损失函数的必要性，并测试了不同“教师模型”（如 Qwen 与 Llama 系列）对 MEG 计算的影响，证明了该方法不依赖于特定模型的偏见。
*   **敏感性分析**：对损失权重 $\alpha$ 和 MEG 阈值 $\tau$ 进行了网格搜索，确定了最优超参数配置。
*   **公平性**：所有对比均在相同的检索候选集下进行，确保了重排序效果的纯粹对比。

#### 6. 主要结论与发现
*   **性能领先**：MEG-RAG 在所有任务上均显著优于 Jina Reranker。例如，在 MMQA 任务中，CIDEr 指标提升了约 7.0%~7.3%；在图像描述任务中，性能提升甚至高达 61.1%。
*   **强大的泛化性**：尽管重排序器仅在 MMQA 数据上训练，但在图像描述和事实核查任务中依然表现优异，证明 MEG 捕捉的是通用的“证据-答案”逻辑关系。
*   **教师模型鲁棒性**：使用不同的 MLLM 作为教师来生成 MEG 标签，最终训练出的学生重排序器性能相近，说明 MEG 指标具有客观的语义价值。

#### 7. 优点（亮点）
*   **语义感知度高**：通过 IDF 锚定机制，有效解决了传统概率指标被高频无意义词汇“稀释”的问题。
*   **计算高效**：相比于需要计算全量词表分布的 KL 散度，MEG 仅需计算正确答案 Token 的概率，更适合大规模 MLLM 环境。
*   **即插即用**：作为一个独立的重排序模块，可以无缝集成到任何现有的 MRAG 管道中。

#### 8. 不足与局限
*   **训练数据源单一**：目前的重排序器主要基于 MMQA 任务构建，虽然表现出跨任务泛化，但如果能加入更多样化的多模态任务数据进行联合训练，效果可能更稳健。
*   **超参数依赖**：MEG 的阈值 $\tau$ 和损失权重 $\alpha$ 对最终效果有一定影响，在不同领域迁移时可能需要重新微调。
*   **模态覆盖**：实验主要集中在文本和图像，对于音频、视频等多模态证据的落地量化尚待进一步验证。

（完）
