---
title: "GAP-MLLM: Geometry-Aligned Pre-training for Activating 3D Spatial Perception in Multimodal Large Language Models"
title_zh: GAP-MLLM：用于激活多模态大语言模型3D空间感知的几何对齐预训练
authors: "Jiaxin Zhang, Junjun Jiang, Haijie Li, Youyu Chen, Kui Jiang, Dave Zhenyu Chen"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16461v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 多模态大模型的几何对齐预训练
tldr: 针对多模态大语言模型（MLLM）在纯RGB输入下3D空间感知能力不足的问题，本文提出GAP-MLLM预训练范式。该方法指出性能瓶颈在于文本主导的微调未能有效激活几何表示。通过引入视觉提示的联合任务（预测稀疏点图和语义标签）以及带门控机制的多级渐进融合模块，GAP-MLLM在不损害语义推理的前提下显著增强了几何特征融合，在3D视觉定位、描述和检测任务中表现优异。
selection_source: fresh_fetch
motivation: 现有MLLM在处理3D空间感知时，因训练范式失配导致无法有效激活和利用图像中的隐式几何先验。
method: 提出几何对齐预训练范式，结合预测稀疏点图的视觉提示任务和带有令牌级门控的多级渐进融合模块。
result: 在3D视觉定位、3D密集描述和3D视频目标检测等多个基准测试中，该方法显著提升了模型的空间感知性能。
conclusion: GAP-MLLM通过显式的几何对齐预训练成功激活了MLLM的3D结构感知能力，证明了优化训练范式对提升空间理解的重要性。
---

## 摘要
多模态大语言模型（MLLMs）展现出卓越的语义推理能力，但在仅限于纯RGB输入时，难以处理3D空间感知。尽管利用了来自3D重建模型的隐式几何先验，但与使用显式3D数据的方法相比，基于图像的方法仍存在显著的性能差距。我们认为，这种差距并非源于几何先验不足，而是源于训练范式的失配：以文本为主导的微调未能激活MLLMs内部的几何表示。现有方法通常采用简单的特征拼接，并在没有特定几何监督的情况下直接针对下游任务进行优化，导致结构利用率不佳。为解决这一局限性，我们提出了GAP-MLLM，一种几何对齐预训练范式，在下游适配之前显式激活结构感知。具体而言，我们引入了一项视觉提示的联合任务，迫使MLLMs在预测语义标签的同时预测稀疏点图，从而强化几何意识。此外，我们设计了一个带有Token级门控机制的多级渐进融合模块，能够在不抑制语义推理的情况下实现几何先验的自适应集成。大量实验表明，GAP-MLLM显著增强了几何特征融合，并在3D视觉定位、3D密集描述和3D视频目标检测任务中持续提升了性能。

## Abstract
Multimodal Large Language Models (MLLMs) demonstrate exceptional semantic reasoning but struggle with 3D spatial perception when restricted to pure RGB inputs. Despite leveraging implicit geometric priors from 3D reconstruction models, image-based methods still exhibit a notable performance gap compared to methods using explicit 3D data. We argue that this gap does not arise from insufficient geometric priors, but from a misalignment in the training paradigm: text-dominated fine-tuning fails to activate geometric representations within MLLMs. Existing approaches typically resort to naive feature concatenation and optimize directly for downstream tasks without geometry-specific supervision, leading to suboptimal structural utilization. To address this limitation, we propose GAP-MLLM, a Geometry-Aligned Pre-training paradigm that explicitly activates structural perception before downstream adaptation. Specifically, we introduce a visual-prompted joint task that compels the MLLMs to predict sparse pointmaps alongside semantic labels, thereby enforcing geometric awareness. Furthermore, we design a multi-level progressive fusion module with a token-level gating mechanism, enabling adaptive integration of geometric priors without suppressing semantic reasoning. Extensive experiments demonstrate that GAP-MLLM significantly enhances geometric feature fusion and consistently enhances performance across 3D visual grounding, 3D dense captioning, and 3D video object detection tasks.

---

## 论文详细总结（自动生成）

以下是对论文《GAP-MLLM: Geometry-Aligned Pre-training for Activating 3D Spatial Perception in Multimodal Large Language Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在语义推理方面表现卓越，但在仅依赖纯RGB输入时，其**3D空间感知能力**较弱。虽然现有研究尝试引入从前馈重建模型（如DUSt3R、VGGT）获得的隐式几何先验，但效果仍显著差于使用显式3D数据（如点云）的方法。
本文认为，这种差距并非源于几何先验本身不足，而是由于**训练范式的失配**：现有的微调过程由文本目标主导，导致模型内部的几何表示未能被有效“激活”，几何特征往往被强大的语义特征所掩盖。

### 2. 方法论
论文提出了 **GAP-MLLM**，其核心思想是在下游任务适配之前，通过显式的几何对齐预训练来激活模型的结构感知能力。

*   **稀疏几何-语义联合预训练（Sparse Joint Pre-training）**：
    *   引入一项视觉提示任务：给定图像序列和红叉标记的像素点，要求模型同时预测该点的**3D度量坐标（Pointmap）**和**语义标签**。
    *   这种联合监督迫使模型将几何结构视为核心能力而非辅助特征，防止几何信息在语言监督下被抑制。
*   **多级渐进融合模块（Multi-level Progressive Fusion）**：
    *   **Token级门控机制**：在几何编码器和视觉编码器的多个层级（不仅是最后一层）进行融合。通过门控系数动态平衡几何与语义贡献。
    *   **分层注入**：将融合后的中间层Token注入到LLM解码器的早期块中，确保几何信号在推理过程中得以保留。
*   **统一坐标系统**：所有任务均采用以第一帧为基准的度量坐标系，确保空间预测的一致性。

### 3. 实验设计
*   **数据集与场景**：
    *   **预训练**：使用 ScanNet 和 EmbodiedScan 的 500K 稀疏样本。
    *   **下游任务**：3D视觉定位（ScanRefer）、3D密集描述（Scan2Cap）、3D视频目标检测（EmbodiedScan）。
*   **Benchmark 与对比方法**：
    *   对比了显式3D输入模型（如 3D-LLM, LLaVA-3D, Video-3D LLM）。
    *   对比了基于隐式先验的RGB模型（如 VG-LLM, SpatialLM）。
    *   在度量重建任务中对比了 CUT3R 和 MapAnything。

### 4. 资源与算力
*   **模型规格**：主模型基于 Qwen3-VL-2B（视觉/语言后端）和 VGGT-1B（几何编码器），总参数量约 3B。
*   **训练细节**：使用 Adam 优化器，预训练和微调各进行 1 个 epoch；学习率峰值 1e-5；预训练 Batch Size 为 32，微调为 16。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号和数量，但提到该工作是在华为实习期间完成，推测使用了大规模计算集群。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三大主流 3D 感知任务及度量重建探测实验。
*   **消融实验**：针对“预训练任务的有无”、“语义监督的作用”、“不同融合策略（加法、交叉注意力、门控）”以及“两阶段定位框架”进行了详尽的对比。
*   **客观性**：实验不仅在自研模型上验证，还将 GAP 范式应用于竞争对手模型（如 VG-LLM），均取得了显著提升，证明了该方法的普适性和公平性。

### 6. 主要结论与发现
*   **激活是关键**：简单的特征融合不足以发挥几何先验的作用，显式的几何对齐预训练能显著提升模型对空间结构的利用率。
*   **语义辅助几何**：在预训练中加入语义标签预测，有助于稳定度量结构的重建效果。
*   **多级融合优于单级**：几何信息在编码器的不同层级具有不同的抽象程度，多级门控融合能更好地捕捉细粒度结构。
*   **性能突破**：GAP-MLLM 在纯 RGB 输入下，在 3D 视觉定位等任务上达到了接近甚至超越部分显式 3D 输入模型的水平。

### 7. 优点（亮点）
*   **范式创新**：从“如何融合特征”转向“如何激活表示”，为 MLLM 的空间感知提供了新视角。
*   **高效性**：仅需极少量的稀疏像素级监督（等效于两张全分辨率图像的像素量）即可有效激活 3D 感知。
*   **架构兼容性**：提出的预训练和融合方案可以轻松迁移至不同的 MLLM 基座。

### 8. 不足与局限
*   **依赖外部编码器**：模型性能在很大程度上受限于冻结的几何编码器（如 VGGT）的质量。
*   **重建精度限制**：由于采用稀疏监督，模型生成的点图在局部细节和边缘锐度上仍有提升空间。
*   **推理开销**：引入并行的几何编码器和多级融合机制会增加模型的计算负担和显存占用。
*   **应用范围**：目前主要集中在室内场景，在室外大规模场景或高速动态场景下的表现尚未验证。

（完）
