---
title: "ReRec: Reasoning-Augmented LLM-based Recommendation Assistant via Reinforcement Fine-tuning"
title_zh: ReRec：通过强化微调实现的推理增强型大语言模型推荐助手
authors: "Jiani Huang, Shijie Wang, Liangbo Ning, Wenqi Fan, Qing Li"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07851v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 使用大语言模型进行个性化推理驱动的推荐
tldr: 针对大语言模型在复杂推荐任务中推理能力不足的问题，本文提出ReRec框架。该框架通过强化微调（RFT）提升模型的推理性能，核心包括双图增强奖励塑造、推理感知优势估计和在线课程调度器。实验证明ReRec在推荐准确性上超越了现有SOTA模型，同时保持了良好的指令遵循和通用知识能力。
selection_source: fresh_fetch
motivation: 现有大语言模型推荐系统在处理复杂查询和多步推理时存在局限，亟需增强其推理驱动的个性化推荐能力。
method: 提出ReRec框架，利用双图增强奖励、推理片段分解及动态课程调度进行强化微调。
result: 实验结果显示ReRec在多个指标上优于最先进的基准模型，并有效保留了模型的通用能力。
conclusion: 通过强化微调结合推理增强，可以显著提升大语言模型在复杂推荐场景下的表现和逻辑性。
---

## 摘要
随着大语言模型（LLMs）的兴起，对能够处理复杂查询并提供个性化、推理驱动推荐的智能推荐助手的需求日益增长。基于大语言模型的推荐系统展现出潜力，但在多步推理方面面临挑战，这凸显了对推理增强系统的需求。为了填补这一空白，我们提出了 ReRec，这是一种新型的强化微调（RFT）框架，旨在提升大语言模型在复杂推荐任务中的推理能力。我们的框架引入了三个关键组件：(1) 双图增强奖励塑造（Dual-Graph Enhanced Reward Shaping），将 NDCG@K 等推荐指标与查询对齐和偏好对齐分数相结合，为大语言模型优化提供细粒度的奖励信号；(2) 推理感知优势估计（Reasoning-aware Advantage Estimation），将大语言模型的输出分解为推理片段并对错误步骤进行惩罚，以增强推荐的推理能力；(3) 在线课程调度器（Online Curriculum Scheduler），动态评估查询难度并组织训练课程，以确保强化微调过程中的稳定学习。实验表明，ReRec 优于最先进的基准模型，并保留了指令遵循和通用知识等核心能力。我们的代码可在 https://github.com/jiani-huang/ReRec 获取。

## Abstract
With the rise of LLMs, there is an increasing need for intelligent recommendation assistants that can handle complex queries and provide personalized, reasoning-driven recommendations. LLM-based recommenders show potential but face challenges in multi-step reasoning, underscoring the need for reasoning-augmented systems. To address this gap, we propose ReRec, a novel reinforcement fine-tuning (RFT) framework designed to improve LLM reasoning in complex recommendation tasks. Our framework introduces three key components: (1) Dual-Graph Enhanced Reward Shaping, integrating recommendation metrics like NDCG@K with Query Alignment and Preference Alignment Scores to provide fine-grained reward signals for LLM optimization; (2) Reasoning-aware Advantage Estimation, which decomposes LLM outputs into reasoning segments and penalizes incorrect steps to enhance reasoning of recommendation; and (3) Online Curriculum Scheduler, dynamically assess query difficulty and organize training curriculum to ensure stable learning during RFT. Experiments demonstrate that ReRec outperforms state-of-the-art baselines and preserves core abilities like instruction-following and general knowledge. Our codes are available at https://github.com/jiani-huang/ReRec.

---

## 论文详细总结（自动生成）

以下是对论文《ReRec: Reasoning-Augmented LLM-based Recommendation Assistant via Reinforcement Fine-tuning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是**如何提升大语言模型（LLM）在处理复杂、推理驱动的推荐查询时的表现**。
*   **研究背景**：传统的推荐系统（如 MF、GNN）依赖历史点击，难以理解复杂的自然语言需求；而现有的 LLM 推荐助手在面对需要多步推理（如从描述中推断电影、识别演员、再推荐该演员的其他作品）的复杂查询时，往往表现不佳。
*   **研究动机**：虽然强化微调（RFT）在数学和代码领域取得了成功，但直接应用于推荐任务面临三大挑战：
    1.  **奖励信号稀疏且粗糙**：传统的推荐指标（如 NDCG）仅关注最终匹配，无法为“接近正确”的推荐提供反馈。
    2.  **缺乏中间推理监督**：现有的 RFT 方法通常对整个回复给出统一奖励，无法识别并纠正中间推理步骤中的错误。
    3.  **训练不稳定**：推荐任务与语言生成的鸿沟导致训练初期难以收敛。

### 2. 方法论：核心思想与关键技术
论文提出了 **ReRec** 框架，通过以下三个核心组件优化 LLM 的推理推荐能力：
*   **双图增强奖励塑造 (Dual-Graph Enhanced Reward Shaping)**：
    *   不再只依赖 NDCG，而是引入了两个细粒度分数：
        *   **查询对齐分数 (QAS)**：利用“项-属性图”，计算推荐项与真实项在属性（导演、演员等）上的重合度。
        *   **偏好对齐分数 (PAS)**：利用预训练的轻量级模型（如 LightGCN）在“用户-项交互图”上计算嵌入向量的余弦相似度，捕捉隐含偏好。
*   **推理感知优势估计 (Reasoning-aware Advantage Estimation, RAAE)**：
    *   将 LLM 的输出分解为多个推理片段（Segments）。
    *   如果某个片段讨论了错误的项并最终导致了错误推荐，则对该片段对应的 Token 进行惩罚（降低其优势值值），从而实现对推理过程的细粒度监督。
*   **在线课程调度器 (Online Curriculum Scheduler)**：
    *   动态评估查询难度：根据模型在上一轮（Epoch）中的平均奖励来定义难度。
    *   自动过滤已掌握的简单样本，并按难度由易到难重新组织训练数据，确保学习过程平滑稳定。

### 3. 实验设计
*   **数据集**：使用 **RecBench+** 评测基准，涵盖电影（Movie）和图书（Book）两个领域。查询被细分为：显式条件（简单）、隐式条件（中等）、错误信息条件（困难）、基于兴趣和基于人口统计学五类。
*   **Benchmark 与对比方法**：
    *   **LLM 基座**：Qwen-2.5-3B, Llama-3.2-3B, GPT-4o, DeepSeek-R1 等。
    *   **LLM 推荐模型**：TallRec, InteRecAgent, CRAG。
    *   **RFT 基准算法**：GRPO, REINFORCE++, RLOO。
*   **评估指标**：主要使用准确率（Accuracy），并在候选集（1个正样本+19个负样本）中进行测试。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 张 NVIDIA H20 GPU (每张 96GB 显存)**。
*   **软件框架**：基于 PyTorch 2.6.0, Verl 0.3.1, VLLM 0.8.5 和 Ray 2.46.0。
*   **训练细节**：学习率 5e-6，Group Size 为 5，最大响应长度 768 Token，训练最多 15 个 Epoch（带早停机制）。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个领域（电影、图书）分别使用了 10,000 条查询进行训练，12,000 条进行测试。
*   **实验维度**：
    1.  **主实验**：对比了 10 余种基准模型。
    2.  **个性化实验**：验证模型结合用户历史记录的能力。
    3.  **泛化性实验**：包括跨领域（电影训练、图书测试）和跨任务（迁移到序列推荐任务）。
    4.  **能力保留实验**：测试 RFT 后模型在通用推理（DROP）、指令遵循（IFEval）、科学常识（ARC/GPQA）上的表现。
    5.  **消融实验**：逐一验证 QAS、PAS、RAAE 和课程调度器的有效性。
*   **评价**：实验设计非常全面，不仅关注推荐准确度，还深入探讨了 RFT 对模型通用智能的影响，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能卓越**：ReRec 在所有复杂查询类别下均显著优于现有 RFT 方法和专门的推荐 LLM。在困难任务（含错误信息的查询）上，相比未微调模型提升了 **440%**。
*   **泛化能力强**：在电影领域训练的模型在图书领域依然表现出色，且能有效迁移到序列推荐任务，达到专业模型（如 SASRec）90% 的水平。
*   **保留通用性**：与 SFT（监督微调）导致严重的“灾难性遗忘”不同，ReRec 在提升推荐能力的同时，几乎完全保留了模型的通用知识和指令遵循能力，甚至提升了推理分数。

### 7. 优点（亮点）
*   **奖励机制创新**：通过双图（属性图和交互图）解决了推荐任务中奖励信号过于稀疏的问题。
*   **过程监督**：RAAE 机制实现了对推理步骤的 Token 级惩罚，这在推荐 LLM 领域具有前瞻性。
*   **动态学习**：在线课程调度器解决了 RFT 训练不稳定的痛点，无需额外模型即可实现自适应学习。

### 8. 不足与局限
*   **单轮对话限制**：目前的研究主要集中在单轮复杂查询，尚未扩展到多轮对话（Multi-turn）场景，而现实中的推荐助手往往需要多轮交互。
*   **推理开销**：虽然模型参数量较小（3B），但 RFT 过程中的多次 Rollout（采样）和推理步骤的增加会带来额外的计算开销。
*   **负样本构建**：实验中使用了 1:19 的负采样比例，在极大规模候选集的真实场景下，性能表现仍有待验证。

（完）
