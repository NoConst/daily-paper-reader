---
title: "OCR-Memory: Optical Context Retrieval for Long-Horizon Agent Memory"
title_zh: OCR-Memory：用于长时程智能体记忆的光学上下文检索
authors: "Jinze Li, Yang Zhang, Xin Yang, Jiayi Qu, Jinfeng Xu, Shuo Yang, Junhua Ding, Edith Cheuk-Han Ngai"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26622v1"
tags: ["query:cir-ml"]
score: 10.0
evidence: 利用视觉模态进行长时程智能体记忆的光学上下文检索
tldr: 针对LLM智能体在长程任务中面临的文本上下文限制和信息丢失问题，本文提出OCR-Memory框架。该方法将历史轨迹渲染为带有视觉标识符的高密度图像，通过“定位并转录”范式检索精确的原始文本。这种光学编码方式在极低提示开销下实现了海量历史信息的保留，显著提升了智能体在长程基准测试中的记忆容量和证据恢复的准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26622v1/fig-001.webp\", \"caption\": \"Table 1: Main Results on Long-Horizon Agent Tasks. We report fine-grained metrics for Mind2Web, including Element Accuracy, Action F1 Score, Step Success Rate, and Task Success Rate. For AppWorld, we report Success Rates across three difficulty levels and the overall Average. OCR-Memory consistently outperforms baselines, achieving significant gains in metrics requiring precise structural grounding.\", \"page\": 6, \"index\": 1, \"width\": 808, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2604.26622v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of the OCR-Memory. The system enables long-horizon agent memory by storing interaction histories as compressed multi-resolution images (left). To retrieve information, we employ a Locate-and-Transcribe paradigm: the model scans the visual history annotated with Set-of-Mark (SoM) visual anchors (center) to predict the index of relevant segments. Finally, the verbatim text corresponding to the selected index is deterministically fetched (right), avoiding generation-based hallucinations and minimizing token usage.\", \"page\": 3, \"index\": 2, \"width\": 824, \"height\": 464}]"
motivation: 现有的智能体记忆系统受限于文本上下文预算，在存储原始轨迹的高成本与摘要提取的信息损失之间难以权衡。
method: 提出OCR-Memory框架，将历史轨迹渲染为包含视觉锚点的图像，并采用“定位并转录”模式检索精确的原始文本。
result: 在长程智能体基准测试中，该方法在严格的上下文限制下取得了持续的性能提升，并有效减少了幻觉。
conclusion: OCR-Memory通过视觉模态实现了高密度的经验表示，为长程智能体提供了一种高效且保真的记忆存储与检索方案。
---

## 摘要
自主大语言模型（LLM）智能体越来越多地在长时程、交互式环境中运行，其成功取决于对长期积累经验的复用。然而，现有的智能体记忆系统从根本上受到文本上下文预算的限制：存储或回顾原始轨迹的 Token 开销极高，而摘要和纯文本检索则以信息丢失和证据碎片化为代价来节省 Token。为了解决这一局限性，我们提出了光学上下文检索记忆（OCR-Memory），这是一种利用视觉模态作为智能体经验高密度表示的记忆框架，能够在检索时以极低的提示词开销保留任意长度的历史记录。具体而言，OCR-Memory 将历史轨迹渲染为带有唯一视觉标识符注释的图像。OCR-Memory 通过“定位并转录”（locate-and-transcribe）范式检索存储的经验，该范式通过视觉锚点选择相关区域并检索相应的逐字文本，从而避免自由生成并减少幻觉。在长时程智能体基准测试上的实验表明，在严格的上下文限制下，该方法取得了持续的性能提升，证明了光学编码在增加有效记忆容量的同时，能够保持忠实的证据恢复。

## Abstract
Autonomous LLM agents increasingly operate in long-horizon, interactive settings where success depends on reusing experience accumulated over extended histories. However, existing agent memory systems are fundamentally constrained by text-context budgets: storing or revisiting raw trajectories is prohibitively token-expensive, while summarization and text-only retrieval trade token savings for information loss and fragmented evidence. To address this limitation, we propose Optical Context Retrieval Memory (OCR-Memory), a memory framework that leverages the visual modality as a high-density representation of agent experience, enabling retention of arbitrarily long histories with minimal prompt overhead at retrieval time. Specifically, OCR-Memory renders historical trajectories into images annotated with unique visual identifiers. OCR-Memory retrieves stored experience via a \emph{locate-and-transcribe} paradigm that selects relevant regions through visual anchors and retrieves the corresponding verbatim text, avoiding free-form generation and reducing hallucination. Experiments on long-horizon agent benchmarks show consistent gains under strict context limits, demonstrating that optical encoding increases effective memory capacity while preserving faithful evidence recovery.

---

## 论文详细总结（自动生成）

这是一份关于论文《OCR-Memory: Optical Context Retrieval for Long-Horizon Agent Memory》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**长时程（Long-Horizon）自主智能体在记忆保留与检索中的瓶颈问题**。
*   **研究背景**：LLM 智能体在处理连续任务（如网页自动化、App 操作）时，需要复用历史经验。
*   **核心矛盾**：现有的记忆系统面临“上下文预算”的限制。存储原始文本轨迹（Raw Trajectories）会导致 Token 开销爆炸；而采用摘要（Summarization）或纯文本检索（RAG）则会造成关键细节（如错误信息、结构化布局）的丢失或证据碎片化。
*   **整体含义**：论文提出将视觉模态作为高密度的经验表示媒介，利用光学编码（将文本渲染为图像）来实现海量历史记录的低成本、无损存储与精确检索。

### 2. 方法论：核心思想与关键技术
OCR-Memory 的核心思想是**“光学压缩存储 + 定位转录检索”**。

*   **核心流程**：
    1.  **视觉存储（Visual Memory Bank）**：将智能体的交互轨迹（推理链、工具调用、环境反馈）渲染成带有视觉锚点的图像。
    2.  **定位并转录（Locate-and-Transcribe）**：
        *   **视觉锚点（Set-of-Mark, SoM）**：在图像中为每个文本段落标注红色边界框和唯一数字 ID。
        *   **判别式检索**：检索模型（基于 DeepSeek-OCR）接收查询和图像，不直接生成文本，而是输出相关段落的 ID（二进制相关性向量）。
        *   **确定性提取**：根据预测的 ID，从外部原始日志中直接提取对应的逐字文本。这种设计彻底消除了生成式检索中的幻觉问题。
    3.  **多分辨率与主动召回（Multi-Resolution & Active Recall）**：
        *   **模拟人类记忆**：新记忆以高分辨率存储，旧记忆自动降采样为低分辨率“缩略图”以节省 Token。
        *   **动态刷新**：一旦低分辨率记忆被检索命中，系统会立即将其重新渲染为高分辨率，确保后续推理的精确性。

### 3. 实验设计
*   **数据集/场景**：
    *   **Mind2Web**：跨域网页导航任务，评估智能体在复杂 DOM 结构中的操作能力。
    *   **AppWorld**：API 交互任务，侧重于评估智能体在长程历史回溯中的表现。
*   **Benchmark 与对比方法**：
    *   **对比基准**：Zero-Shot（无记忆）、标准文本 RAG（基于向量相似度）。
    *   **先进方法**：MemoryBank（长期记忆管理）、AWM（智能体工作流记忆）、ACON（上下文压缩优化）。
*   **评估指标**：元素准确率（Ele Acc）、动作 F1 值、步骤成功率（Step SR）、任务成功率（Task SR）。

### 4. 资源与算力
*   **模型架构**：检索器采用 **DeepSeek-OCR (3B)** 作为骨干网络；推理智能体主要使用 **GPT-4**，并使用 **Qwen3-32B** 验证泛化性。
*   **训练细节**：使用 LoRA 对 DeepSeek-OCR 的语言解码器进行微调（Rank=16），在 HotpotQA 数据集上训练 3 个 Epoch。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号和数量，但提到了使用 AdamW 优化器、余弦学习率调度以及全局 Batch Size 为 128。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流长程智能体基准上进行了主实验。
    *   **消融实验**：验证了 SoM 机制、多分辨率策略、不同 Token 限制（1k 到 8k）下的表现。
    *   **压力测试**：使用了 Needle-in-a-Haystack（大海捞针）测试，验证在 32k 长度下的检索鲁棒性。
*   **充分性评价**：实验设计较为全面，不仅对比了端到端性能，还深入分析了检索层面的召回率（Recall@K）和内容忠实度（Faithfulness）。通过更换推理后端（GPT-4 到 Qwen3），证明了该记忆框架的独立优越性，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能提升**：OCR-Memory 在所有指标上均优于现有的文本压缩和抽象方法，尤其在“困难”任务中提升显著（AppWorld Hard 任务提升约 3.6%）。
*   **高压缩比**：光学编码实现了超过 **10 倍** 的文本压缩率，同时保持了极高的检索准确度（32k 长度下仍有 94.1% 的准确率）。
*   **低 Token 开销**：在 Mind2Web 任务中，单步推理注入的 Token 从 3980 个降至 596 个（约 6.7 倍缩减）。
*   **零幻觉检索**：通过“定位-提取”范式，实现了 100% 的证据恢复忠实度。

### 7. 优点（亮点）
*   **范式创新**：首次提出将智能体轨迹以图像流形式存储，利用视觉模型强大的空间感知能力解决文本上下文不足的问题。
*   **机制巧妙**：SoM 视觉锚点与确定性提取相结合，既保留了视觉布局信息，又规避了视觉模型易产生字符幻觉的弱点。
*   **资源权衡**：通过“多分辨率”设计，巧妙地在存储成本、计算延迟和记忆精度之间取得了平衡。

### 8. 不足与局限
*   **训练成本**：与无需训练的 RAG 相比，该方法需要针对特定检索任务微调视觉模型。
*   **系统开销**：将日志渲染为图像会增加磁盘存储压力（每集从 18KB 增至 1.47MB）和检索延迟（单步增加约 1.4 秒）。
*   **部署复杂性**：需要同时维护视觉编码器和主语言模型，增加了显存占用。
*   **应用限制**：对于纯文本环境且不具备视觉渲染能力的系统，该方法的预处理成本较高。

（完）
