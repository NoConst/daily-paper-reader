---
title: "Dual Causal Inference: Integrating Backdoor Adjustment and Instrumental Variable Learning for Medical VQA"
title_zh: 双重因果推理：集成后门调整与工具变量学习的医疗视觉问答
authors: "Zibo Xu, Qiang Li, Ke Lu, Jin Wang, Weizhi Nie, Yuting Su"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20306v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 解决多模态数据中的跨模态混杂效应和内在偏见
tldr: 针对医疗视觉问答中模型易受多模态偏见干扰的问题，本文提出双重因果推理（DCI）框架。该框架首次结合后门调整与工具变量学习，分别处理可观测和不可观测的混杂因素。通过结构因果模型和互信息约束，DCI能提取去混杂表示并捕捉真实因果关系，在多个基准数据集上显著提升了模型的泛化能力、鲁棒性及推理的可解释性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20306v1/fig-001.webp\", \"caption\": \"TABLE I COMPARISON OF ACCURACY WITH SOTA METHODS ON THREE MEDVQA DATASETS. QUESTIONS ARE CATEGORIZED INTO OPEN AND CLOSED TYPES. “OVERALL” REFERS TO THE ACCURACY ACROSS THE ENTIRE DATASET. THE SECOND-BEST RESULTS ARE UNDERLINED.\", \"page\": 7, \"index\": 1, \"width\": 854, \"height\": 358}, {\"url\": \"assets/figures/arxiv/2604.20306v1/fig-002.webp\", \"caption\": \"Fig. 4. Hyperparameter sensitivity of the dictionary size K.\", \"page\": 8, \"index\": 2, \"width\": 507, \"height\": 294}]"
motivation: 现有MedVQA模型易受多模态数据中观测和未观测混杂因素的影响，导致推理结果不可靠。
method: 提出DCI框架，集成后门调整和工具变量学习，通过互信息约束提取去混杂的跨模态表示。
result: 在四个主流MedVQA数据集上的实验证明，该方法在分布外泛化性能上显著优于现有模型。
conclusion: DCI通过显式解耦因果效应与虚假相关性，有效增强了医疗视觉问答的可靠性与鲁棒性。
---

## 摘要
医疗视觉问答 (MedVQA) 旨在根据复杂的医学图像和问题生成临床可靠的答案。然而，现有方法往往过拟合于表层的跨模态相关性，忽略了多模态医学数据中嵌入的内在偏见。因此，模型容易受到跨模态混杂效应的影响，严重阻碍了其提供可靠诊断推理的能力。为了解决这一局限性，我们为 MedVQA 提出了一种新颖的双重因果推理 (DCI) 框架。据我们所知，DCI 是首个集成后门调整 (BDA) 和工具变量 (IV) 学习的统一架构，旨在共同处理可观测和不可观测的混杂因素。具体而言，我们构建了一个结构因果模型 (SCM)，其中可观测的跨模态偏见（例如频繁的视觉和文本共现）通过 BDA 得到缓解，而不可观测的混杂因素则利用从共享潜空间学习到的 IV 进行补偿。为了保证 IV 的有效性，我们设计了互信息约束，以最大化其对融合多模态表示的依赖，同时最小化其与不可观测混杂因素及目标答案的关联。通过这种双重机制，DCI 提取了去混杂的表示，从而捕捉真实的因果关系。在 SLAKE、SLAKE-CP、VQA-RAD 和 PathVQA 四个基准数据集上的广泛实验表明，我们的方法始终优于现有方法，特别是在分布外 (OOD) 泛化方面。此外，定性分析证实，DCI 通过显式解耦真实的因果效应与虚假的跨模态捷径，显著增强了跨模态推理的可解释性和鲁棒性。

## Abstract
Medical Visual Question Answering (MedVQA) aims to generate clinically reliable answers conditioned on complex medical images and questions. However, existing methods often overfit to superficial cross-modal correlations, neglecting the intrinsic biases embedded in multimodal medical data. Consequently, models become vulnerable to cross-modal confounding effects, severely hindering their ability to provide trustworthy diagnostic reasoning. To address this limitation, we propose a novel Dual Causal Inference (DCI) framework for MedVQA. To the best of our knowledge, DCI is the first unified architecture that integrates Backdoor Adjustment (BDA) and Instrumental Variable (IV) learning to jointly tackle both observable and unobserved confounders. Specifically, we formulate a Structural Causal Model (SCM) where observable cross-modal biases (e.g., frequent visual and textual co-occurrences) are mitigated via BDA, while unobserved confounders are compensated using an IV learned from a shared latent space. To guarantee the validity of the IV, we design mutual information constraints that maximize its dependence on the fused multimodal representations while minimizing its associations with the unobserved confounders and target answers. Through this dual mechanism, DCI extracts deconfounded representations that capture genuine causal relationships. Extensive experiments on four benchmark datasets, SLAKE, SLAKE-CP, VQA-RAD, and PathVQA, demonstrate that our method consistently outperforms existing approaches, particularly in out-of-distribution (OOD) generalization. Furthermore, qualitative analyses confirm that DCI significantly enhances the interpretability and robustness of cross-modal reasoning by explicitly disentangling true causal effects from spurious cross-modal shortcuts.

---

## 论文详细总结（自动生成）

这是一份关于论文《Dual Causal Inference: Integrating Backdoor Adjustment and Instrumental Variable Learning for Medical VQA》的结构化分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：医疗视觉问答（MedVQA）模型在处理复杂的医学图像和问题时，极易过拟合于数据中的“虚假相关性”（Spurious Correlations）。
*   **研究动机**：
    *   **数据偏见**：医学数据中存在严重的分布不均（如特定病种与特定检查设备、提问方式的强绑定），导致模型学习到的是统计捷径而非真正的临床因果逻辑。
    *   **混杂因素复杂性**：现有的因果推理方法通常只处理**可观测的混杂因素**（如显式的文本频率），而忽略了**不可观测的混杂因素**（如潜在的患者特征、未记录的采集环境差异）。
    *   **跨模态纠缠**：图像和文本的偏见往往是交织在一起的，简单的单模态去偏无法解决深层的多模态混杂效应。

### 2. 方法论：双重因果推理（DCI）框架
DCI 是首个在 MedVQA 领域同时集成**后门调整（BDA）**和**工具变量（IV）**学习的统一框架。
*   **结构因果模型 (SCM)**：构建了一个包含图像(V)、问题(Q)、多模态特征(X)、答案(A)以及可观测混杂项($C_v, C_q$)和不可观测混杂项($C$)的因果图。
*   **后门调整 (BDA) 模块**：
    *   **核心思想**：通过干预操作 $P(A|do(V,Q))$ 切断可观测混杂因素带来的虚假路径。
    *   **技术细节**：构建了动态更新的**视觉混杂字典**（基于 K-Means++ 聚类 ROI 特征）和静态的**文本混杂字典**（基于医学概念）。利用归一化加权几何平均（NWGM）近似计算期望，通过交叉注意力机制实现特征级的去混杂。
*   **工具变量 (IV) 学习模块**：
    *   **核心思想**：针对不可观测的混杂因素 $C$，从数据空间中合成一个潜在工具变量 $I$。
    *   **关键约束**：通过**互信息（MI）瓶颈**强制 $I$ 满足三个条件：
        1.  **相关性**：$I$ 必须与融合特征 $X$ 强相关。
        2.  **无混杂性**：$I$ 必须与不可观测混杂因子 $C$ 独立（通过 CLUB 估计器最小化互信息）。
        3.  **排他性约束**：$I$ 只能通过 $X$ 影响答案 $A$，不能有直接路径。
*   **总体目标函数**：结合了交叉熵损失、互信息约束损失和因果一致性正则化损失。

### 3. 实验设计
*   **数据集**：
    *   **VQA-RAD**：放射学数据集，包含 315 张图像和 3,515 个问答对。
    *   **SLAKE**：双语医疗数据集，涵盖 12 种疾病和 39 个器官。
    *   **SLAKE-CP**：专门用于评估去偏能力的**分布外（OOD）**变体，其训练集和测试集的答案分布被刻意设计为不同。
    *   **PathVQA**：大规模病理图像数据集，包含超过 30,000 个问答对。
*   **对比方法**：包括经典的 MEVF-BAN、基于预训练的 PubMedCLIP、PMC-CLIP，以及最新的因果推理方法如 DeCoCT、CIMB-MVQA 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 张 NVIDIA RTX 4090 GPU**。
*   **训练细节**：采用 AdamW 优化器，Batch Size 为 32，学习率为 $1 \times 10^{-4}$，前 5% 的步骤进行线性预热。
*   **时长**：文中未明确给出具体的总训练时长，但提到在 SLAKE-CP 上表现出极高的训练稳定性（方差极小）。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流基准数据集上进行了测试，涵盖了放射、病理、多器官等多种医疗场景。
*   **消融实验**：详细对比了“仅 BDA”、“仅 IV”以及“全模型”的效果，并深入探讨了互信息损失中各项（$L_{IX}, L_{IC}, L_{IA}$）的作用。
*   **敏感性分析**：对混杂字典的大小 $K$ 进行了参数敏感性测试。
*   **定性分析**：利用 Grad-CAM 生成注意力图，直观展示了模型在“一图多问”场景下如何精准定位因果区域。
*   **评价**：实验设计非常充分，特别是引入 SLAKE-CP 进行 OOD 测试，有力地证明了模型不是在“背答案”而是学习因果逻辑，实验过程客观公平。

### 6. 主要结论与发现
*   **性能领先**：DCI 在所有数据集上均刷新了 SOTA 记录，在 VQA-RAD 上达到 82.5% 的准确率，在 SLAKE 上达到 88.6%。
*   **OOD 泛化能力强**：在 SLAKE-CP 数据集上，DCI 显著超过了之前的最优方法（准确率提升约 3.7%），证明了其对数据偏见的极强鲁棒性。
*   **减少幻觉**：定性分析显示，DCI 能有效缓解“基于问题的幻觉”（即不看图只根据问题关键词瞎猜），使模型的视觉注意力更加聚焦于真实的病灶区域。

### 7. 优点（亮点）
*   **理论严密**：首次将 BDA 和 IV 结合，构建了处理“观测+未观测”双重混杂因素的完整因果闭环。
*   **动态字典机制**：视觉字典采用动量更新和 K-Means++ 初始化，能更好地捕捉医疗图像中多变的病理特征。
*   **互信息约束创新**：通过变分下界和 CLUB 上界精准控制工具变量的生成，解决了潜空间中 IV 难以定义的难题。

### 8. 不足与局限
*   **计算复杂度**：引入互信息估计（如 CLUB）和多重 MLP 投影会增加训练时的计算开销。
*   **超参数依赖**：字典大小 $K$ 和互信息损失的权重 $\lambda$ 对性能有一定影响，在不同模态的数据集上可能需要精细调优。
*   **应用限制**：虽然在病理和放射图像上表现良好，但对于极度稀缺的罕见病数据，字典的构建可能面临冷启动或表示不足的问题。

（完）
