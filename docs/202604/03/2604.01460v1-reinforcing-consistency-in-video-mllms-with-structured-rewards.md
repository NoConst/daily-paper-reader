---
title: Reinforcing Consistency in Video MLLMs with Structured Rewards
title_zh: 通过结构化奖励增强视频多模态大语言模型的一致性
authors: "Yihao Quan, Zeru Shi, Jinman Zhao, Ruixiang Tang"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01460v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 视频多模态大语言模型中的组合一致性
tldr: 针对视频多模态大模型（MLLM）在视觉和时间对齐上的幻觉问题，本文提出了一种基于结构化奖励的强化学习方法。研究发现，模型的高层预测往往缺乏底层事实支撑，传统的句子级奖励难以精确定位错误。为此，作者设计了包含场景图、时间顺序及VQA自验证的结构化奖励机制，通过细粒度的反馈引导模型建立更忠实的视频理解能力，在多个基准测试中显著提升了性能。
selection_source: fresh_fetch
motivation: 现有的视频MLLM在生成看似合理的回答时，常存在物体虚构、属性错误或时间顺序混乱等底层事实不一致的问题。
method: 提出一种结构化奖励机制，整合了实例感知的场景图奖励、时间顺序奖励以及基于视频的VQA自验证奖励。
result: 在时间理解、通用视频理解及幻觉评估等多个基准测试中，该方法在开源模型上均取得了显著且一致的性能提升。
conclusion: 结构化奖励塑造是提升视频MLLM忠实度和理解能力的有效途径，能比粗粒度的句子级奖励更精准地优化模型。
---

## 摘要
多模态大语言模型（MLLMs）在视频理解方面取得了显著进展。然而，看似合理的输出往往存在视觉和时间定位（grounding）不佳的问题：模型可能会虚构物体的存在、分配错误的属性或合并重复事件，但仍生成全局合理的描述或回答。我们通过组合式一致性审计研究了这种失效模式，该审计将描述分解为支持性的事实和时间主张，旨在调查正确的高层预测是否确实得到了有效的底层证据支持。我们的自顶向下审计显示，即使是正确的根关系主张也往往缺乏可靠的属性和存在性支持。这表明标准的句子级监督是忠实视频理解的一个微弱代理。此外，当转向强化学习（RL）以寻求更好的对齐时，标准的句子级奖励往往被证明过于粗糙，无法准确地定位特定的定位失败。为解决这一问题，我们用由事实和时间单元构建的结构化奖励取代了通用的句子级奖励。我们的训练目标整合了三个互补的组成部分：(1) 针对事实物体、属性和关系的实例感知场景图奖励；(2) 针对事件排序和重复的时间奖励；以及 (3) 用于分层自我验证的视频定位 VQA 奖励。在时间性、通用视频理解和幻觉导向的基准测试中，该目标在开源骨干模型上取得了持续的提升。这些结果表明，结构化奖励塑造是实现更忠实视频理解的一条切实可行的路径。

## Abstract
Multimodal large language models (MLLMs) have achieved remarkable progress in video understanding. However, seemingly plausible outputs often suffer from poor visual and temporal grounding: a model may fabricate object existence, assign incorrect attributes, or collapse repeated events while still producing a globally reasonable caption or answer. We study this failure mode through a compositional consistency audit that decomposes a caption into supporting factual and temporal claims, investigating whether a correct high-level prediction is actually backed by valid lower-level evidence. Our top-down audit reveals that even correct root relational claims often lack reliable attribute and existence support. This indicates that standard sentence-level supervision is a weak proxy for faithful video understanding. Furthermore, when turning to reinforcement learning (RL) for better alignment, standard sentence-level rewards often prove too coarse to accurately localize specific grounding failures. To address this, we replace generic sentence-level rewards with a structured reward built from factual and temporal units. Our training objective integrates three complementary components: (1) an instance-aware scene-graph reward for factual objects, attributes, and relations; (2) a temporal reward for event ordering and repetition; and (3) a video-grounded VQA reward for hierarchical self-verification. Across temporal, general video understanding, and hallucination-oriented benchmarks, this objective yields consistent gains on open-source backbones. These results suggest that structured reward shaping is a practical route to more faithful video understanding.

---

## 论文详细总结（自动生成）

### 论文总结：通过结构化奖励增强视频 MLLM 的一致性

#### 1. 核心问题与整体含义（研究动机和背景）
视频多模态大语言模型（Video MLLMs）虽然能生成流畅的描述，但常面临**视觉与时间定位（Grounding）不佳**的问题。论文通过“自顶向下组合一致性审计”发现：即使模型给出了正确的高层关系预测（如“狗叼着粉色飞盘”），其底层的支持性事实（如飞盘是否存在、颜色是否为粉色）往往是错误的。这表明模型在很大程度上是在“猜测”全局上下文，而非真正理解视频细节。传统的句子级奖励（Sentence-level Rewards）过于粗糙，无法纠正这些细粒度的幻觉。因此，研究的核心目标是开发一种能强制执行事实和时间一致性的**结构化奖励机制**。

#### 2. 方法论：核心思想与关键技术
论文提出了一种基于强化学习（RL）的框架，将生成的描述解析为结构化单元，并设计了三个互补的奖励分支：
*   **核心思想**：将文本描述分解为场景图（Scene-graph）和时间事件，通过细粒度的结构化反馈而非模糊的整句评分来优化模型。
*   **关键技术细节**：
    1.  **实例感知场景图奖励（Scene-graph Reward）**：利用 FACTUAL 解析器提取物体、属性和关系。引入“锚定层”为重复物体分配 ID（如 cup_1, cup_2），通过二分图匹配计算生成文本与参考文本之间的相似度，防止模型通过重复关键词来刷分。
    2.  **时间顺序奖励（Temporal Reward）**：针对事件的发生顺序和重复性进行验证。将时间主张转化为“Yes/No”问题（如“事件 A 是否在事件 B 之前？”），通过模型自身的验证能力提供反馈。
    3.  **分层 VQA 自验证奖励（VQA Reward）**：构建支持链（Support Chains），检查高层主张是否由底层证据（物体存在、属性对齐等）支撑。
*   **算法流程**：使用 REINFORCE 算法配合 KL 散度正则化，在采样描述上优化策略模型。

#### 3. 实验设计
*   **数据集与基准测试**：
    *   **时间理解**：TempCompass, TVBench。
    *   **通用视频理解**：Video-MME, MVBench。
    *   **幻觉评估**：VideoHallucer, EventHallusion。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Gemini 1.5 Pro。
    *   **开源模型**：LLaVA-OV-7B, Qwen2.5-VL-7B, Qwen3-VL (4B/8B/32B)。
    *   **RL/对齐基准**：Season, TPO, RRPO, DINO-HEAL, ArrowRL 等。
*   **训练设置**：在 TemporalBench 的 1,891 个样本上进行 SFT，随后进行 RL 训练。

#### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA H200 GPU**。
*   **技术细节**：采用 LoRA 效率微调（Rank=8）。
*   **时长/参数**：RL 阶段设定为 300 步，上下文长度 8192。具体的总训练时长未在文中明确标出，但从样本量和步数来看，属于计算高效型方案。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 4B 到 32B 不同规模的骨干模型，验证了方法的通用性。
*   **消融实验**：非常充分。作者分别对**奖励组件**（去掉 SG/Temp/VQA 分支）、**奖励权重**、**数据规模**（从 10% 到 150% 训练量）以及**帧采样策略**（8 到 64 帧）进行了详细测试。
*   **客观性**：通过对训练集和测试集的视频文件名进行去重审计，确认不存在数据泄露（Zero Overlap），保证了实验的公平性。

#### 6 主要结论与发现
*   **一致性提升**：自顶向下审计显示，模型在正确预测根关系时，其属性和存在性的条件准确率显著提高（ACA 提升 18.7%，ECA 提升 13.8%）。
*   **幻觉抑制**：该方法在幻觉导向的基准测试中提升最为显著，证明结构化奖励能有效减少虚构内容。
*   **性能增益**：在不牺牲通用视频 QA 能力的前提下，显著增强了模型的时间顺序推理能力。
*   **扩展性**：随着模型参数量和训练数据的增加，性能表现出持续的增益，且在增加输入帧数时，该方法的优势更加明显。

#### 7. 优点（亮点）
*   **细粒度监督**：解决了传统 RL 在视频任务中奖励信号过于稀疏和模糊的问题。
*   **实例感知**：通过锚定机制处理视频中常见的重复物体/事件，这是以往场景图方法较少触及的。
*   **即插即用**：作为一种后训练（Post-training）策略，能显著提升现有开源模型（如 Qwen3-VL）的忠实度。

#### 8. 不足与局限
*   **解析器依赖**：奖励质量高度依赖于预训练的场景图解析器（如 FACTUAL），如果解析器出错，奖励信号会受损。
*   **计算开销**：虽然比全量微调省力，但 RL 阶段需要多次采样和解析，推理开销高于纯 SFT。
*   **长视频限制**：实验主要集中在短视频和中等长度视频（采样帧数最高 64），对于超长视频的复杂逻辑一致性尚待进一步验证。

（完）
