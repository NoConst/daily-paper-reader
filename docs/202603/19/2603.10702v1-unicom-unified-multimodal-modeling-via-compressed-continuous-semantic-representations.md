---
title: "UniCom: Unified Multimodal Modeling via Compressed Continuous Semantic Representations"
title_zh: UniCom：通过压缩连续语义表示实现的统一多模态建模
authors: "Yaqi Zhao, Wang Lin, Zijian Zhang, Miles Yang, Jingyuan Chen, Wentao Zhang, Zhao Zhong, Liefeng Bo"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10702v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 统一多模态建模与表示融合
tldr: 针对统一多模态模型中离散化导致语义丢失及连续表示建模困难的问题，本文提出UniCom框架。该框架通过注意力机制的语义压缩器，将高维连续语义特征在通道维度进行压缩，并采用transfusion架构实现理解与生成的统一。UniCom在保持生成性能领先的同时，在图像编辑和一致性维护方面表现卓越，无需VAE即可实现高质量生成，为多模态统一建模提供了高效方案。
selection_source: fresh_fetch
motivation: 现有的离散化方案会丢失细粒度语义，而直接建模高维连续表示又面临训练不稳定和收敛慢的挑战。
method: 提出一种基于注意力机制的语义压缩器来降低通道维度，并结合transfusion架构来统一多模态的理解与生成任务。
result: UniCom在统一模型中达到了最先进的生成性能，并在图像编辑的可控性和图像一致性方面表现优异。
conclusion: 实验证明压缩通道维度的连续表示比空间下采样更有效，为高效且高质量的统一多模态建模提供了新路径。
---

## 摘要
当前的统一多模态模型通常依赖离散视觉分词器来弥合模态间隙。然而，离散化不可避免地会丢弃细粒度的语义信息，导致在视觉理解任务中表现欠佳。相反，直接对连续语义表示（如 CLIP、SigLIP）进行建模在高维生成建模中面临重大挑战，导致收敛缓慢和训练不稳定。为了解决这一困境，我们提出了 UniCom，这是一个通过压缩连续表示来协调多模态理解与生成的统一框架。我们通过实验证明，对于重建和生成任务，降低通道维度比空间下采样更为有效。因此，我们设计了一个基于注意力的语义压缩器，将密集特征蒸馏为紧凑的统一表示。此外，我们验证了 transfusion 架构在收敛性和一致性方面优于基于查询的设计。实验表明，UniCom 在统一模型中实现了最先进的生成性能。值得注意的是，通过保留丰富的语义先验，它在图像编辑中提供了卓越的可控性，并且即使在不依赖 VAE 的情况下也能保持图像一致性。

## Abstract
Current unified multimodal models typically rely on discrete visual tokenizers to bridge the modality gap. However, discretization inevitably discards fine-grained semantic information, leading to suboptimal performance in visual understanding tasks. Conversely, directly modeling continuous semantic representations (e.g., CLIP, SigLIP) poses significant challenges in high-dimensional generative modeling, resulting in slow convergence and training instability. To resolve this dilemma, we introduce UniCom, a unified framework that harmonizes multimodal understanding and generation via compressed continuous representation. We empirically demonstrate that reducing channel dimension is significantly more effective than spatial downsampling for both reconstruction and generation. Accordingly, we design an attention-based semantic compressor to distill dense features into a compact unified representation. Furthermore, we validate that the transfusion architecture surpasses query-based designs in convergence and consistency. Experiments demonstrate that UniCom achieves state-of-the-art generation performance among unified models. Notably, by preserving rich semantic priors, it delivers exceptional controllability in image editing and maintains image consistency even without relying on VAE.

---

## 论文详细总结（自动生成）

### 论文总结：UniCom —— 通过压缩连续语义表示实现的统一多模态建模

#### 1. 核心问题与整体含义（研究动机和背景）
当前的多模态统一模型（同时具备理解与生成能力）在视觉表示上面临“两难困境”：
*   **离散化方案（如 VQ-VAE）：** 虽然简化了生成建模，但离散化过程不可避免地丢失了细粒度的纹理和空间细节，限制了图像生成的质量。
*   **连续表示方案（如直接使用 CLIP/SigLIP 特征）：** 虽保留了丰富语义，但高维连续特征流形极其复杂且非平滑，导致生成模型（如扩散模型）收敛极慢且训练不稳定。
*   **研究动机：** 寻找一种既能保留细粒度语义，又具备生成易读性（Generative Tractability）的“统一 Token”。UniCom 旨在通过**压缩连续语义表示**来打破这一瓶颈。

#### 2. 论文提出的方法论
UniCom 提出了一个两阶段的生成框架，核心思想是将高维语义空间投影到一个紧凑且连续的潜在空间。

*   **核心思想：** 
    *   **维度压缩优于空间下采样：** 实验发现，降低特征的通道维度（Channel Dimension）比减少 Token 序列长度（Spatial Downsampling）更能保留图像的重建质量和生成效率。
    *   **语义压缩器（Semantic Compressor）：** 设计了一个轻量级的**基于多头注意力机制（MHA）**的压缩模块，而非简单的 MLP。MHA 能利用 Token 间的上下文关系，更好地保持语义结构。
*   **关键技术细节：**
    *   **架构选择：** 比较了 **Transfusion（路径 I）** 和 **Query-based（路径 II）** 两种架构。最终采用 Transfusion，即在单个 Transformer 中交织处理离散文本 Token 和连续图像潜在表示。
    *   **训练目标：** 结合了交叉熵损失（$L_{ce}$，用于文本）和流匹配损失（$L_{fm}$，用于图像生成）。
    *   **流程：** 先联合预训练压缩器和扩散解码器（重建任务），随后冻结压缩器，训练多模态大模型（MLLM）在压缩空间内进行条件采样。

#### 3. 实验设计
*   **数据集与场景：**
    *   **图像重建：** ImageNet 验证集。
    *   **文本生成图像（T2I）：** GenEval、DPG-Bench、WISE（世界知识信息语义评估）。
    *   **图像编辑：** ImgEdit-Bench、GEdit-Bench、KRIS-Bench（知识密集型）、WorldEdit。
    *   **视觉理解：** GQA、MMMU、ChartQA、OCRBench 等。
*   **Benchmark 与对比方法：**
    *   对比了生成专用模型（SD3, FLUX.1）和统一模型（Emu3, Show-o, Janus-Pro, OmniGen, Bagel 等）。
    *   在图像编辑方面，对比了 Instruct-Pix2Pix、MagicBrush 等专用编辑模型。

#### 4. 资源与算力
*   **模型规格：** 语言部分基于 **Qwen-2.5-7B-Instruct**，视觉编码器使用 **SigLIP2-SO400M**，扩散骨干网络初始化自 **FLUX.1-dev**。
*   **训练细节：** 
    *   全局 Batch Size 为 256。
    *   训练分为四个阶段：对齐（20K 步）、预训练（115K 步）、持续训练（60K 步）、指令微调（7K 步）。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号（如 H100/A100）和数量，但从模型规模（7B+400M+FLUX）和训练步数来看，属于大规模计算资源投入。

#### 5. 实验数量与充分性
*   **实验规模：** 论文进行了大量的定量分析，涵盖了从基础重建到复杂语义理解和生成的全维度评估。
*   **消融实验：** 非常充分。作者系统对比了：
    1.  不同压缩形状（$n \times d$ 的不同组合）；
    2.  不同投影架构（MLP vs. MHA）；
    3.  不同生成路径（Transfusion vs. Query-based）；
    4.  不同初始化策略（纯语言模型 vs. 预训练 VLM）。
*   **客观性：** 使用了多个第三方 Benchmark，并提供了可视化对比（t-SNE 分布图、重建细节放大图），实验设计较为严谨、公平。

#### 6. 论文的主要结论与发现
*   **压缩有效性：** 将特征维度从 1152 压缩到 64（18倍压缩）对重建精度的损失极小，但能使生成任务的收敛速度提升 **3.8x 到 5x**。
*   **架构优越性：** Transfusion 架构在处理图像编辑的一致性和收敛速度上显著优于基于 Query 的架构。
*   **语义保留：** 即使不依赖 VAE 的潜在特征，仅靠压缩后的语义表示，UniCom 也能在图像编辑中实现极高的身份一致性（Identity Preservation）。
*   **SOTA 表现：** 在统一模型中，UniCom 在 GenEval 和 WorldEdit 等多个生成与编辑榜单上达到了领先水平。

#### 7. 优点（亮点）
*   **高效统一：** 成功解决了连续表示难以训练的问题，通过通道压缩实现了高效的端到端统一建模。
*   **无需 VAE：** 证明了语义丰富的压缩表示可以作为理解与生成的通用接口，减少了对传统 VAE 的依赖。
*   **卓越的编辑能力：** 在处理需要世界知识的复杂编辑任务（如生物、地理变换）时表现出色。
*   **CoT 增强：** 引入了思维链（Chain-of-Thought）推理，提升了生成过程的可解释性和逻辑性。

#### 8. 不足与局限
*   **信息丢失风险：** 尽管通道压缩效果好，但在极端复杂的场景下，极高倍率的压缩仍可能导致微小纹理的丢失。
*   **计算成本：** 尽管收敛加快，但基于 7B 参数量和扩散 Transformer 的训练与推理成本依然较高。
*   **应用限制：** 目前主要集中在静态图像，尚未扩展到视频生成等更动态的领域。
*   **数据依赖：** 模型性能高度依赖于高质量的图文对和交织数据，对于低质量数据的鲁棒性有待进一步验证。

（完）
