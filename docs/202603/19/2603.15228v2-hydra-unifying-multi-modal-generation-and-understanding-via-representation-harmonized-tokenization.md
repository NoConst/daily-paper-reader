---
title: "HYDRA: Unifying Multi-modal Generation and Understanding via Representation-Harmonized Tokenization"
title_zh: HYDRA：通过表示协调的分词化统一多模态生成与理解
authors: "Xuerui Qiu, Yutao Cui, Guozhen Zhang, Junzhe Li, JiaKui Hu, Xiao Zhang, Yang Li, Songtao Liu, Miles Yang, Yu Shi, Zhao Zhong, Liefeng Bo"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15228v2"
tags: ["query:cir-ml"]
score: 9.0
evidence: 通过协调标记化统一多模态生成与理解
tldr: 针对统一多模态模型在视觉理解的抽象表征与生成的细节基元之间的鸿沟，本文提出HYDRA框架。其核心是HYDRA-TOK，一种基于纯ViT的表征协调分词器，通过从Gen-ViT到Sem-ViT的渐进式学习，并利用生成-语义瓶颈（GSB）平衡噪声过滤与语义恢复。HYDRA在单一参数空间内实现了感知与生成的深度融合，在视觉重建、图像生成及多项理解任务上均达到SOTA水平。
selection_source: fresh_fetch
motivation: 现有统一多模态模型难以调和理解所需的抽象表征与生成所需的细节基元，导致信息不连贯和优化冲突。
method: 引入HYDRA-TOK分词器，通过Gen-ViT捕获结构并演进为Sem-ViT，利用GSB瓶颈层在低维空间过滤噪声并恢复维度以增强语义理解。
result: HYDRA在视觉重建、GenEval生成测试及八项理解基准测试中均取得领先，理解性能平均提升了10个百分点。
conclusion: 该研究证明了通过表征协调的分词技术，可以在单一原生框架下高效统一视觉生成与理解，并刷新了多项性能基准。
---

## 摘要
统一多模态模型（Unified Multimodal Models）难以弥合视觉理解所需的抽象表示与生成所需的详细原语之间的根本鸿沟。现有方法通常通过采用解耦编码器、在 VAE 之上堆叠表示编码器或利用离散量化来进行折中。然而，这些方法往往会破坏信息连贯性并导致优化冲突。为此，我们基于视觉建模应从生成演进到理解的洞察，提出了 HYDRA-TOK，一种表示协调的纯 ViT。HYDRA-TOK 将标准骨干网络重构为一个渐进式学习器，从捕获结构保持原语的 Gen-ViT 过渡到用于语义编码的 Sem-ViT。关键在于，这种过渡由生成-语义瓶颈（GSB）介导，它将特征压缩到低维空间以过滤噪声从而实现鲁棒合成，随后恢复维度以赋能复杂的语义理解。在此基础上，我们提出了 HYDRA，这是一个在单一参数空间内集成感知与生成的原生统一框架。广泛的实验证明 HYDRA 达到了新的 SOTA 水平。它在视觉重建方面树立了基准（rFID 0.08），并在 GenEval (0.86)、DPG-Bench (86.4) 和 WISE (0.53) 上实现了顶尖的生成性能，同时在八个具有挑战性的理解基准测试中，比之前的原生 UMM 平均高出 10.0 分。

## Abstract
Unified Multimodal Models struggle to bridge the fundamental gap between the abstract representations needed for visual understanding and the detailed primitives required for generation. Existing approaches typically compromise by employing decoupled encoders, stacking representation encoder atop VAEs, or utilizing discrete quantization. However, these methods often disrupt information coherence and lead to optimization conflicts. To this end, we introduce HYDRA-TOK, a representation-harmonized pure ViT in the insight that visual modeling should evolve from generation to understanding. HYDRA-TOK reformulates the standard backbone into a progressive learner that transitions from a Gen-ViT, which captures structure-preserving primitives, to a Sem-ViT for semantic encoding. Crucially, this transition is mediated by a Generation-Semantic Bottleneck (GSB), which compresses features into a low-dimensional space to filter noise for robust synthesis, then restores dimensionality to empower complex semantic comprehension. Built upon this foundation, we present HYDRA, a native unified framework integrating perception and generation within a single parameter space. Extensive experiments establish HYDRA as a new state-of-the-art. It sets a benchmark in visual reconstruction (rFID 0.08) and achieves top-tier generation performance on GenEval (0.86), DPG-Bench (86.4), and WISE (0.53), while simultaneously outperforming previous native UMMs by an average of 10.0 points across eight challenging understanding benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《HYDRA: Unifying Multi-modal Generation and Understanding via Representation-Harmonized Tokenization》的结构化总结：

### 1. 核心问题与整体含义
论文针对**原生统一多模态模型（Native UMMs）**中的核心矛盾：**理解任务需要高层抽象语义，而生成任务需要精细的结构原语**。
现有方法（如解耦编码器、在VAE上堆叠编码器或离散量化）通常会导致信息流不连贯、输入表示不统一或学习过程不兼容（优化冲突）。HYDRA 旨在通过一种“表示协调”的策略，在单一参数空间内完美融合视觉理解与生成。

### 2. 方法论
论文提出了 **HYDRA-TOK**（分词器）和 **HYDRA**（统一框架）：
*   **HYDRA-TOK（纯 ViT 分词器）：** 采用渐进式学习设计，将视觉建模视为从生成到理解的演进。
    *   **Gen-ViT：** 提取保留结构的底层原语，为高保真合成奠定基础。
    *   **GSB（生成-语义瓶颈）：** 核心创新点。通过“压缩-重构”操作，将特征压缩到低维空间（如64维）以过滤冗余噪声（稳定生成），随后恢复维度以进行语义蒸馏（增强理解）。
    *   **Sem-ViT：** 将瓶颈层的特征映射到高维语义空间，实现深层抽象。
*   **HYDRA 统一框架：**
    *   **统一输入：** 将视觉信号处理为连续序列，与文本嵌入直接拼接。
    *   **双头解码机制：** 共享 LLM 主干，末端分支为**语言头**（自回归预测文本）和**视觉头**（基于整流流匹配 Rectified Flow Matching 生成图像）。
    *   **三阶段训练：** 包含表示对齐、全参数联合预训练、以及高质量指令微调。

### 3. 实验设计
*   **数据集：**
    *   **重建：** ImageNet-1K, MS-COCO。
    *   **理解：** MME, AI2D, OCRBench, MMBench, RWQA, SEED, MMMU, MMStar（共8个基准）。
    *   **生成：** GenEval, DPG-Bench, WISE。
*   **对比对象：**
    *   **理解专用：** Qwen2.5-VL, LLaVA-OV。
    *   **统一模型：** Janus-Pro, Show-o2, VILA-U, Ming-UniVision, BAGEL 等。
    *   **生成专用：** FLUX.1, SD3。

### 4. 资源与算力
*   **模型规模：** 评估了 1.5B 和 7B 两个版本（基于 Qwen2.5）。HYDRA-TOK 约为 300M 参数。
*   **算力说明：** 论文**未明确列出**具体的 GPU 型号（如 H100/A100）或总卡时。但提到了训练步数（如阶段一 150K 步，阶段二 100K 步）和全局 Batch Size（1024），以及使用了约 1 亿（100M）图像-文本对。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从底层重建到高层语义理解、再到复杂指令生成的全维度测试。
*   **消融实验：** 非常详尽，包括：
    *   瓶颈层通道数（C=64 是甜点位）。
    *   Gen-ViT 与 Sem-ViT 的层数分配（12+12 效果最好）。
    *   训练阶段的必要性验证。
    *   损失函数组件（重构、正则、蒸馏）的影响。
*   **客观性：** 实验在多个第三方公认基准上进行，对比了当前最先进的同类模型，结果具有较高的说服力。

### 6. 主要结论与发现
*   **互促效应：** 证明了理解与生成并非竞争关系。联合训练在后期能显著提升理解的精确度，并能稳定生成的潜空间，提高收敛效率。
*   **性能突破：** HYDRA 在理解任务上比之前的原生统一模型平均高出约 **10.0 分**；在视觉重建上达到了惊人的 **rFID 0.08**。
*   **OCR 优势：** 由于保持了不压缩的信息流，HYDRA 在 OCRBench 等细粒度任务上表现远超基于 VAE 的统一模型（如 57.7 vs 32.4）。

### 7. 优点
*   **架构优雅：** 摒弃了复杂的 VAE+编码器堆叠，采用纯 ViT 实现了真正的表示统一。
*   **GSB 创新：** 巧妙地利用信息瓶颈解决了生成（去噪）与理解（抽象）的表征冲突。
*   **全能表现：** 在不牺牲生成质量的前提下，大幅提升了统一模型的理解能力，打破了“拔河效应”。

### 8. 不足与局限
*   **规模限制：** 目前主要在 7B 规模验证，且分词器（300M）在处理极度复杂的细粒度细节时可能存在瓶颈。
*   **数据依赖：** 尽管表现优异，但相比于顶级闭源模型，其训练数据量（100M）仍有提升空间，可能限制了世界知识的吸收。
*   **推理开销：** 连续表征的序列长度可能带来一定的计算压力（虽然文中未详细讨论推理延迟）。

（完）
