---
title: The Missing Knowledge Layer in Cognitive Architectures for AI Agents
title_zh: AI 智能体认知架构中缺失的知识层
authors: Michaël Roynard
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11364v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: AI智能体架构中知识与记忆层的调研与建议
tldr: 针对当前AI智能体认知架构（如CoALA和JEPA）中缺失显式知识层的问题，本文指出将事实与经验混为一谈会导致持久化语义错误。研究者提出了一种包含知识、记忆、智慧和智能的四层架构模型，为每层定义了不同的持久化机制（如无限更替、遗忘衰减等）。通过Python和Rust实现验证了该架构的可行性，强调了在工程实现中区分这些层级对提升智能体性能的重要性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11364v1/fig-001.webp\", \"caption\": \"Table 1. Four-layer decomposition with persistence semantics. Each layer requires a different update mechanism. Treating any two identically produces the category error described in section 1.\", \"page\": 5, \"index\": 1, \"width\": 658, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2604.11364v1/fig-002.webp\", \"caption\": \"Fig. 1. Four-layer cognitive architecture. Solid arrows (top) denote working pipelines active during inference. Dashed arrows (bottom) denote offline consolidation pipelines. The layers are co-equal substrates with distinct persistence semantics, not a strict hierarchy.\", \"page\": 5, \"index\": 2, \"width\": 700, \"height\": 253}, {\"url\": \"assets/figures/arxiv/2604.11364v1/fig-003.webp\", \"caption\": \"Table 4. Gap analysis: how CoALA and JEPA handle each cognitive substrate versus the four-layer decomposition.\", \"page\": 14, \"index\": 3, \"width\": 562, \"height\": 336}]"
motivation: 现有的AI认知架构缺乏独立的知识层，导致系统错误地对事实应用遗忘机制或无法正确处理事实与经验的更新差异。
method: 提出一种由知识、记忆、智慧和智能组成的四层分解架构，并为每一层设计了互不相同的持久化语义（如无限更替、艾宾浩斯衰减等）。
result: 通过Python和Rust的配套实现证明了这种架构分离在工程上是可行的，并识别出当前主流框架在处理矛盾解决等方面的架构缺陷。
conclusion: AI智能体架构必须在工程实现中为不同类型的认知功能提供独立的持久化语义，而目前的框架均未满足这一需求。
---

## 摘要
AI 智能体领域最具影响力的两个认知架构框架 CoALA [21] 和 JEPA [12] 都缺乏一个具有独立持久化语义的显式知识层。这一缺失导致了范畴错误：系统对事实性陈述应用认知衰减，或者以相同的更新机制处理事实和经验。我们调研了现有存储系统的持久化语义，并确定了八个收敛点，从 Karpathy 的 LLM 知识库 [10] 到 BEAM 基准测试中接近零的矛盾解决得分 [22]，这些都指向了相关的架构缺陷。我们提出了一个四层分解模型（知识、记忆、智慧、智能），其中每一层都具有根本不同的持久化语义：分别是无限期更替、艾宾浩斯衰减、证据门控修正和瞬时推理。Python 和 Rust 的配套实现证明了这种架构分离是可行的。我们借用了认知科学的术语作为有用的类比（知识/记忆的区别呼应了 Tulving 的三分法），但我们的分层是基于持久化语义需求而建立的工程构件，而非基于神经架构。我们认为，这些区别要求在工程实现中采用不同的持久化语义，而目前尚无框架或系统能够提供这一点。

## Abstract
The two most influential cognitive architecture frameworks for AI agents, CoALA [21] and JEPA [12], both lack an explicit Knowledge layer with its own persistence semantics. This gap produces a category error: systems apply cognitive decay to factual claims, or treat facts and experiences with identical update mechanics. We survey persistence semantics across existing memory systems and identify eight convergence points, from Karpathy's LLM Knowledge Base [10] to the BEAM benchmark's near-zero contradiction-resolution scores [22], all pointing to related architectural gaps. We propose a four-layer decom position (Knowledge, Memory, Wisdom, Intelligence) where each layer has fundamentally different persistence semantics: indefinite supersession, Ebbinghaus decay, evidence-gated revision, and ephemeral inference respectively. Companion implementations in Python and Rust demonstrate the architectural separation is feasible. We borrow terminology from cognitive science as a useful analogy (the Knowledge/Memory distinction echoes Tulving's trichotomy), but our layers are engineering constructs justified by persistence-semantics requirements, not by neural architecture. We argue that these distinctions demand distinct persistence semantics in engineering implementations, and that no current framework or system provides this.

---

## 论文详细总结（自动生成）

这篇论文针对当前 AI 智能体认知架构中的核心缺陷提出了深刻的见解，并给出了工程化的解决方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 AI 智能体认知架构（如 CoALA 和 JEPA）存在“范畴错误”（Category Error），即**混淆了“知识”与“记忆”**。
*   **研究动机**：目前的系统通常对所有数据采用统一的存储和更新机制。这导致了两种极端：要么对永恒的事实应用“认知衰减”（如 NornicDB 让科学发现随时间淡化），要么对琐碎的经验永久保留（如 Hindsight 导致存储膨胀）。
*   **背景**：作者指出，尽管认知科学早已区分了语义记忆（事实）和情境记忆（经历），但当前的 AI 工程实现并未在持久化语义（Persistence Semantics）上做出区分。

### 2. 方法论：四层分解架构
论文提出将智能体的认知底座分解为四个具有不同持久化逻辑的层级：
*   **知识层 (Knowledge)**：存储“世界是什么”。
    *   **语义**：无限期保留，采用**更替（Supersession）**机制。新证据出现时标记旧证据为“被取代”而非删除，保留溯源信息。
*   **记忆层 (Memory)**：存储“发生了什么”。
    *   **语义**：遵循**艾宾浩斯遗忘曲线（Ebbinghaus Decay）**。采用双时态（Bi-temporal）事件溯源，区分“学习时间”和“真实有效时间”。
*   **智慧层 (Wisdom)**：存储“什么行之有效”（行为模式/准则）。
    *   **语义**：**证据门控修正（Evidence-gated Revision）**。只有当观察到的证据（如成功次数、无矛盾周期）超过特定阈值时，才更新或固化行为准则。
*   **智能层 (Intelligence)**：推理与规划能力。
    *   **语义**：**瞬时性（Ephemeral）**。仅存在于推理过程中，不留直接痕迹，其效果通过更新前三层来体现。

### 3. 实验设计
*   **数据集/场景**：使用了 **BEAM 基准测试**（Beyond A Million tokens）的 100K token 切片。
*   **对比方法**：
    *   **分类路由（Typed Routing）**：使用预言机（Oracle）将查询定向到专门的 `knowledge-base`（支持更替）或 `memory-engine`（支持双时态过滤）。
    *   **扁平基准（Flat Baseline）**：所有查询命中一个无差别的 FTS5 全文搜索存储库。
*   **评估指标**：重点测试了所有系统得分最低的两个类别：**矛盾解决（Contradiction Resolution）**和**时间推理（Temporal Reasoning）**。

### 4. 资源与算力
*   **模型使用**：实验使用了本地运行的 **Gemma 2 26B** 模型进行文本生成和评分。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该研究侧重于架构设计和推理侧的存储引擎，而非模型训练，因此算力消耗主要集中在推理和 1000 余项单元测试上。
*   **系统特性**：作者强调其实现的引擎具有极低的资源占用（内存 < 5MB），且核心操作（衰减计算、图遍历）不依赖 LLM 调用。

### 5. 实验数量与充分性
*   **实验规模**：进行了初步实验（Pilot Evaluation），样本量 $N=80$（涵盖 20 个对话）。
*   **充分性评价**：
    *   **优点**：实验针对性强，直接回应了架构设计的核心假设。
    *   **局限**：样本量较小，且在分类路由中使用了“预言机”（假设分类 100% 正确），这在实际应用中可能因分类器误差而导致性能下降。作者承认这只是初步验证，大规模验证留待未来工作。

### 6. 主要结论与发现
*   **架构分离有效性**：分类路由相比扁平存储，在整体准确率上提升了 **12.8%**。
*   **特定能力提升**：时间推理提升最显著（+15.0%），矛盾解决提升了 10.6%。
*   **瓶颈识别**：实验证明，当前 AI 智能体在长文本任务中的失败，往往不是因为检索质量差，而是因为**缺乏正确的持久化语义**（如无法识别事实的更替关系）。

### 7. 优点与亮点
*   **工程落地**：不仅提出了理论，还提供了 Python (`knowledge-base`) 和 Rust (`memory-engine`) 的开源实现，具有极高的实用价值。
*   **确定性设计**：核心存储逻辑（如遗忘计算、更替簿记）采用确定性算法而非概率性的 LLM 调用，保证了低延迟和可预测性。
*   **跨学科融合**：成功将 Tulving 的认知心理学理论转化为具体的软件工程需求。

### 8. 不足与局限
*   **路由依赖**：系统的有效性高度依赖于“如何准确判断查询属于哪一层”，如果分类错误，性能反而会下降（实验中启发式路由导致性能下降了 12.5%）。
*   **基准测试危机**：作者指出当前主流的记忆基准测试（如 LongMemEval）存在严重质量问题（如答案错误、上下文过短），这限制了对架构优越性的全面评估。
*   **智慧层界限模糊**：知识层与智慧层的界限在某些边界案例（如经验总结的事实）中仍存在重叠，需要更复杂的策略来处理。

（完）
