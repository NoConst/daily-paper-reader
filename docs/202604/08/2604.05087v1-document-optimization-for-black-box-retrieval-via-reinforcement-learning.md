---
title: Document Optimization for Black-Box Retrieval via Reinforcement Learning
title_zh: 通过强化学习进行黑盒检索的文档优化
authors: "Omri Uzan, Ron Polonsky, Douwe Kiela, Christopher Potts"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05087v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 黑盒密集检索的文档优化
tldr: 针对传统文档扩展技术在现代检索器中易引入噪声的问题，本文提出一种基于强化学习的文档优化方法。该方法利用GRPO算法，将检索器的排名提升作为奖励，微调语言模型将文档转换为更符合目标检索器查询分布的表示。该方法仅需黑盒访问检索器，在代码和视觉文档检索任务中显著提升了性能，甚至使小型模型超越了大型模型，展现了极高的实用价值。
selection_source: fresh_fetch
motivation: 传统的文档扩展技术在现代检索器中往往会引入噪声并降低性能，因此需要一种能更好对齐文档与查询分布的优化手段。
method: 采用GRPO强化学习算法，以检索器的排名改进作为奖励信号，微调语言模型对文档进行转换优化。
result: 在代码和视觉文档检索中，该方法显著提升了nDCG指标，使OpenAI的小型嵌入模型性能超越了其大型模型。
conclusion: 文档优化是一种通用的黑盒增强技术，能有效提升各类检索器的效率与准确性，且与模型微调具有良好的互补性。
---

## 摘要
文档扩展是提高检索质量的一种经典技术，其吸引力在于将计算转移到线下，从而避免了额外的查询时处理。然而，当应用于现代检索器时，研究表明它会降低性能，通常会引入噪声并模糊判别信号。我们将文档扩展重新定义为文档优化问题：通过微调语言模型或视觉语言模型，将文档转换为更符合目标检索器下预期查询分布的表示形式，并使用 GRPO 将检索器的排名提升作为奖励。该方法仅需要对检索排名进行黑盒访问，且适用于单向量、多向量和词法检索器。我们在代码检索和视觉文档检索（VDR）任务上评估了我们的方法。我们发现，学习到的文档转换带来了检索增益，并且在许多设置下使更小、更高效的检索器能够超越更大的检索器。例如，将文档优化应用于 OpenAI text-embedding-3-small 模型，使代码检索的 nDCG5 从 58.7 提升至 66.8，VDR 从 53.3 提升至 57.6，甚至略微超过了价格贵 6.5 倍的 OpenAI text-embedding-3-large 模型（代码检索为 66.3；VDR 为 57.0）。当检索器权重可访问时，文档优化通常与微调具有竞争力，并且在大多数设置下，两者的结合表现最佳，将 Jina-ColBERT-V2 在 VDR 上的表现从 55.8 提升至 63.3，在代码检索上从 48.6 提升至 61.8。

## Abstract
Document expansion is a classical technique for improving retrieval quality, and is attractive since it shifts computation offline, avoiding additional query-time processing. However, when applied to modern retrievers, it has been shown to degrade performance, often introducing noise that obfuscates the discriminative signal. We recast document expansion as a document optimization problem: a language model or a vision language model is fine-tuned to transform documents into representations that better align with the expected query distribution under a target retriever, using GRPO with the retriever's ranking improvements as rewards. This approach requires only black-box access to retrieval ranks, and is applicable across single-vector, multi-vector and lexical retrievers. We evaluate our approach on code retrieval and visual document retrieval (VDR) tasks. We find that learned document transformations yield retrieval gains and in many settings enable smaller, more efficient retrievers to outperform larger ones. For example, applying document optimization to OpenAI text-embedding-3-small model improves nDCG5 on code (58.7 to 66.8) and VDR (53.3 to 57.6), even slightly surpassing the 6.5X more expensive OpenAI text-embedding-3-large model (66.3 on code; 57.0 on VDR). When retriever weights are accessible, document optimization is often competitive with fine-tuning, and in most settings their combination performs best, improving Jina-ColBERT-V2 from 55.8 to 63.3 on VDR and from 48.6 to 61.8 on code retrieval.

---

## 论文详细总结（自动生成）

这篇论文提出了一种通过强化学习优化文档表示以提升黑盒检索性能的新框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的“文档扩展”（Document Expansion）技术通过增加关键词来解决词汇不匹配问题，但在现代神经检索器（如向量嵌入模型）中，盲目扩展往往会引入噪声，反而降低检索精度。
*   **核心问题**：如何在不改变检索器模型（黑盒访问）且不增加查询时（Query-time）计算开销的前提下，通过离线重写文档来对齐检索器的表示空间，从而提升检索质量。
*   **整体含义**：将文档扩展重新定义为“文档优化”问题，利用生成式大模型作为策略，通过强化学习学习如何将原始文档转化为更易于被特定检索器识别的“代理文档”。

### 2. 方法论
*   **核心思想**：使用指令微调的语言模型（LM）或视觉语言模型（VLM）作为策略网络（Policy），生成文档的重写版本。通过衡量重写文档对检索排名（nDCG）的贡献作为奖励信号，利用强化学习算法进行迭代优化。
*   **关键技术细节**：
    *   **策略模型**：初始化为一个预训练的生成模型（如 Qwen3-4B），输入原始文档和特定指令。
    *   **反事实检索奖励（Counterfactual Reward）**：这是本文的核心创新。奖励函数定义为：如果将索引中的原始文档替换为重写文档，目标查询的 nDCG@k 指标的变化量（$\Delta nDCG$）。该奖励同时考虑了正向增益（提升相关查询排名）和负向惩罚（抑制无关查询排名）。
    *   **GRPO 算法**：采用组相对策略优化（Group Relative Policy Optimization）。该算法通过在同组内多个采样样本间进行相对奖励比较，无需额外的价值函数（Value Function）网络，显著降低了训练资源消耗。
    *   **离线处理**：所有优化和重写均在索引阶段完成，推理时直接检索优化后的文档，保持了极高的线上效率。

### 3. 实验设计
*   **应用场景与数据集**：
    *   **视觉文档检索（VDR）**：使用 ViDoRe 基准测试，包含生物医学、经济、ESG 等领域的 PDF 页面图像。
    *   **代码检索**：使用 MTEB 中的 HumanEval、MBPP、DS1000 和 FreshStack 数据集。
*   **检索器（Benchmark）**：
    *   **黑盒模型**：OpenAI `text-embedding-3-small` 和 `large`。
    *   **开源模型**：Qwen3-Embedding (0.6B/4B)、Jina-ColBERT-V2（多向量模型）。
    *   **词法模型**：BM25。
*   **对比方法**：直接检索（原始文档）、零样本转换（Zero-shot）、检索器微调（Fine-tuning）、联合适配（检索器与策略模型同时训练）。

### 4. 资源与算力
*   **算力设备**：文中提到在单个 **NVIDIA H200 GPU** 上进行索引填充。
*   **效率表现**：使用 vLLM 推理框架，索引填充吞吐量达到每秒 4000 到 8000 个 token。
*   **训练配置**：每个任务运行 150 个 GRPO 步数，Batch Size 为 32，每组生成 8 个候选样本。
*   **模型规模**：策略模型使用 2B 到 4B 参数规模的小型模型，以平衡性能与计算成本。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 大类任务、8 个子数据集、6 种不同架构的检索器。
*   **消融实验**：对比了基于排名的奖励与基于相似度的奖励、不同的弱监督策略（硬负采样 vs 随机采样）以及负样本数量的影响。
*   **充分性评价**：实验设计非常充分且具有说服力。通过跨领域（代码与视觉）、跨模型架构（单向量、多向量、词法）的对比，证明了该方法的通用性。同时，通过与检索器微调的对比，展示了文档侧优化的独特价值。

### 6. 主要结论与发现
*   **性能飞跃**：文档优化显著提升了所有检索器的性能。例如，OpenAI `small` 模型在优化后甚至超过了比其贵 6.5 倍的 `large` 模型。
*   **词法检索的重生**：BM25 在代码检索上的 nDCG@5 从 15.6 暴涨至 46.6，证明了优化后的文档能极好地解决词汇不匹配问题。
*   **协同效应**：在开源模型中，文档优化与检索器微调结合（联合适配）效果最好，说明文档侧和模型侧的改进是互补的。
*   **非简单扩展**：分析显示，优化后的文档并非简单的长度增加，而是有选择性地保留和重构了关键信息。

### 7. 优点
*   **黑盒适用性**：不需要检索器的梯度或内部权重，非常适合优化商业 API（如 OpenAI, Cohere）。
*   **零推理延迟**：所有计算开销都在离线阶段，不影响在线检索速度。
*   **直接目标优化**：直接针对排名指标（nDCG）进行优化，比传统的相似度损失函数更贴近业务目标。
*   **灵活性**：不仅适用于文本，也适用于图像到文本的跨模态转换优化。

### 8. 不足与局限
*   **依赖标注数据**：训练策略模型需要一定量的查询-文档对（虽然文中证明了弱监督也有效，但高质量标注仍是核心）。
*   **索引刷新成本**：在训练过程中需要定期刷新整个文档库的索引以计算奖励，对于超大规模数据集（如亿级文档）可能存在计算瓶颈。
*   **领域局限性**：策略模型在某一领域（如代码）学到的优化逻辑可能无法直接迁移到另一领域（如法律），需要针对性训练。

（完）
