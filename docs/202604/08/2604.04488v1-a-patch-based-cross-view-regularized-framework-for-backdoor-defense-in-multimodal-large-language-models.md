---
title: A Patch-based Cross-view Regularized Framework for Backdoor Defense in Multimodal Large Language Models
title_zh: 多模态大语言模型中基于 Patch 的跨视图正则化后门防御框架
authors: "Tianmeng Fang, Yong Wang, Zetai Kong, Zengzhen Su, Jun Wang, Chengjin Yu, Wei Wang"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04488v1"
tags: ["query:cir-ml"]
score: 9.0
evidence: 专注于多模态大语言模型（MLLM）的安全与防御机制
tldr: 针对多模态大语言模型在微调阶段易受后门攻击的问题，本文提出一种基于补丁增强和跨视图正则化的防御框架。该框架结合补丁级数据增强与输出差异正则化，利用后门响应对非语义扰动异常不变的特性，拉开原始与扰动视图的输出分布，同时引入熵约束以保持正常生成能力。实验证明该方法在多种模型和攻击场景下均能显著降低攻击成功率并维持模型性能。
selection_source: fresh_fetch
motivation: 现有多模态大模型在微调时极易被植入后门，且在抑制攻击与保持正常生成能力之间存在难以平衡的矛盾。
method: 提出一种结合补丁级数据增强、跨视图输出差异正则化以及输出熵约束的统一防御框架。
result: 在三种模型、两类任务和六种攻击方式上的实验表明，该方法有效降低了攻击成功率并保留了高质量的文本生成能力。
conclusion: 该研究为多模态大模型在低频中毒和隐蔽触发场景下的安全部署提供了一种有效的防御方案。
---

## 摘要
多模态大语言模型已成为统一处理视觉和语言任务的重要基础设施。然而，此类模型在有监督微调过程中极易受到后门植入的影响，一旦特定触发模式被激活，就会稳定地输出攻击者预定义的有害响应。后门防御的核心挑战在于低中毒率下抑制攻击成功率，同时保持模型的正常生成能力。这两个目标本质上是冲突的：强力抑制往往会降低良性性能，而弱正则化则无法缓解后门行为。为此，我们提出了一种基于 patch 增强和跨视图正则性的统一防御框架，该框架从特征表示和输出分布两个层面同时约束模型对触发模式的异常行为。具体而言，我们将 patch 级数据增强与跨视图输出差异正则化相结合，利用后门响应对非语义扰动具有异常不变性这一事实，主动拉开原始视图和扰动视图的输出分布，从而显著抑制后门触发的成功率。同时，我们通过施加输出熵约束来避免防御过程中对模型的过度抑制，确保正常指令生成的质量。在三个模型、两个任务和六种攻击上的实验结果表明，我们提出的防御方法在保持高水平正常文本生成能力的同时，有效地降低了攻击成功率。我们的工作使得大规模多模态模型能够在现实的低频中毒和隐蔽触发场景中实现安全、可控的部署。

## Abstract
Multimodal large language models have become an important infrastructure for unified processing of visual and linguistic tasks. However, such models are highly susceptible to backdoor implantation during supervised fine-tuning and will steadily output the attacker's predefined harmful responses once a specific trigger pattern is activated. The core challenge of backdoor defense lies in suppressing attack success under low poisoning ratios while preserving the model's normal generation ability. These two objectives are inherently conflicting. Strong suppression often degrades benign performance, whereas weak regularization fails to mitigate backdoor behaviors. To this end, we propose a unified defense framework based on patch augmentation and cross-view regularity, which simultaneously constrains the model's anomalous behaviors in response to triggered patterns from both the feature representation and output distribution levels. Specifically, patch-level data augmentation is combined with cross-view output difference regularization to exploit the fact that backdoor responses are abnormally invariant to non-semantic perturbations and to proactively pull apart the output distributions of the original and perturbed views, thereby significantly suppressing the success rate of backdoor triggering. At the same time, we avoid over-suppression of the model during defense by imposing output entropy constraints, ensuring the quality of normal command generation. Experimental results across three models, two tasks, and six attacks show that our proposed defense method effectively reduces the attack success rate while maintaining a high level of normal text generation capability. Our work enables the secure, controlled deployment of large-scale multimodal models in realistic low-frequency poisoning and covert triggering scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Patch-based Cross-view Regularized Framework for Backdoor Defense in Multimodal Large Language Models》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：多模态大语言模型（MLLM）在有监督微调（SFT）阶段极易受到后门攻击。攻击者只需注入极少量（如5%）的中毒样本，即可使模型在遇到特定视觉触发器（Trigger）时输出预定义的有害响应。
*   **核心挑战**：现有的防御方法面临“安全-可用性”矛盾。强力抑制后门往往会损害模型在正常指令下的生成质量；此外，低中毒率导致的稀疏信号使得依赖统计特征的传统防御难以奏效。
*   **整体含义**：本文旨在开发一种无需先验攻击知识、能在低中毒率下有效抑制后门，同时保持模型多模态理解能力的统一防御框架。

### 2. 论文提出的方法论
该框架基于**补丁增强（Patch Augmentation）**和**跨视图正则化（Cross-view Regularity）**，从特征和输出两个层面进行约束：
*   **补丁级双视图生成**：对每个输入图像生成原始视图 $x$ 和带有局部补丁扰动（遮盖、噪声或洗牌）的扰动视图 $\tilde{x}$。两者全局语义一致，但局部视觉特征不同。
*   **特征一致性正则化 ($L_{patch}$)**：约束模型中间层的多模态特征表示，要求原始视图和扰动视图的特征尽可能接近，防止模型过度拟合局部触发器模式。
*   **跨视图输出差异优化 ($L_{cv-dis}$)**：**这是核心创新点**。作者发现后门响应对非语义扰动具有“异常不变性”。防御时主动拉开两个视图在输出层 Token 概率分布的距离（降低余弦相似度），从而打破触发器与目标输出之间的强绑定。
*   **不确定性感知熵约束 ($L_{ent}$)**：为了防止强制拉开分布导致模型输出崩溃或过度自信，引入信息熵下限约束，确保模型在正常生成时保持分布的丰富性和多样性。
*   **总损失函数**：$L_{def} = L_{task} + \lambda_1 L_{patch} + \lambda_2 L_{cv-dis} + \lambda_3 L_{ent}$。

### 3. 实验设计
*   **数据集**：使用 **MS COCO** 和 **Flickr30k** 作为清洁测试集；使用 **LADD** 作为中毒训练集（中毒率设为 5%）。
*   **受害模型（Victim Models）**：涵盖了三种主流架构：**OpenFlamingo**（冻结语言模型类）、**Otter**（指令微调类）、**BLIP-2**（桥接架构类）。
*   **攻击场景**：对比了 6 种代表性后门攻击：BadNets、Blended、LowFrequency、WaNet、InputAware、DualKey。
*   **基准方法（Baselines）**：对比了 Fine-Pruning（微调剪枝）、STRIP（输入扰动检测）、Entropy Filtering（熵过滤）等传统防御手段。
*   **评价指标**：使用 BLEU@4 和 CIDEr 衡量正常生成能力，使用攻击成功率（ASR）衡量安全性。

### 4. 资源与算力
*   **算力开销**：论文提到由于需要同时处理原始和扰动两个视图，训练时的前向传播次数增加了一倍，导致**训练时间大约是标准 SFT 的两倍**。显存占用也因存储双视图的中间激活值而适度增加。
*   **硬件细节**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **推理效率**：强调该方法仅在训练阶段生效，**推理阶段不增加任何额外开销**。

### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个模型、2 个数据集和 6 种攻击方式上进行了交叉验证，实验矩阵较为完整。
*   **消融实验**：专门针对三个损失项（$L_{patch}, L_{cv-dis}, L_{ent}$）进行了消融研究，验证了各组件的协同作用。
*   **超参数分析**：对三个正则化权重 $\lambda$ 进行了敏感性分析，展示了防御效果与性能之间的平衡曲线。
*   **客观性**：实验涵盖了从简单像素触发到复杂自适应触发的多种攻击，对比了推理端和训练端的防御效果，评价较为客观、公平。

### 6. 论文的主要结论与发现
*   **防御有效性**：该方法能将多种攻击的 ASR 从接近 100% 降低到 30%-50% 左右，显著优于 Fine-Pruning 等基准方法。
*   **性能保持**：在大幅降低 ASR 的同时，模型在 COCO 和 Flickr30k 上的 BLEU 和 CIDEr 指标仅有轻微下降，成功缓解了安全与可用性的冲突。
*   **机制发现**：跨视图输出差异优化是打破后门绑定的关键，而熵约束对于维持生成质量至关重要。

### 7. 优点
*   **原理创新**：利用后门响应的“异常不变性”作为防御信号，从几何结构层面切断攻击路径。
*   **通用性强**：不依赖于特定的触发器形状或位置，对全局扰动（WaNet）和自适应攻击（InputAware）同样有效。
*   **部署友好**：推理零成本，适合对实时性要求高的实际部署场景。

### 8. 不足与局限
*   **训练成本**：训练时间翻倍可能限制其在超大规模数据集或算力受限环境下的应用。
*   **攻击覆盖**：核心扰动主要针对局部视觉区域，对于复杂的跨模态协同触发（如文本和图像同时存在触发逻辑）的防御效果仍需进一步验证。
*   **参数敏感性**：虽然实验显示了一定的鲁棒性，但在不同任务和模型间切换时，仍可能需要精细调整正则化权重 $\lambda$。
*   **应用阶段限制**：主要针对微调阶段，无法直接应用于已经训练完成并部署的“黑盒”模型。

（完）
